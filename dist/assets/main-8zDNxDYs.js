const __vite__mapDeps=(i,m=__vite__mapDeps,d=(m.f||(m.f=["assets/toolbar-collapsed-storage-YH65vEW9.js","assets/table-configuration-t-aKc2tY.js","assets/event-B_hCB_W5.js","assets/webviewWindow-ZJkDY8Dw.js","assets/window-D2U7H-xd.js","assets/loaded-file-storage-DhVIUlJT.js","assets/optimizer-wasm-bridge-muCRhr2Y.js","assets/mtf-plot-TU6szzT9.js","assets/psf-calculator-CvRmJnrY.js"])))=>i.map(i=>d[i]);
import{Q as as,V as ft,R as zt,T as Bt,S as Lo,U as Bo,W as nt,X as cs,Y as ls,Z as us,$ as Xe,a0 as ps,a1 as Hr,a2 as ds,a3 as ms,a4 as fs,a5 as gs,a6 as ys,a7 as ro,a8 as sn,a9 as an,_ as Re,aa as je,ab as Ke,ac as rt,h as On,ad as hs,ae as bs,t as ws,r as Ut,i as ze,af as vs,ag as io,ah as Ht,ai as Fn,aj as yn,ak as Ss,al as ho,am as Gr,an as Vr,ao as Dn,ap as xs,q as As,m as Ns,n as _s,aq as jn,ar as zo,as as Rs,at as qr,au as Zr,av as Yr,aw as cn,ax as Xr,ay as Cs,N as Ln,P as bo,az as qt,aA as Kr,f as Wo,D as Jr,A as Is,B as Fs,L as Qr,M as Ft,O as ei,aB as Ms,aC as ks,l as ti,aD as Ps,aE as Es,g as Ts,s as Os,aF as it,aG as $o,aH as at,aI as wo,aJ as Uo,aK as gt,aL as yt,aM as ni,aN as Ds,aO as js,aP as Gt,aQ as vo,aR as Pt,aS as Zt,aT as xt,aU as so,aV as Ls,aW as Bn,aX as Bs,aY as zs,aZ as Ws,a_ as $s,a$ as Ho,b0 as Go,b1 as Vo,b2 as Us,b3 as qo,b4 as oi,b5 as Rn,b6 as qn,b7 as Zo,b8 as Hs,b9 as Gs,ba as Vs,bb as qs,bc as Zs,bd as Ys,be as Xs,bf as Ks,bg as Js,bh as Qs,bi as ea,bj as ta,bk as na,bl as oa,bm as ra,bn as ia,bo as ri,bp as ii,bq as si,br as sa,bs as ao,k as ai,bt as ci,bu as So,j as li,bv as ui,bw as xo,bx as aa,by as ca,bz as la,J as ua,K as pa,bA as da,z as ma}from"./toolbar-collapsed-storage-YH65vEW9.js";import{a as fa,b as Ao,l as ot,e as $e,s as No,d as pi,B as pn,f as ga,c as ya,v as di,h as Yo,g as ha,i as Xo,j as mi,k as Ko,m as ba}from"./table-configuration-t-aKc2tY.js";import{s as Zn,a as wa,b as Jo,c as va}from"./mtf-plot-TU6szzT9.js";import{PSFCalculator as Sa}from"./psf-calculator-CvRmJnrY.js";import{listen as zn}from"./event-B_hCB_W5.js";const Qo={type:"change"},_o={type:"start"},fi={type:"end"},hn=new cs,er=new ls,xa=Math.cos(70*us.DEG2RAD),Ze=new ft,st=2*Math.PI,Ee={NONE:-1,ROTATE:0,DOLLY:1,PAN:2,TOUCH_ROTATE:3,TOUCH_PAN:4,TOUCH_DOLLY_PAN:5,TOUCH_DOLLY_ROTATE:6},Yn=1e-6;class gi extends as{constructor(t,n=null){super(t,n),this.state=Ee.NONE,this.target=new ft,this.cursor=new ft,this.minDistance=0,this.maxDistance=1/0,this.minZoom=0,this.maxZoom=1/0,this.minTargetRadius=0,this.maxTargetRadius=1/0,this.minPolarAngle=0,this.maxPolarAngle=Math.PI,this.minAzimuthAngle=-1/0,this.maxAzimuthAngle=1/0,this.enableDamping=!1,this.dampingFactor=.05,this.enableZoom=!0,this.zoomSpeed=1,this.enableRotate=!0,this.rotateSpeed=1,this.keyRotateSpeed=1,this.enablePan=!0,this.panSpeed=1,this.screenSpacePanning=!0,this.keyPanSpeed=7,this.zoomToCursor=!1,this.autoRotate=!1,this.autoRotateSpeed=2,this.keys={LEFT:"ArrowLeft",UP:"ArrowUp",RIGHT:"ArrowRight",BOTTOM:"ArrowDown"},this.mouseButtons={LEFT:zt.ROTATE,MIDDLE:zt.DOLLY,RIGHT:zt.PAN},this.touches={ONE:Bt.ROTATE,TWO:Bt.DOLLY_PAN},this.target0=this.target.clone(),this.position0=this.object.position.clone(),this.zoom0=this.object.zoom,this._domElementKeyEvents=null,this._lastPosition=new ft,this._lastQuaternion=new Lo,this._lastTargetPosition=new ft,this._quat=new Lo().setFromUnitVectors(t.up,new ft(0,1,0)),this._quatInverse=this._quat.clone().invert(),this._spherical=new Bo,this._sphericalDelta=new Bo,this._scale=1,this._panOffset=new ft,this._rotateStart=new nt,this._rotateEnd=new nt,this._rotateDelta=new nt,this._panStart=new nt,this._panEnd=new nt,this._panDelta=new nt,this._dollyStart=new nt,this._dollyEnd=new nt,this._dollyDelta=new nt,this._dollyDirection=new ft,this._mouse=new nt,this._performCursorZoom=!1,this._pointers=[],this._pointerPositions={},this._controlActive=!1,this._onPointerMove=Na.bind(this),this._onPointerDown=Aa.bind(this),this._onPointerUp=_a.bind(this),this._onContextMenu=Pa.bind(this),this._onMouseWheel=Ia.bind(this),this._onKeyDown=Fa.bind(this),this._onTouchStart=Ma.bind(this),this._onTouchMove=ka.bind(this),this._onMouseDown=Ra.bind(this),this._onMouseMove=Ca.bind(this),this._interceptControlDown=Ea.bind(this),this._interceptControlUp=Ta.bind(this),this.domElement!==null&&this.connect(this.domElement),this.update()}connect(t){super.connect(t),this.domElement.addEventListener("pointerdown",this._onPointerDown),this.domElement.addEventListener("pointercancel",this._onPointerUp),this.domElement.addEventListener("contextmenu",this._onContextMenu),this.domElement.addEventListener("wheel",this._onMouseWheel,{passive:!1}),this.domElement.getRootNode().addEventListener("keydown",this._interceptControlDown,{passive:!0,capture:!0}),this.domElement.style.touchAction="none"}disconnect(){this.domElement.removeEventListener("pointerdown",this._onPointerDown),this.domElement.ownerDocument.removeEventListener("pointermove",this._onPointerMove),this.domElement.ownerDocument.removeEventListener("pointerup",this._onPointerUp),this.domElement.removeEventListener("pointercancel",this._onPointerUp),this.domElement.removeEventListener("wheel",this._onMouseWheel),this.domElement.removeEventListener("contextmenu",this._onContextMenu),this.stopListenToKeyEvents(),this.domElement.getRootNode().removeEventListener("keydown",this._interceptControlDown,{capture:!0}),this.domElement.style.touchAction="auto"}dispose(){this.disconnect()}getPolarAngle(){return this._spherical.phi}getAzimuthalAngle(){return this._spherical.theta}getDistance(){return this.object.position.distanceTo(this.target)}listenToKeyEvents(t){t.addEventListener("keydown",this._onKeyDown),this._domElementKeyEvents=t}stopListenToKeyEvents(){this._domElementKeyEvents!==null&&(this._domElementKeyEvents.removeEventListener("keydown",this._onKeyDown),this._domElementKeyEvents=null)}saveState(){this.target0.copy(this.target),this.position0.copy(this.object.position),this.zoom0=this.object.zoom}reset(){this.target.copy(this.target0),this.object.position.copy(this.position0),this.object.zoom=this.zoom0,this.object.updateProjectionMatrix(),this.dispatchEvent(Qo),this.update(),this.state=Ee.NONE}update(t=null){const n=this.object.position;Ze.copy(n).sub(this.target),Ze.applyQuaternion(this._quat),this._spherical.setFromVector3(Ze),this.autoRotate&&this.state===Ee.NONE&&this._rotateLeft(this._getAutoRotationAngle(t)),this.enableDamping?(this._spherical.theta+=this._sphericalDelta.theta*this.dampingFactor,this._spherical.phi+=this._sphericalDelta.phi*this.dampingFactor):(this._spherical.theta+=this._sphericalDelta.theta,this._spherical.phi+=this._sphericalDelta.phi);let o=this.minAzimuthAngle,r=this.maxAzimuthAngle;isFinite(o)&&isFinite(r)&&(o<-Math.PI?o+=st:o>Math.PI&&(o-=st),r<-Math.PI?r+=st:r>Math.PI&&(r-=st),o<=r?this._spherical.theta=Math.max(o,Math.min(r,this._spherical.theta)):this._spherical.theta=this._spherical.theta>(o+r)/2?Math.max(o,this._spherical.theta):Math.min(r,this._spherical.theta)),this._spherical.phi=Math.max(this.minPolarAngle,Math.min(this.maxPolarAngle,this._spherical.phi)),this._spherical.makeSafe(),this.enableDamping===!0?this.target.addScaledVector(this._panOffset,this.dampingFactor):this.target.add(this._panOffset),this.target.sub(this.cursor),this.target.clampLength(this.minTargetRadius,this.maxTargetRadius),this.target.add(this.cursor);let s=!1;if(this.zoomToCursor&&this._performCursorZoom||this.object.isOrthographicCamera)this._spherical.radius=this._clampDistance(this._spherical.radius);else{const a=this._spherical.radius;this._spherical.radius=this._clampDistance(this._spherical.radius*this._scale),s=a!=this._spherical.radius}if(Ze.setFromSpherical(this._spherical),Ze.applyQuaternion(this._quatInverse),n.copy(this.target).add(Ze),this.object.lookAt(this.target),this.enableDamping===!0?(this._sphericalDelta.theta*=1-this.dampingFactor,this._sphericalDelta.phi*=1-this.dampingFactor,this._panOffset.multiplyScalar(1-this.dampingFactor)):(this._sphericalDelta.set(0,0,0),this._panOffset.set(0,0,0)),this.zoomToCursor&&this._performCursorZoom){let a=null;if(this.object.isPerspectiveCamera){const c=Ze.length();a=this._clampDistance(c*this._scale);const i=c-a;this.object.position.addScaledVector(this._dollyDirection,i),this.object.updateMatrixWorld(),s=!!i}else if(this.object.isOrthographicCamera){const c=new ft(this._mouse.x,this._mouse.y,0);c.unproject(this.object);const i=this.object.zoom;this.object.zoom=Math.max(this.minZoom,Math.min(this.maxZoom,this.object.zoom/this._scale)),this.object.updateProjectionMatrix(),s=i!==this.object.zoom;const l=new ft(this._mouse.x,this._mouse.y,0);l.unproject(this.object),this.object.position.sub(l).add(c),this.object.updateMatrixWorld(),a=Ze.length()}else console.warn("WARNING: OrbitControls.js encountered an unknown camera type - zoom to cursor disabled."),this.zoomToCursor=!1;a!==null&&(this.screenSpacePanning?this.target.set(0,0,-1).transformDirection(this.object.matrix).multiplyScalar(a).add(this.object.position):(hn.origin.copy(this.object.position),hn.direction.set(0,0,-1).transformDirection(this.object.matrix),Math.abs(this.object.up.dot(hn.direction))<xa?this.object.lookAt(this.target):(er.setFromNormalAndCoplanarPoint(this.object.up,this.target),hn.intersectPlane(er,this.target))))}else if(this.object.isOrthographicCamera){const a=this.object.zoom;this.object.zoom=Math.max(this.minZoom,Math.min(this.maxZoom,this.object.zoom/this._scale)),a!==this.object.zoom&&(this.object.updateProjectionMatrix(),s=!0)}return this._scale=1,this._performCursorZoom=!1,s||this._lastPosition.distanceToSquared(this.object.position)>Yn||8*(1-this._lastQuaternion.dot(this.object.quaternion))>Yn||this._lastTargetPosition.distanceToSquared(this.target)>Yn?(this.dispatchEvent(Qo),this._lastPosition.copy(this.object.position),this._lastQuaternion.copy(this.object.quaternion),this._lastTargetPosition.copy(this.target),!0):!1}_getAutoRotationAngle(t){return t!==null?st/60*this.autoRotateSpeed*t:st/60/60*this.autoRotateSpeed}_getZoomScale(t){const n=Math.abs(t*.01);return Math.pow(.95,this.zoomSpeed*n)}_rotateLeft(t){this._sphericalDelta.theta-=t}_rotateUp(t){this._sphericalDelta.phi-=t}_panLeft(t,n){Ze.setFromMatrixColumn(n,0),Ze.multiplyScalar(-t),this._panOffset.add(Ze)}_panUp(t,n){this.screenSpacePanning===!0?Ze.setFromMatrixColumn(n,1):(Ze.setFromMatrixColumn(n,0),Ze.crossVectors(this.object.up,Ze)),Ze.multiplyScalar(t),this._panOffset.add(Ze)}_pan(t,n){const o=this.domElement;if(this.object.isPerspectiveCamera){const r=this.object.position;Ze.copy(r).sub(this.target);let s=Ze.length();s*=Math.tan(this.object.fov/2*Math.PI/180),this._panLeft(2*t*s/o.clientHeight,this.object.matrix),this._panUp(2*n*s/o.clientHeight,this.object.matrix)}else this.object.isOrthographicCamera?(this._panLeft(t*(this.object.right-this.object.left)/this.object.zoom/o.clientWidth,this.object.matrix),this._panUp(n*(this.object.top-this.object.bottom)/this.object.zoom/o.clientHeight,this.object.matrix)):(console.warn("WARNING: OrbitControls.js encountered an unknown camera type - pan disabled."),this.enablePan=!1)}_dollyOut(t){this.object.isPerspectiveCamera||this.object.isOrthographicCamera?this._scale/=t:(console.warn("WARNING: OrbitControls.js encountered an unknown camera type - dolly/zoom disabled."),this.enableZoom=!1)}_dollyIn(t){this.object.isPerspectiveCamera||this.object.isOrthographicCamera?this._scale*=t:(console.warn("WARNING: OrbitControls.js encountered an unknown camera type - dolly/zoom disabled."),this.enableZoom=!1)}_updateZoomParameters(t,n){if(!this.zoomToCursor)return;this._performCursorZoom=!0;const o=this.domElement.getBoundingClientRect(),r=t-o.left,s=n-o.top,a=o.width,c=o.height;this._mouse.x=r/a*2-1,this._mouse.y=-(s/c)*2+1,this._dollyDirection.set(this._mouse.x,this._mouse.y,1).unproject(this.object).sub(this.object.position).normalize()}_clampDistance(t){return Math.max(this.minDistance,Math.min(this.maxDistance,t))}_handleMouseDownRotate(t){this._rotateStart.set(t.clientX,t.clientY)}_handleMouseDownDolly(t){this._updateZoomParameters(t.clientX,t.clientX),this._dollyStart.set(t.clientX,t.clientY)}_handleMouseDownPan(t){this._panStart.set(t.clientX,t.clientY)}_handleMouseMoveRotate(t){this._rotateEnd.set(t.clientX,t.clientY),this._rotateDelta.subVectors(this._rotateEnd,this._rotateStart).multiplyScalar(this.rotateSpeed);const n=this.domElement;this._rotateLeft(st*this._rotateDelta.x/n.clientHeight),this._rotateUp(st*this._rotateDelta.y/n.clientHeight),this._rotateStart.copy(this._rotateEnd),this.update()}_handleMouseMoveDolly(t){this._dollyEnd.set(t.clientX,t.clientY),this._dollyDelta.subVectors(this._dollyEnd,this._dollyStart),this._dollyDelta.y>0?this._dollyOut(this._getZoomScale(this._dollyDelta.y)):this._dollyDelta.y<0&&this._dollyIn(this._getZoomScale(this._dollyDelta.y)),this._dollyStart.copy(this._dollyEnd),this.update()}_handleMouseMovePan(t){this._panEnd.set(t.clientX,t.clientY),this._panDelta.subVectors(this._panEnd,this._panStart).multiplyScalar(this.panSpeed),this._pan(this._panDelta.x,this._panDelta.y),this._panStart.copy(this._panEnd),this.update()}_handleMouseWheel(t){this._updateZoomParameters(t.clientX,t.clientY),t.deltaY<0?this._dollyIn(this._getZoomScale(t.deltaY)):t.deltaY>0&&this._dollyOut(this._getZoomScale(t.deltaY)),this.update()}_handleKeyDown(t){let n=!1;switch(t.code){case this.keys.UP:t.ctrlKey||t.metaKey||t.shiftKey?this.enableRotate&&this._rotateUp(st*this.keyRotateSpeed/this.domElement.clientHeight):this.enablePan&&this._pan(0,this.keyPanSpeed),n=!0;break;case this.keys.BOTTOM:t.ctrlKey||t.metaKey||t.shiftKey?this.enableRotate&&this._rotateUp(-st*this.keyRotateSpeed/this.domElement.clientHeight):this.enablePan&&this._pan(0,-this.keyPanSpeed),n=!0;break;case this.keys.LEFT:t.ctrlKey||t.metaKey||t.shiftKey?this.enableRotate&&this._rotateLeft(st*this.keyRotateSpeed/this.domElement.clientHeight):this.enablePan&&this._pan(this.keyPanSpeed,0),n=!0;break;case this.keys.RIGHT:t.ctrlKey||t.metaKey||t.shiftKey?this.enableRotate&&this._rotateLeft(-st*this.keyRotateSpeed/this.domElement.clientHeight):this.enablePan&&this._pan(-this.keyPanSpeed,0),n=!0;break}n&&(t.preventDefault(),this.update())}_handleTouchStartRotate(t){if(this._pointers.length===1)this._rotateStart.set(t.pageX,t.pageY);else{const n=this._getSecondPointerPosition(t),o=.5*(t.pageX+n.x),r=.5*(t.pageY+n.y);this._rotateStart.set(o,r)}}_handleTouchStartPan(t){if(this._pointers.length===1)this._panStart.set(t.pageX,t.pageY);else{const n=this._getSecondPointerPosition(t),o=.5*(t.pageX+n.x),r=.5*(t.pageY+n.y);this._panStart.set(o,r)}}_handleTouchStartDolly(t){const n=this._getSecondPointerPosition(t),o=t.pageX-n.x,r=t.pageY-n.y,s=Math.sqrt(o*o+r*r);this._dollyStart.set(0,s)}_handleTouchStartDollyPan(t){this.enableZoom&&this._handleTouchStartDolly(t),this.enablePan&&this._handleTouchStartPan(t)}_handleTouchStartDollyRotate(t){this.enableZoom&&this._handleTouchStartDolly(t),this.enableRotate&&this._handleTouchStartRotate(t)}_handleTouchMoveRotate(t){if(this._pointers.length==1)this._rotateEnd.set(t.pageX,t.pageY);else{const o=this._getSecondPointerPosition(t),r=.5*(t.pageX+o.x),s=.5*(t.pageY+o.y);this._rotateEnd.set(r,s)}this._rotateDelta.subVectors(this._rotateEnd,this._rotateStart).multiplyScalar(this.rotateSpeed);const n=this.domElement;this._rotateLeft(st*this._rotateDelta.x/n.clientHeight),this._rotateUp(st*this._rotateDelta.y/n.clientHeight),this._rotateStart.copy(this._rotateEnd)}_handleTouchMovePan(t){if(this._pointers.length===1)this._panEnd.set(t.pageX,t.pageY);else{const n=this._getSecondPointerPosition(t),o=.5*(t.pageX+n.x),r=.5*(t.pageY+n.y);this._panEnd.set(o,r)}this._panDelta.subVectors(this._panEnd,this._panStart).multiplyScalar(this.panSpeed),this._pan(this._panDelta.x,this._panDelta.y),this._panStart.copy(this._panEnd)}_handleTouchMoveDolly(t){const n=this._getSecondPointerPosition(t),o=t.pageX-n.x,r=t.pageY-n.y,s=Math.sqrt(o*o+r*r);this._dollyEnd.set(0,s),this._dollyDelta.set(0,Math.pow(this._dollyEnd.y/this._dollyStart.y,this.zoomSpeed)),this._dollyOut(this._dollyDelta.y),this._dollyStart.copy(this._dollyEnd);const a=(t.pageX+n.x)*.5,c=(t.pageY+n.y)*.5;this._updateZoomParameters(a,c)}_handleTouchMoveDollyPan(t){this.enableZoom&&this._handleTouchMoveDolly(t),this.enablePan&&this._handleTouchMovePan(t)}_handleTouchMoveDollyRotate(t){this.enableZoom&&this._handleTouchMoveDolly(t),this.enableRotate&&this._handleTouchMoveRotate(t)}_addPointer(t){this._pointers.push(t.pointerId)}_removePointer(t){delete this._pointerPositions[t.pointerId];for(let n=0;n<this._pointers.length;n++)if(this._pointers[n]==t.pointerId){this._pointers.splice(n,1);return}}_isTrackingPointer(t){for(let n=0;n<this._pointers.length;n++)if(this._pointers[n]==t.pointerId)return!0;return!1}_trackPointer(t){let n=this._pointerPositions[t.pointerId];n===void 0&&(n=new nt,this._pointerPositions[t.pointerId]=n),n.set(t.pageX,t.pageY)}_getSecondPointerPosition(t){const n=t.pointerId===this._pointers[0]?this._pointers[1]:this._pointers[0];return this._pointerPositions[n]}_customWheelEvent(t){const n=t.deltaMode,o={clientX:t.clientX,clientY:t.clientY,deltaY:t.deltaY};switch(n){case 1:o.deltaY*=16;break;case 2:o.deltaY*=100;break}return t.ctrlKey&&!this._controlActive&&(o.deltaY*=10),o}}function Aa(e){this.enabled!==!1&&(this._pointers.length===0&&(this.domElement.setPointerCapture(e.pointerId),this.domElement.ownerDocument.addEventListener("pointermove",this._onPointerMove),this.domElement.ownerDocument.addEventListener("pointerup",this._onPointerUp)),!this._isTrackingPointer(e)&&(this._addPointer(e),e.pointerType==="touch"?this._onTouchStart(e):this._onMouseDown(e)))}function Na(e){this.enabled!==!1&&(e.pointerType==="touch"?this._onTouchMove(e):this._onMouseMove(e))}function _a(e){switch(this._removePointer(e),this._pointers.length){case 0:this.domElement.releasePointerCapture(e.pointerId),this.domElement.ownerDocument.removeEventListener("pointermove",this._onPointerMove),this.domElement.ownerDocument.removeEventListener("pointerup",this._onPointerUp),this.dispatchEvent(fi),this.state=Ee.NONE;break;case 1:const t=this._pointers[0],n=this._pointerPositions[t];this._onTouchStart({pointerId:t,pageX:n.x,pageY:n.y});break}}function Ra(e){let t;switch(e.button){case 0:t=this.mouseButtons.LEFT;break;case 1:t=this.mouseButtons.MIDDLE;break;case 2:t=this.mouseButtons.RIGHT;break;default:t=-1}switch(t){case zt.DOLLY:if(this.enableZoom===!1)return;this._handleMouseDownDolly(e),this.state=Ee.DOLLY;break;case zt.ROTATE:if(e.ctrlKey||e.metaKey||e.shiftKey){if(this.enablePan===!1)return;this._handleMouseDownPan(e),this.state=Ee.PAN}else{if(this.enableRotate===!1)return;this._handleMouseDownRotate(e),this.state=Ee.ROTATE}break;case zt.PAN:if(e.ctrlKey||e.metaKey||e.shiftKey){if(this.enableRotate===!1)return;this._handleMouseDownRotate(e),this.state=Ee.ROTATE}else{if(this.enablePan===!1)return;this._handleMouseDownPan(e),this.state=Ee.PAN}break;default:this.state=Ee.NONE}this.state!==Ee.NONE&&this.dispatchEvent(_o)}function Ca(e){switch(this.state){case Ee.ROTATE:if(this.enableRotate===!1)return;this._handleMouseMoveRotate(e);break;case Ee.DOLLY:if(this.enableZoom===!1)return;this._handleMouseMoveDolly(e);break;case Ee.PAN:if(this.enablePan===!1)return;this._handleMouseMovePan(e);break}}function Ia(e){this.enabled===!1||this.enableZoom===!1||this.state!==Ee.NONE||(e.preventDefault(),this.dispatchEvent(_o),this._handleMouseWheel(this._customWheelEvent(e)),this.dispatchEvent(fi))}function Fa(e){this.enabled!==!1&&this._handleKeyDown(e)}function Ma(e){switch(this._trackPointer(e),this._pointers.length){case 1:switch(this.touches.ONE){case Bt.ROTATE:if(this.enableRotate===!1)return;this._handleTouchStartRotate(e),this.state=Ee.TOUCH_ROTATE;break;case Bt.PAN:if(this.enablePan===!1)return;this._handleTouchStartPan(e),this.state=Ee.TOUCH_PAN;break;default:this.state=Ee.NONE}break;case 2:switch(this.touches.TWO){case Bt.DOLLY_PAN:if(this.enableZoom===!1&&this.enablePan===!1)return;this._handleTouchStartDollyPan(e),this.state=Ee.TOUCH_DOLLY_PAN;break;case Bt.DOLLY_ROTATE:if(this.enableZoom===!1&&this.enableRotate===!1)return;this._handleTouchStartDollyRotate(e),this.state=Ee.TOUCH_DOLLY_ROTATE;break;default:this.state=Ee.NONE}break;default:this.state=Ee.NONE}this.state!==Ee.NONE&&this.dispatchEvent(_o)}function ka(e){switch(this._trackPointer(e),this.state){case Ee.TOUCH_ROTATE:if(this.enableRotate===!1)return;this._handleTouchMoveRotate(e),this.update();break;case Ee.TOUCH_PAN:if(this.enablePan===!1)return;this._handleTouchMovePan(e),this.update();break;case Ee.TOUCH_DOLLY_PAN:if(this.enableZoom===!1&&this.enablePan===!1)return;this._handleTouchMoveDollyPan(e),this.update();break;case Ee.TOUCH_DOLLY_ROTATE:if(this.enableZoom===!1&&this.enableRotate===!1)return;this._handleTouchMoveDollyRotate(e),this.update();break;default:this.state=Ee.NONE}}function Pa(e){this.enabled!==!1&&e.preventDefault()}function Ea(e){e.key==="Control"&&(this._controlActive=!0,this.domElement.getRootNode().addEventListener("keyup",this._interceptControlUp,{passive:!0,capture:!0}))}function Ta(e){e.key==="Control"&&(this._controlActive=!1,this.domElement.getRootNode().removeEventListener("keyup",this._interceptControlUp,{passive:!0,capture:!0}))}const bn=window;function Xn(){const e=window;if(typeof e.__cooptIOSDetected=="boolean")return e.__cooptIOSDetected;try{const t=String(navigator.userAgent||"");if(/iPad|iPhone|iPod/i.test(t)||/Macintosh/i.test(t)&&Number(navigator.maxTouchPoints||0)>1)return!0}catch{}return!1}function Oa(){const e=Xn(),t=window;t.__sceneSetupIOSDetected=e;let n=document.getElementById("threejs-canvas-container");const o=n?n.clientWidth:Xe.CANVAS_WIDTH,r=n?n.clientHeight:Xe.CANVAS_HEIGHT,s=o/r,a=Xe.VIEW_SIZE,c=new ps;c.userData=c.userData||{},c.userData.renderContext={three:Hr,global:typeof window<"u"?window:globalThis};const i=new ds(-a*s/2,a*s/2,a/2,-a/2,Xe.CAMERA_CLIP_NEAR,Xe.CAMERA_CLIP_FAR),l=new ms({antialias:!0,alpha:!0,precision:"highp",logarithmicDepthBuffer:!0});l.setPixelRatio(window.devicePixelRatio),l.setSize(o,r,!1),l.setClearColor(0,0),l.sortObjects=!1,l.shadowMap.enabled=!1;const m=()=>{const u=document.getElementById("threejs-canvas-container");if(!u)return null;const h=Xn();if(h){console.log("📱 iOS detected - applying rendering fix"),u.style.display="block",u.style.position="relative",u.style.minHeight="600px",u.style.height="calc(100vh - 200px)",u.offsetHeight;const g=window.getComputedStyle(u);console.log("📱 iOS container computed style:",{display:g.display,position:g.position,width:g.width,height:g.height,overflow:g.overflow,justifyContent:g.justifyContent})}if(l.domElement.parentElement!==u&&u.appendChild(l.domElement),h){const g=u.getBoundingClientRect();console.log("📱 iOS container rect:",{x:g.x,y:g.y,width:g.width,height:g.height,left:g.left,right:g.right,top:g.top,bottom:g.bottom});const y=g.width>0?g.width:u.clientWidth||800,A=g.height>0?g.height:u.clientHeight||600;console.log(`📱 iOS container dimensions: ${y}x${A}`),l.setSize(y,A,!0),console.log(`📱 iOS canvas size set to: ${y}x${A} (updateStyle=true)`);const x=l.domElement;console.log("📱 iOS canvas attributes:",{width:x.width,height:x.height,styleWidth:x.style.width,styleHeight:x.style.height}),x.style.width=`${y}px`,x.style.height=`${A}px`,x.style.position="absolute",x.style.left="50%",x.style.top="50%",x.style.transform="translate(-50%, -50%)",x.style.webkitTransform="translate(-50%, -50%)",x.style.maxWidth="100%",x.style.maxHeight="100%",console.log(`📱 iOS canvas styled: ${y}px x ${A}px, positioned absolute with transform`),setTimeout(()=>{const _=x.getBoundingClientRect();console.log("📱 iOS canvas position after 100ms:",{x:_.x,y:_.y,width:_.width,height:_.height,left:_.left,right:_.right});const S=window.getComputedStyle(x);console.log("📱 iOS canvas computed style:",{position:S.position,left:S.left,top:S.top,transform:S.transform,width:S.width,height:S.height})},100),console.log("📱 iOS canvas positioned - CSS transform will center")}else l.domElement.style.width="100%",l.domElement.style.height="100%";return n=u,u},p=m();p&&p.clientWidth>0&&p.clientHeight>0&&l.setSize(p.clientWidth,p.clientHeight,!1);const d=new gi(i,l.domElement);return d.enableDamping=!0,d.dampingFactor=.05,d.screenSpacePanning=!0,d.enableZoom=!0,d.maxDistance=5e3,d.minDistance=10,d.enableRotate=!0,d.enablePan=!0,i.position.set(Xe.CAMERA_INITIAL_POSITION.x,Xe.CAMERA_INITIAL_POSITION.y,Xe.CAMERA_INITIAL_POSITION.z),i.lookAt(Xe.CAMERA_INITIAL_TARGET.x,Xe.CAMERA_INITIAL_TARGET.y,Xe.CAMERA_INITIAL_TARGET.z),d.target.set(Xe.CAMERA_INITIAL_TARGET.x,Xe.CAMERA_INITIAL_TARGET.y,Xe.CAMERA_INITIAL_TARGET.z),d.update(),i.updateProjectionMatrix(),window.addEventListener("resize",()=>{const u=Xn();console.log(`🔄 Window resize event (scene-setup.js)${u?" [iOS]":""}`);const h=m();if(h){const g=h.clientWidth,y=h.clientHeight;if(u&&console.log(`📱 iOS resize - container: ${g}x${y}, devicePixelRatio: ${window.devicePixelRatio}`),g<=0||y<=0)return;if(l.setPixelRatio(window.devicePixelRatio),l.setSize(g,y,!1),l.domElement.style.width="100%",l.domElement.style.height="100%",t.updateCameraViewBounds)console.log("📷 Calling updateCameraViewBounds from resize handler"),t.updateCameraViewBounds();else{const A=g/y;i.left=-a*A/2,i.right=a*A/2,i.top=a/2,i.bottom=-a/2,i.updateProjectionMatrix()}console.log(`Canvas resized to: ${g}x${y} (pixelRatio: ${window.devicePixelRatio})`)}}),window.addEventListener("coopt:react-mounted",()=>{const u=m();if(!u)return;const h=u.clientWidth,g=u.clientHeight;h<=0||g<=0||(l.setPixelRatio(window.devicePixelRatio),l.setSize(h,g,!1))}),{scene:c,camera:i,renderer:l,controls:d}}function Da(e){try{const t=new fs(16777215,Xe.AMBIENT_LIGHT_INTENSITY);e.add(t);const n=new gs(16777215,Xe.DIRECTIONAL_LIGHT_INTENSITY);return n.position.set(Xe.DIRECTIONAL_LIGHT_POSITION.x,Xe.DIRECTIONAL_LIGHT_POSITION.y,Xe.DIRECTIONAL_LIGHT_POSITION.z),e.add(n),{ambientLight:t,directionalLight:n}}catch(t){throw console.error("❌ Error initializing lighting:",t),t}}function ja(){function e(){requestAnimationFrame(e);const t=bn.scene,n=bn.camera,o=bn.renderer,r=bn.controls;t&&n&&o&&r&&(r.update(),o.render(t,n))}e()}function La(){try{const e=Ao("lastWavefrontSnapshot");if(!e)return null;const t=JSON.parse(e);return t&&typeof t=="object"?t:null}catch{return null}}function Ba(e){try{fa("lastWavefrontSnapshot",JSON.stringify(e))}catch{}}function za(){try{const e=Ao("lastPsfMeta");if(!e)return null;const t=JSON.parse(e);return t&&typeof t=="object"?t:null}catch{return null}}function Wa(){try{const e=Ao("lastPsfError");if(!e)return null;const t=JSON.parse(e);return t&&typeof t=="object"?t:null}catch{return null}}class pt{constructor(t){this.containerElementIdOrElement=t,this.plotlyConfig={displayModeBar:!0,modeBarButtonsToRemove:["pan2d","lasso2d"],responsive:!0}}_createWavefrontEngine(t,n=.5876){const o=typeof window<"u"?window:null,r=o?.opener||o,s=typeof r?.createOPDCalculator=="function"&&r.createOPDCalculator||typeof o?.createOPDCalculator=="function"&&o.createOPDCalculator||sn,a=typeof r?.createWavefrontAnalyzer=="function"&&r.createWavefrontAnalyzer||typeof o?.createWavefrontAnalyzer=="function"&&o.createWavefrontAnalyzer||an,c=s?s(t,n):null;if(!c)throw new Error("波面収差計算機の初期化に失敗しました: OPDCalculator を作成できません");const i=a?a(c):null;if(!i)throw new Error("波面収差計算機の初期化に失敗しました: WavefrontAnalyzer を作成できません");return{calculator:c,analyzer:i}}_resolveHostWindow(){const t=typeof window<"u"?window:null;return t?.opener||t}_buildWavefrontMapFromOpdGrid(t,n=.5876){const o=Array.isArray(t)?t.length:0,r=o>1?Array.from({length:o},(p,d)=>-1+2*d/(o-1)):[0],s=[],a=[],c=[];for(let p=0;p<o;p++){const d=Array.isArray(t[p])?t[p]:[];for(let u=0;u<d.length;u++){const h=d[u];if(h==null)continue;const g=Number(h);if(!Number.isFinite(g))continue;const y=r[Math.min(u,r.length-1)]??0,A=r[Math.min(p,r.length-1)]??0;s.push({x:y,y:A,ix:u,iy:p}),c.push(g),a.push(g*n)}}const i=p=>{if(!Array.isArray(p)||p.length===0)return{count:0,sampleCount:0,mean:NaN,rms:NaN,peakToPeak:NaN,min:NaN,max:NaN};let d=0,u=0,h=1/0,g=-1/0,y=0;for(const x of p)Number.isFinite(x)&&(d+=x,u+=x*x,x<h&&(h=x),x>g&&(g=x),y+=1);if(y===0)return{count:0,sampleCount:0,mean:NaN,rms:NaN,peakToPeak:NaN,min:NaN,max:NaN};const A=d/y;return{count:y,sampleCount:y,mean:A,rms:Math.sqrt(Math.max(0,u/y)),peakToPeak:g-h,min:h,max:g}},l=i(c),m=i(a);return{pupilCoordinates:s,pupilRange:1,opds:a,opdsInWavelengths:c,wavefrontAberrations:c,raw:{opds:a.slice(),opdsInWavelengths:c.slice(),wavefrontAberrations:c.slice(),opdGrid:t,opdMicrons:m,opdWavelengths:l},display:{opds:a.slice(),opdsInWavelengths:c.slice(),wavefrontAberrations:c.slice(),opdGrid:t},statistics:{wavefront:l,opdMicrons:m,opdWavelengths:l,raw:{wavefront:l,opdMicrons:m,opdWavelengths:l},display:{opdMicrons:m,opdWavelengths:l}}}}_buildGridFromSamples(t,n,o){const r=Math.max(1,Math.floor(Number(o)||1)),s=Array.from({length:r},()=>Array.from({length:r},()=>null));if(!Array.isArray(t)||!Array.isArray(n))return s;const a=Math.min(t.length,n.length);for(let c=0;c<a;c++){const i=t[c],l=Number(n[c]);if(!i||!Number.isFinite(l))continue;const m=Number.isInteger(i?.ix)?i.ix:Math.round((Number(i?.x)+1)*.5*(r-1)),p=Number.isInteger(i?.iy)?i.iy:Math.round((Number(i?.y)+1)*.5*(r-1));!Number.isFinite(m)||!Number.isFinite(p)||p<0||p>=r||m<0||m>=r||(s[p][m]=l)}return s}_applyNativeZernikePostProcess(t,n,o={}){const r=Number(o?.wavelength),s=String(o?.opdDisplayMode||"pistonTiltRemoved"),a=!!o?.skipZernikeFit,c=Number.isFinite(Number(o?.zernikeMaxNoll))?Math.max(1,Math.floor(Number(o.zernikeMaxNoll))):37;if(t.opdMode="simple",t.opdDisplayModeRequested=s,t.skipZernikeFit=a,a)t.zernike=null;else{const y=Array.isArray(t?.raw?.opds)?t.raw.opds.length:0,A=Math.max(1,Math.min(c,y));t.zernike=n.fitZernikePolynomials({pupilCoordinates:t.pupilCoordinates,opds:t.raw.opds},A)}if(t?.zernike?.removedModelMicrons?.length===t?.opds?.length)for(let y=0;y<t.opds.length;y++){const A=Number(t.raw.opds[y]),x=Number(t.zernike.removedModelMicrons[y]),_=Number.isFinite(A)&&Number.isFinite(x)?A-x:NaN;t.opds[y]=_,t.opdsInWavelengths[y]=Number.isFinite(_)&&Number.isFinite(r)&&r>0?_/r:NaN,t.wavefrontAberrations[y]=t.opdsInWavelengths[y]}let i=null,l=null;if(s==="pistonTiltRemoved"){const y=n._removeBestFitPlane(t.pupilCoordinates,t.opds);if(y&&Array.isArray(y.residualMicrons)&&Array.isArray(y.residualWaves))i={mode:"pistonTiltRemoved",planeCoefficientsMicrons:y.coefficientsMicrons,opds:y.residualMicrons,opdsInWavelengths:y.residualWaves,wavefrontAberrations:y.residualWaves},l={mode:"pistonTiltRemoved",planeCoefficientsMicrons:y.coefficientsMicrons,opdMicrons:n.calculateStatistics(y.residualMicrons,{removePiston:!1}),opdWavelengths:n.calculateStatistics(y.residualWaves,{removePiston:!1})};else{const A=n._calculateLowOrderRemovedStats(t.pupilCoordinates,t.opds,{removeIndices:[0,1,2],maxOrder:2,pupilRange:t.pupilRange});A&&Array.isArray(A.residualMicrons)&&Array.isArray(A.residualWaves)&&(i={mode:"pistonTiltRemoved",opds:A.residualMicrons,opdsInWavelengths:A.residualWaves,wavefrontAberrations:A.residualWaves},l={mode:"pistonTiltRemoved",opdMicrons:n.calculateStatistics(A.residualMicrons,{removePiston:!1}),opdWavelengths:n.calculateStatistics(A.residualWaves,{removePiston:!1})})}}else if(s==="pistonTiltDefocusRemoved"){const y=n._calculateLowOrderRemovedStats(t.pupilCoordinates,t.opds,{removeIndices:[0,1,2,4],maxOrder:2,pupilRange:t.pupilRange});y&&Array.isArray(y.residualMicrons)&&Array.isArray(y.residualWaves)&&(i={mode:"pistonTiltDefocusRemoved",opds:y.residualMicrons,opdsInWavelengths:y.residualWaves,wavefrontAberrations:y.residualWaves},l={mode:"pistonTiltDefocusRemoved",opdMicrons:n.calculateStatistics(y.residualMicrons,{removePiston:!1}),opdWavelengths:n.calculateStatistics(y.residualWaves,{removePiston:!1})})}if(!i||!l){if(i={mode:"default",opds:t.opds.slice(),opdsInWavelengths:t.opdsInWavelengths.slice(),wavefrontAberrations:t.wavefrontAberrations.slice()},s!=="default"){const y=i.opds.slice();let A=0,x=0;for(const S of y)Number.isFinite(S)&&(A+=S,x+=1);const _=x>0?A/x:0;for(let S=0;S<y.length;S++){const N=y[S];y[S]=Number.isFinite(N)?N-_:NaN}i.opds=y,i.opdsInWavelengths=y.map(S=>Number.isFinite(S)&&Number.isFinite(r)&&r>0?S/r:NaN),i.wavefrontAberrations=i.opdsInWavelengths.slice(),i.mode=`${s} (piston-only fallback)`}l={mode:i.mode||"default",opdMicrons:n.calculateStatistics(i.opds,{removePiston:!1}),opdWavelengths:n.calculateStatistics(i.opdsInWavelengths,{removePiston:!1})}}const m=Array.isArray(t?.raw?.opdGrid)?t.raw.opdGrid.length:0;i.opdGrid=this._buildGridFromSamples(t.pupilCoordinates,i.opdsInWavelengths,m),t.display=i;const p=n._calculateLowOrderRemovedStats(t.pupilCoordinates,t.raw.opds,{removeIndices:[0,1,2,4],maxOrder:2,pupilRange:t.pupilRange}),d=t.opdMode||"simple",u=!!t.skipZernikeFit,h=t.pupilSamplingMode||null;t.statistics={wavefront:n.calculateStatistics(t.wavefrontAberrations,{removePiston:!0}),opdMicrons:l.opdMicrons,opdWavelengths:l.opdWavelengths,raw:{wavefront:n.calculateStatistics(t.raw.wavefrontAberrations,{removePiston:!1}),opdMicrons:n.calculateStatistics(t.raw.opds,{removePiston:!1}),opdWavelengths:n.calculateStatistics(t.raw.opdsInWavelengths,{removePiston:!1})},aberration:p,display:l};const g=[t.statistics?.wavefront,t.statistics?.opdMicrons,t.statistics?.opdWavelengths,t.statistics?.raw?.wavefront,t.statistics?.raw?.opdMicrons,t.statistics?.raw?.opdWavelengths,t.statistics?.display?.opdMicrons,t.statistics?.display?.opdWavelengths,t.statistics?.aberration?.opdMicrons,t.statistics?.aberration?.opdWavelengths];for(const y of g)!y||typeof y!="object"||(y.pupilSamplingMode=h,y.opdMode=d,y.skipZernikeFit=u);t.statistics?.display?.opdMicrons&&(t.statistics.display.opdMicrons.opdDisplayMode=s),t.statistics?.display?.opdWavelengths&&(t.statistics.display.opdWavelengths.opdDisplayMode=s),t.statistics?.aberration?.opdMicrons&&t.statistics?.aberration?.removeIndices&&(t.statistics.aberration.opdMicrons.removeIndices=t.statistics.aberration.removeIndices),t.statistics?.aberration?.opdWavelengths&&t.statistics?.aberration?.removeIndices&&(t.statistics.aberration.opdWavelengths.removeIndices=t.statistics.aberration.removeIndices)}async _computeNativeOpdWavefrontMap(t,n,o,r={}){const s=(P,Z)=>{try{typeof r?.onProgress=="function"&&r.onProgress({percent:P,message:Z,phase:"opd-native"})}catch{}};s(5,"Checking Rust/WASM OPD runtime..."),s(10,"Loading native OPD bridge...");const{runNativeOpdMap:a}=await Re(async()=>{const{runNativeOpdMap:P}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(Z=>Z.bU);return{runNativeOpdMap:P}},__vite__mapDeps([0,1])),c=this._resolveHostWindow();s(16,"Collecting optical/source/object rows...");const i=c&&typeof c.getSourceRows=="function"?c.getSourceRows():[],l=c&&typeof c.getObjectRows=="function"?c.getObjectRows():[],m=Number.isInteger(Number(n?.objectIndex))?Number(n.objectIndex):0,d=(()=>{const P=Z=>{const R=typeof Z=="string"?Z.trim().toLowerCase():"";return R==="stop"||R==="entrance"?R:""};try{const Z=globalThis?.__COOPT_FORCE_INFINITE_PUPIL_MODE??globalThis?.COOPT_FORCE_INFINITE_PUPIL_MODE,R=P(Z);if(R)return R}catch{}try{const Z=localStorage.getItem("coopt.forceInfinitePupilMode");return P(Z)}catch{return""}})(),u=Array.isArray(l)&&l[m]?l[m]:null,h=String(u?.position??u?.object??u?.objectType??u?.type??n?.type??"").trim().toLowerCase(),g=h.includes("angle")||h==="point"||String(n?.type||"").trim().toLowerCase()==="angle",y=Number(u?.xHeightAngle??u?.xFieldAngle??u?.xAngle??n?.fieldAngle?.x??0),A=Number(u?.yHeightAngle??u?.yFieldAngle??u?.yAngle??u?.fieldAngle??n?.fieldAngle?.y??0),x=Math.abs(y)>1e-12||Math.abs(A)>1e-12,_=d==="stop"||d==="entrance"?d:g&&x?"entrance":void 0,S=(()=>{const P=Number(o);return Number.isFinite(P)?Math.max(17,Math.floor(P)):129})(),N=Number.isFinite(Number(o))?Math.floor(Number(o)):129;N<17&&N!==S&&s(20,`Grid request raised to minimum for native compute (${N} -> ${S})`),s(25,`Tracing rays in native Rust (grid=${S})...`);const f=`native-opd-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;let w=null;try{const{listen:P}=await Re(async()=>{const{listen:Z}=await import("./event-B_hCB_W5.js");return{listen:Z}},__vite__mapDeps([2,0,1]));w=await P("analysis-progress",Z=>{try{const R=Z?.payload||{};if(!R||String(R.jobId||"")!==f)return;s(R.percent,R.phase||"opd-native",R.message||"Tracing rays in native Rust...")}catch{}})}catch{}let b;try{const P=Number(n?.wavelength);b=await a({jobId:f,opticalSystemRows:Array.isArray(t)?t:[],sourceRows:Array.isArray(i)?i:[],objectRows:Array.isArray(l)?l:[],objectIndex:m,wavelengthUm:Number.isFinite(P)&&P>0?P:void 0,gridSize:S,pupilSamplingMode:_,opdDisplayMode:r?.opdDisplayMode||"pistonTiltRemoved"})}finally{try{typeof w=="function"&&w()}catch{}}try{console.log("🧭 [OPD Backend] native",b?.backend,{gridSize:b?.gridSize,hitCount:b?.hitCount})}catch{}const v=Number(b?.hitCount)||0,M=Number(b?.sampleCount)||0,C=M>0?v/M*100:0,D=Number.isFinite(C)?C.toFixed(1):"0.0";s(75,`Converting OPD map (hits=${v}/${M}, valid=${D}%)...`);const B=P=>{if(!Array.isArray(P)||P.length===0)return!1;for(const Z of P)if(Array.isArray(Z)){for(const R of Z)if(Number.isFinite(Number(R)))return!0}return!1},j=Array.isArray(b?.displayOpdGrid)?b.displayOpdGrid:[],H=Array.isArray(b?.rawOpdGrid)?b.rawOpdGrid:[],ce=B(j),U=ce?j:H;if(!ce)try{console.warn("⚠️ [OPD Native] display grid had no finite samples; falling back to raw grid",{backend:b?.backend,message:b?.message,hitCount:b?.hitCount,sampleCount:b?.sampleCount})}catch{}if(!U.length||!Array.isArray(U[0])||!B(U))throw new Error("Native OPD map is empty");const G=Number(n?.wavelength)||.5876,F=B(H)?H:U,W=this._buildWavefrontMapFromOpdGrid(F,G),I=this._buildWavefrontMapFromOpdGrid(U,G),E={...I,raw:{...W?.raw||{},opdGrid:F},display:{...I?.display||{},mode:r?.opdDisplayMode||"pistonTiltRemoved",opdGrid:U},statistics:{...I?.statistics||{},raw:{...W?.statistics?.raw||{}},display:{...I?.statistics?.display||{},opdDisplayMode:r?.opdDisplayMode||"pistonTiltRemoved"}},zernike:null,opdMode:"native-grid",opdDisplayModeRequested:r?.opdDisplayMode||"pistonTiltRemoved",skipZernikeFit:!0};try{const P=r?.opdDisplayMode||"pistonTiltRemoved",Z=String(b?.pupilSamplingMode||"").toLowerCase(),R=Z==="stop"||Z==="entrance"?Z:String(b?.message||"").includes("stop -> entrance")?"entrance":"stop",k=String(b?.chiefReferenceMode||"").trim(),$=String(b?.backend||"").trim();E.pupilSamplingMode=R;const te=[E?.statistics?.wavefront,E?.statistics?.opdMicrons,E?.statistics?.opdWavelengths,E?.statistics?.raw?.wavefront,E?.statistics?.raw?.opdMicrons,E?.statistics?.raw?.opdWavelengths,E?.statistics?.display?.opdMicrons,E?.statistics?.display?.opdWavelengths];for(const q of te)!q||typeof q!="object"||(q.pupilSamplingMode=R,q.opdDisplayMode=P,k&&(q.chiefReferenceMode=k),$&&(q.backend=$),Number.isFinite(Number(b?.wavelengthUm))&&(q.wavelengthUm=Number(b.wavelengthUm),q.wavelengthNm=Number(b.wavelengthUm)*1e3),Number.isFinite(Number(b?.gridSize))&&(q.gridSize=Number(b.gridSize)),Number.isInteger(Number(b?.targetSurface))&&(q.targetSurface=Number(b.targetSurface)),Number.isInteger(Number(b?.stopSurface))&&(q.stopSurface=Number(b.stopSurface)),Number.isFinite(Number(b?.usedObjectX))&&(q.usedObjectX=Number(b.usedObjectX)),Number.isFinite(Number(b?.usedObjectY))&&(q.usedObjectY=Number(b.usedObjectY)),b?.usedObjectPosition&&(q.usedObjectPosition=String(b.usedObjectPosition)),q.hitCount=Number(b?.hitCount??0),q.sampleCount=Number(b?.sampleCount??0));E.nativeMeta={backend:$||"run_native_opd_map",message:b?.message||null,gridSource:ce?"displayOpdGrid":"rawOpdGrid",hitCount:Number(b?.hitCount??0),sampleCount:Number(b?.sampleCount??0),pupilSamplingMode:R,chiefReferenceMode:k||null,wavelengthUm:Number(b?.wavelengthUm??0),gridSize:Number(b?.gridSize??0),targetSurface:Number(b?.targetSurface??0),stopSurface:Number(b?.stopSurface??0)};try{console.log("🧭 [OPD Native] reference policy",{backend:$||"(unknown)",chiefReferenceMode:k||"(unknown)",message:b?.message||null,hitCount:b?.hitCount,sampleCount:b?.sampleCount})}catch{}}catch{}return s(90,`Preparing plot samples (${Number(E?.statistics?.opdWavelengths?.count)||0})...`),E}static getBlueGreenRedColorscale(){return[[0,"rgb(0, 0, 255)"],[.5,"rgb(0, 255, 0)"],[1,"rgb(255, 0, 0)"]]}_extractHintText(t){const n=String(t??""),o=n.indexOf("hint=");return o<0?"":n.slice(o+5).trim()}_renderCalculationUnavailable(t,{title:n,message:o,wavefrontMap:r,fieldSetting:s,extra:a}={}){try{if(!t)return;try{t.style.display="block",t.style.visibility="visible",t.style.minHeight||(t.style.minHeight="120px")}catch{}try{const d=this.resolvePlotly(t);d&&typeof d.purge=="function"&&d.purge(t)}catch{}const c=String(o??r?.error?.message??"有効な光線データがありません。"),i=this._extractHintText(c),l=s?.displayName||s?.id?String(s.displayName??s.id):"Field Point",m=a?String(a):"";try{console.warn("⚠️ [WavefrontPlotter] calculation unavailable",{title:n||"波面計算不能",field:l,message:c,hint:i||null})}catch{}t.innerHTML=`
                <div style="padding: 16px; text-align: left; color: #b71c1c; border: 1px solid #d32f2f; border-radius: 6px; background-color: #ffebee;">
                    <h3 style="margin: 0 0 8px 0;">${n||"波面計算不能"}</h3>
                    <div style="margin: 0 0 8px 0; color: #333;">${l} では波面/OPD を計算できませんでした（ビネッティング/有効FOV外の可能性）。</div>
                    <pre style="margin: 0; white-space: pre-wrap; word-break: break-word; color: #b71c1c;">${c}</pre>
                    ${i?`<div style="margin-top: 10px; color: #333;"><b>hint</b>: ${i}</div>`:""}
                    ${m?`<div style="margin-top: 10px; color: #333;">${m}</div>`:""}
                </div>
            `;const p=this.resolveStatsContainer(t);p&&(p.textContent=""),this._setSystemDataText(`Wavefront/OPD unavailable for ${l}.

${c}${i?`

hint: ${i}`:""}`)}catch{}}resolveContainer(){return this.containerElementIdOrElement?typeof this.containerElementIdOrElement=="string"?document.getElementById(this.containerElementIdOrElement):this.containerElementIdOrElement:null}resolvePlotly(t){return t?.ownerDocument?.defaultView?.Plotly||(typeof Plotly<"u"?Plotly:null)}resolveStatsContainer(t){if(!t)return null;const n=t.id;return n?t.ownerDocument.getElementById(`${n}-stats`):null}_setSystemDataText(t){const n=o=>{const r=["system-data","systemData","popup-system-data"];for(const s of r){const a=o?.getElementById?.(s);if(a&&typeof a.value=="string")return a.value=t,!0}return!1};try{if(n(document))return}catch{}try{if(window.opener&&window.opener.document&&n(window.opener.document))return}catch{}}_updateSystemDataWithZernike(t,n,o=37){try{if(!t||typeof t.formatZernikeReportText!="function")return;const r=t.formatZernikeReportText(n,{maxNoll:o});this._setSystemDataText(typeof r=="string"?r:String(r??""))}catch{}}async plotOPDSurface(t,n,o=.5876,r=16,s={}){try{console.log("🌊 OPD 3Dサーフェスプロット生成開始...");const a=(N,f)=>{try{typeof s?.onProgress=="function"&&s.onProgress({percent:N,message:f,phase:"opd-render"})}catch{}},c=s?.opdDisplayMode||"pistonTiltRemoved",i=await this._computeNativeOpdWavefrontMap(t,n,r,{opdDisplayMode:c,onProgress:s?.onProgress||null});if(i?.error)return this._renderCalculationUnavailable(this.resolveContainer(),{title:"OPD計算不能",message:i.error?.message,wavefrontMap:i,fieldSetting:n}),i;if(!(Array.isArray(i?.raw?.opds)?i.raw.opds.length:Array.isArray(i?.opds)?i.opds.length:0)){const N=(()=>{try{const f=i?.invalidReasonCounts;if(f&&typeof f=="object")return`invalid reasons: ${JSON.stringify(f)}`}catch{}return""})();return console.error("❌ OPD波面データが0点です（全光線失敗）。",i?.invalidReasonCounts||i?.error||{}),this._renderCalculationUnavailable(this.resolveContainer(),{title:"OPD計算不能",message:"波面データが0点です（全光線失敗）。",wavefrontMap:i,fieldSetting:n,extra:N}),i}try{if(typeof globalThis<"u"&&globalThis.__OPD_DEBUG===!0){const f=Array.isArray(i?.pupilCoordinates)?i.pupilCoordinates:[];let w=0,b=1/0,v=0;for(const M of f){const C=Number(M?.x),D=Number(M?.y);if(!Number.isFinite(C)||!Number.isFinite(D))continue;const B=Math.hypot(C,D);Number.isFinite(B)&&(v++,B>w&&(w=B),B<b&&(b=B))}console.log("🔍 [OPD] Pupil coverage",{pupilSamplingMode:i?.pupilSamplingMode,bestEffortVignettedPupil:!!i?.bestEffortVignettedPupil,points:v,maxRho:w,minRho:Number.isFinite(b)?b:null})}}catch{}const m=Array.isArray(i?.display?.opdGrid)?i.display.opdGrid:[],p=Array.isArray(i?.raw?.opdGrid)?i.raw.opdGrid:[],d=Array.isArray(m)&&m.length&&Array.isArray(m[0])?m:p;if(!d.length||!Array.isArray(d[0]))throw new Error("Native OPD grid is unavailable for plotting");const u=d.length,h=255,g=u>h?h:u;if(u>g)try{console.warn(`⚠️ [OPD] Surface render grid downsampled ${u} -> ${g} (compute grid kept at ${u})`)}catch{}const y=g>1?Array.from({length:g},(N,f)=>-1+2*f/(g-1)):[0],A=N=>N==null||!Number.isFinite(Number(N))?NaN:Number(N),x=(()=>{const N=d.map(w=>(Array.isArray(w)?w:[]).map(b=>A(b)));if(g>=u)return N;const f=Array.from({length:g},()=>Array.from({length:g},()=>NaN));for(let w=0;w<g;w++){const b=Math.floor(w*u/g),v=Math.max(b,Math.min(u-1,Math.floor((w+1)*u/g)-1));for(let M=0;M<g;M++){const C=Math.floor(M*u/g),D=Math.max(C,Math.min(u-1,Math.floor((M+1)*u/g)-1));let B=0,j=0;for(let H=b;H<=v;H++){const ce=N[H]||[];for(let U=C;U<=D;U++){const G=ce[U];Number.isFinite(G)&&(B+=G,j+=1)}}j>0&&(f[w][M]=B/j)}}for(let w=0;w<2;w++){const b=f.map(v=>v.slice());for(let v=0;v<g;v++)for(let M=0;M<g;M++){if(Number.isFinite(f[v][M]))continue;let C=0,D=0;for(let B=-1;B<=1;B++)for(let j=-1;j<=1;j++){if(j===0&&B===0)continue;const H=v+B,ce=M+j;if(H<0||H>=g||ce<0||ce>=g)continue;const U=f[H][ce];Number.isFinite(U)&&(C+=U,D+=1)}D>=5&&(b[v][M]=C/D)}for(let v=0;v<g;v++)f[v]=b[v]}return f})(),_={type:"surface",x:y,y,z:x,connectgaps:!1,colorscale:pt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:"OPD [λ]"}},S={title:{text:`Optical Path Difference (OPD) Distribution - ${n.displayName||"Field Point"}`,font:{size:16}},scene:{xaxis:{title:"Pupil coordinate X",range:[-1.1,1.1],dtick:.5},yaxis:{title:"Pupil coordinate Y",range:[-1.1,1.1],dtick:.5},zaxis:{title:"OPD [λ]"},camera:{eye:{x:1.5,y:1.5,z:1.5}}},margin:{l:0,r:0,b:0,t:40}};try{const N=this.resolveContainer();if(!N)throw new Error("プロットコンテナが見つかりません");console.log("✅ プロットコンテナ確認:",N);const f=this.resolvePlotly(N);if(!f)throw new Error("Plotly.jsライブラリが読み込まれていません");this.validatePlotlyData(_),console.log("✅ データ検証完了"),console.log("🎨 Plotly描画開始:",{container:N.id||"(element)",dataType:_.type,dataSize:`${_.z.length}x${_.z[0]?.length}`}),a(96,"Rendering OPD surface..."),S.autosize=!0,await f.newPlot(N,[_],S,this.plotlyConfig),a(100,"Completed"),console.log("✅ OPD 3Dサーフェス描画完了")}catch(N){console.error("❌ OPD 3Dサーフェス描画エラー:",N),console.log("📊 問題のデータ:",{x:_.x?_.x.length:"undefined",y:_.y?_.y.length:"undefined",z:_.z?`${_.z.length}x${_.z[0]?.length}`:"undefined"});const f=this.createFallbackSurfaceData("opd"),w=this.resolveContainer(),b=this.resolvePlotly(w);w&&b&&(S.autosize=!0,await b.newPlot(w,[f],S,this.plotlyConfig)),console.log("⚠️ フォールバックデータで描画しました")}{const N=i?.statistics?.display?.opdWavelengths?i.statistics.display.opdWavelengths:i?.statistics?.raw?.opdWavelengths||i?.statistics?.opdWavelengths;this.displayStatistics(N,"OPD","λ")}return console.log("✅ OPD 3Dサーフェスプロット生成完了"),i}catch(a){throw console.error("❌ OPD プロット生成エラー:",a),a}}async plotWavefrontAberrationSurface(t,n,o=.5876,r=16){try{console.log("🌊 波面収差（Wλ）3Dサーフェスプロット生成開始...");const s=typeof globalThis<"u"&&globalThis.__WAVEFRONT_PROFILE===!0,{analyzer:a}=this._createWavefrontEngine(t,o),c=typeof globalThis<"u"&&globalThis.__WAVEFRONT_DIAG_DISCONTINUITIES===!0;s&&console.time("⏱️ plotWavefrontSurface.generateWavefrontMap");const i=await a.generateWavefrontMap(n,r,"circular",{recordRays:!1,progressEvery:512,diagnoseDiscontinuities:c,diagTopK:5,opdMode:"simple",zernikeMaxNoll:37,renderFromZernike:!0,profile:s});if(s&&console.timeEnd("⏱️ plotWavefrontSurface.generateWavefrontMap"),i?.error)return this._renderCalculationUnavailable(this.resolveContainer(),{title:"波面計算不能",message:i.error?.message,wavefrontMap:i,fieldSetting:n}),i;if(!(Array.isArray(i?.raw?.opds)?i.raw.opds.length:Array.isArray(i?.opds)?i.opds.length:0)){const d=(()=>{try{const u=i?.invalidReasonCounts;if(u&&typeof u=="object")return`invalid reasons: ${JSON.stringify(u)}`}catch{}return""})();return console.error("❌ 波面データが0点です（全光線失敗）。",i?.invalidReasonCounts||i?.error||{}),this._renderCalculationUnavailable(this.resolveContainer(),{title:"波面計算不能",message:"波面データが0点です（全光線失敗）。",wavefrontMap:i,fieldSetting:n,extra:d}),i}options?.updateSystemData===!0&&this._updateSystemDataWithZernike(a,i,37);let m;try{const d=Math.floor(Number(i?.gridSize))||16,u=typeof globalThis<"u"&&Number.isFinite(globalThis.__WAVEFRONT_RENDER_GRID_MAX)?Math.max(33,Math.floor(Number(globalThis.__WAVEFRONT_RENDER_GRID_MAX))):257,h=typeof globalThis<"u"&&Number.isFinite(globalThis.__WAVEFRONT_RENDER_GRID_SCALE)?Math.max(1,Number(globalThis.__WAVEFRONT_RENDER_GRID_SCALE)):3,g=Math.max(129,Math.min(u,Math.floor(d*h-2)));let y=null;if(i?.renderFromZernike&&typeof a.generateZernikeRenderGrid=="function"){s&&console.time("⏱️ plotWavefrontSurface.generateZernikeRenderGrid");const A=i?.pupilSamplingMode&&i.pupilSamplingMode!=="finite";y=a.generateZernikeRenderGrid(i,g,"wavefront",{rhoMax:.99,useWavefrontMask:A}),s&&console.timeEnd("⏱️ plotWavefrontSurface.generateZernikeRenderGrid")}y&&y.x&&y.y&&y.z?m={type:"surface",x:y.x,y:y.y,z:y.z,colorscale:pt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:"Wλ [波長]"},flatshading:!1,lighting:{ambient:.85,diffuse:.85,specular:.03,roughness:.95,fresnel:.05}}:m=this.convertToPlotlySurfaceData(i,"wavefront",{rawMode:!1})}catch{m=this.convertToPlotlySurfaceData(i,"wavefront",{rawMode:!1})}const p={title:{text:`Wavefront Aberration (Wλ) Distribution - ${n.displayName||"Field Point"}`,font:{size:16}},scene:{xaxis:{title:"Pupil coordinate X",range:[-1.1,1.1],dtick:.5},yaxis:{title:"Pupil coordinate Y",range:[-1.1,1.1],dtick:.5},zaxis:{title:"Wavefront aberration [λ]"},camera:{eye:{x:1.5,y:1.5,z:1.5}}},margin:{l:0,r:0,b:0,t:40}};try{const d=this.resolveContainer();if(!d)throw new Error("プロットコンテナが見つかりません");console.log("✅ プロットコンテナ確認:",d);const u=this.resolvePlotly(d);if(!u)throw new Error("Plotly.jsライブラリが読み込まれていません");this.validatePlotlyData(m),console.log("✅ データ検証完了"),console.log("🎨 Plotly描画開始:",{container:d.id||"(element)",dataType:m.type,dataSize:`${m.z.length}x${m.z[0]?.length}`}),p.autosize=!0,s&&console.time("⏱️ plotWavefrontSurface.Plotly.newPlot"),await u.newPlot(d,[m],p,this.plotlyConfig),s&&console.timeEnd("⏱️ plotWavefrontSurface.Plotly.newPlot"),console.log("✅ 波面収差 3Dサーフェス描画完了")}catch(d){console.error("❌ 波面収差 3Dサーフェス描画エラー:",d),console.log("📊 問題のデータ:",{x:m.x?m.x.length:"undefined",y:m.y?m.y.length:"undefined",z:m.z?`${m.z.length}x${m.z[0]?.length}`:"undefined"});const u=this.createFallbackSurfaceData("wavefront"),h=this.resolveContainer(),g=this.resolvePlotly(h);h&&g&&(p.autosize=!0,await g.newPlot(h,[u],p,this.plotlyConfig)),console.log("⚠️ フォールバックデータで描画しました")}return this.displayStatistics(i.statistics.wavefront,"Optical Path Difference","λ"),console.log("✅ 波面収差 3Dサーフェスプロット生成完了"),i}catch(s){throw console.error("❌ 波面収差プロット生成エラー:",s),s}}async plotOPDHeatmap(t,n,o=.5876,r=31,s={}){try{console.log("🌊 OPD ヒートマップ生成開始...");const a=(N,f)=>{try{typeof s?.onProgress=="function"&&s.onProgress({percent:N,message:f,phase:"opd-render"})}catch{}},c=s?.opdDisplayMode||"pistonTiltRemoved",i=await this._computeNativeOpdWavefrontMap(t,n,r,{opdDisplayMode:c,onProgress:s?.onProgress||null});if(typeof globalThis<"u"&&globalThis.__WAVEFRONT_DEBUG_DUMP===!0&&console.log("🟦 wavefrontMap:",i),!i||!i.pupilCoordinates||i.pupilCoordinates.length===0)throw new Error("有効な光線データがありません。光学系設定を確認してください。");const l=Array.isArray(i?.display?.opdGrid)?i.display.opdGrid:[],m=Array.isArray(i?.raw?.opdGrid)?i.raw.opdGrid:[],p=Array.isArray(l)&&l.length&&Array.isArray(l[0])?l:m;if(!p.length||!Array.isArray(p[0]))throw new Error("Native OPD grid is unavailable for heatmap plotting");const d=p.length,u=d>1?Array.from({length:d},(N,f)=>-1+2*f/(d-1)):[0],h=p.map(N=>(Array.isArray(N)?N:[]).map(f=>f==null||!Number.isFinite(Number(f))?NaN:Number(f))),g={x:u,y:u,z:h,type:"heatmap",zsmooth:"best",colorscale:pt.getBlueGreenRedColorscale(),colorbar:{title:"OPD [λ]"}},y=g.x.length>0?[Math.min(...g.x)-.1,Math.max(...g.x)+.1]:[-1.1,1.1],A=g.y.length>0?[Math.min(...g.y)-.1,Math.max(...g.y)+.1]:[-1.1,1.1],x={title:{text:`Optical Path Difference (OPD) Heatmap - ${n.displayName||"Field Point"}`,font:{size:16}},xaxis:{title:"Pupil coordinate X",range:y,dtick:.5,constrain:"domain"},yaxis:{title:"Pupil coordinate Y",scaleanchor:"x",scaleratio:1,range:A,dtick:.5,constrain:"domain"},width:600,height:600,margin:{l:60,r:60,b:60,t:60}},_=this.resolveContainer(),S=this.resolvePlotly(_);if(!_||!S)throw new Error("Plotly.jsライブラリが読み込まれていません");x.autosize=!0,delete x.width,delete x.height,a(96,"Rendering OPD heatmap..."),await S.newPlot(_,[g],x,this.plotlyConfig),a(100,"Completed");{const N=i?.statistics?.display?.opdWavelengths?i.statistics.display.opdWavelengths:i?.statistics?.raw?.opdWavelengths||i?.statistics?.opdWavelengths;this.displayStatistics(N,"OPD","λ")}return console.log("✅ OPD ヒートマップ生成完了"),i}catch(a){throw console.error("❌ OPD ヒートマップ生成エラー:",a),a}}async plotWavefrontHeatmap(t,n,o=.5876,r=31){try{console.log("🌊 波面収差（Wλ）ヒートマップ生成開始...");const{analyzer:s}=this._createWavefrontEngine(t,o),a=typeof globalThis<"u"&&globalThis.__WAVEFRONT_DIAG_DISCONTINUITIES===!0,c=await s.generateWavefrontMap(n,r,"circular",{recordRays:!1,progressEvery:512,diagnoseDiscontinuities:a,diagTopK:5,opdMode:"simple",zernikeMaxNoll:37,renderFromZernike:!0});if(this._updateSystemDataWithZernike(s,c,37),!c||!c.pupilCoordinates||c.pupilCoordinates.length===0)throw new Error("有効な光線データがありません。光学系設定を確認してください。");const i=this.convertToPlotlyHeatmapData(c,"wavefront",r,{rawMode:!1}),l=i.x.length>0?[Math.min(...i.x)-.1,Math.max(...i.x)+.1]:[-1.1,1.1],m=i.y.length>0?[Math.min(...i.y)-.1,Math.max(...i.y)+.1]:[-1.1,1.1],p={title:{text:`Wavefront Aberration (Wλ) Heatmap - ${n.displayName||"Field Point"}`,font:{size:16}},xaxis:{title:"Pupil coordinate X",range:l,dtick:.5,constrain:"domain"},yaxis:{title:"Pupil coordinate Y",scaleanchor:"x",scaleratio:1,range:m,dtick:.5,constrain:"domain"},width:600,height:600,margin:{l:60,r:60,b:60,t:60}},d=this.resolveContainer(),u=this.resolvePlotly(d);if(!d||!u)throw new Error("Plotly.jsライブラリが読み込まれていません");return p.autosize=!0,delete p.width,delete p.height,await u.newPlot(d,[i],p,this.plotlyConfig),this.displayStatistics(c.statistics.wavefront,"Wavefront","λ"),console.log("✅ 波面収差（Wλ）ヒートマップ生成完了"),c}catch(s){throw console.error("❌ 波面収差（Wλ）ヒートマップ生成エラー:",s),s}}async plotMultiFieldComparison(t,n,o=.5876,r=16){try{console.log("🌊 マルチフィールド波面収差比較プロット生成開始...");const s=[],{analyzer:a}=this._createWavefrontEngine(t,o);for(const m of n){const p=await a.generateWavefrontMap(m,r,"circular",{recordRays:!1,progressEvery:512,opdMode:"simple",zernikeMaxNoll:37,renderFromZernike:!0}),d=this.convertToPlotlySurfaceData(p,"wavefront");d.name=m.displayName||`Field ${m.id}`,d.opacity=.8,s.push(d)}const c={title:{text:"Multi-field Wavefront Aberration Comparison",font:{size:16}},scene:{xaxis:{title:"Pupil coordinate X"},yaxis:{title:"Pupil coordinate Y"},zaxis:{title:"Wavefront aberration [λ]"},camera:{eye:{x:1.5,y:1.5,z:1.5}}},margin:{l:0,r:0,b:0,t:40}},i=this.resolveContainer(),l=this.resolvePlotly(i);if(!i||!l)throw new Error("Plotly.jsライブラリが読み込まれていません");c.autosize=!0,await l.newPlot(i,s,c,this.plotlyConfig),console.log("✅ マルチフィールド比較プロット生成完了")}catch(s){throw console.error("❌ マルチフィールド比較プロット生成エラー:",s),s}}async plotOPDMultiFieldComparisonRust(t,n,o=.5876,r=16){try{console.log("🌊 マルチフィールド OPD 比較プロット生成開始... (Rust)");const s=[];for(const l of n){const m=await this._computeNativeOpdWavefrontMap(t,l,r,{opdDisplayMode:"pistonTiltRemoved"}),p=m?.display?.opdsInWavelengths?{...m,opds:m.display.opds,opdsInWavelengths:m.display.opdsInWavelengths,wavefrontAberrations:m.display.wavefrontAberrations}:m,d=this.convertToPlotlySurfaceData(p,"opd",{rawMode:!0,fillHoles:!0});d.name=l?.displayName||`Field ${l?.id??""}`,d.opacity=.8,d.showscale=!1,s.push(d)}const a={title:{text:"Multi-field OPD Comparison",font:{size:16}},scene:{xaxis:{title:"Pupil coordinate X"},yaxis:{title:"Pupil coordinate Y"},zaxis:{title:"OPD [λ]"},camera:{eye:{x:1.5,y:1.5,z:1.5}}},margin:{l:0,r:0,b:0,t:40}},c=this.resolveContainer(),i=this.resolvePlotly(c);if(!c||!i)throw new Error("Plotly.jsライブラリが読み込まれていません");a.autosize=!0,await i.newPlot(c,s,a,this.plotlyConfig),console.log("✅ マルチフィールド OPD 比較プロット生成完了 (Rust)")}catch(s){throw console.error("❌ マルチフィールド OPD 比較プロット生成エラー:",s),s}}_median(t){const n=Array.isArray(t)?t.filter(s=>Number.isFinite(s)).slice():[],o=n.length;if(!o)return NaN;n.sort((s,a)=>s-a);const r=Math.floor(o/2);return o%2?n[r]:(n[r-1]+n[r])/2}_filterOutliersMAD(t,{label:n="values",madK:o=null,absMax:r=null,minScale:s=null}={}){try{if(!Array.isArray(t)||t.length===0)return t;const a=typeof globalThis<"u"?globalThis:null;if(a&&a.__WAVEFRONT_DISABLE_OUTLIER_FILTER===!0)return t;const c=t.filter(_=>Number.isFinite(_));if(c.length<16)return t;const i=this._median(c);if(!Number.isFinite(i))return t;const l=c.map(_=>Math.abs(_-i)),m=this._median(l),p=Number.isFinite(m)?1.4826*m:NaN,d=Number.isFinite(o)?Number(o):a&&Number.isFinite(a.__WAVEFRONT_OUTLIER_MAD_K)?Number(a.__WAVEFRONT_OUTLIER_MAD_K):8,u=Number.isFinite(r)?Number(r):a&&Number.isFinite(a.__WAVEFRONT_OUTLIER_ABS_MAX)?Number(a.__WAVEFRONT_OUTLIER_ABS_MAX):60,h=Number.isFinite(s)?Number(s):a&&Number.isFinite(a.__WAVEFRONT_OUTLIER_MIN_SCALE)?Number(a.__WAVEFRONT_OUTLIER_MIN_SCALE):1e-6,g=Number.isFinite(p)?Math.max(h,p):h,y=Math.max(h,g*Math.max(1,d));let A=0;const x=t.slice();for(let _=0;_<x.length;_++){const S=x[_];Number.isFinite(S)&&(Math.abs(S-i)>y||Math.abs(S)>u)&&(x[_]=NaN,A++)}return A>0&&console.log(`🧹 [WavefrontPlot] outlier filter (${n}): removed=${A}/${t.length}, median=${i.toFixed(4)}, mad=${Number.isFinite(m)?m.toFixed(4):"NaN"}, cutoff=${y.toFixed(4)}`),x}catch{return t}}convertToPlotlySurfaceData(t,n="wavefront",o={}){const{pupilCoordinates:r,wavefrontAberrations:s,opdsInWavelengths:a}=t,c=n==="opd"?a:s,i=!!o?.rawMode,l=o?.fillHoles!==void 0?!!o.fillHoles:!i,m=i&&n==="opd"?{label:`${n}:${i?"raw":"interp"}:surface`,madK:1e9,absMax:1e9}:{label:`${n}:${i?"raw":"interp"}:surface`},p=this._filterOutliersMAD(c,m),d=this._tryBuildRegularGrid(t,p,null,{fillHoles:l});if(d)return{type:"surface",x:d.x,y:d.y,z:d.z,connectgaps:!1,colorscale:pt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:n==="opd"?"OPD [λ]":"Wλ [波長]"}};const u=[],h=[];for(let M=0;M<p.length;M++)isFinite(p[M])&&!isNaN(p[M])&&(u.push(r[M]),h.push(p[M]));if(console.log(`📊 有効データ: ${h.length}/${p.length} 点 (${(h.length/p.length*100).toFixed(1)}%)`),h.length<4)return console.warn("⚠️ 有効データが少なすぎます。簡易データを生成します。"),this.createFallbackSurfaceData(n);const g=[...new Set(u.map(M=>M.x))].sort((M,C)=>M-C),y=[...new Set(u.map(M=>M.y))].sort((M,C)=>M-C);if(console.log(`📊 グリッドサイズ: X=${g.length}, Y=${y.length}`),i){const M=[];let C=0,D=0;for(let j=0;j<y.length;j++){const H=[];for(let ce=0;ce<g.length;ce++){const U=g[ce],G=y[j];if(Math.sqrt(U*U+G*G)>1+1e-9){H.push(null),D++;continue}const W=u.find(I=>Math.abs(I.x-U)<1e-10&&Math.abs(I.y-G)<1e-10);if(W){const I=u.indexOf(W);H.push(h[I]),C++}else H.push(null),D++}M.push(H)}console.log(`📊 [RawMode] grid cells: valid=${C}, null=${D}, total=${C+D}`);const B={type:"surface",x:g,y,z:M,connectgaps:!1,colorscale:pt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:n==="opd"?"OPD [λ]":"Wλ [波長]"}};return this._transposeZForPlotly(B)}let A=g,x=y;if(g.length<20||y.length<20){console.log("🔧 低密度データを補間用グリッドに拡張中...");const M=Math.min(...g),C=Math.max(...g),D=Math.min(...y),B=Math.max(...y),j=Math.max(32,Math.max(g.length,y.length)*2);A=[],x=[];for(let H=0;H<j;H++)A.push(M+(C-M)*H/(j-1)),x.push(D+(B-D)*H/(j-1));console.log(`📊 補間グリッドサイズ: X=${A.length}, Y=${x.length}`)}const _=[];let S=0,N=0;for(let M=0;M<x.length;M++){const C=[];for(let D=0;D<A.length;D++){const B=A[D],j=x[M];if(Math.sqrt(B*B+j*j)>1+1e-9){C.push(null),N++;continue}const ce=u.find(U=>Math.abs(U.x-B)<1e-10&&Math.abs(U.y-j)<1e-10);if(ce){const U=u.indexOf(ce);C.push(h[U]),S++}else{const U=this.interpolateValue(B,j,u,h);U!==null?(C.push(U),S++):(C.push(null),N++)}}_.push(C)}console.log(`📊 グリッドセル: 有効=${S}, null=${N}, 合計=${S+N}`);let f=1/0,w=-1/0,b=0;for(let M=0;M<_.length;M++){const C=_[M];if(Array.isArray(C))for(let D=0;D<C.length;D++){const B=C[D];B===null||!Number.isFinite(B)||(b++,B<f&&(f=B),B>w&&(w=B))}}b>0&&console.log(`📊 Z値範囲: ${f.toFixed(3)} ~ ${w.toFixed(3)}`);const v={type:"surface",x:A,y:x,z:_,connectgaps:!1,colorscale:pt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:n==="opd"?"OPD [λ]":"Wλ [波長]"}};return this._transposeZForPlotly(v)}validatePlotlyData(t){if(!t)throw new Error("データがありません");if(!t.x||!t.y||!t.z)throw new Error("x, y, z データが不足しています");if(!Array.isArray(t.x)||!Array.isArray(t.y)||!Array.isArray(t.z))throw new Error("x, y, z データが配列ではありません");if(t.x.length===0||t.y.length===0||t.z.length===0)throw new Error("データが空です");if(!Array.isArray(t.z[0]))throw new Error("z データが2次元配列ではありません");if(t.z.length!==t.y.length)throw new Error(`y座標数(${t.y.length})とz行数(${t.z.length})が一致しません`);if(t.z[0].length!==t.x.length)throw new Error(`x座標数(${t.x.length})とz列数(${t.z[0].length})が一致しません`);let n=0,o=0;for(let r=0;r<t.z.length;r++){const s=t.z[r];if(Array.isArray(s)){o+=s.length;for(let a=0;a<s.length;a++){const c=s[a];c!==null&&Number.isFinite(c)&&n++}}}if(n===0)throw new Error("有効なZ値がありません");console.log(`✅ データ検証完了: ${n}/${o} 有効値`)}_transposeZForPlotly(t){try{if(!t||!Array.isArray(t.z)||t.z.length===0||!Array.isArray(t.z[0]))return t;const n=t.z,o=n.length,r=Math.max(0,...n.map(a=>Array.isArray(a)?a.length:0));if(o===0||r===0)return t;const s=Array.from({length:r},(a,c)=>Array.from({length:o},(i,l)=>{const m=n[l];return Array.isArray(m)&&c<m.length?m[c]:null}));if(Array.isArray(t.x)&&Array.isArray(t.y)&&t.x.length!==t.y.length){const a=t.x;t.x=t.y,t.y=a}return t.z=s,t}catch{return t}}createFallbackSurfaceData(t){return{type:"surface",x:[-1,0,1],y:[-1,0,1],z:[[0,0,0],[0,0,0],[0,0,0]],colorscale:pt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:t==="opd"?"OPD [λ]":"Wλ [波長]"}}}convertToPlotlyHeatmapData(t,n="opd",o=31,r={}){const{pupilCoordinates:s,wavefrontAberrations:a,opdsInWavelengths:c,rayData:i}=t,l=n==="opd"?c:a,m=!!r?.rawMode,p=m&&n==="opd"?{label:`${n}:${m?"raw":"interp"}:heatmap`,madK:1e9,absMax:1e9}:{label:`${n}:${m?"raw":"interp"}:heatmap`},d=this._filterOutliersMAD(l,p),u=this._tryBuildRegularGrid(t,d,o,{fillHoles:!m});if(u){let b=0,v=0;try{const B=u.z;if(Array.isArray(B)){for(const j of B)if(Array.isArray(j))for(const H of j)v++,H!==null&&Number.isFinite(H)&&b++}}catch{}const M=v>0?b/v:0,C=!m&&M>=.85,D={type:"heatmap",x:u.x,y:u.y,z:u.z,zsmooth:m?!1:C?"best":!1,connectgaps:m?!1:C,colorscale:pt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:n==="opd"?"OPD [λ]":"Wλ [波長]"}};return this._transposeZForPlotly(D)}const h=[],g=[];for(let b=0;b<d.length;b++){const v=d[b],M=s[b];isFinite(v)&&!isNaN(v)&&M&&(h.push(M),g.push(v))}console.log(`📊 ヒートマップ有効データ: ${g.length}/${d.length} 点 (${(g.length/d.length*100).toFixed(1)}%)`);const y=[...new Set(h.map(b=>b.x))].sort((b,v)=>b-v),A=[...new Set(h.map(b=>b.y))].sort((b,v)=>b-v);if(console.log(`📊 ヒートマップグリッドサイズ: X=${y.length}, Y=${A.length}`),m){const b=[];for(let M=0;M<A.length;M++){const C=[];for(let D=0;D<y.length;D++){const B=h.find(j=>Math.abs(j.x-y[D])<1e-10&&Math.abs(j.y-A[M])<1e-10);if(B){const j=h.indexOf(B);C.push(g[j])}else C.push(null)}b.push(C)}const v={type:"heatmap",x:y,y:A,z:b,zsmooth:!1,connectgaps:!1,colorscale:pt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:n==="opd"?"OPD [λ]":"Wλ [波長]"}};return this._transposeZForPlotly(v)}const x=[];let _=0,S=0;for(let b=0;b<A.length;b++){const v=[];for(let M=0;M<y.length;M++){const C=h.find(D=>Math.abs(D.x-y[M])<1e-10&&Math.abs(D.y-A[b])<1e-10);if(C){const D=h.indexOf(C);v.push(g[D]),_++}else v.push(null),S++}x.push(v)}console.log(`📊 ヒートマップグリッドセル: 有効=${_}, null=${S}, 合計=${_+S}`);const f=(_+S>0?_/(_+S):0)>=.85,w={type:"heatmap",x:y,y:A,z:x,zsmooth:f?"best":!1,connectgaps:f,colorscale:pt.getBlueGreenRedColorscale(),showscale:!0,colorbar:{title:n==="opd"?"OPD [λ]":"Wλ [λ]"}};return this._transposeZForPlotly(w)}displayStatistics(t,n,o){const r=this.resolveContainer(),s=this.resolveStatsContainer(r);if(!s)return;const a=t?.pupilSamplingMode,c=t?.opdMode,i=t?.skipZernikeFit,l=a==="entrance"?'<div class="stats-note"><strong>Pupil sampling:</strong> entrance (best effort / vignetting possible)</div>':a==="stop"?'<div class="stats-note"><strong>Pupil sampling:</strong> stop (requires reaching stop surface)</div>':"",m=c?`<div class="stats-note"><strong>OPD mode:</strong> ${String(c)}</div>`:"",p=t?.opdDisplayMode,d=p?`<div class="stats-note"><strong>OPD display mode:</strong> ${String(p)}</div>`:"",u=t?.backend,h=u?`<div class="stats-note"><strong>Backend:</strong> ${String(u)}</div>`:"",g=t?.chiefReferenceMode,y=g?`<div class="stats-note"><strong>Chief reference mode:</strong> ${String(g)}</div>`:"",A=typeof i=="boolean"?`<div class="stats-note"><strong>Zernike fit:</strong> ${i?"OFF (raw)":"ON"}</div>`:"",x=!i&&Number.isFinite(t?.rawMean)?`<div class="stats-note"><strong>Raw mean (piston):</strong> ${Number(t.rawMean).toFixed(4)} ${o}</div>`:"",_=Array.isArray(t?.removeIndices)&&t.removeIndices.length?`<div class="stats-note"><strong>Stats removal (OSA):</strong> [${t.removeIndices.join(", ")}] (piston/tilt/defocus)</div>`:"",S=Number.isFinite(t?.wavelengthUm)?`<div class="stats-note"><strong>Wavelength:</strong> ${(t.wavelengthUm*1e3).toFixed(2)} nm (${t.wavelengthUm.toFixed(6)} μm)</div>`:"",N=Number.isFinite(t?.gridSize)?`<div class="stats-note"><strong>Grid size:</strong> ${Math.floor(t.gridSize)} × ${Math.floor(t.gridSize)}</div>`:"",f=(()=>{const j=Number.isInteger(t?.stopSurface)?Number(t.stopSurface):null,H=Number.isInteger(t?.targetSurface)?Number(t.targetSurface):null;if(j!==null||H!==null){const ce=[];return j!==null&&ce.push(`stop=${j}`),H!==null&&ce.push(`target=${H}`),`<div class="stats-note"><strong>Surfaces:</strong> ${ce.join(", ")}</div>`}return""})(),w=(()=>{const j=t?.usedObjectPosition,H=Number.isFinite(t?.usedObjectX)?Number(t.usedObjectX):null,ce=Number.isFinite(t?.usedObjectY)?Number(t.usedObjectY):null;if(j||H!==null||ce!==null){let U="";return j&&(U+=String(j)),H!==null&&(U+=` x=${H.toFixed(2)}`),ce!==null&&(U+=` y=${ce.toFixed(2)}`),`<div class="stats-note"><strong>Object:</strong> ${U.trim()}</div>`}return""})(),b=(()=>{const j=Number(t?.hitCount),H=Number(t?.sampleCount);if(Number.isFinite(j)&&Number.isFinite(H)&&H>0){const ce=(j/H*100).toFixed(1);return`<div class="stats-note"><strong>Ray hits:</strong> ${j}/${H} (${ce}%)</div>`}return""})(),v=(j,H=4)=>{const ce=Number(j);return Number.isFinite(ce)?ce.toFixed(H):"n/a"},M=Number(t?.mean),C=Math.abs(M),D=o==="λ"&&Number.isFinite(C)&&C>10?`<div class="stats-warning" style="color: #ff6b6b; margin-top: 8px;">
                ⚠️ <strong>Large piston (mean)</strong>: mean=${v(M,2)} ${o}<br>
                → Enable piston removal in the statistics to make the mean closer to 0.
               </div>`:"",B=`
            <div class="wavefront-statistics">
                <h4>${n} Statistics</h4>
                ${l}
                ${m}
                ${d}
                ${h}
                ${y}
                ${A}
                ${x}
                ${_}
                ${S}
                ${N}
                ${f}
                ${w}
                ${b}
                <div class="stats-grid">
                    <div><strong>Data points:</strong> ${Number.isFinite(Number(t?.count))?Number(t.count):"n/a"}</div>
                    <div><strong>Mean:</strong> ${v(t?.mean,4)} ${o}</div>
                    <div><strong>RMS:</strong> ${v(t?.rms,4)} ${o}</div>
                    <div><strong>Peak-to-Peak:</strong> ${v(t?.peakToPeak,4)} ${o}</div>
                    <div><strong>Min:</strong> ${v(t?.min,4)} ${o}</div>
                    <div><strong>Max:</strong> ${v(t?.max,4)} ${o}</div>
                </div>
                ${D}
            </div>
        `;s.innerHTML=B}interpolateValue(t,n,o,r){const s=typeof globalThis<"u"?globalThis:null,a=s&&Number.isFinite(s.__WAVEFRONT_INTERP_MAX_DIST)?Math.max(.01,Number(s.__WAVEFRONT_INTERP_MAX_DIST)):.12,c=s&&Number.isFinite(s.__WAVEFRONT_INTERP_MIN_NEIGHBORS)?Math.max(2,Math.floor(Number(s.__WAVEFRONT_INTERP_MIN_NEIGHBORS))):6,i=s&&Number.isFinite(s.__WAVEFRONT_INTERP_MAX_NEIGHBORS)?Math.max(c,Math.floor(Number(s.__WAVEFRONT_INTERP_MAX_NEIGHBORS))):12,l=[];for(let u=0;u<o.length;u++){const h=o[u],g=r[u];if(!h||!Number.isFinite(h.x)||!Number.isFinite(h.y)||!Number.isFinite(g))continue;const y=Math.hypot(h.x-t,h.y-n);l.push({index:u,value:g,distance:y})}if(l.length===0)return null;if(l.sort((u,h)=>u.distance-h.distance),l[0].distance<.01)return l[0].value;const m=[];for(const u of l)if(u.distance>a||(m.push(u),m.length>=i))break;if(m.length<c)return null;let p=0,d=0;for(const u of m){const h=1/(u.distance*u.distance+1e-6);p+=u.value*h,d+=h}return d>0?p/d:null}_tryBuildRegularGrid(t,n,o=null,r={}){try{const s=Number(t?.pupilRange),a=Number.isFinite(o)?Math.floor(o):Math.floor(Number(t?.gridSize));if(!Number.isFinite(s)||s<=0||!Number.isFinite(a)||a<2)return null;const c=[],i=[];for(let g=0;g<a;g++)c.push(g/(a-1)*2*s-s),i.push(g/(a-1)*2*s-s);const l=Array.from({length:a},()=>Array.from({length:a},()=>0)),m=Array.from({length:a},()=>Array.from({length:a},()=>0)),p=t?.pupilCoordinates||[],d=Math.min(p.length,n?.length||0);if(d===0)return null;const u=(a-1)/(2*s);for(let g=0;g<d;g++){const y=p[g],A=n[g];if(!y||!isFinite(y.x)||!isFinite(y.y)||!isFinite(A)||isNaN(A))continue;let x=Number.isInteger(y.ix)?y.ix:Math.round((y.x+s)*u),_=Number.isInteger(y.iy)?y.iy:Math.round((y.y+s)*u);x<0||x>=a||_<0||_>=a||Math.hypot(y.x,y.y)>s+1e-9||(l[_][x]+=A,m[_][x]+=1)}const h=Array.from({length:a},()=>Array.from({length:a},()=>null));for(let g=0;g<a;g++)for(let y=0;y<a;y++){const A=m[g][y];A>0&&(h[g][y]=l[g][y]/A)}if(r?.fillHoles){const g=s*.9;let y=0,A=0,x=0;for(let N=0;N<a;N++){const f=i[N];for(let w=0;w<a;w++){const b=c[w];Math.hypot(b,f)>g+1e-12||(A++,h[N][w]===null?y++:x++)}}const _=A>0?x/A:0,S=y>0&&_>=.6;if(!S&&y>0&&console.log(`🩹 [WavefrontPlot] surface hole-fill skipped (sparse): coreValidFrac=${_.toFixed(3)}, coreValid=${x}, coreNulls=${y}`),S){const f=(v=>{const M=v.map(j=>j.slice());let C=0,D=0;const B=2;for(let j=0;j<a;j++){const H=i[j];for(let ce=0;ce<a;ce++){if(M[j][ce]!==null)continue;const U=c[ce];if(Math.hypot(U,H)>g+1e-12)continue;let G=0,F=0,W=0;for(let I=1;I<=B;I++){for(let E=-I;E<=I;E++)for(let P=-I;P<=I;P++){if(P===0&&E===0)continue;const Z=ce+P,R=j+E;if(Z<0||Z>=a||R<0||R>=a)continue;const k=v[R][Z];if(k===null||!isFinite(k))continue;const te=1/(Math.hypot(P,E)+1e-6);G+=k*te,F+=te,W++}if(W>=6)break}W>=6&&F>0?(M[j][ce]=G/F,C++):D++}}return{out:M,filled:C,remaining:D}})(h),w=f.filled||0,b=f.remaining;w>0&&console.log(`🩹 [WavefrontPlot] surface hole-fill: coreNulls=${y}, filled=${w}, remaining=${b}`);for(let v=0;v<a;v++)for(let M=0;M<a;M++)h[v][M]=f.out[v][M]}}return{x:c,y:i,z:h}}catch{return null}}}async function $a(e="surface",t="wavefront",n=64,o=0,r={}){try{const s=r?.cancelToken||null,a=r?.onProgress||null,c=()=>{try{if(typeof localStorage>"u")return"";const N=ot(),f=N?.activeConfigId,w=Array.isArray(N?.configurations)?N.configurations.find(b=>String(b?.id)===String(f)):null;return w?`id=${w.id} name=${w.name||""}`.trim():f!=null?`id=${f}`:""}catch{return""}},i=window.getOpticalSystemRows?window.getOpticalSystemRows():null,l=window.getObjectRows?window.getObjectRows():[];try{const N=i?.[4],f=i?.[5],w=i?.[6],b=c()}catch{}if(!i||i.length===0)throw console.error("❌ 光学系データがありません"),new Error("光学系データがありません。JSONファイルをロードしてください。");if(!l||l.length===0){console.warn("⚠️ Objectデータがありません、軸上設定を使用");const N={id:1,xHeightAngle:0,yHeightAngle:0,position:"Angle"};l.push(N),o=0}(o<0||o>=l.length)&&(console.warn(`⚠️ 無効なObjectインデックス: ${o}, デフォルト(0)を使用`),o=0);const m=l[o],p=(()=>{try{if(typeof window<"u"&&typeof window.getPrimaryWavelength=="function"){const N=Number(window.getPrimaryWavelength());if(Number.isFinite(N)&&N>0)return N}}catch{}throw new Error("Primary wavelength is unavailable. Please set Source Primary Wavelength.")})(),d=(()=>{const f=i?.[0]?.thickness;return f==="INF"||f==="Infinity"||f===1/0})(),u=N=>{const f=typeof N=="number"?N:parseFloat(N);return Number.isFinite(f)?f:0},h=(N,f)=>{const w=String(N?.position??N?.Position??N?.type??"").toLowerCase(),b=u(N?.xHeightAngle),v=u(N?.yHeightAngle),M=w==="angle"||w==="field angle"||w==="angles"||w==="point",C=w==="rectangle"||w==="height";let D={x:0,y:0},B=0,j=0,H=N?.position??N?.type??"";M?(D={x:b,y:v},B=0,j=0,H="Angle"):C?(D={x:0,y:0},B=b,j=v,H="Rectangle"):d?(D={x:b,y:v},B=0,j=0,H="Angle"):(D={x:0,y:0},B=b,j=v,H="Rectangle");const ce=H==="Angle"?`(${D.x||0}°, ${D.y||0}°)`:`(${B||0}mm, ${j||0}mm)`;return{id:N?.id||f+1,displayName:`Object ${f+1} ${ce}`,type:H,fieldAngle:D,xHeight:B,yHeight:j,objectIndex:f,wavelength:p}},g=h(m,o),y=l.map((N,f)=>h(N,f)),A=new pt(r?.containerElement||"wavefront-container"),x=r?.opdDisplayMode||"pistonTiltRemoved",_=N=>{try{ys(N,{plotType:e,dataType:t,gridSize:n,selectedObjectIndex:o,wavelength:p,fieldSetting:g},window);try{if(typeof localStorage<"u"){const w=N?.statistics||null,b={at:new Date().toISOString(),from:"eva-wavefront-plot.js:storeLast",wavefront:{meta:ro(window),hasError:!!N?.error,error:N?.error?{message:String(N.error?.message||N.error||"Wavefront error").slice(0,600),code:N.error?.code||null}:null,statistics:w?{opdMicrons:w.opdMicrons?{rms:Number.isFinite(Number(w.opdMicrons.rms))?Number(w.opdMicrons.rms):null,peakToPeak:Number.isFinite(Number(w.opdMicrons.peakToPeak))?Number(w.opdMicrons.peakToPeak):null}:null,rawOpdMicrons:w.raw?.opdMicrons?{rms:Number.isFinite(Number(w.raw.opdMicrons.rms))?Number(w.raw.opdMicrons.rms):null,peakToPeak:Number.isFinite(Number(w.raw.opdMicrons.peakToPeak))?Number(w.raw.opdMicrons.peakToPeak):null}:null}:null}};try{const M=(window.lastWavefrontAnalyzer||null)?.opdCalculator||null,C=typeof M?.getLastRayCalculation=="function"?M.getLastRayCalculation():M?.lastRayCalculation??null;C&&(b.opdLastRay={success:C.success??null,error:C.error??null,fieldKey:C.fieldKey??null,pupilCoord:C.pupilCoord??null,stopHit:C.stopHit??null})}catch{}Ba(b)}}catch{}try{window.dispatchEvent(new CustomEvent("coopt:lastWavefrontMapUpdated",{detail:ro(window)}))}catch{}}catch{}};let S=null;switch(e){case"surface":if(t==="opd"){const N=await A.plotOPDSurface(i,g,p,n,{cancelToken:s,onProgress:a,opdDisplayMode:x,wasmFastOnly:r?.wasmFastOnly,enableHeavyDiagnostics:r?.enableHeavyDiagnostics});_(N),S=N}else{const N=await A.plotWavefrontAberrationSurface(i,g,p,n);_(N),S=N}break;case"heatmap":if(t==="opd"){const N=await A.plotOPDHeatmap(i,g,p,n,{cancelToken:s,onProgress:a,opdDisplayMode:x,wasmFastOnly:r?.wasmFastOnly,enableHeavyDiagnostics:r?.enableHeavyDiagnostics});_(N),S=N}else{const N=await A.plotWavefrontHeatmap(i,g,p,n);_(N),S=N}break;case"multifield":t==="opd"?await A.plotOPDMultiFieldComparisonRust(i,y,p,n):await A.plotMultiFieldComparison(i,y,p,n),S={success:!0};break;default:throw new Error(`未対応のプロットタイプ: ${e}`)}if(S?.error)throw new Error(String(S?.error?.message||S?.error||"Wavefront plot failed"));return console.log("✅ 波面収差図表示完了"),S||{success:!0}}catch(s){if(console.error("❌ 波面収差図表示エラー:",s),r?.showAlert!==!1&&alert(`波面収差図エラー: ${s?.message||s}`),r?.throwOnError===!0)throw s;return{error:{message:String(s?.message||s||"Wavefront plot failed")}}}}function Ua(){try{return typeof localStorage>"u"?null:ot()}catch{return null}}function Ha(e){if(!e||!Array.isArray(e.configurations))return null;const t=e.activeConfigId;return e.configurations.find(n=>n&&String(n.id)===String(t))||e.configurations[0]||null}async function Ga(){const e=typeof localStorage<"u"?Ua():null,t=e?Ha(e):null,n=e&&Array.isArray(e.systemRequirements)?e.systemRequirements:[],o=e&&Array.isArray(e.meritFunction)?e.meritFunction:[],r=t&&Array.isArray(t.opticalSystem)&&t.opticalSystem.length?t.opticalSystem:je(),s=t&&Array.isArray(t.object)&&t.object.length?t.object:Ke(),a=rt(),c=f=>{if(f===""||f===null||f===void 0)return null;const w=Number(f);return Number.isFinite(w)?w:null},i=f=>{const b=(f==null?"":String(f)).trim();return b===""?null:b},l=r.map((f,w)=>{const b=i(f?.type??f?.["object type"]??f?.object??f?._blockType??f?.surfType),M=i(f?.material??f?.glass)??"AIR",C=c(f?.radius),D=C===null?i(f?.radius):null,B=c(f?.semidia??f?.semiDiameter??f?.semiDia??f?.semi_diameter);return{surf:w,surf1:w+1,type:b,radius:C,radiusRaw:D,thickness:c(f?.thickness),material:M,isAir:M==="AIR",semidia:B,conic:c(f?.conic)??0,isImage:b?b.toLowerCase().includes("image"):!1,isObject:b?b.toLowerCase().includes("object"):!1}}),m=(f,w=1200)=>{const b=f==null?"":String(f);return b.length>w?b.slice(0,w)+"…":b},p=f=>{const w=f&&f.name?String(f.name):"Error",b=f&&f.message?String(f.message):String(f||"Unknown error"),v=f&&f.stack?String(f.stack):null;return{name:w,message:m(b,600),stack:v?m(v,1600):null}},d=(()=>{const f=[],w=Array.isArray(r)?r:[],b=Array.isArray(a)?a:[],v=Array.isArray(s)?s:[];w.length===0&&f.push({kind:"fatal",code:"NO_SURFACES",message:"optical system has 0 surfaces"}),b.length===0&&f.push({kind:"warn",code:"NO_SOURCES",message:"source table is empty"}),v.length===0&&f.push({kind:"warn",code:"NO_OBJECTS",message:"object table is empty"});for(let C=0;C<w.length;C++){const D=w[C]||{},B=c(D.thickness);if(B!==null&&B<0){f.push({kind:"warn",code:"NEGATIVE_THICKNESS",surf:C,message:`negative thickness at surf ${C}`});break}}const M=w.findIndex(C=>String(C?.type??"").toLowerCase().includes("stop"));return M>=0&&c(w[M]?.semidia??w[M]?.semiDiameter??w[M]?.semiDia)===null&&f.push({kind:"warn",code:"STOP_SEMIDIA_NULL",surf:M,message:"stop surface semidia is null/non-numeric"}),f})(),u={version:1,timestamp:new Date().toISOString(),inputs:{surfaceCount:Array.isArray(r)?r.length:0,sourceCount:Array.isArray(a)?a.length:0,objectCount:Array.isArray(s)?s.length:0},preflightIssues:d,steps:[]},h=(()=>{const f=Array.isArray(a)?a:[],w=b=>{const v=b==null?"":String(b).trim().toLowerCase();return v==="true"||v==="yes"||v==="1"||v==="primary"};for(const b of f)if(b&&(w(b.primary)||b.isPrimary===!0)){const v=c(b.wavelength);if(v!==null&&v>0)return v}for(const b of f){const v=c(b?.wavelength);if(v!==null&&v>0)return v}return .5875618})();u.inputs.primaryWavelengthUm=h;let g={},y=null,A=null;try{y=On(r,a,s),u.steps.push({step:"paraxial",ok:!0})}catch(f){u.steps.push({step:"paraxial",ok:!1,error:p(f)})}try{A=hs(r,h,s),u.steps.push({step:"seidel",ok:!0})}catch(f){u.steps.push({step:"seidel",ok:!1,error:p(f)})}u.steps.some(f=>f&&f.ok===!1)?(console.warn("AI Context: Failed to calculate performance metrics",u),g={error:"Calculation failed",diagnostics:u}):g={focalLength:y?.focalLength??null,fNumber:y?.fNumber??null,magnification:y?.magnification??null,seidel:{spherical:A?.sumS1??null,coma:A?.sumS2??null,astigmatism:A?.sumS3??null,fieldCurvature:A?.sumS4??null,distortion:A?.sumS5??null,lca:A?.sumLch??null,tca:A?.sumTch??null},diagnostics:u};const _=(()=>{const f=typeof globalThis<"u"?globalThis:null;if(!f)return null;const w={};try{const b=ro(f)||null,v=bs(f)||null;if(b||v){const B=v&&v.error?{message:m(v.error.message||v.error||"Wavefront error",600),code:v.error.code||null,details:v.error}:null,j=v&&v.statistics?{opdMicrons:v.statistics.opdMicrons?{rms:c(v.statistics.opdMicrons.rms),peakToPeak:c(v.statistics.opdMicrons.peakToPeak)}:null,rawOpdMicrons:v.statistics.raw?.opdMicrons?{rms:c(v.statistics.raw.opdMicrons.rms),peakToPeak:c(v.statistics.raw.opdMicrons.peakToPeak)}:null}:null;w.wavefront={meta:b?{source:b.source??null,gridSize:b.gridSize??null,wavelength:b.wavelength??null,fieldSetting:b.fieldSetting??null}:null,hasError:!!B,error:B,statistics:j}}const C=(f.lastWavefrontAnalyzer||null)?.opdCalculator||null,D=typeof C?.getLastRayCalculation=="function"?C.getLastRayCalculation():C?.lastRayCalculation??null;D&&(w.opdLastRay={success:D.success??null,error:D.error??null,fieldKey:D.fieldKey??null,pupilCoord:D.pupilCoord??null,stopHit:D.stopHit??null})}catch{}try{const b=f.lastPsfError||null;b&&(w.psfError={at:b.at??null,code:b.code??null,message:m(b.message??b.rawMessage??"PSF error",700),hint:m(b.hint??"",300)||null,wavelength:b.wavelength??null,gridSize:b.gridSize??null,objectIndex:b.objectIndex??null,debugMode:b.debugMode??null});const v=f.lastPsfResult||null;if(v){const M=v.metrics||v.characteristics||null;w.psfLastResult={wavelength:v.wavelength??f.lastPsfWavelength??null,gridSize:v.gridSize??f.lastPsfGridSize??null,calculationTime:v.calculationTime??null,hasMetrics:!!M,metricKeys:M?Object.keys(M).slice(0,30):[]}}}catch{}try{if(typeof localStorage<"u"){try{const b=La();if(b&&typeof b=="object"){if(!w.wavefront&&b.wavefront&&typeof b.wavefront=="object"){const v=b.wavefront;w.wavefront={meta:v.meta??null,hasError:!!v.hasError,error:v.error??null,statistics:v.statistics??null,from:b.from??"localStorage:lastWavefrontSnapshot"}}!w.opdLastRay&&b.opdLastRay&&typeof b.opdLastRay=="object"&&(w.opdLastRay={success:b.opdLastRay.success??null,error:b.opdLastRay.error??null,fieldKey:b.opdLastRay.fieldKey??null,pupilCoord:b.opdLastRay.pupilCoord??null,stopHit:b.opdLastRay.stopHit??null,from:b.from??"localStorage:lastWavefrontSnapshot"})}}catch{}if(!w.psfLastResult){const b=za();b&&typeof b=="object"&&(w.psfLastResult={wavelength:b.wavelength??null,gridSize:b.gridSize??null,calculationTime:b.calculationTime??null,hasMetrics:!!b.hasMetrics,metricKeys:Array.isArray(b.metricKeys)?b.metricKeys.slice(0,30):[],psfMethod:b.psfMethod??null,performanceMode:b.performanceMode??null,zernikeFitSamplingSize:b.zernikeFitSamplingSize??null,objectIndex:b.objectIndex??null,psfSummary:b.psfSummary??null,at:b.at??null,from:"localStorage:lastPsfMeta"})}if(!w.psfError){const b=Wa();b&&typeof b=="object"&&(w.psfError={at:b.at??null,code:b.code??null,message:m(b.message??b.rawMessage??"PSF error",700),hint:m(b.hint??"",300)||null,wavelength:b.wavelength??null,gridSize:b.gridSize??null,objectIndex:b.objectIndex??null,debugMode:b.debugMode??null,from:"localStorage:lastPsfError"})}}}catch{}try{const b=typeof document<"u"?document:null;if(b){const v=b.getElementById("psf-object-select"),M=b.getElementById("psf-sampling-select"),C=b.getElementById("psf-zernike-sampling-select"),D=b.getElementById("psf-performance-select"),B=b.getElementById("psf-wasm-status");w.psfUI={objectIndex:v?.value??null,samplingSize:M?.value??null,zernikeFitSamplingSize:C?.value??null,performanceMode:D?.value??null,wasmStatusText:B?.textContent?m(B.textContent,200):null}}}catch{}try{const b=f.__lastSurfaceEdit||null;if(b){const v=b.row||{};w.lastUserSurfaceEdit={field:b.field??null,oldValue:b.oldValue??null,newValue:b.newValue??null,rowSummary:{type:i(v?.type??v?.surfType),radius:i(v?.radius),thickness:i(v?.thickness),material:i(v?.material??v?.glass),semidia:i(v?.semidia??v?.semiDiameter)}}}}catch{}return Object.keys(w).length?w:null})(),S=(()=>{const f=Array.isArray(n)?n:[],w=f.filter(v=>v&&typeof v=="object").filter(v=>v.enabled===void 0||v.enabled===null?!0:!!v.enabled).slice(0,60).map(v=>({id:v.id??null,enabled:v.enabled===void 0||v.enabled===null?!0:!!v.enabled,operand:v.operand??null,op:v.op??null,tol:c(v.tol),target:c(v.target),weight:c(v.weight)??1,configId:v.configId===void 0||v.configId===null?null:String(v.configId),rationale:i(v.rationale),current:c(v.current),status:i(v.status)})),b=(Array.isArray(o)?o:[]).filter(v=>v&&typeof v=="object").slice(0,40).map(v=>({id:v.id??null,operand:v.operand??null,target:c(v.target),weight:c(v.weight),configId:v.configId===void 0||v.configId===null?null:String(v.configId)}));return{requirements:{count:f.length,enabledCount:w.length,rows:w},meritFunction:{count:Array.isArray(o)?o.length:0,preview:b}}})(),N=(()=>{const f=(H,ce)=>{const U=Number(H),G=Number(ce);return!Number.isFinite(U)||!Number.isFinite(G)||G===0?null:U/G},w=g?.focalLength,b=g?.fNumber,v=S?.requirements?.rows,M=Array.isArray(v)?v.find(H=>String(H?.operand||"").trim()==="FL")?.target??null:null,C=Array.isArray(v)?v.find(H=>String(H?.operand||"").trim()==="FNO_WRK")?.target??v.find(H=>String(H?.operand||"").trim()==="FNO_IMG")?.target??v.find(H=>String(H?.operand||"").trim()==="FNO_OBJ")?.target??null:null,D=M!==null?f(w,M):null,B=C!==null?f(b,C):null,j=H=>H===null?null:(H-1)*100;return{focalLength:{current:typeof w=="number"?w:null,target:typeof M=="number"?M:null,ratioToTarget:D,percentError:j(D)},fNumber:{current:typeof b=="number"?b:null,target:typeof C=="number"?C:null,ratioToTarget:B,percentError:j(B)}}})();return{meta:{surfIndexing:"0-based",surfaceCount:l.length,activeConfig:t?{id:t.id??null,name:t.name??null,activeScenarioId:t.activeScenarioId??null}:null},system:l,designIntent:t&&Array.isArray(t.blocks)?{blockCount:t.blocks.length,blocks:t.blocks.slice(0,80).map(f=>({blockId:f?.blockId??null,blockType:f?.blockType??null,parameters:{glass:i(f?.parameters?.glass??f?.parameters?.material)??null,radius:c(f?.parameters?.radius),thickness:c(f?.parameters?.thickness),semiDiameter:c(f?.parameters?.semiDiameter)},variables:f?.variables?Object.keys(f.variables).slice(0,30):[]}))}:null,sources:a.map(f=>({id:c(f?.id),wavelength:c(f?.wavelength),weight:c(f?.weight),primary:i(f?.primary),angle:c(f?.angle)})),objects:s.map(f=>({id:c(f?.id),position:i(f?.position),xHeightAngle:c(f?.xHeightAngle),yHeightAngle:c(f?.yHeightAngle),angle:c(f?.angle)})),targets:S,normalizedPerformance:N,performance:g,runtimeDiagnostics:_,timestamp:new Date().toISOString()}}const yi="ai_assistant_config",co="ai_assistant_history",hi={provider:"gemini",apiKey:"",model:""};let Ye={...hi},ct=[],tr=!1,wn=null,bi=1,Kn=!1;const wi=6,Wn=12e4,vi=18e4;try{typeof window<"u"&&(window.__COOPT_AI_ASSISTANT_VERSION||(window.__COOPT_AI_ASSISTANT_VERSION="co-opt 1.9.6 (ai-assistant) 2026-01-05"),typeof window.DEFAULT_AI_REQUEST_TIMEOUT_MS>"u"&&(window.DEFAULT_AI_REQUEST_TIMEOUT_MS=Wn),typeof window.DEFAULT_AI_STREAM_TIMEOUT_MS>"u"&&(window.DEFAULT_AI_STREAM_TIMEOUT_MS=vi))}catch{}function $n(e,t={},n=Wn){const o=new AbortController,r=setTimeout(()=>{try{o.abort()}catch{}},n),s={...t,signal:o.signal};return fetch(e,s).catch(a=>{throw String(a?.name||"")==="AbortError"?new Error(`Request timed out after ${n}ms`):a}).finally(()=>{try{clearTimeout(r)}catch{}})}let nr=!1;function Va(){if(nr)return;nr=!0;const e=(t,n)=>{const o=n&&typeof n=="object"&&"message"in n?String(n.message):String(n),r=n&&typeof n=="object"&&"stack"in n?String(n.stack):"",s=[`❌ ${t}: ${o}`,r?`
${r}`:""].join("");try{document.getElementById("ai-chat-history")&&Cn("assistant",s)}catch{}try{console.error(`[AI Assistant] ${t}:`,n)}catch{}};try{window.addEventListener("error",t=>{e("Unhandled error",t?.error||t?.message||t)}),window.addEventListener("unhandledrejection",t=>{e("Unhandled promise rejection",t?.reason||t)})}catch{}}const or=`あなたは「co-opt」（ブラウザで動作する光学設計・解析ツール）の熟練した光学設計コンサルタントです。
ユーザーの目的に合わせて、設計・解析・最適化の方針を日本語で簡潔に提案してください。

重要:
- co-opt の面番号は UI 上は Surf 0..N-1 の 0-index です（context.system[].surf がそれ）。
- context.system[].surf1 は参考用の 1-index です。面番号の混同を避けてください。
- 最終行が像面（Image）で thickness が null/空でも、それ自体は必ずしもエラーではありません。
    エラー断定はせず、必要なら「どの計算が失敗したか」「どの入力が欠けているか」を確認する質問をしてください。
        - もし context.performance.error があり、かつ context.performance.diagnostics がある場合は、それを最優先の根拠にしてください。
            特に diagnostics.steps（paraxial / seidel 等）の ok=false と error.message/stack を引用し、「どの計算が失敗したか」を明確に述べてください。
            diagnostics.preflightIssues がある場合は、推測ではなくその指摘（例: NEGATIVE_THICKNESS 等）に沿って確認手順を提案してください。
        - もし diagnostics.steps がすべて ok=true なのにユーザーが「計算が失敗する」と言っている場合、原因は paraxial/seidel 以外（例: Wavefront/OPD/PSF/Spot/最適化）にある可能性が高いです。
            その場合は context.runtimeDiagnostics（wavefront/opdLastRay/psfError など）があれば必ず引用し、どの機能が失敗しているかを切り分けてください。
- context.system[].material が "AIR" の行は「空気（材料指定なし）」として正常です。ガラス未指定と断定しないでください。
- Design Intent（blocks）上の material 系キーはガラス名の文字列です（例: Lens.material, Doublet.material1/material2, Triplet.material1/material2/material3）。
    - これらは数値ではなく離散(categorical)変数として Optimize(V) 対象にできます。"non-numeric" だからといって最適化不可とは限りません。
    - ただし Lens/Doublet/Triplet の material* に "AIR" を提案・設定しないでください（空気はレンズ材として不正）。
    - 材料変更は set_block_param で parameters 側（key=material/material1/material2/material3）を更新してください。
- object は height ではなく xHeightAngle / yHeightAngle（フィールド定義）を主に使います。
- 口径は context.system[].semidia（光学系テーブルの semidia 列）です。
- 半径は context.system[].radius が数値、数値でない場合は context.system[].radiusRaw（例: "INF"）になります。null=欠落と決めつけないでください。

設計変更について:
- Design Intent（blocks）を最優先に維持してください。基本はブロックパラメータ更新→展開（Expanded Optical System）です。
- ユーザーが「〜を〜に変更」「set_*」「Surf/面番号指定」「semidia/semiDiameter/radius/thickness/material 等の値変更」を求めた場合は、必ずツール関数を呼んで“実際に反映”してください。文章だけで「変更します」と宣言して終わらないでください。
- blockId が不明でも、Surf（0-index）が指定されていれば set_block_param の surf を使って対象ブロックを解決できます。
- blocks が存在する場合は set_block_param を必ず優先し、set_surface_field は「blocks が無い場合のみ」のフォールバックとして使ってください。
- 「全てのconfig / all configurations」に同じ変更を適用したい場合は、set_block_param に applyToAllConfigs=true を指定してください（blockId が config 間で一致しない場合があるので、surf 指定が安全です）。
- 変更できない/失敗した場合は、理由を1行で述べ、次に必要な情報を最大1つだけ質問してください。
- 変更しない場合は、適用可能な具体値を含む提案（または ACTION_PLAN_JSON）を返してください。

出力:
- まず「現状のボトルネック（根拠=contextの値）」→「修正方針」→「具体的な数値案」の順で。
- context.performance.diagnostics がある場合、回答冒頭に「診断要約」を2〜6行で書いてください。
`,rr="ACTION_PLAN_JSON",Si="COOPT_AI_REFRESH_UI";let ir=!1;function qa(){if(!ir){ir=!0;try{window.addEventListener("message",e=>{try{const t=e?.data;if(!t||typeof t!="object"||t.type!==Si)return;xi(window)}catch{}})}catch{}}}async function xi(e){const t=e;if(t){try{const n=t.ConfigurationManager,o=n&&typeof n.loadActiveConfigurationToTables=="function"?n.loadActiveConfigurationToTables:typeof t.loadActiveConfigurationToTables=="function"?t.loadActiveConfigurationToTables:null;o&&await o.call(n||t,{applyToUI:!0,suppressOpticalSystemDataChanged:!0})}catch{}try{Ut(t)}catch{}try{t.meritFunctionEditor&&typeof t.meritFunctionEditor.calculateMerit=="function"&&t.meritFunctionEditor.calculateMerit()}catch{}try{t.systemRequirementsEditor&&typeof t.systemRequirementsEditor.scheduleEvaluateAndUpdate=="function"&&t.systemRequirementsEditor.scheduleEvaluateAndUpdate()}catch{}}}function Za(e){const t=String(e||"");return[/\bset_(block_param|surface_field)\b/i,/\bSurf\s*\d+\b/i,/semidia|semi\s*dia(meter)?/i,/\b(radius|thickness|material|glass|conic|coef)\b/i,/変更|変(更|えて|える|えたい)/].some(o=>o.test(t))}function Ya(e,t){const n=Number(t);if(!Number.isInteger(n)||n<=0)return null;const o=e?.blocks;if(!Array.isArray(o)||o.length===0)return null;const s=o.filter(c=>c&&(c.blockType==="Lens"||c.blockType==="PositiveLens"))[n-1];return(typeof s?.blockId=="string"?s.blockId.trim():"")||null}async function Xa(e){const t=String(e||"");if(!/(非球面|aspher(e|ic))/i.test(t))return null;const n=/(全(て)?|全部|すべて|all)\s*(system\s*)?config/i.test(t),o=t.match(/(?:\bLens\b|レンズ)\s*[-#]?\s*(\d+)\s*(?:の)?\s*R\s*([12])\b/i);if(!o)return null;const r=Number(o[1]),s=Number(o[2]);if(!Number.isInteger(r)||r<=0||!(s===1||s===2))return null;const a=Yt(),c=Xt(a);if(!a||!c||!Array.isArray(c.blocks)||c.blocks.length===0)return null;const i=s===1?"front":"back",l=p=>{try{const d=Ya(p,r);if(!d)return{ok:!1,configId:p?.id,error:`Lens-${r} not found in blocks`};const u=dn(p.blocks),h=Array.isArray(u)?u.find(x=>x&&String(x.blockId)===String(d)):null;if(!h)return{ok:!1,configId:p?.id,error:`blockId not found: ${d}`};et(h.parameters)||(h.parameters={}),h.parameters[`${i}SurfType`]="Aspheric even",h.parameters[`${i}Conic`]=0,p.blocks=u;const g=Co(p,a),y=Array.isArray(p?.opticalSystem)?p.opticalSystem:null,A=$e(p.blocks);return A&&Array.isArray(A.rows)&&(y&&y.length>0&&Un(A.rows,y),g!==void 0&&A.rows[0]&&typeof A.rows[0]=="object"&&(A.rows[0].thickness=g),p.opticalSystem=A.rows),{ok:!0,configId:p?.id,blockId:d}}catch(d){return{ok:!1,configId:p?.id,error:String(d?.message||d)}}};if(n){const p=[];for(const h of a.configurations||[])p.push(l(h));await Mt(a);const d=p.filter(h=>h.ok).length,u=p.length-d;return d>0&&u===0?`全configの Lens-${r} の R${s}（${i}）を非球面（Aspheric even）に設定しました。`:`全configへ適用: ok=${d}, ng=${u}。Lens-${r} が存在しないconfigがある可能性があります。`}return l(c).ok?(await Mt(a),`Lens-${r} の R${s}（${i}）を非球面（Aspheric even）に設定しました。`):`⚠️ Lens-${r} を blocks から特定できませんでした。`}function Ka(){tr||(tr=!0,Qa(),tc(),oc(),ic(),ac(),cc(),Ja(),qa(),Va())}function Ja(){const e=document.getElementById("ai-assistant-panel");if(!e)return;const t=e.querySelector(".ai-panel-header");if(!(t instanceof HTMLElement))return;let n=!1,o=0,r=0,s=0,a=0,c=0,i=0,l=null;const m=(h,g,y)=>Math.max(g,Math.min(y,h)),p=h=>{if(!(h instanceof PointerEvent)||h.button!==0)return;const g=h.target;if(g instanceof HTMLElement&&(g.closest(".ai-panel-controls")||g.tagName==="BUTTON"))return;const y=e.getBoundingClientRect();c=y.width,i=y.height,e.style.right="auto",e.style.bottom="auto",e.style.left||(e.style.left=`${y.left}px`),e.style.top||(e.style.top=`${y.top}px`),s=Number.parseFloat(e.style.left)||y.left,a=Number.parseFloat(e.style.top)||y.top,o=h.clientX,r=h.clientY,n=!0,l=h.pointerId;try{t.setPointerCapture(l)}catch{}h.preventDefault()},d=h=>{if(!(h instanceof PointerEvent)||!n||l!==null&&h.pointerId!==l)return;const g=h.clientX-o,y=h.clientY-r,A=Math.max(0,window.innerWidth-c),x=Math.max(0,window.innerHeight-i),_=m(s+g,0,A),S=m(a+y,0,x);e.style.left=`${_}px`,e.style.top=`${S}px`},u=h=>{if(h instanceof PointerEvent&&n&&!(l!==null&&h.pointerId!==l)){n=!1;try{t.releasePointerCapture(l)}catch{}l=null}};t.addEventListener("pointerdown",p),t.addEventListener("pointermove",d),t.addEventListener("pointerup",u),t.addEventListener("pointercancel",u)}function Qa(){const e=localStorage.getItem(yi);if(e)try{Ye={...hi,...JSON.parse(e)}}catch(t){console.error("Failed to parse AI config",t)}}function ec(){localStorage.setItem(yi,JSON.stringify(Ye))}function tc(){const e=localStorage.getItem(co);if(e)try{const t=JSON.parse(e),n=nc(t);ct=n.history,bi=n.nextId}catch(t){console.error("Failed to parse AI history",t)}}function nc(e){const t=Array.isArray(e)?e:[];let n=0;for(const s of t){const a=Number(s?.id);Number.isFinite(a)&&a>n&&(n=a)}const o=new Set,r=[];for(const s of t){if(!s||typeof s!="object")continue;let a=Number(s.id);(!Number.isFinite(a)||a<=0||o.has(a))&&(n+=1,a=n),o.add(a);const c=s.role==="assistant"||s.role==="user"||s.role==="system"?s.role:"assistant",i=s.content===void 0||s.content===null?"":String(s.content),l=Number(s.timestamp),m=Array.isArray(s.actions)?s.actions:null;r.push({id:a,role:c,content:i,timestamp:Number.isFinite(l)?l:Date.now(),actions:m})}return{history:r,nextId:Math.max(1,n+1)}}function Ro(){try{localStorage.setItem(co,JSON.stringify(ct));return}catch(e){try{console.warn("[AI Assistant] saveHistory failed; pruning history",e)}catch{}}try{Array.isArray(ct)&&ct.length>200&&(ct=ct.slice(-200)),localStorage.setItem(co,JSON.stringify(ct))}catch(e){try{console.warn("[AI Assistant] saveHistory retry failed; disabling persistence for this session",e)}catch{}}}function oc(){if(document.getElementById("ai-assistant-btn"))return;const e=document.getElementById("optimize-design-intent-btn"),t=e?.parentElement||document.querySelector(".top-buttons-row");if(!t)return;const n=document.createElement("button");n.id="ai-assistant-btn",n.textContent="🤖 AI Assistant",n.title="Open AI Optical Consultant",n.onclick=()=>{rc()||Ni()},e&&e.parentElement===t?t.insertBefore(n,e.nextSibling):t.appendChild(n)}function rc(){try{if(wn&&!wn.closed)return wn.focus(),!0;const e=Math.min(720,Math.max(520,Math.floor(window.innerWidth*.45))),t=Math.min(900,Math.max(640,Math.floor(window.innerHeight*.8))),n=Math.max(0,Math.floor((window.screenX||0)+(window.outerWidth-e)/2)),o=Math.max(0,Math.floor((window.screenY||0)+(window.outerHeight-t)/2)),r=[`width=${e}`,`height=${t}`,`left=${n}`,`top=${o}`,"resizable=yes","scrollbars=yes"].join(","),s=window.open("","coopt-ai-assistant",r);if(!s||!s.document){try{s?.close()}catch{}return!1}wn=s;const c=`<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <base href="${new URL(".",window.location.href).toString()}" />
    <title>AI Assistant</title>
    <link rel="stylesheet" href="styles.css" />
</head>
<body>
    <script>window['__AI_ASSISTANT_MODE'] = 'popup';<\/script>
    <script type="module">
        import { initAIAssistantPopup } from './ai-assistant.ts';
        initAIAssistantPopup();
    <\/script>
</body>
</html>`;try{s.document.open()}catch{}try{s.document.write(c)}catch{}try{s.document.close()}catch{}try{s.focus()}catch{}return!0}catch(e){return console.warn("Failed to open AI Assistant popup:",e),!1}}function ic(){document.body.insertAdjacentHTML("beforeend",`
        <div id="ai-settings-modal" class="modal">
            <div class="modal-content">
                <span class="close-modal">&times;</span>
                <h2>AI Assistant Settings</h2>
                <div class="form-group">
                    <label for="ai-provider">Provider:</label>
                    <select id="ai-provider">
                        <option value="gemini">Google Gemini</option>
                        <option value="openai">OpenAI</option>
                        <option value="anthropic">Anthropic</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="ai-model-select">Model:</label>
                    <select id="ai-model-select"></select>
                    <input type="text" id="ai-model-custom" placeholder="Custom model id" style="display:none; margin-top:6px;">
                </div>
                <div class="form-group">
                    <label for="ai-api-key">API Key:</label>
                    <input type="password" id="ai-api-key" placeholder="Enter your API Key">
                </div>
                <div class="modal-actions">
                    <button id="save-ai-settings-btn">Save Settings</button>
                </div>
            </div>
        </div>
    `)}function sc(e){const t=String(e||"").trim();return t==="openai"?[{value:"",label:"(Auto)"},{value:"gpt-4o-mini",label:"gpt-4o-mini"},{value:"gpt-4o",label:"gpt-4o"},{value:"__custom__",label:"Custom…"}]:t==="anthropic"?[{value:"",label:"(Auto)"},{value:"claude-3-5-sonnet-latest",label:"claude-3-5-sonnet-latest"},{value:"claude-3-5-haiku-latest",label:"claude-3-5-haiku-latest"},{value:"__custom__",label:"Custom…"}]:[{value:"",label:"(Auto: ListModels)"},{value:"gemini-1.5-flash",label:"gemini-1.5-flash"},{value:"gemini-1.5-pro",label:"gemini-1.5-pro"},{value:"gemini-2.0-flash",label:"gemini-2.0-flash"},{value:"__custom__",label:"Custom…"}]}function sr({provider:e,options:t,desiredModel:n}){const o=document.getElementById("ai-model-select"),r=document.getElementById("ai-model-custom");if(!(o instanceof HTMLSelectElement)||!(r instanceof HTMLInputElement))return;const s=Array.isArray(t)?t:[];o.innerHTML=s.map(l=>`<option value="${String(l.value)}">${_i(String(l.label))}</option>`).join("");const a=String(n||"").trim(),c=a!==""&&s.some(l=>String(l.value)===a);a===""?(o.value="",r.style.display="none",r.value=""):c?(o.value=a,r.style.display="none",r.value=""):(o.value="__custom__",r.style.display="",r.value=a),o.value==="__custom__"?(r.style.display="",r.value||(r.value=a)):r.style.display="none"}async function ar({provider:e,apiKey:t,desiredModel:n}){const o=String(e||"").trim();if(sr({provider:o,options:sc(o),desiredModel:n}),o==="gemini"){const r=String(t||"").trim();if(!r)return;try{const a=(await Ri(r)).map(i=>String(i).replace(/^models\//,"")).filter(Boolean),c=[{value:"",label:"(Auto: ListModels)"}].concat(a.slice(0,60).map(i=>({value:i,label:i}))).concat([{value:"__custom__",label:"Custom…"}]);sr({provider:o,options:c,desiredModel:n})}catch{}}}function ac(){try{if(document.getElementById("ai-assistant-panel")){kn();return}}catch{}document.body.insertAdjacentHTML("beforeend",`
        <div id="ai-assistant-panel" class="ai-panel" style="display: none;">
            <div class="ai-panel-header">
                <h3>AI Optical Consultant</h3>
                <div class="ai-panel-controls">
                    <button id="ai-settings-open-btn" title="Settings">⚙️</button>
                    <button id="ai-clear-chat-btn" title="Clear Chat">🗑️</button>
                    <button id="ai-close-panel-btn" title="Close">✖️</button>
                </div>
            </div>
            <div id="ai-chat-history" class="ai-chat-history">
                <!-- Messages will appear here -->
            </div>
            <div class="ai-input-area">
                <textarea id="ai-user-input" placeholder="Ask about your optical system... (e.g., 'Why is the spot size large?')"></textarea>
                <button id="ai-send-btn">Send</button>
            </div>
        </div>
    `),kn()}function cc(){const e=document.getElementById("ai-settings-modal"),t=e?.querySelector?.(".close-modal"),n=document.getElementById("save-ai-settings-btn"),o=document.getElementById("ai-settings-open-btn"),r=document.getElementById("ai-provider"),s=document.getElementById("ai-api-key"),a=document.getElementById("ai-model-select"),c=document.getElementById("ai-model-custom");o&&e&&(o.onclick=()=>{r&&(r.value=Ye.provider),s&&(ze()?(s.value="",s.disabled=!0,s.placeholder="Desktop mode: API key is managed by Rust backend env vars"):(s.disabled=!1,s.value=Ye.apiKey)),e.style.display="block",ar({provider:r?.value,apiKey:s?.value,desiredModel:Ye.model})}),t&&e&&(t.onclick=()=>e.style.display="none"),e&&window.addEventListener("click",u=>{try{u&&u.target===e&&(e.style.display="none")}catch{}}),n&&e&&(n.onclick=()=>{Ye.provider=r?.value||"gemini";const u=a?.value??"",h=u==="__custom__"?c?.value??"":u;Ye.model=String(h||"").trim(),Ye.apiKey=ze()?"":s?.value??"",ec(),e.style.display="none",alert("AI Settings Saved!")}),r&&r.addEventListener("change",()=>{ar({provider:r.value,apiKey:s?.value,desiredModel:""})}),a&&c&&a.addEventListener("change",()=>{c.style.display=a.value==="__custom__"?"":"none"});const i=document.getElementById("ai-close-panel-btn");i&&(i.onclick=Ni);const l=document.getElementById("ai-clear-chat-btn");l&&(l.onclick=()=>{confirm("Clear chat history?")&&(ct=[],Ro(),kn())});const m=document.getElementById("ai-send-btn"),p=document.getElementById("ai-user-input"),d=async()=>{try{const u=p?.value?.trim?.()||"";if(!u)return;Cn("user",u),p&&(p.value="");const h=Cn("assistant","");We(h,{content:"Collecting system context..."});let g={};try{g=await Promise.race([Ga(),new Promise((C,D)=>setTimeout(()=>D(new Error("getSystemContext timed out")),15e3))])}catch(C){console.error("Failed to get system context",C),g={error:"Failed to retrieve system data"}}We(h,{content:"Thinking..."});const y=lc(u,g);if(y){We(h,{content:y});return}const A=await dc(u);if(A){We(h,{content:A});return}const x=await Xa(u);if(x){We(h,{content:x});return}const _=JSON.stringify(g),S={role:"system",content:or},N={role:"user",content:`CONTEXT_JSON (do not edit; read-only):
${_}`},f={role:"user",content:`ユーザーの要望: ${u}`},w=[S,N,f],b=Za(u),v=or,M=[{role:"user",parts:[{text:`CONTEXT_JSON (do not edit; read-only):
${_}`}]},{role:"user",parts:[{text:`ユーザーの要望: ${u}`}]}];if(console.log("Sending to AI:",{provider:Ye.provider,model:Ye.model||"(auto)",userText:u}),ze()){try{const C=await vs({provider:Ye.provider,model:Ye.model||"auto",userMessage:u});We(h,{content:C?.answer||"(no response)"})}catch(C){const D=C&&typeof C=="object"&&"message"in C?String(C.message):String(C);We(h,{content:`❌ Desktop AI command failed: ${D}`})}return}if(!Ye.apiKey){We(h,{content:"⚠️ Please configure your API Key in settings (⚙️)."});return}try{Ye.provider==="openai"?await Bc({messages:w,apiKey:Ye.apiKey,model:Ye.model,thinkingMsgId:h,forceToolCall:b}):Ye.provider==="gemini"?await Ac({systemInstruction:v,contents:M,apiKey:Ye.apiKey,model:Ye.model,thinkingMsgId:h,forceToolCall:b}):We(h,{content:"Provider not supported yet."})}catch(C){console.error("AI API Error:",C);const D=C&&typeof C=="object"&&"message"in C?String(C.message):String(C),B=C&&typeof C=="object"&&"stack"in C?String(C.stack):"";We(h,{content:`❌ Error: ${D}${B?`

${B}`:""}`})}}catch(u){console.error("[AI Assistant] handleSend failed:",u);const h=u&&typeof u=="object"&&"message"in u?String(u.message):String(u),g=u&&typeof u=="object"&&"stack"in u?String(u.stack):"";try{Cn("assistant",`❌ Internal error: ${h}${g?`

${g}`:""}`)}catch{}}};m&&(m.onclick=d),p&&(p.onkeydown=u=>{if(!(!u||u.isComposing)&&u.key==="Enter"&&(u.metaKey||u.ctrlKey)){u.preventDefault(),d();return}})}function Ai(){Kn||(Kn=!0,requestAnimationFrame(()=>{Kn=!1;try{kn()}catch{}}))}function We(e,t){const n=ct.findIndex(o=>o&&o.id===e);n<0||(ct[n]={...ct[n],...t},Ro(),Ai())}function Mn(e){return!Array.isArray(e)||e.length===0?"":[`✅ Applied ${e.length} change(s).`,"","---","(適用ログ)",...e.map(t=>`- ${t}`),"---",""].join(`
`)}function lc(e,t){const n=mc(e),o=fc(e),r=gc(e),s=uc(e);if(!n&&!o&&!r&&!s)return null;if(s)return pc(t);const a=t?.runtimeDiagnostics;if(!a)return["runtimeDiagnostics が context に存在しません。","この質問に答えるには、まず PSF 計算を実行してから AI を呼び出してください。"].join(`
`);const c=[];if(n){const i=a.psfLastResult||null,l=a.psfError||null,m=a.psfUI||null;c.push("PSF（runtimeDiagnostics.psfLastResult）要約（引用ベース）:"),i?(c.push("- psfLastResult:"),c.push(Jt(i,["from","at","wavelength","objectIndex","psfMethod","performanceMode","psfSamplingSize","zernikeFitSamplingSize","gridSize","calculationTime","calculationTimeMs","hasMetrics","metricKeys","psfSummary"])),(i.hasMetrics===!1||Array.isArray(i.metricKeys)&&i.metricKeys.length===0)&&c.push("補足: この実行結果にはメトリクスが含まれていません（hasMetrics=false または metricKeys が空）。")):c.push("- psfLastResult: null（PSFの直近成功結果が保存されていません）"),l&&(c.push("- psfError（直近エラー）:"),c.push(Jt(l,["from","at","code","message","hint","wavelength","objectIndex","gridSize","psfSamplingSize","zernikeFitSamplingSize"]))),m&&(c.push("- psfUI（取得できたUI値）:"),c.push(Jt(m,["objectIndex","samplingSize","zernikeFitSamplingSize","performanceMode","wasmStatusText"]))),c.push("次の確認:"),c.push("- PSF をもう一度実行し、psfLastResult.psfSummary が入るか（または hasMetrics/metricKeys が変わるか）を確認"),c.push("- もし psfError が出ている場合は、まず psfError.message/hint の内容に沿って切り分け")}if(o){const i=a.wavefront||null;c.push("Wavefront（runtimeDiagnostics.wavefront）要約（引用ベース）:"),i?(c.push("- wavefront:"),c.push(Jt(i,["from","meta","hasError","error","statistics"])),i?.hasError&&c.push("補足: wavefront.hasError=true なので wavefront.error を根拠に切り分けしてください。")):c.push("- wavefront: null（直近の波面/OPDマップ結果が保存されていません）")}if(r){const i=a.opdLastRay||null;c.push("OPD last ray（runtimeDiagnostics.opdLastRay）要約（引用ベース）:"),i?(c.push("- opdLastRay:"),c.push(Jt(i,["success","error","fieldKey","pupilCoord","stopHit"]))):c.push("- opdLastRay: null（直近のOPDレイ計算が保存されていません）")}if(o||r){const i=o&&!a.wavefront,l=r&&!a.opdLastRay;(i||l)&&(c.push("次の確認:"),i&&c.push("- 波面/光路差表示を一度実行し、runtimeDiagnostics.wavefront が埋まるか確認"),l&&c.push("- Show wavefront diagram を一度実行し、runtimeDiagnostics.opdLastRay が埋まるか確認"))}return c.join(`
`)}function uc(e){if(!e)return!1;const t=String(e);return t.includes("現在の光学系")||t.includes("この光学系")||t.includes("光学系について説明")||t.includes("システムについて説明")||/describe\s+(the\s+)?(current\s+)?optical\s+system/i.test(t)}function pc(e){if(!e||typeof e!="object")return"context が取得できませんでした。";const t=[],o=(e.meta||{}).activeConfig||null,r=Array.isArray(e.system)?e.system:[],s=Array.isArray(e.sources)?e.sources:[],a=Array.isArray(e.objects)?e.objects:[],c=e.performance||{},i=c?.diagnostics||null,l=e.targets||null,m=e.designIntent||null;if(t.push("光学系サマリ（CONTEXT_JSONより）:"),o){const A=o.name?String(o.name):"(no name)",x=o.id===null||o.id===void 0?"(no id)":String(o.id),_=o.activeScenarioId===null||o.activeScenarioId===void 0?null:String(o.activeScenarioId);t.push(`- Active config: ${A} (id=${x}${_?`, scenario=${_}`:""})`)}t.push(`- Surface count: ${r.length} (Surf 0..${Math.max(0,r.length-1)})`);const p=r.findIndex(A=>A&&(A.isObject||String(A.type||"").toLowerCase().includes("object"))),d=r.findIndex(A=>A&&(A.isImage||String(A.type||"").toLowerCase().includes("image"))),u=r.findIndex(A=>A&&String(A.type||"").toLowerCase().includes("stop"));if(p>=0&&t.push(`- Object surface: Surf ${p}`),u>=0){const A=r[u]?.semidia;t.push(`- Stop: Surf ${u}${typeof A=="number"?` (semidia=${A})`:""}`)}d>=0&&t.push(`- Image surface: Surf ${d}`);const h=new Set;let g=0;for(const A of r){const x=A&&A.material?String(A.material):"AIR";x==="AIR"?g++:h.add(x)}if(t.push(`- Media: AIR rows=${g}, glass kinds=${h.size}${h.size?` (${Array.from(h).slice(0,6).join(", ")}${h.size>6?", …":""})`:""}`),m&&typeof m=="object"){const A=typeof m.blockCount=="number"?m.blockCount:null;t.push(`- Design Intent (blocks): ${A!==null?A:"n/a"} block(s)`)}else t.push("- Design Intent (blocks): not available (surface-only / import mode の可能性)");if(s.length){const A=s.map(N=>N?.wavelength).filter(N=>typeof N=="number"&&Number.isFinite(N)),x=s.find(N=>String(N?.primary||"").toLowerCase().includes("primary"))?.wavelength,_=A.length?Math.min(...A):null,S=A.length?Math.max(...A):null;t.push(`- Sources: ${s.length} (λ: ${_!==null?_:"n/a"} .. ${S!==null?S:"n/a"} μm${typeof x=="number"?`, primary=${x} μm`:""})`)}else t.push("- Sources: 0");if(a.length){const A=Array.from(new Set(a.map(x=>String(x?.position||"")).filter(Boolean)));t.push(`- Objects/Fields: ${a.length}${A.length?` (position: ${A.join(", ")})`:""}`)}else t.push("- Objects/Fields: 0");if(i&&Array.isArray(i.steps)&&i.steps.some(A=>A&&A.ok===!1)){const A=i.steps.filter(x=>x&&x.ok===!1);t.push("診断要約:");for(const x of A.slice(0,3)){const _=x?.error?.message?String(x.error.message):"(no message)";t.push(`- 計算失敗: ${String(x.step)}: ${_}`)}A.length>3&&t.push(`- (他 ${A.length-3} 件)`)}else{const A=typeof c.focalLength=="number"?c.focalLength:null,x=typeof c.fNumber=="number"?c.fNumber:null,_=typeof c.magnification=="number"?c.magnification:null;t.push("近軸/Seidel（計算できた範囲）:"),t.push(`- focalLength: ${A!==null?A:"n/a"}`),t.push(`- fNumber: ${x!==null?x:"n/a"}`),t.push(`- magnification: ${_!==null?_:"n/a"}`);const S=c?.seidel||{};["spherical","coma","astigmatism","fieldCurvature","distortion","lca","tca"].some(w=>typeof S[w]=="number")&&(t.push(`- seidel sums: S1=${S.spherical??"n/a"}, S2=${S.coma??"n/a"}, S3=${S.astigmatism??"n/a"}, S4=${S.fieldCurvature??"n/a"}, S5=${S.distortion??"n/a"}`),(typeof S.lca=="number"||typeof S.tca=="number")&&t.push(`- chromatic: LCA=${S.lca??"n/a"}, TCA=${S.tca??"n/a"}`))}const y=l?.requirements?.rows;if(Array.isArray(y)&&y.length){const A=y.filter(x=>typeof x?.current=="number"||x?.status);t.push(`Requirements: enabled=${y.length}${A.length?` (evaluated=${A.length})`:""}`);for(const x of A.slice(0,6)){const _=x.op?String(x.op):"",S=typeof x.target=="number"?x.target:"n/a",N=typeof x.current=="number"?x.current:"n/a",f=x.status?String(x.status):"";t.push(`- ${String(x.operand)} ${_} target=${S} current=${N}${f?` status=${f}`:""}`)}}return t.push("次にできること:"),t.push("- どの評価結果（Spot/PSF/Wavefront）を根拠に改善するかを指定すると、blocks優先で具体的な変更案を出せます。"),t.join(`
`)}async function dc(e){const t=String(e||""),n=/(全て|すべて|全部|全\s*config|全\s*コンフィグ|全\s*構成|all|every)/i.test(t),o=/(config|configs|configuration|configurations|system\s*config|sytem\s*config|コンフィグ|構成)/i.test(t);if(!(n&&o))return null;const r=t.match(/\bSurf\s*(\d+)\b/i)||t.match(/面\s*(\d+)/);if(!r)return null;const s=Number(r[1]);if(!Number.isInteger(s)||s<0||!/stop/i.test(t)||!/semidia|semi\s*dia(meter)?/i.test(t))return null;const a=t.match(/([0-9]+(?:\.[0-9]+)?)\s*(?:に|へ)(?:\s|$|[、。,.])/)||t.match(/=\s*([0-9]+(?:\.[0-9]+)?)/)||t.match(/\b([0-9]+(?:\.[0-9]+)?)\b\s*$/);if(!a)return null;const c=Number(a[1]);if(!Number.isFinite(c)||c<=0)return null;const i=Yt(),l=Xt(i);if(!i||!l)return"❌ Error: systemConfigurations / active configuration not found";const m=[];for(const g of i.configurations||[])try{if(!Array.isArray(g.blocks)||g.blocks.length===0){m.push({ok:!1,configId:g?.id,error:"no blocks"});continue}const A=$e(g.blocks)?.rows?.[s],x=A?._blockId,_=A?._blockType;if(!x){m.push({ok:!1,configId:g?.id,error:`surf=${s} has no _blockId`});continue}if(_!=="Stop"){m.push({ok:!1,configId:g?.id,error:`surf=${s} is not Stop (blockType=${String(_)})`});continue}const S=dn(g.blocks),N=S.find(M=>M&&String(M.blockId)===String(x));if(!N){m.push({ok:!1,configId:g?.id,error:`blockId not found: ${String(x)}`});continue}if(String(N.blockType||"").trim()!=="Stop"){m.push({ok:!1,configId:g?.id,error:`blockId=${String(x)} is not Stop`});continue}et(N.parameters)||(N.parameters={}),N.parameters.semiDiameter=c,et(N.variables)||(N.variables={});const f=N.variables.semiDiameter;et(f)?f.value=c:N.variables.semiDiameter={value:c},g.blocks=S;const w=Co(g,i),b=Array.isArray(g?.opticalSystem)?g.opticalSystem:null,v=$e(g.blocks);v&&Array.isArray(v.rows)&&(b&&b.length>0&&Un(v.rows,b),w!==void 0&&v.rows[0]&&typeof v.rows[0]=="object"&&(v.rows[0].thickness=w),g.opticalSystem=v.rows),m.push({ok:!0,configId:g?.id,blockId:String(x)})}catch(y){m.push({ok:!1,configId:g?.id,error:y?.message||String(y)})}await Mt(i);const p=m.filter(g=>g.ok).length,d=m.length,u=m.filter(g=>!g.ok),h=u.slice(0,3).map(g=>`- ${String(g.configId)}: ${g.error}`);return[`Surf ${s}（Stop）のsemiDiameterを${c}に変更しました（全config）。`,"","---","(適用ログ)",`- set_block_param(all configs): ok=${p}/${d}${u.length?` fail=${u.length}`:""}`,...h.length?h:[]].join(`
`)}function mc(e){if(!e)return!1;const t=String(e);return(t.includes("runtimeDiagnostics.psfLastResult")||t.includes("psfLastResult"))&&(t.includes("引用")||t.includes("要約"))}function fc(e){if(!e)return!1;const t=String(e),n=t.includes("runtimeDiagnostics.wavefront")||/wavefront/i.test(t)||t.includes("波面"),o=t.includes("引用")||t.includes("要約");return n&&o}function gc(e){if(!e)return!1;const t=String(e),n=t.includes("runtimeDiagnostics.opdLastRay")||t.includes("opdLastRay")||/\bOPD\b/i.test(t)||t.includes("光路差"),o=t.includes("引用")||t.includes("要約");return n&&o}function Jt(e,t){if(!e||typeof e!="object")return"  - (none)";const n=Array.isArray(t)&&t.length?t.filter(o=>Object.prototype.hasOwnProperty.call(e,o)):Object.keys(e);return n.length===0?"  - (no fields)":n.map(o=>{const r=e[o];let s;if(r==null)s=String(r);else if(typeof r=="string")s=r;else try{s=JSON.stringify(r)}catch{s=String(r)}return`  - ${o}: ${s}`}).join(`
`)}function Ni(){const e=document.getElementById("ai-assistant-panel"),t=e.style.display!=="none";e.style.display=t?"none":"flex"}function Cn(e,t){const n=bi++;return ct.push({id:n,role:e,content:t,timestamp:Date.now(),actions:null}),Ro(),Ai(),n}function kn(){const e=document.getElementById("ai-chat-history");e&&(e.innerHTML=ct.map(t=>{const o=Array.isArray(t.actions)&&t.actions.length>0?`<div class="ai-message-actions"><button class="ai-apply-btn" data-ai-action="apply" data-ai-msg-id="${t.id}">提案を適用</button></div>`:"";return`
        <div class="ai-message ai-message-${t.role}" data-ai-msg-id="${t.id}">
            <div class="ai-message-content">${_i(t.content)}</div>
            ${o}
        </div>`}).join(""),e.scrollTop=e.scrollHeight)}typeof document<"u"&&document?.addEventListener&&document.addEventListener("click",async e=>{const t=e?.target;if(!(t instanceof HTMLElement)||t.dataset?.aiAction!=="apply")return;const n=Number(t.dataset.aiMsgId);if(!Number.isFinite(n))return;const o=ct.find(s=>s&&s.id===n),r=o?.actions;if(!(!Array.isArray(r)||r.length===0)){t.setAttribute("disabled","true");try{const s=await Dc(r),a=[o.content,`

---
適用結果:`,s.ok?`✅ OK: ${s.appliedCount} actions`:`❌ Failed: ${s.reason||"unknown reason"}`,...Array.isArray(s.logs)?s.logs.map(c=>`- ${c}`):[]].join(`
`);We(n,{content:a,actions:null})}catch(s){We(n,{content:`${o.content}

❌ Apply error: ${s?.message||s}`})}finally{try{t.removeAttribute("disabled")}catch{}}}});function _i(e){const t=document.createElement("div");return t.textContent=e,t.innerHTML.replace(/\n/g,"<br>")}function yc(e){const t=String(e||"").trim();return t?t.startsWith("models/")?t.slice(7):t:""}async function Ri(e){const t=`https://generativelanguage.googleapis.com/v1beta/models?key=${e}`,n=await $n(t,{method:"GET"},2e4);if(!n.ok){let a="";try{a=(await n.json())?.error?.message||""}catch{}throw new Error(a||"Gemini ListModels failed")}const o=await n.json();return(Array.isArray(o?.models)?o.models:[]).filter(a=>{const c=a?.supportedGenerationMethods;return Array.isArray(c)?c.includes("generateContent"):!0}).map(a=>a?.name).filter(Boolean)}function hc(e){const n=(Array.isArray(e)?e:[]).map(r=>String(r)).filter(Boolean).map(r=>r.replace(/^models\//,"")),o=[r=>r.includes("gemini-1.5-flash"),r=>r.includes("gemini-2.0-flash"),r=>r.includes("flash"),r=>r.includes("gemini-1.5-pro"),r=>r.includes("pro"),r=>r.includes("gemini")];for(const r of o){const s=n.find(r);if(s)return s}return n[0]||""}async function bc(e,t){const n=yc(t);if(n)return n;const o=await Ri(e),r=hc(o);if(!r)throw new Error("Gemini: no available models returned by ListModels");return r}function wc(){return[{functionDeclarations:[{name:"set_block_param",description:"Design Intent (blocks) の特定ブロックのパラメータ/変数を更新し、blocks→expanded optical system に再展開してUIへ反映します。",parameters:{type:"object",properties:{blockId:{type:"string",description:"対象ブロックの blockId（不明な場合は surf を指定可）"},surf:{type:"integer",description:"0-based surface index（expanded optical system の行）。blockId が不明な場合に使用"},applyToAllConfigs:{type:"boolean",description:"true の場合、全ての configuration に同じ変更を適用します（surf 指定推奨）"},section:{type:"string",enum:["parameters","variables"],description:"更新先: parameters または variables"},key:{type:"string",description:"更新キー（blockTypeに依存。例: Lens=frontRadius/backRadius/centerThickness/material、asphere=frontSurfType/frontConic/frontCoef1..10、Doublet=radius1..3/material1..2、Triplet=radius1..4/material1..3、Stop=semiDiameter、AirGap=thickness）"},value:{description:"新しい値（数値または文字列）"}},required:["section","key","value"]}},{name:"set_surface_field",description:"フォールバック: Expanded Optical System の表（active config opticalSystem）上の特定面のフィールドを更新します（blocks が無い場合など）。",parameters:{type:"object",properties:{surf:{type:"integer",description:"0-based surface index"},key:{type:"string",description:"更新キー（例: radius, thickness, material, semidia, conic）"},applyToAllConfigs:{type:"boolean",description:"true の場合、全ての configuration に同じ変更を適用します（blocks がある場合は可能な限り blocks に同期）"},value:{description:"新しい値（数値または文字列）"}},required:["surf","key","value"]}},{name:"set_surface_color",description:"Render Optical System の Surface Colors（面ごとの色）を更新します。localStorage の coopt.surfaceColorOverrides を更新し、3D描画を再描画します。",parameters:{type:"object",properties:{all:{type:"boolean",description:"true の場合、全Surface対象（色=Default/None で全解除）"},surf:{type:"integer",description:"0-based surface index（Surface Colors の # と同じ）"},surfaceId:{type:"integer",description:"surface row の id（存在する場合）。指定すると id:NN で更新します。"},blockId:{type:"string",description:"provenance 用 blockId（_blockId）。surfaceRole と組で指定すると p:blockId|surfaceRole を更新します。"},surfaceRole:{type:"string",description:"provenance 用 surfaceRole（_surfaceRole）。blockId と組で指定します。"},color:{type:"string",description:'色。#RRGGBB / 0xRRGGBB / "Light Pink" 等。"Default"/"None" で解除。'}},required:["color"]}},{name:"apply_optical_system_rows",description:"Optical System（surface rows）を active configuration に取り込み、可能なら Design Intent（blocks）へ自動変換してUIへ反映します。特許の処方表/表をAIが行配列に整形した後に適用する用途。",parameters:{type:"object",properties:{applyToAllConfigs:{type:"boolean",description:"true の場合、全ての configuration の opticalSystem を同じ rows に置換します（blocksの自動変換も試みます）"},rows:{type:"array",description:"Optical system rows (Surf 0..N-1). Each row may include: object type, radius, thickness, semidia, material, surfType, conic, coef1..coef10, comment",items:{type:"object"}}},required:["rows"]}}]}]}async function vc({apiKey:e,model:t,systemInstruction:n,contents:o,tools:r,forceToolCall:s}){const c=`https://generativelanguage.googleapis.com/v1beta/models/${await bc(e,t)}:generateContent?key=${e}`,i={contents:Array.isArray(o)?o:[]};i.systemInstruction={parts:[{text:String(n)}]},Array.isArray(r)&&r.length&&(i.tools=r,i.toolConfig={functionCallingConfig:{mode:s?"ANY":"AUTO"}});const l=await $n(c,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(i)},Wn);if(!l.ok){let m="Gemini API failed";try{m=(await l.json())?.error?.message||m}catch{try{m=await l.text()}catch{}}throw new Error(m)}return await l.json()}function Sc(e){const t=e?.content?.parts;return Array.isArray(t)?t.map(n=>n?.text).filter(Boolean).join(""):""}function xc(e){const t=e?.content?.parts;if(!Array.isArray(t))return[];const n=[];for(const o of t){const r=o?.functionCall;r&&r.name&&n.push({name:String(r.name),args:et(r.args)?r.args:r.args??{}})}return n}async function Ac({systemInstruction:e,contents:t,apiKey:n,model:o,thinkingMsgId:r,forceToolCall:s}){const a=wc();let c=Array.isArray(t)?[...t]:[];const i=[];for(let l=0;l<wi;l++){const m=await vc({apiKey:n,model:o,systemInstruction:e,contents:c,tools:a,forceToolCall:!!s&&l===0}),p=Array.isArray(m?.candidates)?m.candidates[0]:null,d=xc(p),u=Sc(p);if(!d.length){const y=u||"(Gemini: empty response)",A=Mn(i);We(r,{content:A?A+y:y});const x=Mi(y);x?.actions&&We(r,{actions:x.actions});return}We(r,{content:`Applying ${d.length} change(s)...`});const h=[];for(const y of d){let A=null;try{y.name==="set_block_param"?A=await Hn(y.args):y.name==="set_surface_field"?A=await Fo(y.args):y.name==="set_surface_color"?A=await Fi(y.args):y.name==="apply_optical_system_rows"?A=await Ii(y.args):A={ok:!1,error:`Unknown tool: ${y.name}`}}catch(x){A={ok:!1,error:x?.message||String(x)}}i.push(`${y.name}: ${A?.ok?"ok":"fail"}${A?.error?` (${A.error})`:""}`),h.push({functionResponse:{name:y.name,response:A}})}const g=Mn(i);g&&We(r,{content:g}),c=c.concat([{role:"user",parts:h}])}We(r,{content:"❌ Tool loop exceeded max iterations. Please re-try with a narrower request."})}function Ci(e){return String(e||"").trim()||"gpt-4o-mini"}function Nc(){return[{type:"function",function:{name:"set_block_param",description:"Design Intent (blocks) の特定ブロックのパラメータ/変数を更新し、blocks→expanded optical system に再展開してUIへ反映します。",parameters:{type:"object",additionalProperties:!1,properties:{blockId:{type:"string",description:"対象ブロックの blockId（不明な場合は surf を指定可）"},surf:{type:"integer",description:"0-based surface index（expanded optical system の行）。blockId が不明な場合に使用"},applyToAllConfigs:{type:"boolean",description:"true の場合、全ての configuration に同じ変更を適用します（surf 指定推奨）"},section:{type:"string",enum:["parameters","variables"],description:"更新先: parameters または variables"},key:{type:"string",description:"更新キー（blockTypeに依存。例: Lens=frontRadius/backRadius/centerThickness/material、asphere=frontSurfType/frontConic/frontCoef1..10、Doublet=radius1..3/material1..2、Triplet=radius1..4/material1..3、Stop=semiDiameter、AirGap=thickness）"},value:{description:"新しい値（数値または文字列）"}},required:["section","key","value"],anyOf:[{required:["blockId"]},{required:["surf"]}]}}},{type:"function",function:{name:"set_surface_field",description:"フォールバック: Expanded Optical System の表（active config opticalSystem）上の特定面のフィールドを更新します（blocks が無い場合など）。",parameters:{type:"object",additionalProperties:!1,properties:{surf:{type:"integer",description:"0-based surface index"},key:{type:"string",description:"更新キー（例: radius, thickness, material, semidia, conic）"},applyToAllConfigs:{type:"boolean",description:"true の場合、全ての configuration に同じ変更を適用します（blocks がある場合は可能な限り blocks に同期）"},value:{description:"新しい値（数値または文字列）"}},required:["surf","key","value"]}}},{type:"function",function:{name:"set_surface_color",description:"Render Optical System の Surface Colors（面ごとの色）を更新します。localStorage の coopt.surfaceColorOverrides を更新し、3D描画を再描画します。",parameters:{type:"object",additionalProperties:!1,properties:{all:{type:"boolean",description:"true の場合、全Surface対象（色=Default/None で全解除）"},surf:{type:"integer",description:"0-based surface index（Surface Colors の # と同じ）"},surfaceId:{type:"integer",description:"surface row の id（存在する場合）。指定すると id:NN で更新します。"},blockId:{type:"string",description:"provenance 用 blockId（_blockId）。surfaceRole と組で指定すると p:blockId|surfaceRole を更新します。"},surfaceRole:{type:"string",description:"provenance 用 surfaceRole（_surfaceRole）。blockId と組で指定します。"},color:{type:"string",description:'色。#RRGGBB / 0xRRGGBB / "Light Pink" 等。"Default"/"None" で解除。'}},required:["color"],anyOf:[{required:["all"]},{required:["surf"]},{required:["surfaceId"]},{required:["blockId","surfaceRole"]}]}}},{type:"function",function:{name:"apply_optical_system_rows",description:"Optical System（surface rows）を active configuration に取り込み、可能なら Design Intent（blocks）へ自動変換してUIへ反映します。特許の処方表/表をAIが行配列に整形した後に適用する用途。",parameters:{type:"object",additionalProperties:!1,properties:{applyToAllConfigs:{type:"boolean",description:"true の場合、全ての configuration の opticalSystem を同じ rows に置換します（blocksの自動変換も試みます）"},rows:{type:"array",items:{type:"object"},description:"Optical system rows (Surf 0..N-1). Each row may include: object type, radius, thickness, semidia, material, surfType, conic, coef1..coef10, comment"}},required:["rows"]}}}]}function cr(e){const t=Array.isArray(e)?dn(e):[];for(let n=0;n<t.length;n++)(!t[n]||typeof t[n]!="object")&&(t[n]={}),t[n].id=n,t[n].type!==void 0&&t[n]["object type"]===void 0&&(t[n]["object type"]=t[n].type),t[n].glass!==void 0&&t[n].material===void 0&&(t[n].material=t[n].glass),t[n].semiDiameter!==void 0&&t[n].semidia===void 0&&(t[n].semidia=t[n].semiDiameter);return t}async function Ii(e){const t=Yt(),n=Xt(t);if(!t||!n)throw new Error("systemConfigurations / active configuration not found");const o=!!e?.applyToAllConfigs,r=e?.rows,s=cr(r);if(!Array.isArray(s)||s.length===0)throw new Error("rows must be a non-empty array");const a=o?t.configurations||[]:[n],c=[];for(const i of a)try{const l=cr(s);i.opticalSystem=l;const m=pi(l),p=Array.isArray(m?.issues)?m.issues:[],d=p.some(u=>u&&u.severity==="fatal");if(!d&&Array.isArray(m?.blocks)&&m.blocks.length>0){i.schemaVersion=i.schemaVersion||pn,i.blocks=m.blocks;const u=$e(i.blocks);if(u&&Array.isArray(u.rows)){try{Un(u.rows,l)}catch{}try{const h=l?.[0]?.thickness;String(h??"").trim()!==""&&u.rows[0]&&typeof u.rows[0]=="object"&&(u.rows[0].thickness=h)}catch{}i.opticalSystem=u.rows}(!i.metadata||typeof i.metadata!="object")&&(i.metadata={}),i.metadata.importAnalyzeMode=!1}else(!i.metadata||typeof i.metadata!="object")&&(i.metadata={}),i.metadata.importAnalyzeMode=!0;c.push({ok:!0,configId:i?.id,blocksDerived:!d&&Array.isArray(m?.blocks)&&m.blocks.length>0,issues:p})}catch(l){c.push({ok:!1,configId:i?.id,error:l?.message||String(l)})}return await Mt(t),{ok:c.some(i=>i.ok),applyToAllConfigs:o,appliedToConfigs:c}}function et(e){return!!e&&typeof e=="object"&&!Array.isArray(e)}function dn(e){return JSON.parse(JSON.stringify(e))}function Yt(){try{return typeof localStorage>"u"?null:ot()}catch{return null}}function Xt(e){if(!e||!Array.isArray(e.configurations))return null;const t=e.activeConfigId;return e.configurations.find(n=>n&&String(n.id)===String(t))||e.configurations[0]||null}function Co(e,t){try{if(Array.isArray(e?.blocks)&&e.blocks.some(o=>String(o?.blockType??"").trim()==="ObjectSurface"))return}catch{}try{const n=e?.opticalSystem?.[0]?.thickness;if(String(n??"").trim()!=="")return n}catch{}try{if(t&&String(t.activeConfigId)===String(e?.id)){const n=ws();if(n){const o=n?.[0]?.thickness;if(String(o??"").trim()!=="")return o}}}catch{}}function Un(e,t){if(!Array.isArray(e)||!Array.isArray(t))return;const n=Math.min(e.length,t.length),o=s=>s==null?!1:String(s).trim()!=="",r=s=>!s||typeof s!="object"?null:s.semidia??s["Semi Diameter"]??s["semi diameter"]??s.semiDiameter??s.semiDia;for(let s=0;s<n;s++){const a=e[s],c=t[s];if(!a||typeof a!="object"||!c||typeof c!="object")continue;const i=String(a["object type"]??a.object??"").trim().toLowerCase();if(i==="stop"||i==="image")continue;const l=r(c);o(l)&&(a.semidia=l)}}async function Mt(e){try{typeof localStorage<"u"&&No(e)}catch{}const n=(()=>{try{if(window.ConfigurationManager||window.tableOpticalSystem)return window}catch{}try{if(window.opener&&!window.opener.closed)return window.opener}catch{}return window})();try{if(window.opener&&!window.opener.closed){const o=typeof window.location?.origin=="string"&&window.location.origin&&window.location.origin!=="null"?window.location.origin:"*";window.opener.postMessage({type:Si},o)}}catch{}await xi(n)}const St=(e,t,n)=>{const o=[];for(let r=t;r<=n;r++)o.push(`${e}${r}`);return o},_c=new Set(["frontRadius","backRadius","centerThickness","material","frontSurfType","backSurfType","frontConic","backConic",...St("frontCoef",1,10),...St("backCoef",1,10)]),Rc=new Set(["radius1","radius2","radius3","thickness1","thickness2","material1","material2","surf1SurfType","surf2SurfType","surf3SurfType","surf1Conic","surf2Conic","surf3Conic",...St("surf1Coef",1,10),...St("surf2Coef",1,10),...St("surf3Coef",1,10)]),Cc=new Set(["radius1","radius2","radius3","radius4","thickness1","thickness2","thickness3","material1","material2","material3","surf1SurfType","surf2SurfType","surf3SurfType","surf4SurfType","surf1Conic","surf2Conic","surf3Conic","surf4Conic",...St("surf1Coef",1,10),...St("surf2Coef",1,10),...St("surf3Coef",1,10),...St("surf4Coef",1,10)]),Ic=new Set(["thickness"]),Fc=new Set(["semiDiameter"]);function Mc(e){const t=String(e||"").trim();return t==="Lens"||t==="PositiveLens"?_c:t==="Doublet"?Rc:t==="Triplet"?Cc:t==="AirGap"?Ic:t==="Stop"?Fc:null}const kc=new Set(["radius","radiusRaw","thickness","material","glass","semidia","conic","type"]),Io="coopt.surfaceColorOverrides",lr=new Map([["light pink","#F8BBD0"],["light red","#FFCDD2"],["light orange","#FFE0B2"],["light amber","#FFECB3"],["light yellow","#FFF9C4"],["light lime","#F0F4C3"],["light green","#C8E6C9"],["light mint","#B2DFDB"],["light cyan","#B3E5FC"],["light sky","#BBDEFB"],["light blue","#90CAF9"],["light indigo","#C5CAE9"],["light purple","#E1BEE7"],["light lavender","#D1C4E9"],["light peach","#FFCCBC"],["light gray","#ECEFF1"]]);function Pc(){try{const e=localStorage.getItem(Io);if(!e)return{};const t=JSON.parse(e);return et(t)?t:{}}catch{return{}}}function Ec(e){try{localStorage.setItem(Io,JSON.stringify(e||{}))}catch{}}function Tc(e){const t=String(e??"").trim();if(!t)return null;const n=t.toLowerCase();if(n==="default"||n==="none"||n==="clear"||n==="reset"||n==="auto")return null;if(/^#[0-9a-fA-F]{6}$/.test(t))return t.toUpperCase();if(/^0x[0-9a-fA-F]{6}$/.test(t))return("#"+t.slice(2)).toUpperCase();const o=lr.get(n);if(o)return o;const r=n.replace(/\s+/g," ").trim(),s=lr.get(r);if(s)return s;throw new Error(`Unsupported color: ${t}`)}function Oc({row:e,surf:t,surfaceId:n,blockId:o,surfaceRole:r}){try{const a=String(e?._blockId??o??"").trim(),c=String(e?._surfaceRole??r??"").trim();if(a&&c)return"p:"+a+"|"+c}catch{}try{const a=Number(e?.id??n);if(Number.isFinite(a))return"id:"+String(Math.floor(a))}catch{}const s=Number.isInteger(Number(t))?Number(t):0;return"i:"+String(Math.floor(s))}function ur(){const e=t=>{try{return!t||t.closed?!1:(t.postMessage({action:"request-redraw"},"*"),!0)}catch{return!1}};if(!e(window.popup3DWindow))try{if(window.opener&&!window.opener.closed&&e(window.opener.popup3DWindow))return}catch{}}async function Fi(e){const t=Yt(),n=Xt(t);if(!t||!n)throw new Error("systemConfigurations / active configuration not found");const o=Tc(e?.color);if(!!e?.all){if(o!==null)throw new Error("all=true is currently only supported with color=Default/None (reset)");return localStorage.removeItem(Io),ur(),{ok:!0,applied:{key:"(all)",color:"Default"}}}const s=Number.isInteger(Number(e?.surf))&&Number(e?.surf)>=0,a=Number.isFinite(Number(e?.surfaceId)),c=String(e?.blockId??"").trim()&&String(e?.surfaceRole??"").trim();if(!s&&!a&&!c)throw new Error("surf or surfaceId or (blockId+surfaceRole) is required");const i=s?Number(e.surf):null;let l=null;try{if(s)Array.isArray(n?.blocks)&&n.blocks.length>0?l=$e(n.blocks)?.rows?.[i]??null:Array.isArray(n?.opticalSystem)&&(l=n.opticalSystem?.[i]??null);else if(a){const d=Math.floor(Number(e.surfaceId)),u=Array.isArray(n?.blocks)&&n.blocks.length>0?$e(n.blocks)?.rows||[]:Array.isArray(n?.opticalSystem)?n.opticalSystem:[];l=Array.isArray(u)&&u.find(h=>Number(h?.id)===d)||null}}catch{l=null}const m=Oc({row:l,surf:s?i:0,surfaceId:a?Number(e.surfaceId):null,blockId:e?.blockId,surfaceRole:e?.surfaceRole}),p=Pc();return o===null?delete p[m]:p[m]=o,Ec(p),ur(),{ok:!0,applied:{key:m,color:o??"Default"}}}async function Hn(e){const t=Yt(),n=Xt(t);if(!t||!n)throw new Error("systemConfigurations / active configuration not found");if(!Array.isArray(n.blocks)||n.blocks.length===0)throw new Error("Active configuration has no blocks (Design Intent).");const o=!!e?.applyToAllConfigs,r=e?.surf;let s=String(e?.blockId||"").trim();const a=String(e?.section||"").trim();let c=String(e?.key||"").trim(),i=e?.value;const l=d=>{const u=s?String(s):"";if(u&&Array.isArray(d?.blocks)&&d.blocks.some(_=>_&&String(_.blockId)===u))return u;const h=Number(r);if(!Number.isInteger(h)||h<0)return null;const A=$e(d.blocks)?.rows?.[h]?._blockId;return A?String(A):null},m=d=>{if(!Array.isArray(d?.blocks)||d.blocks.length===0)return{ok:!1,configId:d?.id,error:"no blocks"};const u=l(d);if(!u)return{ok:!1,configId:d?.id,error:"blockId or surf (0-based) required / could not resolve blockId"};const h=dn(d.blocks),g=h.find(j=>j&&String(j.blockId)===u);if(!g)return{ok:!1,configId:d?.id,error:`blockId not found: ${u}`};const y=String(g.blockType||"").trim();if(y==="ImageSurface")return{ok:!1,configId:d?.id,error:"ImageSurface is not editable"};let A=a,x=c,_=i;x==="glass"&&(x="material"),x==="glass1"&&(x="material1"),x==="glass2"&&(x="material2"),x==="glass3"&&(x="material3"),y==="Stop"&&x==="semiDiameter"&&(A="parameters");const S=Mc(y);if(!S||!S.has(x))return{ok:!1,configId:d?.id,error:`Unsupported key for blockType=${y}: ${x}`};const N=j=>{if(typeof j=="number")return Number.isFinite(j)?j:NaN;if(typeof j=="string"){const H=j.trim();if(H==="")return NaN;if(/^inf(inity)?$/i.test(H))return 1/0;const ce=Number(H);return Number.isFinite(ce)?ce:NaN}return NaN};if(x.toLowerCase().includes("thickness")){const j=N(_);if(Number.isFinite(j)&&j<0)return{ok:!1,configId:d?.id,error:`Negative thickness is not allowed: ${j}`};if(y==="AirGap"&&Number.isFinite(j)&&j<0)return{ok:!1,configId:d?.id,error:`Negative AirGap thickness is not allowed: ${j}`}}if(x==="semiDiameter"){const j=N(_);if(!Number.isFinite(j)||j<=0)return{ok:!1,configId:d?.id,error:`Stop.semiDiameter must be positive: ${String(_)}`};_=j}const f=()=>{try{if(window.undoHistory&&window.SetBlockParameterCommand&&!window.undoHistory.isExecuting){const j=et(g.parameters)?g.parameters[x]:void 0;if(j!==_){const H=new window.SetBlockParameterCommand(d.id,u,`parameters.${x}`,j,_);window.undoHistory.record(H)}}}catch{}et(g.parameters)||(g.parameters={}),g.parameters[x]=_},w=()=>{try{if(window.undoHistory&&window.SetBlockParameterCommand&&!window.undoHistory.isExecuting){const H=et(g.variables)&&et(g.variables[x])?g.variables[x].value:void 0;if(H!==_){const ce=new window.SetBlockParameterCommand(d.id,u,`variables.${x}.value`,H,_);window.undoHistory.record(ce)}}}catch{}et(g.variables)||(g.variables={});const j=g.variables[x];et(j)?j.value=_:g.variables[x]={value:_}},b=et(g.parameters)&&Object.prototype.hasOwnProperty.call(g.parameters,x),v=et(g.variables)&&Object.prototype.hasOwnProperty.call(g.variables,x);if(b&&f(),v&&w(),!b&&!v&&(A==="parameters"?f():w()),y==="Stop"&&x==="semiDiameter"){et(g.variables)||(g.variables={});const j=g.variables[x];et(j)?j.value=_:g.variables[x]={value:_}}d.blocks=h;const M=Co(d,t),C=Array.isArray(d?.opticalSystem)?d.opticalSystem:null,D=$e(d.blocks);D&&Array.isArray(D.rows)&&(C&&C.length>0&&Un(D.rows,C),M!==void 0&&D.rows[0]&&typeof D.rows[0]=="object"&&(D.rows[0].thickness=M),d.opticalSystem=D.rows);const B=b||!b&&!v&&A==="parameters"?"parameters":"variables";return{ok:!0,configId:d?.id,applied:{blockId:u,section:B,key:x,value:_}}};if(!(a==="parameters"||a==="variables"))throw new Error("section must be parameters|variables");if(!c)throw new Error("key is required");if(o){const d=[];for(const u of t.configurations||[])d.push(m(u));return await Mt(t),{ok:d.some(u=>u.ok),applyToAllConfigs:!0,appliedToConfigs:d}}const p=m(n);if(!p.ok)throw new Error(p.error||"failed");return await Mt(t),{ok:!0,applyToAllConfigs:!1,applied:p.applied}}async function Fo(e){const t=Yt(),n=Xt(t);if(!t||!n)throw new Error("systemConfigurations / active configuration not found");const o=Number(e?.surf);let r=String(e?.key||"").trim();const s=e?.value,a=!!e?.applyToAllConfigs;if(!Number.isInteger(o)||o<0)throw new Error("surf must be a 0-based non-negative integer");if(!r)throw new Error("key is required");if(r==="radiusRaw"&&(r="radius"),!kc.has(r))throw new Error(`Unsupported surface key: ${r}`);if(r==="semidia")try{const l=typeof s=="number"?s:Number(String(s).trim());if(!Number.isFinite(l)||l<=0)throw new Error(`semidia must be positive number: ${String(s)}`);const m=await Hn({surf:o,applyToAllConfigs:a,section:"parameters",key:"semiDiameter",value:l});return{ok:!0,applyToAllConfigs:a,applied:{surf:o,key:r,value:s,translatedTo:"set_block_param"},result:m}}catch{}const c=a?t.configurations||[]:[n],i=[];for(const l of c)try{const m=Array.isArray(l.opticalSystem)?dn(l.opticalSystem):[];if(!m[o])throw new Error(`surface out of range: ${o}`);m[o][r]=s,l.opticalSystem=m,i.push({ok:!0,configId:l?.id})}catch(m){i.push({ok:!1,configId:l?.id,error:m?.message||String(m)})}return await Mt(t),{ok:i.some(l=>l.ok),applyToAllConfigs:a,applied:{surf:o,key:r,value:s},appliedToConfigs:i}}async function Dc(e){if(!Array.isArray(e)||e.length===0)return{ok:!1,reason:"no actions"};const t=[];let n=0;for(const o of e){const r=String(o?.type||"").trim();if(r==="set_block_param"){const s=await Hn(o);n++,t.push(`set_block_param ${s.applied.blockId} ${s.applied.section}.${s.applied.key}=${JSON.stringify(s.applied.value)}`)}else if(r==="set_surface_field"){const s=await Fo(o);n++,t.push(`set_surface_field surf=${s.applied.surf} ${s.applied.key}=${JSON.stringify(s.applied.value)}`)}else t.push(`skip unknown action type: ${r||"(missing)"}`)}return{ok:!0,appliedCount:n,logs:t}}function Mi(e){if(!e)return null;const t=String(e),n=t.indexOf(rr);if(n>=0){const r=t.slice(n+rr.length),s=r.match(/```json\s*([\s\S]*?)```/i)||r.match(/```\s*([\s\S]*?)```/);if(s&&s[1])try{const a=JSON.parse(s[1]);if(a&&Array.isArray(a.actions))return a}catch{}}const o=[...t.matchAll(/```json\s*([\s\S]*?)```/gi)];for(const r of o)try{const s=JSON.parse(r[1]);if(s&&Array.isArray(s.actions))return s}catch{}return null}async function jc({apiKey:e,model:t,payload:n}){const r=await $n("https://api.openai.com/v1/chat/completions",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${e}`},body:JSON.stringify({model:Ci(t),...n,stream:!1})},Wn);if(!r.ok){let s="OpenAI API failed";try{s=(await r.json())?.error?.message||s}catch{try{s=await r.text()}catch{}}throw new Error(s)}return await r.json()}async function Lc({apiKey:e,model:t,messages:n,onDelta:o}){const s=await $n("https://api.openai.com/v1/chat/completions",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${e}`},body:JSON.stringify({model:Ci(t),messages:n,stream:!0})},vi);if(!s.ok){let l="OpenAI stream failed";try{l=(await s.json())?.error?.message||l}catch{try{l=await s.text()}catch{}}throw new Error(l)}const a=s.body.getReader(),c=new TextDecoder("utf-8");let i="";for(;;){const{value:l,done:m}=await a.read();if(m)break;i+=c.decode(l,{stream:!0});const p=i.split(`
`);i=p.pop()||"";for(const d of p){const u=d.trim();if(!u.startsWith("data:"))continue;const h=u.slice(5).trim();if(h==="[DONE]")return;try{const y=JSON.parse(h)?.choices?.[0]?.delta?.content;y&&o(String(y))}catch{}}}}async function Bc({messages:e,apiKey:t,model:n,thinkingMsgId:o,forceToolCall:r}){const s=Nc();let a=0,c=[...e];const i=[];for(;a++<wi;){const m=!!r&&i.length===0?{type:"function",function:{name:"set_block_param"}}:"auto",d=(await jc({apiKey:t,model:n,payload:{messages:c,tools:s,tool_choice:m}}))?.choices?.[0]?.message,u=Array.isArray(d?.tool_calls)?d.tool_calls:[],h=d?.content||"";if(u.length===0){let g="";const y=Mn(i);We(o,{content:y||""}),await Lc({apiKey:t,model:n,messages:c.concat([{role:"assistant",content:h}]),onDelta:x=>{g+=x,We(o,{content:(y||"")+g})}});const A=Mi(g);A?.actions&&We(o,{actions:A.actions});return}c=c.concat([{role:"assistant",content:h,tool_calls:u}]);for(const g of u){const y=g?.function?.name,A=g?.function?.arguments;let x={};try{x=A?JSON.parse(A):{}}catch{x={}}let _=null;try{y==="set_block_param"?_=await Hn(x):y==="set_surface_field"?_=await Fo(x):y==="set_surface_color"?_=await Fi(x):y==="apply_optical_system_rows"?_=await Ii(x):_={ok:!1,error:`Unknown tool: ${y}`}}catch(N){_={ok:!1,error:N?.message||String(N)}}i.push(`${y}: ${_?.ok?"ok":"fail"}${_?.error?` (${_.error})`:""}`);const S=Mn(i);S&&We(o,{content:S}),c.push({role:"tool",tool_call_id:g.id,content:JSON.stringify(_)})}}We(o,{content:"❌ Tool loop exceeded max iterations. Please re-try with a narrower request."})}function zc(e){return e==null||String(e).trim()===""}function Oe(e){if(e==null)return null;if(typeof e=="number"&&Number.isFinite(e))return e;const t=String(e).trim();if(t==="")return null;if(/^inf(inity)?$/i.test(t))return 1/0;const n=Number(t);return Number.isFinite(n)?n:null}function Wc(e){const t=String(e??"").trim();return t===""||t.toUpperCase()==="AIR"||t.toUpperCase()==="VACUUM"?"AIR":t.replace(/\s+/g,"")}function $c(e){const t=new Uint8Array(2+e.length*2);t[0]=255,t[1]=254;for(let n=0;n<e.length;n++){const o=e.charCodeAt(n);t[2+n*2]=o&255,t[2+n*2+1]=o>>8&255}return t}function Uc(e){const t=[];for(let n=0;n<e.length;n++){const o=e[n]??{};String(o.surfType??"").trim()==="Aspheric odd"&&t.push(n+1)}return{odd:t}}function Hc(e){const t=String(e?.surfType??e?.["surface type"]??e?.surfaceType??"").trim().toLowerCase(),n=String(e?.["object type"]??e?.object??"").trim().toLowerCase();return t==="coord trans"||t==="coordinate transform"||t==="ct"||t==="coordtrans"||t==="coordinatetransform"||n==="coord trans"||n==="coordinate transform"||n==="ct"||n==="coordtrans"||n==="coordinatetransform"}function Gc(e){const t=String(e??"").trim().toLowerCase();return t==="orientation"?1:t==="xy"?2:t==="xyz"?3:0}function He(e){if(!Number.isFinite(e))return"0";const t=Math.abs(e);return t!==0&&(t<1e-6||t>1e6)?String(e):Number(e.toFixed(15)).toString()}function pr(e){return Array.isArray(e)?e:[]}function Vc(e){for(let t=0;t<e.length;t++){const n=e[t]??{};if(String(n.primary??"").trim())return t+1}return 1}function qc(e,t={}){const n=Array.isArray(e)?e:[],{odd:o}=Uc(n);if(o.length>0){const m=o.slice(0,20).join(", "),p=o.length>20?` (+${o.length-20} more)`:"",d=new Error(`Zemax export: Aspheric odd surfaces are not supported yet. Surface(s): ${m}${p}`);throw d.code="ZMX_UNSUPPORTED_ODD",d}const r=String(t.title??"co-opt export").replace(/\r?\n/g," ").trim(),s=String(t.units??"MM").trim().toUpperCase(),a=[];a.push(`UNIT ${s}`),r&&a.push(`NAME "${r.replace(/\"/g,"'")}"`),a.push('NOTE "Generated by co-opt"');const c=pr(t.sourceRows);if(c.length>0){for(let p=0;p<c.length;p++){const d=c[p]??{},u=Oe(d.wavelength);if(u===null||!Number.isFinite(u)||u<=0)continue;const h=Oe(d.weight),g=h!==null&&Number.isFinite(h)?h:1;a.push(`WAVM ${p+1} ${He(u)} ${He(g)}`)}const m=Vc(c);m>0&&a.push(`PWAV ${m}`)}const i=pr(t.objectRows);if(i.length>0){a.push("FTYP 0 0 2 3 0 0 0 2");const m=[],p=[],d=[];for(let u=0;u<i.length;u++){const h=i[u]??{},g=Oe(h.xHeightAngle),y=Oe(h.yHeightAngle);(g===null||!Number.isFinite(g))&&(y===null||!Number.isFinite(y))||(m.push(He(Number.isFinite(g)?g:0)),p.push(He(Number.isFinite(y)?y:0)),d.push("1"))}m.length>0&&(a.push(`XFLN ${m.join(" ")}`),a.push(`YFLN ${p.join(" ")}`),a.push(`FWGN ${d.join(" ")}`))}const l=Oe(t.entrancePupilDiameterMm??t.pupilDiameterMm);l!==null&&Number.isFinite(l)&&l>0&&a.push(`ENPD ${He(l)}`);for(let m=0;m<n.length;m++){const p=n[m]??{};if(a.push(`SURF ${m}`),Hc(p)){a.push("TYPE COORDBRK"),a.push("CURV 0");const N=(()=>{const j=Oe(p.decenterZ);if(j!==null&&Number.isFinite(j))return j;const H=Oe(p.thickness);return H!==null&&Number.isFinite(H)?H:0})();a.push(`DISZ ${He(N)}`);const f=(()=>{const j=Oe(p.decenterX);if(j!==null&&Number.isFinite(j))return j;const H=Oe(p.semidia);return H!==null&&Number.isFinite(H)?H:0})(),w=(()=>{const j=Oe(p.decenterY);if(j!==null&&Number.isFinite(j))return j;const H=Oe(p.material);return H!==null&&Number.isFinite(H)?H:0})(),b=(()=>{const j=Oe(p.tiltX);if(j!==null&&Number.isFinite(j))return j;const H=Oe(p.rindex);return H!==null&&Number.isFinite(H)?H:0})(),v=(()=>{const j=Oe(p.tiltY);if(j!==null&&Number.isFinite(j))return j;const H=Oe(p.abbe);return H!==null&&Number.isFinite(H)?H:0})(),M=(()=>{const j=Oe(p.tiltZ);if(j!==null&&Number.isFinite(j))return j;const H=Oe(p.conic);return H!==null&&Number.isFinite(H)?H:0})(),C=(()=>{const j=Oe(p.order),H=Oe(p.coef1);return(j!==null&&Number.isFinite(j)?j:H!==null&&Number.isFinite(H)?H:0)===1?1:0})();a.push(`PARM 1 ${He(f)}`),a.push(`PARM 2 ${He(w)}`),a.push(`PARM 3 ${He(b)}`),a.push(`PARM 4 ${He(v)}`),a.push(`PARM 5 ${He(M)}`),a.push(`PARM 6 ${He(C)}`);const D=Gc(p.coordReturn);if(D!==0){a.push(`PARM 7 ${He(D)}`);const j=Oe(p.toSurf),H=j!==null&&Number.isFinite(j)&&j>=0?Math.trunc(j):0;a.push(`PARM 8 ${He(H)}`)}const B=String(p.comment??"").trim();B&&a.push(`COMM "${B.replace(/\"/g,"'")}"`);continue}String(p["object type"]??"").trim()==="Stop"&&a.push("STOP");const u=Oe(p.radius);let h=0;u===null||u===1/0||u===0?h=0:h=1/u,a.push(`CURV ${He(h)}`);const g=Oe(p.thickness);let y=0;g===null?y=0:g===1/0?y=1e10:y=g,a.push(`DISZ ${He(y)}`);const A=Wc(p.material);!zc(A)&&String(A).toUpperCase()!=="AIR"&&a.push(`GLAS ${A}`);const x=Oe(p.semidia);if(x!==null&&Number.isFinite(x)&&x>0&&a.push(`DIAM ${He(x)} 1 0 0 1 ""`),String(p.surfType??"").trim()==="Aspheric even"){a.push("TYPE EVENASPH");const N=Oe(p.conic);N!==null&&Number.isFinite(N)&&N!==0&&a.push(`CONI ${He(N)}`),a.push("PARM 1 0");for(let f=1;f<=10;f++){const w=`coef${f}`,b=Oe(p[w]);b===null||!Number.isFinite(b)||b===0||a.push(`PARM ${f+1} ${He(b)}`)}}else{const N=Oe(p.conic);N!==null&&Number.isFinite(N)&&N!==0&&a.push(`CONI ${He(N)}`)}const S=String(p.comment??"").trim();S&&a.push(`COMM "${S.replace(/\"/g,"'")}"`)}return a.push(""),a.join(`
`)}function Zc(e,t){const n=String(t??"co-opt-export.zmx").trim()||"co-opt-export.zmx",o=n.toLowerCase().endsWith(".zmx")?n:`${n}.zmx`,r=$c(String(e??"")),s=new Blob([r],{type:"text/plain"}),a=URL.createObjectURL(s),c=document.createElement("a");c.href=a,c.download=o,c.click(),URL.revokeObjectURL(a)}const dr="analysis-progress-wrap",mr="analysis-progress-label",fr="analysis-progress-bar";let Ot=null,lo=null,_t=null;function Mo(){if(Ot)return Ot;const e=document.getElementById(dr),t=document.getElementById(mr),n=document.getElementById(fr);if(e&&t&&n instanceof HTMLProgressElement)return Ot={wrap:e,label:t,prog:n},Ot;const o=document.createElement("div");o.id=dr,o.className="analysis-progress-wrap",o.style.display="none";const r=document.createElement("div");r.id=mr,r.className="analysis-progress-label",r.textContent="";const s=document.createElement("progress");return s.id=fr,s.className="analysis-progress-bar",s.max=100,s.value=0,o.appendChild(r),o.appendChild(s),document.body.appendChild(o),Ot={wrap:o,label:r,prog:s},Ot}function ko(e="Computing analysis...",t=180){const n=Mo();n&&(_t!==null&&(clearTimeout(_t),_t=null),t<=0?n.wrap.style.display="block":_t=window.setTimeout(()=>{_t=null;try{n.wrap.style.display="block"}catch{}},t),kt({label:e,percent:null}))}function kt(e){const t=Mo();if(!t)return;const n=String(e?.label??"Computing analysis..."),o=Number(e?.percent),r=Number.isFinite(o),s=r?Math.max(0,Math.min(100,o)):0,a=Date.now(),c=lo;if(c){const i=c.label===n,l=c.value===s,m=i&&!l&&a-c.at<120;if(i&&l||m)return}t.label.textContent=n,r?(t.prog.max=100,t.prog.value=s,t.prog.removeAttribute("data-indeterminate")):(t.prog.value=0,t.prog.setAttribute("data-indeterminate","1")),lo={label:n,value:s,max:100,at:a}}function Po(){_t!==null&&(clearTimeout(_t),_t=null);const e=Mo();e&&(e.wrap.style.display="none",lo=null)}const T=window;function lt(){let e=[],t=[],n=[];try{const o=je(T.tableOpticalSystem);Array.isArray(o)&&(e=o)}catch{}try{const o=rt(T.tableSource);Array.isArray(o)&&(t=o)}catch{}try{const o=Ke(T.tableObject);Array.isArray(o)&&(n=o)}catch{}try{let o=null;if(typeof T.getActiveConfiguration=="function"&&(o=T.getActiveConfiguration()),!o){const r=ot(),s=r?.activeConfigId,a=Number(s),c=Array.isArray(r?.configurations)?r.configurations:[];Number.isFinite(a)&&(o=c.find(i=>Number(i?.id)===a)||null),!o&&c.length>0&&(o=c[0])}if(o){let r=Array.isArray(o?.opticalSystem)?o.opticalSystem.map(a=>a&&typeof a=="object"?{...a}:a):[];if(Array.isArray(o?.blocks)&&o.blocks.length>0){const a=T.expandBlocksToOpticalSystemRows?T.expandBlocksToOpticalSystemRows(o.blocks):null;Array.isArray(a?.rows)&&a.rows.length>0&&(r=a.rows.map(c=>c&&typeof c=="object"?{...c}:c))}const s=Array.isArray(o?.object)?o.object.map(a=>a&&typeof a=="object"?{...a}:a):[];r.length>0&&(e=r),s.length>0&&(n=s)}}catch{}if(!Array.isArray(n)||n.length===0)try{const o=T.tableObject&&Array.isArray(T.tableObject.data)?T.tableObject.data:null;Array.isArray(o)&&o.length>0&&(n=o)}catch{}if(!Array.isArray(n)||n.length===0)try{const o=localStorage.getItem("objectTableData");if(o){const r=JSON.parse(o);Array.isArray(r)&&r.length>0&&(n=r)}}catch{}if(!Array.isArray(n)||n.length===0)try{let o=null;if(typeof T.getActiveConfiguration=="function"&&(o=T.getActiveConfiguration()),!o){const s=ot(),a=Number(s?.activeConfigId),c=Array.isArray(s?.configurations)?s.configurations:[];Number.isFinite(a)&&(o=c.find(i=>Number(i?.id)===a)||null),!o&&c.length>0&&(o=c[0])}const r=o&&Array.isArray(o.object)?o.object:null;Array.isArray(r)&&r.length>0&&(n=r.map(s=>s&&typeof s=="object"?{...s}:s))}catch{}return{opticalSystemRows:e,sourceRows:t,objectRows:n}}function ht(e){try{if(!Array.isArray(e)||e.length===0)return .5876;const t=o=>{if(o===!0||o===1)return!0;const r=String(o??"").trim().toLowerCase();return r.includes("primary")||r==="true"||r==="yes"||r==="1"};let n=NaN;for(let o=0;o<e.length;o++){const r=e[o],s=Number(r?.wavelength);if(!(!Number.isFinite(s)||s<=0)&&(Number.isFinite(n)||(n=s),t(r?.primary)))return s}return Number.isFinite(n)&&n>0?n:.5876}catch{return .5876}}function mn(e,t){try{const n=qr(e,t,!0),o=Number(n?.pupilDiameterMm),r=Number(n?.focalLengthMm);return{pupilDiameterMm:o,focalLengthMm:r}}catch{}return{pupilDiameterMm:NaN,focalLengthMm:NaN}}function Yc(e,t){try{const n=ht(t),{pupilDiameterMm:o,focalLengthMm:r}=mn(e,n);if(![n,o,r].every(Number.isFinite))return NaN;if(n<=0||o<=0||r<=0)return NaN;const s=r/o;if(!Number.isFinite(s)||s<=0)return NaN;const a=1.22*n*s;return Number.isFinite(a)&&a>0?a:NaN}catch{return NaN}}async function Xc(e){const t=`analysis-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;let n=null;ko("Starting analysis...",180);const{opticalSystemRows:o,sourceRows:r,objectRows:s}=lt();try{try{n=await zn("analysis-progress",c=>{try{const i=c?.payload;if(!i||String(i.jobId||"")!==t)return;const l=String(i?.message||i?.phase||"Computing analysis..."),m=Number(i?.percent),p=Number.isFinite(m)?m:null;kt({label:l,percent:p})}catch{}})}catch(c){try{console.warn("[analysis-progress] listen unavailable in this window, continuing without progress events:",c)}catch{}n=null}const a=await Ss({...e,jobId:t,opticalSystemRows:o,sourceRows:r,objectRows:s});return kt({label:"Done",percent:100}),a}finally{if(typeof n=="function")try{n()}catch{}window.setTimeout(()=>{try{Po()}catch{}},220)}}T.runDesktopAnalysisComputeForPopup=Xc;async function ki(e){const{opticalSystemRows:t,sourceRows:n,objectRows:o}=lt(),r=`native-opd-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;let s=null;try{ko("Native OPD: starting...",140);try{s=await zn("analysis-progress",u=>{try{const h=u?.payload||{};if(!h||String(h.jobId||"")!==r)return;const g=Number(h.percent);kt({label:h.message||"Native OPD running...",percent:Number.isFinite(g)?Math.max(0,Math.min(100,g)):null})}catch{}})}catch(u){try{console.warn("[analysis-progress] listen unavailable for native OPD, continuing without progress events:",u)}catch{}s=null}const a=Gn(o,Number.isFinite(Number(e?.objectIndex))?Number(e.objectIndex):0,Number.isFinite(Number(e?.wavelengthUm))?Number(e.wavelengthUm):ht(n||[])),c=Et(),i=Number(a?.fieldAngle?.x??0),l=Number(a?.fieldAngle?.y??0),m=Math.abs(i)>1e-12||Math.abs(l)>1e-12,p=c==="stop"||c==="entrance"?c:String(a?.type||"").toLowerCase()==="angle"&&m?"entrance":void 0,d=await ho({jobId:r,opticalSystemRows:t,sourceRows:n,objectRows:o,objectIndex:Number.isFinite(Number(e?.objectIndex))?Number(e.objectIndex):0,gridSize:Number.isFinite(Number(e?.gridSize))?Number(e.gridSize):129,wavelengthUm:Number.isFinite(Number(e?.wavelengthUm))?Number(e.wavelengthUm):void 0,pupilSamplingMode:p,opdDisplayMode:e?.opdDisplayMode||"pistonTiltRemoved"});return kt({label:"Native OPD done",percent:100}),d}finally{try{s&&s()}catch{}setTimeout(()=>{try{Po()}catch{}},220)}}T.runDesktopNativeOpdMapForPopup=ki;async function Pi(e){const t=`native-psf-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;let n=null;try{ko("Native PSF: starting...",140);try{n=await zn("analysis-progress",r=>{try{const s=r?.payload||{};if(!s||String(s.jobId||"")!==t)return;const a=Number(s.percent);kt({label:s.message||"Native PSF running...",percent:Number.isFinite(a)?Math.max(0,Math.min(100,a)):null})}catch{}})}catch(r){try{console.warn("[analysis-progress] listen unavailable for native PSF, continuing without progress events:",r)}catch{}n=null}const o=await Gr({jobId:t,gridOpd:Array.isArray(e?.gridOpd)?e.gridOpd:[],pupilMask:Array.isArray(e?.pupilMask)?e.pupilMask:[],gridAmplitude:Array.isArray(e?.gridAmplitude)?e.gridAmplitude:void 0,wavelengthUm:Number(e?.wavelengthUm),pixelSizeUm:Number.isFinite(Number(e?.pixelSizeUm))?Number(e?.pixelSizeUm):void 0,removeTilt:!!e?.removeTilt,zeroPadTo:Number.isFinite(Number(e?.zeroPadTo))?Number(e?.zeroPadTo):void 0,recenterIfWrapped:!!e?.recenterIfWrapped});return kt({label:"Native PSF done",percent:100}),o}finally{try{n&&n()}catch{}setTimeout(()=>{try{Po()}catch{}},220)}}T.runDesktopNativePsfMapForPopup=Pi;async function Ei(e){return await Vr({psfData:Array.isArray(e?.psfData)?e.psfData:[],pixelSizeUm:Number(e?.pixelSizeUm),maxFrequencyLpmm:Number.isFinite(Number(e?.maxFrequencyLpmm))?Number(e.maxFrequencyLpmm):void 0,points:Number.isFinite(Number(e?.points))?Number(e.points):void 0})}T.runDesktopNativeMtfMapForPopup=Ei;async function Ti(e){const{opticalSystemRows:t,sourceRows:n,objectRows:o}=lt(),r=`native-tfmtf-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;let s=null;const a=Number.isFinite(Number(e?.samplingSize))?Math.max(32,Math.floor(Number(e.samplingSize))):256,c=Number.isFinite(Number(e?.zeroPadTo))?Math.floor(Number(e.zeroPadTo)):0,i=Number.isFinite(Number(e?.objectIndex))?Math.max(0,Math.floor(Number(e.objectIndex))):0,l=Gn(o,i,Number.isFinite(Number(e?.wavelengths?.[0]))?Number(e.wavelengths?.[0]):ht(n||[])),m=Et(),p=Number(l?.fieldAngle?.x??0),d=Number(l?.fieldAngle?.y??0),u=Math.abs(p)>1e-12||Math.abs(d)>1e-12,h=m==="stop"||m==="entrance"?m:String(l?.type||"").toLowerCase()==="angle"&&u?"entrance":void 0;let g=Number.NaN;if(Array.isArray(e?.wavelengths)&&e.wavelengths.length>0){const f=Number(e.wavelengths[0]);Number.isFinite(f)&&f>0&&(g=f)}(!Number.isFinite(g)||g<=0)&&(g=ht(n||[])),(!Number.isFinite(g)||g<=0)&&(g=.5876);let y=Number.NaN,A=Number.NaN;try{const f=Dn(t||[],g),w=Number(f?.fNumberWorking),b=Number(f?.focalLengthMm);Number.isFinite(w)&&w>0&&Number.isFinite(b)&&b>0&&(A=Math.abs(b),y=A/w)}catch{}if(!(Number.isFinite(y)&&y>0)||!(Number.isFinite(A)&&A>0)){const f=mn(t||[],g);y=Number(f?.pupilDiameterMm),A=Number(f?.focalLengthMm)}const x=!c||c===0?Math.max(a,512):Math.max(a,c),S=g*Math.abs(Number(A))/Math.max(1e-12,Math.abs(Number(y)))*(a/x),N=f=>{try{const w=e?.onProgress;if(typeof w!="function")return;w({percent:Number.isFinite(Number(f?.percent))?Number(f?.percent):void 0,message:typeof f?.message=="string"&&f.message.trim()?f.message:void 0})}catch{}};if(ze())try{s=await zn("analysis-progress",f=>{try{const w=f?.payload||{};if(!w||String(w.jobId||"")!==r)return;const b=Number(w?.percent),v=String(w?.message||w?.phase||"Computing Through-Focus MTF...");N({percent:Number.isFinite(b)?Math.max(0,Math.min(100,b)):void 0,message:v})}catch{}})}catch{s=null}try{return await xs({jobId:r,opticalSystemRows:t,sourceRows:n,objectRows:o,objectIndex:i,pupilSamplingMode:e?.pupilSamplingMode==="stop"||e?.pupilSamplingMode==="entrance"?e.pupilSamplingMode:h,wavelengths:Array.isArray(e?.wavelengths)?e.wavelengths.filter(f=>Number.isFinite(Number(f))&&Number(f)>0).map(f=>Number(f)):[],targetFrequencyLpmm:Number.isFinite(Number(e?.targetFrequencyLpmm))?Number(e.targetFrequencyLpmm):10,defocusMinMm:Number.isFinite(Number(e?.defocusMinMm))?Number(e.defocusMinMm):-.5,defocusMaxMm:Number.isFinite(Number(e?.defocusMaxMm))?Number(e.defocusMaxMm):.5,steps:Number.isFinite(Number(e?.steps))?Math.floor(Number(e.steps)):21,samplingSize:a,zeroPadTo:x,pixelSizeUm:S,opdDisplayMode:String(e?.opdDisplayMode||"pistonTiltRemoved")},N)}finally{try{s&&s()}catch{}}}T.runDesktopNativeThroughFocusMtfForPopup=Ti;async function Kc(e){if(!ze())throw new Error("Desktop runtime is not available");const{opticalSystemRows:t,sourceRows:n}=lt(),o=Number.isFinite(Number(e?.objectIndex))?Math.max(0,Math.floor(Number(e.objectIndex))):0,r=Number.isFinite(Number(e?.targetFrequencyLpmm))?Number(e.targetFrequencyLpmm):10,s=Number.isFinite(Number(e?.samplingSize))?Math.max(32,Math.floor(Number(e.samplingSize))):256,a=Number.isFinite(Number(e?.zeroPadTo))?Math.floor(Number(e.zeroPadTo)):0,c=!a||a===0?Math.max(s,512):Math.max(s,a),i=String(e?.opdDisplayMode||"pistonTiltRemoved"),l=i==="raw"||i==="pistonTiltDefocusRemoved"?i:"pistonTiltRemoved",m=Number(e?.wavelengthUm),p=Number.isFinite(m)&&m>0?m:ht(n||[]),d=Number.isFinite(p)&&p>0?p:.5876,u=(k,$,te)=>{if(!Array.isArray(k)||!Array.isArray($)||k.length!==$.length)return NaN;const q=k.map((z,V)=>({x:Number(z),y:Number($[V])})).filter(z=>Number.isFinite(z.x)&&Number.isFinite(z.y)).sort((z,V)=>z.x-V.x);if(!q.length)return NaN;if(q.length===1||te<=q[0].x)return q[0].y;if(te>=q[q.length-1].x)return q[q.length-1].y;for(let z=1;z<q.length;z++){const V=q[z-1],K=q[z];if(te<=K.x&&K.x>V.x){const oe=(te-V.x)/(K.x-V.x);return V.y+oe*(K.y-V.y)}}return q[q.length-1].y},h=(k,$)=>{if(!Array.isArray(k)||!Array.isArray($)||k.length!==$.length||k.length===0)return NaN;let te=0,q=1/0;for(let V=0;V<k.length;V++){const K=Number(k[V]);if(!Number.isFinite(K))continue;const oe=Math.abs(K);oe<q&&(q=oe,te=V)}const z=Number($[te]);return Number.isFinite(z)?z:NaN},g=await Ti({objectIndex:o,pupilSamplingMode:e?.pupilSamplingMode,wavelengths:[d],targetFrequencyLpmm:r,defocusMinMm:0,defocusMaxMm:0,steps:3,samplingSize:s,zeroPadTo:c,opdDisplayMode:l}),y=Array.isArray(g?.series)?g.series[0]:null,A=Array.isArray(g?.xAxis)?g.xAxis:[],x=h(A,Array.isArray(y?.mtfTangential)?y.mtfTangential:[]),_=h(A,Array.isArray(y?.mtfSagittal)?y.mtfSagittal:[]),S=await ki({objectIndex:o,gridSize:s,wavelengthUm:d,opdDisplayMode:l}),N=s,f=Array.from({length:N},()=>new Float32Array(N)),w=Array.from({length:N},()=>new Float32Array(N)),b=Array.from({length:N},()=>Array(N).fill(!1)),v=Array.isArray(S?.displayOpdGrid)?S.displayOpdGrid:[],M=Array.isArray(S?.rawOpdGrid)?S.rawOpdGrid:[];for(let k=0;k<N;k++){const $=v[k]||[],te=M[k]||[];for(let q=0;q<N;q++){const z=te[q];if(z==null)continue;const V=Number(z);if(!Number.isFinite(V))continue;const K=$[q],oe=K==null?NaN:Number(K),re=Number.isFinite(oe)?oe:V;f[k][q]=re*d,w[k][q]=1,b[k][q]=!0}}let C=Number.NaN,D=Number.NaN,B=Number.NaN,j=Number.NaN,H=Number.NaN;try{const k=Dn(t||[],d),$=Number(k?.fNumberWorking),te=Number(k?.focalLengthMm),q=Number(k?.naImage),z=Number(k?.cutoffLpmm);Number.isFinite($)&&$>0&&Number.isFinite(te)&&te>0&&(D=Math.abs(te),C=D/$,B=$),Number.isFinite(q)&&q>0&&(j=q),Number.isFinite(z)&&z>0&&(H=z)}catch{}if(!(Number.isFinite(C)&&C>0)||!(Number.isFinite(D)&&D>0)){const k=mn(t||[],d);C=Number(k?.pupilDiameterMm),D=Number(k?.focalLengthMm)}const U=d*Math.abs(Number(D))/Math.max(1e-12,Math.abs(Number(C)))*(s/c);if(!(Number.isFinite(B)&&B>0)){const k=Math.abs(Number(D))/Math.max(1e-12,Math.abs(Number(C)));Number.isFinite(k)&&k>0&&(B=k)}!(Number.isFinite(H)&&H>0)&&Number.isFinite(B)&&B>0&&(H=1e3/(Math.max(1e-12,d)*B));const G=await Pi({gridOpd:Array.from({length:N},(k,$)=>Array.from(f[$]||[])),gridAmplitude:Array.from({length:N},(k,$)=>Array.from(w[$]||[])),pupilMask:Array.from({length:N},(k,$)=>Array.from(b[$]||[])),wavelengthUm:d,pixelSizeUm:U,removeTilt:!1,zeroPadTo:c,recenterIfWrapped:!1}),F=await Ei({psfData:G?.psfData,pixelSizeUm:U,maxFrequencyLpmm:Number.isFinite(r)?Math.max(1,r):10,points:121}),W=Array.isArray(F?.frequencyAxis)?F.frequencyAxis:[],I=Array.isArray(F?.mtfTangential)?F.mtfTangential:[],E=Array.isArray(F?.mtfSagittal)?F.mtfSagittal:[],P=u(W,I,r),Z=u(W,E,r),R={backend:"desktop-native-rust",conditions:{objectIndex:o,wavelengthUm:d,targetFrequencyLpmm:r,samplingSize:s,zeroPadTo:c,opdDisplayMode:l,pupilSamplingMode:String(S?.pupilSamplingMode||""),pixelSizeUm:U,fNumberForDiffraction:B,naImage:j,cutoffLpmm:H},mtf:{tangential:P,sagittal:Z,nyquistLpmm:Number(F?.nyquistLpmm)},tfmtfAtDefocus0:{tangential:x,sagittal:_},delta:{tangential:Number.isFinite(P)&&Number.isFinite(x)?x-P:NaN,sagittal:Number.isFinite(Z)&&Number.isFinite(_)?_-Z:NaN}};return console.log("📊 [Native Rust MTF vs TFMTF parity]",R),R}T.runDesktopNativeCompareMtfVsTfmtfForPopup=Kc;async function Eo(e){if(!ze())throw new Error("Desktop runtime is not available");const{opticalSystemRows:t,sourceRows:n,objectRows:o}=lt(),r=Number.isFinite(Number(e?.samplingSize))?Math.max(32,Math.floor(Number(e.samplingSize))):256,s=Number.isFinite(Number(e?.zeroPadTo))?Math.floor(Number(e.zeroPadTo)):0,a=Number.isFinite(Number(e?.objectIndex))?Math.max(0,Math.floor(Number(e.objectIndex))):0,c=Et(),i=e?.pupilSamplingMode==="stop"||e?.pupilSamplingMode==="entrance"?e.pupilSamplingMode:c==="stop"||c==="entrance"?c:void 0,l=I=>Number.isInteger(I)&&I>0&&(I&I-1)===0,m=I=>{let E=1;const P=Math.max(1,Math.floor(Number(I)||1));for(;E<P&&E<4096;)E<<=1;return E},p=Number.isFinite(Number(e?.firstFrequencyLpmm))?Number(e.firstFrequencyLpmm):10,d=Number.isFinite(Number(e?.secondFrequencyLpmm))?Number(e.secondFrequencyLpmm):30,u=Number.isFinite(Number(e?.fieldMin))?Number(e.fieldMin):0,h=Number.isFinite(Number(e?.fieldMax))?Number(e.fieldMax):10,g=Math.min(u,h),y=Math.max(u,h),A=Number.isFinite(Number(e?.steps))?Math.max(3,Math.floor(Number(e.steps))):21,x=String(e?.opdDisplayMode||"pistonTiltRemoved"),_=x==="raw"||x==="pistonTiltDefocusRemoved"?x:"pistonTiltRemoved",S=e?.fieldAxisMode==="height"?"height":"angle",N=121,f=Number.isFinite(s)&&s>=r&&l(s),w=Math.max(r,2*(N-1)),b=m(w);let v=f?s:b;f&&v===r&&r<=32&&(v=64);const M=(()=>{const I=Array.isArray(e?.wavelengths)?e.wavelengths.filter(P=>Number.isFinite(Number(P))&&Number(P)>0).map(P=>Number(P)):[];if(I.length>0)return I;const E=ht(n||[]);return[Number.isFinite(E)&&E>0?E:.5876]})(),C=Array.from({length:A},(I,E)=>{if(A<=1)return g;const P=E/(A-1);return g+P*(y-g)}),D=(I,E)=>{try{const P=e?.onProgress;typeof P=="function"&&P({percent:I,message:E})}catch{}},B=(I,E,P)=>{if(!Array.isArray(I)||!Array.isArray(E)||I.length!==E.length)return 0;const Z=I.map((R,k)=>({x:Number(R),y:Number(E[k])})).filter(R=>Number.isFinite(R.x)&&Number.isFinite(R.y)).sort((R,k)=>R.x-k.x);if(!Z.length)return 0;if(Z.length===1||P<=Z[0].x)return Math.max(0,Math.min(1,Z[0].y));if(P>=Z[Z.length-1].x)return Math.max(0,Math.min(1,Z[Z.length-1].y));for(let R=1;R<Z.length;R++){const k=Z[R-1],$=Z[R];if(P<=$.x&&$.x>k.x){const te=(P-k.x)/($.x-k.x);return Math.max(0,Math.min(1,k.y+te*($.y-k.y)))}}return Math.max(0,Math.min(1,Z[Z.length-1].y))},j=(I,E)=>{if(!Array.isArray(I)||I.length===0||!Number.isFinite(E))return[null,null];const P=I.map(Z=>Number(Z)).filter(Z=>Number.isFinite(Z)).sort((Z,R)=>Z-R);if(!P.length)return[null,null];if(E<=P[0])return[P[0],P[0]];if(E>=P[P.length-1])return[P[P.length-1],P[P.length-1]];for(let Z=1;Z<P.length;Z++)if(E<=P[Z])return[P[Z-1],P[Z]];return[P[P.length-1],P[P.length-1]]},H=I=>{const E=Array.isArray(o)?o.map(R=>{try{return JSON.parse(JSON.stringify(R))}catch{return{...R||{}}}}):[];E.length||E.push(S==="angle"?{name:"AutoField0",position:"Angle",xHeightAngle:0,yHeightAngle:I,x:0,y:I}:{name:"AutoField0",position:"Rectangle",xHeight:0,yHeight:I,x:0,y:I});const P=Math.max(0,Math.min(a,E.length-1)),Z=E[P]&&typeof E[P]=="object"?E[P]:{};return S==="angle"?(Z.position="Angle",Z.xHeightAngle=0,Z.yHeightAngle=I,Z.x=0,Z.y=I):(Z.position="Rectangle",Z.xHeight=0,Z.yHeight=I,Z.x=0,Z.y=I),E[P]=Z,E},ce=I=>{const P=Number(I);return Math.abs(0)>0||Math.abs(P)>0?Math.abs(0)>=Math.abs(P)?"x":"y":"x"},U=I=>{let E=Number.NaN,P=Number.NaN;try{const R=Dn(t||[],I),k=Number(R?.fNumberWorking),$=Number(R?.focalLengthMm);Number.isFinite($)&&$>0&&Number.isFinite(k)&&k>0&&(P=Math.abs($),E=P/k)}catch{}if(!(Number.isFinite(E)&&E>0))try{const R=Number(Zr(t||[])),k=Number.isFinite(R)&&R>=0?t?.[R]:null,$=k?.semidia??k?.Semidia??k?.["Semi Diameter"]??k?.aperture??k?.Aperture??NaN,te=Math.abs(parseFloat($));if(Number.isFinite(te)&&te>0){const z=!!(k&&(k.aperture!==void 0||k.Aperture!==void 0))?te*.5:te;Number.isFinite(z)&&z>0&&(E=z*2)}}catch{}if(!(Number.isFinite(P)&&P>0))try{const R=Number(Yr(t||[],I));Number.isFinite(R)&&Math.abs(R)>1e-9&&R!==1/0&&(P=Math.abs(R))}catch{}if(!(Number.isFinite(E)&&E>0)||!(Number.isFinite(P)&&P>0)){const R=mn(t||[],I);Number.isFinite(E)&&E>0||(E=Number(R?.pupilDiameterMm)),Number.isFinite(P)&&P>0||(P=Number(R?.focalLengthMm))}return Number.isFinite(P)&&P>0||(P=100),Number.isFinite(E)&&E>0||(E=10),I*Math.abs(Number(P))/Math.max(1e-12,Math.abs(Number(E)))*(r/v)},G=[],F=Math.max(1,M.length*C.length);let W=0;D(5,`Computing Object MTF: ${M.length} wavelength(s), ${C.length} field points`);for(const I of M){const E=[],P=[],Z=[],R=[],k=[],$=U(I);for(let te=0;te<C.length;te++){const q=C[te],z=W+1,V=(I*1e3).toFixed(1),K=S==="height"?"mm":"deg",oe=5+z/F*90;D(Math.max(5,Math.min(95,oe)),`Computing Object MTF: λ=${V}nm, point ${te+1}/${C.length} (${Number(q).toFixed(3)} ${K})`);const re=H(q),pe=await ho({opticalSystemRows:t,sourceRows:n,objectRows:re,objectIndex:a,surfaceIndex:void 0,gridSize:r,wavelengthUm:I,pupilSamplingMode:i||(S==="angle"?"entrance":void 0),opdDisplayMode:_}),me=r,L=Array.from({length:me},()=>Array.from({length:me},()=>0)),X=Array.from({length:me},()=>Array.from({length:me},()=>!1)),Y=Array.isArray(pe?.displayOpdGrid)?pe.displayOpdGrid:[],J=Array.isArray(pe?.rawOpdGrid)?pe.rawOpdGrid:[];for(let ue=0;ue<me;ue++){const ve=Y[ue]||[],ke=J[ue]||[];for(let Fe=0;Fe<me;Fe++){const Le=ke[Fe];if(Le==null||Le==="")continue;const Ue=Number(Le);if(!Number.isFinite(Ue))continue;const Be=ve[Fe],bt=Be==null||Be===""?NaN:Number(Be),At=Number.isFinite(bt)?bt:Ue;L[ue][Fe]=At*I,X[ue][Fe]=!0}}const le=await Gr({gridOpd:L,pupilMask:X,wavelengthUm:I,pixelSizeUm:$,removeTilt:!1,zeroPadTo:v,recenterIfWrapped:!1}),Q=await Vr({psfData:le?.psfData,pixelSizeUm:$,maxFrequencyLpmm:Math.max(1,Math.max(p,d)*2),points:121}),ee=Array.isArray(Q?.frequencyAxis)?Q.frequencyAxis:[],ie=Array.isArray(Q?.mtfTangential)?Q.mtfTangential:[],be=Array.isArray(Q?.mtfSagittal)?Q.mtfSagittal:[],ae=ce(q),he=ae==="x"?be:ie,de=ae==="x"?ie:be,Ae=B(ee,he,p),xe=B(ee,de,p),Ne=B(ee,he,d),ye=B(ee,de,d);E.push(Ae),P.push(xe),Z.push(Ne),R.push(ye);const[Se,_e]=j(ee,p),[Ce,Me]=j(ee,d),ge=Number(pe?.sampleCount||0),we=Number(pe?.hitCount||0);k.push({fieldValue:q,effectivePupilSamplingMode:String(pe?.pupilSamplingMode||""),usedObjectPosition:String(pe?.usedObjectPosition||""),targetSurfaceIndex:Number(pe?.targetSurface),usedObjectIndex:Number(pe?.usedObjectIndex),opdSampleCount:ge,opdHitCount:we,opdHitRate:ge>0?we/ge:0,firstFrequencyLpmm:p,firstBracketLowLpmm:Se,firstBracketHighLpmm:_e,firstValueMeridional:Ae,firstValueSagittal:xe,secondFrequencyLpmm:d,secondBracketLowLpmm:Ce,secondBracketHighLpmm:Me,secondValueMeridional:Ne,secondValueSagittal:ye}),W+=1}G.push({wavelengthUm:I,label:`${(I*1e3).toFixed(1)}nm`,meridionalFirst:E,sagittalFirst:P,meridionalSecond:Z,sagittalSecond:R,fieldDiagnostics:k})}return{backend:"desktop-native-field-mtf-via-analysis-mtf",xAxis:C,axisMode:S,series:G,message:"Native Object MTF computed via background analysis MTF sampling"}}T.runDesktopNativeFieldMtfForPopup=Eo;async function Jc(e){if(ze())return Eo(e||{});const{opticalSystemRows:t,sourceRows:n,objectRows:o}=lt();return As({opticalSystemRows:t,sourceRows:n,objectRows:o,objectIndex:Number.isFinite(Number(e?.objectIndex))?Math.max(0,Math.floor(Number(e?.objectIndex))):0,pupilSamplingMode:e?.pupilSamplingMode,wavelengths:Array.isArray(e?.wavelengths)?e.wavelengths:void 0,firstFrequencyLpmm:Number.isFinite(Number(e?.firstFrequencyLpmm))?Number(e?.firstFrequencyLpmm):void 0,secondFrequencyLpmm:Number.isFinite(Number(e?.secondFrequencyLpmm))?Number(e?.secondFrequencyLpmm):void 0,fieldMin:Number.isFinite(Number(e?.fieldMin))?Number(e?.fieldMin):void 0,fieldMax:Number.isFinite(Number(e?.fieldMax))?Number(e?.fieldMax):void 0,steps:Number.isFinite(Number(e?.steps))?Math.max(3,Math.floor(Number(e?.steps))):void 0,samplingSize:Number.isFinite(Number(e?.samplingSize))?Math.max(32,Math.floor(Number(e?.samplingSize))):void 0,zeroPadTo:Number.isFinite(Number(e?.zeroPadTo))?Math.floor(Number(e?.zeroPadTo)):void 0,opdDisplayMode:e?.opdDisplayMode,fieldAxisMode:e?.fieldAxisMode,onProgress:e?.onProgress})}T.runPortableFieldMtfForPopup=Jc;function Qc(e){const t=Array.isArray(e)?e:[],n=t.map(r=>String(r?.position??r?.fieldType??r?.field_type??r?.field??r?.type??"").toLowerCase()).filter(Boolean);return n.some(r=>r.includes("rect")||r.includes("rectangle")||r.includes("height"))?"height":n.some(r=>r.includes("angle"))?"angle":t.some(r=>{const s=parseFloat(r?.yHeight??r?.y??r?.height??r?.y_height??Number.NaN);return Number.isFinite(s)&&Math.abs(s)>0})?"height":"angle"}function el(e){if(Qc(e)==="height"){const a=(Array.isArray(e)?e:[]).map(p=>parseFloat(p?.yHeight??p?.y??p?.height??p?.y_height??Number.NaN)).filter(p=>Number.isFinite(p));if(!a.length)return{fieldValues:[.001],heightMode:!0};let c=Math.min(...a),i=Math.max(...a);if(c<=0&&(c=.001,i<c&&(i=c)),c===i)return{fieldValues:[c],heightMode:!0};const l=10;return{fieldValues:Array.from({length:l},(p,d)=>{const u=d/(l-1);return parseFloat((c+(i-c)*u).toFixed(6))}),heightMode:!0}}let n=0;for(const a of Array.isArray(e)?e:[]){const c=[a?.yFieldAngle,a?.yAngle,a?.fieldAngle,a?.xFieldAngle,a?.xAngle,a?.xHeightAngle,a?.yHeightAngle];for(const i of c)typeof i=="number"&&Number.isFinite(i)&&(n=Math.max(n,Math.abs(i)))}n>0||(n=20);let o=1;n<=5?o=.5:n<=15?o=1:n<=40?o=2:o=Math.ceil(n/25);const r=n*.001,s=[];for(let a=r;a<=n+1e-9;a+=o)s.push(parseFloat(a.toFixed(6)));return s[s.length-1]!==n&&s.push(n),{fieldValues:s,heightMode:!1}}async function tl(e){const{opticalSystemRows:t,sourceRows:n,objectRows:o}=lt(),{fieldValues:r,heightMode:s}=el(o||[]),a=(()=>{const i=Array.isArray(e?.wavelengths)?e.wavelengths.filter(p=>Number.isFinite(Number(p))&&Number(p)>0).map(p=>Number(p)):[];if(i.length)return i;const l=(Array.isArray(n)?n:[]).map(p=>Number(p?.wavelength)).filter(p=>Number.isFinite(p)&&p>0);if(l.length)return l;const m=ht(n||[]);return[Number.isFinite(m)&&m>0?m:.5876]})(),c=[];for(let i=0;i<a.length;i++){const l=a[i],m=i/Math.max(1,a.length)*100,p=100/Math.max(1,a.length),d=await Ns({opticalSystemRows:t,sourceRows:n,objectRows:o,fieldSamples:r,heightMode:s,wavelength:l});c.push({fieldValues:Array.isArray(d?.fieldValues)?d.fieldValues:r,idealHeights:Array.isArray(d?.idealHeights)?d.idealHeights:[],realHeights:Array.isArray(d?.realHeights)?d.realHeights:[],distortion:Array.isArray(d?.distortion)?d.distortion:[],distortionPercent:Array.isArray(d?.distortionPercent)?d.distortionPercent:[],meta:{...d?.meta||{},wavelength:l,heightMode:s}});try{e?.onProgress?.({percent:m+p,message:`Distortion (λ=${l.toFixed(4)} μm)`})}catch{}}return{backend:"portable-distortion",allData:c}}async function nl(e){const{opticalSystemRows:t,sourceRows:n,objectRows:o}=lt(),r=Number.isFinite(Number(e?.wavelength))&&Number(e?.wavelength)>0?Number(e?.wavelength):(()=>{const a=ht(n||[]);return Number.isFinite(a)&&a>0?a:.5876})(),s=await _s({opticalSystemRows:t,sourceRows:n,objectRows:o,gridSize:Number.isFinite(Number(e?.gridSize))?Number(e?.gridSize):20,wavelength:r});try{e?.onProgress?.({percent:100,message:"Done"})}catch{}return{backend:"portable-grid-distortion",idealGrid:{x:Array.isArray(s?.idealX)?s.idealX:[],y:Array.isArray(s?.idealY)?s.idealY:[]},realGrid:{x:Array.isArray(s?.realX)?s.realX:[],y:Array.isArray(s?.realY)?s.realY:[]},gridSize:Number.isFinite(Number(s?.gridSize))?Number(s.gridSize):20,maxFieldAngle:Number.isFinite(Number(s?.maxFieldAngle))?Number(s.maxFieldAngle):0,meta:{...s?.meta||{},wavelength:r}}}T.runPortableDistortionDataForPopup=tl;T.runPortableGridDistortionForPopup=nl;async function ol(e){const t=await Eo(e||{}),n=Array.isArray(t?.series)?t.series:[],o=Array.isArray(n?.[0]?.fieldDiagnostics)?n[0].fieldDiagnostics:[],r=[];let s=0,a=0,c=0;for(let u=1;u<o.length;u++){const h=o[u-1]||{},g=o[u]||{};String(h.effectivePupilSamplingMode||"")!==String(g.effectivePupilSamplingMode||"")&&(s+=1),String(h.usedObjectPosition||"")!==String(g.usedObjectPosition||"")&&(a+=1),Number(h.targetSurfaceIndex)!==Number(g.targetSurfaceIndex)&&(c+=1);const y=Math.abs(Number(g.firstValueMeridional)-Number(h.firstValueMeridional)),A=Math.abs(Number(g.firstValueSagittal)-Number(h.firstValueSagittal)),x=Math.abs(Number(g.secondValueMeridional)-Number(h.secondValueMeridional)),_=Math.abs(Number(g.secondValueSagittal)-Number(h.secondValueSagittal));[y,A,x,_].some(S=>Number.isFinite(S)&&S>.12)&&r.push({fromField:Number(h.fieldValue),toField:Number(g.fieldValue),deltaFirstMeridional:y,deltaFirstSagittal:A,deltaSecondMeridional:x,deltaSecondSagittal:_,opdHitRateFrom:Number(h.opdHitRate),opdHitRateTo:Number(g.opdHitRate),opdHitRateDelta:Math.abs(Number(g.opdHitRate)-Number(h.opdHitRate)),modeFrom:String(h.effectivePupilSamplingMode||""),modeTo:String(g.effectivePupilSamplingMode||""),usedObjectPositionFrom:String(h.usedObjectPosition||""),firstBracketTo:[Number(g.firstBracketLowLpmm),Number(g.firstBracketHighLpmm)],secondBracketTo:[Number(g.secondBracketLowLpmm),Number(g.secondBracketHighLpmm)],usedObjectPositionTo:String(g.usedObjectPosition||""),targetSurfaceFrom:Number(h.targetSurfaceIndex),targetSurfaceTo:Number(g.targetSurfaceIndex),usedObjectIndexFrom:Number(h.usedObjectIndex),usedObjectIndexTo:Number(g.usedObjectIndex)})}let i=0,l=0,m=0,p=0;for(const u of r)String(u.modeFrom||"")!==String(u.modeTo||"")&&(i+=1),String(u.usedObjectPositionFrom||"")!==String(u.usedObjectPositionTo||"")&&(l+=1),Number(u.targetSurfaceFrom)!==Number(u.targetSurfaceTo)&&(m+=1),Number.isFinite(Number(u.opdHitRateDelta))&&Number(u.opdHitRateDelta)>.03&&(p+=1);const d={conditions:{objectIndex:Number.isFinite(Number(e?.objectIndex))?Number(e?.objectIndex):0,firstFrequencyLpmm:Number.isFinite(Number(e?.firstFrequencyLpmm))?Number(e?.firstFrequencyLpmm):10,secondFrequencyLpmm:Number.isFinite(Number(e?.secondFrequencyLpmm))?Number(e?.secondFrequencyLpmm):30,fieldMin:Number.isFinite(Number(e?.fieldMin))?Number(e?.fieldMin):0,fieldMax:Number.isFinite(Number(e?.fieldMax))?Number(e?.fieldMax):10,steps:Number.isFinite(Number(e?.steps))?Number(e?.steps):21,samplingSize:Number.isFinite(Number(e?.samplingSize))?Number(e?.samplingSize):256,zeroPadTo:Number.isFinite(Number(e?.zeroPadTo))?Number(e?.zeroPadTo):0,opdDisplayMode:String(e?.opdDisplayMode||"pistonTiltRemoved"),fieldAxisMode:e?.fieldAxisMode==="height"?"height":"angle",pupilSamplingMode:e?.pupilSamplingMode},diagnosticsCount:o.length,summary:{discontinuityCount:r.length,modeSwitchAcrossAll:s,positionSwitchAcrossAll:a,surfaceSwitchAcrossAll:c,modeSwitchOnDiscontinuity:i,positionSwitchOnDiscontinuity:l,surfaceSwitchOnDiscontinuity:m,hitRateJumpOnDiscontinuity:p},discontinuities:r,diagnostics:o};try{console.log("📊 [Object MTF native diagnostics await]",d)}catch{}return d}T.runDesktopNativeFieldMtfDiagnosticsForPopup=ol;function Gn(e,t,n){const o=Array.isArray(e)?e:[],r=Number.isInteger(t)?Math.max(0,Math.min(t,Math.max(0,o.length-1))):0,s=o[r]||{},a=String(s?.position??s?.object??s?.type??"").trim().toLowerCase();return a.includes("angle")||a==="point"?{objectIndex:r,type:"Angle",fieldAngle:{x:Number(s?.xHeightAngle??s?.xFieldAngle??s?.xAngle??s?.x??0)||0,y:Number(s?.yHeightAngle??s?.yFieldAngle??s?.fieldAngle??s?.yAngle??s?.angle??s?.y??0)||0},xHeight:0,yHeight:0,wavelength:Number.isFinite(n)&&n>0?n:.5876}:{objectIndex:r,type:"Height",fieldAngle:{x:0,y:0},xHeight:Number(s?.xHeight??s?.x??0)||0,yHeight:Number(s?.yHeight??s?.y??s?.height??0)||0,wavelength:Number.isFinite(n)&&n>0?n:.5876}}function gr(e,t,n=12){const o=Array.isArray(e)?e:[],r=Array.isArray(t)?t:[],s=Math.min(o.length,r.length),a=s>0?Math.min(Array.isArray(o[0])?o[0].length:0,Array.isArray(r[0])?r[0].length:0):0,c=[];let i=0,l=0,m=0,p=0,d=0,u=0,h=0,g=0,y=0;for(let C=0;C<s;C++){const D=Array.isArray(o[C])?o[C]:[],B=Array.isArray(r[C])?r[C]:[];for(let j=0;j<a;j++){const H=D[j]===null||D[j]===void 0?NaN:Number(D[j]),ce=B[j]===null||B[j]===void 0?NaN:Number(B[j]);if(!Number.isFinite(H)||!Number.isFinite(ce))continue;const U=Math.abs(H-ce);i+=1,l+=U,m+=U*U,U>p&&(p=U),d+=H,u+=ce,h+=H*H,g+=ce*ce,y+=H*ce,c.push({x:j,y:C,ts:H,native:ce,abs:U})}}const A=i>0?d/i:0,x=i>0?u/i:0,_=h-d*d/Math.max(1,i),S=g-u*u/Math.max(1,i),N=y-d*u/Math.max(1,i),f=Math.abs(_)>1e-20?N/_:NaN,w=Number.isFinite(f)?x-f*A:NaN,b=_>1e-20&&S>1e-20?N/Math.sqrt(_*S):NaN;let v=NaN,M=NaN;if(i>0){let C=0,D=0;for(const B of c){if(Number.isFinite(f)&&Number.isFinite(w)){const H=f*B.ts+w,ce=B.native-H;C+=ce*ce}const j=B.native- -B.ts;D+=j*j}Number.isFinite(f)&&Number.isFinite(w)&&(v=Math.sqrt(C/i)),M=Math.sqrt(D/i)}return c.sort((C,D)=>D.abs-C.abs),{overlap:{width:a,height:s,comparedPointCount:i},maxAbsDeltaWaves:p,meanAbsDeltaWaves:i>0?l/i:0,rmsDeltaWaves:i>0?Math.sqrt(m/i):0,affine:{slopeTsToNative:f,interceptTsToNative:w,correlation:b,rmsAfterAffineFit:v,rmsAfterNegTs:M,meanTs:A,meanNative:x},topDiffs:c.slice(0,Math.max(1,Math.floor(n))).map(C=>({x:C.x,y:C.y,tsWaves:C.ts,nativeWaves:C.native,absDeltaWaves:C.abs}))}}async function rl(e={}){const{opticalSystemRows:t,sourceRows:n,objectRows:o}=lt();if(!Array.isArray(t)||t.length===0)throw new Error("No optical system data");if(!Array.isArray(o)||o.length===0)throw new Error("No object data");const r=Number.isFinite(Number(e?.objectIndex))?Math.max(0,Math.floor(Number(e.objectIndex))):Math.max(0,o.length-1),s=Math.max(0,Math.min(r,o.length-1)),a=Math.max(16,Math.floor(Number(e?.gridSize??128))),c=Number.isFinite(Number(e?.wavelengthUm))?Number(e.wavelengthUm):ht(n),i=Number.isFinite(Number(e?.zeroPadTo))?Number(e.zeroPadTo):0,l=String(e?.opdDisplayMode||"pistonTiltRemoved"),m=Gn(o,s,c),p=sn(t,c),u=await an(p).generateWavefrontMap(m,a,"circular",{recordRays:!1,progressEvery:0,renderFromZernike:!1,skipZernikeFit:!0,opdMode:"simple",opdDisplayMode:l});if(u&&u.error){const se=String(u.error?.message||"Wavefront map generation failed");throw new Error(se)}const h=a,g=Array.from({length:h},()=>new Float32Array(h)),y=Array.from({length:h},()=>new Float32Array(h)),A=Array.from({length:h},()=>Array(h).fill(!1)),x=new Float32Array(h),_=new Float32Array(h),S=Number.isFinite(Number(u?.pupilRange))&&Number(u.pupilRange)>0?Number(u.pupilRange):1;for(let se=0;se<h;se++){const pe=se/(h-1||1)*2-1;x[se]=pe*S,_[se]=pe*S}const N=Array.isArray(u?.pupilCoordinates)?u.pupilCoordinates:[],f=u?.display&&Array.isArray(u.display.opds)?u.display.opds:Array.isArray(u?.opds)?u.opds:[],w=Math.min(N.length,f.length);for(let se=0;se<w;se++){const pe=N[se],me=Number.isInteger(pe?.ix)?pe.ix:null,L=Number.isInteger(pe?.iy)?pe.iy:null;if(me===null||L===null||me<0||me>=h||L<0||L>=h)continue;const X=Number(f[se]);Number.isFinite(X)&&(A[L][me]=!0,g[L][me]=X,y[L][me]=1)}const b={gridSize:h,wavelength:c,rayData:[],gridData:{opd:g,amplitude:y,pupilMask:A,xCoords:x,yCoords:_}},v=mn(t,c),M=Number.isFinite(Number(v?.pupilDiameterMm))&&Number(v.pupilDiameterMm)>0?Number(v.pupilDiameterMm):10,C=Number.isFinite(Number(v?.focalLengthMm))&&Number(v.focalLengthMm)>0?Number(v.focalLengthMm):100,D=new Sa,B={samplingSize:h,wavelength:c,zeroPadTo:i,pupilDiameter:M,focalLength:C,removeTilt:!1,recenterIfWrapped:!1},j=await D.calculatePSF(b,{...B,forceImplementation:ze()?"native":"javascript"}),H=await D.calculatePSF(b,{...B,forceImplementation:"javascript"}),ce=Array.isArray(j?.psfData)?j.psfData:[],U=Array.isArray(H?.psfData)?H.psfData:[],G=Math.min(ce.length,U.length),F=G>0?Math.min((ce[0]||[]).length,(U[0]||[]).length):0;let W=0,I=0,E=0,P=0;for(let se=0;se<G;se++)for(let pe=0;pe<F;pe++){const me=Number(ce[se]?.[pe]),L=Number(U[se]?.[pe]);if(!Number.isFinite(me)||!Number.isFinite(L))continue;const X=me-L,Y=Math.abs(X);I+=X*X,E+=Y,Y>P&&(P=Y),W++}const Z=W>0?Math.sqrt(I/W):NaN,R=W>0?E/W:NaN,k=j?.metrics||{},$=H?.metrics||{},te=Number(k?.fwhm?.average),q=Number($?.fwhm?.average),z=Number(k?.strehlRatio),V=Number($?.strehlRatio),K=k?.centerPosition||{},oe=$?.centerPosition||{},re="📊 [PSF Parity][main] native-vs-js grid="+G+"x"+F+" n="+W+" rmsAbs="+(Number.isFinite(Z)?Z.toExponential(4):"NaN")+" meanAbs="+(Number.isFinite(R)?R.toExponential(4):"NaN")+" maxAbs="+(Number.isFinite(P)?P.toExponential(4):"NaN")+" strehlΔ="+(Number.isFinite(z)&&Number.isFinite(V)?(z-V).toExponential(4):"NaN")+" fwhmAvgΔ="+(Number.isFinite(te)&&Number.isFinite(q)?(te-q).toExponential(4):"NaN")+"µm centerΔ=("+(Number(K?.x)-Number(oe?.x))+","+(Number(K?.y)-Number(oe?.y))+")",fe={objectIndex:s,gridSize:h,wavelengthUm:c,impl:{native:String(j?.implementationUsed||"unknown"),js:String(H?.implementationUsed||"unknown")},abs:{rms:Number.isFinite(Z)?Z:null,mean:Number.isFinite(R)?R:null,max:Number.isFinite(P)?P:null},delta:{strehl:Number.isFinite(z)&&Number.isFinite(V)?z-V:null,fwhmAvgUm:Number.isFinite(te)&&Number.isFinite(q)?te-q:null,centerX:Number(K?.x)-Number(oe?.x),centerY:Number(K?.y)-Number(oe?.y)}};return globalThis.__lastPsfParityLine=re,globalThis.__lastPsfParity=fe,console.log(re),{parityLine:re,parity:fe,nativeResult:j,jsResult:H}}T.runPsfParityInMain=rl;function il(e,t){const n=Array.isArray(e)?e:[],o=n.length,r=o>0&&Array.isArray(n[0])?n[0].length:0;if(o===0||r===0)return[];const s=(c,i)=>{const l=n[i];return Array.isArray(l)?l[c]??null:null},a=(c,i,l)=>{const m=Array.from({length:c},()=>Array.from({length:i},()=>null));for(let p=0;p<c;p++)for(let d=0;d<i;d++)m[p][d]=l(d,p);return m};switch(t){case"identity":return a(o,r,(c,i)=>s(c,i));case"flipX":return a(o,r,(c,i)=>s(r-1-c,i));case"flipY":return a(o,r,(c,i)=>s(c,o-1-i));case"flipXY":return a(o,r,(c,i)=>s(r-1-c,o-1-i));case"transpose":return a(r,o,(c,i)=>s(i,c));case"transposeFlipX":return a(r,o,(c,i)=>s(i,o-1-c));case"transposeFlipY":return a(r,o,(c,i)=>s(r-1-i,c));case"transposeFlipXY":return a(r,o,(c,i)=>s(r-1-i,o-1-c));default:return a(o,r,(c,i)=>s(c,i))}}function yr(e,t,n){const o=["identity","flipX","flipY","flipXY","transpose","transposeFlipX","transposeFlipY","transposeFlipXY"];let r={alignment:"identity",summary:gr(e,t,n)};for(const s of o){const a=il(e,s),c=gr(a,t,n),i=Number(c?.overlap?.comparedPointCount||0),l=Number(r?.summary?.overlap?.comparedPointCount||0),m=Number(c?.rmsDeltaWaves||1/0),p=Number(r?.summary?.rmsDeltaWaves||1/0);(i>l||i===l&&m<p)&&(r={alignment:s,summary:c})}return r}function hr(e,t,n,o="display"){const r=f=>!Array.isArray(f)||f.length===0?!1:Array.isArray(f[0]),s=f=>(Array.isArray(f)?f:[]).map(b=>Array.isArray(b)?b.map(v=>{if(v==null)return null;const M=Number(v);return Number.isFinite(M)?M:null}):[]),a=Array.isArray(e?.pupilCoordinates)?e.pupilCoordinates:[],c=Number.isFinite(Number(t))?Math.max(3,Math.floor(Number(t))):129,i=Array.from({length:c},()=>Array.from({length:c},()=>null)),l=e?.display?.opdsInWavelengths,m=e?.display?.opds,p=e?.raw?.opdsInWavelengths??e?.opdsInWavelengths,d=e?.raw?.opds??e?.opds,u=o==="raw"?p:l,h=o==="raw"?d:m,g=o==="raw"?l:p,y=o==="raw"?m:d;if(r(u))return s(u);if(r(g))return s(g);const A=Array.isArray(u)?u:Array.isArray(g)?g:Array.isArray(h)?h:Array.isArray(y)?y:[],x=!Array.isArray(u)&&!Array.isArray(g);if(!Array.isArray(A)||A.length===0)return i;const _=f=>{if(f==null)return null;const w=Number(f);return Number.isFinite(w)?x?!Number.isFinite(n)||n<=0?null:w/n:w:null};let S=0;for(let f=0;f<A.length;f++){const w=a[f];if(!w||!Number.isFinite(Number(w?.ix))||!Number.isFinite(Number(w?.iy)))continue;const b=Math.floor(Number(w.ix)),v=Math.floor(Number(w.iy));if(b<0||b>=c||v<0||v>=c)continue;const M=_(A[f]);Number.isFinite(Number(M))&&(i[v][b]=Number(M),S+=1)}if(S>0)return i;const N=Math.min(A.length,c*c);for(let f=0;f<N;f++){const w=Math.floor(f/c),b=f%c,v=_(A[f]);Number.isFinite(Number(v))&&(i[w][b]=Number(v))}return i}async function sl(e={}){if(!ze())throw new Error("Desktop runtime is not available");const{opticalSystemRows:t,sourceRows:n,objectRows:o}=lt();if(!Array.isArray(t)||t.length===0)throw new Error("compareOpdTsVsRustForPopup: opticalSystemRows is empty");const r=Number.isInteger(e?.objectIndex)?Number(e.objectIndex):0,s=Number.isFinite(Number(e?.gridSize))?Math.max(17,Math.floor(Number(e.gridSize))):129,a=Number.isFinite(Number(e?.wavelengthUm))?Number(e?.wavelengthUm):ht(n),c=e?.opdDisplayMode||"pistonTiltRemoved",i=Gn(o,r,a),l=Et(),m=Number(i?.fieldAngle?.x??0),p=Number(i?.fieldAngle?.y??0),d=Math.abs(m)>1e-12||Math.abs(p)>1e-12,u=l==="stop"||l==="entrance"?l:String(i?.type||"").toLowerCase()==="angle"&&d?"entrance":void 0,h=sn(t,a),g=Number(h?._getCachedStopRadiusMm?.()),y=await ho({opticalSystemRows:t,sourceRows:n,objectRows:o,objectIndex:r,gridSize:s,wavelengthUm:a,pupilRadiusMm:Number.isFinite(g)&&g>0?g:void 0,pupilSamplingMode:u,opdDisplayMode:c}),x=await an(h).generateWavefrontMap(i,s,"circular",{recordRays:!1,progressEvery:0,renderFromZernike:!1,skipZernikeFit:!0,opdMode:"simple",opdDisplayMode:c}),_=hr(x,s,a,"display"),S=hr(x,s,a,"raw"),N=Array.isArray(y?.displayOpdGrid)?y.displayOpdGrid:Array.isArray(y?.rawOpdGrid)?y.rawOpdGrid:[],f=Array.isArray(y?.rawOpdGrid)?y.rawOpdGrid:Array.isArray(y?.displayOpdGrid)?y.displayOpdGrid:[],w=yr(_,N,Number(e?.topK)||12),b=yr(S,f,Number(e?.topK)||12),v={...w.summary,alignment:w.alignment},M={...b.summary,alignment:b.alignment},C={config:{objectIndex:r,gridSize:s,wavelengthUm:a,opdDisplayMode:c},nativeMeta:{backend:y?.backend,targetSurface:y?.targetSurface,stopSurface:y?.stopSurface,usedObjectPosition:y?.usedObjectPosition,usedObjectX:y?.usedObjectX,usedObjectY:y?.usedObjectY,hitCount:y?.hitCount,sampleCount:y?.sampleCount,message:y?.message},rawSummary:M,summary:v};try{console.log("📊 [OPD TS-Rust][opener] summary",{objectIndex:r,gridSize:s,wavelengthUm:a,mode:c,tsGridShape:{h:Array.isArray(_)?_.length:0,w:Array.isArray(_)&&Array.isArray(_[0])?_[0].length:0},comparedPointCount:v?.overlap?.comparedPointCount,maxAbsDeltaWaves:v?.maxAbsDeltaWaves,meanAbsDeltaWaves:v?.meanAbsDeltaWaves,rmsDeltaWaves:v?.rmsDeltaWaves,rawComparedPointCount:M?.overlap?.comparedPointCount,rawMaxAbsDeltaWaves:M?.maxAbsDeltaWaves,rawMeanAbsDeltaWaves:M?.meanAbsDeltaWaves,rawRmsDeltaWaves:M?.rmsDeltaWaves,alignment:v?.alignment,rawAlignment:M?.alignment,affineSlopeTsToNative:v?.affine?.slopeTsToNative,affineInterceptTsToNative:v?.affine?.interceptTsToNative,affineCorrelation:v?.affine?.correlation,affineRmsAfterFit:v?.affine?.rmsAfterAffineFit,affineRmsAfterNegTs:v?.affine?.rmsAfterNegTs,rawAffineSlopeTsToNative:M?.affine?.slopeTsToNative,rawAffineInterceptTsToNative:M?.affine?.interceptTsToNative,rawAffineCorrelation:M?.affine?.correlation,rawAffineRmsAfterFit:M?.affine?.rmsAfterAffineFit,rawAffineRmsAfterNegTs:M?.affine?.rmsAfterNegTs,nativeHitCount:y?.hitCount,nativeSampleCount:y?.sampleCount})}catch{}try{T.__COOPT_LAST_OPD_TS_RUST_DIFF={...C,at:Date.now()}}catch{}return C}T.compareOpdTsVsRustForPopup=sl;function al(e,t){if(!Array.isArray(e))return[];const n=Number(t),o=e.map(l=>l&&typeof l=="object"?{...l}:l);if(!Number.isFinite(n)||Math.abs(n)<1e-15)return o;const r=o.findIndex(l=>String(l?.["object type"]??l?.object??l?.Object??"").trim().toLowerCase()==="image"),s=r>0?r-1:Math.max(0,o.length-2);if(s<0||s>=o.length)return o;const a=o[s]&&typeof o[s]=="object"?{...o[s]}:{},c=Number(a.thickness),i=Number.isFinite(c)?c:0;return a.thickness=i+n,o[s]=a,o}async function cl(e){const t=f=>{const w=Number(f);return!Number.isFinite(w)||w<=0?null:w>10?w/1e3:w},n=f=>{if(f===!0||f===1)return!0;const w=String(f??"").trim().toLowerCase();return w==="1"||w==="true"||w==="yes"||w.includes("primary")},o=(f,w)=>{const b=String(f?.id??"").trim();if(b)return b;const v=String(f?.name??"").trim();if(v)return v;const M=String(f?.position??f?.object??"").trim();return M?`Object ${w+1} (${M})`:`Object ${w+1}`},r=(f,w)=>{const b=String(w||"all").trim().toLowerCase()==="primary"?"primary":"all",v=["#2563eb","#dc2626","#16a34a","#7c3aed","#ea580c","#0891b2","#4f46e5","#0f766e"],M=(Array.isArray(f)?f:[]).map((D,B)=>{const j=t(D?.wavelength??D?.Wavelength);return!Number.isFinite(j)||j<=0?null:{wavelengthUm:j,isPrimary:n(D?.primary??D?.Primary),name:String(D?.name??"").trim(),idx:B}}).filter(D=>!!D);if(M.length===0)return[{wavelengthUm:.5876,label:"Primary 587.6nm",color:v[0],isPrimary:!0}];const C=[];for(let D=0;D<M.length;D++){const B=M[D];if(C.some(ce=>Math.abs(ce.wavelengthUm-B.wavelengthUm)<1e-12))continue;const j=B.wavelengthUm*1e3,H=Number.isFinite(j)?j.toFixed(1):"587.6";C.push({wavelengthUm:B.wavelengthUm,label:B.isPrimary?`Primary ${H}nm`:B.name||`${H}nm`,color:v[C.length%v.length],isPrimary:B.isPrimary})}if(b==="primary"){const D=C.find(B=>B.isPrimary)||C[0];return D?[D]:[]}return C},{opticalSystemRows:s,sourceRows:a,objectRows:c}=lt(),i=Array.isArray(e?.objectRows)?e.objectRows:null,l=i&&i.length>0?i:c,m=al(s,e?.defocusMm),p=Number.isInteger(e?.surfaceIndex)?Number(e.surfaceIndex):void 0,d=typeof performance<"u"&&typeof performance.now=="function"?performance.now():Date.now(),u=e?.pattern?String(e.pattern):"annular",h=Yc(m,a),g=typeof performance<"u"&&typeof performance.now=="function"?performance.now():Date.now(),y={opticalSystemRows:m,sourceRows:a,objectRows:l,surfaceIndex:p,rayCount:Number.isInteger(e?.rayCount)?e.rayCount:void 0,ringCount:Number.isInteger(e?.ringCount)?e.ringCount:void 0,pattern:u,wavelengthMode:e?.wavelengthMode?String(e.wavelengthMode):void 0};try{const f=u==="grid"||u==="annular"?u:"annular",w=Number.isInteger(e?.rayCount)?Math.max(1,Number(e?.rayCount)):501,b=Number.isInteger(e?.ringCount)?Math.max(1,Number(e?.ringCount)):10,v=r(a,e?.wavelengthMode?String(e.wavelengthMode):void 0),M=[];if(Array.isArray(l)&&l.length>0&&v.length>0)for(let C=0;C<l.length;C++){const D=l[C];for(let B=0;B<v.length;B++){const j=v[B],H=jn(D,m,w,null,{annularRingCount:b,wavelengthUm:j.wavelengthUm,pattern:f}),ce=(Array.isArray(H)?H:[]).map(U=>({startP:{x:Number(U?.startP?.x)||0,y:Number(U?.startP?.y)||0,z:Number(U?.startP?.z)||0},dir:{x:Number(U?.dir?.x)||0,y:Number(U?.dir?.y)||0,z:Number(U?.dir?.z)||1},wavelengthUm:j.wavelengthUm,pupilU:Number.isFinite(Number(U?.planeCoords?.u))?Number(U.planeCoords.u):void 0,pupilV:Number.isFinite(Number(U?.planeCoords?.v))?Number(U.planeCoords.v):void 0,isChief:U?.isChief===!0||U?.isChief==null&&(U?.rayIndex===0||U?.index===0)})).filter(U=>Number.isFinite(U.startP.x)&&Number.isFinite(U.startP.y)&&Number.isFinite(U.startP.z));ce.length!==0&&M.push({label:`${o(D,C)} ${j.label}`,color:j.color,hasFieldAngle:String(D?.position??D?.object??"").trim().toLowerCase()==="angle",rays:ce})}}M.length>0&&(y.raySeries=M)}catch{}let A=await zo(y);if((Number(A?.totalHitRays)||0)<=0){const w=(b=>{if(!Array.isArray(b)||b.length===0)return;const v=M=>String(M??"").trim().toLowerCase().replace(/[\s_-]+/g,"");for(let M=0;M<b.length;M++){const C=b[M]||{},D=v(C?.["object type"]??C?.object??C?.Object??C?.type??C?.surfType);if(D==="image"||D.startsWith("image"))return M}for(let M=b.length-1;M>=0;M--){const C=b[M]||{},D=v(C?.["object type"]??C?.object??C?.Object??C?.type??C?.surfType);if(D!=="object"&&D!=="gap"&&D!=="airgap")return M}})(m);if(Number.isInteger(w)&&w!==p){A=await zo({...y,surfaceIndex:w});try{A.__surfaceIndexRetriedFrom=p,A.__surfaceIndexRetriedTo=w}catch{}}}const _=typeof performance<"u"&&typeof performance.now=="function"?performance.now():Date.now(),S=Number(A?.rayGenerationMs),N=Number(A?.traceMs);return{...A,airyRadiusUm:Number.isFinite(h)?h:void 0,timingMs:{raySeriesGeneration:Number.isFinite(S)?S:0,nativeInvoke:Math.max(0,_-g),nativeTrace:Number.isFinite(N)?N:0,total:Math.max(0,_-d)}}}T.runDesktopNativeSpotRaytraceForPopup=cl;async function ll(e){const{opticalSystemRows:t,sourceRows:n,objectRows:o}=lt();return Rs({opticalSystemRows:t,sourceRows:n,objectRows:o,surfaceIndex:Number.isInteger(e?.surfaceIndex)?Number(e.surfaceIndex):void 0,rayCount:Number.isInteger(e?.rayCount)?Number(e.rayCount):void 0,referenceFocusMode:e?.referenceFocusMode||"current-paraxial",wavelengthMode:e?.wavelengthMode||"all"})}T.runDesktopNativeSphericalAberrationForPopup=ll;function ul(e){const t=r=>{const s=Number(r);return!Number.isFinite(s)||s<=0?null:s>10?s/1e3:s},n=Array.isArray(e)?e:[],o=[];for(const r of n){const s=t(r?.wavelength??r?.Wavelength);if(!(s===null||!Number.isFinite(s)||s<=0)&&(o.some(a=>Math.abs(a-s)<1e-12)||o.push(s),o.length>=6))break}return o.length>0?o:[.5876]}function pl(e,t){const n=p=>{const d=new Map;for(const u of Array.isArray(p)?p:[]){const h=Number(u?.wavelength);if(!Number.isFinite(h))continue;const g=h.toFixed(7);d.set(g,u)}return d},o=[],r=(p,d,u)=>{const h=n(d),g=n(u),y=[];for(const[A,x]of g.entries()){const _=h.get(A);if(!_)continue;const S=Array.isArray(_?.points)?_.points:[],N=Array.isArray(x?.points)?x.points:[];if(S.length===0||N.length===0)continue;const f=new Map;for(const M of S){const C=Number(M?.pupilCoordinate),D=Number(M?.longitudinalAberration);!Number.isFinite(C)||!Number.isFinite(D)||f.set(C.toFixed(6),D)}let w=0,b=0,v=0;for(const M of N){const C=Number(M?.pupilCoordinate),D=Number(M?.longitudinalAberration);if(!Number.isFinite(C)||!Number.isFinite(D))continue;const B=f.get(C.toFixed(6));if(!Number.isFinite(B))continue;const j=Math.abs(D-B);w=Math.max(w,j),b+=j,v+=1}y.push({wl:Number(x?.wavelength),maxAbs:w,sumAbs:b,count:v})}return y},s=r("meridional",e?.meridionalData,t?.meridionalData),a=r("sagittal",e?.sagittalData,t?.sagittalData),c=new Map;for(const p of s){if(!Number.isFinite(p.wl))continue;const d=c.get(p.wl)||{wavelengthUm:p.wl,meridionalMaxAbsDeltaMm:0,sagittalMaxAbsDeltaMm:0,comparedPointCount:0,sumAbs:0};d.meridionalMaxAbsDeltaMm=Math.max(d.meridionalMaxAbsDeltaMm,p.maxAbs),d.comparedPointCount+=p.count,d.sumAbs+=p.sumAbs,c.set(p.wl,d)}for(const p of a){if(!Number.isFinite(p.wl))continue;const d=c.get(p.wl)||{wavelengthUm:p.wl,meridionalMaxAbsDeltaMm:0,sagittalMaxAbsDeltaMm:0,comparedPointCount:0,sumAbs:0};d.sagittalMaxAbsDeltaMm=Math.max(d.sagittalMaxAbsDeltaMm,p.maxAbs),d.comparedPointCount+=p.count,d.sumAbs+=p.sumAbs,c.set(p.wl,d)}let i=0,l=0,m=0;for(const p of c.values())i=Math.max(i,p.meridionalMaxAbsDeltaMm,p.sagittalMaxAbsDeltaMm),l+=p.sumAbs,m+=p.comparedPointCount,o.push({wavelengthUm:p.wavelengthUm,meridionalMaxAbsDeltaMm:p.meridionalMaxAbsDeltaMm,sagittalMaxAbsDeltaMm:p.sagittalMaxAbsDeltaMm,comparedPointCount:p.comparedPointCount});return o.sort((p,d)=>p.wavelengthUm-d.wavelengthUm),{comparedPointCount:m,maxAbsDeltaMm:i,meanAbsDeltaMm:m>0?l/m:0,largeDeltaThresholdMm:.001,hasLargeDelta:i>.001,perWavelength:o}}async function dl(e){const t=e?.rustResult;if(!t||typeof t!="object")throw new Error("compareSphericalAberrationTsVsRustForPopup: rustResult is required");const{opticalSystemRows:n,sourceRows:o}=lt();if(!Array.isArray(n)||n.length===0)throw new Error("compareSphericalAberrationTsVsRustForPopup: opticalSystemRows is empty");const r=Number.isInteger(e?.surfaceIndex)?Number(e.surfaceIndex):n.length-1,s=Number.isInteger(e?.rayCount)?Number(e.rayCount):51,a=e?.referenceFocusMode||"current-paraxial",c=ul(o),{calculateLongitudinalAberrationAsync:i}=await Re(async()=>{const{calculateLongitudinalAberrationAsync:p}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(d=>d.bR);return{calculateLongitudinalAberrationAsync:p}},__vite__mapDeps([0,1])),l=await i(n,r,c,s,{silent:!0,referenceFocusMode:a}),m=pl(l,t);try{console.log("📊 [SA TS-Rust][opener] summary",{comparedPointCount:m?.comparedPointCount,maxAbsDeltaMm:m?.maxAbsDeltaMm,meanAbsDeltaMm:m?.meanAbsDeltaMm,hasLargeDelta:m?.hasLargeDelta})}catch{}try{T.__COOPT_LAST_SA_TS_RUST_DIFF={...m,at:Date.now(),referenceFocusMode:a,rayCount:s,surfaceIndex:r}}catch{}return m}T.compareSphericalAberrationTsVsRustForPopup=dl;function ml(){try{if(typeof window<"u"&&window.__COOPT_DISABLE_DESKTOP_RUST_ANALYSIS===!0)return!1;if(typeof window<"u"&&window.__COOPT_FORCE_DESKTOP_RUST_ANALYSIS===!0)return!0}catch{}return!0}T.shouldUseDesktopRustAnalysis=ml;const uo="coopt.forceInfinitePupilMode",vn="coopt-force-infinite-pupil-mode-changed";function ln(e){const t=typeof e=="string"?e.trim().toLowerCase():"";return t==="stop"||t==="entrance"?t:""}function Et(){try{const e=T.__COOPT_FORCE_INFINITE_PUPIL_MODE;if(e)return ln(e)}catch{}try{const e=localStorage.getItem(uo);return ln(e)}catch{return""}}function po(e){const t=ln(e);try{if(t)T.__COOPT_FORCE_INFINITE_PUPIL_MODE=t;else try{delete T.__COOPT_FORCE_INFINITE_PUPIL_MODE}catch{T.__COOPT_FORCE_INFINITE_PUPIL_MODE=void 0}}catch{}try{t?localStorage.setItem(uo,t):localStorage.removeItem(uo)}catch{}}function fl(){const e=Et();if(e)try{T.__COOPT_FORCE_INFINITE_PUPIL_MODE=e}catch{}}function Oi(){ze()&&(T.__cooptForceInfinitePupilModeBridgeInstalled||(T.__cooptForceInfinitePupilModeBridgeInstalled=!0,T.__cooptBroadcastForceInfinitePupilMode=e=>{const t=ln(e);po(t),(async()=>{try{const n=await Re(()=>import("./event-B_hCB_W5.js"),__vite__mapDeps([2,0,1]));if(n&&typeof n.emit=="function"&&await n.emit(vn,{mode:t}),n&&typeof n.emitTo=="function"){try{await n.emitTo("main",vn,{mode:t})}catch{}try{await n.emitTo("settings-window",vn,{mode:t})}catch{}}}catch{}})()},(async()=>{try{const e=await Re(()=>import("./event-B_hCB_W5.js"),__vite__mapDeps([2,0,1]));if(!e||typeof e.listen!="function")return;const t=await e.listen(vn,n=>{const o=ln(n?.payload?.mode);po(o)});T.__cooptForceInfinitePupilModeBridgeUnlisten=t}catch{}})()))}T.__cooptGetForceInfinitePupilMode=Et;T.__cooptSetForceInfinitePupilMode=po;fl();Oi();function In(){return{getOpticalSystemRows:T.getOpticalSystemRows||(()=>[]),getObjectRows:T.getObjectRows||(()=>[]),generateCrossBeam:T.generateCrossBeam||(()=>({results:[]})),generateInfiniteSystemCrossBeam:T.generateInfiniteSystemCrossBeam||(()=>({results:[]})),drawOpticalSystemSurfaces:T.drawOpticalSystemSurfaces||(()=>{}),drawCrossBeamRays:T.drawCrossBeamRays||(()=>{}),harmonizeSceneGeometry:T.harmonizeSceneGeometry||(()=>{}),clearAllOpticalElements:T.clearAllOpticalElements||(()=>{})}}function Di(e){return""}function Jn(e,t,n){const o=Di(),r=`${t}${o}`;try{const s=e?.document?.getElementById?.("status");s&&(s.textContent=r)}catch{}try{e?.postMessage?.({status:r},"*")}catch{}}function gl(){if(T.popupMessageHandlerRegistered)return;T.popupMessageHandlerRegistered=!0;const e=t=>{try{const n=T.popupCamera||t?.camera,o=T.popupRenderer||t?.renderer;if(!n?.isOrthographicCamera||!o||typeof o.getSize!="function")return;const r=t?.THREE||T.THREE;if(!r?.Vector2)return;const s=o.getSize(new r.Vector2),a=Number(s?.x)||0,c=Number(s?.y)||0;if(!Number.isFinite(a)||!Number.isFinite(c)||a<2||c<2)return;const i=a/c,l=n.top-n.bottom||1,m=(n.left+n.right)/2,p=(n.top+n.bottom)/2,d=l*i;n.left=m-d/2,n.right=m+d/2,n.top=p+l/2,n.bottom=p-l/2,n.updateProjectionMatrix()}catch{}};window.addEventListener("message",async t=>{const n=T.popup3DWindow;if(!n||t.source!==n)return;const o=t.data||{};if(o.action==="popup-ready"){const r=T.popup3DWindow;if(r&&!r.closed)try{const a={userAdjustedView:!1,viewAxis:"YZ",rayCount:(()=>{const c=document.getElementById("draw-ray-count-input"),i=parseInt(c?.value||"51",10);return Number.isFinite(i)&&i>0?i:51})(),rayColorMode:"object"};setTimeout(()=>{window.postMessage({action:"draw-cross",...a},"*")},100)}catch(s){console.error("Failed to trigger initial draw:",s)}return}if(o.action==="popup-resize"){if(!T.popup3DWindow){console.warn("⚠️ Popup window reference is unavailable (resize)");return}const r=T.popup3DWindow,a=(o?.viewAxis||T.__currentPopupViewAxis||"YZ").toString().toUpperCase()==="XZ"?"XZ":"YZ";try{const i=r.camera?.userData?.__drawCrossOrthoBounds,l=Number.isFinite(i?.centerZ)?i.centerZ:void 0,m={camera:r.camera,controls:r.controls,scene:r.scene,renderer:r.renderer,includeRayStartMargin:!0,preserveDrawCrossBounds:!0,storeDrawCrossBounds:!1,...Number.isFinite(l)?{centerZOverride:l}:{}};a==="XZ"&&typeof T.setCameraForXZCrossSection=="function"?T.setCameraForXZCrossSection(m):a==="YZ"&&typeof T.setCameraForYZCrossSection=="function"&&T.setCameraForYZCrossSection(m),e(r);const p=T.popupRenderer||r?.renderer,d=T.popupScene||r?.scene,u=T.popupCamera||r?.camera;p&&d&&u&&p.render(d,u)}catch(c){console.error("❌ Popup resize handling error:",c)}return}if(o.action==="draw-cross"){try{const r=T.popup3DWindow,a=(o?.viewAxis||"YZ").toString().toUpperCase()==="XZ"?"XZ":"YZ",c=o?.userAdjustedView===!0,i=o?.target&&Number.isFinite(o.target.x)&&Number.isFinite(o.target.y)&&Number.isFinite(o.target.z)?o.target:null,l=(()=>{const G=parseInt(o?.rayCount??51,10);return Number.isFinite(G)&&G>0?G:51})(),m=o?.rayColorMode==="segment"?"segment":"object";try{typeof T.setRayColorMode=="function"&&T.setRayColorMode(m)}catch{}const p=typeof globalThis<"u"?!!T.__cooptOptimizerIsRunning:!1;if(p||typeof T.loadActiveConfigurationToTables=="function"&&T.loadActiveConfigurationToTables(),!p)try{typeof globalThis<"u"&&(T.__cooptOpticalSystemRowsOverride=null)}catch{}const{getOpticalSystemRows:d,getObjectRows:u,drawOpticalSystemSurfaces:h,harmonizeSceneGeometry:g,clearAllOpticalElements:y,generateCrossBeam:A,generateInfiniteSystemCrossBeam:x,drawCrossBeamRays:_}=In(),S=d();try{const G=Array.isArray(S)?S.map((F,W)=>({index0:W,id:F&&typeof F=="object"?F.id??null:null,type:F&&typeof F=="object"?F.type??F["object type"]??"":"",_blockId:F&&typeof F=="object"?F._blockId??"":"",_surfaceRole:F&&typeof F=="object"?F._surfaceRole??"":""})):[];T.popup3DWindow?.postMessage({action:"surface-list",surfaces:G},"*")}catch{}if(!S||S.length===0){T.popup3DWindow.postMessage({status:"Error: No optical system data"},"*");return}const N=T.popupScene||T.popup3DWindow?.scene||null;if(!N){T.popup3DWindow.postMessage({status:"Error: Scene not ready"},"*");return}const f=T.popupRenderer||T.popup3DWindow?.renderer||null;N&&[...N.children].forEach(F=>{N.remove(F),F.geometry&&F.geometry.dispose(),F.material&&(Array.isArray(F.material)?F.material.forEach(W=>W.dispose()):F.material.dispose())}),typeof y=="function"&&y(N),typeof h=="function"&&h({opticalSystemData:S,scene:N,showSemidiaRing:!0,showSurfaceOrigins:!1,crossSectionOnly:!1}),typeof g=="function"&&g(N);let w=[];try{typeof u=="function"&&(w=u()||[])}catch{}if(!Array.isArray(w)||w.length===0)try{if(T.tableObject&&typeof T.tableObject.getData=="function")w=T.tableObject.getData();else{const G=document.getElementById("table-object");G&&G.tabulator?w=G.tabulator.getData():w=[]}}catch{w=[]}const v=(S[0]||{})?.thickness,M=v!=null&&v!=="",C=M?String(v).trim().toUpperCase():"",D=Number(v),B=M&&(v===1/0||C==="INF"||C==="INFINITY"||C==="∞"||Number.isFinite(D)&&Math.abs(D)>1e6),j=!w||w.length===0||w.every(G=>G.position==="Angle"||!G.height&&!G.y&&!G.xHeightAngle&&!G.yHeightAngle||parseFloat(G.height||0)===0),H=M?B:j,ce=(()=>{try{if(typeof T.getPrimaryWavelength=="function"){const G=Number(T.getPrimaryWavelength());if(Number.isFinite(G)&&G>0)return G}}catch{}throw new Error("Primary wavelength is unavailable. Please set Source Primary Wavelength.")})();let U;if(H){const G=w.map(E=>({x:parseFloat(E.xHeightAngle)||0,y:parseFloat(E.yHeightAngle)||0})),F=E=>{const P=E?.["object type"]??E?.object??E?.Object??E?.type??"",Z=String(P).trim().toLowerCase().replace(/[\s_-]+/g,"");return Z==="image"||Z.startsWith("image")},W=S.findIndex(E=>E&&F(E)),I=W>=0?W:Math.max(0,S.length-1);W<0&&console.warn(`⚠️ [DrawCross] Image surface not detected by object type. Falling back to last row index=${I}.`),U=await x(S,G,{rayCount:l,debugMode:!1,wavelength:ce,crossType:"both",targetSurfaceIndex:I,pupilSamplingMode:"entrance",logEntrancePupilConfig:!1,angleUnit:"deg",chiefZ:-20})}else{const G=W=>{const I=parseFloat(W);return Number.isFinite(I)?I:0},F=(w||[]).map((W,I)=>{if(Array.isArray(W))return{x:G(W[1]),y:G(W[2]),z:0,objectIndex:I};const E=G(W.xHeightAngle??W.x??W.height??W.heightX),P=G(W.yHeightAngle??W.y??W.height??W.heightY);return{x:E,y:P,z:0,objectIndex:W.objectIndex??I}});F.length===0&&F.push({x:0,y:0,z:0,objectIndex:0}),U=await A(S,F,{rayCount:l,debugMode:!1,wavelength:ce,crossType:"both"})}if(U.success){let G=[];if(U.results&&Array.isArray(U.results)?U.results.forEach(I=>{I.rays&&Array.isArray(I.rays)&&(G=G.concat(I.rays))}):U.allTracedRays&&Array.isArray(U.allTracedRays)?G=U.allTracedRays:U.tracedRays&&Array.isArray(U.tracedRays)?G=U.tracedRays:Array.isArray(U)&&(G=U),G&&G.length>0){_(G,N);try{r.__lastCrossRays=G,T.__lastCrossRays=G}catch{}}try{}catch(I){console.warn("⚠️ Popup lens fill postprocess failed:",I)}if(g(N),!r)console.warn("⚠️ Popup window reference missing (camera)");else if(a==="XZ"&&typeof T.setCameraForXZCrossSection=="function")T.setCameraForXZCrossSection({camera:T.popupCamera||r.camera,controls:T.popupControls||r.controls,scene:T.popupScene||r.scene,renderer:T.popupRenderer||r.renderer,includeRayStartMargin:!0,preserveDrawCrossBounds:c===!0,preserveCurrentOrthoBounds:c===!0,storeDrawCrossBounds:c!==!0,...c===!0&&i?{targetOverride:i}:{}});else if(a==="YZ"&&typeof T.setCameraForYZCrossSection=="function")T.setCameraForYZCrossSection({camera:T.popupCamera||r.camera,controls:T.popupControls||r.controls,scene:T.popupScene||r.scene,renderer:T.popupRenderer||r.renderer,includeRayStartMargin:!0,preserveDrawCrossBounds:c===!0,preserveCurrentOrthoBounds:c===!0,storeDrawCrossBounds:c!==!0,...c===!0&&i?{targetOverride:i}:{}});else{const I=T.popupRenderer||r?.renderer,E=T.popupCamera||r?.camera;I&&N&&E&&I.render(N,E),console.warn(`⚠️ setCameraFor${a}CrossSection not available`)}e(r);const F=T.popupRenderer||r?.renderer,W=T.popupCamera||r?.camera;F&&N&&W&&F.render(N,W),Jn(T.popup3DWindow,"Drawing complete",a)}else T.popup3DWindow.postMessage({status:"Error: "+U.error},"*")}catch(r){console.error("Error stack:",r.stack),T.popup3DWindow.postMessage({status:"Error: "+r.message},"*")}return}if(o.action==="view-xz"||o.action==="view-yz"){if(console.log("🎥 Handling popup view action:",o.action),!T.popup3DWindow)return;const r=T.popup3DWindow,s=T.popupScene||r.scene||null,a=T.popupCamera||r.camera||null,c=T.popupControls||r.controls||null,i=T.popupRenderer||r.renderer||null,l=r.document?.getElementById("status")||null;try{const m=o.action==="view-xz"?"XZ":"YZ",p=o?.userAdjustedView===!0,d=o?.target&&Number.isFinite(o.target.x)&&Number.isFinite(o.target.y)&&Number.isFinite(o.target.z)?o.target:null;if(!!a?.userData?.__drawCrossOrthoBounds&&s&&a&&c&&i&&typeof T.setCameraForXZCrossSection=="function"&&typeof T.setCameraForYZCrossSection=="function"){(({viewAxis:y,target:A})=>{const x=a,_=c,S=i,N=s;if(!x||!_)return;const f=()=>{try{if(!x||!x.isOrthographicCamera||!S||typeof S.getSize!="function")return;const F=r?.THREE||T.THREE,W=S.getSize(new F.Vector2),I=Number(W?.x)||0,E=Number(W?.y)||0;if(!Number.isFinite(I)||!Number.isFinite(E)||I<2||E<2)return;const P=I/E,Z=x.top-x.bottom||1,R=(x.left+x.right)/2,k=(x.top+x.bottom)/2,$=Z*P;x.left=R-$/2,x.right=R+$/2,x.top=k+Z/2,x.bottom=k-Z/2}catch{}},w=A||{x:_.target?.x??0,y:_.target?.y??0,z:_.target?.z??0},b=(x.position?.x??0)-w.x,v=(x.position?.y??0)-w.y,M=Math.abs(v)>Math.abs(b)?"XZ":"YZ";let C={...w};M==="YZ"&&y==="XZ"?C={x:w.y,y:0,z:w.z}:M==="XZ"&&y==="YZ"?C={x:0,y:w.x,z:w.z}:(y==="XZ"&&(C.y=0),y==="YZ"&&(C.x=0));const D=C.x-w.x,B=C.y-w.y,j=C.z-w.z;x.position.set((x.position?.x??0)+D,(x.position?.y??0)+B,(x.position?.z??0)+j),_.target.set(C.x,C.y,C.z);const H=(x.position?.x??0)-C.x,ce=(x.position?.y??0)-C.y,U=(x.position?.z??0)-C.z,G=Math.hypot(H,ce,U)||300;y==="XZ"?(x.position.set(C.x,C.y+G,C.z),x.up.set(1,0,0)):(x.position.set(C.x-G,C.y,C.z),x.up.set(0,1,0)),x.lookAt(C.x,C.y,C.z),_.update(),f(),x.updateProjectionMatrix(),S&&N&&S.render(N,x)})({viewAxis:m,target:p&&d?d:null});try{(b=>{if(!b)return;const v=[];b.traverse(M=>{const C=M.userData||{};if(C.type==="popupLensFill"||C.isUltraDebugOverlay===!0||C.isRayLine||C.type==="ray")return;const D=(M.name||"").toString(),B=C.type==="semidiaRing"||C.type==="ring"||C.surfaceType==="ring"||D.toLowerCase().includes("ring"),j=C.isLensSurface||C.surfaceType==="3DSurface"||D.toLowerCase().includes("lenssurface")||D.startsWith("surface")||D.startsWith("lens"),H=!!(M.material&&M.material.transparent&&typeof M.material.opacity=="number"&&M.material.opacity<1);(M.isMesh&&(j||H)||M.isLine&&B)&&v.push(M)}),[...new Set(v)].forEach(M=>{b.remove(M),M.geometry&&M.geometry.dispose(),M.material&&(Array.isArray(M.material)?M.material.forEach(C=>C.dispose()):M.material.dispose())})})(s);const{getOpticalSystemRows:A,drawOpticalSystemSurfaces:x,drawCrossBeamRays:_,harmonizeSceneGeometry:S}=In(),N=b=>{if(!b)return;const v=[];b.traverse(M=>{const C=M.userData||{};(C.isRayLine||C.type==="ray")&&v.push(M)}),[...new Set(v)].forEach(M=>{b.remove(M),M.geometry&&M.geometry.dispose(),M.material&&(Array.isArray(M.material)?M.material.forEach(C=>C.dispose()):M.material.dispose())})},f=A();Array.isArray(f)&&f.length>0&&(x({opticalSystemData:f,scene:s,showSemidiaRing:!1,showSurfaceOrigins:!1,crossSectionOnly:!0,crossSectionDirection:m}),S(s));const w=r.__lastCrossRays||T.__lastCrossRays;Array.isArray(w)&&w.length>0&&typeof _=="function"&&(N(s),_(w,s),S(s));try{Array.isArray(f)}catch(b){console.warn("⚠️ Popup lens fill postprocess failed:",b)}i&&s&&a&&i.render(s,a)}catch(y){console.error("Error updating surfaces:",y)}Jn(r,`${m==="XZ"?"X-Z":"Y-Z"} view ready`,m)}else{await Pn({viewAxis:m,statusElement:l,targetScene:s,targetCamera:a,targetControls:c,targetRenderer:i,showAlerts:!1});try{const{getOpticalSystemRows:g}=In(),y=g();Array.isArray(y),i&&s&&a&&i.render(s,a)}catch(g){console.warn("⚠️ Popup lens fill postprocess failed:",g)}Jn(r,`${m==="XZ"?"X-Z":"Y-Z"} view ready`,m)}}catch(m){r.postMessage({status:`Error: ${m.message}`},"*")}return}})}function Pn(e){const{viewAxis:t,buttonElement:n=null,statusElement:o=null,targetScene:r=null,targetCamera:s=null,targetControls:a=null,targetRenderer:c=null,showAlerts:i=!1}=e,l=()=>n?{originalText:n.textContent,disabled:n.disabled}:null,m=d=>{!n||!d||(n.textContent=d.originalText,n.disabled=d.disabled)},p=l();n&&(n.textContent="Drawing...",n.disabled=!0);try{const d=!!T.__cooptOptimizerIsRunning;if(!d){const I=T.loadActiveConfigurationToTables;typeof I=="function"&&I()}if(!d)try{T.__cooptOpticalSystemRowsOverride=null}catch{}const{getOpticalSystemRows:u,getObjectRows:h,generateCrossBeam:g,generateInfiniteSystemCrossBeam:y,drawOpticalSystemSurfaces:A,drawCrossBeamRays:x}=In(),_=u(),S=h();if(!_||_.length===0){i&&alert("No optical system data available."),m(p);return}const N=S&&S.length>0&&S[0]?S[0].thickness:null,f=String(N).trim().toUpperCase(),w=N===1/0||f==="INF"||f==="INFINITY"||Number(N)>1e6,b=(()=>{const I=document.getElementById("draw-ray-count-input"),E=parseInt(I?.value||"51",10);return Number.isFinite(E)&&E>0?E:51})();let v;w?typeof y=="function"&&(v=y(_,{rayCount:b})):typeof g=="function"&&S&&S.length>0&&(v=g(_,S,{rayCount:b}));const C=(I=>{if(!I)return[];let E=[];return Array.isArray(I.results)&&(I.results.forEach(P=>{P?.rays&&Array.isArray(P.rays)&&(E=E.concat(P.rays))}),E.length>0)?E:Array.isArray(I.allTracedRays)?I.allTracedRays:Array.isArray(I.tracedRays)?I.tracedRays:Array.isArray(I)?I:[]})(v),D=r||T.scene,B=s||T.camera,j=a||T.controls,H=c||T.renderer;D&&typeof Fn=="function"&&Fn(D),D&&typeof A=="function"&&A({scene:D,opticalSystemData:_,crossSectionOnly:!0,crossSectionDirection:t});const ce=t==="XZ"?"XZ":"YZ";try{}catch(I){console.warn("[RenderWindow] Lens fill postprocess failed:",I)}D&&typeof yn=="function"&&yn(D);const U=()=>{if(!B||!D)return;const I=B.userData?.__drawCrossOrthoBounds;if(I&&I.left!==void 0){const P=H&&H.domElement.width/H.domElement.height||1,{left:Z,right:R,top:k,bottom:$}=I;if((R-Z)/(k-$)>P){const q=(R-Z)/P;B.top=q/2,B.bottom=-q/2,B.left=Z,B.right=R}else{const q=(k-$)*P;B.left=-q/2,B.right=q/2,B.top=k,B.bottom=$}}else{const P=T.calculateOpticalSystemZRange;let Z=-10,R=100,k=10,$=50;if(typeof P=="function")try{const K=P(_);K&&(Z=K.minZ??Z,R=K.maxZ??R,k=K.maxY??k,$=K.centerZ??$)}catch{}const te=R-Z,q=k*2,z=H&&H.domElement.width/H.domElement.height||1;if(te/q>z){const K=te/z;B.top=K/2,B.bottom=-K/2,B.left=-te/2,B.right=te/2}else{const K=q*z;B.left=-K/2,B.right=K/2,B.top=q/2,B.bottom=-q/2}}B.position.set(0,300,I?.centerZ??50),B.lookAt(0,0,I?.centerZ??50),B.up.set(1,0,0),B.updateProjectionMatrix(),j&&(j.target.set(0,0,I?.centerZ??50),j.update())},G=()=>{if(!B||!D)return;const I=B.userData?.__drawCrossOrthoBounds;if(I&&I.left!==void 0){const P=H&&H.domElement.width/H.domElement.height||1,{left:Z,right:R,top:k,bottom:$}=I;if((R-Z)/(k-$)>P){const q=(R-Z)/P;B.top=q/2,B.bottom=-q/2,B.left=Z,B.right=R}else{const q=(k-$)*P;B.left=-q/2,B.right=q/2,B.top=k,B.bottom=$}}else{const P=T.calculateOpticalSystemZRange;let Z=-10,R=100,k=10,$=50;if(typeof P=="function")try{const K=P(_);K&&(Z=K.minZ??Z,R=K.maxZ??R,k=K.maxY??k,$=K.centerZ??$)}catch{}const te=R-Z,q=k*2,z=H&&H.domElement.width/H.domElement.height||1;if(te/q>z){const K=te/z;B.top=K/2,B.bottom=-K/2,B.left=-te/2,B.right=te/2}else{const K=q*z;B.left=-K/2,B.right=K/2,B.top=q/2,B.bottom=-q/2}}B.position.set(-300,0,I?.centerZ??50),B.lookAt(0,0,I?.centerZ??50),B.up.set(0,1,0),B.updateProjectionMatrix(),j&&(j.target.set(0,0,I?.centerZ??50),j.update())},F=T.setCameraForXZCrossSection,W=T.setCameraForYZCrossSection;t==="XZ"?typeof F=="function"?F(D,B,{includeRayStartMargin:!0,preserveDrawCrossBounds:!0}):U():typeof W=="function"?W(D,B,{includeRayStartMargin:!0,preserveDrawCrossBounds:!0}):G(),C&&C.length>0&&D&&typeof x=="function"&&x(C,D),D&&typeof yn=="function"&&yn(D);try{}catch(I){console.warn("[RenderWindow] Lens fill postprocess (final pass) failed:",I)}if(H&&D&&B&&H.render(D,B),o){const I=Di(t);o.textContent=`${t} view complete${I}`}}catch(d){console.error("Error in executeCrossSectionView:",d),i&&alert(`Failed to generate ${t} view: ${d}`)}finally{m(p)}}T.executeCrossSectionView=Pn;function yl(){const e=document.getElementById("annular-pattern-btn"),t=document.getElementById("grid-pattern-btn"),n=o=>{e&&(o==="annular"?e.classList.add("active"):e.classList.remove("active")),t&&(o==="grid"?t.classList.add("active"):t.classList.remove("active"))};e&&e.addEventListener("click",()=>{io("annular"),n("annular")}),t&&t.addEventListener("click",()=>{io("grid"),n("grid")})}function hl(){const e=document.getElementById("object-color-btn"),t=document.getElementById("segment-color-btn"),n=o=>{e&&(o==="object"?e.classList.add("active"):e.classList.remove("active")),t&&(o==="segment"?t.classList.add("active"):t.classList.remove("active"))};e&&e.addEventListener("click",()=>{cn("object"),n("object")}),t&&t.addEventListener("click",()=>{cn("segment"),n("segment")})}function bl(){const e=document.getElementById("view-xz-btn"),t=document.getElementById("view-yz-btn");e&&e.addEventListener("click",()=>{Pn({viewAxis:"XZ",buttonElement:e,showAlerts:!0})}),t&&t.addEventListener("click",()=>{Pn({viewAxis:"YZ",buttonElement:t,showAlerts:!0})})}function Wt(e){if(T.__opticalSystemChangeListenersBound)return;T.__opticalSystemChangeListenersBound=!0;const t=T.tableOpticalSystem;if(t){const r=()=>{};t.on("cellEdited",r),t.on("rowAdded",r),t.on("rowDeleted",r),t.on("dataChanged",r)}gl();const n=document.getElementById("open-3d-window-btn"),o=()=>{const r=T.popup3DWindow;if(r&&!r.closed)try{if(r.focus(),r.document&&r.document.getElementById("threejs-container"))return}catch{}const s=window.open("","3D Optical System","width=800,height=600");if(!s){alert("Popup blocked. Please allow popups for this site.");return}s.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <title>Render Optical System</title>
    <style>
        html, body { height: 100%; width: 100%; overflow: hidden; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f0f0f0;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 12px;
        }
        .header #status {
            font-size: 12px;
            font-weight: 400;
            color: #666;
            margin-left: auto;
            white-space: nowrap;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f9f9f9;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
        }
        .controls button:hover { background: #e9e9e9; }
        .controls button.active { background: #d0d0d0; }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            width: 80px;
        }
        #main {
            flex: 1 1 auto;
            display: flex;
            flex-direction: row;
            min-height: 0;
            position: relative;
            width: 100%;
            max-width: 100vw;
            overflow: hidden;
        }
        #threejs-container {
            flex: 1 1 auto;
            min-height: 0;
            position: relative;
            background: white;
            width: 100%;
            max-width: 100%;
            overflow: hidden;
        }
        #surface-colors {
            position: absolute;
            top: 0;
            right: 0;
            bottom: 0;
            width: 240px;
            display: flex;
            flex-direction: column;
            background: #fafafa;
            border-left: 1px solid #ddd;
            overflow: hidden;
            transition: width 0.2s;
            z-index: 10;
        }
        #surface-colors.collapsed {
            width: 32px;
        }
        #surface-colors .header-row {
            padding: 8px 12px;
            background: #f0f0f0;
            border-bottom: 1px solid #ddd;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex: 0 0 auto;
        }
        #surface-colors .title {
            font-size: 12px;
            font-weight: 600;
            color: #333;
        }
        #surface-colors.collapsed .title {
            display: none;
        }
        #surface-colors-toggle {
            cursor: pointer;
            user-select: none;
            font-size: 14px;
            color: #666;
            padding: 0 4px;
        }
        #surface-colors-toggle:hover {
            color: #333;
        }
        .table-wrap {
            flex: 1 1 auto;
            overflow: auto;
            padding: 8px;
        }
        #surface-colors.collapsed .table-wrap {
            display: none;
        }
        #surface-colors table {
            width: 100%;
            border-collapse: collapse;
            font-size: 12px;
        }
        #surface-colors th {
            text-align: left;
            padding: 6px 8px;
            background: #e8e8e8;
            border-bottom: 1px solid #ccc;
            position: sticky;
            top: 0;
            z-index: 1;
        }
        #surface-colors td {
            padding: 4px 8px;
            border-bottom: 1px solid #eee;
        }
        #surface-colors select {
            width: 100%;
            padding: 4px;
            font-size: 11px;
            border: 1px solid #bbb;
            border-radius: 3px;
        }
    </style>
</head>
<body>
    <div class="header">
        <span>Render Optical System</span>
        <span id="status"></span>
    </div>
    <div class="controls">
        <button id="draw-btn" type="button">Render</button>
        <button id="view-xz-btn" type="button">X-Z View</button>
        <button id="view-yz-btn" type="button">Y-Z View</button>
        <button id="clear-btn" type="button">Clear</button>
        <label for="draw-ray-count-input">Ray number:</label>
        <input type="number" id="draw-ray-count-input" value="5" min="1" max="10001" step="2" />
        <label>Ray colors by:</label>
        <button id="object-color-btn" type="button" class="active">Object</button>
        <button id="segment-color-btn" type="button">Segment</button>
    </div>
    <div id="main">
        <div id="threejs-container"></div>
        <div id="surface-colors" class="collapsed">
            <div class="header-row">
                <span class="title">Surface Colors</span>
                <span id="surface-colors-toggle">▶</span>
            </div>
            <div class="table-wrap">
                <table>
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Color</th>
                        </tr>
                    </thead>
                    <tbody id="surface-colors-tbody"></tbody>
                </table>
            </div>
        </div>
    </div>

    <script type="module">
        import * as THREE from 'https://esm.sh/three@0.182.0';
        import { OrbitControls } from 'https://esm.sh/three@0.182.0/examples/jsm/controls/OrbitControls.js';
        
        console.log('THREE.js loaded in popup:', !!THREE);
        
        // Try to get THREE from parent first, fallback to imported version
        const parentTHREE = window.opener && window.opener.THREE;
        const useTHREE = parentTHREE || THREE;
        const useOrbitControls = (window.opener && window.opener.OrbitControls) || OrbitControls;
        
        console.log('Using THREE from:', parentTHREE ? 'parent window' : 'local import');
        
        function initializePopup(THREE, OrbitControls) {
            const container = document.getElementById('threejs-container');
            const status = document.getElementById('status');
            const MAX_SAFE_DIMENSION = 8192;

            const scene = new THREE.Scene();
            scene.userData.renderContext = {
                three: THREE,
                global: window.opener
            };

            const pickSafeViewportDimension = (candidates, fallback) => {
                const dpr = Number(window.devicePixelRatio || 1);
                const normalized = [];

                const addCandidate = (value) => {
                    const n = Number(value);
                    if (!Number.isFinite(n)) return;
                    if (n >= 200 && n <= MAX_SAFE_DIMENSION) normalized.push(n);
                    if (dpr > 1.25) {
                        const cssLike = n / dpr;
                        if (cssLike >= 200 && cssLike <= MAX_SAFE_DIMENSION) normalized.push(cssLike);
                    }
                };

                for (const value of candidates) addCandidate(value);
                if (normalized.length === 0) return fallback;

                normalized.sort((a, b) => a - b);
                return Math.round(normalized[0]);
            };

            const getSafeViewportSize = () => {
                const width = pickSafeViewportDimension([
                    window.visualViewport && window.visualViewport.width,
                    window.innerWidth,
                    document.documentElement && document.documentElement.clientWidth,
                    window.outerWidth,
                    window.screen && window.screen.width
                ], 1280);
                const height = pickSafeViewportDimension([
                    window.visualViewport && window.visualViewport.height,
                    window.innerHeight,
                    document.documentElement && document.documentElement.clientHeight,
                    window.outerHeight,
                    window.screen && window.screen.height
                ], 720);
                return { width, height };
            };

            let lastGoodContainerWidth = 960;
            let lastGoodContainerHeight = 640;

            const getClampedContainerSize = () => {
                const rect = container.getBoundingClientRect();
                const rawWidth = Number(rect.width || container.clientWidth || 0);
                const rawHeight = Number(rect.height || container.clientHeight || 0);
                const viewport = getSafeViewportSize();
                const baselineWidthCandidates = [
                    viewport.width,
                    window.visualViewport && window.visualViewport.width,
                    window.innerWidth,
                    document.documentElement && document.documentElement.clientWidth,
                    window.screen && window.screen.width
                ]
                    .map((v) => Number(v))
                    .filter((v) => Number.isFinite(v) && v >= 200 && v <= MAX_SAFE_DIMENSION)
                    .sort((a, b) => a - b);
                const baselineHeightCandidates = [
                    viewport.height,
                    window.visualViewport && window.visualViewport.height,
                    window.innerHeight,
                    document.documentElement && document.documentElement.clientHeight,
                    window.screen && window.screen.height
                ]
                    .map((v) => Number(v))
                    .filter((v) => Number.isFinite(v) && v >= 200 && v <= MAX_SAFE_DIMENSION)
                    .sort((a, b) => a - b);

                const viewportWidthBaseline = baselineWidthCandidates[0] || viewport.width;
                const viewportHeightBaseline = baselineHeightCandidates[0] || viewport.height;

                const widthFactor = 1.35;
                const heightFactor = 1.5;

                const maxWidth = Math.min(MAX_SAFE_DIMENSION, Math.max(320, Math.round(viewportWidthBaseline * widthFactor)));
                const maxHeight = Math.min(MAX_SAFE_DIMENSION, Math.max(320, Math.round(viewportHeightBaseline * heightFactor)));

                const preferredWidth = Math.min(maxWidth, Math.max(2, Math.round(viewportWidthBaseline)));
                const preferredHeight = Math.min(maxHeight, Math.max(2, Math.round(viewportHeightBaseline)));

                const pickValue = (raw, fallback, min, max) => {
                    if (!Number.isFinite(raw)) return fallback;
                    if (raw < min || raw > max) return fallback;
                    return Math.round(raw);
                };

                let width = pickValue(rawWidth, preferredWidth, 2, maxWidth);
                let height = pickValue(rawHeight, preferredHeight, 2, maxHeight);

                if (width < 2) width = Math.min(maxWidth, Math.max(2, viewport.width));
                if (height < 2) height = Math.min(maxHeight, Math.max(2, viewport.height));

                if (Number.isFinite(rawWidth) && Math.round(rawWidth) !== width) {
                    console.warn('[popup] Ignored abnormal container width:', rawWidth, '->', width, '(max=', maxWidth, ')');
                }
                if (Number.isFinite(rawHeight) && Math.round(rawHeight) !== height) {
                    console.warn('[popup] Ignored abnormal container height:', rawHeight, '->', height, '(max=', maxHeight, ')');
                }

                lastGoodContainerWidth = width;
                lastGoodContainerHeight = height;
                return { width, height };
            };
            
            const viewSize = 50;
            const initialSize = getClampedContainerSize();
            const aspect = initialSize.width / initialSize.height || 1;
            const camera = new THREE.OrthographicCamera(
                -viewSize * aspect / 2,
                viewSize * aspect / 2,
                viewSize / 2,
                -viewSize / 2,
                0.1,
                10000
            );
            
            const rendererOptions = { antialias: true, alpha: true, precision: 'highp', logarithmicDepthBuffer: true };
            const renderer = new THREE.WebGLRenderer(rendererOptions);
            renderer.setPixelRatio(window.devicePixelRatio || 1);
            renderer.setSize(initialSize.width, initialSize.height, false);
            renderer.setClearColor(0xffffff, 1);
            renderer.sortObjects = false;
            renderer.shadowMap.enabled = false;
            container.appendChild(renderer.domElement);
            
            const ambientLight = new THREE.AmbientLight(0xffffff, 0.6);
            scene.add(ambientLight);
            const directionalLight = new THREE.DirectionalLight(0xffffff, 0.4);
            directionalLight.position.set(10, 10, 10);
            scene.add(directionalLight);
            
            const controls = new OrbitControls(camera, renderer.domElement);
            controls.enableDamping = true;
            controls.dampingFactor = 0.05;
            controls.enableRotate = true;
            controls.enablePan = true;
            controls.enableZoom = true;

            let popupUserAdjustedView = false;
            let popupCurrentViewAxis = 'YZ';
            let popupRayColorMode = 'object';
            let popupSurfaceColorsCollapsed = true;

            function setPopupUserAdjustedView(v) {
                popupUserAdjustedView = v === true;
            }

            function getPopupUserAdjustedView() {
                return popupUserAdjustedView === true;
            }

            function setCurrentViewAxis(axis) {
                popupCurrentViewAxis = axis === 'XZ' ? 'XZ' : 'YZ';
            }

            function getCurrentViewAxis() {
                return popupCurrentViewAxis;
            }

            function setPopupRayColorModeValue(mode) {
                popupRayColorMode = mode === 'segment' ? 'segment' : 'object';
            }

            function getPopupRayColorMode() {
                return popupRayColorMode;
            }

            function setSurfaceColorsCollapsed(collapsed) {
                popupSurfaceColorsCollapsed = collapsed === true;
            }

            function getSurfaceColorsCollapsed() {
                return popupSurfaceColorsCollapsed === true;
            }

            function toggleSurfaceColorsCollapsed() {
                setSurfaceColorsCollapsed(!getSurfaceColorsCollapsed());
            }
            
            setPopupUserAdjustedView(false);
            controls.addEventListener('start', () => {
                setPopupUserAdjustedView(true);
            });
            
            camera.position.set(0, 50, 100);
            camera.lookAt(0, 0, 0);
            camera.up.set(0, 1, 0);
            controls.target.set(0, 0, 100);
            controls.update();

            const renderNow = () => {
                renderer.render(scene, camera);
            };
            
            function animate() {
                requestAnimationFrame(animate);
                controls.update();
                renderNow();
            }
            animate();
            
            let resizeScheduled = false;
            let lastResizeSentAt = 0;
            let lastResizeWidth = -1;
            let lastResizeHeight = -1;

            const shouldSendPopupResize = (now, width, height, thresholdMs, deltaPx) => {
                const hasLastSize = lastResizeWidth > 0 && lastResizeHeight > 0;
                const sizeChangedEnough = !hasLastSize ||
                    Math.abs(lastResizeWidth - width) >= deltaPx ||
                    Math.abs(lastResizeHeight - height) >= deltaPx;
                return !lastResizeSentAt ||
                    (now - lastResizeSentAt > thresholdMs) ||
                    sizeChangedEnough;
            };

            const markPopupResizeSent = (now, width, height) => {
                lastResizeSentAt = now;
                lastResizeWidth = width;
                lastResizeHeight = height;
            };

            const scheduleResize = () => {
                if (resizeScheduled) return;
                resizeScheduled = true;
                requestAnimationFrame(() => {
                    requestAnimationFrame(() => {
                        resizeScheduled = false;
                        applyResize();
                    });
                });
            };
            
            const applyResize = () => {
                const size = getClampedContainerSize();
                const w = Math.max(1, size.width);
                const h = Math.max(1, size.height);
                if (w < 2 || h < 2) return;
                
                renderer.setPixelRatio(window.devicePixelRatio || 1);
                renderer.setSize(w, h, false);
                controls.update();
                
                const aspect = w / h;
                const currentHeight = camera.top - camera.bottom;
                const currentCenterX = (camera.left + camera.right) / 2;
                const currentCenterY = (camera.top + camera.bottom) / 2;
                const newWidth = currentHeight * aspect;
                camera.left = currentCenterX - newWidth / 2;
                camera.right = currentCenterX + newWidth / 2;
                camera.top = currentCenterY + currentHeight / 2;
                camera.bottom = currentCenterY - currentHeight / 2;
                camera.updateProjectionMatrix();
                
                if (!getPopupUserAdjustedView()) {
                    const now = Date.now();
                    const threshold = 200;
                    const deltaPx = 8;
                    const shouldSend = shouldSendPopupResize(now, w, h, threshold, deltaPx);
                    
                    if (shouldSend && window.opener) {
                        markPopupResizeSent(now, w, h);
                        try {
                            const axis = getCurrentViewAxis() || 'YZ';
                            window.opener.postMessage({ action: 'popup-resize', viewAxis: axis }, '*');
                        } catch (_) {}
                    }
                }
                renderNow();
            };
            
            let resizeObserver = null;
            if (typeof ResizeObserver !== 'undefined') {
                resizeObserver = new ResizeObserver(() => scheduleResize());
                resizeObserver.observe(container);
            } else {
                let lastW = -1;
                let lastH = -1;
                setInterval(() => {
                    const w = container.clientWidth;
                    const h = container.clientHeight;
                    if (w !== lastW || h !== lastH) {
                        lastW = w;
                        lastH = h;
                        scheduleResize();
                    }
                }, 300);
            }
            window.addEventListener('resize', scheduleResize);
            
            const normalizeSceneGeometry = (scene) => {
                const normalizeArray = (attr, isIndex) => {
                    if (!attr || !attr.array) return;
                    const arr = attr.array;
                    
                    if (Array.isArray(arr)) {
                        const TypedArray = isIndex
                            ? (arr.length < 65536 ? Uint16Array : Uint32Array)
                            : Float32Array;
                        attr.array = new TypedArray(arr);
                        attr.needsUpdate = true;
                        return;
                    }
                    
                    if (arr.constructor && arr.constructor.name && 
                        arr.constructor.name.includes('Array') &&
                        arr.buffer && arr.buffer.constructor &&
                        arr.buffer.constructor.name === 'ArrayBuffer') {
                        const TypedArray = isIndex
                            ? (arr.length < 65536 ? Uint16Array : Uint32Array)
                            : Float32Array;
                        attr.array = new TypedArray(arr);
                        attr.needsUpdate = true;
                    }
                };
                
                scene.traverse((obj) => {
                    const geom = obj.geometry;
                    if (!geom) return;
                    
                    if (geom.attributes) {
                        for (const name in geom.attributes) {
                            const attr = geom.attributes[name];
                            if (attr.isInterleavedBufferAttribute && attr.data && attr.data.array) {
                                normalizeArray(attr.data, false);
                            } else {
                                normalizeArray(attr, false);
                            }
                        }
                    }
                    
                    if (geom.morphAttributes) {
                        for (const name in geom.morphAttributes) {
                            const morphs = geom.morphAttributes[name];
                            if (Array.isArray(morphs)) {
                                morphs.forEach(attr => normalizeArray(attr, false));
                            }
                        }
                    }
                    
                    if (geom.index) {
                        normalizeArray(geom.index, true);
                    }
                });
            };
            
            const findBadGeometry = (scene) => {
                const tempScene = new THREE.Scene();
                const tempCamera = new THREE.PerspectiveCamera();
                const tempRenderer = new THREE.WebGLRenderer();
                
                const objects = [];
                scene.traverse(obj => {
                    if (obj.geometry) objects.push(obj);
                });
                
                for (let i = 0; i < objects.length; i++) {
                    const obj = objects[i];
                    tempScene.add(obj.clone());
                    
                    try {
                        tempRenderer.render(tempScene, tempCamera);
                    } catch (err) {
                        console.error('Bad geometry found:', {
                            name: obj.name,
                            uuid: obj.uuid,
                            type: obj.type,
                            geometryType: obj.geometry?.type,
                            hasIndex: !!obj.geometry?.index,
                            attributes: Object.keys(obj.geometry?.attributes || {}).map(k => {
                                const attr = obj.geometry.attributes[k];
                                return {
                                    name: k,
                                    itemSize: attr?.itemSize,
                                    count: attr?.count,
                                    arrayType: attr?.array?.constructor?.name,
                                    isInterleaved: !!attr?.isInterleavedBufferAttribute
                                };
                            }),
                            index: obj.geometry?.index ? {
                                count: obj.geometry.index.count,
                                arrayType: obj.geometry.index.array?.constructor?.name
                            } : null,
                            userData: obj.userData
                        });
                        tempRenderer.dispose();
                        return obj;
                    }
                }
                
                tempRenderer.dispose();
                return null;
            };
            
            try {
                window.scene = scene;
                window.camera = camera;
                window.renderer = renderer;
                window.controls = controls;
            } catch (_) {}

            if (window.opener) {
                window.opener.popupScene = scene;
                window.opener.popupCamera = camera;
                window.opener.popupRenderer = renderer;
                window.opener.popupControls = controls;
                window.opener.popup3DWindow = window;
            }
            
            const SURFACE_COLOR_OVERRIDES_STORAGE_KEY = 'coopt.surfaceColorOverrides';
            
            const COLOR_PALETTE = [
                { name: 'Light Pink', hex: '#FFB6C1' },
                { name: 'Light Red', hex: '#FF6B6B' },
                { name: 'Light Orange', hex: '#FFA07A' },
                { name: 'Light Amber', hex: '#FFBF00' },
                { name: 'Light Yellow', hex: '#FFFF99' },
                { name: 'Light Lime', hex: '#CCFF66' },
                { name: 'Light Green', hex: '#90EE90' },
                { name: 'Light Mint', hex: '#98FF98' },
                { name: 'Light Cyan', hex: '#AFEEEE' },
                { name: 'Light Sky', hex: '#87CEEB' },
                { name: 'Light Blue', hex: '#ADD8E6' },
                { name: 'Light Indigo', hex: '#9FA8DA' },
                { name: 'Light Purple', hex: '#DDA0DD' },
                { name: 'Light Lavender', hex: '#E6E6FA' },
                { name: 'Light Peach', hex: '#FFDAB9' },
                { name: 'Light Gray', hex: '#D3D3D3' }
            ];
            
            function surfaceColorKey(surf) {
                if (surf._blockId && surf._surfaceRole) {
                    return 'p:' + surf._blockId + '|' + surf._surfaceRole;
                }
                if (surf.id !== undefined && surf.id !== null) {
                    return 'id:' + surf.id;
                }
                if (surf.index0 !== undefined && surf.index0 !== null) {
                    return 'i:' + surf.index0;
                }
                return '';
            }
            
            function loadColorOverrides() {
                try {
                    const raw = localStorage.getItem(SURFACE_COLOR_OVERRIDES_STORAGE_KEY);
                    return raw ? JSON.parse(raw) : {};
                } catch (_) {
                    return {};
                }
            }
            
            function saveColorOverrides(map) {
                try {
                    localStorage.setItem(SURFACE_COLOR_OVERRIDES_STORAGE_KEY, JSON.stringify(map));
                } catch (_) {}
            }
            
            function requestRedrawFromPopup() {
                if (!window.opener) return;
                const viewState = getPopupViewState();
                try {
                    window.opener.postMessage({ action: 'draw-cross', ...viewState }, '*');
                } catch (_) {}
            }
            
            function renderSurfaceColorsTable(surfaces) {
                const tbody = document.getElementById('surface-colors-tbody');
                if (!tbody) return;
                
                tbody.innerHTML = '';
                
                const overrides = loadColorOverrides();
                
                for (let i = 0; i < surfaces.length; i++) {
                    const surf = surfaces[i];
                    const key = surfaceColorKey(surf);
                    if (!key) continue;
                    
                    const tr = document.createElement('tr');
                    
                    const tdIndex = document.createElement('td');
                    tdIndex.textContent = String(i);
                    
                    const tdColor = document.createElement('td');
                    const sel = document.createElement('select');
                    
                    const defaultOpt = document.createElement('option');
                    defaultOpt.value = '';
                    defaultOpt.textContent = 'Default';
                    sel.appendChild(defaultOpt);
                    
                    for (const c of COLOR_PALETTE) {
                        const opt = document.createElement('option');
                        opt.value = c.hex;
                        opt.textContent = c.name;
                        sel.appendChild(opt);
                    }
                    
                    const current = overrides[key] || '';
                    sel.value = current;
                    applySelectSwatch(sel);
                    sel.addEventListener('change', () => {
                        const next = String(sel.value || '').trim();
                        const nextMap = loadColorOverrides();
                        if (!next) {
                            delete nextMap[key];
                        } else {
                            nextMap[key] = next;
                        }
                        saveColorOverrides(nextMap);
                        applySelectSwatch(sel);
                        requestRedrawFromPopup();
                    });
                    
                    tdColor.appendChild(sel);
                    
                    tr.appendChild(tdIndex);
                    tr.appendChild(tdColor);
                    tbody.appendChild(tr);
                }
            }
            
            function applySelectSwatch(sel) {
                const val = String(sel.value || '').trim();
                if (val && val.startsWith('#')) {
                    sel.style.backgroundColor = val;
                } else {
                    sel.style.backgroundColor = '';
                }
            }
            
            function applySurfaceColorsCollapsedState(collapsed) {
                const surfaceColorsPanel = document.getElementById('surface-colors');
                const surfaceColorsToggle = document.getElementById('surface-colors-toggle');
                if (!surfaceColorsPanel || !surfaceColorsToggle) return;
                const isCollapsed = collapsed === true;
                surfaceColorsPanel.classList.toggle('collapsed', isCollapsed);
                surfaceColorsToggle.textContent = isCollapsed ? '◀' : '▶';
            }

            setSurfaceColorsCollapsed(true);
            applySurfaceColorsCollapsedState(getSurfaceColorsCollapsed());
            
            const surfaceColorsToggle = document.getElementById('surface-colors-toggle');
            if (surfaceColorsToggle) {
                surfaceColorsToggle.addEventListener('click', () => {
                    toggleSurfaceColorsCollapsed();
                    applySurfaceColorsCollapsedState(getSurfaceColorsCollapsed());
                });
            }
            
            setPopupRayColorModeValue('object');
            
            function setPopupRayColorMode(mode) {
                setPopupRayColorModeValue(mode);
                const objectColorBtn = document.getElementById('object-color-btn');
                const segmentColorBtn = document.getElementById('segment-color-btn');
                if (objectColorBtn && segmentColorBtn) {
                    objectColorBtn.classList.toggle('active', getPopupRayColorMode() === 'object');
                    segmentColorBtn.classList.toggle('active', getPopupRayColorMode() === 'segment');
                }
            }
            
            const objectColorBtn = document.getElementById('object-color-btn');
            const segmentColorBtn = document.getElementById('segment-color-btn');
            if (objectColorBtn) {
                objectColorBtn.addEventListener('click', () => setPopupRayColorMode('object'));
            }
            if (segmentColorBtn) {
                segmentColorBtn.addEventListener('click', () => setPopupRayColorMode('segment'));
            }
            
            console.log('Buttons:', {
                drawBtn: document.getElementById('draw-btn'),
                xzBtn: document.getElementById('view-xz-btn'),
                yzBtn: document.getElementById('view-yz-btn'),
                clearBtn: document.getElementById('clear-btn'),
                status: document.getElementById('status')
            });
            
            window.addEventListener('message', (event) => {
                if (!window.opener || event.source !== window.opener) {
                    return;
                }
                const data = event.data || {};
                if (data && data.action === 'surface-list') {
                    try {
                        renderSurfaceColorsTable(data.surfaces);
                    } catch (e) {}
                    return;
                }
                if (data && data.action === 'set-user-adjusted-view') {
                    try {
                        setPopupUserAdjustedView(data.value === true);
                    } catch (_) {}
                    return;
                }
                if (data && data.action === 'request-redraw') {
                    try {
                        const axisRaw = (data.viewAxis || getCurrentViewAxis() || 'YZ').toString().toUpperCase();
                        setCurrentViewAxis(axisRaw);
                    } catch (_) {}
                    
                    try {
                        const viewState = getPopupViewState();
                        if (status) {
                            status.textContent = 'Redrawing...';
                        }
                        window.opener.postMessage({ action: 'draw-cross', ...viewState }, '*');
                    } catch (e) {}
                    return;
                }
                if (typeof data.status === 'string' && status) {
                    status.textContent = data.status;
                }
            });
            
            function getPopupViewState() {
                const rayCountInput = document.getElementById('draw-ray-count-input');
                const rayCount = (() => {
                    const v = parseInt(rayCountInput?.value || '51', 10);
                    return Number.isFinite(v) && v > 0 ? v : 51;
                })();
                return {
                    userAdjustedView: getPopupUserAdjustedView(),
                    viewAxis: getCurrentViewAxis(),
                    rayCount,
                    rayColorMode: getPopupRayColorMode(),
                    target: {
                        x: controls?.target?.x ?? 0,
                        y: controls?.target?.y ?? 0,
                        z: controls?.target?.z ?? 0
                    },
                    camera: {
                        x: camera?.position?.x ?? 0,
                        y: camera?.position?.y ?? 0,
                        z: camera?.position?.z ?? 0
                    },
                    zoom: camera?.zoom ?? 1
                };
            }

            const drawBtn = document.getElementById('draw-btn');
            if (drawBtn) {
                drawBtn.addEventListener('click', () => {
                    const viewState = getPopupViewState();
                    console.log('📤 Sending message to parent:', { action: 'draw-cross', ...viewState });
                    if (window.opener) {
                        window.opener.postMessage({ action: 'draw-cross', ...viewState }, '*');
                        status.textContent = 'Drawing...';
                    }
                });
            }
            
            const xzBtn = document.getElementById('view-xz-btn');
            if (xzBtn) {
                xzBtn.addEventListener('click', () => {
                    setCurrentViewAxis('XZ');
                    if (window.opener) {
                        const viewState = getPopupViewState();
                        window.opener.postMessage({ action: 'view-xz', ...viewState }, '*');
                        status.textContent = 'Switching to X-Z view...';
                    }
                });
            }
            
            const yzBtn = document.getElementById('view-yz-btn');
            if (yzBtn) {
                yzBtn.addEventListener('click', () => {
                    setCurrentViewAxis('YZ');
                    if (window.opener) {
                        const viewState = getPopupViewState();
                        window.opener.postMessage({ action: 'view-yz', ...viewState }, '*');
                        status.textContent = 'Switching to Y-Z view...';
                    }
                });
            }
            
            const clearBtn = document.getElementById('clear-btn');
            if (clearBtn) {
                clearBtn.addEventListener('click', () => {
                    const objectsToRemove = [];
                    scene.traverse((object) => {
                        if (object !== scene && !(object instanceof THREE.Light)) {
                            objectsToRemove.push(object);
                        }
                    });
                    objectsToRemove.forEach((obj) => {
                        scene.remove(obj);
                        if (obj.geometry) obj.geometry.dispose();
                        if (obj.material) {
                            if (Array.isArray(obj.material)) {
                                obj.material.forEach(mat => mat.dispose());
                            } else {
                                obj.material.dispose();
                            }
                        }
                    });
                    renderNow();
                    status.textContent = 'Cleared';
                });
            }
            
            if (window.opener) {
                window.opener.postMessage({ action: 'popup-ready' }, '*');
            }
            
            if (drawBtn && window.opener) {
                setTimeout(() => {
                    try {
                        drawBtn.click();
                    } catch (e) {}
                }, 0);
            }
        }
        
        function initPopup() {
            if (THREE) {
                setupScene();
            } else {
                setTimeout(initPopup, 100);
            }
        }
        
        // Initialize with the loaded THREE.js
        initializePopup(useTHREE, useOrbitControls);
    <\/script>
</body>
</html>
            `),s.document.close(),T.popup3DWindow=s};if(typeof window<"u")try{window.__open3DWindowLegacy=o}catch{}n&&n.addEventListener("click",o)}function $t(){if(!!!(document.getElementById("open-system-data-window-btn")||document.getElementById("open-spot-diagram-window-btn")||document.getElementById("open-opd-window-btn")||document.getElementById("open-psf-window-btn")||document.getElementById("open-mtf-window-btn")||document.getElementById("open-through-focus-spot-window-btn")||document.getElementById("open-through-focus-mtf-window-btn")||document.getElementById("open-field-mtf-window-btn"))||T.__analysisWindowsBound)return;const t=(()=>{try{return new URL(window.location.href).searchParams.get("coopt_analysis_window")==="1"}catch{return!1}})();if(ze()&&!t){T.__analysisWindowsBound=!0;return}T.__analysisWindowsBound=!0;const n=(S,N)=>{try{const f=T.__preopenedAnalysisPopupMap,w=f&&f[S];if(w&&!w.closed){try{delete f[S]}catch{}try{w.focus()}catch{}return w}}catch{}return window.open("",S,N)},o=document.getElementById("open-system-data-window-btn");o&&(o.getAttribute("data-react-handled")==="1"||o.addEventListener("click",()=>{if(T.__systemDataPopup&&!T.__systemDataPopup.closed){try{T.__systemDataPopup.focus()}catch{}return}const N=n("System Data","width=1200,height=600");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__systemDataPopup=N,N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>System Data</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: white;
            color: #333;
            font-weight: 600;
            flex: 0 0 auto;
            border-bottom: 1px solid #ddd;
        }
        .controls {
            padding: 10px 12px;
            background: white;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
        }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
        }
        .content {
            flex: 1 1 auto;
            padding: 10px 12px;
            min-height: 0;
            display: flex;
            flex-direction: column;
            gap: 8px;
        }
        textarea {
            flex: 1 1 auto;
            width: 100%;
            resize: none;
            font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
            font-size: 12px;
            line-height: 1.4;
            border: 1px solid #bbb;
            border-radius: 4px;
            padding: 10px;
            box-sizing: border-box;
            min-height: 0;
            background: white;
        }
    </style>
</head>
<body>
    <div class="header">System Data</div>
    <div class="controls">
        <button id="popup-calculate-paraxial">Calculate Paraxial</button>
        <button id="popup-calculate-seidel">Aberration Coefficients</button>
        <button id="popup-calculate-seidel-afocal">Aberration Coefficients (Afocal)</button>
        <label for="popup-reference-focal-length">Reference Focal Length:</label>
        <input type="text" id="popup-reference-focal-length" placeholder="Auto" style="width: 80px;" />
        <button id="popup-coord-transform">Coord Transform</button>
    </div>
    <div class="content">
        <div id="popup-transform-error-bar" style="display:none; padding:8px 12px; margin-bottom:8px; background:#fff3cd; border:1px solid #ffc107; border-radius:3px; color:#856404;">
            <strong>Error:</strong> <span id="popup-transform-error-text"></span>
        </div>
        <div id="popup-transform-progress-wrapper" style="display:none; padding:8px 12px; border-bottom:1px solid #eee; background:#fff; margin-bottom:8px;">
            <div id="popup-transform-progress-text">Calculating...</div>
            <progress id="popup-transform-progressbar" max="100" value="0" style="width:100%; margin-top:4px;"></progress>
        </div>
        <textarea id="popup-system-data" placeholder="System information will appear here..."></textarea>
    </div>

    <script>
        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (e) {
                return null;
            }
        }

        function syncFromOpener() {
            const ref = getOpenerEl('reference-focal-length');
            const src = getOpenerEl('system-data');
            const popupRef = document.getElementById('popup-reference-focal-length');
            const popupText = document.getElementById('popup-system-data');

            if (popupRef && ref && popupRef.value !== ref.value) {
                popupRef.value = ref.value;
            }
            if (popupText && src && popupText.value !== src.value) {
                popupText.value = src.value;
            }
        }

        function triggerOpenerClick(id) {
            const btn = getOpenerEl(id);
            if (btn) {
                btn.click();
                // allow async handlers to update textarea
                setTimeout(syncFromOpener, 50);
                setTimeout(syncFromOpener, 200);
                setTimeout(syncFromOpener, 500);
                setTimeout(syncFromOpener, 1000);
                setTimeout(syncFromOpener, 2000);
            }
        }

        document.getElementById('popup-calculate-paraxial').addEventListener('click', () => triggerOpenerClick('calculate-paraxial-btn'));
        document.getElementById('popup-calculate-seidel').addEventListener('click', () => triggerOpenerClick('calculate-seidel-btn'));
        document.getElementById('popup-calculate-seidel-afocal').addEventListener('click', () => triggerOpenerClick('calculate-seidel-afocal-btn'));
        document.getElementById('popup-coord-transform').addEventListener('click', () => triggerOpenerClick('coord-transform-btn'));

        document.getElementById('popup-reference-focal-length').addEventListener('input', (e) => {
            const value = e.target.value;
            const ref = getOpenerEl('reference-focal-length');
            if (ref) ref.value = value;
            try {
                import('../data/table-configuration.ts').then(({ saveReferenceFocalLengthProjection }) => {
                    try { saveReferenceFocalLengthProjection(value); } catch (_) {}
                });
            } catch (_) {}
        });

        // Coordinate transformation controls in popup
        const popupTransformSurfaceSelect = document.getElementById('popup-transform-surface-select');
        const popupShowLocalCoordsBtn = document.getElementById('popup-show-local-coords-btn');
        const popupCancelTransformBtn = document.getElementById('popup-cancel-transform-btn');
        const popupSaveLocalCoordsBtn = document.getElementById('popup-save-local-coords-btn');
        const popupErrorBar = document.getElementById('popup-transform-error-bar');
        const popupErrorText = document.getElementById('popup-transform-error-text');
        const popupProgressWrapper = document.getElementById('popup-transform-progress-wrapper');
        const popupProgressText = document.getElementById('popup-transform-progress-text');
        const popupProgressBar = document.getElementById('popup-transform-progressbar');

        function showPopupError(message) {
            if (popupErrorBar && popupErrorText) {
                popupErrorText.textContent = message;
                popupErrorBar.style.display = '';
            }
        }

        function hidePopupError() {
            if (popupErrorBar) popupErrorBar.style.display = 'none';
        }

        function setPopupProgress(percent, message) {
            if (popupProgressWrapper) popupProgressWrapper.style.display = 'block';
            if (popupProgressBar && Number.isFinite(percent)) {
                popupProgressBar.value = Math.max(0, Math.min(100, percent));
            }
            if (popupProgressText && message) popupProgressText.textContent = message;
        }

        function hidePopupProgress() {
            if (popupProgressWrapper) popupProgressWrapper.style.display = 'none';
        }

        // Update surface select from opener
        function updatePopupSurfaceSelect() {
            if (!popupTransformSurfaceSelect) return;
            try {
                const getOpticalSystemRows = window.opener && window.opener.getOpticalSystemRows;
                if (typeof getOpticalSystemRows !== 'function') return;
                
                const opticalSystemRows = getOpticalSystemRows();
                if (!opticalSystemRows || opticalSystemRows.length === 0) return;
                
                popupTransformSurfaceSelect.innerHTML = '<option value="">Select surface...</option>';
                
                opticalSystemRows.forEach((row, index) => {
                    const objectType = String(row?.['object type'] ?? row?.object ?? '').toLowerCase();
                    if (objectType === 'object') return;
                    
                    const surfType = String(row?.surfType ?? row?.type ?? '').toLowerCase();
                    if (surfType === 'ct' || surfType === 'coordtrans' || surfType === 'coordinatebreak' ||
                        surfType === 'coord trans' || surfType === 'coordinate break') {
                        return;
                    }
                    
                    const option = document.createElement('option');
                    option.value = index;
                    
                    let label = 'Surf ' + index;
                    if (row.comment) label += ': ' + row.comment;
                    else if (row.material && row.material !== 'AIR') label += ': ' + row.material;
                    
                    option.textContent = label;
                    popupTransformSurfaceSelect.appendChild(option);
                });
            } catch (error) {
                console.error('Error updating popup surface select:', error);
            }
        }

        // Show Local Coords button
        if (popupShowLocalCoordsBtn) {
            popupShowLocalCoordsBtn.addEventListener('click', async function() {
                hidePopupError();
                
                try {
                    const surfaceIndex = parseInt(popupTransformSurfaceSelect?.value);
                    if (!surfaceIndex && surfaceIndex !== 0) {
                        showPopupError('Please select a surface first.');
                        return;
                    }
                    
                    const calculateAllSurfacesLocalCoordinates = window.opener && window.opener.calculateAllSurfacesLocalCoordinates;
                    const getOpticalSystemRows = window.opener && window.opener.getOpticalSystemRows;
                    const tableOpticalSystem = window.opener && window.opener.tableOpticalSystem;
                    
                    if (typeof calculateAllSurfacesLocalCoordinates !== 'function') {
                        showPopupError('Coordinate transformation function not available.');
                        return;
                    }
                    
                    if (typeof getOpticalSystemRows !== 'function') {
                        showPopupError('Optical system data not available.');
                        return;
                    }
                    
                    const opticalSystemRows = getOpticalSystemRows();
                    if (!opticalSystemRows || opticalSystemRows.length === 0) {
                        showPopupError('No optical system data. Please load or create an optical system.');
                        return;
                    }
                    
                    popupShowLocalCoordsBtn.disabled = true;
                    if (popupCancelTransformBtn) popupCancelTransformBtn.style.display = '';
                    if (popupSaveLocalCoordsBtn) popupSaveLocalCoordsBtn.style.display = 'none';
                    
                    if (window.opener) window.opener._transformCalculationCancelled = false;
                    
                    const result = await calculateAllSurfacesLocalCoordinates(
                        opticalSystemRows,
                        surfaceIndex,
                        (percent, message) => setPopupProgress(percent, message)
                    );
                    
                    if (window.opener) {
                        window.opener._cachedLocalCoords = result;
                        window.opener._showLocalCoords = true;
                    }
                    
                    if (tableOpticalSystem) {
                        tableOpticalSystem.redraw();
                    }
                    
                    if (popupSaveLocalCoordsBtn) popupSaveLocalCoordsBtn.style.display = '';
                    
                    hidePopupProgress();
                    
                } catch (error) {
                    console.error('Coordinate transformation error:', error);
                    showPopupError(error.message || 'Failed to calculate local coordinates.');
                    hidePopupProgress();
                } finally {
                    popupShowLocalCoordsBtn.disabled = false;
                    if (popupCancelTransformBtn) popupCancelTransformBtn.style.display = 'none';
                }
            });
        }

        // Cancel button
        if (popupCancelTransformBtn) {
            popupCancelTransformBtn.addEventListener('click', function() {
                if (window.opener) window.opener._transformCalculationCancelled = true;
                if (popupCancelTransformBtn) popupCancelTransformBtn.style.display = 'none';
                hidePopupProgress();
                showPopupError('Calculation cancelled by user.');
            });
        }

        // Save as JSON button
        if (popupSaveLocalCoordsBtn) {
            popupSaveLocalCoordsBtn.addEventListener('click', function() {
                try {
                    const data = window.opener && window.opener._cachedLocalCoords;
                    if (!data) {
                        showPopupError('No coordinate data to save. Please calculate first.');
                        return;
                    }
                    
                    const json = JSON.stringify(data, null, 2);
                    const blob = new Blob([json], { type: 'application/json' });
                    const url = URL.createObjectURL(blob);
                    
                    const timestamp = new Date().toISOString().replace(/[:.]/g, '-').slice(0, 19);
                    const surfaceIndex = data.metadata?.targetSurfaceIndex ?? 'unknown';
                    const filename = 'local-coords-surf' + surfaceIndex + '-' + timestamp + '.json';
                    
                    const a = document.createElement('a');
                    a.href = url;
                    a.download = filename;
                    a.click();
                    
                    URL.revokeObjectURL(url);
                    
                } catch (error) {
                    console.error('Save error:', error);
                    showPopupError('Failed to save JSON file: ' + error.message);
                }
            });
        }

        // Update surface select on load and periodically
        updatePopupSurfaceSelect();
        setInterval(updatePopupSurfaceSelect, 1000);

        // Keep in sync with the main window.
        setInterval(syncFromOpener, 500);
        window.addEventListener('focus', syncFromOpener);
        syncFromOpener();
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}}));const r=document.getElementById("open-spot-diagram-window-btn");r&&r.addEventListener("click",()=>{if(T.__spotDiagramPopup&&!T.__spotDiagramPopup.closed){try{T.__spotDiagramPopup.close()}catch{}T.__spotDiagramPopup=null}try{Ht(T)}catch(N){console.warn("Failed to update spot diagram selects:",N)}const S=n("Spot Diagram","width=800,height=600");if(!S){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__spotDiagramPopup=S,S.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Spot Diagram</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: white;
            color: #333;
            font-weight: 600;
            flex: 0 0 auto;
            border-bottom: 1px solid #ddd;
        }
        .controls {
            padding: 10px 12px;
            background: white;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .controls input, .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .pattern-btn.active { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: auto;
            background: white;
        }
        #popup-spot-diagram-container {
            min-height: 100%;
        }
        .note {
            padding: 10px 12px;
            color: #666;
            font-size: 12px;
            border-bottom: 1px solid #eee;
            background: #fff;
        }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="header"></div>
    <div class="controls">
        <label for="popup-surface-number-select">Surf:</label>
        <select id="popup-surface-number-select"></select>

        <label for="popup-ray-count-input">Ray number:</label>
        <input type="number" id="popup-ray-count-input" value="501" min="1" step="1" />

        <label for="popup-ring-count-select">Ring count:</label>
        <select id="popup-ring-count-select">
            <option value="1">1</option>
            <option value="2">2</option>
            <option value="3">3</option>
            <option value="4">4</option>
            <option value="5">5</option>
            <option value="6">6</option>
            <option value="7">7</option>
            <option value="8">8</option>
            <option value="9">9</option>
            <option value="10" selected>10</option>
            <option value="12">12</option>
            <option value="15">15</option>
            <option value="16">16</option>
            <option value="20">20</option>
            <option value="24">24</option>
            <option value="32">32</option>
        </select>

        <label for="popup-pattern-select">Ray pattern:</label>
        <select id="popup-pattern-select">
            <option value="annular" selected>Annular</option>
            <option value="grid">Rectangle</option>
        </select>

        <button id="popup-show-spot-diagram-btn" type="button">Show spot diagram</button>
    </div>
    <div id="popup-spot-debug" style="display:none;"></div>
    <div id="popup-spot-run-debug" style="display:none;"></div>
    <div id="popup-spot-stats" style="display:none;"></div>
    <div id="popup-spot-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-spot-progress-text" style="margin-bottom: 6px;">Calculating spot diagram...</div>
        <progress id="popup-spot-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-spot-diagram-container"></div>
    </div>

    <script>
        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (e) {
                return null;
            }
        }

        function setPopupDebug(text) {
            return;
        }

        function setPopupRunDebug(text) {
            return;
        }

        function setPopupStats(text) {
            try {
                const el = document.getElementById('popup-spot-stats');
                if (el) el.textContent = String(text || '');
            } catch (_) {}
        }

        function syncSurfaceOptionsFromOpener() {
            const openerSelect = getOpenerEl('surface-number-select');
            const popupSelect = document.getElementById('popup-surface-number-select');
            if (!popupSelect) return;

            const populateFromOpenerRows = () => {
                try {
                    const rows = (() => {
                        try {
                            if (!window.opener) return null;
                            if (typeof window.opener.getOpticalSystemRows === 'function') {
                                const r = window.opener.getOpticalSystemRows(window.opener.tableOpticalSystem);
                                if (Array.isArray(r) && r.length > 0) return r;
                                const r2 = window.opener.getOpticalSystemRows();
                                if (Array.isArray(r2) && r2.length > 0) return r2;
                            }
                        } catch (_) {}
                        try {
                            if (window.opener && window.opener.tableOpticalSystem && typeof window.opener.tableOpticalSystem.getData === 'function') {
                                const r = window.opener.tableOpticalSystem.getData();
                                if (Array.isArray(r) && r.length > 0) return r;
                            }
                        } catch (_) {}
                        return null;
                    })();
                    if (!Array.isArray(rows) || rows.length === 0) {
                        setPopupDebug('Surf sync: opener rows unavailable (0 rows)');
                        return false;
                    }

                    const sharedOptions = (() => {
                        try {
                            if (window.opener && typeof window.opener.generateSurfaceOptions === 'function') {
                                const opts = window.opener.generateSurfaceOptions(rows);
                                if (Array.isArray(opts) && opts.length > 0) return opts;
                            }
                        } catch (_) {}
                        return null;
                    })();

                    if (Array.isArray(sharedOptions) && sharedOptions.length > 0) {
                        popupSelect.innerHTML = '';
                        const placeholder = document.createElement('option');
                        placeholder.value = '';
                        placeholder.textContent = 'Select Surf';
                        popupSelect.appendChild(placeholder);

                        for (const o of sharedOptions) {
                            const opt = document.createElement('option');
                            opt.value = String(o?.value ?? o?.surfaceId ?? '');
                            opt.textContent = String(o?.label ?? ('Surf ' + String(o?.surfaceId ?? '')));
                            {
                                const labelLower = String(opt.textContent || '').toLowerCase();
                                const imageFlag = !!(o?.isImage === true || labelLower.includes('(image)') || labelLower.includes(' image'));
                                opt.dataset.isImage = imageFlag ? '1' : '0';
                            }
                            if (o && o.rowId !== undefined && o.rowId !== null && String(o.rowId) !== '') {
                                opt.dataset.rowId = String(o.rowId);
                            }
                            if (o && Number.isInteger(o.rowIndex)) {
                                opt.dataset.rowIndex = String(o.rowIndex);
                            }
                            popupSelect.appendChild(opt);
                        }

                        if (String(popupSelect.value || '').trim() === '') {
                            const opts = Array.from(popupSelect.options || []);
                            const image = opts.find((opt) => String(opt.textContent || '').includes('(Image)') && String(opt.value || '').trim() !== '');
                            if (image) popupSelect.value = String(image.value);
                            else {
                                const last = opts.filter((opt) => String(opt.value || '').trim() !== '').pop();
                                if (last) popupSelect.value = String(last.value);
                            }
                        }

                        setPopupDebug('Surf sync: populated from shared generateSurfaceOptions. rows=' + rows.length + ', options=' + sharedOptions.length + ', selected=' + String(popupSelect.value || '(none)'));
                        return true;
                    }

                    const normalizeType = (v) => String(v || '').trim().toLowerCase();
                    const compactType = (v) => normalizeType(v).replace(/[s_-]+/g, '');
                    const isObjectType = (v) => {
                        const n = normalizeType(v);
                        const c = compactType(v);
                        if (!n && !c) return false;
                        if (n === 'object' || c === 'object' || c === 'objectsurface') return true;
                        return n.startsWith('object ') || n.startsWith('object-') || n.startsWith('object_');
                    };
                    const isCoordTransType = (v) => {
                        const n = normalizeType(v);
                        const c = compactType(v);
                        return n === 'ct' || n === 'coord trans' || n === 'coordinate break' || c === 'ct' || c === 'coordtrans' || c === 'coordinatebreak';
                    };
                    const isGapType = (v) => {
                        const n = normalizeType(v);
                        const c = compactType(v);
                        return n === 'gap' || n === 'air gap' || c === 'gap' || c === 'airgap';
                    };
                    const isSkippableRow = (row) => {
                        const objTypeRaw = row && (row['object type'] ?? row.objectType ?? row.object ?? '');
                        const surfTypeRaw = row && (row.surfType ?? row['surf type'] ?? row.type ?? '');
                        const surfaceType = objTypeRaw || surfTypeRaw || 'Standard';
                        const blockType = row && (row._blockType ?? row.blockType ?? '');
                        const blockRole = row && (row._surfaceRole ?? row.surfaceRole ?? '');
                        return (
                            isObjectType(objTypeRaw) || isObjectType(surfTypeRaw) || isObjectType(surfaceType) ||
                            isCoordTransType(objTypeRaw) || isCoordTransType(surfTypeRaw) || isCoordTransType(surfaceType) ||
                            isGapType(objTypeRaw) || isGapType(surfTypeRaw) || isGapType(surfaceType) ||
                            isGapType(blockType) || isGapType(blockRole)
                        );
                    };

                    let surfaceId = 0;
                    let added = 0;
                    popupSelect.innerHTML = '';

                    const placeholder = document.createElement('option');
                    placeholder.value = '';
                    placeholder.textContent = 'Select Surf';
                    popupSelect.appendChild(placeholder);

                    for (const [i, row] of rows.entries()) {
                        const objTypeRaw = row && (row['object type'] ?? row.objectType ?? row.object ?? '');
                        const surfTypeRaw = row && (row.surfType ?? row['surf type'] ?? row.type ?? '');
                        const surfaceType = objTypeRaw || surfTypeRaw || 'Standard';

                        if (isSkippableRow(row)) continue;
                        surfaceId++;

                        const n = normalizeType(surfaceType);
                        const c = compactType(surfaceType);
                        const isStop = (n === 'stop' || c === 'stop' || n.includes('stop'));
                        const isImage = (n === 'image' || c === 'image' || n.includes('image'));

                        let label = 'Surf ' + surfaceId;
                        if (isStop) label += ' (Stop)';
                        else if (isImage) label += ' (Image)';
                        else label += ' (' + surfaceType + ')';

                        const opt = document.createElement('option');
                        opt.value = String(surfaceId);
                        opt.textContent = label;
                        opt.dataset.rowIndex = String(i);
                        opt.dataset.isImage = isImage ? '1' : '0';
                        if (row && row.id !== undefined && row.id !== null && String(row.id) !== '') {
                            opt.dataset.rowId = String(row.id);
                        }
                        try {
                            const norm = (v) => String(v ?? '').trim().toLowerCase();
                            const n0 = (v) => {
                                const x = Number(v);
                                return Number.isFinite(x) ? String(x) : norm(v);
                            };
                            const rowSig = [
                                't:' + norm(surfaceType),
                                'r:' + n0(row?.radius ?? row?.R ?? ''),
                                'th:' + n0(row?.thickness ?? row?.T ?? ''),
                                'sd:' + n0(row?.semidia ?? row?.semiDia ?? ''),
                                'm:' + norm(row?.material ?? row?.glass ?? row?.['glass'] ?? row?.refractiveIndex ?? ''),
                                'c:' + norm(row?.comment ?? row?.name ?? '')
                            ].join('|');
                            if (rowSig) opt.dataset.rowSig = rowSig;
                        } catch (_) {}
                        popupSelect.appendChild(opt);
                        added++;
                    }
                    if (added > 0 && String(popupSelect.value || '').trim() === '') {
                        const opts = Array.from(popupSelect.options || []);
                        const image = opts.find((opt) => String(opt.textContent || '').includes('(Image)') && String(opt.value || '').trim() !== '');
                        if (image) popupSelect.value = String(image.value);
                        else {
                            const last = opts.filter((opt) => String(opt.value || '').trim() !== '').pop();
                            if (last) popupSelect.value = String(last.value);
                        }
                    }
                    setPopupDebug('Surf sync: populated from opener rows. rows=' + rows.length + ', options=' + added + ', selected=' + String(popupSelect.value || '(none)'));
                    return added > 0;
                } catch (_) {
                    setPopupDebug('Surf sync: failed to read opener rows');
                    return false;
                }
            };

            const normalizeLabel = (text) => {
                const t = String(text || '').trim();
                // Drop leading "Surf N:" / "Surface N:" / "面 N" etc.
                return t
                    .replace(/^Surfs*d+s*[:-]?s*/i, '')
                    .replace(/^Surfaces*d+s*[:-]?s*/i, '')
                    .replace(/^面s*d+s*[:-]?s*/i, '')
                    .trim();
            };

            const prevValue = popupSelect.value;
            const prevText = popupSelect.options && popupSelect.selectedIndex >= 0
                ? popupSelect.options[popupSelect.selectedIndex]?.textContent
                : '';
            const prevKey = normalizeLabel(prevText);
            const prevWasLast = popupSelect.options && popupSelect.options.length > 0
                ? popupSelect.selectedIndex === (popupSelect.options.length - 1)
                : false;

            popupSelect.innerHTML = '';
            if (!openerSelect || !openerSelect.options || openerSelect.options.length <= 1) {
                if (populateFromOpenerRows()) {
                    if (prevValue !== '') popupSelect.value = prevValue;
                    setPopupDebug('Surf sync: fallback row population used');
                    return;
                }
                const opt = document.createElement('option');
                opt.value = '';
                opt.textContent = 'Select Surf';
                popupSelect.appendChild(opt);
                setPopupDebug('Surf sync: opener select unavailable and fallback failed');
                return;
            }

            for (const o of openerSelect.options) {
                const opt = document.createElement('option');
                opt.value = o.value;
                // Replace Japanese "面" prefix and "Surface" prefix with "Surf".
                const label = (o.textContent || '').replace(/^面s*/,'Surf ').replace(/^Surfaces*/,'Surf ');
                opt.textContent = label;
                if (o.dataset && o.dataset.rowIndex) opt.dataset.rowIndex = String(o.dataset.rowIndex);
                if (o.dataset && o.dataset.rowId) opt.dataset.rowId = String(o.dataset.rowId);
                if (o.dataset && o.dataset.rowSig) opt.dataset.rowSig = String(o.dataset.rowSig);
                if (o.dataset && o.dataset.isImage) opt.dataset.isImage = String(o.dataset.isImage);
                else {
                    const labelLower = String(label || '').toLowerCase();
                    opt.dataset.isImage = (labelLower.includes('(image)') || labelLower.includes(' image')) ? '1' : '0';
                }
                popupSelect.appendChild(opt);
            }

            // Preserve selection robustly across insert/delete (e.g., Image surface shifts index).
            const hasValue = (v) => Array.from(popupSelect.options || []).some((opt) => String(opt.value) === String(v));
            if (prevValue !== '' && hasValue(prevValue)) {
                popupSelect.value = prevValue;
                return;
            }
            if (prevKey) {
                const opts = Array.from(popupSelect.options || []);
                const match = opts.find((opt) => normalizeLabel(opt.textContent) === prevKey);
                if (match) {
                    popupSelect.value = match.value;
                    return;
                }
            }
            if (prevWasLast && popupSelect.options && popupSelect.options.length > 0) {
                popupSelect.selectedIndex = popupSelect.options.length - 1;
                return;
            }
            // Fallback: mirror opener selection.
            popupSelect.value = openerSelect.value;
            if (String(popupSelect.value || '').trim() === '') {
                const opts = Array.from(popupSelect.options || []);
                const image = opts.find((opt) => String(opt.textContent || '').includes('(Image)') && String(opt.value || '').trim() !== '');
                if (image) popupSelect.value = String(image.value);
                else {
                    const last = opts.filter((opt) => String(opt.value || '').trim() !== '').pop();
                    if (last) popupSelect.value = String(last.value);
                }
            }
            setPopupDebug('Surf sync: mirrored opener select. openerOptions=' + openerSelect.options.length + ', popupOptions=' + Math.max(0, popupSelect.options.length - 1) + ', selected=' + String(popupSelect.value || '(none)'));
        }

        function resolvePopupSurfaceRowIndexForNative(popupSelect, selectedOption) {
            try {
                const ds = selectedOption && selectedOption.dataset ? selectedOption.dataset : null;
                const rowIndexRaw = ds ? ds.rowIndex : undefined;
                const rowIndex = Number(rowIndexRaw);
                if (Number.isInteger(rowIndex) && rowIndex >= 0) {
                    return rowIndex;
                }

                const ordinal = Number.parseInt(String(popupSelect && popupSelect.value !== undefined ? popupSelect.value : ''), 10);
                if (!Number.isInteger(ordinal) || ordinal <= 0) {
                    return undefined;
                }

                const rows = (() => {
                    try {
                        if (!window.opener) return null;
                        if (typeof window.opener.getOpticalSystemRows === 'function') {
                            const r = window.opener.getOpticalSystemRows(window.opener.tableOpticalSystem);
                            if (Array.isArray(r) && r.length > 0) return r;
                            const r2 = window.opener.getOpticalSystemRows();
                            if (Array.isArray(r2) && r2.length > 0) return r2;
                        }
                    } catch (_) {}
                    try {
                        if (window.opener && window.opener.tableOpticalSystem && typeof window.opener.tableOpticalSystem.getData === 'function') {
                            const r = window.opener.tableOpticalSystem.getData();
                            if (Array.isArray(r) && r.length > 0) return r;
                        }
                    } catch (_) {}
                    return null;
                })();

                if (!Array.isArray(rows) || rows.length === 0) {
                    return undefined;
                }

                const normalizeType = (v) => String(v || '').trim().toLowerCase();
                const compactType = (v) => normalizeType(v).replace(/[s_-]+/g, '');
                const isObjectType = (v) => {
                    const n = normalizeType(v);
                    const c = compactType(v);
                    if (!n && !c) return false;
                    if (n === 'object' || c === 'object' || c === 'objectsurface') return true;
                    return n.startsWith('object ') || n.startsWith('object-') || n.startsWith('object_');
                };
                const isCoordTransType = (v) => {
                    const n = normalizeType(v);
                    const c = compactType(v);
                    return n === 'ct' || n === 'coord trans' || n === 'coordinate break' || c === 'ct' || c === 'coordtrans' || c === 'coordinatebreak';
                };
                const isGapType = (v) => {
                    const n = normalizeType(v);
                    const c = compactType(v);
                    return n === 'gap' || n === 'air gap' || c === 'gap' || c === 'airgap';
                };
                const isSkippableRow = (row) => {
                    const objTypeRaw = row && (row['object type'] ?? row.objectType ?? row.object ?? '');
                    const surfTypeRaw = row && (row.surfType ?? row['surf type'] ?? row.type ?? '');
                    const surfaceType = objTypeRaw || surfTypeRaw || 'Standard';
                    const blockType = row && (row._blockType ?? row.blockType ?? '');
                    const blockRole = row && (row._surfaceRole ?? row.surfaceRole ?? '');
                    return (
                        isObjectType(objTypeRaw) || isObjectType(surfTypeRaw) || isObjectType(surfaceType) ||
                        isCoordTransType(objTypeRaw) || isCoordTransType(surfTypeRaw) || isCoordTransType(surfaceType) ||
                        isGapType(objTypeRaw) || isGapType(surfTypeRaw) || isGapType(surfaceType) ||
                        isGapType(blockType) || isGapType(blockRole)
                    );
                };

                let count = 0;
                for (let i = 0; i < rows.length; i++) {
                    if (isSkippableRow(rows[i])) continue;
                    count += 1;
                    if (count === ordinal) return i;
                }

                return undefined;
            } catch (_) {
                return undefined;
            }
        }

        function syncInputsFromOpener() {
            const openerRay = getOpenerEl('ray-count-input');
            const openerRing = getOpenerEl('ring-count-select');
            const popupRay = document.getElementById('popup-ray-count-input');
            const popupRing = document.getElementById('popup-ring-count-select');

            if (popupRay && openerRay && popupRay.value !== openerRay.value) popupRay.value = openerRay.value;
            if (popupRing && openerRing && popupRing.value !== openerRing.value) popupRing.value = openerRing.value;

            // pattern
            const annular = getOpenerEl('annular-pattern-btn');
            const popupPattern = document.getElementById('popup-pattern-select');
            if (popupPattern) {
                const userLocked = popupPattern.dataset && popupPattern.dataset.userLocked === '1';
                if (!userLocked) {
                    const isAnnular = !!annular && annular.classList.contains('active');
                    popupPattern.value = isAnnular ? 'annular' : 'grid';
                }
            }
        }

        function setPopupPattern(pattern) {
            const isAnnular = String(pattern || 'annular') !== 'grid';

            const openerAnnular = getOpenerEl('annular-pattern-btn');
            const openerGrid = getOpenerEl('grid-pattern-btn');
            if (isAnnular && openerAnnular) openerAnnular.click();
            if (!isAnnular && openerGrid) openerGrid.click();

            try {
                import('./spot-diagram-settings-storage.ts').then(({ setSpotDiagramPattern }) => {
                    try {
                        setSpotDiagramPattern(isAnnular ? 'annular' : 'grid', { preferOpener: true });
                    } catch (_) {}
                });
            } catch (_) {}
        }

        document.getElementById('popup-pattern-select').addEventListener('change', (e) => {
            const v = e && e.target && e.target.value ? String(e.target.value) : 'annular';
            try {
                const popupPattern = document.getElementById('popup-pattern-select');
                if (popupPattern && popupPattern.dataset) popupPattern.dataset.userLocked = '1';
            } catch (_) {}
            setPopupPattern(v);
        });

        document.getElementById('popup-show-spot-diagram-btn').addEventListener('click', async () => {
            const popupContainer = document.getElementById('popup-spot-diagram-container');
            if (popupContainer) popupContainer.innerHTML = '';

            const opener = window.opener;
            if (!opener) {
                if (popupContainer) popupContainer.textContent = 'Main window is not available.';
                return;
            }

            try {
                if (typeof opener.loadActiveConfigurationToTables === 'function') {
                    await opener.loadActiveConfigurationToTables({
                        applyToUI: true,
                        suppressOpticalSystemDataChanged: true,
                    });
                    await new Promise((resolve) => setTimeout(resolve, 0));
                }
            } catch (_) {}

            // Ensure surface indices/options are up-to-date (CB insert/delete shifts indices).
            try { syncSurfaceOptionsFromOpener(); } catch (_) {}

            const progressWrapper = document.getElementById('popup-spot-progress-wrapper');
            const progressBarEl = document.getElementById('popup-spot-progressbar');
            const progressTextEl = document.getElementById('popup-spot-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl) {
                        const pct = Number.isFinite(value) ? (String(Math.round(Math.max(0, Math.min(100, Number(value))))) + '%') : '';
                        const msg = (typeof text === 'string' && text.trim().length > 0) ? text : 'Working...';
                        progressTextEl.textContent = pct ? (msg + ' (' + pct + ')') : msg;
                    }
                } catch (_) {}
            };

            const openerRay = getOpenerEl('ray-count-input');
            const openerRing = getOpenerEl('ring-count-select');
            const openerSurface = getOpenerEl('surface-number-select');
            const popupRay = document.getElementById('popup-ray-count-input');
            const popupRing = document.getElementById('popup-ring-count-select');
            const popupSurface = document.getElementById('popup-surface-number-select');
            const popupPattern = document.getElementById('popup-pattern-select');
            const popupSurfaceSelectedOption = (popupSurface && popupSurface.selectedOptions && popupSurface.selectedOptions.length > 0)
                ? popupSurface.selectedOptions[0]
                : null;
            const popupSurfaceIsImage = !!(popupSurfaceSelectedOption && (
                (popupSurfaceSelectedOption.dataset && String(popupSurfaceSelectedOption.dataset.isImage || '') === '1') ||
                String(popupSurfaceSelectedOption.textContent || '').toLowerCase().includes('(image)')
            ));
            const popupSurfaceRowIndex = resolvePopupSurfaceRowIndexForNative(popupSurface, popupSurfaceSelectedOption);
            const popupSurfaceRowId = (() => {
                const raw = popupSurfaceSelectedOption && popupSurfaceSelectedOption.dataset
                    ? popupSurfaceSelectedOption.dataset.rowId
                    : undefined;
                const s = String(raw || '').trim();
                return s !== '' ? s : undefined;
            })();
            const popupSurfaceRowSig = (() => {
                const raw = popupSurfaceSelectedOption && popupSurfaceSelectedOption.dataset
                    ? popupSurfaceSelectedOption.dataset.rowSig
                    : undefined;
                const s = String(raw || '').trim();
                return s !== '' ? s : undefined;
            })();

            if (!popupSurface || String(popupSurface.value || '').trim() === '') {
                if (popupContainer) {
                    popupContainer.textContent = 'Please select Surf before running Spot Diagram.';
                }
                setPopupRunDebug('Show failed: Surf not selected. popupOptions=' + ((popupSurface && popupSurface.options) ? Math.max(0, popupSurface.options.length - 1) : 0));
                setProgress(100, 'Failed: Surf not selected');
                return;
            }

            if (openerRay && popupRay) openerRay.value = popupRay.value;
            if (openerRing && popupRing) openerRing.value = popupRing.value;
            if (openerSurface && popupSurface) openerSurface.value = popupSurface.value;

            try {
                setProgress(0, 'Starting...');
                setPopupStats('Native stats: running...');

                const shouldUseDesktopRust = (() => {
                    try {
                        if (typeof window !== 'undefined' && typeof window['shouldUseDesktopRustAnalysis'] === 'function') {
                            return !!window['shouldUseDesktopRustAnalysis']();
                        }
                        if (opener && typeof opener.shouldUseDesktopRustAnalysis === 'function') {
                            return !!opener.shouldUseDesktopRustAnalysis();
                        }
                        return true;
                    } catch (_) {
                        return false;
                    }
                })();
                const canUseDesktopRust = shouldUseDesktopRust && !!(
                    typeof opener.runDesktopAnalysisComputeForPopup === 'function'
                );
                const canUseNativeRustSpot = canUseDesktopRust && !!(
                    typeof opener.runDesktopNativeSpotRaytraceForPopup === 'function'
                );
                const canUseRustSpotDiagram = canUseDesktopRust && !!(
                    typeof window !== 'undefined'
                    && window.__COOPT_ENABLE_RUST_SPOT_DIAGRAM !== false
                );

                if (canUseNativeRustSpot) {
                    try {
                        setProgress(25, 'Computing Spot Diagram (Native Rust)...');
                        const nativeStart = (typeof performance !== 'undefined' && typeof performance.now === 'function') ? performance.now() : Date.now();
                        try {
                            const objCount = Array.isArray(opener.getObjectRows ? opener.getObjectRows(opener.tableObject) : null)
                                ? opener.getObjectRows(opener.tableObject).length
                                : (Array.isArray(opener.tableObject?.data) ? opener.tableObject.data.length : 0);
                            setPopupRunDebug('Native Spot input: objectRows=' + objCount);
                        } catch (_) {}
                        const objectRowsForNative = (() => {
                            try {
                                if (opener.tableObject && typeof opener.tableObject.getData === 'function') {
                                    const rows = opener.tableObject.getData();
                                    if (Array.isArray(rows)) return rows;
                                }
                            } catch (_) {}
                            try {
                                if (typeof opener.getObjectRows === 'function') {
                                    const rows = opener.getObjectRows(opener.tableObject);
                                    if (Array.isArray(rows)) return rows;
                                }
                            } catch (_) {}
                            try {
                                const raw = opener.localStorage && opener.localStorage.getItem('objectTableData');
                                if (raw) {
                                    const parsed = JSON.parse(raw);
                                    if (Array.isArray(parsed)) return parsed;
                                }
                            } catch (_) {}
                            return [];
                        })();
                        try {
                            const highFieldObjects = (Array.isArray(objectRowsForNative) ? objectRowsForNative : []).filter((row) => {
                                const ax = Number(row?.xAngle ?? row?.objectAngleX ?? row?.xHeightAngle ?? row?.x ?? row?.angleX);
                                const ay = Number(row?.yAngle ?? row?.objectAngleY ?? row?.yHeightAngle ?? row?.y ?? row?.angle ?? row?.angleY);
                                const maxField = Math.max(Math.abs(Number.isFinite(ax) ? ax : 0), Math.abs(Number.isFinite(ay) ? ay : 0));
                                return maxField >= 15;
                            });
                            setPopupRunDebug(
                                'Native Spot preflight: objects=' + String(objectRowsForNative.length) +
                                ', highFieldObjects=' + String(highFieldObjects.length)
                            );
                        } catch (_) {}

                        const result = await opener.runDesktopNativeSpotRaytraceForPopup({
                            surfaceIndex: (popupSurfaceRowIndex !== undefined)
                                ? popupSurfaceRowIndex
                                : (popupSurface && popupSurface.value !== '' ? parseInt(popupSurface.value, 10) : undefined),
                            rayCount: popupRay && popupRay.value !== '' ? parseInt(popupRay.value, 10) : undefined,
                            ringCount: popupRing && popupRing.value !== '' ? parseInt(popupRing.value, 10) : undefined,
                            pattern: popupPattern ? String(popupPattern.value || 'annular') : 'annular',
                            wavelengthMode: 'all',
                            objectRows: objectRowsForNative,
                        });
                        const series = Array.isArray(result?.series) ? result.series : [];
                        const totalPointCount = series.reduce((sum, s) => {
                            const pts = Array.isArray(s?.points) ? s.points.length : 0;
                            return sum + pts;
                        }, 0);
                        const firstSeries = series.length > 0 ? series[0] : null;
                        const firstPoint = (firstSeries && Array.isArray(firstSeries.points) && firstSeries.points.length > 0)
                            ? firstSeries.points[0]
                            : null;
                        try {
                            setPopupRunDebug(
                                'Native Spot run: selected=' + String(popupSurface && popupSurface.value || '(none)') +
                                ', rowIndex=' + String(popupSurfaceRowIndex !== undefined ? popupSurfaceRowIndex : '(unresolved)') +
                                ', hit=' + String(Number(result?.totalHitRays) || 0) +
                                ', series=' + String(series.length) +
                                ', points=' + String(totalPointCount) +
                                (firstPoint
                                    ? ', first=(' + Number(firstPoint?.xUm || 0).toFixed(2) + ',' + Number(firstPoint?.yUm || 0).toFixed(2) + ')'
                                    : ', first=(none)')
                            );
                        } catch (_) {}
                        if (!series.length) {
                            throw new Error('Native Rust Spot result is empty');
                        }
                        const airyRadiusUm = Number(result?.airyRadiusUm);
                        try {
                            const requestedRays = Number(result?.requestedRays);
                            const generatedRays = Number(result?.generatedRays);
                            const totalAttemptedRays = Number(result?.totalAttemptedRays);
                            const totalHitRays = Number(result?.totalHitRays);
                            const meanHitRatePercent = Number(result?.meanHitRatePercent);
                            const maxHitRays = Number(result?.maxHitRays);
                            const wavelengthCount = Number(result?.wavelengthCount);
                            const raySeriesGenerationMs = Number(result?.timingMs?.raySeriesGeneration);
                            const nativeInvokeMs = Number(result?.timingMs?.nativeInvoke);
                            const nativeTraceMs = Number(result?.timingMs?.nativeTrace);
                            const stats = Array.isArray(result?.seriesStats) ? result.seriesStats : [];
                            const statsText = stats.slice(0, 3).map((s) => {
                                const label = String(s?.label || '-');
                                const h = Number(s?.hitRays) || 0;
                                const a = Number(s?.attemptedRays) || 0;
                                const r = Number(s?.hitRatePercent) || 0;
                                return label + ': ' + h + '/' + a + ' (' + r.toFixed(1) + '%)';
                            }).join(', ');
                            const timingsText =
                                'timing(ms): gen=' + (Number.isFinite(raySeriesGenerationMs) ? raySeriesGenerationMs.toFixed(1) : '-') +
                                ', trace=' + (Number.isFinite(nativeTraceMs) ? nativeTraceMs.toFixed(1) : '-') +
                                ', native=' + (Number.isFinite(nativeInvokeMs) ? nativeInvokeMs.toFixed(1) : '-');
                            setPopupStats(
                                'Native stats: requested=' + (Number.isFinite(requestedRays) ? requestedRays : '-') +
                                ', generated=' + (Number.isFinite(generatedRays) ? generatedRays : '-') +
                                ', attempted=' + (Number.isFinite(totalAttemptedRays) ? totalAttemptedRays : '-') +
                                ', hits=' + (Number.isFinite(totalHitRays) ? totalHitRays : '-') +
                                ', meanHitRate=' + (Number.isFinite(meanHitRatePercent) ? meanHitRatePercent.toFixed(1) + '%' : '-') +
                                ', maxHit=' + (Number.isFinite(maxHitRays) ? maxHitRays : '-') +
                                ', wavelengths=' + (Number.isFinite(wavelengthCount) ? wavelengthCount : '-') +
                                (Number.isFinite(airyRadiusUm) && airyRadiusUm > 0 ? ', airyR=' + airyRadiusUm.toFixed(2) + 'µm' : '') +
                                (statsText ? ' | ' + statsText : '') +
                                ' | ' + timingsText
                            );
                        } catch (_) {}
                        if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                            throw new Error('Plotly is not available in Spot Diagram popup');
                        }

                        const traces = [];
                        const toWavelengthLabel = (rawLabel) => {
                            const text = String(rawLabel || '').trim();
                            const nm = text.match(/(d+(?:.d+)?)s*nm/i);
                            if (nm && nm[1]) return 'Wavelength ' + nm[1] + 'nm';
                            const lower = text.toLowerCase();
                            if (lower.includes('primary')) return 'Wavelength Primary';
                            return 'Wavelength ' + text;
                        };
                        const wavelengthLabelFromSeries = (seriesItem, rawLabel) => {
                            const wl = Number(seriesItem?.wavelengthUm);
                            if (Number.isFinite(wl) && wl > 0) {
                                return 'Wavelength ' + (wl * 1000).toFixed(1) + 'nm';
                            }
                            return toWavelengthLabel(rawLabel);
                        };
                        const parseSeriesLabel = (label, fallbackObjectLabel) => {
                            const raw = String(label || '').trim();
                            if (!raw) {
                                return { objectLabel: fallbackObjectLabel, wavelengthLabel: 'Wavelength Primary' };
                            }
                            const m = raw.match(/(Primary(?:s*([^)]*))?|d+(?:.d+)?s*nm)s*$/i);
                            if (m && m[1]) {
                                const wlRaw = String(m[1] || 'Primary');
                                const prefix = raw.slice(0, Math.max(0, m.index || 0)).replace(/[|@-s]+$/, '').trim();
                                return {
                                    objectLabel: prefix || fallbackObjectLabel,
                                    wavelengthLabel: toWavelengthLabel(wlRaw),
                                };
                            }
                            return { objectLabel: raw, wavelengthLabel: 'Wavelength Primary' };
                        };

                        const calcPercentile = (arr, q) => {
                            if (!Array.isArray(arr) || arr.length === 0) return 0;
                            const sorted = arr
                                .filter((v) => Number.isFinite(v) && v >= 0)
                                .sort((a, b) => a - b);
                            if (sorted.length === 0) return 0;
                            const qq = Math.max(0, Math.min(1, Number(q) || 0));
                            const idx = Math.floor((sorted.length - 1) * qq);
                            return sorted[Math.max(0, Math.min(sorted.length - 1, idx))] || 0;
                        };

                        const cullExtremeSpotOutliers = (rawPoints, centerXUm, centerYUm) => {
                            const pts = (Array.isArray(rawPoints) ? rawPoints : [])
                                .map((p) => ({
                                    xUm: Number(p?.xUm),
                                    yUm: Number(p?.yUm),
                                }))
                                .filter((p) => Number.isFinite(p.xUm) && Number.isFinite(p.yUm));
                            if (pts.length < 40) {
                                return { points: pts, culled: 0 };
                            }

                            const cx = Number.isFinite(centerXUm) ? centerXUm : 0;
                            const cy = Number.isFinite(centerYUm) ? centerYUm : 0;
                            const radii = pts.map((p) => {
                                const dx = p.xUm - cx;
                                const dy = p.yUm - cy;
                                return Math.sqrt(dx * dx + dy * dy);
                            });

                            const p95 = calcPercentile(radii, 0.95);
                            const p99 = calcPercentile(radii, 0.99);
                            const maxR = radii.reduce((m, v) => Math.max(m, Number(v) || 0), 0);
                            const clipR = Math.max(5, p99 * 1.35, p95 * 2.2);
                            if (!Number.isFinite(clipR) || clipR <= 0 || maxR <= clipR) {
                                return { points: pts, culled: 0 };
                            }

                            const filtered = [];
                            for (let i = 0; i < pts.length; i++) {
                                const p = pts[i];
                                const dx = p.xUm - cx;
                                const dy = p.yUm - cy;
                                const r = Math.sqrt(dx * dx + dy * dy);
                                if (r <= clipR) {
                                    filtered.push(p);
                                }
                            }

                            const culled = pts.length - filtered.length;
                            const maxAllowCull = Math.max(3, Math.floor(pts.length * 0.02));
                            if (culled <= 0 || culled > maxAllowCull || filtered.length < 10) {
                                return { points: pts, culled: 0 };
                            }

                            return { points: filtered, culled };
                        };

                        const groupedByObject = new Map();
                        for (let i = 0; i < series.length; i++) {
                            const s = series[i] || {};
                            const parsed = parseSeriesLabel(s?.label, 'Object ' + (i + 1));
                            if (!groupedByObject.has(parsed.objectLabel)) {
                                groupedByObject.set(parsed.objectLabel, []);
                            }
                            groupedByObject.get(parsed.objectLabel).push(s);
                        }
                        const objectEntries = Array.from(groupedByObject.entries());

                        let culledPointsCount = 0;
                        const preparedObjects = objectEntries.map(([objectLabel, objectSeries], index) => {
                            const groups = [];
                            for (let sIdx = 0; sIdx < objectSeries.length; sIdx++) {
                                const s = objectSeries[sIdx] || {};
                                const parsed = parseSeriesLabel(s?.label, objectLabel);
                                const points = Array.isArray(s?.points) ? s.points : [];
                                const hasFieldAngle = !!s?.hasFieldAngle;
                                const chiefXUm = Number(s?.chiefPointUm?.xUm);
                                const chiefYUm = Number(s?.chiefPointUm?.yUm);
                                const useChiefCentering = hasFieldAngle && Number.isFinite(chiefXUm) && Number.isFinite(chiefYUm);
                                const centeredPoints = useChiefCentering
                                    ? points.map((p) => ({
                                        xUm: (Number(p?.xUm) || 0) - chiefXUm,
                                        yUm: (Number(p?.yUm) || 0) - chiefYUm,
                                    }))
                                    : points.map((p) => ({
                                        xUm: Number(p?.xUm) || 0,
                                        yUm: Number(p?.yUm) || 0,
                                    }));

                                const culled = cullExtremeSpotOutliers(
                                    centeredPoints,
                                    useChiefCentering ? 0 : undefined,
                                    useChiefCentering ? 0 : undefined,
                                );
                                culledPointsCount += Number(culled?.culled) || 0;

                                groups.push({
                                    label: wavelengthLabelFromSeries(s, parsed.wavelengthLabel),
                                    color: String(s?.color || '#2563eb'),
                                    points: culled.points,
                                });
                            }
                            return { objectLabel, objectIndex: index + 1, groups };
                        });

                        const absSamples = [];
                        for (const obj of preparedObjects) {
                            for (const g of obj.groups) {
                                const pts = Array.isArray(g?.points) ? g.points : [];
                                for (const p of pts) {
                                    const x = Math.abs(Number(p?.xUm) || 0);
                                    const y = Math.abs(Number(p?.yUm) || 0);
                                    absSamples.push(x, y);
                                }
                            }
                        }
                        const maxAbs = absSamples.reduce((m, v) => Math.max(m, Number(v) || 0), 0);
                        const p98Abs = calcPercentile(absSamples, 0.98);
                        let unifiedRangeAbs = Math.max(1, p98Abs * 1.25);
                        if (Number.isFinite(airyRadiusUm) && airyRadiusUm > 0) {
                            unifiedRangeAbs = Math.max(unifiedRangeAbs, airyRadiusUm * 1.5);
                        }
                        unifiedRangeAbs = Math.min(unifiedRangeAbs, Math.max(50, maxAbs * 1.05 || 50));

                        const layout = {
                            margin: { l: 40, r: 16, t: 92, b: 32 },
                            showlegend: true,
                            paper_bgcolor: '#ffffff',
                            plot_bgcolor: '#ffffff',
                            annotations: [],
                            shapes: [],
                            legend: {
                                orientation: 'h',
                                yanchor: 'bottom',
                                y: 1.12,
                                xanchor: 'left',
                                x: 0,
                                traceorder: 'normal',
                                font: { size: 11 },
                            },
                            legendgroupclick: 'togglegroup',
                        };

                        const count = Math.max(1, preparedObjects.length);
                        const cols = Math.max(1, Math.min(3, Math.ceil(Math.sqrt(count))));
                        const rows = Math.max(1, Math.ceil(count / cols));
                        const hGap = 0.05;
                        const vGap = 0.12;
                        const cellW = (1 - (cols - 1) * hGap) / cols;
                        const cellH = (1 - (rows - 1) * vGap) / rows;
                        layout.height = Math.max(360, rows * 340);

                        const legendAdded = new Set();
                        const legendColorByLabel = new Map();

                        for (let i = 0; i < preparedObjects.length; i++) {
                            const obj = preparedObjects[i];
                            const objectLabel = obj.objectLabel;
                            const groups = obj.groups;

                            const axisSuffix = i === 0 ? '' : String(i + 1);
                            const xAxisName = 'x' + axisSuffix;
                            const yAxisName = 'y' + axisSuffix;
                            const xAxisLayoutKey = 'xaxis' + axisSuffix;
                            const yAxisLayoutKey = 'yaxis' + axisSuffix;

                            const col = i % cols;
                            const rowFromTop = Math.floor(i / cols);
                            const x0 = col * (cellW + hGap);
                            const x1 = x0 + cellW;
                            const y1 = 1 - rowFromTop * (cellH + vGap);
                            const y0 = y1 - cellH;

                            layout[xAxisLayoutKey] = {
                                domain: [x0, x1],
                                zeroline: true,
                                range: [-unifiedRangeAbs, unifiedRangeAbs],
                                title: rowFromTop === (rows - 1) ? 'X (µm)' : '',
                                anchor: yAxisName,
                            };
                            layout[yAxisLayoutKey] = {
                                domain: [y0, y1],
                                zeroline: true,
                                range: [-unifiedRangeAbs, unifiedRangeAbs],
                                title: col === 0 ? 'Y (µm)' : '',
                                anchor: xAxisName,
                                scaleanchor: xAxisName,
                                scaleratio: 1,
                            };

                            if (Number.isFinite(airyRadiusUm) && airyRadiusUm > 0) {
                                layout.shapes.push({
                                    type: 'circle',
                                    xref: xAxisName,
                                    yref: yAxisName,
                                    x0: -airyRadiusUm,
                                    y0: -airyRadiusUm,
                                    x1: airyRadiusUm,
                                    y1: airyRadiusUm,
                                    line: { color: '#111827', width: 1 },
                                    fillcolor: 'rgba(0,0,0,0)',
                                });
                            }

                            for (const g of groups) {
                                const wlLabel = String(g?.label || 'Wavelength');
                                const pts = Array.isArray(g?.points) ? g.points : [];
                                traces.push({
                                    x: pts.map((p) => Number(p?.xUm) || 0),
                                    y: pts.map((p) => Number(p?.yUm) || 0),
                                    xaxis: xAxisName,
                                    yaxis: yAxisName,
                                    type: 'scattergl',
                                    mode: 'markers',
                                    name: wlLabel,
                                    legendgroup: wlLabel,
                                    showlegend: false,
                                    marker: {
                                        size: 6,
                                        color: String(g?.color || '#2563eb'),
                                        opacity: 0.85,
                                        symbol: 'circle',
                                        line: {
                                            width: 0.8,
                                            color: '#333333',
                                        },
                                    },
                                    hovertemplate: 'x=%{x:.2f}µm<br>y=%{y:.2f}µm<extra></extra>',
                                });
                                if (!legendColorByLabel.has(wlLabel)) {
                                    legendColorByLabel.set(wlLabel, String(g?.color || '#2563eb'));
                                }
                            }
                        }

                        for (const [wlLabel, color] of legendColorByLabel.entries()) {
                            if (legendAdded.has(wlLabel)) continue;
                            legendAdded.add(wlLabel);
                            traces.push({
                                x: [null],
                                y: [null],
                                type: 'scatter',
                                mode: 'markers',
                                name: wlLabel,
                                legendgroup: wlLabel,
                                showlegend: true,
                                marker: {
                                    size: 8,
                                    color: color,
                                    symbol: 'circle',
                                },
                                hoverinfo: 'skip',
                            });
                        }

                        setProgress(85, 'Rendering Spot Diagram...');
                        const plotStart = (typeof performance !== 'undefined' && typeof performance.now === 'function') ? performance.now() : Date.now();
                        await window.Plotly.newPlot(popupContainer, traces, layout, { responsive: true, displaylogo: false });
                        const plotEnd = (typeof performance !== 'undefined' && typeof performance.now === 'function') ? performance.now() : Date.now();
                        try {
                            if (progressWrapper) progressWrapper.style.display = 'none';
                        } catch (_) {}
                        return;
                    } catch (nativeErr) {
                        setPopupRunDebug('Native spot failed; fallback to Rust-WASM: ' + String(nativeErr && nativeErr.message ? nativeErr.message : nativeErr));
                        setPopupStats('Native stats: unavailable (fallback to Rust-WASM)');
                    }
                }

                if (canUseRustSpotDiagram) {
                    setProgress(25, 'Computing Spot Diagram (Rust)...');
                    const result = await opener.runDesktopAnalysisComputeForPopup({
                        kind: 'spot-diagram',
                        surfaceIndex: popupSurface && popupSurface.value !== '' ? parseInt(popupSurface.value, 10) : undefined,
                        rayCount: popupRay && popupRay.value !== '' ? parseInt(popupRay.value, 10) : undefined,
                        ringCount: popupRing && popupRing.value !== '' ? parseInt(popupRing.value, 10) : undefined,
                        pattern: popupPattern ? String(popupPattern.value || 'annular') : 'annular',
                    });
                    const series = Array.isArray(result?.spotDiagramSeries) ? result.spotDiagramSeries : [];
                    if (!series.length) {
                        throw new Error('Rust Spot Diagram result is empty');
                    }
                    if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                        throw new Error('Plotly is not available in Spot Diagram popup');
                    }

                    const traces = [];
                    for (const s of series) {
                        const points = Array.isArray(s?.points) ? s.points : [];
                        traces.push({
                            x: points.map((p) => Number(p?.xUm) || 0),
                            y: points.map((p) => Number(p?.yUm) || 0),
                            type: 'scattergl',
                            mode: 'markers',
                            name: String(s?.label || 'Series'),
                            marker: {
                                size: 3,
                                color: String(s?.color || '#2563eb'),
                                opacity: 0.6,
                            },
                            hovertemplate: 'x=%{x:.2f}µm<br>y=%{y:.2f}µm<extra></extra>',
                        });
                    }

                    setProgress(85, 'Rendering Spot Diagram...');
                    await window.Plotly.newPlot(popupContainer, traces, {
                        margin: { l: 52, r: 20, t: 20, b: 45 },
                        xaxis: { title: 'X (µm)', zeroline: true },
                        yaxis: { title: 'Y (µm)', zeroline: true, scaleanchor: 'x', scaleratio: 1 },
                        showlegend: true,
                        paper_bgcolor: '#ffffff',
                        plot_bgcolor: '#ffffff',
                    }, { responsive: true, displaylogo: false });
                    try {
                        if (progressWrapper) progressWrapper.style.display = 'none';
                    } catch (_) {}
                    return;
                }

                if (typeof opener.showSpotDiagram !== 'function') {
                    throw new Error('showSpotDiagram is not available on opener');
                }

                setProgress(25, 'Computing Spot Diagram (Rust-WASM raytrace)...');
                await opener.showSpotDiagram({
                    containerElement: popupContainer,
                    surfaceIndex: popupSurface && popupSurface.value !== '' ? parseInt(popupSurface.value, 10) : undefined,
                    surfaceRowIndex: popupSurfaceRowIndex,
                    surfaceRowId: popupSurfaceRowId,
                    surfaceRowSig: popupSurfaceRowSig,
                    surfaceIsImage: popupSurfaceIsImage,
                    rayCount: popupRay && popupRay.value !== '' ? parseInt(popupRay.value, 10) : undefined,
                    ringCount: popupRing && popupRing.value !== '' ? parseInt(popupRing.value, 10) : undefined,
                    pattern: popupPattern ? String(popupPattern.value || 'annular') : 'annular',
                    wavelengthMode: 'all',
                    forceRustWasmTrace: true,
                    requireRustWasmTrace: true,
                    onProgress: (evt) => {
                        try {
                            const p = Number(evt && evt.percent);
                            const msg = (evt && evt.message) || (evt && evt.phase) || 'Working...';
                            if (Number.isFinite(p)) setProgress(p, msg);
                            else setProgress(undefined, msg);
                        } catch (_) {}
                    },
                });
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            } catch (e) {
                if (popupContainer) popupContainer.textContent = String(e && e.message ? e.message : e);
                setProgress(100, 'Failed');
            }
        });

        function syncAll() {
            syncSurfaceOptionsFromOpener();
            syncInputsFromOpener();
        }

        try {
            w.__cooptSpotPopupSyncAll = syncAll;
        } catch (_) {}
        window.addEventListener('message', (ev) => {
            try {
                const data = ev && ev.data;
                if (!data || data.action !== 'coopt-spot-sync') return;
                syncAll();
            } catch (_) {}
        });

        window.addEventListener('focus', syncAll);
        setInterval(syncAll, 1000);
        syncAll();
    <\/script>
</body>
</html>
                        `);try{S.document.close()}catch{}});const s=document.getElementById("open-spherical-aberration-window-btn");s&&s.addEventListener("click",()=>{if(T.__sphericalAberrationPopup&&!T.__sphericalAberrationPopup.closed){try{T.__sphericalAberrationPopup.focus()}catch{}try{typeof T.__sphericalAberrationPopup.renderSphericalAberration=="function"&&T.__sphericalAberrationPopup.renderSphericalAberration()}catch{}return}const S=n("Spherical Aberration","width=800,height=600");if(!S){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__sphericalAberrationPopup=S,S.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Spherical Aberration</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
            width: 90px;
        }
        .note {
            padding: 10px 12px;
            color: #666;
            font-size: 12px;
            border-bottom: 1px solid #eee;
            background: #fff;
        }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: auto;
            background: white;
        }
        #popup-longitudinal-aberration-container { height: 100%; min-height: 100%; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-longitudinal-ray-count-input">Ray number:</label>
        <input type="number" id="popup-longitudinal-ray-count-input" value="100" min="1" max="1001" step="1" />
        <span class="note-inline" style="font-size:12px;color:#666;">(Always normalized by stop diameter)</span>
        <label for="popup-longitudinal-reference-focus-mode" style="margin-left:6px;">Reference focus:</label>
        <select id="popup-longitudinal-reference-focus-mode" style="font-size:12px;">
            <option value="primary-paraxial">Primary paraxial</option>
            <option value="current-paraxial" selected>Current paraxial</option>
            <option value="chief-ray">Chief ray</option>
        </select>
        <button id="popup-show-spherical-aberration-btn" type="button">Show</button>
    </div>
    <div id="popup-spherical-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-spherical-progress-text" style="margin-bottom: 6px;">Calculating spherical aberration...</div>
        <progress id="popup-spherical-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-longitudinal-aberration-container"></div>
    </div>

    <script>
        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (e) {
                return null;
            }
        }

        function syncFromOpener() {
            const openerRay = getOpenerEl('longitudinal-ray-count-input');
            const popupRay = document.getElementById('popup-longitudinal-ray-count-input');
            if (openerRay && popupRay) {
                popupRay.value = openerRay.value;
            }
            const openerMode = getOpenerEl('longitudinal-reference-focus-mode');
            const popupMode = document.getElementById('popup-longitudinal-reference-focus-mode');
            if (openerMode && popupMode && openerMode.value) {
                popupMode.value = openerMode.value;
            }
        }

        window['renderSphericalAberration'] = async () => {
            const progressWrapper = document.getElementById('popup-spherical-progress-wrapper');
            const progressBarEl = document.getElementById('popup-spherical-progressbar');
            const progressTextEl = document.getElementById('popup-spherical-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl) {
                        const pct = Number.isFinite(value) ? (String(Math.round(Math.max(0, Math.min(100, Number(value))))) + '%') : '';
                        const msg = (typeof text === 'string' && text.trim().length > 0) ? text : 'Working...';
                        progressTextEl.textContent = pct ? (msg + ' (' + pct + ')') : msg;
                    }
                } catch (_) {}
            };

            const popupRay = document.getElementById('popup-longitudinal-ray-count-input');
            const rayCount = popupRay ? parseInt(popupRay.value, 10) : 51;
            const openerRay = getOpenerEl('longitudinal-ray-count-input');
            if (openerRay && Number.isFinite(rayCount)) {
                openerRay.value = String(rayCount);
            }
            const popupMode = document.getElementById('popup-longitudinal-reference-focus-mode');
            const referenceFocusMode = popupMode && popupMode.value ? String(popupMode.value) : 'current-paraxial';
            const openerMode = getOpenerEl('longitudinal-reference-focus-mode');
            if (openerMode && referenceFocusMode) {
                openerMode.value = referenceFocusMode;
            }

            const opener = window.opener;
            const shouldUseDesktopRust = (() => {
                try {
                    if (typeof window !== 'undefined' && typeof window['shouldUseDesktopRustAnalysis'] === 'function') {
                        return !!window['shouldUseDesktopRustAnalysis']();
                    }
                    if (opener && typeof opener.shouldUseDesktopRustAnalysis === 'function') {
                        return !!opener.shouldUseDesktopRustAnalysis();
                    }
                        return true;
                } catch (_) {
                    return false;
                }
            })();
            const canUseDesktopRust = shouldUseDesktopRust && !!(
                opener
                && typeof opener.runDesktopNativeSphericalAberrationForPopup === 'function'
            );
            try {
                console.log('📊 [SA TS-Rust] mode', {
                    canUseDesktopRust,
                    hasNativeRunner: !!(opener && typeof opener.runDesktopNativeSphericalAberrationForPopup === 'function'),
                    referenceFocusMode,
                    rayCount,
                });
            } catch (_) {}

            const onProgress = (evt) => {
                try {
                    const p = Number(evt?.percent);
                    const msg = evt?.message || evt?.phase || 'Working...';
                    if (Number.isFinite(p)) setProgress(p, msg);
                    else setProgress(undefined, msg);
                } catch (_) {}
            };

            const containerEl = document.getElementById('popup-longitudinal-aberration-container');
            if (containerEl) containerEl.innerHTML = '';

            try {
                setProgress(0, 'Starting...');
                if (!opener || typeof opener.showLongitudinalAberrationDiagram !== 'function') {
                    throw new Error('showLongitudinalAberrationDiagram is not available on opener');
                }

                if (canUseDesktopRust) {
                    setProgress(25, 'Computing spherical aberration (Rust)...');
                    const rustResult = await opener.runDesktopNativeSphericalAberrationForPopup({
                        rayCount: Number.isFinite(rayCount) ? rayCount : 51,
                        referenceFocusMode: referenceFocusMode,
                        wavelengthMode: 'all',
                    });
                    setProgress(80, 'Rendering...');
                    await opener.showLongitudinalAberrationDiagram({
                        containerElement: containerEl,
                        onProgress,
                        precomputedAberrationData: rustResult,
                    });
                } else {
                    // Web fallback: run the existing JS/WASM spherical aberration path.
                    setProgress(25, 'Computing spherical aberration (Web)...');
                    await opener.showLongitudinalAberrationDiagram({
                        containerElement: containerEl,
                        onProgress,
                        rayCount: Number.isFinite(rayCount) ? rayCount : 51,
                        referenceFocusMode,
                    });
                }

                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
                return;
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate spherical aberration diagram. Check console.</div>';
                }
            }
        };

        document.getElementById('popup-show-spherical-aberration-btn').addEventListener('click', () => {
            window.renderSphericalAberration();
        });

        window.addEventListener('focus', syncFromOpener);
        syncFromOpener();

        // Auto-render immediately on open
        window.addEventListener('load', () => {
            try { window.renderSphericalAberration(); } catch (_) {}
        });
    <\/script>
</body>
</html>
                        `);try{S.document.close()}catch{}});const a=document.getElementById("open-astigmatism-window-btn");a&&a.addEventListener("click",()=>{if(T.__astigmatismPopup&&!T.__astigmatismPopup.closed){try{T.__astigmatismPopup.focus()}catch{}try{typeof T.__astigmatismPopup.renderAstigmatism=="function"&&T.__astigmatismPopup.renderAstigmatism()}catch{}return}const S=n("Astigmatism","width=800,height=600");if(!S){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__astigmatismPopup=S,S.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Astigmatism</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
        }
        #popup-astigmatic-field-curves-container { height: 100%; min-height: 100%; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-astigmatism-chief-ray" style="font-size:12px;color:#333;white-space:nowrap;">Chief ray:</label>
        <select id="popup-astigmatism-chief-ray" style="padding:5px 8px;font-size:12px;border:1px solid #bbb;border-radius:4px;background:white;">
            <option value="stop-center">Stop center</option>
            <option value="beam-midpoint">Beam midpoint</option>
            <option value="beam-centroid">Beam centroid</option>
        </select>
        <label for="popup-astigmatism-beam" style="font-size:12px;color:#333;white-space:nowrap;">Beam:</label>
        <select id="popup-astigmatism-beam" style="padding:5px 8px;font-size:12px;border:1px solid #bbb;border-radius:4px;background:white;">
            <option value="cross">Cross</option>
            <option value="grid">Grid</option>
            <option value="annular" selected>Annular</option>
        </select>
        <label for="popup-astigmatism-ray-count" style="font-size:12px;color:#333;white-space:nowrap;">Rays:</label>
        <input id="popup-astigmatism-ray-count" type="number" min="9" max="2001" step="1" value="30" style="width:72px;padding:5px 8px;font-size:12px;border:1px solid #bbb;border-radius:4px;background:white;" />
        <label id="popup-astigmatism-ring-label" for="popup-astigmatism-ring-count" style="font-size:12px;color:#333;white-space:nowrap;">Rings:</label>
        <input id="popup-astigmatism-ring-count" type="number" min="1" max="64" step="1" value="32" style="width:72px;padding:5px 8px;font-size:12px;border:1px solid #bbb;border-radius:4px;background:white;" />
        <button id="popup-show-astigmatism-btn" type="button">Show</button>
    </div>
    <div id="popup-astigmatism-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-astigmatism-progress-text" style="margin-bottom: 6px;">Calculating...</div>
        <progress id="popup-astigmatism-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-astigmatic-field-curves-container"></div>
    </div>

    <script>
        window['renderAstigmatism'] = async () => {
            const containerEl = document.getElementById('popup-astigmatic-field-curves-container');
            if (containerEl) containerEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-astigmatism-progress-wrapper');
            const progressBarEl = document.getElementById('popup-astigmatism-progressbar');
            const progressTextEl = document.getElementById('popup-astigmatism-progress-text');
            const chiefRaySelect = document.getElementById('popup-astigmatism-chief-ray');
            const beamSelect = document.getElementById('popup-astigmatism-beam');
            const rayCountInput = document.getElementById('popup-astigmatism-ray-count');
            const ringCountInput = document.getElementById('popup-astigmatism-ring-count');
            const ringCountLabel = document.getElementById('popup-astigmatism-ring-label');
            const chiefRayDefinition = (chiefRaySelect && chiefRaySelect.value) ? chiefRaySelect.value : 'stop-center';
            const beamPattern = (() => {
                const v = String((beamSelect && beamSelect.value) ? beamSelect.value : 'annular').trim().toLowerCase();
                return (v === 'cross' || v === 'grid' || v === 'annular') ? v : 'annular';
            })();
            const openerRay = (window.opener && window.opener.document)
                ? window.opener.document.getElementById('ray-count-input')
                : null;
            const openerRing = (window.opener && window.opener.document)
                ? window.opener.document.getElementById('ring-count-select')
                : null;
            const initialRayCount = (() => {
                const n = Number(openerRay && openerRay.value);
                return Number.isFinite(n) ? Math.max(9, Math.min(2001, Math.round(n))) : 30;
            })();
            const initialRingCount = (() => {
                const n = Number(openerRing && openerRing.value);
                return Number.isFinite(n) ? Math.max(1, Math.min(64, Math.round(n))) : 32;
            })();
            if (rayCountInput && !rayCountInput.value) rayCountInput.value = String(initialRayCount);
            if (ringCountInput && !ringCountInput.value) ringCountInput.value = String(initialRingCount);

            const updateRingVisibility = () => {
                const isAnnular = beamSelect && String(beamSelect.value || '').toLowerCase() === 'annular';
                if (ringCountLabel) ringCountLabel.style.display = isAnnular ? '' : 'none';
                if (ringCountInput) ringCountInput.style.display = isAnnular ? '' : 'none';
            };
            updateRingVisibility();
            if (beamSelect) beamSelect.addEventListener('change', updateRingVisibility);

            const rayCount = (() => {
                const fromInput = Number(rayCountInput && rayCountInput.value);
                if (Number.isFinite(fromInput)) return Math.max(9, Math.min(2001, Math.round(fromInput)));
                return initialRayCount;
            })();
            const ringCount = (() => {
                const fromInput = Number(ringCountInput && ringCountInput.value);
                if (Number.isFinite(fromInput)) return Math.max(1, Math.min(64, Math.round(fromInput)));
                return initialRingCount;
            })();

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            try {
                if (!window.opener || typeof window.opener.showAstigmatismDiagram !== 'function') {
                    throw new Error('showAstigmatismDiagram is not available on opener');
                }
                setProgress(0, 'Starting...');
                const onProgress = (evt) => {
                    try {
                        try { console.log('[ASTIG_POPUP][progress]', evt); } catch (_) {}
                        const p = Number(evt?.percent);
                        const msg = evt?.message || evt?.phase || 'Working...';
                        if (Number.isFinite(p)) setProgress(p, msg);
                        else setProgress(undefined, msg);
                    } catch (_) {}
                };
                await window.opener.showAstigmatismDiagram({
                    containerElement: containerEl,
                    rayCount,
                    ringCount,
                    pattern: beamPattern,
                    requireRustWasm: true,
                    forceRustWasmTrace: true,
                    requireRustWasmTrace: true,
                    onProgress,
                    chiefRayDefinition,
                    logChiefRayDefinition: true,
                    useActiveConfigSnapshot: false
                });
                try { console.log('[ASTIG_POPUP][done] showAstigmatismDiagram resolved'); } catch (_) {}
                setProgress(100, '');
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate astigmatism diagram. Check console.</div>';
                }
            }
        };

        document.getElementById('popup-show-astigmatism-btn').addEventListener('click', () => {
            window.renderAstigmatism();
        });

        const popupBeamSelect = document.getElementById('popup-astigmatism-beam');
        const popupRingLabel = document.getElementById('popup-astigmatism-ring-label');
        const popupRingInput = document.getElementById('popup-astigmatism-ring-count');
        const syncAstigRingVisibility = () => {
            const isAnnular = popupBeamSelect && String(popupBeamSelect.value || '').toLowerCase() === 'annular';
            if (popupRingLabel) popupRingLabel.style.display = isAnnular ? '' : 'none';
            if (popupRingInput) popupRingInput.style.display = isAnnular ? '' : 'none';
        };
        syncAstigRingVisibility();
        if (popupBeamSelect) popupBeamSelect.addEventListener('change', syncAstigRingVisibility);

        // Do not auto-render on open; user triggers calculation via "Show".
    <\/script>
</body>
</html>
                        `);try{S.document.close()}catch{}});const c=document.getElementById("open-distortion-window-btn");c&&c.addEventListener("click",()=>{if(!ze()){if(T.__distortionPopup&&!T.__distortionPopup.closed){try{T.__distortionPopup.focus()}catch{}return}try{const N=new URL(window.location.href);N.searchParams.set("coopt_analysis_window","1"),N.searchParams.set("coopt_analysis","distortion");const f=window.open(N.toString(),"Distortion","width=800,height=600");if(!f){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__distortionPopup=f;return}catch{}}if(T.__distortionPopup&&!T.__distortionPopup.closed){try{T.__distortionPopup.focus()}catch{}try{typeof T.__distortionPopup.renderDistortion=="function"&&T.__distortionPopup.renderDistortion()}catch{}return}const S=n("Distortion","width=800,height=600");if(!S){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__distortionPopup=S,S.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Distortion</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: block;
        }
        .plot-area { width: 100%; height: 100%; min-height: 0; }
        #popup-distortion-percent { height: 100%; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <button id="popup-show-distortion-btn" type="button">Show distortion diagram</button>
    </div>
    <div id="popup-distortion-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-distortion-progress-text" style="margin-bottom: 6px;">Calculating distortion...</div>
        <progress id="popup-distortion-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-distortion-percent-area" class="plot-area"><div id="popup-distortion-percent"></div></div>
    </div>

    <script>
        function getWavelengthColorLocal(wavelength) {
            if (wavelength < 0.45) return '#8B00FF';
            if (wavelength < 0.495) return '#0000FF';
            if (wavelength < 0.57) return '#00FF00';
            if (wavelength < 0.59) return '#9ACD32';
            if (wavelength < 0.62) return '#FF8800';
            return '#FF0000';
        }

        function plotDistortionLocal(dataList, targetEl) {
            if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                throw new Error('Plotly is not available in distortion popup');
            }
            const list = Array.isArray(dataList) ? dataList.filter(Boolean) : [];
            if (!list.length) throw new Error('No distortion data to plot');

            const traces = list.map((data) => {
                const wavelength = Number(data?.meta?.wavelength || 0.5876);
                const wavelengthNm = (wavelength * 1000).toFixed(1);
                const color = getWavelengthColorLocal(wavelength);
                const isHeightMode = !!data?.meta?.heightMode;
                const label = isHeightMode ? 'h' : 'θ';
                return {
                    x: Array.isArray(data?.distortionPercent) ? data.distortionPercent : [],
                    y: Array.isArray(data?.fieldValues) ? data.fieldValues : [],
                    name: 'DIST ' + wavelengthNm + 'nm (' + label + ')',
                    mode: 'lines',
                    line: { color, width: 2 },
                    type: 'scatter',
                };
            }).filter((trace) => Array.isArray(trace.x) && trace.x.length > 0 && Array.isArray(trace.y) && trace.y.length > 0);

            if (!traces.length) throw new Error('No plottable distortion traces');

            const maxFieldValue = Math.max(...list.map((data) => Array.isArray(data?.fieldValues) ? Math.max(...data.fieldValues) : 0));
            const minFieldValue = Math.min(...list.map((data) => Array.isArray(data?.fieldValues) ? Math.min(...data.fieldValues) : 0));
            const heightMode = list.some((d) => !!d?.meta?.heightMode);

            return window.Plotly.newPlot(targetEl, traces, {
                title: heightMode ? 'Distortion vs Object Height' : 'Distortion vs Object Angle',
                xaxis: { title: 'Distortion (%)', range: [-5, 5], dtick: 1 },
                yaxis: { title: heightMode ? 'Object Height (mm)' : 'Object Angle θ (deg)' },
                showlegend: true,
                legend: { orientation: 'v', x: 1.02, y: 1 },
                autosize: true,
                shapes: [{ type: 'line', x0: 0, x1: 0, y0: minFieldValue, y1: maxFieldValue, line: { color: 'black', width: 1, dash: 'dot' } }],
                margin: { l: 60, r: 120, t: 50, b: 50 },
            }, { responsive: true, displayModeBar: true, displaylogo: false });
        }

        function resizePlots() {
            try {
                const plotly = window.Plotly;
                if (!plotly || !plotly.Plots) return;
                const a = document.getElementById('popup-distortion-percent');
                if (a) plotly.Plots.resize(a);
            } catch (_) {}
        }

        window['renderDistortion'] = async () => {
            const percentEl = document.getElementById('popup-distortion-percent');
            if (percentEl) percentEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-distortion-progress-wrapper');
            const progressBarEl = document.getElementById('popup-distortion-progressbar');
            const progressTextEl = document.getElementById('popup-distortion-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };
            let renderSucceeded = false;

            try {
                if (!window.opener || typeof window.opener.runPortableDistortionDataForPopup !== 'function') {
                    throw new Error('runPortableDistortionDataForPopup is not available on opener');
                }
                setProgress(0, 'Starting...');
                await new Promise((resolve) => setTimeout(resolve, 0));
                const onProgress = (evt) => {
                    try {
                        const p = Number(evt?.percent);
                        const msg = evt?.message || evt?.phase || 'Working...';
                        if (Number.isFinite(p)) setProgress(p, msg);
                        else setProgress(undefined, msg);
                    } catch (_) {}
                };
                const result = await window.opener.runPortableDistortionDataForPopup({ onProgress });
                const allData = Array.isArray(result?.allData) ? result.allData : [];
                await plotDistortionLocal(allData, percentEl);
                renderSucceeded = true;
                setTimeout(resizePlots, 0);
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (percentEl) {
                    percentEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate distortion diagram. Check console.</div>';
                }
            } finally {
                if (renderSucceeded && progressWrapper) {
                    setTimeout(() => {
                        try { progressWrapper.style.display = 'none'; } catch (_) {}
                    }, 250);
                }
            }
        };

        document.getElementById('popup-show-distortion-btn').addEventListener('click', () => window.renderDistortion());
        window.addEventListener('resize', resizePlots);

        // Auto-render immediately on open
        window.addEventListener('load', () => {
            try { window.renderDistortion(); } catch (_) {}
        });
    <\/script>
</body>
</html>
                        `);try{S.document.close()}catch{}});const i=document.getElementById("open-distortion-grid-window-btn");i&&i.addEventListener("click",()=>{if(!ze()){if(T.__distortionGridPopup&&!T.__distortionGridPopup.closed){try{T.__distortionGridPopup.focus()}catch{}return}try{const N=new URL(window.location.href);N.searchParams.set("coopt_analysis_window","1"),N.searchParams.set("coopt_analysis","distortion-grid");const f=window.open(N.toString(),"Distortion Grid","width=800,height=600");if(!f){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__distortionGridPopup=f;return}catch{}}if(T.__distortionGridPopup&&!T.__distortionGridPopup.closed){try{T.__distortionGridPopup.focus()}catch{}return}const S=n("Distortion Grid","width=800,height=600");if(!S){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__distortionGridPopup=S,S.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Distortion Grid</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
        }
        #popup-distortion-grid { width: 100%; height: 100%; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-distortion-grid-size">Grid Size:</label>
        <select id="popup-distortion-grid-size">
            <option value="10">10×10</option>
            <option value="15">15×15</option>
            <option value="20" selected>20×20</option>
            <option value="25">25×25</option>
            <option value="30">30×30</option>
            <option value="35">35×35</option>
            <option value="40">40×40</option>
            <option value="45">45×45</option>
            <option value="50">50×50</option>
        </select>
        <button id="popup-show-distortion-grid-btn" type="button">Show distortion grid</button>
    </div>
    <div id="popup-distortion-grid-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-distortion-grid-progress-text" style="margin-bottom: 6px;">Calculating grid distortion...</div>
        <progress id="popup-distortion-grid-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-distortion-grid"></div>
    </div>

    <script>
        function getWavelengthColorLocal(wavelength) {
            if (wavelength < 0.45) return '#8B00FF';
            if (wavelength < 0.495) return '#0000FF';
            if (wavelength < 0.57) return '#00FF00';
            if (wavelength < 0.59) return '#9ACD32';
            if (wavelength < 0.62) return '#FF8800';
            return '#FF0000';
        }

        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (_) {
                return null;
            }
        }

        function syncFromOpener() {
            const openerGrid = getOpenerEl('grid-size-select');
            const popupGrid = document.getElementById('popup-distortion-grid-size');
            if (openerGrid && popupGrid) {
                popupGrid.value = openerGrid.value;
            }
        }

        function resizePlot() {
            try {
                const plotly = window.Plotly;
                if (!plotly || !plotly.Plots) return;
                const grid = document.getElementById('popup-distortion-grid');
                if (grid) plotly.Plots.resize(grid);
            } catch (_) {}
        }

        async function plotGridLocal(data, targetEl, onProgress) {
            if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                throw new Error('Plotly is not available in distortion grid popup');
            }
            if (!data || !data.idealGrid || !data.realGrid) {
                throw new Error('Invalid grid distortion data');
            }

            const progress = (typeof onProgress === 'function') ? onProgress : null;
            const reportProgress = (percent, message) => {
                try { progress && progress({ percent, message }); } catch (_) {}
            };

            const idealGrid = data.idealGrid;
            const realGrid = data.realGrid;
            const gridSize = Number(data.gridSize) || 20;
            const meta = data.meta || {};
            const traces = [];

            for (let i = 0; i < gridSize; i++) {
                const startIdx = i * gridSize;
                const endIdx = startIdx + gridSize - 1;
                traces.push({
                    x: idealGrid.x.slice(startIdx, endIdx + 1),
                    y: idealGrid.y.slice(startIdx, endIdx + 1),
                    mode: 'lines',
                    line: { color: '#888888', width: 1 },
                    showlegend: i === 0,
                    name: i === 0 ? 'Ideal Grid' : undefined,
                    hoverinfo: 'skip',
                    type: 'scatter',
                });
                if ((i + 1) % 10 === 0) await new Promise((resolve) => setTimeout(resolve, 0));
            }

            for (let j = 0; j < gridSize; j++) {
                const xLine = [];
                const yLine = [];
                for (let i = 0; i < gridSize; i++) {
                    const idx = i * gridSize + j;
                    xLine.push(idealGrid.x[idx]);
                    yLine.push(idealGrid.y[idx]);
                }
                traces.push({
                    x: xLine,
                    y: yLine,
                    mode: 'lines',
                    line: { color: '#888888', width: 1 },
                    showlegend: false,
                    hoverinfo: 'skip',
                    type: 'scatter',
                });
                if ((j + 1) % 10 === 0) await new Promise((resolve) => setTimeout(resolve, 0));
            }

            const realX = [];
            const realY = [];
            const totalPoints = Math.max(1, Array.isArray(realGrid.x) ? realGrid.x.length : 0);
            for (let i = 0; i < totalPoints; i++) {
                const x = realGrid.x[i];
                const y = realGrid.y[i];
                const idealX = idealGrid.x[i];
                const idealY = idealGrid.y[i];
                if (x !== null && y !== null && x !== undefined && y !== undefined && isFinite(x) && isFinite(y) && isFinite(idealX) && isFinite(idealY)) {
                    realX.push(x);
                    realY.push(y);
                }
                reportProgress(((i + 1) / totalPoints) * 100, 'Grid distortion: ' + (i + 1) + '/' + totalPoints);
                if ((i + 1) % 50 === 0) await new Promise((resolve) => setTimeout(resolve, 0));
            }

            traces.push({
                x: realX,
                y: realY,
                mode: 'markers',
                marker: {
                    color: getWavelengthColorLocal(Number(meta.wavelength || 0.5876)),
                    size: 4,
                    symbol: 'circle',
                    opacity: 0.8,
                },
                name: 'Real Positions (λ=' + Number(meta.wavelength || 0.5876).toFixed(4) + ' μm)',
                hovertemplate: 'Real: (%{x:.3f}, %{y:.3f}) mm<extra></extra>',
                type: 'scatter',
            });

            const maxAbsIdealX = idealGrid.x.reduce((m, v) => (isFinite(v) ? Math.max(m, Math.abs(v)) : m), 0);
            const maxAbsIdealY = idealGrid.y.reduce((m, v) => (isFinite(v) ? Math.max(m, Math.abs(v)) : m), 0);
            const equalRangeHalf = Math.max(maxAbsIdealX, maxAbsIdealY, 1e-9);

            return window.Plotly.newPlot(targetEl, traces, {
                title: 'Grid Distortion (' + gridSize + '×' + gridSize + ', λ=' + Number(meta.wavelength || 0.5876).toFixed(4) + ' μm)',
                xaxis: { title: 'Image Height X (mm)', scaleanchor: 'y', scaleratio: 1, zeroline: false, range: [-equalRangeHalf, equalRangeHalf] },
                yaxis: { title: 'Image Height Y (mm)', zeroline: false, range: [-equalRangeHalf, equalRangeHalf] },
                hovermode: 'closest',
                showlegend: true,
                legend: { x: 1.02, y: 1 },
                autosize: true,
                margin: { l: 60, r: 120, t: 50, b: 50 },
            }, { responsive: true, displayModeBar: true, displaylogo: false });
        }

        window['renderDistortionGrid'] = async () => {
            if (window.__distortionGridRenderInFlight) return;
            window.__distortionGridRenderInFlight = true;
            const gridEl = document.getElementById('popup-distortion-grid');
            const gridBtn = document.getElementById('popup-show-distortion-grid-btn');
            if (gridBtn) gridBtn.disabled = true;
            if (gridEl) gridEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-distortion-grid-progress-wrapper');
            const progressBarEl = document.getElementById('popup-distortion-grid-progressbar');
            const progressTextEl = document.getElementById('popup-distortion-grid-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const gridSizeEl = document.getElementById('popup-distortion-grid-size');
            const gridSize = gridSizeEl ? parseInt(gridSizeEl.value, 10) : 20;
            const openerGrid = getOpenerEl('grid-size-select');
            if (openerGrid && Number.isFinite(gridSize)) openerGrid.value = String(gridSize);
            let renderSucceeded = false;

            try {
                if (!window.opener || typeof window.opener.runPortableGridDistortionForPopup !== 'function') {
                    throw new Error('runPortableGridDistortionForPopup is not available on opener');
                }
                setProgress(0, 'Starting...');
                await new Promise((resolve) => setTimeout(resolve, 0));
                const onProgress = (evt) => {
                    try {
                        const p = Number(evt?.percent);
                        const msg = evt?.message || evt?.phase || 'Working...';
                        if (Number.isFinite(p)) setProgress(p, msg);
                        else setProgress(undefined, msg);
                    } catch (_) {}
                };
                const data = await window.opener.runPortableGridDistortionForPopup({ gridSize: Number.isFinite(gridSize) ? gridSize : 20, onProgress });
                await plotGridLocal(data, gridEl, onProgress);
                renderSucceeded = true;
                setTimeout(resizePlot, 0);
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (gridEl) {
                    gridEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate grid distortion. Check console.</div>';
                }
            } finally {
                window.__distortionGridRenderInFlight = false;
                if (gridBtn) gridBtn.disabled = false;
                if (renderSucceeded && progressWrapper) {
                    setTimeout(() => {
                        try { progressWrapper.style.display = 'none'; } catch (_) {}
                    }, 250);
                }
            }
        };

        document.getElementById('popup-show-distortion-grid-btn').addEventListener('click', () => window.renderDistortionGrid());
        window.addEventListener('resize', resizePlot);
        window.addEventListener('focus', syncFromOpener);
        syncFromOpener();
    <\/script>
</body>
</html>
                        `);try{S.document.close()}catch{}});const l=document.getElementById("open-magnification-chromatic-aberration-window-btn");l&&l.addEventListener("click",()=>{if(T.__magnificationChromaticAberrationPopup&&!T.__magnificationChromaticAberrationPopup.closed){try{T.__magnificationChromaticAberrationPopup.focus()}catch{}try{typeof T.__magnificationChromaticAberrationPopup.renderMagnificationChromaticAberration=="function"&&T.__magnificationChromaticAberrationPopup.renderMagnificationChromaticAberration()}catch{}return}const S=n("Lateral Chromatic Aberration","width=800,height=600");if(!S){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__magnificationChromaticAberrationPopup=S,S.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Lateral Chromatic Aberration</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            width: 90px;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-mca-container { flex: 1 1 auto; min-height: 0; }
        .note { padding: 6px 12px; font-size: 12px; color: #666; background: #fff; border-bottom: 1px solid #eee; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-mca-xmin">Lateral displacement:</label>
        <input type="number" id="popup-mca-xmin" value="-0.05" step="0.01" />
        <span style="font-size:12px;color:#333;">to</span>
        <input type="number" id="popup-mca-xmax" value="0.05" step="0.01" />
        <span style="font-size:12px;color:#666;">(mm)</span>
        <label for="popup-mca-points" style="margin-left:6px;">Points:</label>
        <input type="number" id="popup-mca-points" value="21" min="2" max="201" step="1" />
        <label for="popup-mca-chief-ray" style="margin-left:6px;">Chief ray:</label>
        <select id="popup-mca-chief-ray" style="padding:5px 8px;font-size:12px;border:1px solid #bbb;border-radius:4px;background:white;">
            <option value="stop-center">Stop center</option>
            <option value="beam-midpoint">Beam midpoint</option>
            <option value="beam-centroid">Beam centroid</option>
        </select>
        <button id="popup-show-mca-btn" type="button">Show lateral chromatic aberration</button>
    </div>
    <div id="popup-mca-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-mca-progress-text" style="margin-bottom: 6px;">Calculating lateral chromatic aberration...</div>
        <progress id="popup-mca-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-mca-container"></div>
    </div>

    <script>
        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (_) {
                return null;
            }
        }

        function syncFromOpener() {
            const openerMin = getOpenerEl('mca-xmin-input');
            const openerMax = getOpenerEl('mca-xmax-input');
            const openerPoints = getOpenerEl('mca-point-count-input');
            const popupMin = document.getElementById('popup-mca-xmin');
            const popupMax = document.getElementById('popup-mca-xmax');
            const popupPoints = document.getElementById('popup-mca-points');
            if (openerMin && popupMin && openerMin.value !== '') popupMin.value = openerMin.value;
            if (openerMax && popupMax && openerMax.value !== '') popupMax.value = openerMax.value;
            if (openerPoints && popupPoints && openerPoints.value !== '') popupPoints.value = openerPoints.value;
        }

        window['renderMagnificationChromaticAberration'] = async () => {
            const containerEl = document.getElementById('popup-mca-container');
            if (containerEl) containerEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-mca-progress-wrapper');
            const progressBarEl = document.getElementById('popup-mca-progressbar');
            const progressTextEl = document.getElementById('popup-mca-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            const xMinEl = document.getElementById('popup-mca-xmin');
            const xMaxEl = document.getElementById('popup-mca-xmax');
            const pointEl = document.getElementById('popup-mca-points');
            const chiefRayEl = document.getElementById('popup-mca-chief-ray');
            const xMin = xMinEl ? parseFloat(xMinEl.value) : -0.5;
            const xMax = xMaxEl ? parseFloat(xMaxEl.value) : 0.5;
            const pointCount = pointEl ? parseInt(pointEl.value, 10) : 11;
            const chiefRayDefinition = (chiefRayEl && chiefRayEl.value) ? chiefRayEl.value : 'stop-center';

            try {
                if (!window.opener || typeof window.opener.showMagnificationChromaticAberrationDiagram !== 'function') {
                    throw new Error('showMagnificationChromaticAberrationDiagram is not available on opener');
                }
                setProgress(0, 'Starting...');
                const onProgress = (evt) => {
                    try {
                        const p = Number(evt?.percent);
                        const msg = evt?.message || evt?.phase || 'Working...';
                        if (Number.isFinite(p)) setProgress(p, msg);
                        else setProgress(undefined, msg);
                    } catch (_) {}
                };
                await window.opener.showMagnificationChromaticAberrationDiagram({
                    containerElement: containerEl,
                    xMin,
                    xMax,
                    pointCount,
                    chiefRayDefinition,
                    onProgress
                });
                hideProgress();
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate magnification chromatic aberration. Check console.</div>';
                }
            }
        };

        document.getElementById('popup-show-mca-btn').addEventListener('click', () => window.renderMagnificationChromaticAberration());
        window.addEventListener('focus', syncFromOpener);
        window.addEventListener('load', () => {
            syncFromOpener();
            try { window.renderMagnificationChromaticAberration(); } catch (_) {}
        });
    <\/script>
</body>
</html>
                        `);try{S.document.close()}catch{}});const m=document.getElementById("open-integrated-aberration-window-btn");m&&m.addEventListener("click",()=>{if(T.__integratedAberrationPopup&&!T.__integratedAberrationPopup.closed){try{T.__integratedAberrationPopup.focus()}catch{}try{typeof T.__integratedAberrationPopup.renderIntegratedAberration=="function"&&T.__integratedAberrationPopup.renderIntegratedAberration()}catch{}return}const S=n("Integrated Aberration","width=800,height=600");if(!S){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__integratedAberrationPopup=S,S.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Integrated Aberration</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
        }
        #popup-integrated-aberration-container { height: 100%; min-height: 100%; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div id="popup-integrated-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <progress id="popup-integrated-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-integrated-aberration-container"></div>
    </div>

    <script>
        function resizePlot() {
            try {
                const plotly = window.Plotly;
                if (!plotly || !plotly.Plots) return;
                const el = document.getElementById('popup-integrated-aberration-container');
                if (el) plotly.Plots.resize(el);
            } catch (_) {}
        }

        window['renderIntegratedAberration'] = async () => {
            const containerEl = document.getElementById('popup-integrated-aberration-container');
            if (containerEl) containerEl.innerHTML = '';
            resizePlot();

            const progressWrapper = document.getElementById('popup-integrated-progress-wrapper');
            const progressBarEl = document.getElementById('popup-integrated-progressbar');
            const progressTextEl = document.getElementById('popup-integrated-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressBarEl && Number.isFinite(value)) progressBarEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            try {
                if (!window.opener || typeof window.opener.showIntegratedAberrationDiagram !== 'function') {
                    throw new Error('showIntegratedAberrationDiagram is not available on opener');
                }
                setProgress(0, 'Starting...');
                const onProgress = (evt) => {
                    try {
                        const p = Number(evt?.percent);
                        const msg = evt?.message || evt?.phase || 'Working...';
                        if (Number.isFinite(p)) setProgress(p, msg);
                        else setProgress(undefined, msg);
                    } catch (_) {}
                };
                await window.opener.showIntegratedAberrationDiagram({
                    containerElement: containerEl,
                    onProgress,
                    useActiveConfigSnapshot: true
                });
                hideProgress();
                resizePlot();
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate integrated aberration diagram. Check console.</div>';
                }
            }
        };

        window.addEventListener('resize', resizePlot);

        // Auto-render immediately on open
        window.addEventListener('load', () => {
            try { window.renderIntegratedAberration(); } catch (_) {}
        });
    <\/script>
</body>
</html>
                        `);try{S.document.close()}catch{}});const p=document.getElementById("open-opd-window-btn");p&&p.addEventListener("click",()=>{if(T.__opdPopup&&!T.__opdPopup.closed){try{T.__opdPopup.close()}catch{}T.__opdPopup=null}const S=n("Optical Path Difference","width=800,height=600");if(!S){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__opdPopup=S;try{S.document.open()}catch{}S.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Optical Path Difference</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-wavefront-container { flex: 1 1 auto; min-height: 0; }
        #popup-wavefront-container-stats { flex: 0 0 auto; padding: 8px 12px; font-size: 12px; color: #333; border-top: 1px solid #eee; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-wavefront-object-select">Object:</label>
        <select id="popup-wavefront-object-select"></select>
        <label for="popup-wavefront-plot-type-select">Plot type:</label>
        <select id="popup-wavefront-plot-type-select">
            <option value="surface">3D Surface</option>
            <option value="heatmap">Heatmap</option>
            <option value="multifield">Multi-field Comparison</option>
        </select>
        <label for="popup-wavefront-grid-size-select">Grid size:</label>
        <select id="popup-wavefront-grid-size-select">
            <option value="32">32x32</option>
            <option value="64">64x64</option>
            <option value="128">128x128</option>
            <option value="256" selected>256x256</option>
            <option value="512">512x512</option>
            <option value="1024">1024x1024</option>
            <option value="2048">2048x2048</option>
            <option value="4096">4096x4096</option>
        </select>
        <label style="display:flex;align-items:center;gap:6px;">
            <input id="popup-zernike-fit-checkbox" type="checkbox" />
            Zernike (calc)
        </label>
        <label style="display:flex;align-items:center;gap:6px;">
            <input id="popup-opd-remove-ptd-checkbox" type="checkbox" />
            Remove P/T/D
        </label>
        <button id="popup-show-wavefront-btn" type="button">Show wavefront diagram</button>
        <button id="popup-stop-opd-btn" type="button" disabled>Stop</button>
    </div>
    <div id="popup-opd-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-opd-progress-text" style="margin-bottom: 6px;">Calculating OPD...</div>
        <progress id="popup-opd-progressbar" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-wavefront-container"></div>
        <div id="popup-wavefront-container-stats"></div>
    </div>

    <script>
        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (_) {
                return null;
            }
        }

        function syncObjectOptionsFromOpener() {
            const openerSelect = getOpenerEl('wavefront-object-select');
            const popupSelect = document.getElementById('popup-wavefront-object-select');
            if (!popupSelect) return;

            const current = popupSelect.value;
            const nextOptions = [];

            // MTF-style: build from opener.getObjectRows() first.
            let opener = null;
            try { opener = window.opener || null; } catch (_) { opener = null; }
            let objects = [];
            if (opener && typeof opener.getObjectRows === 'function') {
                try { objects = opener.getObjectRows(opener.tableObject); } catch (_) { objects = []; }
            }
            if (Array.isArray(objects) && objects.length > 0) {
                const toFiniteNumber = (v) => {
                    const n = (typeof v === 'number') ? v : parseFloat(v);
                    return (Number.isFinite(n) ? n : NaN);
                };
                const pickNumber = (obj, keys, fallback) => {
                    for (let i = 0; i < keys.length; i++) {
                        const k = keys[i];
                        if (!k) continue;
                        const raw = obj ? obj[k] : undefined;
                        if (raw === undefined || raw === null || raw === '') continue;
                        const n = toFiniteNumber(raw);
                        if (Number.isFinite(n)) return n;
                    }
                    return fallback;
                };
                for (let i = 0; i < objects.length; i++) {
                    const obj = objects[i];
                    if (!obj) continue;
                    const typeRaw = String(obj.position ?? obj.object ?? obj.Object ?? obj.objectType ?? 'Point');
                    const x = (obj.x ?? obj.xHeightAngle ?? 0);
                    const y = (obj.y ?? obj.yHeightAngle ?? 0);
                    nextOptions.push({ value: String(i), label: (String(i + 1) + ': ' + typeRaw + ' (' + x + ', ' + y + ')') });
                }
            }

            // Fallback: clone opener select.
            if (nextOptions.length === 0 && openerSelect && openerSelect.options) {
                Array.from(openerSelect.options).forEach(opt => {
                    nextOptions.push({ value: String(opt.value), label: String(opt.textContent ?? '') });
                });
            }
            // Last fallback: placeholder + schedule a retry (opener tables may not be ready yet).
            if (nextOptions.length === 0) {
                nextOptions.push({ value: '0', label: '1' });
                setTimeout(() => {
                    try { syncObjectOptionsFromOpener(); } catch (_) {}
                }, 250);
            }

            popupSelect.innerHTML = '';
            for (const opt of nextOptions) {
                const o = document.createElement('option');
                o.value = opt.value;
                o.textContent = opt.label;
                popupSelect.appendChild(o);
            }

            if (current && Array.from(popupSelect.options).some(o => o.value === current)) {
                popupSelect.value = current;
            } else if (openerSelect && openerSelect.value && Array.from(popupSelect.options).some(o => o.value === openerSelect.value)) {
                popupSelect.value = openerSelect.value;
            } else {
                popupSelect.value = popupSelect.options[0]?.value ?? '0';
            }

            try {
                if (typeof window.__opdStage === 'function') {
                    window.__opdStage('Object options synchronized', 'count=' + popupSelect.options.length + ', selected=' + String(popupSelect.value ?? 'n/a'));
                }
            } catch (_) {}
        }

        function syncInputsFromOpener() {
            const openerPlotType = getOpenerEl('wavefront-plot-type-select');
            const openerGrid = getOpenerEl('wavefront-grid-size-select');
            const openerRemovePtd = getOpenerEl('opd-remove-ptd-checkbox');
            const popupPlotType = document.getElementById('popup-wavefront-plot-type-select');
            const popupGrid = document.getElementById('popup-wavefront-grid-size-select');
            const popupRemovePtd = document.getElementById('popup-opd-remove-ptd-checkbox');
            if (openerPlotType && popupPlotType) popupPlotType.value = openerPlotType.value;
            if (openerGrid && popupGrid) popupGrid.value = openerGrid.value;
            if (popupRemovePtd) popupRemovePtd.checked = !!(openerRemovePtd && openerRemovePtd.checked);
        }

        function resizePlot() {
            try {
                const plotly = window.Plotly;
                if (!plotly || !plotly.Plots) return;
                const el = document.getElementById('popup-wavefront-container');
                if (el) plotly.Plots.resize(el);
            } catch (_) {}
        }

        function createStageTraceHelpers() {
            const clear = (_label = 'Idle') => {};
            const push = (_stage, _detail = '', _level = 'info') => {};
            const pushWarn = (_stage, _detail = '') => {};

            return { clear, push, pushWarn };
        }

        const opdStageTrace = createStageTraceHelpers();
        window.__opdStage = opdStageTrace.push;

        window['renderOPD'] = async () => {
            const containerEl = document.getElementById('popup-wavefront-container');
            if (containerEl) containerEl.innerHTML = '';

            try {
                opdStageTrace.clear('Preparing...');
                opdStageTrace.push('Render requested');
            } catch (_) {}

            const progressWrapper = document.getElementById('popup-opd-progress-wrapper');
            const progressBarEl = document.getElementById('popup-opd-progressbar');
            const progressTextEl = document.getElementById('popup-opd-progress-text');
            const PROGRESS_UI_UPDATE_INTERVAL_MS = 120;
            let lastProgressPercent = null;
            let lastProgressText = null;
            let progressVisible = false;
            let lastProgressUiUpdateAt = 0;
            let pendingProgressEvent = null;
            let progressFlushTimer = null;
            let lastComputingMilestone = 0;

            const setProgress = (value, text, force = false) => {
                try {
                    if (progressWrapper && !progressVisible) {
                        progressWrapper.style.display = 'block';
                        progressVisible = true;
                    }

                    if (progressBarEl && Number.isFinite(value)) {
                        const nextValue = Math.max(0, Math.min(100, value));
                        if (force || !Number.isFinite(lastProgressPercent) || Math.abs(nextValue - lastProgressPercent) >= 0.001) {
                            progressBarEl.value = nextValue;
                            lastProgressPercent = nextValue;
                        }
                    }

                    if (progressTextEl && typeof text === 'string') {
                        if (force || text !== lastProgressText) {
                            progressTextEl.textContent = text;
                            lastProgressText = text;
                        }
                    }
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                    progressVisible = false;
                } catch (_) {}
            };

            const flushPendingProgress = (force = false) => {
                if (progressFlushTimer) {
                    clearTimeout(progressFlushTimer);
                    progressFlushTimer = null;
                }
                const evt = pendingProgressEvent;
                if (!evt) return;
                pendingProgressEvent = null;
                const p = Number(evt?.percent);
                const msg = evt?.message || evt?.phase || 'Working...';
                if (Number.isFinite(p)) setProgress(p, msg, force);
                else setProgress(undefined, msg, force);
                lastProgressUiUpdateAt = Date.now();
            };

            const popupObject = document.getElementById('popup-wavefront-object-select');
            const popupPlotType = document.getElementById('popup-wavefront-plot-type-select');
            const popupGrid = document.getElementById('popup-wavefront-grid-size-select');
            const popupZernikeFit = document.getElementById('popup-zernike-fit-checkbox');
            const popupRemovePtd = document.getElementById('popup-opd-remove-ptd-checkbox');

            try {
                if (!popupObject) {
                    opdStageTrace.push('Object selector missing', 'popup-wavefront-object-select not found; fallback index=0');
                } else {
                    opdStageTrace.push('Object selector ready', 'options=' + String(popupObject.options?.length ?? 0));
                }
            } catch (_) {}

            const objectIndex = (() => {
                if (!popupObject) return 0;
                const v = parseInt(String(popupObject.value), 10);
                if (Number.isFinite(v)) return v;
                const idx = Number(popupObject.selectedIndex);
                return Number.isFinite(idx) && idx >= 0 ? idx : 0;
            })();
            const plotType = popupPlotType ? popupPlotType.value : 'surface';
            const gridSize = popupGrid ? parseInt(popupGrid.value, 10) : 256;
            const opdDisplayMode = (popupRemovePtd && popupRemovePtd.checked)
                ? 'pistonTiltDefocusRemoved'
                : 'pistonTiltRemoved';

            try {
                opdStageTrace.push('Input resolved', 'object=' + String(objectIndex) + ', plot=' + String(plotType) + ', grid=' + String(gridSize) + ', mode=' + String(opdDisplayMode));
            } catch (_) {}

            try {
                const optionCount = Number(popupObject?.options?.length ?? 0);
                const selectedText = popupObject?.selectedOptions?.[0]?.textContent || popupObject?.options?.[popupObject?.selectedIndex || 0]?.textContent || '';
                const inRange = optionCount <= 0 ? true : (objectIndex >= 0 && objectIndex < optionCount);
                if (!inRange) {
                    opdStageTrace.push('Object index out of range', 'index=' + String(objectIndex) + ', options=' + String(optionCount));
                } else {
                    opdStageTrace.push('Object resolved', 'index=' + String(objectIndex) + (selectedText ? (', label=' + String(selectedText)) : ''));
                }
            } catch (_) {}

            const openerObject = getOpenerEl('wavefront-object-select');
            const openerPlotType = getOpenerEl('wavefront-plot-type-select');
            const openerGrid = getOpenerEl('wavefront-grid-size-select');
            const openerRemovePtd = getOpenerEl('opd-remove-ptd-checkbox');
            if (openerObject && Number.isFinite(objectIndex)) openerObject.value = String(objectIndex);
            if (openerPlotType) openerPlotType.value = plotType;
            if (openerGrid && Number.isFinite(gridSize)) openerGrid.value = String(gridSize);
            if (openerRemovePtd) openerRemovePtd.checked = (opdDisplayMode === 'pistonTiltDefocusRemoved');

            try {
                const openerSyncOk = !!openerObject;
                if (openerSyncOk) {
                    opdStageTrace.push('Opener sync', 'object=' + String(objectIndex));
                } else {
                    opdStageTrace.pushWarn('Opener sync', 'opener object selector not found');
                }
            } catch (_) {}

            try {
                const computeInPopup = false;
                
                // Create cancel token (reuse PSF helper if available, or inline)
                const createCancelToken = window.opener.createCancelToken || (() => {
                    let aborted = false;
                    let reason = null;
                    const listeners = [];
                    return {
                        get aborted() { return aborted; },
                        get reason() { return reason; },
                        abort(r = 'User requested stop') {
                            if (aborted) return;
                            aborted = true;
                            reason = r;
                            listeners.forEach(fn => { try { fn(r); } catch (_) {} });
                        },
                        onAbort(fn) { listeners.push(fn); }
                    };
                });
                
                const popupCancelToken = createCancelToken();
                window['__popupOpdCancelToken'] = popupCancelToken;
                
                const stopBtn = document.getElementById('popup-stop-opd-btn');
                
                
                if (stopBtn) {
                    stopBtn.disabled = false;
                    stopBtn.textContent = 'Stop';
                }

                setProgress(0, 'Starting...');

                const opener = window.opener;
                const shouldUseDesktopRust = (() => {
                    try {
                        if (typeof window !== 'undefined' && typeof window['shouldUseDesktopRustAnalysis'] === 'function') {
                            return !!window['shouldUseDesktopRustAnalysis']();
                        }
                        if (opener && typeof opener.shouldUseDesktopRustAnalysis === 'function') {
                            return !!opener.shouldUseDesktopRustAnalysis();
                        }
                        return true;
                    } catch (_) {
                        return false;
                    }
                })();

                const canUseDesktopRust = shouldUseDesktopRust && !!(
                    opener
                    && typeof opener.showWavefrontDiagram === 'function'
                );
                const canUseWavefront = !!(
                    opener
                    && typeof opener.showWavefrontDiagram === 'function'
                );

                try {
                    opdStageTrace.push('Runtime check', 'desktopRust=' + String(!!canUseDesktopRust));
                } catch (_) {}

                if (!canUseWavefront) {
                    try { opdStageTrace.push('Blocked', 'Wavefront renderer unavailable on opener'); } catch (_) {}
                    throw new Error('OPD renderer is unavailable on opener window.');
                }

                try {
                    if (popupObject && popupObject.options && popupObject.options.length > 0) {
                        const optionCount = popupObject.options.length;
                        if (objectIndex < 0 || objectIndex >= optionCount) {
                            opdStageTrace.push('Blocked', 'Object index is invalid for current options');
                        }
                    }
                } catch (_) {}

                // NOTE: Wavefront generator supports only options.onProgress (same as PSF)
                const onProgress = (evt) => {
                    try {
                        pendingProgressEvent = evt;
                        const pct = Number(evt?.percent);
                        if (Number.isFinite(pct)) {
                            const msg = String(evt?.message || evt?.phase || 'working');
                            const milestones = [25, 50, 75, 100];
                            for (const milestone of milestones) {
                                if (milestone > lastComputingMilestone && pct >= (milestone - 0.001)) {
                                    opdStageTrace.push('Computing', msg + ' (' + String(milestone) + '%)');
                                    lastComputingMilestone = milestone;
                                }
                            }
                        }
                        const now = Date.now();
                        const elapsed = now - lastProgressUiUpdateAt;
                        if (elapsed >= PROGRESS_UI_UPDATE_INTERVAL_MS) {
                            flushPendingProgress(false);
                            return;
                        }

                        if (!progressFlushTimer) {
                            const delay = Math.max(0, PROGRESS_UI_UPDATE_INTERVAL_MS - elapsed);
                            progressFlushTimer = setTimeout(() => {
                                progressFlushTimer = null;
                                flushPendingProgress(false);
                            }, delay);
                        }
                    } catch (_) {}
                };
                
                try {
                    if (!window.opener || typeof window.opener.showWavefrontDiagram !== 'function') {
                        try { opdStageTrace.push('Blocked', 'showWavefrontDiagram is unavailable'); } catch (_) {}
                        throw new Error('showWavefrontDiagram is not available on opener');
                    }
                    try { opdStageTrace.push('Wavefront call started', 'Delegating to opener.showWavefrontDiagram'); } catch (_) {}
                    const wavefrontResult = await window.opener.showWavefrontDiagram(plotType, 'opd', Number.isFinite(gridSize) ? gridSize : 256, Number.isFinite(objectIndex) ? objectIndex : 0, {
                        containerElement: containerEl,
                        cancelToken: popupCancelToken,
                        onProgress,
                        opdDisplayMode,
                        forceRustWasm: true,
                        throwOnError: true,
                        showAlert: false
                    });
                    if (wavefrontResult?.error) {
                        throw new Error(String(wavefrontResult?.error?.message || wavefrontResult?.error || 'Wavefront plot failed'));
                    }
                    if (lastComputingMilestone < 100) {
                        try {
                            const completionMilestones = [25, 50, 75, 100];
                            for (const milestone of completionMilestones) {
                                if (milestone > lastComputingMilestone) {
                                    const detail = (milestone === 100)
                                        ? 'Plot data prepared (100%)'
                                        : ('Progress checkpoint (' + String(milestone) + '%)');
                                    opdStageTrace.push('Computing', detail);
                                    lastComputingMilestone = milestone;
                                }
                            }
                        } catch (_) {}
                    }
                    try { opdStageTrace.push('Wavefront call completed', 'Plot render returned'); } catch (_) {}

                    // Optional: Zernike fit + push report to System Data
                    const shouldZernikeFit = !!(popupZernikeFit && popupZernikeFit.checked);
                    if (shouldZernikeFit) {
                        try {
                            if (String(plotType) === 'multifield') {
                                try { opdStageTrace.push('Zernike skipped', 'Not supported for multi-field'); } catch (_) {}
                                setProgress(100, 'Zernike fit is not available for Multi-field');
                            } else {
                                try { opdStageTrace.push('Zernike started'); } catch (_) {}
                                setProgress(98, 'Zernike fitting...');

                                const opener = window.opener;
                                const map = opener ? getLastWavefrontMap(opener) : null;
                                const meta = opener ? getLastWavefrontMeta(opener) : null;
                                if (!map || map?.error) {
                                    throw new Error('No valid wavefrontMap to fit');
                                }

                                const coordsAll = Array.isArray(map?.pupilCoordinates) ? map.pupilCoordinates : [];
                                const opdsAll = Array.isArray(map?.raw?.opds) ? map.raw.opds : (Array.isArray(map?.opds) ? map.opds : []);
                                const n = Math.min(coordsAll.length, opdsAll.length);
                                if (!n) {
                                    throw new Error('No OPD samples found');
                                }

                                // Filter out invalid samples for fitting
                                const coords = [];
                                const opds = [];
                                for (let i = 0; i < n; i++) {
                                    const c = coordsAll[i];
                                    const v = opdsAll[i];
                                    const x = Number(c?.x);
                                    const y = Number(c?.y);
                                    const opd = Number(v);
                                    if (!Number.isFinite(x) || !Number.isFinite(y) || !Number.isFinite(opd)) continue;
                                    coords.push({ x, y });
                                    opds.push(opd);
                                }
                                if (coords.length < 5) {
                                    throw new Error('Not enough valid samples for Zernike fitting');
                                }

                                const wavelength = (() => {
                                    const fromMeta = Number(meta?.wavelength);
                                    if (Number.isFinite(fromMeta) && fromMeta > 0) return fromMeta;
                                    try {
                                        const w = Number(opener?.getPrimaryWavelength?.());
                                        if (Number.isFinite(w) && w > 0) return w;
                                    } catch (_) {}
                                    throw new Error('Primary wavelength is unavailable. Please set Source Primary Wavelength.');
                                })();

                                const opticalSystemRows = (typeof opener?.getOpticalSystemRows === 'function')
                                    ? opener.getOpticalSystemRows()
                                    : null;
                                const host = opener || window;
                                const calculatorFactory =
                                    (host && typeof host.createOPDCalculator === 'function' && host.createOPDCalculator)
                                    || (typeof window.createOPDCalculator === 'function' && window.createOPDCalculator)
                                    || (typeof createOPDCalculator === 'function' ? createOPDCalculator : null);
                                const analyzerFactory =
                                    (host && typeof host.createWavefrontAnalyzer === 'function' && host.createWavefrontAnalyzer)
                                    || (typeof window.createWavefrontAnalyzer === 'function' && window.createWavefrontAnalyzer)
                                    || (typeof createWavefrontAnalyzer === 'function' ? createWavefrontAnalyzer : null);
                                const calculator = calculatorFactory ? calculatorFactory(opticalSystemRows, wavelength) : null;
                                const analyzer = (calculator && analyzerFactory) ? analyzerFactory(calculator) : null;

                                if (!analyzer || typeof analyzer.fitZernikePolynomials !== 'function' || typeof analyzer.formatZernikeReportText !== 'function') {
                                    throw new Error('Wavefront analyzer is not available for Zernike fitting');
                                }

                                const maxNoll = Math.max(1, Math.min(37, opds.length));
                                const fit = analyzer.fitZernikePolynomials({ pupilCoordinates: coords, opds }, maxNoll);

                                // Store coefficients for the main window Zernike Fit button
                                try {
                                    patchLastWavefrontMap((current) => {
                                        current.zernike = fit;
                                        current.statistics = current.statistics || (map.statistics || {});
                                        current.statistics.skipZernikeFit = false;
                                    }, { host: opener, fallbackMap: map });
                                } catch (_) {}

                                // Build a lightweight report map so formatting can rely on aligned sample arrays
                                const reportMap = {
                                    ...map,
                                    pupilCoordinates: coords,
                                    raw: { ...(map.raw || {}), opds },
                                    opds,
                                    zernike: fit
                                };

                                const reportText = analyzer.formatZernikeReportText(reportMap, { maxNoll });

                                const pushSystemData = (text) => {
                                    try {
                                        const ta = opener?.document?.getElementById?.('system-data');
                                        if (!ta || typeof ta.value !== 'string') return false;
                                        // Replace (clear then push) so each run shows only the latest report.
                                        ta.value = String(text || '');
                                        return true;
                                    } catch (_) {
                                        return false;
                                    }
                                };

                                const tryOpenSystemDataWindow = () => {
                                    try {
                                        const w = opener?.__systemDataPopup;
                                        if (w && !w.closed) {
                                            try { w.focus(); } catch (_) {}
                                            return true;
                                        }
                                    } catch (_) {}

                                    // Some browsers block popups triggered by synthetic clicks.
                                    // We still try the main-window button first, but fall back to
                                    // opening the System Data window directly from this user action.
                                    try {
                                        const btn = opener?.document?.getElementById?.('open-system-data-window-btn');
                                        if (btn) {
                                            btn.click();
                                            // If the click worked, the opener should set __systemDataPopup.
                                            try {
                                                const w = opener?.__systemDataPopup;
                                                if (w && !w.closed) {
                                                    try { w.focus(); } catch (_) {}
                                                    return true;
                                                }
                                            } catch (_) {}
                                        }
                                    } catch (_) {}

                                    // Fallback: open and render a minimal System Data popup directly.
                                    try {
                                        const popup = opener?.open?.('', 'System Data', 'width=1200,height=600');
                                        if (!popup) return false;
                                        try { opener.__systemDataPopup = popup; } catch (_) {}
                                        try { popup.document.open(); } catch (_) {}

                                        const html = [
                                            '<!DOCTYPE html>',
                                            '<html>',
                                            '<head>',
                                            '  <meta charset="UTF-8" />',
                                            '  <title>System Data</title>',
                                            '  <style>',
                                            '    html, body { height: 100%; }',
                                            '    body { margin: 0; font-family: Arial, sans-serif; display: flex; flex-direction: column; height: 100vh; background: #f4f4f4; }',
                                            '    .header { padding: 10px 12px; background: white; color: #333; font-weight: 600; border-bottom: 1px solid #ddd; }',
                                            '    .content { flex: 1 1 auto; padding: 10px 12px; min-height: 0; display: flex; }',
                                            '    textarea { flex: 1 1 auto; width: 100%; resize: none; font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace; font-size: 12px; line-height: 1.4; border: 1px solid #bbb; border-radius: 4px; padding: 10px; box-sizing: border-box; min-height: 0; background: white; }',
                                            '  </style>',
                                            '</head>',
                                            '<body onload="(function(){function getOpenerEl(id){try{return window.opener&&window.opener.document?window.opener.document.getElementById(id):null;}catch(e){return null;}}function sync(){var src=getOpenerEl(\\'system-data\\');var dst=document.getElementById(\\'popup-system-data\\');if(dst&&src&&dst.value!==src.value){dst.value=src.value;}}setInterval(sync,500);window.addEventListener(\\'focus\\',sync);sync();})();">',
                                            '  <div class="header">System Data</div>',
                                            '  <div class="content">',
                                            '    <textarea id="popup-system-data" placeholder="System information will appear here..."></textarea>',
                                            '  </div>',
                                            '</body>',
                                            '</html>'
                                        ].join('\\n');

                                        popup.document.write(html);
                                        try { popup.document.close(); } catch (_) {}
                                        try { popup.focus(); } catch (_) {}
                                        return true;
                                    } catch (_) {}

                                    return false;
                                };

                                const pushed = pushSystemData(reportText);
                                const opened = tryOpenSystemDataWindow();
                                if (pushed && opened) {
                                    try { opdStageTrace.push('Zernike completed', 'Report pushed to System Data'); } catch (_) {}
                                    setProgress(100, 'Zernike report pushed to System Data');
                                } else if (pushed) {
                                    try { opdStageTrace.push('Zernike completed', 'Report pushed, popup open skipped'); } catch (_) {}
                                    setProgress(100, 'Zernike report pushed. See System data.');
                                } else {
                                    try { opdStageTrace.push('Zernike partial', 'Could not push report to System Data'); } catch (_) {}
                                    setProgress(100, 'Zernike fit done (could not write System Data). See System data.');
                                }
                            }
                        } catch (e) {
                            try { opdStageTrace.push('Zernike failed', String(e?.message || e || 'unknown error')); } catch (_) {}
                            setProgress(100, 'Zernike fit failed. See console.');
                        }
                    }

                    flushPendingProgress(true);
                    resizePlot();
                    try { opdStageTrace.push('Render complete'); } catch (_) {}
                } catch (err) {
                    if (err?.message?.includes('Cancelled')) {
                        flushPendingProgress(true);
                        setProgress(100, 'Cancelled');
                        try { opdStageTrace.push('Cancelled', 'Stopped by user'); } catch (_) {}
                        console.log('🛑 OPD calculation cancelled by user');
                    } else {
                        try {
                            const em = String(err?.message || err || 'unknown error').toLowerCase();
                            if (em.includes('object')) {
                                opdStageTrace.push('Object-stage failure', String(err?.message || err || 'unknown error'));
                            }
                        } catch (_) {}
                        try { opdStageTrace.push('Wavefront call failed', String(err?.message || err || 'unknown error')); } catch (_) {}
                        throw err;
                    }
                } finally {
                    flushPendingProgress(true);
                    setTimeout(() => {
                        try { hideProgress(); } catch (_) {}
                    }, 250);
                    if (stopBtn) {
                        stopBtn.disabled = true;
                        stopBtn.textContent = 'Stop';
                    }
                    window['__popupOpdCancelToken'] = null;
                }
            } catch (err) {
                console.error(err);
                flushPendingProgress(true);
                setProgress(100, 'Failed');
                try { opdStageTrace.push('Failed', String(err?.message || err || 'unknown error')); } catch (_) {}
                setTimeout(() => {
                    try { hideProgress(); } catch (_) {}
                }, 600);
                if (containerEl) {
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate OPD diagram. Check console.</div>';
                }
            }
        };

        document.getElementById('popup-show-wavefront-btn').addEventListener('click', () => window.renderOPD());
        document.getElementById('popup-stop-opd-btn').addEventListener('click', () => {
            console.log('🛑 Popup OPD Stop button clicked');
            const token = window.__popupOpdCancelToken;
            if (token && typeof token.abort === 'function') {
                token.abort('Stopped by user');
                const stopBtn = document.getElementById('popup-stop-opd-btn');
                if (stopBtn) {
                    stopBtn.disabled = true;
                    stopBtn.textContent = 'Stopping...';
                }
            }
        });

        function syncAll() {
            syncObjectOptionsFromOpener();
            syncInputsFromOpener();
        }
        window.addEventListener('resize', resizePlot);
        window.addEventListener('focus', syncAll);
        syncAll();
    <\/script>
</body>
</html>
                        `);try{S.document.close()}catch{}});const d=document.getElementById("open-psf-window-btn");if(d){d.addEventListener("click",()=>{if(T.__psfPopup&&!T.__psfPopup.closed){try{T.__psfPopup.close()}catch{}T.__psfPopup=null}const f=n("Point Spread Function","width=800,height=600");if(!f||!f.document){try{f?.close()}catch{}alert("Popup could not be opened. Please allow popups for this site.");return}T.__psfPopup=f;const b=(()=>{try{if(T.tableObject&&typeof T.tableObject.getData=="function"){const M=T.tableObject.getData();if(Array.isArray(M)&&M.length>0)return M}}catch{}try{if(typeof T.getObjectRows=="function"){const M=T.getObjectRows(T.tableObject);if(Array.isArray(M)&&M.length>0)return M}}catch{}try{const M=localStorage.getItem("objectTableData"),C=M?JSON.parse(M):null;if(Array.isArray(C)&&C.length>0)return C}catch{}try{if(typeof T.loadSystemConfigurations=="function"){const M=T.loadSystemConfigurations(),C=Array.isArray(M&&M.configurations)?M.configurations:[],D=Number(M&&M.activeConfigId),B=Number.isFinite(D)?C.find(H=>Number(H&&H.id)===D)||null:C[0]||null,j=B&&Array.isArray(B.object)?B.object:null;if(Array.isArray(j)&&j.length>0)return j}}catch{}return[]})(),v=JSON.stringify(b).replace(/</g,"\\u003c");try{f.document.open()}catch{}f.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Point Spread Function</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-psf-container { flex: 1 1 auto; min-height: 0; }
        #popup-psf-container-stats { flex: 0 0 auto; padding: 8px 12px; font-size: 12px; color: #333; border-top: 1px solid #eee; }
        .note { padding: 8px 12px; font-size: 12px; color: #666; border-bottom: 1px solid #eee; background: #fff; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="header"></div>
    <div class="controls">
        <label for="popup-psf-object-select">Object:</label>
        <select id="popup-psf-object-select"><option value="0">1</option></select>
        <label for="popup-psf-sampling-select" title="Zero-padding increases FFT size without increasing OPD ray grid.">Zero pad:</label>
        <select id="popup-psf-sampling-select" title="Auto: pad to at least 512. None: no padding (FFT size = OPD grid). Or choose an explicit FFT size.">
            <option value="auto" selected>Auto (≥512)</option>
            <option value="none">None</option>
            <option value="512">512</option>
            <option value="1024">1024</option>
            <option value="2048">2048</option>
            <option value="4096">4096</option>
        </select>
        <label for="popup-psf-zernike-sampling-select">OPD grid:</label>
        <select id="popup-psf-zernike-sampling-select" title="Ray-traced OPD grid size (number of rays traced across pupil)">
            <option value="32">32x32</option>
            <option value="64">64x64</option>
            <option value="128">128x128</option>
            <option value="256" selected>256x256</option>
            <option value="512">512x512</option>
            <option value="1024">1024x1024</option>
            <option value="2048">2048x2048</option>
            <option value="4096">4096x4096</option>
        </select>
        <label><input type="checkbox" id="popup-psf-log-scale-checkbox"> Log scale</label>
        <label><input type="checkbox" id="popup-psf-remove-ptd-checkbox"> Remove P/T/D</label>
        <button id="popup-show-psf-btn" type="button">Show PSF</button>
        <button id="popup-stop-psf-btn" type="button" disabled>Stop</button>
        <span id="popup-psf-pipeline-badge"></span>
    </div>
    <div id="popup-psf-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-psf-progress-text" style="margin-bottom: 6px;">Calculating PSF...</div>
        <progress id="popup-psf-progress" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-psf-container"></div>
        <div id="popup-psf-container-stats"></div>
        <div id="popup-psf-opd-parity-diff" style="display:none;height:220px;border-top:1px solid #eee;"></div>
    </div>
        <script>
        const __PSF_INITIAL_OBJECT_ROWS = ${v};
        // Debug: confirm the popup script version in console.
                // build tag intentionally not shown
        function isIOSLike() {
            try {
                const ua = String(navigator.userAgent || '');
                if (/iPad|iPhone|iPod/i.test(ua)) return true;
                // iPadOS 13+ may masquerade as Mac
                if (/Macintosh/i.test(ua) && Number(navigator.maxTouchPoints || 0) > 1) return true;
            } catch (_) {}
            return false;
        }

        function isPopupTauriRuntime() {
            try {
                if (window && window.__TAURI_INTERNALS__) return true;
            } catch (_) {}
            try {
                if (window && window.opener && window.opener.__TAURI_INTERNALS__) return true;
            } catch (_) {}
            return false;
        }

        function createCancelToken() {
            return {
                aborted: false,
                reason: null,
                _listeners: [],
                abort(reason = 'User requested stop') {
                    if (this.aborted) return;
                    this.aborted = true;
                    this.reason = reason;
                    const ls = Array.isArray(this._listeners) ? this._listeners.slice() : [];
                    for (const fn of ls) {
                        try { fn(reason); } catch (_) {}
                    }
                },
                onAbort(fn) {
                    if (typeof fn !== 'function') return;
                    if (this.aborted) {
                        try { fn(this.reason); } catch (_) {}
                        return;
                    }
                    this._listeners.push(fn);
                }
            };
        }

        let activeCancelToken = null;

        let __psfObjectSyncRetries = 0;
        const __psfScheduleObjectResync = () => {
            // On some loads, the opener's Tabulator may not be ready yet; retry briefly.
            if (__psfObjectSyncRetries >= 60) return;
            const delay = Math.min(2000, 150 + (__psfObjectSyncRetries * 150));
            __psfObjectSyncRetries++;
            setTimeout(() => {
                try { syncObjectOptionsFromOpener(); } catch (_) {}
            }, delay);
        };

        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (_) {
                return null;
            }
        }

        function syncObjectOptionsFromOpener() {
            const openerPsfSelect = getOpenerEl('psf-object-select');
            const openerWavefrontSelect = getOpenerEl('wavefront-object-select');
            const openerSelect = openerWavefrontSelect || openerPsfSelect;
            const popupSelect = document.getElementById('popup-psf-object-select');
            if (!popupSelect) return;

            const toFiniteNumber = (v) => {
                const n = (typeof v === 'number') ? v : parseFloat(v);
                return Number.isFinite(n) ? n : NaN;
            };
            const pickNumber = (obj, keys, fallback) => {
                for (let i = 0; i < keys.length; i++) {
                    const k = keys[i];
                    if (!k) continue;
                    const raw = obj ? obj[k] : undefined;
                    if (raw === undefined || raw === null || raw === '') continue;
                    const n = toFiniteNumber(raw);
                    if (Number.isFinite(n)) return n;
                }
                return fallback;
            };

            const current = popupSelect.value;
            const nextOptions = [];

            // OPD/MTF-style: resolve object rows from multiple opener sources.
            let opener = null;
            try { opener = window.opener || null; } catch (_) { opener = null; }
            let objects = [];
            if (opener) {
                try {
                    if (opener.tableObject && typeof opener.tableObject.getData === 'function') {
                        const rows = opener.tableObject.getData();
                        if (Array.isArray(rows) && rows.length > 0) objects = rows;
                    }
                } catch (_) {}

                if (!Array.isArray(objects) || objects.length === 0) {
                    try {
                        if (typeof opener.getObjectRows === 'function') {
                            const rows = opener.getObjectRows(opener.tableObject);
                            if (Array.isArray(rows) && rows.length > 0) objects = rows;
                        }
                    } catch (_) {}
                }

                if (!Array.isArray(objects) || objects.length === 0) {
                    try {
                        if (opener.localStorage && typeof opener.localStorage.getItem === 'function') {
                            const raw = opener.localStorage.getItem('objectTableData');
                            const parsed = raw ? JSON.parse(raw) : null;
                            if (Array.isArray(parsed) && parsed.length > 0) objects = parsed;
                        }
                    } catch (_) {}
                }

                if (!Array.isArray(objects) || objects.length === 0) {
                    try {
                        if (typeof opener.getActiveConfiguration === 'function') {
                            const cfg = opener.getActiveConfiguration();
                            const rows = cfg && Array.isArray(cfg.object) ? cfg.object : null;
                            if (Array.isArray(rows) && rows.length > 0) objects = rows;
                        }
                    } catch (_) {}
                }

                if (!Array.isArray(objects) || objects.length === 0) {
                    try {
                        if (typeof opener.loadSystemConfigurations === 'function') {
                            const all = opener.loadSystemConfigurations();
                            const list = Array.isArray(all && all.configurations) ? all.configurations : [];
                            const activeId = Number(all && all.activeConfigId);
                            const active = Number.isFinite(activeId)
                                ? (list.find((c) => Number(c && c.id) === activeId) || null)
                                : (list[0] || null);
                            const rows = active && Array.isArray(active.object) ? active.object : null;
                            if (Array.isArray(rows) && rows.length > 0) objects = rows;
                        }
                    } catch (_) {}
                }
            }

            if ((!Array.isArray(objects) || objects.length === 0) && Array.isArray(__PSF_INITIAL_OBJECT_ROWS) && __PSF_INITIAL_OBJECT_ROWS.length > 0) {
                objects = __PSF_INITIAL_OBJECT_ROWS;
            }

            if (!Array.isArray(objects) || objects.length === 0) {
                try {
                    const raw = localStorage.getItem('objectTableData');
                    const parsed = raw ? JSON.parse(raw) : null;
                    if (Array.isArray(parsed) && parsed.length > 0) objects = parsed;
                } catch (_) {}
            }

            if (!Array.isArray(objects) || objects.length === 0) {
                try {
                    const rawCfg = localStorage.getItem('systemConfigurations');
                    const parsedCfg = rawCfg ? JSON.parse(rawCfg) : null;
                    const list = Array.isArray(parsedCfg && parsedCfg.configurations) ? parsedCfg.configurations : [];
                    const activeId = Number(parsedCfg && parsedCfg.activeConfigId);
                    const active = Number.isFinite(activeId)
                        ? (list.find((c) => Number(c && c.id) === activeId) || null)
                        : (list[0] || null);
                    const rows = active && Array.isArray(active.object) ? active.object : null;
                    if (Array.isArray(rows) && rows.length > 0) objects = rows;
                } catch (_) {}
            }

            if (Array.isArray(objects) && objects.length > 0) {
                for (let i = 0; i < objects.length; i++) {
                    const obj = objects[i];
                    if (!obj) continue;
                    const typeRaw = String(
                        (obj.position !== undefined && obj.position !== null) ? obj.position :
                        (obj.object !== undefined && obj.object !== null) ? obj.object :
                        (obj.Object !== undefined && obj.Object !== null) ? obj.Object :
                        (obj.objectType !== undefined && obj.objectType !== null) ? obj.objectType :
                        'Point'
                    );
                    const x = pickNumber(obj, ['x', 'X', 'xFieldAngle', 'xAngle', 'xHeightAngle', 'XHeightAngle', 'x_height_angle', 'x_field_angle', 'x_angle'], 0);
                    const y = pickNumber(obj, ['y', 'Y', 'yFieldAngle', 'yAngle', 'yHeightAngle', 'YHeightAngle', 'y_height_angle', 'y_field_angle', 'y_angle', 'fieldAngle', 'angle'], 0);
                    nextOptions.push({ value: String(i), label: (String(i + 1) + ': ' + typeRaw + ' (' + x + ', ' + y + ')') });
                }
            }

            // Fallback: clone opener select options if present (but ignore single placeholder).
            if (nextOptions.length === 0 && openerSelect && openerSelect.options && openerSelect.options.length > 0) {
                const opts = Array.from(openerSelect.options);
                let looksLikePlaceholder = false;
                if (opts.length === 1) {
                    const t = String(opts[0].textContent || '').trim().toLowerCase();
                    looksLikePlaceholder = (t === '0' || t === 'object 1' || t === 'object1');
                }
                if (!looksLikePlaceholder) {
                    const normalizeLabel = (label) => {
                        const s = String(label || '').trim();
                        // Convert e.g. "Object1 : Angle (...)" / "Object 1: ..." / "object1" => "1: ..." / "1"
                        const m = s.match(/^objects*(d+)s*[:：]?s*(.*)$/i);
                        if (m) {
                            const n = m[1];
                            const rest = String(m[2] || '').trim();
                            return rest ? (n + ': ' + rest) : String(n);
                        }
                        return s;
                    };
                    opts.forEach(opt => {
                        const raw = String((opt.textContent !== undefined && opt.textContent !== null) ? opt.textContent : '');
                        nextOptions.push({ value: String(opt.value), label: normalizeLabel(raw) });
                    });
                }
            }

            if (nextOptions.length === 0) {
                nextOptions.push({ value: '0', label: '1' });
                __psfScheduleObjectResync();
            } else {
                __psfObjectSyncRetries = 0;
            }

            popupSelect.innerHTML = '';
            for (const opt of nextOptions) {
                const o = document.createElement('option');
                o.value = opt.value;
                o.textContent = opt.label;
                popupSelect.appendChild(o);
            }

            if (current && Array.from(popupSelect.options).some(o => o.value === current)) {
                popupSelect.value = current;
                return;
            }
            const preferred = (openerPsfSelect && openerPsfSelect.value)
                ? openerPsfSelect.value
                : (openerSelect && openerSelect.value ? openerSelect.value : '');
            if (preferred && Array.from(popupSelect.options).some(o => o.value === preferred)) {
                popupSelect.value = preferred;
            } else {
                const firstOpt = (popupSelect.options && popupSelect.options.length > 0) ? popupSelect.options[0] : null;
                popupSelect.value = (firstOpt && firstOpt.value !== undefined && firstOpt.value !== null) ? firstOpt.value : '0';
            }
        }

        function syncInputsFromOpener() {
            const openerZeroPad = getOpenerEl('psf-zeropad-select');
            const openerZernikeSampling = getOpenerEl('psf-zernike-sampling-select');
            const openerLog = getOpenerEl('psf-log-scale-checkbox');
            const openerRemovePtd = getOpenerEl('psf-remove-ptd-checkbox');
            const popupSampling = document.getElementById('popup-psf-sampling-select');
            const popupZernikeSampling = document.getElementById('popup-psf-zernike-sampling-select');
            const popupLog = document.getElementById('popup-psf-log-scale-checkbox');
            const popupRemovePtd = document.getElementById('popup-psf-remove-ptd-checkbox');
            if (openerZeroPad && popupSampling && openerZeroPad.value) {
                if (Array.from(popupSampling.options || []).some(o => String(o.value) === String(openerZeroPad.value))) {
                    popupSampling.value = openerZeroPad.value;
                }
            }
            if (popupZernikeSampling && openerZernikeSampling && openerZernikeSampling.value) popupZernikeSampling.value = openerZernikeSampling.value;
            try {
                if (popupLog && openerLog) popupLog.checked = !!openerLog.checked;
            } catch (_) {}
            try {
                if (popupRemovePtd) popupRemovePtd.checked = !!(openerRemovePtd && openerRemovePtd.checked);
            } catch (_) {}
        }

        function resizePlot() {
            try {
                const plotly = window.Plotly;
                if (!plotly || !plotly.Plots) return;
                const el = document.getElementById('popup-psf-container');
                if (el) plotly.Plots.resize(el);
                const diffEl = document.getElementById('popup-psf-opd-parity-diff');
                if (diffEl && diffEl.style.display !== 'none') {
                    plotly.Plots.resize(diffEl);
                }
            } catch (_) {}
        }

        function clearOpdParityHeatmap() {
            try {
                const diffEl = document.getElementById('popup-psf-opd-parity-diff');
                if (!diffEl) return;
                diffEl.style.display = 'none';
                diffEl.innerHTML = '';
            } catch (_) {}
        }

        async function renderOpdParityHeatmap(diffGridWaves, meta) {
            try {
                const diffEl = document.getElementById('popup-psf-opd-parity-diff');
                if (!diffEl) return;
                if (!Array.isArray(diffGridWaves) || diffGridWaves.length === 0 || !window.Plotly) {
                    clearOpdParityHeatmap();
                    return;
                }

                const maxAbs = Number(meta && meta.maxAbsWaves);
                const zAbs = (Number.isFinite(maxAbs) && maxAbs > 0) ? maxAbs : 1;
                const title = 'OPD parity map (Native - JS) [waves]';

                diffEl.style.display = 'block';
                await window.Plotly.newPlot(diffEl, [{
                    z: diffGridWaves,
                    type: 'heatmap',
                    colorscale: 'RdBu',
                    zmid: 0,
                    zmin: -zAbs,
                    zmax: zAbs,
                    colorbar: { title: 'waves' }
                }], {
                    title: { text: title, font: { size: 12 } },
                    margin: { l: 50, r: 20, t: 32, b: 40 },
                    xaxis: { title: 'Pupil ix' },
                    yaxis: { title: 'Pupil iy', autorange: 'reversed' }
                }, { responsive: true, displaylogo: false });
            } catch (_) {
                clearOpdParityHeatmap();
            }
        }

        window['renderPSF'] = async () => {
            const containerEl = document.getElementById('popup-psf-container');
            if (containerEl) containerEl.innerHTML = '';
            clearOpdParityHeatmap();

            const setPopupPsfBadge = (status) => {
                try {
                    const el = document.getElementById('popup-psf-pipeline-badge');
                    if (!el) return;
                    const text = String(status || '');
                    el.textContent = text;
                    if (text) {
                        el.setAttribute('title', text);
                    } else {
                        el.removeAttribute('title');
                    }
                } catch (_) {}
            };

            const progressWrapper = document.getElementById('popup-psf-progress-wrapper');
            const progressEl = document.getElementById('popup-psf-progress');
            const progressTextEl = document.getElementById('popup-psf-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressEl && Number.isFinite(value)) progressEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            const stopBtn = document.getElementById('popup-stop-psf-btn');
            if (stopBtn) {
                stopBtn.disabled = false;
            }

            activeCancelToken = createCancelToken();

            const popupObject = document.getElementById('popup-psf-object-select');
            const popupSampling = document.getElementById('popup-psf-sampling-select');
            const popupZernikeSampling = document.getElementById('popup-psf-zernike-sampling-select');
            const popupLog = document.getElementById('popup-psf-log-scale-checkbox');
            const popupRemovePtd = document.getElementById('popup-psf-remove-ptd-checkbox');

            let objectIndex = popupObject ? parseInt(popupObject.value, 10) : 0;
            if (!Number.isFinite(objectIndex) && popupObject && Number.isFinite(popupObject.selectedIndex)) {
                objectIndex = popupObject.selectedIndex;
            }
            const zernikeSampling = popupZernikeSampling ? parseInt(popupZernikeSampling.value, 10) : 128;
            const zeroPadRaw = popupSampling ? String(popupSampling.value || 'auto') : 'auto';
            const logScale = !!(popupLog && popupLog.checked);
            const opdDisplayMode = (popupRemovePtd && popupRemovePtd.checked)
                ? 'pistonTiltDefocusRemoved'
                : 'pistonTiltRemoved';
            const PSF_DEBUG = !!(typeof globalThis !== 'undefined' && globalThis.__PSF_DEBUG);

            const openerObject = getOpenerEl('psf-object-select');
            const openerSampling = getOpenerEl('psf-sampling-select');
            const openerZeroPad = getOpenerEl('psf-zeropad-select');
            const openerZernikeSampling = getOpenerEl('psf-zernike-sampling-select');
            const openerLog = getOpenerEl('psf-log-scale-checkbox');
            const openerRemovePtd = getOpenerEl('psf-remove-ptd-checkbox');
            if (openerObject && Number.isFinite(objectIndex)) openerObject.value = String(objectIndex);
            if (openerSampling && Number.isFinite(zernikeSampling)) openerSampling.value = String(zernikeSampling);
            if (openerZeroPad && zeroPadRaw) {
                if (Array.from(openerZeroPad.options || []).some(o => String(o.value) === String(zeroPadRaw))) {
                    openerZeroPad.value = zeroPadRaw;
                }
            }
            try {
                if (openerRemovePtd) openerRemovePtd.checked = (opdDisplayMode === 'pistonTiltDefocusRemoved');
            } catch (_) {}
            if (openerZernikeSampling && Number.isFinite(zernikeSampling)) openerZernikeSampling.value = String(zernikeSampling);
            if (openerLog) openerLog.checked = logScale;

            try {
                setPopupPsfBadge('Running');
                setProgress(0, 'Starting...');
                // Allow the popup to paint the progress UI before heavy computation begins.
                await new Promise(r => setTimeout(r, 0));

                const onProgress = (evt) => {
                    try {
                        const p = Number(evt && evt.percent);
                        const msg = (evt && evt.message) || (evt && evt.phase) || 'Working...';
                        if (Number.isFinite(p)) setProgress(p, msg);
                        else setProgress(undefined, msg);
                    } catch (_) {}
                };

                // Always compute inside the popup to avoid background throttling of the opener
                // when the main window is hidden/minimized/unfocused.
                {
                    const moduleURL = (relPath) => {
                        const baseHref = (() => {
                            try {
                                return (window.opener && window.opener.location && window.opener.location.href)
                                    ? window.opener.location.href
                                    : window.location.href;
                            } catch (_) {
                                return window.location.href;
                            }
                        })();
                        const url = new URL(relPath, baseHref);
                        return url.href;
                    };

                    const throwIfCancelled = (token) => {
                        if (token && token.aborted) {
                            const err = new Error(String(token.reason || 'Cancelled'));
                            err.code = 'CANCELLED';
                            throw err;
                        }
                    };
                    const raceWithCancel = async (promise, token) => {
                        if (!token) return await promise;
                        throwIfCancelled(token);
                        const cancelPromise = new Promise((_, reject) => {
                            token.onAbort((reason) => {
                                const err = new Error(String(reason || 'Cancelled'));
                                err.code = 'CANCELLED';
                                reject(err);
                            });
                        });
                        return await Promise.race([promise, cancelPromise]);
                    };

                    // Modules are already imported at the top of this file

                    const cloneRows = (rows) => {
                        if (!Array.isArray(rows)) return rows;
                        try {
                            if (typeof structuredClone === 'function') return structuredClone(rows);
                        } catch (_) {}
                        try {
                            return JSON.parse(JSON.stringify(rows));
                        } catch (_) {
                            return rows;
                        }
                    };

                    const opticalSystemRows = (() => {
                        try {
                            if (window.opener && typeof window.opener.getOpticalSystemRows === 'function') {
                                // Prefer live Tabulator table data if available.
                                const r = window.opener.getOpticalSystemRows(window.opener.tableOpticalSystem);
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        // Fallback: try opener again without parameter
                        try {
                            if (window.opener && typeof window.opener.getOpticalSystemRows === 'function') {
                                const r = window.opener.getOpticalSystemRows();
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        return [];
                    })();

                    const objects = (() => {
                        if (Array.isArray(__PSF_INITIAL_OBJECT_ROWS) && __PSF_INITIAL_OBJECT_ROWS.length > 0) {
                            return cloneRows(__PSF_INITIAL_OBJECT_ROWS);
                        }
                        try {
                            if (window.opener && window.opener.tableObject && typeof window.opener.tableObject.getData === 'function') {
                                const r = window.opener.tableObject.getData();
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        try {
                            if (window.opener && typeof window.opener.getObjectRows === 'function') {
                                const r = window.opener.getObjectRows(window.opener.tableObject);
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        // Fallback: try opener again without parameter
                        try {
                            if (window.opener && typeof window.opener.getObjectRows === 'function') {
                                const r = window.opener.getObjectRows();
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        try {
                            const raw = localStorage.getItem('objectTableData');
                            const parsed = raw ? JSON.parse(raw) : null;
                            if (Array.isArray(parsed) && parsed.length > 0) return cloneRows(parsed);
                        } catch (_) {}
                        try {
                            const rawCfg = localStorage.getItem('systemConfigurations');
                            const parsedCfg = rawCfg ? JSON.parse(rawCfg) : null;
                            const list = Array.isArray(parsedCfg && parsedCfg.configurations) ? parsedCfg.configurations : [];
                            const activeId = Number(parsedCfg && parsedCfg.activeConfigId);
                            const active = Number.isFinite(activeId)
                                ? (list.find((c) => Number(c && c.id) === activeId) || null)
                                : (list[0] || null);
                            const rows = active && Array.isArray(active.object) ? active.object : null;
                            if (Array.isArray(rows) && rows.length > 0) return cloneRows(rows);
                        } catch (_) {}
                        return [];
                    })();

                    const sources = (() => {
                        try {
                            if (window.opener && typeof window.opener.getSourceRows === 'function') {
                                const r = window.opener.getSourceRows(window.opener.tableSource);
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        // Fallback: try opener again without tableSource parameter
                        try {
                            if (window.opener && typeof window.opener.getSourceRows === 'function') {
                                const r = window.opener.getSourceRows();
                                if (Array.isArray(r) && r.length > 0) return cloneRows(r);
                            }
                        } catch (_) {}
                        return [];
                    })();
                    if (!Array.isArray(opticalSystemRows) || opticalSystemRows.length === 0) throw new Error('No optical system data (popup).');
                    if (!Array.isArray(objects) || objects.length === 0) throw new Error('No object data (popup).');

                    const toFiniteNumber = (v) => {
                        const n = (typeof v === 'number') ? v : parseFloat(v);
                        return (Number.isFinite(n) ? n : NaN);
                    };
                    const pickNumber = (obj, keys, fallback) => {
                        for (let i = 0; i < keys.length; i++) {
                            const k = keys[i];
                            if (!k) continue;
                            const raw = obj ? obj[k] : undefined;
                            if (raw === undefined || raw === null || raw === '') continue;
                            const n = toFiniteNumber(raw);
                            if (Number.isFinite(n)) return n;
                        }
                        return fallback;
                    };

                    let idx = Number.isFinite(objectIndex) ? objectIndex : 0;
                    // If the select values are 1-based for any reason, normalize.
                    if ((idx >= objects.length) && idx > 0 && (idx - 1) < objects.length) idx = idx - 1;
                    if (idx < 0) idx = 0;
                    const selectedObject = objects[idx];
                    if (!selectedObject) throw new Error('Selected object not found (popup).');

                    const primaryWl = (() => {
                        try {
                            if (window.opener && typeof window.opener.getPrimaryWavelength === 'function') {
                                const v = Number(window.opener.getPrimaryWavelength());
                                if (Number.isFinite(v) && v > 0) return v;
                            }
                        } catch (_) {}
                        return NaN;
                    })();
                    const s0 = (sources && sources.length > 0) ? sources[0] : null;
                    const wl0 = (s0 && s0.wavelength !== undefined && s0.wavelength !== null) ? Number(s0.wavelength) : NaN;
                    const wavelength = Number.isFinite(primaryWl)
                        ? primaryWl
                        : (() => { throw new Error('Primary wavelength is unavailable. Please set Source Primary Wavelength.'); })();

                    // Match showPSFDiagram(): build a fieldSetting compatible with eva-wavefront.js.
                    const objectX = pickNumber(selectedObject, ['x', 'X', 'xFieldAngle', 'xAngle', 'xHeightAngle', 'XHeightAngle', 'x_height_angle', 'x_field_angle', 'x_angle'], 0);
                    const objectY = pickNumber(selectedObject, ['y', 'Y', 'yFieldAngle', 'yAngle', 'yHeightAngle', 'YHeightAngle', 'y_height_angle', 'y_field_angle', 'y_angle', 'fieldAngle', 'angle'], 0);
                    const objectTypeRaw = String(
                        (selectedObject && selectedObject.position !== undefined && selectedObject.position !== null) ? selectedObject.position :
                        (selectedObject && selectedObject.object !== undefined && selectedObject.object !== null) ? selectedObject.object :
                        (selectedObject && selectedObject.Object !== undefined && selectedObject.Object !== null) ? selectedObject.Object :
                        (selectedObject && selectedObject.objectType !== undefined && selectedObject.objectType !== null) ? selectedObject.objectType :
                        'Point'
                    );
                    const objectType = objectTypeRaw;
                    const objectTypeLower = String(objectTypeRaw).toLowerCase();
                    let fieldAngle = { x: 0, y: 0 };
                    let xHeight = 0;
                    let yHeight = 0;
                    if (/\\bangle\\b/.test(objectTypeLower)) {
                        fieldAngle = { x: Number(objectX) || 0, y: Number(objectY) || 0 };
                        xHeight = 0;
                        yHeight = 0;
                    } else {
                        fieldAngle = { x: 0, y: 0 };
                        xHeight = Number(objectX) || 0;
                        yHeight = Number(objectY) || 0;
                    }
                    const fieldSetting = {
                        objectIndex: idx,
                        type: objectType,
                        fieldAngle,
                        xHeight,
                        yHeight,
                        wavelength
                    };

                    const getActiveConfigLabel = () => {
                        try {
                            if (typeof localStorage === 'undefined') return '';
                            const sys = loadSystemConfigurations();
                            const activeId = sys?.activeConfigId;
                            const cfg = Array.isArray(sys?.configurations)
                                ? sys.configurations.find(c => String(c?.id) === String(activeId))
                                : null;
                            if (!cfg) return (activeId !== undefined && activeId !== null) ? ('id=' + activeId) : '';
                            return ('id=' + cfg.id + ' name=' + (cfg.name || '')).trim();
                        } catch (_) {
                            return '';
                        }
                    };

                    const calcFNV1a32 = (str) => {
                        let hash = 0x811c9dc5;
                        for (let i = 0; i < str.length; i++) {
                            hash ^= str.charCodeAt(i);
                            hash = Math.imul(hash, 0x01000193);
                        }
                        return (hash >>> 0).toString(16);
                    };

                    const summarizeOpticalSystemRows = (rows) => {
                        if (!Array.isArray(rows) || rows.length === 0) return { checksum: '0' };
                        const parts = [];
                        for (const r of rows) {
                            if (!r) continue;
                            const obj = r['object type'] ?? r.object ?? r.Object ?? '';
                            const radius = r.radius ?? r.Radius ?? '';
                            const thickness = r.thickness ?? r.Thickness ?? '';
                            const material = r.material ?? r.Material ?? '';
                            const semidia = r.semidia ?? r.semidiameter ?? r.SemiDia ?? '';
                            const id = r.id ?? '';
                            parts.push(String(id) + '|' + String(obj) + '|' + String(radius) + '|' + String(thickness) + '|' + String(material) + '|' + String(semidia));
                        }
                        return { checksum: calcFNV1a32(parts.join(';')) };
                    };

                    if (PSF_DEBUG) {
                        try {
                            const fa = (fieldSetting && fieldSetting.fieldAngle && typeof fieldSetting.fieldAngle === 'object')
                                ? fieldSetting.fieldAngle
                                : { x: 0, y: 0 };
                            const line = '🧭 [PSF popup] objectIndex=' + idx + ' type=' + objectType +
                                ' fieldAngle=(' + (Number(fa.x) || 0) + ',' + (Number(fa.y) || 0) + ')' +
                                ' height=(' + (Number(fieldSetting && fieldSetting.xHeight) || 0) + ',' + (Number(fieldSetting && fieldSetting.yHeight) || 0) + ')' +
                                ' wl=' + (Number(wavelength) || 0);
                            console.log(line);
                            try {
                                if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                    window.opener.console.log(line);
                                }
                            } catch (_) {}
                        } catch (_) {}
                    }

                    if (PSF_DEBUG) {
                        try {
                            const summary = summarizeOpticalSystemRows(opticalSystemRows);
                            const idLine = '🧾 [PSF popup] activeConfig=' + (getActiveConfigLabel() || '(none)') +
                                ' rows=' + (Array.isArray(opticalSystemRows) ? opticalSystemRows.length : 0) +
                                ' checksum=' + (summary && summary.checksum ? summary.checksum : '0');
                            console.log(idLine);
                            try {
                                if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                    window.opener.console.log(idLine);
                                }
                            } catch (_) {}
                        } catch (_) {}
                    }

                    // Also log PSF physical scale inputs (pupil diameter / focal length) once available.
                    // This is critical when the plot auto-normalizes intensity and hides differences.
                    const logScaleInputs = (pupilDiameterMm, focalLengthMm, stopIndex) => {
                        if (!PSF_DEBUG) return;
                        try {
                            const line = '📏 [PSF popup] pupilDiameterMm=' + (Number(pupilDiameterMm) || 0) +
                                ' focalLengthMm=' + (Number(focalLengthMm) || 0) +
                                ' stopIndex=' + (Number.isFinite(Number(stopIndex)) ? Number(stopIndex) : -1);
                            console.log(line);
                            try {
                                if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                    window.opener.console.log(line);
                                }
                            } catch (_) {}
                        } catch (_) {}
                    };

                    const wavefrontGridSize = Number.isFinite(zernikeSampling)
                        ? Math.max(16, Math.floor(Number(zernikeSampling)))
                        : 128;
                    onProgress({ percent: 0, phase: 'opd', message: 'OPD...' });

                    let wavefrontMap = null;
                    let nativeOpdResp = null;
                    let nativeOpdDiag = null;
                    const forceJsOpdMap = !!(typeof window !== 'undefined' && window.__PSF_POPUP_FORCE_JS_OPD_MAP === true);
                    const preferNativeOpdMap = (typeof window !== 'undefined' && window.__PSF_POPUP_USE_NATIVE_OPD_MAP === false)
                        ? false
                        : true;
                    const canUseNativeOpdMap = !forceJsOpdMap && preferNativeOpdMap
                        && !!(window.opener && typeof window.opener.runDesktopNativeOpdMapForPopup === 'function');

                    if (canUseNativeOpdMap) {
                        try {
                            nativeOpdResp = await raceWithCancel(window.opener.runDesktopNativeOpdMapForPopup({
                                objectIndex: idx,
                                gridSize: wavefrontGridSize,
                                wavelengthUm: wavelength,
                                opdDisplayMode,
                            }), activeCancelToken);
                            throwIfCancelled(activeCancelToken);
                            {
                                const usedIdx = Number(nativeOpdResp?.usedObjectIndex);
                                if (Number.isFinite(usedIdx) && Number.isFinite(idx) && usedIdx !== idx) {
                                    throw new Error('Native OPD object mismatch: requested=' + String(idx) + ', used=' + String(usedIdx));
                                }
                            }
                            try {
                                const opdLine = '🧪 [PSF popup] OPD backend=' + String(nativeOpdResp?.backend || 'unknown') +
                                    ' reqObjIdx=' + (nativeOpdResp?.requestedObjectIndex ?? 'n/a') +
                                    ' usedObjIdx=' + Number(nativeOpdResp?.usedObjectIndex ?? -1) +
                                    ' usedObj=' + String(nativeOpdResp?.usedObjectPosition || 'n/a') +
                                    ' field=(' + Number(nativeOpdResp?.usedObjectX ?? 0) + ',' + Number(nativeOpdResp?.usedObjectY ?? 0) + ')' +
                                    ' hitCount=' + Number(nativeOpdResp?.hitCount || 0) +
                                    ' sampleCount=' + Number(nativeOpdResp?.sampleCount || 0);
                                console.log(opdLine);
                                try {
                                    if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                        window.opener.console.log(opdLine);
                                    }
                                } catch (_) {}
                            } catch (_) {}

                            try {
                                const enableOpdParityDiag = !!(typeof window !== 'undefined' && window.__PSF_POPUP_ENABLE_OPD_PARITY_DIAG === true);
                                if (!enableOpdParityDiag) {
                                    // Skip heavy JS re-computation parity diagnostics unless explicitly enabled.
                                    throw new Error('__skip_opd_parity_diag__');
                                }

                                const host = window.opener || window;
                                const calculatorFactory =
                                    (host && typeof host.createOPDCalculator === 'function' && host.createOPDCalculator)
                                    || (typeof window.createOPDCalculator === 'function' && window.createOPDCalculator)
                                    || (typeof createOPDCalculator === 'function' ? createOPDCalculator : null);
                                const analyzerFactory =
                                    (host && typeof host.createWavefrontAnalyzer === 'function' && host.createWavefrontAnalyzer)
                                    || (typeof window.createWavefrontAnalyzer === 'function' && window.createWavefrontAnalyzer)
                                    || (typeof createWavefrontAnalyzer === 'function' ? createWavefrontAnalyzer : null);
                                const opdCalculator = calculatorFactory ? calculatorFactory(opticalSystemRows, wavelength) : null;
                                const analyzer = analyzerFactory ? analyzerFactory(opdCalculator) : null;
                                if (analyzer && Array.isArray(nativeOpdResp?.displayOpdGrid)) {
                                    try {
                                        const nativeTarget = Number(nativeOpdResp?.targetSurface);
                                        const nativeStop = Number(nativeOpdResp?.stopSurface);
                                        const isFiniteFieldObj = !!(fieldSetting && String(fieldSetting.type || '').toLowerCase().includes('angle'));

                                        if (Number.isFinite(nativeStop) && nativeStop >= 0 && opdCalculator) {
                                            opdCalculator.stopSurfaceIndex = Math.floor(nativeStop);
                                        }
                                        if (Number.isFinite(nativeTarget) && nativeTarget >= 0 && opdCalculator) {
                                            opdCalculator.evaluationSurfaceIndex = Math.floor(nativeTarget);
                                        }
                                        if (opdCalculator) {
                                            const evalIdx = Number(opdCalculator.evaluationSurfaceIndex);
                                            const stopIdx = Number(opdCalculator.stopSurfaceIndex);
                                            const maxIdx = Math.max(
                                                Number.isFinite(evalIdx) ? evalIdx : 0,
                                                Number.isFinite(stopIdx) ? stopIdx : 0
                                            );
                                            opdCalculator.traceMaxSurfaceIndex = maxIdx;

                                            if (isFiniteFieldObj && typeof opdCalculator._setInfinitePupilMode === 'function') {
                                                const nativeMode = String(nativeOpdResp?.pupilSamplingMode || '').toLowerCase();
                                                opdCalculator._setInfinitePupilMode(fieldSetting, nativeMode === 'stop' ? 'stop' : 'entrance');
                                            }

                                            // Rebuild chief/reference after overriding indices/mode.
                                            opdCalculator.referenceOpticalPath = null;
                                            opdCalculator.lastFieldKey = null;
                                            if (typeof opdCalculator.setReferenceRay === 'function') {
                                                opdCalculator.setReferenceRay(fieldSetting);
                                            }
                                        }
                                    } catch (_) {}

                                    const jsDiagMap = await raceWithCancel(analyzer.generateWavefrontMap(fieldSetting, wavefrontGridSize, 'circular', {
                                        recordRays: false,
                                        progressEvery: 0,
                                        renderFromZernike: false,
                                        skipZernikeFit: true,
                                        opdMode: 'simple',
                                        opdDisplayMode,
                                        diagnoseDiscontinuities: false,
                                        cancelToken: activeCancelToken,
                                    }), activeCancelToken);

                                    if (jsDiagMap && !jsDiagMap.error) {
                                        const sDiag = Math.max(2, Math.floor(Number(wavefrontGridSize)));
                                        const jsGrid = Array.from({ length: sDiag }, () => Array(sDiag).fill(NaN));
                                        const coords = Array.isArray(jsDiagMap?.pupilCoordinates) ? jsDiagMap.pupilCoordinates : [];
                                        const jsOpdMicrons = (jsDiagMap?.display && Array.isArray(jsDiagMap.display.opds))
                                            ? jsDiagMap.display.opds
                                            : (Array.isArray(jsDiagMap?.opds) ? jsDiagMap.opds : []);
                                        const jsRawOpdMicrons = (jsDiagMap?.raw && Array.isArray(jsDiagMap.raw.opds))
                                            ? jsDiagMap.raw.opds
                                            : (Array.isArray(jsDiagMap?.opds) ? jsDiagMap.opds : []);
                                        const nDiag = Math.min(coords.length, jsOpdMicrons.length);
                                        for (let k = 0; k < nDiag; k++) {
                                            const c = coords[k];
                                            const ix = Number.isInteger(c?.ix) ? c.ix : null;
                                            const iy = Number.isInteger(c?.iy) ? c.iy : null;
                                            if (ix === null || iy === null) continue;
                                            if (ix < 0 || ix >= sDiag || iy < 0 || iy >= sDiag) continue;
                                            const rawV = jsOpdMicrons[k];
                                            if (rawV === null || rawV === undefined || rawV === '') continue;
                                            const v = Number(rawV);
                                            if (!Number.isFinite(v)) continue;
                                            jsGrid[iy][ix] = v;
                                        }

                                        const jsRawGrid = Array.from({ length: sDiag }, () => Array(sDiag).fill(NaN));
                                        const nRawDiag = Math.min(coords.length, jsRawOpdMicrons.length);
                                        for (let k = 0; k < nRawDiag; k++) {
                                            const c = coords[k];
                                            const ix = Number.isInteger(c?.ix) ? c.ix : null;
                                            const iy = Number.isInteger(c?.iy) ? c.iy : null;
                                            if (ix === null || iy === null) continue;
                                            if (ix < 0 || ix >= sDiag || iy < 0 || iy >= sDiag) continue;
                                            const rawV = jsRawOpdMicrons[k];
                                            if (rawV === null || rawV === undefined || rawV === '') continue;
                                            const v = Number(rawV);
                                            if (!Number.isFinite(v)) continue;
                                            jsRawGrid[iy][ix] = v;
                                        }

                                        let count = 0;
                                        let sumAbs = 0;
                                        let sumSq = 0;
                                        let maxAbs = 0;
                                        let sumA = 0;
                                        let sumB = 0;
                                        let sumBB = 0;
                                        let sumBA = 0;
                                        let maxIx = -1;
                                        let maxIy = -1;
                                        const diffGridWaves = Array.from({ length: sDiag }, () => Array(sDiag).fill(NaN));
                                        const radial = {
                                            center: { count: 0, sumAbs: 0, sumSq: 0 },
                                            mid: { count: 0, sumAbs: 0, sumSq: 0 },
                                            edge: { count: 0, sumAbs: 0, sumSq: 0 },
                                        };
                                        const cx = (sDiag - 1) / 2;
                                        const cy = (sDiag - 1) / 2;
                                        const rMax = Math.max(1, Math.hypot(cx, cy));
                                        for (let iy = 0; iy < sDiag; iy++) {
                                            const nRow = nativeOpdResp.displayOpdGrid[iy] || [];
                                            for (let ix = 0; ix < sDiag; ix++) {
                                                const rawA = nRow[ix];
                                                const rawB = jsGrid[iy][ix];
                                                if (rawA === null || rawA === undefined || rawA === '' || rawB === null || rawB === undefined || rawB === '') continue;
                                                const a = Number(rawA);
                                                const b = Number(rawB);
                                                if (!Number.isFinite(a) || !Number.isFinite(b)) continue;
                                                // Native OPD map is returned in waves, while JS map here is in microns.
                                                // Convert JS OPD to waves before parity comparison.
                                                const bWaves = b / wavelength;
                                                const dWaves = a - bWaves;
                                                diffGridWaves[iy][ix] = dWaves;
                                                const ad = Math.abs(dWaves);
                                                sumAbs += ad;
                                                sumSq += dWaves * dWaves;
                                                sumA += a;
                                                sumB += bWaves;
                                                sumBB += bWaves * bWaves;
                                                sumBA += bWaves * a;
                                                if (ad > maxAbs) {
                                                    maxAbs = ad;
                                                    maxIx = ix;
                                                    maxIy = iy;
                                                }
                                                const rn = Math.hypot(ix - cx, iy - cy) / rMax;
                                                const band = (rn < 0.35) ? radial.center : (rn < 0.7 ? radial.mid : radial.edge);
                                                band.count++;
                                                band.sumAbs += ad;
                                                band.sumSq += dWaves * dWaves;
                                                count++;
                                            }
                                        }

                                        if (count > 0) {
                                            let fitScale = NaN;
                                            let fitOffset = NaN;
                                            let fitResidualRms = NaN;
                                            if (count >= 2) {
                                                const denom = (count * sumBB) - (sumB * sumB);
                                                if (Number.isFinite(denom) && Math.abs(denom) > 1e-18) {
                                                    fitScale = ((count * sumBA) - (sumB * sumA)) / denom;
                                                    fitOffset = (sumA - fitScale * sumB) / count;

                                                    let fitSumSq = 0;
                                                    let fitCount = 0;
                                                    for (let iy = 0; iy < sDiag; iy++) {
                                                        const nRow2 = nativeOpdResp.displayOpdGrid[iy] || [];
                                                        const jRow2 = jsGrid[iy] || [];
                                                        for (let ix = 0; ix < sDiag; ix++) {
                                                            const rawA2 = nRow2[ix];
                                                            const rawB2 = jRow2[ix];
                                                            if (rawA2 === null || rawA2 === undefined || rawA2 === '' || rawB2 === null || rawB2 === undefined || rawB2 === '') continue;
                                                            const a2 = Number(rawA2);
                                                            const b2 = Number(rawB2);
                                                            if (!Number.isFinite(a2) || !Number.isFinite(b2)) continue;
                                                            const bw2 = b2 / wavelength;
                                                            const e2 = a2 - (fitScale * bw2 + fitOffset);
                                                            fitSumSq += e2 * e2;
                                                            fitCount++;
                                                        }
                                                    }
                                                    fitResidualRms = fitCount > 0 ? Math.sqrt(fitSumSq / fitCount) : NaN;
                                                }
                                            }

                                            let rawCount = 0;
                                            let rawSumAbs = 0;
                                            let rawSumSq = 0;
                                            let rawMaxAbs = 0;
                                            for (let iy = 0; iy < sDiag; iy++) {
                                                const nRowRaw = Array.isArray(nativeOpdResp.rawOpdGrid) ? (nativeOpdResp.rawOpdGrid[iy] || []) : [];
                                                const jRowRaw = jsRawGrid[iy] || [];
                                                for (let ix = 0; ix < sDiag; ix++) {
                                                    const rawARaw = nRowRaw[ix];
                                                    const rawBRaw = jRowRaw[ix];
                                                    if (rawARaw === null || rawARaw === undefined || rawARaw === '' || rawBRaw === null || rawBRaw === undefined || rawBRaw === '') continue;
                                                    const aRaw = Number(rawARaw);
                                                    const bRaw = Number(rawBRaw);
                                                    if (!Number.isFinite(aRaw) || !Number.isFinite(bRaw)) continue;
                                                    const dRawWaves = aRaw - (bRaw / wavelength);
                                                    const adRaw = Math.abs(dRawWaves);
                                                    rawSumAbs += adRaw;
                                                    rawSumSq += dRawWaves * dRawWaves;
                                                    if (adRaw > rawMaxAbs) rawMaxAbs = adRaw;
                                                    rawCount++;
                                                }
                                            }

                                            const rawDiag = (rawCount > 0)
                                                ? {
                                                    count: rawCount,
                                                    rmsWaves: Math.sqrt(rawSumSq / rawCount),
                                                    meanAbsWaves: rawSumAbs / rawCount,
                                                    maxAbsWaves: rawMaxAbs,
                                                }
                                                : null;

                                            const maxAllowedRmsWaves = (() => {
                                                const v = Number((typeof window !== 'undefined')
                                                    ? window.__PSF_POPUP_NATIVE_OPD_PARITY_MAX_RMS_WAVES
                                                    : NaN);
                                                return (Number.isFinite(v) && v > 0) ? v : 5e-2;
                                            })();
                                            nativeOpdDiag = {
                                                count,
                                                rmsWaves: Math.sqrt(sumSq / count),
                                                meanAbsWaves: sumAbs / count,
                                                maxAbsWaves: maxAbs,
                                                maxLoc: { ix: maxIx, iy: maxIy },
                                                radialBands: {
                                                    center: {
                                                        count: radial.center.count,
                                                        rmsWaves: radial.center.count > 0 ? Math.sqrt(radial.center.sumSq / radial.center.count) : NaN,
                                                        meanAbsWaves: radial.center.count > 0 ? (radial.center.sumAbs / radial.center.count) : NaN,
                                                    },
                                                    mid: {
                                                        count: radial.mid.count,
                                                        rmsWaves: radial.mid.count > 0 ? Math.sqrt(radial.mid.sumSq / radial.mid.count) : NaN,
                                                        meanAbsWaves: radial.mid.count > 0 ? (radial.mid.sumAbs / radial.mid.count) : NaN,
                                                    },
                                                    edge: {
                                                        count: radial.edge.count,
                                                        rmsWaves: radial.edge.count > 0 ? Math.sqrt(radial.edge.sumSq / radial.edge.count) : NaN,
                                                        meanAbsWaves: radial.edge.count > 0 ? (radial.edge.sumAbs / radial.edge.count) : NaN,
                                                    },
                                                },
                                                fit: {
                                                    scale: fitScale,
                                                    offsetWaves: fitOffset,
                                                    residualRmsWaves: fitResidualRms,
                                                },
                                                rawParity: rawDiag,
                                            };
                                            const diagLine = '📐 [PSF popup][OPD parity] Native-vs-JS count=' + count +
                                                ' rms=' + Number(nativeOpdDiag.rmsWaves).toExponential(3) + 'λ' +
                                                ' meanAbs=' + Number(nativeOpdDiag.meanAbsWaves).toExponential(3) + 'λ' +
                                                ' maxAbs=' + Number(nativeOpdDiag.maxAbsWaves).toExponential(3) + 'λ' +
                                                ' @(' + String(maxIx) + ',' + String(maxIy) + ')' +
                                                ' fitScale=' + (Number.isFinite(fitScale) ? Number(fitScale).toExponential(3) : 'NaN') +
                                                ' fitOffset=' + (Number.isFinite(fitOffset) ? Number(fitOffset).toExponential(3) : 'NaN') + 'λ' +
                                                ' fitResidual=' + (Number.isFinite(fitResidualRms) ? Number(fitResidualRms).toExponential(3) : 'NaN') + 'λ' +
                                                (rawDiag
                                                    ? (' | raw rms=' + Number(rawDiag.rmsWaves).toExponential(3) + 'λ meanAbs=' + Number(rawDiag.meanAbsWaves).toExponential(3) + 'λ maxAbs=' + Number(rawDiag.maxAbsWaves).toExponential(3) + 'λ')
                                                    : '');
                                            const hasReliableRawParity = !!(rawDiag && Number.isFinite(Number(rawDiag.rmsWaves)) && Number(rawDiag.count) >= 64);
                                            const hasReliableDisplayParity = Number.isFinite(Number(nativeOpdDiag.rmsWaves)) && Number(count) >= 64;
                                            const parityForFallback = hasReliableRawParity
                                                ? Number(rawDiag.rmsWaves)
                                                : (hasReliableDisplayParity ? Number(nativeOpdDiag.rmsWaves) : NaN);
                                            const shouldFallbackToJsOpd = Number.isFinite(parityForFallback)
                                                ? (parityForFallback > maxAllowedRmsWaves)
                                                : false;

                                            if (shouldFallbackToJsOpd) {
                                                console.warn('⚠️ ' + diagLine);
                                            } else {
                                                console.log(diagLine);
                                            }
                                            try {
                                                if (window.opener && window.opener.console) {
                                                    const logFn = shouldFallbackToJsOpd
                                                        ? window.opener.console.warn
                                                        : window.opener.console.log;
                                                    if (typeof logFn === 'function') {
                                                        logFn.call(window.opener.console, (shouldFallbackToJsOpd ? '⚠️ ' : '') + diagLine);
                                                    }
                                                }
                                            } catch (_) {}

                                            try {
                                                window['__lastOpdParityDiffWaves'] = diffGridWaves;
                                                window['__lastOpdParitySummary'] = nativeOpdDiag;
                                                window['__lastOpdParityRawSummary'] = rawDiag;
                                                if (window.opener) {
                                                    window.opener['__lastOpdParitySummary'] = nativeOpdDiag;
                                                    window.opener['__lastOpdParityRawSummary'] = rawDiag;
                                                }
                                            } catch (_) {}

                                            await renderOpdParityHeatmap(diffGridWaves, nativeOpdDiag);

                                            if (shouldFallbackToJsOpd) {
                                                try {
                                                    const fbMsg = '⚠️ [PSF popup] Native OPD parity exceeded threshold (rms=' +
                                                        Number(parityForFallback).toExponential(3) + 'λ > ' +
                                                        Number(maxAllowedRmsWaves).toExponential(3) + 'λ). Falling back to JS OPD.';
                                                    console.warn(fbMsg);
                                                    if (window.opener && window.opener.console && typeof window.opener.console.warn === 'function') {
                                                        window.opener.console.warn(fbMsg);
                                                    }
                                                } catch (_) {}
                                                nativeOpdResp = null;
                                            }
                                        }
                                    }
                                }
                            } catch (diagErr) {
                                try {
                                    if (String(diagErr?.message || '') !== '__skip_opd_parity_diag__') {
                                        // Non-fatal diagnostic path failure should not block native pipeline.
                                        console.warn('⚠️ [PSF popup] OPD parity diagnostic skipped due to error:', diagErr);
                                    }
                                } catch (_) {}
                            }

                            if (nativeOpdResp) {
                                const nativePupilMode = String(nativeOpdResp?.pupilSamplingMode || '').toLowerCase();
                                wavefrontMap = {
                                    pupilSamplingMode: nativePupilMode === 'stop' ? 'stop' : 'entrance',
                                    pupilPhysicalRadiusMm: NaN,
                                    entranceEffectiveRadiusMm: NaN,
                                    pupilRange: 1.0,
                                    pupilCoordinates: [],
                                    display: { opds: [] },
                                    opds: []
                                };
                            }
                        } catch (nativeErr) {
                            nativeOpdResp = null;
                            try {
                                const msg = '⚠️ [PSF popup] Native OPD failed, falling back to JS OPD: ' + String(nativeErr?.message || nativeErr || 'unknown');
                                console.warn(msg);
                                try {
                                    if (window.opener && window.opener.console && typeof window.opener.console.warn === 'function') {
                                        window.opener.console.warn(msg);
                                    }
                                } catch (_) {}
                            } catch (_) {}
                        }
                    }

                    if (!nativeOpdResp) {
                        const host = window.opener || window;
                        const calculatorFactory =
                            (host && typeof host.createOPDCalculator === 'function' && host.createOPDCalculator)
                            || (typeof window.createOPDCalculator === 'function' && window.createOPDCalculator)
                            || (typeof createOPDCalculator === 'function' ? createOPDCalculator : null);
                        const analyzerFactory =
                            (host && typeof host.createWavefrontAnalyzer === 'function' && host.createWavefrontAnalyzer)
                            || (typeof window.createWavefrontAnalyzer === 'function' && window.createWavefrontAnalyzer)
                            || (typeof createWavefrontAnalyzer === 'function' ? createWavefrontAnalyzer : null);
                        const opdCalculator = calculatorFactory ? calculatorFactory(opticalSystemRows, wavelength) : null;
                        if (!opdCalculator) {
                            throw new Error('createOPDCalculator is not available');
                        }
                        const analyzer = analyzerFactory ? analyzerFactory(opdCalculator) : null;
                        if (!analyzer) {
                            throw new Error('WavefrontAberrationAnalyzer is not available');
                        }

                        wavefrontMap = await raceWithCancel(analyzer.generateWavefrontMap(fieldSetting, wavefrontGridSize, 'circular', {
                            recordRays: false,
                            progressEvery: 0,
                            renderFromZernike: false,
                            skipZernikeFit: true,
                            opdMode: 'simple',
                            opdDisplayMode,
                            diagnoseDiscontinuities: PSF_DEBUG,
                            diagTopK: 8,
                            cancelToken: activeCancelToken,
                            onProgress: (evt) => {
                                const p = Number(evt && evt.percent);
                                const msg = (evt && evt.message) || (evt && evt.phase) || 'OPD...';
                                const phase = (evt && evt.phase) ? evt.phase : 'opd';
                                if (Number.isFinite(p)) onProgress({ percent: Math.max(0, Math.min(80, p * 0.8)), phase, message: msg });
                                else onProgress({ percent: null, phase, message: msg });
                            }
                        }), activeCancelToken);
                        throwIfCancelled(activeCancelToken);

                        if (wavefrontMap && wavefrontMap.error) {
                            const err = new Error((wavefrontMap.error && wavefrontMap.error.message) ? wavefrontMap.error.message : 'Wavefront generation failed (popup).');
                            err.code = 'WAVEFRONT_UNAVAILABLE';
                            err.wavefrontError = wavefrontMap.error;
                            throw err;
                        }
                    }

                    if (PSF_DEBUG) {
                    }
                    
                    // CRITICAL: Use actual entrance pupil radius for spatial frequency scaling
                    // In entrance pupil mode, pupilPhysicalRadiusMm (stop radius) != actual entrance pupil radius
                    const actualPupilRadiusMm = (wavefrontMap.pupilSamplingMode === 'entrance' && 
                                                  Number.isFinite(wavefrontMap.entranceEffectiveRadiusMm))
                        ? wavefrontMap.entranceEffectiveRadiusMm
                        : wavefrontMap.pupilPhysicalRadiusMm;
                    

                    // Convert to PSF calculator format (gridData)
                    // Build OPD grid from the wavefront map samples (piston+tilt removed display OPD).
                    const gridSize = wavefrontGridSize;
                    const s = Math.max(2, Math.floor(Number(gridSize)));
                    const opdGrid = Array.from({ length: s }, () => new Float32Array(s));
                    const ampGrid = Array.from({ length: s }, () => new Float32Array(s));
                    const maskGrid = Array.from({ length: s }, () => Array(s).fill(false));
                    const xCoords = new Float32Array(s);
                    const yCoords = new Float32Array(s);

                    const pupilRange = (Number.isFinite(Number(wavefrontMap?.pupilRange)) && Number(wavefrontMap.pupilRange) > 0)
                        ? Number(wavefrontMap.pupilRange)
                        : 1.0;
                    for (let i = 0; i < s; i++) {
                        const t = (i / (s - 1 || 1)) * 2 - 1;
                        xCoords[i] = t * pupilRange;
                        yCoords[i] = t * pupilRange;
                    }

                    let rayData = [];
                    let coords = [];
                    let opdMicrons = [];
                    if (nativeOpdResp && Array.isArray(nativeOpdResp.displayOpdGrid)) {
                        for (let iy = 0; iy < s; iy++) {
                            const rowDisplay = nativeOpdResp.displayOpdGrid[iy] || [];
                            const rowRaw = Array.isArray(nativeOpdResp.rawOpdGrid) ? (nativeOpdResp.rawOpdGrid[iy] || []) : [];
                            for (let ix = 0; ix < s; ix++) {
                                const rawCell = rowRaw[ix];
                                if (rawCell === null || rawCell === undefined || rawCell === '') continue;
                                const vRawWaves = Number(rawCell);
                                // IMPORTANT: Use raw OPD occupancy as the pupil validity mask.
                                // Native display grid can be detrended/fitted and become dense, which
                                // incorrectly turns PSF pupil mask into full grid.
                                if (!Number.isFinite(vRawWaves)) continue;

                                const displayCell = rowDisplay[ix];
                                const vDisplayWaves = (displayCell === null || displayCell === undefined || displayCell === '')
                                    ? NaN
                                    : Number(displayCell);
                                const vWaves = Number.isFinite(vDisplayWaves) ? vDisplayWaves : vRawWaves;
                                // Native OPD map grid values are in waves; PSF calculator expects microns.
                                const vMicrons = vWaves * wavelength;
                                coords.push({ ix, iy, x: Number(xCoords[ix]), y: Number(yCoords[iy]) });
                                opdMicrons.push(vMicrons);
                            }
                        }
                    } else {
                        coords = Array.isArray(wavefrontMap?.pupilCoordinates) ? wavefrontMap.pupilCoordinates : [];
                        opdMicrons = (wavefrontMap?.display && Array.isArray(wavefrontMap.display.opds))
                            ? wavefrontMap.display.opds
                            : (Array.isArray(wavefrontMap?.opds) ? wavefrontMap.opds : []);
                    }

                    const n = Math.min(coords.length, opdMicrons.length);
                    for (let k = 0; k < n; k++) {
                        const c = coords[k];
                        const ix = Number.isInteger(c?.ix) ? c.ix : null;
                        const iy = Number.isInteger(c?.iy) ? c.iy : null;
                        if (ix === null || iy === null) continue;
                        if (ix < 0 || ix >= s || iy < 0 || iy >= s) continue;
                        const vMicrons = Number(opdMicrons[k]);
                        if (!Number.isFinite(vMicrons)) continue;
                        maskGrid[iy][ix] = true;
                        opdGrid[iy][ix] = vMicrons;
                        ampGrid[iy][ix] = 1.0;
                    }

                    for (let k = 0; k < n; k++) {
                        const c = coords[k];
                        const x = Number(c?.x);
                        const y = Number(c?.y);
                        const vMicrons = Number(opdMicrons[k]);
                        if (!Number.isFinite(x) || !Number.isFinite(y) || !Number.isFinite(vMicrons)) continue;
                        rayData.push({
                            pupilX: x,
                            pupilY: y,
                            opd: vMicrons,
                            isVignetted: false
                        });
                    }

                    let filledCount = 0;
                    for (let iy = 0; iy < s; iy++) {
                        for (let ix = 0; ix < s; ix++) {
                            if (maskGrid[iy][ix]) filledCount++;
                        }
                    }

                    const opdData = {
                        gridSize: s,
                        wavelength: wavelength,
                        rayData,
                        gridData: {
                            opd: opdGrid,
                            amplitude: ampGrid,
                            pupilMask: maskGrid,
                            xCoords,
                            yCoords
                        }
                    };

                    if (PSF_DEBUG) try {
                        let valid = 0;
                        let sum = 0;
                        let sum2 = 0;
                        let min = Infinity;
                        let max = -Infinity;
                        for (let iy = 0; iy < s; iy++) {
                            for (let ix = 0; ix < s; ix++) {
                                if (!maskGrid[iy][ix]) continue;
                                const v = Number(opdGrid[iy][ix]);
                                if (!Number.isFinite(v)) continue;
                                valid++;
                                sum += v;
                                sum2 += v * v;
                                if (v < min) min = v;
                                if (v > max) max = v;
                            }
                        }
                        const mean = valid ? (sum / valid) : NaN;
                        const rms = valid ? Math.sqrt(Math.max(0, sum2 / valid - mean * mean)) : NaN;
                        const ptp = (Number.isFinite(min) && Number.isFinite(max)) ? (max - min) : NaN;
                        const rmsW = (Number.isFinite(rms) && Number.isFinite(wavelength) && wavelength > 0) ? (rms / wavelength) : NaN;
                        const ptpW = (Number.isFinite(ptp) && Number.isFinite(wavelength) && wavelength > 0) ? (ptp / wavelength) : NaN;
                        const line = '📌 [PSF popup] OPD grid stats: valid=' + valid + '/' + (s * s) +
                            ' (' + (100 * valid / (s * s)).toFixed(1) + '%)' +
                            ' rms=' + (Number.isFinite(rms) ? rms.toExponential(3) : String(rms)) + 'µm' +
                            ' (' + (Number.isFinite(rmsW) ? rmsW.toExponential(3) : String(rmsW)) + 'λ)' +
                            ' ptp=' + (Number.isFinite(ptp) ? ptp.toExponential(3) : String(ptp)) + 'µm' +
                            ' (' + (Number.isFinite(ptpW) ? ptpW.toExponential(3) : String(ptpW)) + 'λ)';
                        console.log(line);
                        try {
                            if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                window.opener.console.log(line);
                            }
                        } catch (_) {}
                        if (wavefrontMap?.pupilMaskStats) {
                            console.log('📌 [PSF popup] pupilMaskStats:', wavefrontMap.pupilMaskStats);
                        }

                        try {
                            const mode = wavefrontMap && wavefrontMap.pupilSamplingMode;
                            const bestEffort = !!(wavefrontMap && wavefrontMap.bestEffortVignettedPupil);
                            if (mode) {
                                const msg = '📌 [PSF popup] pupilSamplingMode=' + String(mode) + (bestEffort ? ' (bestEffortVignettedPupil=true)' : '');
                                console.log(msg);
                                try {
                                    if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                        window.opener.console.log(msg);
                                    }
                                } catch (_) {}
                            }

                            const reasons = (wavefrontMap && wavefrontMap.invalidReasonCounts) ? wavefrontMap.invalidReasonCounts : null;
                            const top = reasons
                                ? Object.entries(reasons).sort((a, b) => ((b && b[1]) || 0) - ((a && a[1]) || 0)).slice(0, 8)
                                : [];
                            if (top.length) {
                                const msg = '📌 [PSF popup] invalid reasons top: ' + top.map((kv) => String(kv && kv[0]) + ':' + String(kv && kv[1])).join(', ');
                                console.log(msg);
                                try {
                                    if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                        window.opener.console.log(msg);
                                    }
                                } catch (_) {}
                            }
                        } catch (_) {}
                    } catch (_) {}

                    let pupilDiameterMm = actualPupilRadiusMm * 2;
                    let focalLengthMm = Number.NaN;
                    let stopIndexForLog = -1;

                    try {
                        const si = Number(findStopSurfaceIndex(opticalSystemRows));
                        if (Number.isFinite(si)) stopIndexForLog = si;
                    } catch (_) {}

                    try {
                        const diffParams = calculateImageSpaceDiffractionParams(opticalSystemRows, wavelength);
                        const fWork = Number(diffParams?.fNumberWorking);
                        const derivedFocal = Number(diffParams?.focalLengthMm);

                        if (Number.isFinite(fWork) && fWork > 0 && Number.isFinite(derivedFocal) && derivedFocal > 0) {
                            focalLengthMm = Math.abs(derivedFocal);
                            pupilDiameterMm = focalLengthMm / fWork;
                        }
                    } catch (_) {}

                    // Fallback to stop diameter only when paraxial/image-space data is unavailable.
                    if (!(Number.isFinite(pupilDiameterMm) && pupilDiameterMm > 0)) {
                        try {
                            const si = Number((window.opener && typeof window.opener.findStopSurfaceIndex === 'function')
                                ? window.opener.findStopSurfaceIndex(opticalSystemRows)
                                : findStopSurfaceIndex(opticalSystemRows));
                            const stopRow = (Number.isFinite(si) && si >= 0) ? opticalSystemRows?.[si] : null;
                            const sdRaw = stopRow?.semidia ?? stopRow?.Semidia ?? stopRow?.['Semi Diameter'] ?? stopRow?.aperture ?? stopRow?.Aperture ?? NaN;
                            const sd = Math.abs(parseFloat(sdRaw));
                            if (Number.isFinite(sd) && sd > 0) {
                                const isApertureField = !!(stopRow && (stopRow.aperture !== undefined || stopRow.Aperture !== undefined));
                                const stopRadiusMm = isApertureField ? (sd * 0.5) : sd;
                                if (Number.isFinite(stopRadiusMm) && stopRadiusMm > 0) {
                                    pupilDiameterMm = stopRadiusMm * 2;
                                }
                            }
                        } catch (_) {}
                    }

                    if (!(Number.isFinite(focalLengthMm) && focalLengthMm > 0)) {
                        // Last-resort safeguard to keep FFT scaling defined even when paraxial FL is unavailable.
                        focalLengthMm = 100.0;
                    }

                    logScaleInputs(pupilDiameterMm, focalLengthMm, stopIndexForLog);

                    const psfSamplingSize = Number.isFinite(zernikeSampling) ? zernikeSampling : 128;
                    const zeroPadTo = (zeroPadRaw === 'none')
                        ? psfSamplingSize
                        : (zeroPadRaw === 'auto')
                            ? 0
                            : (Number.isFinite(parseInt(zeroPadRaw)) ? parseInt(zeroPadRaw) : 0);
                    const canUseNativePsfMap = !!(window.opener && typeof window.opener.runDesktopNativePsfMapForPopup === 'function');
                    if (!canUseNativePsfMap) {
                        throw new Error('Native Rust PSF map path is required but unavailable.');
                    }

                    const minRecommendedFftSize = 512;
                    const requestedFftSize = (!zeroPadTo || zeroPadTo === 0)
                        ? Math.max(psfSamplingSize, minRecommendedFftSize)
                        : Math.max(psfSamplingSize, zeroPadTo);
                    const basePixelPitchUm = (Number(wavelength) * Math.abs(Number(focalLengthMm))) / Math.max(1e-12, Math.abs(Number(pupilDiameterMm)));
                    const pixelSizeUm = basePixelPitchUm * (psfSamplingSize / requestedFftSize);

                    onProgress({ percent: 80, phase: 'psf', message: 'PSF (native)...' });
                    const nativePsfResp = await raceWithCancel(window.opener.runDesktopNativePsfMapForPopup({
                        gridOpd: Array.from({ length: s }, (_, iy) => Array.from(opdGrid[iy] || [])),
                        gridAmplitude: Array.from({ length: s }, (_, iy) => Array.from(ampGrid[iy] || [])),
                        pupilMask: Array.from({ length: s }, (_, iy) => Array.from(maskGrid[iy] || [])),
                        wavelengthUm: wavelength,
                        pixelSizeUm,
                        removeTilt: false,
                        zeroPadTo: requestedFftSize,
                        recenterIfWrapped: false,
                    }), activeCancelToken);
                    const psfResult = {
                        psfData: nativePsfResp?.psfData,
                        metrics: nativePsfResp?.metrics,
                        samplingSize: psfSamplingSize,
                        wavelength,
                        gridData: opdData?.gridData,
                        options: { pupilDiameter: pupilDiameterMm, focalLength: focalLengthMm, pixelSize: pixelSizeUm },
                        metadata: {
                            method: 'native-rust-psf-map',
                            backend: nativePsfResp?.backend,
                            samplingSize: psfSamplingSize,
                            fftSize: nativePsfResp?.fftSize,
                            wavelength,
                            pixelSize: pixelSizeUm,
                        },
                        implementationUsed: 'NativeRust',
                    };
                    throwIfCancelled(activeCancelToken);

                    setPopupPsfBadge('');

                    try {
                        const impl = String(psfResult?.implementationUsed || 'unknown');
                        const method = String(psfResult?.metadata?.method || '');
                        const implLine = '🧪 [PSF popup] implementation=' + impl + (method ? (' method=' + method) : '');
                        const pixelSizeUm = Number(psfResult?.options?.pixelSize);
                        const scaleLine = '📏 [PSF popup] scale pupilDiameterMm=' + Number(pupilDiameterMm).toFixed(6)
                            + ' focalLengthMm=' + Number(focalLengthMm).toFixed(6)
                            + ' pixelSizeUm=' + (Number.isFinite(pixelSizeUm) ? pixelSizeUm.toFixed(6) : 'n/a');
                        console.log(implLine);
                        console.log(scaleLine);
                        try {
                            window['__lastPsfImplLine'] = implLine;
                            window['__lastPsfScaleLine'] = scaleLine;
                        } catch (_) {}
                        try {
                            if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                window.opener.console.log(implLine);
                                window.opener.console.log(scaleLine);
                            }
                        } catch (_) {}

                        const statsEl = document.getElementById('popup-psf-container-stats');
                        if (statsEl) {
                            statsEl.innerHTML = '';
                            statsEl.textContent = '';
                            statsEl.style.display = 'none';
                            if (nativeOpdDiag && Number.isFinite(nativeOpdDiag.rmsWaves)) {
                                const maxAllowedRmsWaves = (() => {
                                    const v = Number((typeof window !== 'undefined')
                                        ? window.__PSF_POPUP_NATIVE_OPD_PARITY_MAX_RMS_WAVES
                                        : NaN);
                                    return (Number.isFinite(v) && v > 0) ? v : 5e-2;
                                })();
                                const warn = Number(nativeOpdDiag.rmsWaves) > maxAllowedRmsWaves;
                                const prefix = warn ? '⚠️ ' : 'ℹ️ ';
                                const centerRms = Number(nativeOpdDiag?.radialBands?.center?.rmsWaves);
                                const midRms = Number(nativeOpdDiag?.radialBands?.mid?.rmsWaves);
                                const edgeRms = Number(nativeOpdDiag?.radialBands?.edge?.rmsWaves);
                                const maxIx = Number(nativeOpdDiag?.maxLoc?.ix);
                                const maxIy = Number(nativeOpdDiag?.maxLoc?.iy);
                                const fitScale = Number(nativeOpdDiag?.fit?.scale);
                                const fitOffset = Number(nativeOpdDiag?.fit?.offsetWaves);
                                const fitResidual = Number(nativeOpdDiag?.fit?.residualRmsWaves);
                                const rawRms = Number(nativeOpdDiag?.rawParity?.rmsWaves);
                                const rawMean = Number(nativeOpdDiag?.rawParity?.meanAbsWaves);
                                const rawMax = Number(nativeOpdDiag?.rawParity?.maxAbsWaves);
                                const bandMsg = ' bands(rms λ): C=' + (Number.isFinite(centerRms) ? centerRms.toExponential(2) : 'n/a')
                                    + ' M=' + (Number.isFinite(midRms) ? midRms.toExponential(2) : 'n/a')
                                    + ' E=' + (Number.isFinite(edgeRms) ? edgeRms.toExponential(2) : 'n/a');
                                const fitMsg = ' fit: s=' + (Number.isFinite(fitScale) ? fitScale.toExponential(2) : 'n/a')
                                    + ' b=' + (Number.isFinite(fitOffset) ? fitOffset.toExponential(2) : 'n/a') + 'λ'
                                    + ' rmsRes=' + (Number.isFinite(fitResidual) ? fitResidual.toExponential(2) : 'n/a') + 'λ';
                                const rawMsg = (Number.isFinite(rawRms) || Number.isFinite(rawMean) || Number.isFinite(rawMax))
                                    ? (' raw(rms/mean/max λ)=' +
                                        (Number.isFinite(rawRms) ? rawRms.toExponential(2) : 'n/a') + '/' +
                                        (Number.isFinite(rawMean) ? rawMean.toExponential(2) : 'n/a') + '/' +
                                        (Number.isFinite(rawMax) ? rawMax.toExponential(2) : 'n/a'))
                                    : '';
                                const msg = prefix + 'OPD parity (Native vs JS): ' +
                                    'rms=' + Number(nativeOpdDiag.rmsWaves).toExponential(3) + 'λ, ' +
                                    'meanAbs=' + Number(nativeOpdDiag.meanAbsWaves).toExponential(3) + 'λ, ' +
                                    'maxAbs=' + Number(nativeOpdDiag.maxAbsWaves).toExponential(3) + 'λ@(' + (Number.isFinite(maxIx) ? String(maxIx) : '?') + ',' + (Number.isFinite(maxIy) ? String(maxIy) : '?') + ')' +
                                    bandMsg + fitMsg + rawMsg;
                                void msg;
                                statsEl.style.color = warn ? '#b91c1c' : '#334155';
                            }
                        }
                    } catch (_) {}

                    try {
                        const enablePsfParityDiag = !!(typeof window !== 'undefined' && window.__PSF_POPUP_ENABLE_PSF_PARITY_DIAG === true);
                        if (!enablePsfParityDiag) {
                            throw new Error('__skip_psf_parity_diag__');
                        }

                        const isNative = String(psfResult?.implementationUsed || '').toLowerCase() === 'nativerust';
                        const parityEnabled = isPopupTauriRuntime() && isNative;
                        const parityRan = !!window['__psfNativeParityDone'];
                        if (parityEnabled && !parityRan) {
                            window['__psfNativeParityDone'] = true;
                            void (async () => {
                                try {
                                    const psfCalculator = (() => {
                                        const host = window.opener || window;
                                        const PsfCalculatorCtor =
                                            (host && host.PSFCalculator)
                                            || window.PSFCalculator;
                                        if (typeof PsfCalculatorCtor !== 'function') {
                                            throw new Error('PSFCalculator is not available');
                                        }
                                        return new PsfCalculatorCtor();
                                    })();
                                    const jsResult = await psfCalculator.calculatePSF(opdData, {
                                        samplingSize: psfSamplingSize,
                                        wavelength,
                                        zeroPadTo,
                                        pupilDiameter: pupilDiameterMm,
                                        focalLength: focalLengthMm,
                                        forceImplementation: 'javascript',
                                        removeTilt: false,
                                    });

                                    const nativeGrid = Array.isArray(psfResult?.psfData) ? psfResult.psfData : [];
                                    const jsGrid = Array.isArray(jsResult?.psfData) ? jsResult.psfData : [];
                                    const h = Math.min(nativeGrid.length, jsGrid.length);
                                    const w = h > 0 ? Math.min((nativeGrid[0] || []).length, (jsGrid[0] || []).length) : 0;

                                    let n = 0;
                                    let sumSq = 0;
                                    let maxAbs = 0;
                                    let sumAbs = 0;
                                    for (let iy = 0; iy < h; iy++) {
                                        for (let ix = 0; ix < w; ix++) {
                                            const a = Number(nativeGrid[iy]?.[ix]);
                                            const b = Number(jsGrid[iy]?.[ix]);
                                            if (!Number.isFinite(a) || !Number.isFinite(b)) continue;
                                            const d = a - b;
                                            const ad = Math.abs(d);
                                            sumSq += d * d;
                                            sumAbs += ad;
                                            if (ad > maxAbs) maxAbs = ad;
                                            n++;
                                        }
                                    }

                                    const rmsAbs = n > 0 ? Math.sqrt(sumSq / n) : NaN;
                                    const meanAbs = n > 0 ? (sumAbs / n) : NaN;

                                    const nMetrics = psfResult?.metrics || {};
                                    const jMetrics = jsResult?.metrics || {};
                                    const fwhmNative = Number(nMetrics?.fwhm?.average);
                                    const fwhmJs = Number(jMetrics?.fwhm?.average);
                                    const strehlNative = Number(nMetrics?.strehlRatio);
                                    const strehlJs = Number(jMetrics?.strehlRatio);
                                    const centerNative = nMetrics?.centerPosition || {};
                                    const centerJs = jMetrics?.centerPosition || {};

                                    const parityLine =
                                        '📊 [PSF Parity] native-vs-js ' +
                                        'grid=' + h + 'x' + w + ' n=' + n + ' ' +
                                        'rmsAbs=' + (Number.isFinite(rmsAbs) ? rmsAbs.toExponential(4) : 'NaN') + ' ' +
                                        'meanAbs=' + (Number.isFinite(meanAbs) ? meanAbs.toExponential(4) : 'NaN') + ' ' +
                                        'maxAbs=' + (Number.isFinite(maxAbs) ? maxAbs.toExponential(4) : 'NaN') + ' ' +
                                        'strehlΔ=' + (strehlNative - strehlJs).toExponential(4) + ' ' +
                                        'fwhmAvgΔ=' + (fwhmNative - fwhmJs).toExponential(4) + 'µm ' +
                                        'centerΔ=(' + (Number(centerNative?.x) - Number(centerJs?.x)) + ',' + (Number(centerNative?.y) - Number(centerJs?.y)) + ')';

                                    const paritySummary = {
                                        grid: { h, w, n },
                                        abs: {
                                            rms: Number.isFinite(rmsAbs) ? rmsAbs : null,
                                            mean: Number.isFinite(meanAbs) ? meanAbs : null,
                                            max: Number.isFinite(maxAbs) ? maxAbs : null,
                                        },
                                        delta: {
                                            strehl: Number.isFinite(strehlNative) && Number.isFinite(strehlJs) ? (strehlNative - strehlJs) : null,
                                            fwhmAvgUm: Number.isFinite(fwhmNative) && Number.isFinite(fwhmJs) ? (fwhmNative - fwhmJs) : null,
                                            centerX: Number(centerNative?.x) - Number(centerJs?.x),
                                            centerY: Number(centerNative?.y) - Number(centerJs?.y),
                                        },
                                    };

                                    try {
                                        window['__lastPsfParityLine'] = parityLine;
                                        window['__lastPsfParity'] = paritySummary;
                                        if (window.opener) {
                                            window.opener['__lastPsfParityLine'] = parityLine;
                                            window.opener['__lastPsfParity'] = paritySummary;
                                        }
                                    } catch (_) {}

                                    console.log(parityLine);
                                    try {
                                        if (window.opener && window.opener.console && typeof window.opener.console.log === 'function') {
                                            window.opener.console.log(parityLine);
                                        }
                                    } catch (_) {}
                                } catch (parityErr) {
                                    console.warn('⚠️ [PSF Parity] native-vs-js compare failed:', parityErr);
                                }
                            })();
                        }
                    } catch (parityDiagErr) {
                        try {
                            if (String(parityDiagErr?.message || '') !== '__skip_psf_parity_diag__') {
                                console.warn('⚠️ [PSF popup] PSF parity diagnostic skipped due to error:', parityDiagErr);
                            }
                        } catch (_) {}
                    }

                    const plotter = (() => {
                        if (window.opener && window.opener.PSFPlotter) {
                            return new window.opener.PSFPlotter(containerEl);
                        }
                        throw new Error('PSFPlotter not available from opener window');
                    })();
                    await plotter.plot2DPSF(psfResult, { logScale, title: '', recenterToCentroid: false });
                }

                hideProgress();
                resizePlot();
                hideProgress();
            } catch (err) {
                setPopupPsfBadge('Error');
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    const msg = String((err && err.message) || err || 'Unknown error');
                    const stack = String((err && err.stack) || '');
                    const escapeHtml = (s) => String(s).replace(/[&<>"']/g, (ch) => {
                        switch (ch) {
                            case '&': return '&amp;';
                            case '<': return '&lt;';
                            case '>': return '&gt;';
                            case '"': return '&quot;';
                            case "'": return '&#39;';
                            default: return ch;
                        }
                    });
                    const details = escapeHtml(msg + (stack ? '\\n\\n' + stack : ''));
                    containerEl.innerHTML =
                        '<div style="padding:20px;color:red;font-family:Arial;">' +
                            '<div style="font-weight:bold;margin-bottom:8px;">Failed to generate PSF</div>' +
                            '<pre style="white-space:pre-wrap;word-break:break-word;">' + details + '</pre>' +
                        '</div>';
                }
            } finally {
                try {
                    if (stopBtn) stopBtn.disabled = true;
                } catch (_) {}
            }
        };

        document.getElementById('popup-show-psf-btn').addEventListener('click', () => window.renderPSF());

        document.getElementById('popup-stop-psf-btn').addEventListener('click', () => {
            try {
                const el = document.getElementById('popup-psf-pipeline-badge');
                if (el) el.textContent = '';
            } catch (_) {}
            try {
                if (activeCancelToken && typeof activeCancelToken.abort === 'function') {
                    activeCancelToken.abort('Stopped by user');
                }
            } catch (_) {}
        });

        function syncAll() {
            syncObjectOptionsFromOpener();
            syncInputsFromOpener();
        }
        // Expose for opener-triggered refresh when reusing an existing popup window.
        window['syncAll'] = syncAll;
        window['syncObjectOptionsFromOpener'] = syncObjectOptionsFromOpener;
        window.addEventListener('resize', resizePlot);
        window.addEventListener('focus', syncAll);
        syncAll();

        // Do not auto-render on open; user triggers calculation via "Show PSF".
        window.addEventListener('load', () => {
            try {
                const popupSampling = document.getElementById('popup-psf-sampling-select');
                const popupZernikeSampling = document.getElementById('popup-psf-zernike-sampling-select');
                const popupLog = document.getElementById('popup-psf-log-scale-checkbox');
                if (popupSampling && !popupSampling.value) popupSampling.value = 'auto';
                if (popupZernikeSampling && !popupZernikeSampling.value) popupZernikeSampling.value = '256';
                if (popupLog && typeof popupLog.checked !== 'boolean') popupLog.checked = false;
            } catch (_) {}
        });
    <\/script>
</body>
</html>
                        `);try{f.document.close()}catch{}}),T.runPsfParityCapture=async(f={})=>{const w=f||{},b=Math.max(1e3,Number(w.timeoutMs)||15e3),v=Math.max(50,Number(w.pollMs)||120),M=Date.now();try{(!T.__psfPopup||T.__psfPopup.closed)&&d.click()}catch{}for(;(!T.__psfPopup||T.__psfPopup.closed)&&Date.now()-M<b;)await new Promise(B=>setTimeout(B,v));const C=T.__psfPopup;if(!C||C.closed)throw new Error("PSF popup could not be opened. Please open it manually and run again.");for(;typeof C.renderPSF!="function"&&Date.now()-M<b;)await new Promise(B=>setTimeout(B,v));if(typeof C.renderPSF!="function")throw new Error("PSF popup is open but renderPSF is not ready yet. Try again in 1-2 seconds.");C.__PSF_DEBUG=!0,C.__PSF_USE_DESKTOP_ANALYSIS_PREVIEW=!1,C.__psfNativeParityDone=!1,await C.renderPSF();const D={implLine:C.__lastPsfImplLine||null,scaleLine:C.__lastPsfScaleLine||null,parityLine:C.__lastPsfParityLine||T.__lastPsfParityLine||null,parity:C.__lastPsfParity||T.__lastPsfParity||null};try{console.log("🧪 [runPsfParityCapture] result:",D)}catch{}return D};const S=document.getElementById("show-psf-btn");S&&!S.__cooptUnifiedPsfPipelineBound&&(S.__cooptUnifiedPsfPipelineBound=!0,S.addEventListener("click",async()=>{const f=v=>{try{const M=document.getElementById("psf-pipeline-badge");if(!M)return;const C=String(v||"");M.textContent=C,C?M.setAttribute("title",C+" (Main Show PSF uses Popup renderPSF)"):M.removeAttribute("title")}catch{}},w=v=>{try{const C=String(v?.__lastPsfImplLine||"").match(/implementation=([^\s]+)/),D=C&&C[1]?String(C[1]):"";return D?"Done ("+D+")":"Done"}catch{return"Done"}},b=async(v,M=12e3)=>{const C=Date.now();for(;Date.now()-C<M;){try{if(v&&!v.closed&&typeof v.renderPSF=="function")return!0}catch{}await new Promise(D=>setTimeout(D,80))}return!1};try{if(f("Running"),T.__psfPopup&&!T.__psfPopup.closed){try{typeof T.__psfPopup.syncAll=="function"&&T.__psfPopup.syncAll()}catch{}try{T.__psfPopup.focus()}catch{}if(typeof T.__psfPopup.renderPSF=="function"){await T.__psfPopup.renderPSF(),f(w(T.__psfPopup));return}}d.click();const v=T.__psfPopup;if(!await b(v,15e3))throw new Error("PSF popup is not ready.");try{typeof v.syncAll=="function"&&v.syncAll()}catch{}await v.renderPSF(),f(w(v))}catch(v){f("Error"),console.error("❌ [PSF Unified] Failed to render via popup pipeline:",v),alert(`PSF calculation failed: ${String(v?.message||v||"Unknown error")}`)}}));const N=document.getElementById("stop-psf-btn");N&&!N.__cooptUnifiedPsfPipelineBound&&(N.__cooptUnifiedPsfPipelineBound=!0,N.addEventListener("click",()=>{try{const f=document.getElementById("psf-pipeline-badge");f&&(f.textContent="")}catch{}try{const f=T.__psfPopup;if(!f||f.closed)return;const w=f.document&&f.document.getElementById("popup-stop-psf-btn");if(w&&typeof w.click=="function"){w.click();return}typeof f.activeCancelToken?.abort=="function"&&f.activeCancelToken.abort("Stopped by user")}catch{}}))}const u=document.getElementById("open-mtf-window-btn");u&&u.addEventListener("click",()=>{if(T.__mtfPopup&&!T.__mtfPopup.closed){try{T.__mtfPopup.focus()}catch{}return}const S=n("Modulation Transfer Function","width=800,height=600");if(!S){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__mtfPopup=S,S.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Modulation Transfer Function</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
            width: 120px;
        }
        .controls input[type="checkbox"] {
            width: auto;
            padding: 0;
            border: none;
            background: transparent;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .note { padding: 8px 12px; font-size: 12px; color: #666; border-bottom: 1px solid #eee; background: #fff; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-mtf-container { flex: 1 1 auto; min-height: 0; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-mtf-wavelength-select">Wavelength:</label>
        <select id="popup-mtf-wavelength-select"></select>
        <label for="popup-mtf-object-select">Object:</label>
        <select id="popup-mtf-object-select"></select>
        <label for="popup-mtf-max-freq-input">Max (lp/mm):</label>
        <input id="popup-mtf-max-freq-input" type="number" min="0" step="1" value="100" />
        <label for="popup-mtf-sampling-select">Sampling:</label>
        <select id="popup-mtf-sampling-select">
            <option value="32">32x32</option>
            <option value="64">64x64</option>
            <option value="128">128x128</option>
            <option value="256" selected>256x256</option>
            <option value="512">512x512</option>
            <option value="1024">1024x1024</option>
            <option value="2048">2048x2048</option>
            <option value="4096">4096x4096</option>
        </select>
        <label for="popup-mtf-zeropad-select" title="Zero-padding increases FFT size without increasing OPD ray grid.">Zero pad:</label>
        <select id="popup-mtf-zeropad-select">
            <option value="none">None</option>
            <option value="auto" selected>Auto</option>
            <option value="512">512</option>
            <option value="1024">1024</option>
            <option value="2048">2048</option>
            <option value="4096">4096</option>
        </select>
        <label style="display:flex;align-items:center;gap:6px;">
            <input id="popup-mtf-remove-ptd-checkbox" type="checkbox" />
            Remove P/T/D
        </label>
        <label style="display:flex;align-items:center;gap:6px;">
            <input id="popup-mtf-show-diff-limit-checkbox" type="checkbox" checked />
            Diffraction Limit
        </label>
        <button id="popup-show-mtf-btn" type="button">Show MTF</button>
    </div>
    <div id="popup-mtf-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-mtf-progress-text" style="margin-bottom: 6px;">Calculating MTF...</div>
        <progress id="popup-mtf-progress" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-mtf-container"></div>
    </div>

    <script>
        function safeCall(fn, fallback) {
            try { return fn(); } catch (_) { return fallback; }
        }

        function getOpener() {
            try { return window.opener || window; } catch (_) { return window; }
        }

        function popupLog(...args) {
            try { console.log(...args); } catch (_) {}
            try {
                const op = window.opener;
                if (op && op.console && typeof op.console.log === 'function') {
                    op.console.log(...args);
                }
            } catch (_) {}
        }

        function popupError(...args) {
            try { console.error(...args); } catch (_) {}
            try {
                const op = window.opener;
                if (op && op.console && typeof op.console.error === 'function') {
                    op.console.error(...args);
                }
            } catch (_) {}
        }

        function getPrimaryWavelength() {
            const opener = getOpener();
            if (!opener) return null;
            if (typeof opener.getPrimaryWavelength !== 'function') return null;
            const v = Number(safeCall(() => opener.getPrimaryWavelength(), 0));
            return Number.isFinite(v) && v > 0 ? v : null;
        }

        function buildWavelengthOptions() {
            const opener = getOpener();
            if (!opener) return [];
            const getSourceRows = opener.getSourceRows;
            const sources = (typeof getSourceRows === 'function')
                ? safeCall(() => getSourceRows(opener.tableSource), [])
                : [];
            const primary = getPrimaryWavelength();
            const out = [{ value: 'all', label: 'All' }];
            if (Array.isArray(sources) && sources.length > 0) {
                for (let i = 0; i < sources.length; i++) {
                    const wl = Number(sources[i]?.wavelength);
                    if (!Number.isFinite(wl) || wl <= 0) continue;
                    const nm = wl * 1000;
                    const label = Number.isFinite(primary) && Math.abs(wl - primary) < 1e-9
                        ? (nm.toFixed(1) + ' nm (primary)')
                        : (nm.toFixed(1) + ' nm');
                    out.push({ value: String(wl), label });
                }
            }
            if (out.length === 1) {
                if (Number.isFinite(primary) && primary > 0) {
                    out.push({ value: String(primary), label: ((primary * 1000).toFixed(1) + ' nm') });
                }
            }
            return out;
        }

        function buildObjectOptions() {
            const opener = getOpener();
            if (!opener) return [];
            const getObjectRows = opener.getObjectRows;
            const objects = (typeof getObjectRows === 'function')
                ? safeCall(() => getObjectRows(opener.tableObject), [])
                : [];
            const out = [];
            if (Array.isArray(objects) && objects.length > 0) {
                for (let i = 0; i < objects.length; i++) {
                    const obj = objects[i];
                    if (!obj) continue;
                    const typeRaw = String(obj.position ?? obj.object ?? obj.Object ?? obj.objectType ?? 'Point');
                    const x = (obj.x ?? obj.xHeightAngle ?? 0);
                    const y = (obj.y ?? obj.yHeightAngle ?? 0);
                    out.push({ value: String(i), label: (String(i + 1) + ': ' + typeRaw + ' (' + x + ', ' + y + ')') });
                }
            }
            if (out.length === 0) out.push({ value: '0', label: '0' });
            return out;
        }

        function populateSelect(selectEl, options) {
            if (!selectEl) return;
            const current = selectEl.value;
            selectEl.innerHTML = '';
            for (const opt of options) {
                const o = document.createElement('option');
                o.value = opt.value;
                o.textContent = opt.label;
                selectEl.appendChild(o);
            }
            if (current && Array.from(selectEl.options).some(o => o.value === current)) {
                selectEl.value = current;
            }
        }

        function syncAllOptions() {
            const wlSel = document.getElementById('popup-mtf-wavelength-select');
            const prevWl = wlSel ? wlSel.value : '';
            populateSelect(wlSel, buildWavelengthOptions());
            // Default to Primary (not All) on first open.
            if (wlSel && (!prevWl || !Array.from(wlSel.options).some(o => o.value === prevWl))) {
                const primary = getPrimaryWavelength();
                if (Number.isFinite(primary) && Array.from(wlSel.options).some(o => o.value === String(primary))) {
                    wlSel.value = String(primary);
                } else {
                    // Fallback to first numeric wavelength if present
                    const firstNumeric = Array.from(wlSel.options).find(o => o.value !== 'all');
                    if (firstNumeric) wlSel.value = firstNumeric.value;
                }
            }
            populateSelect(document.getElementById('popup-mtf-object-select'), buildObjectOptions());
        }

        window['renderMTF'] = async () => {
            const containerEl = document.getElementById('popup-mtf-container');
            if (containerEl) containerEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-mtf-progress-wrapper');
            const progressEl = document.getElementById('popup-mtf-progress');
            const progressTextEl = document.getElementById('popup-mtf-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressEl && Number.isFinite(value)) progressEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            const wlSel = document.getElementById('popup-mtf-wavelength-select');
            const objSel = document.getElementById('popup-mtf-object-select');
            const maxEl = document.getElementById('popup-mtf-max-freq-input');
            const samplingEl = document.getElementById('popup-mtf-sampling-select');
            const zeroPadEl = document.getElementById('popup-mtf-zeropad-select');
            const removePtdEl = document.getElementById('popup-mtf-remove-ptd-checkbox');
            const showDiffLimitEl = document.getElementById('popup-mtf-show-diff-limit-checkbox');

            const wlValue = wlSel ? String(wlSel.value) : '';
            const primary = getPrimaryWavelength();
            const wavelength = (wlValue === 'all') ? 'all' : Number(wlValue);
            const objectIndex = objSel ? parseInt(objSel.value, 10) : 0;
            const maxFreq = maxEl ? Number(maxEl.value) : 100;
            const sampling = samplingEl ? Number(samplingEl.value) : 256;
            const zeroPadRaw = zeroPadEl ? String(zeroPadEl.value || 'auto') : 'auto';
            const opdDisplayMode = (removePtdEl && removePtdEl.checked)
                ? 'pistonTiltDefocusRemoved'
                : 'pistonTiltRemoved';
            const showDiffractionLimit = !!(showDiffLimitEl && showDiffLimitEl.checked);
            const zeroPadTo = (zeroPadRaw === 'none')
                ? (Number.isFinite(sampling) ? sampling : 256)
                : (zeroPadRaw === 'auto')
                    ? 0
                    : (Number.isFinite(parseInt(zeroPadRaw, 10)) ? parseInt(zeroPadRaw, 10) : 0);

            try {
                const opener = getOpener();
                if (!opener) {
                    throw new Error('Opener is not available');
                }
                if (wavelength !== 'all' && !Number.isFinite(wavelength) && !(Number.isFinite(primary) && primary > 0)) {
                    throw new Error('Primary wavelength is unavailable. Please set Source Primary Wavelength.');
                }

                const shouldUseDesktopRust = (() => {
                    try {
                        if (typeof window !== 'undefined' && typeof window['shouldUseDesktopRustAnalysis'] === 'function') {
                            return !!window['shouldUseDesktopRustAnalysis']();
                        }
                        if (opener && typeof opener.shouldUseDesktopRustAnalysis === 'function') {
                            return !!opener.shouldUseDesktopRustAnalysis();
                        }
                        return true;
                    } catch (_) {
                        return false;
                    }
                })();
                const canUseDesktopRust = shouldUseDesktopRust && !!(
                    typeof opener.runDesktopNativeOpdMapForPopup === 'function'
                    && typeof opener.runDesktopNativePsfMapForPopup === 'function'
                    && typeof opener.runDesktopNativeMtfMapForPopup === 'function'
                );

                if (canUseDesktopRust) {
                    setProgress(0, 'Starting...');
                    await new Promise(r => setTimeout(r, 0));
                    const wavefrontGridSize = Number.isFinite(sampling) ? Math.max(32, Math.floor(sampling)) : 256;

                    const sourceRows = (typeof opener.getSourceRows === 'function')
                        ? (safeCall(() => opener.getSourceRows(opener.tableSource), []) || [])
                        : [];
                    const wavelengthList = (() => {
                        const out = [];
                        if (wavelength === 'all') {
                            if (Array.isArray(sourceRows) && sourceRows.length > 0) {
                                for (let i = 0; i < sourceRows.length; i++) {
                                    const wl = Number(sourceRows[i]?.wavelength);
                                    if (!Number.isFinite(wl) || wl <= 0) continue;
                                    if (out.some(v => Math.abs(v - wl) < 1e-9)) continue;
                                    out.push(wl);
                                }
                            }
                            if (out.length === 0 && Number.isFinite(primary) && primary > 0) {
                                out.push(primary);
                            }
                        } else {
                            const wl = (Number.isFinite(Number(wavelength)) && Number(wavelength) > 0)
                                ? Number(wavelength)
                                : ((Number.isFinite(primary) && primary > 0) ? primary : 0.5876);
                            out.push(wl);
                        }
                        if (out.length === 0) out.push(0.5876);
                        return out;
                    })();

                    const getColorForWavelengthPopup = (wl) => {
                        try {
                            if (typeof opener.getColorForWavelength === 'function') {
                                const c = opener.getColorForWavelength(wl);
                                if (typeof c === 'string' && c) return c;
                            }
                        } catch (_) {}
                        const nm = Number(wl) * 1000;
                        if (!Number.isFinite(nm)) return '#2563eb';
                        if (nm < 470) return '#2563eb';
                        if (nm < 530) return '#16a34a';
                        if (nm < 600) return '#f59e0b';
                        return '#dc2626';
                    };

                    const traces = [];
                    let nyquistGlobal = 0;
                    const selectedObjectIndex = Number.isFinite(objectIndex) ? objectIndex : 0;

                    for (let wli = 0; wli < wavelengthList.length; wli++) {
                        const wl = wavelengthList[wli];
                        const titleNm = (wl * 1000).toFixed(1);
                        const baseProgress = (wli / Math.max(1, wavelengthList.length)) * 80;
                        setProgress(10 + baseProgress, 'λ=' + titleNm + 'nm: OPD (Rust native)...');

                        const nativeOpdResp = await opener.runDesktopNativeOpdMapForPopup({
                            objectIndex: selectedObjectIndex,
                            gridSize: wavefrontGridSize,
                            wavelengthUm: wl,
                            opdDisplayMode,
                        });

                        const s = wavefrontGridSize;
                        const opdGrid = Array.from({ length: s }, () => new Float32Array(s));
                        const ampGrid = Array.from({ length: s }, () => new Float32Array(s));
                        const maskGrid = Array.from({ length: s }, () => Array(s).fill(false));

                        const displayOpdGrid = Array.isArray(nativeOpdResp?.displayOpdGrid) ? nativeOpdResp.displayOpdGrid : [];
                        const rawOpdGrid = Array.isArray(nativeOpdResp?.rawOpdGrid) ? nativeOpdResp.rawOpdGrid : [];
                        for (let iy = 0; iy < s; iy++) {
                            const rowDisplay = displayOpdGrid[iy] || [];
                            const rowRaw = rawOpdGrid[iy] || [];
                            for (let ix = 0; ix < s; ix++) {
                                const rawCell = rowRaw[ix];
                                if (rawCell === null || rawCell === undefined || rawCell === '') continue;
                                const vRawWaves = Number(rawCell);
                                if (!Number.isFinite(vRawWaves)) continue;

                                const displayCell = rowDisplay[ix];
                                const vDisplayWaves = (displayCell === null || displayCell === undefined || displayCell === '')
                                    ? NaN
                                    : Number(displayCell);
                                const vWaves = Number.isFinite(vDisplayWaves) ? vDisplayWaves : vRawWaves;
                                const vMicrons = vWaves * wl;

                                maskGrid[iy][ix] = true;
                                opdGrid[iy][ix] = vMicrons;
                                ampGrid[iy][ix] = 1.0;
                            }
                        }

                        const opticalRows = (typeof opener.getOpticalSystemRows === 'function')
                            ? (opener.getOpticalSystemRows(opener.tableOpticalSystem) || [])
                            : [];
                        let pupilDiameterMm = 10.0;
                        let focalLengthMm = Number.NaN;
                        try {
                            const diffParams = (typeof opener.calculateImageSpaceDiffractionParams === 'function')
                                ? opener.calculateImageSpaceDiffractionParams(opticalRows, wl)
                                : null;
                            const fWork = Number(diffParams?.fNumberWorking);
                            const fl = Number(diffParams?.focalLengthMm);
                            if (Number.isFinite(fl) && fl > 0 && Number.isFinite(fWork) && fWork > 0) {
                                focalLengthMm = Math.abs(fl);
                                pupilDiameterMm = focalLengthMm / fWork;
                            }
                        } catch (_) {}

                        if (!(Number.isFinite(pupilDiameterMm) && pupilDiameterMm > 0)) {
                            try {
                                const si = Number((typeof opener.findStopSurfaceIndex === 'function')
                                    ? opener.findStopSurfaceIndex(opticalRows)
                                    : -1);
                                const stopRow = (Number.isFinite(si) && si >= 0) ? opticalRows?.[si] : null;
                                const sdRaw = stopRow?.semidia ?? stopRow?.Semidia ?? stopRow?.['Semi Diameter'] ?? stopRow?.aperture ?? stopRow?.Aperture ?? NaN;
                                const sd = Math.abs(parseFloat(sdRaw));
                                if (Number.isFinite(sd) && sd > 0) {
                                    const isApertureField = !!(stopRow && (stopRow.aperture !== undefined || stopRow.Aperture !== undefined));
                                    const stopRadiusMm = isApertureField ? (sd * 0.5) : sd;
                                    if (Number.isFinite(stopRadiusMm) && stopRadiusMm > 0) {
                                        pupilDiameterMm = stopRadiusMm * 2;
                                    }
                                }
                            } catch (_) {}
                        }

                        if (!(Number.isFinite(focalLengthMm) && focalLengthMm > 0)) {
                            try {
                                const fl = Number((typeof opener.calculateFocalLength === 'function')
                                    ? opener.calculateFocalLength(opticalRows, wl)
                                    : NaN);
                                if (Number.isFinite(fl) && Math.abs(fl) > 1e-9 && fl !== Infinity) {
                                    focalLengthMm = Math.abs(fl);
                                }
                            } catch (_) {}
                        }

                        if (!(Number.isFinite(focalLengthMm) && focalLengthMm > 0)) {
                            // Last-resort safeguard to keep FFT scaling defined even when paraxial FL is unavailable.
                            focalLengthMm = 100.0;
                        }

                        const minRecommendedFftSize = 512;
                        const requestedFftSize = (!zeroPadTo || zeroPadTo === 0)
                            ? Math.max(wavefrontGridSize, minRecommendedFftSize)
                            : Math.max(wavefrontGridSize, zeroPadTo);
                        const basePixelPitchUm = (wl * Math.abs(Number(focalLengthMm))) / Math.max(1e-12, Math.abs(Number(pupilDiameterMm)));
                        const pixelSizeUm = basePixelPitchUm * (wavefrontGridSize / requestedFftSize);

                        setProgress(20 + baseProgress, 'λ=' + titleNm + 'nm: PSF (Rust native)...');
                        const nativePsfResp = await opener.runDesktopNativePsfMapForPopup({
                            gridOpd: Array.from({ length: s }, (_, iy) => Array.from(opdGrid[iy] || [])),
                            gridAmplitude: Array.from({ length: s }, (_, iy) => Array.from(ampGrid[iy] || [])),
                            pupilMask: Array.from({ length: s }, (_, iy) => Array.from(maskGrid[iy] || [])),
                            wavelengthUm: wl,
                            pixelSizeUm,
                            removeTilt: false,
                            zeroPadTo: requestedFftSize,
                            recenterIfWrapped: false,
                        });

                        setProgress(30 + baseProgress, 'λ=' + titleNm + 'nm: MTF (Rust native)...');
                        const mtfResp = await opener.runDesktopNativeMtfMapForPopup({
                            psfData: nativePsfResp?.psfData,
                            pixelSizeUm,
                            maxFrequencyLpmm: Number.isFinite(maxFreq) ? maxFreq : undefined,
                            // Keep parity with TFMTF native path which uses 121 plot points.
                            points: 121,
                        });

                        const freq = Array.isArray(mtfResp?.frequencyAxis) ? mtfResp.frequencyAxis : [];
                        const tan = Array.isArray(mtfResp?.mtfTangential) ? mtfResp.mtfTangential : [];
                        const sag = Array.isArray(mtfResp?.mtfSagittal) ? mtfResp.mtfSagittal : [];
                        if (!freq.length || !tan.length || !sag.length) {
                            throw new Error('Native Rust MTF result does not contain valid curves');
                        }

                        const color = getColorForWavelengthPopup(wl);
                        traces.push({
                            x: freq,
                            y: tan,
                            type: 'scatter',
                            mode: 'lines',
                            name: 'Tangential (' + titleNm + 'nm)',
                            line: { color, width: 2, dash: 'solid' },
                        });
                        traces.push({
                            x: freq,
                            y: sag,
                            type: 'scatter',
                            mode: 'lines',
                            name: 'Sagittal (' + titleNm + 'nm)',
                            line: { color, width: 2, dash: 'dot' },
                        });

                        const nyquist = Number(mtfResp?.nyquistLpmm);
                        if (Number.isFinite(nyquist) && nyquist > 0) {
                            nyquistGlobal = Math.max(nyquistGlobal, nyquist);
                        }

                        if (showDiffractionLimit) {
                            try {
                                const fNumber = Math.abs(Number(focalLengthMm)) / Math.max(1e-12, Math.abs(Number(pupilDiameterMm)));
                                if (Number.isFinite(fNumber) && fNumber > 0) {
                                    const diffY = [];
                                    for (let i = 0; i < freq.length; i++) {
                                        const f = Number(freq[i]);
                                        const cutoff = 1000.0 / (Math.max(1e-12, wl) * fNumber);
                                        const nu = f / Math.max(1e-12, cutoff);
                                        let val = 0;
                                        if (nu <= 0) val = 1;
                                        else if (nu >= 1) val = 0;
                                        else {
                                            const c = Math.max(-1, Math.min(1, nu));
                                            val = (2 / Math.PI) * (Math.acos(c) - c * Math.sqrt(Math.max(0, 1 - c * c)));
                                        }
                                        diffY.push(Number.isFinite(val) ? Math.max(0, Math.min(1, val)) : 0);
                                    }
                                    traces.push({
                                        x: freq,
                                        y: diffY,
                                        type: 'scatter',
                                        mode: 'lines',
                                        name: 'Diff. Limit (' + titleNm + 'nm)',
                                        line: { color, width: 1.5, dash: 'dash' },
                                    });
                                }
                            } catch (_) {}
                        }
                    }

                    if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                        throw new Error('Plotly is not available in MTF popup');
                    }
                    if (!traces.length) {
                        throw new Error('Native Rust MTF did not produce any traces');
                    }

                    setProgress(80, 'Rendering MTF...');
                    const xAxisRangeMax = Number.isFinite(maxFreq) && maxFreq > 0
                        ? maxFreq
                        : (nyquistGlobal > 0 ? nyquistGlobal : undefined);
                    await window.Plotly.newPlot(containerEl, traces, {
                        margin: { l: 50, r: 20, t: 28, b: 42 },
                        xaxis: {
                            title: 'Spatial frequency (lp/mm)',
                            ...(Number.isFinite(xAxisRangeMax) ? { range: [0, xAxisRangeMax] } : {}),
                        },
                        yaxis: { title: 'MTF', range: [0, 1.05] },
                        showlegend: true,
                    }, { responsive: true });

                    hideProgress();
                    return;
                }

                throw new Error('Rust MTF path is required but unavailable. Ensure desktop Rust analysis is enabled.');
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    const details = String((err && err.message) ? err.message : err || 'Unknown error');
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate MTF.<br/><span style="font-size:12px;color:#555;">' + details.replace(/</g, '&lt;').replace(/>/g, '&gt;') + '</span></div>';
                }
            }
        };

        document.getElementById('popup-show-mtf-btn').addEventListener('click', () => window.renderMTF());
        window.addEventListener('focus', syncAllOptions);
        window.addEventListener('load', () => syncAllOptions());
    <\/script>
</body>
</html>
                        `);try{S.document.close()}catch{}});const h=document.getElementById("open-through-focus-spot-window-btn");h&&h.addEventListener("click",()=>{if(T.__throughFocusSpotPopup&&!T.__throughFocusSpotPopup.closed){try{T.__throughFocusSpotPopup.focus()}catch{}return}try{Ht(T)}catch{}const S=n("Through-Focus Spot","width=980,height=700");if(!S){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__throughFocusSpotPopup=S;try{S.__analysisHostWindow=window}catch{}S.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Through-Focus Spot</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
            width: 110px;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-through-focus-spot-container {
            flex: 1 1 auto;
            min-height: 0;
        }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-through-focus-spot-wavelength-mode-select">Wavelength:</label>
        <select id="popup-through-focus-spot-wavelength-mode-select">
            <option value="all" selected>All</option>
            <option value="primary">Primary</option>
        </select>

        <label for="popup-through-focus-spot-min-defocus-input">Defocus min (mm):</label>
        <input id="popup-through-focus-spot-min-defocus-input" type="number" step="0.001" value="-0.5" />

        <label for="popup-through-focus-spot-max-defocus-input">Defocus max (mm):</label>
        <input id="popup-through-focus-spot-max-defocus-input" type="number" step="0.001" value="0.5" />

        <label for="popup-through-focus-spot-steps-input">Steps:</label>
        <input id="popup-through-focus-spot-steps-input" type="number" min="3" max="61" step="1" value="5" />

        <label for="popup-through-focus-spot-scale-input">Scale (µm):</label>
        <input id="popup-through-focus-spot-scale-input" type="number" min="1" step="1" value="100" />

        <label for="popup-through-focus-spot-ray-count-input">Ray number:</label>
        <input type="number" id="popup-through-focus-spot-ray-count-input" value="501" min="1" max="20001" step="1" />

        <label for="popup-through-focus-spot-ring-count-select">Ring count:</label>
        <select id="popup-through-focus-spot-ring-count-select">
            <option value="1">1</option>
            <option value="2">2</option>
            <option value="3">3</option>
            <option value="4">4</option>
            <option value="5">5</option>
            <option value="6">6</option>
            <option value="7">7</option>
            <option value="8">8</option>
            <option value="9">9</option>
            <option value="10" selected>10</option>
            <option value="12">12</option>
            <option value="15">15</option>
            <option value="16">16</option>
            <option value="20">20</option>
            <option value="24">24</option>
            <option value="32">32</option>
        </select>

        <label for="popup-through-focus-spot-pattern-select">Ray pattern:</label>
        <select id="popup-through-focus-spot-pattern-select">
            <option value="annular" selected>Annular</option>
            <option value="grid">Rectangle</option>
        </select>

        <button id="popup-show-through-focus-spot-btn" type="button">Show Through-Focus Spot</button>
    </div>
    <div id="popup-through-focus-spot-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-through-focus-spot-progress-text" style="margin-bottom: 6px;">Calculating Through-Focus Spot...</div>
        <progress id="popup-through-focus-spot-progress" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-through-focus-spot-container"></div>
    </div>

    <script>
        function getHostWindow() {
            try {
                if (window.opener && !window.opener.closed) return window.opener;
            } catch (_) {}
            try {
                if (window.__analysisHostWindow) return window.__analysisHostWindow;
            } catch (_) {}
            return null;
        }

        function getOpenerEl(id) {
            try {
                const host = getHostWindow();
                return host && host.document ? host.document.getElementById(id) : null;
            } catch (_) {
                return null;
            }
        }

        function syncInputsFromOpener() {
            const openerRay = getOpenerEl('ray-count-input');
            const openerRing = getOpenerEl('ring-count-select');
            const popupRay = document.getElementById('popup-through-focus-spot-ray-count-input');
            const popupRing = document.getElementById('popup-through-focus-spot-ring-count-select');
            if (popupRay && openerRay && popupRay.value !== openerRay.value) popupRay.value = openerRay.value;
            if (popupRing && openerRing && popupRing.value !== openerRing.value) popupRing.value = openerRing.value;

            const annular = getOpenerEl('annular-pattern-btn');
            const popupPattern = document.getElementById('popup-through-focus-spot-pattern-select');
            if (popupPattern) {
                const isAnnular = !!annular && annular.classList.contains('active');
                popupPattern.value = isAnnular ? 'annular' : 'grid';
            }
        }

        function setPopupPattern(pattern) {
            const isAnnular = String(pattern || 'annular') !== 'grid';
            const openerAnnular = getOpenerEl('annular-pattern-btn');
            const openerGrid = getOpenerEl('grid-pattern-btn');
            if (isAnnular && openerAnnular) openerAnnular.click();
            if (!isAnnular && openerGrid) openerGrid.click();
        }

        function syncAll() {
            syncInputsFromOpener();
        }

        window.renderThroughFocusSpot = async () => {
            const containerEl = document.getElementById('popup-through-focus-spot-container');
            if (containerEl) containerEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-through-focus-spot-progress-wrapper');
            const progressEl = document.getElementById('popup-through-focus-spot-progress');
            const progressTextEl = document.getElementById('popup-through-focus-spot-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressEl && Number.isFinite(value)) progressEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const openerRay = getOpenerEl('ray-count-input');
            const openerRing = getOpenerEl('ring-count-select');

            const wlModeEl = document.getElementById('popup-through-focus-spot-wavelength-mode-select');
            const minDefocusEl = document.getElementById('popup-through-focus-spot-min-defocus-input');
            const maxDefocusEl = document.getElementById('popup-through-focus-spot-max-defocus-input');
            const stepsEl = document.getElementById('popup-through-focus-spot-steps-input');
            const scaleEl = document.getElementById('popup-through-focus-spot-scale-input');
            const rayEl = document.getElementById('popup-through-focus-spot-ray-count-input');
            const ringEl = document.getElementById('popup-through-focus-spot-ring-count-select');
            const patternEl = document.getElementById('popup-through-focus-spot-pattern-select');

            if (openerRay && rayEl) openerRay.value = rayEl.value;
            if (openerRing && ringEl) openerRing.value = ringEl.value;

            const opener = getHostWindow();
            if (!opener) {
                if (containerEl) containerEl.textContent = 'Main window is not available.';
                return;
            }

            try {
                setProgress(0, 'Starting...');

                const shouldUseDesktopRust = (() => {
                    try {
                        if (typeof window !== 'undefined' && typeof window['shouldUseDesktopRustAnalysis'] === 'function') {
                            return !!window['shouldUseDesktopRustAnalysis']();
                        }
                        if (opener && typeof opener.shouldUseDesktopRustAnalysis === 'function') {
                            return !!opener.shouldUseDesktopRustAnalysis();
                        }
                        return true;
                    } catch (_) {
                        return false;
                    }
                })();
                const canUseDesktopRust = shouldUseDesktopRust && !!(
                    typeof opener.runDesktopAnalysisComputeForPopup === 'function'
                );
                const canUseNativeRustSpot = canUseDesktopRust && !!(
                    typeof opener.runDesktopNativeSpotRaytraceForPopup === 'function'
                );

                const toFiniteNumber = (v, fallback) => {
                    const n = Number(v);
                    return Number.isFinite(n) ? n : fallback;
                };
                const toInt = (v, fallback) => {
                    const n = parseInt(String(v ?? ''), 10);
                    return Number.isInteger(n) ? n : fallback;
                };
                const clamp = (v, lo, hi) => Math.max(lo, Math.min(hi, v));
                const selectedSurfaceIndex = undefined;
                const selectedRayCount = rayEl && rayEl.value !== '' ? parseInt(rayEl.value, 10) : undefined;
                const selectedRingCount = ringEl && ringEl.value !== '' ? parseInt(ringEl.value, 10) : undefined;
                const selectedPattern = patternEl ? String(patternEl.value || 'annular') : 'annular';
                const selectedWavelengthMode = wlModeEl ? String(wlModeEl.value || 'all') : 'all';
                const minDefocusMm = toFiniteNumber(minDefocusEl ? minDefocusEl.value : -0.5, -0.5);
                const maxDefocusMm = toFiniteNumber(maxDefocusEl ? maxDefocusEl.value : 0.5, 0.5);
                const steps = clamp(toInt(stepsEl ? stepsEl.value : 5, 5), 3, 61);
                const scaleUm = Math.max(1, toFiniteNumber(scaleEl ? scaleEl.value : 100, 100));
                const halfScaleUm = scaleUm * 0.5;

                // TFSD is intentionally fixed to Rust backend for parity.
                if (!canUseNativeRustSpot) {
                    throw new Error('Through-Focus Spot requires Rust backend (runDesktopNativeSpotRaytraceForPopup unavailable).');
                }

                if (canUseNativeRustSpot) {
                    const toWavelengthLabel = (rawLabel) => {
                        const text = String(rawLabel || '').trim();
                        const nm = text.match(/(d+(?:.d+)?)s*nm/i);
                        if (nm && nm[1]) return 'Wavelength ' + nm[1] + 'nm';
                        const lower = text.toLowerCase();
                        if (lower.includes('primary')) return 'Wavelength Primary';
                        return 'Wavelength ' + text;
                    };
                    const wavelengthLabelFromSeries = (seriesItem, rawLabel) => {
                        const wl = Number(seriesItem?.wavelengthUm);
                        if (Number.isFinite(wl) && wl > 0) {
                            return 'Wavelength ' + (wl * 1000).toFixed(1) + 'nm';
                        }
                        return toWavelengthLabel(rawLabel);
                    };
                    const parseSeriesLabel = (label, fallbackObjectLabel) => {
                        const raw = String(label || '').trim();
                        if (!raw) {
                            return { objectLabel: fallbackObjectLabel, wavelengthLabel: 'Wavelength Primary' };
                        }
                        const m = raw.match(/(Primary(?:s*([^)]*))?|d+(?:.d+)?s*nm)s*$/i);
                        if (m && m[1]) {
                            const wlRaw = String(m[1] || 'Primary');
                            const prefix = raw.slice(0, Math.max(0, m.index || 0)).replace(/[|@-s]+$/, '').trim();
                            return {
                                objectLabel: prefix || fallbackObjectLabel,
                                wavelengthLabel: toWavelengthLabel(wlRaw),
                            };
                        }
                        return { objectLabel: raw, wavelengthLabel: 'Wavelength Primary' };
                    };

                    const objectRows = (() => {
                        try {
                            if (opener.tableObject && typeof opener.tableObject.getData === 'function') {
                                const rows = opener.tableObject.getData();
                                if (Array.isArray(rows)) return rows;
                            }
                        } catch (_) {}
                        try {
                            if (typeof opener.getObjectRows === 'function') {
                                const rows = opener.getObjectRows(opener.tableObject);
                                if (Array.isArray(rows)) return rows;
                            }
                        } catch (_) {}
                        try {
                            const raw = opener.localStorage && opener.localStorage.getItem('objectTableData');
                            if (raw) {
                                const parsed = JSON.parse(raw);
                                if (Array.isArray(parsed)) return parsed;
                            }
                        } catch (_) {}
                        return [];
                    })();

                    const getObjectLabel = (row, index) => {
                        const id = row && row.id !== undefined ? String(row.id).trim() : '';
                        if (id) return id;
                        const obj = row && row.object !== undefined ? String(row.object).trim() : '';
                        if (obj) return obj;
                        const pos = row && row.position !== undefined ? String(row.position).trim() : '';
                        if (pos) return 'Object ' + (index + 1) + ' (' + pos + ')';
                        return 'Object ' + (index + 1);
                    };

                    const objectLabels = [];
                    const focusGrid = [];
                    const tfTraceStatsRows = [];
                    const getObjectLabelByIndex = (index) => {
                        if (Array.isArray(objectRows) && objectRows[index]) {
                            return getObjectLabel(objectRows[index], index);
                        }
                        return 'Object ' + (index + 1);
                    };
                    const ensureObjectRow = (preferredIndex, label) => {
                        let idx = Number(preferredIndex);
                        if (!Number.isInteger(idx) || idx < 0) {
                            idx = objectLabels.length;
                        }
                        while (objectLabels.length <= idx) {
                            const next = objectLabels.length;
                            objectLabels.push(getObjectLabelByIndex(next));
                            focusGrid.push([]);
                        }
                        const labelText = String(label || '').trim();
                        if (labelText) {
                            objectLabels[idx] = labelText;
                        }
                        return idx;
                    };
                    const defocusValues = Array.from({ length: steps }, (_, i) => {
                        if (steps <= 1) return minDefocusMm;
                        const t = i / (steps - 1);
                        return minDefocusMm + t * (maxDefocusMm - minDefocusMm);
                    });

                    for (let i = 0; i < defocusValues.length; i++) {
                        const shift = defocusValues[i];
                        const baseProgress = Math.floor((i / Math.max(1, defocusValues.length)) * 85);
                        setProgress(baseProgress, 'Computing defocus ' + shift.toFixed(4) + ' mm (' + (i + 1) + '/' + defocusValues.length + ')...');

                        const result = await opener.runDesktopNativeSpotRaytraceForPopup({
                            surfaceIndex: selectedSurfaceIndex,
                            rayCount: selectedRayCount,
                            ringCount: selectedRingCount,
                            pattern: selectedPattern,
                            wavelengthMode: selectedWavelengthMode,
                            objectRows,
                            defocusMm: shift,
                        });
                        const stats = Array.isArray(result?.seriesStats) ? result.seriesStats : [];
                        for (const stat of stats) {
                            tfTraceStatsRows.push({
                                backend: String(result?.backend || 'native-rust-raytrace'),
                                defocusMm: Number(shift),
                                label: String(stat?.label || ''),
                                attemptedRays: Number(stat?.attemptedRays || 0),
                                hitRays: Number(stat?.hitRays || 0),
                                missRays: Number(stat?.missRays || 0),
                                apertureBlockRays: Number(stat?.apertureBlockRays || 0),
                                noIntersectionRays: Number(stat?.noIntersectionRays || 0),
                                tirRays: Number(stat?.tirRays || 0),
                                unknownFailRays: Number(stat?.unknownFailRays || 0),
                                statusCounts: (stat && typeof stat.statusCounts === 'object' && !Array.isArray(stat.statusCounts)) ? stat.statusCounts : {},
                                hitRatePercent: Number(stat?.hitRatePercent || 0),
                            });
                        }
                        const airyRadiusUm = Number(result?.airyRadiusUm);
                        const safeAiryRadiusUm = (Number.isFinite(airyRadiusUm) && airyRadiusUm > 0) ? airyRadiusUm : NaN;

                        const series = Array.isArray(result?.series) ? result.series : [];
                        const groupedByObject = new Map();
                        const wlCountRaw = Number(result?.wavelengthCount);
                        const inferredWavelengthCount = (() => {
                            if (Number.isInteger(wlCountRaw) && wlCountRaw > 0) return wlCountRaw;
                            const unique = new Set();
                            for (const s of series) {
                                const key = wavelengthLabelFromSeries(s, String(s?.label || ''));
                                unique.add(key);
                            }
                            return Math.max(1, unique.size);
                        })();

                        for (let sIdx = 0; sIdx < series.length; sIdx++) {
                            const s = series[sIdx] || {};
                            const objectIndexByOrder = Math.floor(sIdx / inferredWavelengthCount);
                            const fallbackObj = getObjectLabelByIndex(objectIndexByOrder);
                            const parsed = parseSeriesLabel(s.label, fallbackObj);
                            const rowIndex = ensureObjectRow(objectIndexByOrder, fallbackObj || parsed.objectLabel);
                            const pts = Array.isArray(s.points) ? s.points : [];
                            const hasFieldAngle = !!s.hasFieldAngle;
                            const chiefXUm = Number(s?.chiefPointUm?.xUm);
                            const chiefYUm = Number(s?.chiefPointUm?.yUm);
                            const useChiefCentering = hasFieldAngle && Number.isFinite(chiefXUm) && Number.isFinite(chiefYUm);
                            const centered = useChiefCentering
                                ? pts.map((p) => ({
                                    xUm: (Number(p?.xUm) || 0) - chiefXUm,
                                    yUm: (Number(p?.yUm) || 0) - chiefYUm,
                                }))
                                : pts.map((p) => ({
                                    xUm: Number(p?.xUm) || 0,
                                    yUm: Number(p?.yUm) || 0,
                                }));

                            if (!groupedByObject.has(rowIndex)) {
                                groupedByObject.set(rowIndex, []);
                            }
                            const wlLabel = wavelengthLabelFromSeries(s, parsed.wavelengthLabel);
                            groupedByObject.get(rowIndex).push({
                                key: wlLabel,
                                label: wlLabel,
                                color: String(s?.color || '#2563eb'),
                                points: centered,
                            });
                        }

                        for (let rowIndex = 0; rowIndex < focusGrid.length; rowIndex++) {
                            const groups = Array.isArray(groupedByObject.get(rowIndex)) ? groupedByObject.get(rowIndex) : [];
                            const merged = [];
                            for (const g of groups) {
                                const pts = Array.isArray(g?.points) ? g.points : [];
                                for (const p of pts) {
                                    merged.push({ xUm: Number(p?.xUm) || 0, yUm: Number(p?.yUm) || 0 });
                                }
                            }

                            let cx = 0;
                            let cy = 0;
                            if (merged.length > 0) {
                                cx = merged.reduce((sum, p) => sum + p.xUm, 0) / merged.length;
                                cy = merged.reduce((sum, p) => sum + p.yUm, 0) / merged.length;
                            }

                            focusGrid[rowIndex].push({
                                shiftMm: shift,
                                airyRadiusUm: safeAiryRadiusUm,
                                pointsByWavelength: groups.map((g) => ({
                                    key: g.key,
                                    label: g.label,
                                    color: g.color,
                                    points: (Array.isArray(g.points) ? g.points : []).map((p) => ({
                                        xUm: (Number(p?.xUm) || 0) - cx,
                                        yUm: (Number(p?.yUm) || 0) - cy,
                                    })),
                                })),
                            });
                        }
                    }

                    try {
                        window.__cooptTfSpotLastTraceStats = tfTraceStatsRows;
                    } catch (_) {}
                    try {
                        if (opener && typeof opener === 'object') {
                            opener.__cooptTfSpotLastTraceStats = tfTraceStatsRows;
                        }
                    } catch (_) {}
                    try {
                        globalThis.__cooptTfSpotLastTraceStats = tfTraceStatsRows;
                    } catch (_) {}
                    try {
                        if (console.table) {
                            console.table(tfTraceStatsRows);
                        } else {
                            console.log('[TFSD_TRACE_STATS]', tfTraceStatsRows);
                        }
                    } catch (_) {}

                    if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                        throw new Error('Plotly is not available in Through-Focus Spot popup');
                    }

                    const rows = Math.max(1, focusGrid.length);
                    const cols = Math.max(1, defocusValues.length);
                    const traces = [];
                    const layout = {
                        showlegend: true,
                        grid: { rows, columns: cols, pattern: 'independent' },
                        margin: { l: 60, r: 20, t: 56, b: 60 },
                        paper_bgcolor: '#ffffff',
                        plot_bgcolor: '#ffffff',
                        height: Math.max(420, rows * 145 + 90),
                        legend: {
                            orientation: 'h',
                            yanchor: 'bottom',
                            y: 1.06,
                            xanchor: 'center',
                            x: 0.5,
                        },
                        legendgroupclick: 'togglegroup',
                        shapes: [],
                    };

                    const legendEntries = new Map();
                    for (let r = 0; r < rows; r++) {
                        for (let c = 0; c < cols; c++) {
                            const idx = r * cols + c + 1;
                            const axisRefX = idx === 1 ? 'x' : 'x' + idx;
                            const axisRefY = idx === 1 ? 'y' : 'y' + idx;
                            const axisKeyX = idx === 1 ? 'xaxis' : 'xaxis' + idx;
                            const axisKeyY = idx === 1 ? 'yaxis' : 'yaxis' + idx;
                            const cell = (focusGrid[r] && focusGrid[r][c]) ? focusGrid[r][c] : { pointsByWavelength: [] };
                            const airyRadiusUm = Number(cell?.airyRadiusUm);
                            const groups = Array.isArray(cell?.pointsByWavelength) ? cell.pointsByWavelength : [];

                            for (const group of groups) {
                                const pts = Array.isArray(group?.points) ? group.points : [];
                                const groupKey = String(group?.key || group?.label || 'wavelength');
                                if (!legendEntries.has(groupKey)) {
                                    legendEntries.set(groupKey, {
                                        label: String(group?.label || groupKey),
                                        color: String(group?.color || '#2563eb'),
                                    });
                                }
                                traces.push({
                                    x: pts.map((p) => Number(p?.xUm) || 0),
                                    y: pts.map((p) => Number(p?.yUm) || 0),
                                    mode: 'markers',
                                    type: 'scattergl',
                                    name: String(group?.label || groupKey),
                                    legendgroup: groupKey,
                                    showlegend: false,
                                    marker: {
                                        size: 4,
                                        color: String(group?.color || '#2563eb'),
                                        opacity: 0.75,
                                    },
                                    xaxis: axisRefX,
                                    yaxis: axisRefY,
                                    hovertemplate: 'x=%{x:.2f} µm<br>y=%{y:.2f} µm<extra></extra>',
                                });
                            }

                            layout[axisKeyX] = {
                                range: [-halfScaleUm, halfScaleUm],
                                showgrid: true,
                                zeroline: true,
                                showticklabels: r === rows - 1,
                                title: r === rows - 1 ? defocusValues[c].toFixed(3) + ' mm' : '',
                            };
                            layout[axisKeyY] = {
                                range: [-halfScaleUm, halfScaleUm],
                                showgrid: true,
                                zeroline: true,
                                showticklabels: c === 0,
                                title: c === 0 ? (objectLabels[r] || ('Object ' + (r + 1))) : '',
                                scaleanchor: axisRefX,
                                scaleratio: 1,
                            };

                            if (Number.isFinite(airyRadiusUm) && airyRadiusUm > 0) {
                                layout.shapes.push({
                                    type: 'circle',
                                    xref: axisRefX,
                                    yref: axisRefY,
                                    x0: -airyRadiusUm,
                                    y0: -airyRadiusUm,
                                    x1: airyRadiusUm,
                                    y1: airyRadiusUm,
                                    line: { color: '#111827', width: 1 },
                                    fillcolor: 'rgba(0,0,0,0)',
                                });
                            }
                        }
                    }

                    for (const [groupKey, entry] of legendEntries.entries()) {
                        traces.push({
                            x: [null],
                            y: [null],
                            mode: 'markers',
                            type: 'scatter',
                            name: entry.label,
                            legendgroup: groupKey,
                            showlegend: true,
                            marker: {
                                size: 8,
                                color: entry.color,
                                symbol: 'circle',
                            },
                            hoverinfo: 'skip',
                        });
                    }

                    setProgress(92, 'Rendering Through-Focus Spot...');
                    await window.Plotly.newPlot(containerEl, traces, layout, { responsive: true, displaylogo: false });
                    try {
                        if (progressWrapper) progressWrapper.style.display = 'none';
                    } catch (_) {}
                    return;
                }

                throw new Error('Through-Focus Spot web fallback is disabled (Rust backend fixed).');
            } catch (e) {
                if (containerEl) containerEl.textContent = String(e && e.message ? e.message : e);
                setProgress(100, 'Failed');
            }
        };

        document.getElementById('popup-through-focus-spot-pattern-select').addEventListener('change', (e) => {
            const target = e && e.target ? e.target : null;
            const v = target && target.value ? String(target.value) : 'annular';
            setPopupPattern(v);
        });
        document.getElementById('popup-show-through-focus-spot-btn').addEventListener('click', () => window.renderThroughFocusSpot());
        window.addEventListener('focus', syncAll);
        window.addEventListener('load', () => syncAll());
        syncAll();
    <\/script>
</body>
</html>
                        `);try{S.document.close()}catch{}});const g=document.getElementById("open-through-focus-mtf-window-btn");g&&g.addEventListener("click",()=>{if(T.__throughFocusMtfPopup&&!T.__throughFocusMtfPopup.closed){try{T.__throughFocusMtfPopup.focus()}catch{}return}const S=n("Through-Focus MTF","width=900,height=680");if(!S){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__throughFocusMtfPopup=S,S.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Analysis</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
            width: 110px;
        }
        .controls input[type="checkbox"] {
            width: auto;
            padding: 0;
            border: none;
            background: transparent;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .note { padding: 8px 12px; font-size: 12px; color: #666; border-bottom: 1px solid #eee; background: #fff; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-through-focus-mtf-container { flex: 1 1 auto; min-height: 0; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-through-focus-mtf-wavelength-select">Wavelength:</label>
        <select id="popup-through-focus-mtf-wavelength-select"></select>
        <label for="popup-through-focus-mtf-object-select">Object:</label>
        <select id="popup-through-focus-mtf-object-select"></select>
        <label for="popup-through-focus-mtf-target-freq-input">Freq (lp/mm):</label>
        <input id="popup-through-focus-mtf-target-freq-input" type="number" min="0" step="1" value="10" />
        <label for="popup-through-focus-mtf-min-defocus-input">Defocus min (mm):</label>
        <input id="popup-through-focus-mtf-min-defocus-input" type="number" step="0.001" value="-0.5" />
        <label for="popup-through-focus-mtf-max-defocus-input">Defocus max (mm):</label>
        <input id="popup-through-focus-mtf-max-defocus-input" type="number" step="0.001" value="0.5" />
        <label for="popup-through-focus-mtf-steps-input">Steps:</label>
        <input id="popup-through-focus-mtf-steps-input" type="number" min="3" max="201" step="1" value="21" />
        <label for="popup-through-focus-mtf-sampling-select">Sampling:</label>
        <select id="popup-through-focus-mtf-sampling-select">
            <option value="32">32x32</option>
            <option value="64">64x64</option>
            <option value="128">128x128</option>
            <option value="256" selected>256x256</option>
            <option value="512">512x512</option>
            <option value="1024">1024x1024</option>
            <option value="2048">2048x2048</option>
            <option value="4096">4096x4096</option>
        </select>
        <label for="popup-through-focus-mtf-zeropad-select" title="Zero-padding increases FFT size without increasing OPD ray grid.">Zero pad:</label>
        <select id="popup-through-focus-mtf-zeropad-select">
            <option value="none">None</option>
            <option value="auto" selected>Auto</option>
            <option value="512">512</option>
            <option value="1024">1024</option>
            <option value="2048">2048</option>
            <option value="4096">4096</option>
        </select>
        <label style="display:flex;align-items:center;gap:6px;">
            <input id="popup-through-focus-mtf-remove-ptd-checkbox" type="checkbox" />
            Remove P/T/D
        </label>
        <button id="popup-show-through-focus-mtf-btn" type="button">Show Plot</button>
    </div>
    <div id="popup-through-focus-mtf-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-through-focus-mtf-progress-text" style="margin-bottom: 6px;">Calculating...</div>
        <progress id="popup-through-focus-mtf-progress" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-through-focus-mtf-container"></div>
    </div>

    <script>
        function safeCall(fn, fallback) {
            try { return fn(); } catch (_) { return fallback; }
        }

        function getOpener() {
            try { return window.opener || window; } catch (_) { return window; }
        }

        function popupLog(...args) {
            try { console.log(...args); } catch (_) {}
            try {
                const op = window.opener;
                if (op && op.console && typeof op.console.log === 'function') {
                    op.console.log(...args);
                }
            } catch (_) {}
        }

        function popupError(...args) {
            try { console.error(...args); } catch (_) {}
            try {
                const op = window.opener;
                if (op && op.console && typeof op.console.error === 'function') {
                    op.console.error(...args);
                }
            } catch (_) {}
        }

        function getPrimaryWavelength() {
            const opener = getOpener();
            if (!opener) return null;
            if (typeof opener.getPrimaryWavelength !== 'function') return null;
            const v = Number(safeCall(() => opener.getPrimaryWavelength(), 0));
            return Number.isFinite(v) && v > 0 ? v : null;
        }

        function buildWavelengthOptions() {
            const opener = getOpener();
            if (!opener) return [];
            const getSourceRows = opener.getSourceRows;
            const sources = (typeof getSourceRows === 'function')
                ? safeCall(() => getSourceRows(opener.tableSource), [])
                : [];
            const primary = getPrimaryWavelength();
            const out = [{ value: 'all', label: 'All' }];
            if (Array.isArray(sources) && sources.length > 0) {
                for (let i = 0; i < sources.length; i++) {
                    const wl = Number(sources[i]?.wavelength);
                    if (!Number.isFinite(wl) || wl <= 0) continue;
                    const nm = wl * 1000;
                    const label = Number.isFinite(primary) && Math.abs(wl - primary) < 1e-9
                        ? (nm.toFixed(1) + ' nm (primary)')
                        : (nm.toFixed(1) + ' nm');
                    out.push({ value: String(wl), label });
                }
            }
            if (out.length === 1) {
                if (Number.isFinite(primary) && primary > 0) {
                    out.push({ value: String(primary), label: ((primary * 1000).toFixed(1) + ' nm') });
                }
            }
            return out;
        }

        function buildObjectOptions() {
            const opener = getOpener();
            if (!opener) return [];
            const getObjectRows = opener.getObjectRows;
            const objects = (typeof getObjectRows === 'function')
                ? safeCall(() => getObjectRows(opener.tableObject), [])
                : [];
            const out = [];
            if (Array.isArray(objects) && objects.length > 0) {
                for (let i = 0; i < objects.length; i++) {
                    const obj = objects[i];
                    if (!obj) continue;
                    const typeRaw = String(obj.position ?? obj.object ?? obj.Object ?? obj.objectType ?? 'Point');
                    const x = (obj.x ?? obj.xHeightAngle ?? 0);
                    const y = (obj.y ?? obj.yHeightAngle ?? 0);
                    out.push({ value: String(i), label: (String(i + 1) + ': ' + typeRaw + ' (' + x + ', ' + y + ')') });
                }
            }
            if (out.length === 0) out.push({ value: '0', label: '0' });
            return out;
        }

        function populateSelect(selectEl, options) {
            if (!selectEl) return;
            const current = selectEl.value;
            selectEl.innerHTML = '';
            for (const opt of options) {
                const o = document.createElement('option');
                o.value = opt.value;
                o.textContent = opt.label;
                selectEl.appendChild(o);
            }
            if (current && Array.from(selectEl.options).some(o => o.value === current)) {
                selectEl.value = current;
            }
        }

        function syncAllOptions() {
            const wlSel = document.getElementById('popup-through-focus-mtf-wavelength-select');
            const prevWl = wlSel ? wlSel.value : '';
            populateSelect(wlSel, buildWavelengthOptions());
            if (wlSel && (!prevWl || !Array.from(wlSel.options).some(o => o.value === prevWl))) {
                const primary = getPrimaryWavelength();
                if (Number.isFinite(primary) && Array.from(wlSel.options).some(o => o.value === String(primary))) {
                    wlSel.value = String(primary);
                } else {
                    const firstNumeric = Array.from(wlSel.options).find(o => o.value !== 'all');
                    if (firstNumeric) wlSel.value = firstNumeric.value;
                }
            }
            populateSelect(document.getElementById('popup-through-focus-mtf-object-select'), buildObjectOptions());
        }

        window['renderThroughFocusMTF'] = async () => {
            const containerEl = document.getElementById('popup-through-focus-mtf-container');
            if (containerEl) containerEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-through-focus-mtf-progress-wrapper');
            const progressEl = document.getElementById('popup-through-focus-mtf-progress');
            const progressTextEl = document.getElementById('popup-through-focus-mtf-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressEl && Number.isFinite(value)) progressEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            const wlSel = document.getElementById('popup-through-focus-mtf-wavelength-select');
            const objSel = document.getElementById('popup-through-focus-mtf-object-select');
            const targetFreqEl = document.getElementById('popup-through-focus-mtf-target-freq-input');
            const minDefocusEl = document.getElementById('popup-through-focus-mtf-min-defocus-input');
            const maxDefocusEl = document.getElementById('popup-through-focus-mtf-max-defocus-input');
            const stepsEl = document.getElementById('popup-through-focus-mtf-steps-input');
            const samplingEl = document.getElementById('popup-through-focus-mtf-sampling-select');
            const zeroPadEl = document.getElementById('popup-through-focus-mtf-zeropad-select');
            const removePtdEl = document.getElementById('popup-through-focus-mtf-remove-ptd-checkbox');

            const wlValue = wlSel ? String(wlSel.value) : '';
            const primary = getPrimaryWavelength();
            const wavelength = (wlValue === 'all') ? 'all' : Number(wlValue);
            const objectIndex = objSel ? parseInt(objSel.value, 10) : 0;
            const targetFrequencyLpmm = targetFreqEl ? Number(targetFreqEl.value) : 10;
            const defocusMinMm = minDefocusEl ? Number(minDefocusEl.value) : -0.5;
            const defocusMaxMm = maxDefocusEl ? Number(maxDefocusEl.value) : 0.5;
            const steps = stepsEl ? Number(stepsEl.value) : 21;
            const sampling = samplingEl ? Number(samplingEl.value) : 256;
            const zeroPadRaw = zeroPadEl ? String(zeroPadEl.value || 'auto') : 'auto';
            const opdDisplayMode = (removePtdEl && removePtdEl.checked)
                ? 'pistonTiltDefocusRemoved'
                : 'pistonTiltRemoved';
            const zeroPadTo = (zeroPadRaw === 'none')
                ? (Number.isFinite(sampling) ? sampling : 256)
                : (zeroPadRaw === 'auto')
                    ? 0
                    : (Number.isFinite(parseInt(zeroPadRaw, 10)) ? parseInt(zeroPadRaw, 10) : 0);

            try {
                const opener = getOpener();
                if (!opener) {
                    throw new Error('Opener is not available');
                }
                setProgress(0, 'Starting...');
                await new Promise(r => setTimeout(r, 0));
                if (wavelength !== 'all' && !Number.isFinite(wavelength) && !(Number.isFinite(primary) && primary > 0)) {
                    throw new Error('Primary wavelength is unavailable. Please set Source Primary Wavelength.');
                }

                if (typeof opener.runDesktopNativeThroughFocusMtfForPopup !== 'function') {
                    throw new Error('runDesktopNativeThroughFocusMtfForPopup is not available on opener');
                }

                const sourceRows = (typeof opener.getSourceRows === 'function')
                    ? (safeCall(() => opener.getSourceRows(opener.tableSource), []) || [])
                    : [];
                const wavelengthList = (() => {
                    const out = [];
                    if (wavelength === 'all') {
                        if (Array.isArray(sourceRows) && sourceRows.length > 0) {
                            for (let i = 0; i < sourceRows.length; i++) {
                                const wl = Number(sourceRows[i]?.wavelength);
                                if (!Number.isFinite(wl) || wl <= 0) continue;
                                if (out.some(v => Math.abs(v - wl) < 1e-9)) continue;
                                out.push(wl);
                            }
                        }
                        if (out.length === 0 && Number.isFinite(primary) && primary > 0) {
                            out.push(primary);
                        }
                    } else {
                        const wl = (Number.isFinite(Number(wavelength)) && Number(wavelength) > 0)
                            ? Number(wavelength)
                            : ((Number.isFinite(primary) && primary > 0) ? primary : 0.5876);
                        out.push(wl);
                    }
                    if (out.length === 0) out.push(0.5876);
                    return out;
                })();

                let lastProgress = 20;
                setProgress(lastProgress, 'Computing Through-Focus MTF (Rust/WASM)...');
                const nativeResp = await opener.runDesktopNativeThroughFocusMtfForPopup({
                    objectIndex: Number.isFinite(objectIndex) ? objectIndex : 0,
                    wavelengths: wavelengthList,
                    targetFrequencyLpmm: Number.isFinite(targetFrequencyLpmm) ? targetFrequencyLpmm : 10,
                    defocusMinMm: Number.isFinite(defocusMinMm) ? defocusMinMm : -0.5,
                    defocusMaxMm: Number.isFinite(defocusMaxMm) ? defocusMaxMm : 0.5,
                    steps: Number.isFinite(steps) ? steps : 21,
                    samplingSize: Number.isFinite(sampling) ? sampling : 256,
                    zeroPadTo,
                    opdDisplayMode,
                    onProgress: (evt) => {
                        try {
                            const p = Number(evt?.percent);
                            const msg = String(evt?.message || 'Computing Through-Focus MTF...');
                            if (Number.isFinite(p)) {
                                lastProgress = Math.max(lastProgress, Math.max(0, Math.min(100, p)));
                                setProgress(lastProgress, msg);
                            } else {
                                setProgress(lastProgress, msg);
                            }
                        } catch (_) {}
                    },
                });

                if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                    throw new Error('Plotly is not available in Through-Focus MTF popup');
                }
                const xAxis = Array.isArray(nativeResp?.xAxis) ? nativeResp.xAxis : [];
                const series = Array.isArray(nativeResp?.series) ? nativeResp.series : [];
                if (!xAxis.length || !series.length) {
                    throw new Error('Native Through-Focus MTF did not produce valid data');
                }

                const getColorForWavelengthPopup = (wl) => {
                    try {
                        if (typeof opener.getColorForWavelength === 'function') {
                            const c = opener.getColorForWavelength(wl);
                            if (typeof c === 'string' && c) return c;
                        }
                    } catch (_) {}
                    const nm = Number(wl) * 1000;
                    if (!Number.isFinite(nm)) return '#2563eb';
                    if (nm < 470) return '#2563eb';
                    if (nm < 530) return '#16a34a';
                    if (nm < 600) return '#f59e0b';
                    return '#dc2626';
                };

                const traces = [];
                for (let i = 0; i < series.length; i++) {
                    const s = series[i] || {};
                    const wl = Number(s.wavelengthUm);
                    const nm = Number.isFinite(wl) ? (wl * 1000).toFixed(1) : 'N/A';
                    const color = getColorForWavelengthPopup(wl);
                    const tan = Array.isArray(s.mtfTangential) ? s.mtfTangential : [];
                    const sag = Array.isArray(s.mtfSagittal) ? s.mtfSagittal : [];
                    traces.push({
                        x: xAxis,
                        y: tan,
                        type: 'scatter',
                        mode: 'lines',
                        name: 'Meridional (' + nm + 'nm)',
                        line: { color, width: 2, dash: 'solid' },
                    });
                    traces.push({
                        x: xAxis,
                        y: sag,
                        type: 'scatter',
                        mode: 'lines',
                        name: 'Sagittal (' + nm + 'nm)',
                        line: { color, width: 2, dash: 'dot' },
                    });
                }

                setProgress(85, 'Rendering Through-Focus MTF...');
                await window.Plotly.newPlot(containerEl, traces, {
                    title: String(Number.isFinite(targetFrequencyLpmm) ? targetFrequencyLpmm.toFixed(1) : 10) + ' lp/mm',
                    xaxis: { title: 'Defocus shift (mm)' },
                    yaxis: { title: 'MTF', range: [0, 1.05] },
                    margin: { l: 60, r: 20, t: 50, b: 50 },
                    showlegend: true,
                }, { responsive: true, displaylogo: false });

                hideProgress();
                return;
            } catch (err) {
                console.error(err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    const details = String((err && err.message) ? err.message : err || 'Unknown error');
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate Through-Focus MTF.<br/><span style="font-size:12px;color:#555;">' + details.replace(/</g, '&lt;').replace(/>/g, '&gt;') + '</span></div>';
                }
            }
        };

        document.getElementById('popup-show-through-focus-mtf-btn').addEventListener('click', () => window.renderThroughFocusMTF());
        window.addEventListener('focus', syncAllOptions);
        window.addEventListener('load', () => syncAllOptions());
    <\/script>
</body>
</html>
                        `);try{S.document.close()}catch{}});const y=document.getElementById("open-field-mtf-window-btn");y&&y.addEventListener("click",()=>{if(T.__fieldMtfPopup&&!T.__fieldMtfPopup.closed){try{T.__fieldMtfPopup.focus()}catch{}return}const S=n("Object MTF","width=900,height=650");if(!S){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__fieldMtfPopup=S,S.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Object MTF</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls select {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
        }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
            width: 110px;
        }
        .controls input[type="checkbox"] {
            width: auto;
            padding: 0;
            border: none;
            background: transparent;
        }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .note { padding: 8px 12px; font-size: 12px; color: #666; border-bottom: 1px solid #eee; background: #fff; }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
            display: flex;
            flex-direction: column;
        }
        #popup-field-mtf-container { flex: 1 1 auto; min-height: 0; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-field-mtf-wavelength-select">Wavelength:</label>
        <select id="popup-field-mtf-wavelength-select"></select>
        <label for="popup-field-mtf-meridional-freq-input">1st Freq (lp/mm):</label>
        <input id="popup-field-mtf-meridional-freq-input" type="number" min="0" step="1" value="10" />
        <label for="popup-field-mtf-sagittal-freq-input">2nd Freq (lp/mm):</label>
        <input id="popup-field-mtf-sagittal-freq-input" type="number" min="0" step="1" value="30" />
        <label for="popup-field-mtf-min-field-input">Object min:</label>
        <input id="popup-field-mtf-min-field-input" type="number" step="0.001" value="0" />
        <label for="popup-field-mtf-max-field-input">Object max:</label>
        <input id="popup-field-mtf-max-field-input" type="number" step="0.001" value="10" />
        <label for="popup-field-mtf-steps-input">Steps:</label>
        <input id="popup-field-mtf-steps-input" type="number" min="3" max="201" step="1" value="21" />
        <label for="popup-field-mtf-sampling-select">Sampling:</label>
        <select id="popup-field-mtf-sampling-select">
            <option value="32">32x32</option>
            <option value="64">64x64</option>
            <option value="128">128x128</option>
            <option value="256" selected>256x256</option>
            <option value="512">512x512</option>
            <option value="1024">1024x1024</option>
            <option value="2048">2048x2048</option>
            <option value="4096">4096x4096</option>
        </select>
        <label for="popup-field-mtf-zeropad-select" title="Zero-padding increases FFT size without increasing OPD ray grid.">Zero pad:</label>
        <select id="popup-field-mtf-zeropad-select">
            <option value="none">None</option>
            <option value="auto" selected>Auto</option>
            <option value="512">512</option>
            <option value="1024">1024</option>
            <option value="2048">2048</option>
            <option value="4096">4096</option>
        </select>
        <label style="display:flex;align-items:center;gap:6px;">
            <input id="popup-field-mtf-remove-ptd-checkbox" type="checkbox" />
            Remove P/T/D
        </label>
        <button id="popup-show-field-mtf-btn" type="button">Show Plot</button>
    </div>
    <div id="popup-field-mtf-progress-wrapper" style="display:none; padding: 8px 12px; font-size: 12px; color: #333; border-bottom: 1px solid #eee; background: #fff;">
        <div id="popup-field-mtf-progress-text" style="margin-bottom: 6px;">Calculating...</div>
        <progress id="popup-field-mtf-progress" style="display:block;width:calc(100% + 24px);margin-left:-12px;" max="100"></progress>
    </div>
    <div class="content">
        <div id="popup-field-mtf-container"></div>
    </div>

    <script>
        function safeCall(fn, fallback) {
            try { return fn(); } catch (_) { return fallback; }
        }

        function getOpener() {
            try { return window.opener || window; } catch (_) { return window; }
        }

        function popupLog(...args) {
            try { console.log(...args); } catch (_) {}
            try {
                const op = window.opener;
                if (op && op.console && typeof op.console.log === 'function') {
                    op.console.log(...args);
                }
            } catch (_) {}
        }

        function popupError(...args) {
            try { console.error(...args); } catch (_) {}
            try {
                const op = window.opener;
                if (op && op.console && typeof op.console.error === 'function') {
                    op.console.error(...args);
                }
            } catch (_) {}
        }

        function getPrimaryWavelength() {
            const opener = getOpener();
            if (!opener) return null;
            if (typeof opener.getPrimaryWavelength !== 'function') return null;
            const v = Number(safeCall(() => opener.getPrimaryWavelength(), 0));
            return Number.isFinite(v) && v > 0 ? v : null;
        }

        function buildWavelengthOptions() {
            const opener = getOpener();
            if (!opener) return [];
            const getSourceRows = opener.getSourceRows;
            const sources = (typeof getSourceRows === 'function')
                ? safeCall(() => getSourceRows(opener.tableSource), [])
                : [];
            const primary = getPrimaryWavelength();
            const out = [{ value: 'all', label: 'All' }];
            if (Array.isArray(sources) && sources.length > 0) {
                for (let i = 0; i < sources.length; i++) {
                    const wl = Number(sources[i]?.wavelength);
                    if (!Number.isFinite(wl) || wl <= 0) continue;
                    const nm = wl * 1000;
                    const label = Number.isFinite(primary) && Math.abs(wl - primary) < 1e-9
                        ? (nm.toFixed(1) + ' nm (primary)')
                        : (nm.toFixed(1) + ' nm');
                    out.push({ value: String(wl), label });
                }
            }
            if (out.length === 1) {
                if (Number.isFinite(primary) && primary > 0) {
                    out.push({ value: String(primary), label: ((primary * 1000).toFixed(1) + ' nm') });
                }
            }
            return out;
        }

        function getAxisInfo() {
            const opener = getOpener();
            if (!opener) return { mode: 'angle', label: 'Object Angle (deg)', unit: 'deg', max: 10 };

            // Priority 1: check optical system first surface thickness.
            // If INF (infinite conjugate), object coordinates MUST be angles.
            let detectedMode = null;
            try {
                const getOpticalSystemRows = opener.getOpticalSystemRows;
                if (typeof getOpticalSystemRows === 'function') {
                    const optRows = safeCall(() => getOpticalSystemRows(opener.tableOpticalSystem), []);
                    const firstSurf = Array.isArray(optRows) && optRows.length > 0 ? optRows[0] : null;
                    if (firstSurf) {
                        const thickness = firstSurf.thickness ?? firstSurf.Thickness;
                        const isInf = thickness === 'INF' || thickness === Infinity || String(thickness).trim().toUpperCase() === 'INF';
                        if (isInf) {
                            detectedMode = 'angle';
                        } else {
                            const numThick = parseFloat(String(thickness));
                            if (Number.isFinite(numThick) && numThick > 0) {
                                detectedMode = 'finite'; // finite conjugate, defer to position field
                            }
                        }
                    }
                }
            } catch (_) {}

            // Priority 2: object rows position field (used when finite conjugate or inconclusive)
            const getObjectRows = opener.getObjectRows;
            const objects = (typeof getObjectRows === 'function')
                ? safeCall(() => getObjectRows(opener.tableObject), [])
                : [];
            const first = Array.isArray(objects) && objects.length > 0 ? objects[0] : null;

            let isAngle;
            if (detectedMode === 'angle') {
                isAngle = true;
            } else {
                const posRaw = String(first?.position ?? first?.object ?? first?.objectType ?? 'Angle');
                isAngle = /\bangle\b/i.test(posRaw);
            }

            const unit = isAngle ? 'deg' : 'mm';
            const label = isAngle ? 'Object Angle (deg)' : 'Object Height (mm)';
            let maxVal = 10;
            if (Array.isArray(objects) && objects.length > 0) {
                const vals = objects.map(o => Number(o?.yHeightAngle)).filter(v => Number.isFinite(v));
                if (vals.length > 0) {
                    maxVal = Math.max(1e-6, Math.max.apply(null, vals.map(v => Math.abs(v))));
                }
            }
            return { mode: isAngle ? 'angle' : 'height', label, unit, max: maxVal };
        }

        function populateSelect(selectEl, options) {
            if (!selectEl) return;
            const current = selectEl.value;
            selectEl.innerHTML = '';
            for (const opt of options) {
                const o = document.createElement('option');
                o.value = opt.value;
                o.textContent = opt.label;
                selectEl.appendChild(o);
            }
            if (current && Array.from(selectEl.options).some(o => o.value === current)) {
                selectEl.value = current;
            }
        }

        function syncAllOptions() {
            const wlSel = document.getElementById('popup-field-mtf-wavelength-select');
            const prevWl = wlSel ? wlSel.value : '';
            populateSelect(wlSel, buildWavelengthOptions());
            if (wlSel && (!prevWl || !Array.from(wlSel.options).some(o => o.value === prevWl))) {
                const primary = getPrimaryWavelength();
                if (Number.isFinite(primary) && Array.from(wlSel.options).some(o => o.value === String(primary))) {
                    wlSel.value = String(primary);
                } else {
                    const firstNumeric = Array.from(wlSel.options).find(o => o.value !== 'all');
                    if (firstNumeric) wlSel.value = firstNumeric.value;
                }
            }

            const axisInfo = getAxisInfo();
            const maxEl = document.getElementById('popup-field-mtf-max-field-input');
            if (maxEl && !maxEl.dataset.cooptInit) {
                maxEl.dataset.cooptInit = '1';
                maxEl.value = String(axisInfo.max || 10);
            }
        }

        window['renderFieldMTF'] = async () => {
            const containerEl = document.getElementById('popup-field-mtf-container');
            if (containerEl) containerEl.innerHTML = '';

            const progressWrapper = document.getElementById('popup-field-mtf-progress-wrapper');
            const progressEl = document.getElementById('popup-field-mtf-progress');
            const progressTextEl = document.getElementById('popup-field-mtf-progress-text');

            const setProgress = (value, text) => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'block';
                    if (progressEl && Number.isFinite(value)) progressEl.value = Math.max(0, Math.min(100, value));
                    if (progressTextEl && typeof text === 'string') progressTextEl.textContent = text;
                } catch (_) {}
            };

            const hideProgress = () => {
                try {
                    if (progressWrapper) progressWrapper.style.display = 'none';
                } catch (_) {}
            };

            const wlSel = document.getElementById('popup-field-mtf-wavelength-select');
            const minFieldEl = document.getElementById('popup-field-mtf-min-field-input');
            const maxFieldEl = document.getElementById('popup-field-mtf-max-field-input');
            const stepsEl = document.getElementById('popup-field-mtf-steps-input');
            const meridionalEl = document.getElementById('popup-field-mtf-meridional-freq-input');
            const sagittalEl = document.getElementById('popup-field-mtf-sagittal-freq-input');
            const samplingEl = document.getElementById('popup-field-mtf-sampling-select');
            const zeroPadEl = document.getElementById('popup-field-mtf-zeropad-select');
            const removePtdEl = document.getElementById('popup-field-mtf-remove-ptd-checkbox');

            const wlValue = wlSel ? String(wlSel.value) : '';
            const primary = getPrimaryWavelength();
            const wavelength = (wlValue === 'all') ? 'all' : Number(wlValue);
            const fieldMin = minFieldEl ? Number(minFieldEl.value) : 0;
            const fieldMax = maxFieldEl ? Number(maxFieldEl.value) : 10;
            const steps = stepsEl ? Number(stepsEl.value) : 21;
            const meridionalFreq = meridionalEl ? Number(meridionalEl.value) : 10;
            const sagittalFreq = sagittalEl ? Number(sagittalEl.value) : 30;
            const sampling = samplingEl ? Number(samplingEl.value) : 256;
            const zeroPadRaw = zeroPadEl ? String(zeroPadEl.value || 'auto') : 'auto';
            const opdDisplayMode = (removePtdEl && removePtdEl.checked)
                ? 'pistonTiltDefocusRemoved'
                : 'pistonTiltRemoved';
            const zeroPadTo = (zeroPadRaw === 'none')
                ? (Number.isFinite(sampling) ? sampling : 256)
                : (zeroPadRaw === 'auto')
                    ? 0
                    : (Number.isFinite(parseInt(zeroPadRaw, 10)) ? parseInt(zeroPadRaw, 10) : 0);

            const axisInfo = getAxisInfo();

            try {
                const opener = getOpener();
                if (!opener) {
                    throw new Error('Opener is not available');
                }
                setProgress(0, 'Starting...');
                await new Promise(r => setTimeout(r, 0));
                if (wavelength !== 'all' && !Number.isFinite(wavelength) && !(Number.isFinite(primary) && primary > 0)) {
                    throw new Error('Primary wavelength is unavailable. Please set Source Primary Wavelength.');
                }

                if (typeof opener.runPortableFieldMtfForPopup !== 'function') {
                    throw new Error('runPortableFieldMtfForPopup is not available on opener');
                }

                const sourceRows = (typeof opener.getSourceRows === 'function')
                    ? (safeCall(() => opener.getSourceRows(opener.tableSource), []) || [])
                    : [];
                const wavelengthList = (() => {
                    const out = [];
                    if (wavelength === 'all') {
                        if (Array.isArray(sourceRows) && sourceRows.length > 0) {
                            for (let i = 0; i < sourceRows.length; i++) {
                                const wl = Number(sourceRows[i]?.wavelength);
                                if (!Number.isFinite(wl) || wl <= 0) continue;
                                if (out.some(v => Math.abs(v - wl) < 1e-9)) continue;
                                out.push(wl);
                            }
                        }
                        if (out.length === 0 && Number.isFinite(primary) && primary > 0) {
                            out.push(primary);
                        }
                    } else {
                        const wl = (Number.isFinite(Number(wavelength)) && Number(wavelength) > 0)
                            ? Number(wavelength)
                            : ((Number.isFinite(primary) && primary > 0) ? primary : 0.5876);
                        out.push(wl);
                    }
                    if (out.length === 0) out.push(0.5876);
                    return out;
                })();

                setProgress(20, 'Computing Object MTF (Rust native)...');
                const nativeResp = await opener.runPortableFieldMtfForPopup({
                    objectIndex: 0,
                    wavelengths: wavelengthList,
                    firstFrequencyLpmm: Number.isFinite(meridionalFreq) ? meridionalFreq : 10,
                    secondFrequencyLpmm: Number.isFinite(sagittalFreq) ? sagittalFreq : 30,
                    fieldMin: Number.isFinite(fieldMin) ? fieldMin : 0,
                    fieldMax: Number.isFinite(fieldMax) ? fieldMax : 10,
                    steps: Number.isFinite(steps) ? steps : 21,
                    samplingSize: Number.isFinite(sampling) ? sampling : 256,
                    zeroPadTo,
                    opdDisplayMode,
                    fieldAxisMode: axisInfo.mode,
                    onProgress: (evt) => {
                        const p = Number(evt && evt.percent);
                        const msg = String((evt && evt.message) || 'Computing Object MTF...');
                        if (Number.isFinite(p)) {
                            setProgress(p, msg);
                        } else {
                            setProgress(20, msg);
                        }
                    },
                });

                if (!window.Plotly || typeof window.Plotly.newPlot !== 'function') {
                    throw new Error('Plotly is not available in Object MTF popup');
                }
                const xAxis = Array.isArray(nativeResp?.xAxis) ? nativeResp.xAxis : [];
                const series = Array.isArray(nativeResp?.series) ? nativeResp.series : [];
                if (!xAxis.length || !series.length) {
                    throw new Error('Native Object MTF did not produce valid data');
                }

                try {
                    const diag = Array.isArray(series?.[0]?.fieldDiagnostics) ? series[0].fieldDiagnostics : [];
                    if (diag.length) {
                        popupLog('📊 [Object MTF diag] field diagnostics count=', diag.length);
                        const jumps = [];
                        for (let i = 1; i < diag.length; i++) {
                            const a = diag[i - 1];
                            const b = diag[i];
                            const d1 = Math.abs(Number(b?.firstValueMeridional) - Number(a?.firstValueMeridional));
                            const d2 = Math.abs(Number(b?.secondValueMeridional) - Number(a?.secondValueMeridional));
                            if ((Number.isFinite(d1) && d1 > 0.12) || (Number.isFinite(d2) && d2 > 0.12)) {
                                jumps.push({
                                    fromField: Number(a?.fieldValue),
                                    toField: Number(b?.fieldValue),
                                    deltaFirstMeridional: d1,
                                    deltaSecondMeridional: d2,
                                    modeFrom: String(a?.effectivePupilSamplingMode || ''),
                                    modeTo: String(b?.effectivePupilSamplingMode || ''),
                                    firstBracketTo: [Number(b?.firstBracketLowLpmm), Number(b?.firstBracketHighLpmm)],
                                    secondBracketTo: [Number(b?.secondBracketLowLpmm), Number(b?.secondBracketHighLpmm)],
                                });
                            }
                        }
                        if (jumps.length) {
                            popupLog('⚠️ [Object MTF diag] potential discontinuities:', jumps.slice(0, 20));
                        }
                    }
                } catch (_) {}

                const getColorForWavelengthPopup = (wl) => {
                    try {
                        if (typeof opener.getColorForWavelength === 'function') {
                            const c = opener.getColorForWavelength(wl);
                            if (typeof c === 'string' && c) return c;
                        }
                    } catch (_) {}
                    const nm = Number(wl) * 1000;
                    if (!Number.isFinite(nm)) return '#2563eb';
                    if (nm < 470) return '#2563eb';
                    if (nm < 530) return '#16a34a';
                    if (nm < 600) return '#f59e0b';
                    return '#dc2626';
                };

                const firstFreqText = String(Number.isFinite(meridionalFreq) ? meridionalFreq.toFixed(1) : '10.0');
                const secondFreqText = String(Number.isFinite(sagittalFreq) ? sagittalFreq.toFixed(1) : '30.0');
                const traces = [];
                for (let i = 0; i < series.length; i++) {
                    const s = series[i] || {};
                    const wl = Number(s.wavelengthUm);
                    const nm = Number.isFinite(wl) ? (wl * 1000).toFixed(1) : 'N/A';
                    const color = getColorForWavelengthPopup(wl);

                    traces.push({
                        x: xAxis,
                        y: Array.isArray(s.meridionalFirst) ? s.meridionalFirst : [],
                        type: 'scatter',
                        mode: 'lines',
                        name: 'Meridional ' + firstFreqText + ' lp/mm (' + nm + 'nm)',
                        line: { color, width: 2, dash: 'solid' },
                    });
                    traces.push({
                        x: xAxis,
                        y: Array.isArray(s.sagittalFirst) ? s.sagittalFirst : [],
                        type: 'scatter',
                        mode: 'lines',
                        name: 'Sagittal ' + firstFreqText + ' lp/mm (' + nm + 'nm)',
                        line: { color, width: 2, dash: 'dot' },
                    });
                    traces.push({
                        x: xAxis,
                        y: Array.isArray(s.meridionalSecond) ? s.meridionalSecond : [],
                        type: 'scatter',
                        mode: 'lines',
                        name: 'Meridional ' + secondFreqText + ' lp/mm (' + nm + 'nm)',
                        line: { color, width: 2, dash: 'dash' },
                    });
                    traces.push({
                        x: xAxis,
                        y: Array.isArray(s.sagittalSecond) ? s.sagittalSecond : [],
                        type: 'scatter',
                        mode: 'lines',
                        name: 'Sagittal ' + secondFreqText + ' lp/mm (' + nm + 'nm)',
                        line: { color, width: 2, dash: 'dashdot' },
                    });
                }

                setProgress(85, 'Rendering Object MTF...');
                await window.Plotly.newPlot(containerEl, traces, {
                    title: firstFreqText + ' / ' + secondFreqText + ' lp/mm',
                    xaxis: { title: axisInfo.label },
                    yaxis: { title: 'MTF', range: [0, 1.05] },
                    margin: { l: 60, r: 20, t: 50, b: 50 },
                    showlegend: true,
                }, { responsive: true, displaylogo: false });

                hideProgress();
                return;
            } catch (err) {
                popupError('[Object MTF Popup] renderFieldMTF failed', err);
                setProgress(100, 'Failed');
                if (containerEl) {
                    const details = String((err && err.message) ? err.message : err || 'Unknown error');
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate Object MTF.<br/><span style="font-size:12px;color:#555;">' + details.replace(/</g, '&lt;').replace(/>/g, '&gt;') + '</span></div>';
                }
            }
        };

        document.getElementById('popup-show-field-mtf-btn').addEventListener('click', () => window.renderFieldMTF());
        window.addEventListener('focus', syncAllOptions);
        window.addEventListener('load', () => syncAllOptions());
    <\/script>
</body>
</html>
                        `);try{S.document.close()}catch{}});const A=document.getElementById("open-transverse-aberration-window-btn");A&&A.addEventListener("click",()=>{if(T.__transverseAberrationPopup&&!T.__transverseAberrationPopup.closed){try{T.__transverseAberrationPopup.focus()}catch{}try{typeof T.__transverseAberrationPopup.renderTransverseAberration=="function"&&T.__transverseAberrationPopup.renderTransverseAberration()}catch{}return}const S=n("Transverse Aberration","width=800,height=600");if(!S){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}T.__transverseAberrationPopup=S,S.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Transverse Aberration</title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .controls {
            padding: 10px 12px;
            background: #f8f8f8;
            border-bottom: 1px solid #ddd;
            display: flex;
            flex-wrap: wrap;
            gap: 8px 10px;
            align-items: center;
            flex: 0 0 auto;
        }
        .controls label { font-size: 12px; color: #333; white-space: nowrap; }
        .controls button {
            padding: 6px 10px;
            border: 1px solid #bbb;
            background: #f8f8f8;
            cursor: pointer;
            border-radius: 4px;
            font-size: 12px;
            color: #333;
        }
        .controls button:hover { background: #e9e9e9; }
        .controls input {
            padding: 5px 8px;
            font-size: 12px;
            border: 1px solid #bbb;
            border-radius: 4px;
            background: white;
            width: 90px;
        }
        .content {
            flex: 1 1 auto;
            min-height: 0;
            overflow: hidden;
            background: white;
        }
        #popup-transverse-aberration-container { height: 100%; min-height: 100%; }
    </style>
    <script src="https://cdn.plot.ly/plotly-2.32.0.min.js"><\/script>
</head>
<body>
    <div class="controls">
        <label for="popup-transverse-ray-count-input">Ray number:</label>
        <input type="number" id="popup-transverse-ray-count-input" value="101" min="9" max="10001" step="1" />
        <span class="note-inline" style="font-size:12px;color:#666;">(Always normalized by stop diameter)</span>
        <button id="popup-show-transverse-aberration-btn" type="button">Show transverse aberration diagram</button>
    </div>
    <div id="popup-transverse-progress-wrapper" style="display:none;padding:10px 12px;border-bottom:1px solid #eee;background:#fff;">
        <div id="popup-transverse-progress-text" style="margin-bottom: 6px; font-size:12px; color:#555;">Starting...</div>
        <progress id="popup-transverse-progressbar" value="0" max="100" style="display:block;width:calc(100% + 24px);margin-left:-12px;height:14px;"></progress>
    </div>
    <div class="content">
        <div id="popup-transverse-aberration-container"></div>
    </div>

    <script>
        function getOpenerEl(id) {
            try {
                return window.opener && window.opener.document ? window.opener.document.getElementById(id) : null;
            } catch (e) {
                return null;
            }
        }

        function syncFromOpener() {
            const openerRay = getOpenerEl('transverse-ray-count-input');
            const popupRay = document.getElementById('popup-transverse-ray-count-input');
            if (openerRay && popupRay) {
                popupRay.value = openerRay.value;
            }
        }

        window['renderTransverseAberration'] = async () => {
            const progressWrap = document.getElementById('popup-transverse-progress-wrapper');
            const progressBar = document.getElementById('popup-transverse-progressbar');
            const progressText = document.getElementById('popup-transverse-progress-text');
            const setProgress = (percent, message) => {
                try {
                    if (progressWrap) progressWrap.style.display = 'block';
                    if (progressBar && Number.isFinite(percent)) progressBar.value = Math.max(0, Math.min(100, percent));
                    if (progressText) progressText.textContent = message || '';
                } catch (_) {}
            };
            const hideProgress = () => {
                try {
                    if (progressWrap) progressWrap.style.display = 'none';
                } catch (_) {}
            };
            const onProgress = (evt) => {
                const p = Number(evt?.percent);
                const msg = (evt && (evt.message || evt.phase)) ? String(evt.message || evt.phase) : '';
                setProgress(Number.isFinite(p) ? p : 0, msg);
            };

            const popupRay = document.getElementById('popup-transverse-ray-count-input');
            const rayCount = popupRay ? parseInt(popupRay.value, 10) : 51;
            const openerRay = getOpenerEl('transverse-ray-count-input');
            if (openerRay && Number.isFinite(rayCount)) {
                openerRay.value = String(rayCount);
            }

            const containerEl = document.getElementById('popup-transverse-aberration-container');
            if (containerEl) containerEl.innerHTML = '';

            try {
                if (!window.opener || typeof window.opener.showTransverseAberrationDiagram !== 'function') {
                    throw new Error('showTransverseAberrationDiagram is not available on opener');
                }
                setProgress(0, 'Starting...');
                await window.opener.showTransverseAberrationDiagram({
                    rayCount: Number.isFinite(rayCount) ? rayCount : 51,
                    containerElement: containerEl,
                    onProgress
                });
                hideProgress();
            } catch (err) {
                console.error(err);
                if (containerEl) {
                    containerEl.innerHTML = '<div style="padding:20px;color:red;font-family:Arial;">Failed to generate transverse aberration diagram. Check console.</div>';
                }
                setProgress(100, 'Failed');
            }
        };

        document.getElementById('popup-show-transverse-aberration-btn').addEventListener('click', () => {
            window.renderTransverseAberration();
        });

        window.addEventListener('focus', syncFromOpener);
        syncFromOpener();

        // Auto-render immediately on open
        window.addEventListener('load', () => {
            try { window.renderTransverseAberration(); } catch (_) {}
        });
    <\/script>
</body>
</html>
                        `);try{S.document.close()}catch{}});const x=document.getElementById("open-settings-btn"),_=()=>{Oi();const S=(()=>{try{return new URL(window.location.href).searchParams.get("coopt_settings_window")==="1"}catch{return!1}})();if(ze()&&!S){(async()=>{try{const{WebviewWindow:f}=await Re(async()=>{const{WebviewWindow:C}=await import("./webviewWindow-ZJkDY8Dw.js");return{WebviewWindow:C}},__vite__mapDeps([3,4,0,1,2])),w="settings-window",b=await f.getByLabel(w);if(b){await b.setFocus();return}const v=new URL(window.location.href);v.searchParams.set("coopt_settings_window","1");const M=Et();M?v.searchParams.set("coopt_force_mode",M):v.searchParams.delete("coopt_force_mode"),new f(w,{title:"Settings",url:v.toString(),width:520,height:620,resizable:!0,focus:!0})}catch(f){console.error("❌ [Settings][Desktop] WebviewWindow error:",f),alert("Failed to open Settings window.")}})();return}if(T.__settingsPopup&&!T.__settingsPopup.closed){try{T.__settingsPopup.focus()}catch{}return}const N=ze()&&S?window:window.open("","Settings","width=520,height=340");if(!N){alert(`ポップアップがブロックされました。ブラウザのポップアップブロッカーを無効にしてください。

Popup was blocked. Please disable your browser's popup blocker.`);return}ze()&&S||(T.__settingsPopup=N),N.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title></title>
    <style>
        html, body { height: 100%; }
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: #f4f4f4;
        }
        .header {
            padding: 10px 12px;
            background: #f8f8f8;
            color: #333;
            border-bottom: 1px solid #ddd;
            font-size: 14px;
            font-weight: 600;
        }
        .content {
            padding: 12px;
            background: #fff;
            flex: 1 1 auto;
            overflow: auto;
        }
        .section-title { font-size: 13px; font-weight: 600; color: #333; margin: 0 0 8px 0; }
        .help { font-size: 12px; color: #666; margin: 0 0 10px 0; line-height: 1.35; }
        .radio-group { display: flex; flex-direction: column; gap: 8px; margin: 8px 0 12px 0; }
        label { font-size: 13px; color: #333; }
        .footer {
            padding: 10px 12px;
            border-top: 1px solid #ddd;
            background: #f8f8f8;
            display: flex;
            justify-content: flex-end;
            gap: 8px;
        }
        code { font-family: Menlo, Consolas, monospace; font-size: 12px; }
    </style>
</head>
<body>
    <div class="header"></div>
    <div class="content">
        <div class="section-title">Glass Map: Default Manufacturers</div>
        <div class="help">
            Choose which manufacturers are enabled by default when opening Glass Map.
            <br />If nothing is selected, Glass Map will show all manufacturers.
        </div>

        <div class="checkbox-group" style="display:flex;flex-direction:column;gap:8px;margin:8px 0 14px 0;">
            <label><input type="checkbox" class="glassmap-mfr-cb" value="SCHOTT" /> SCHOTT</label>
            <label><input type="checkbox" class="glassmap-mfr-cb" value="HOYA" /> HOYA</label>
            <label><input type="checkbox" class="glassmap-mfr-cb" value="HIKARI" /> HIKARI</label>
            <label><input type="checkbox" class="glassmap-mfr-cb" value="OHARA" /> OHARA</label>
            <label><input type="checkbox" class="glassmap-mfr-cb" value="Sumita" /> Sumita</label>
            <label><input type="checkbox" class="glassmap-mfr-cb" value="CDGM" /> CDGM</label>
            <label><input type="checkbox" class="glassmap-mfr-cb" value="Special" /> Special</label>
        </div>

        <div class="section-title">Dark Mode</div>
        <div class="help">
            Enable VS Code-style dark mode for the entire UI.
        </div>
        <label style="margin: 8px 0 14px 0; display: block;">
            <input type="checkbox" id="dark-mode-cb" /> Enable Dark Mode
        </label>

        <div class="section-title">Infinite Field: Pupil Sampling Mode</div>
        <div class="help">
            Fix the sampling mode used for infinite-field wavefront/PSF/MTF generation.
            <br />
            This sets <code>__COOPT_FORCE_INFINITE_PUPIL_MODE</code> to <code>stop</code> or <code>entrance</code>.
        </div>

        <div class="radio-group">
            <label><input type="radio" name="force-mode" value="" /> Auto (default)</label>
            <label><input type="radio" name="force-mode" value="stop" /> Force <code>stop</code></label>
            <label><input type="radio" name="force-mode" value="entrance" /> Force <code>entrance</code></label>
        </div>

        <div class="help" style="margin-top: 6px;">
            Note: Changes take effect on the next calculation.
        </div>
    </div>
    <div class="footer"></div>

    <script>
        const KEY = 'coopt.forceInfinitePupilMode';
        const isDesktopRuntime = !!(window && (window.__TAURI_INTERNALS__ || window.__TAURI__));
        const GLASS_MAP_MFR_KEY = 'coopt.glassMap.defaultManufacturers';
        const DARK_MODE_KEY = 'coopt.darkMode';
        const sanitize = (v) => {
            const s = (typeof v === 'string') ? v.trim().toLowerCase() : '';
            return (s === 'stop' || s === 'entrance') ? s : '';
        };

        const sanitizeMfrList = (list) => {
            if (!Array.isArray(list)) return [];
            const allow = new Set(['SCHOTT', 'HOYA', 'HIKARI', 'OHARA', 'SUMITA', 'CDGM', 'SPECIAL']);
            const out = [];
            for (const v of list) {
                const s = String(v ?? '').trim();
                if (!s) continue;
                const upper = s.toUpperCase();
                if (!allow.has(upper)) continue;
                // Preserve canonical casing used in the checkboxes.
                if (upper === 'SUMITA') out.push('Sumita');
                else if (upper === 'SPECIAL') out.push('Special');
                else out.push(upper);
            }
            // Deduplicate
            return Array.from(new Set(out));
        };

        function getOpener() {
            try { return window.opener || null; } catch (_) { return null; }
        }

        async function readDesktopModeDirect() {
            try {
                const invoke = window?.__TAURI_INTERNALS__?.invoke || window?.__TAURI__?.core?.invoke;
                if (typeof invoke !== 'function') return '';
                const raw = await invoke('read_desktop_setting', { key: KEY });
                return sanitize(raw);
            } catch (_) {
                return '';
            }
        }

        async function writeDesktopModeDirect(mode) {
            const m = sanitize(mode);
            try {
                const invoke = window?.__TAURI_INTERNALS__?.invoke || window?.__TAURI__?.core?.invoke;
                if (typeof invoke !== 'function') return;
                await invoke('write_desktop_setting', { key: KEY, value: m || null });
            } catch (_) {}
        }

        function getFromWindow(target) {
            if (!target) return '';
            try {
                if (typeof target.__cooptGetForceInfinitePupilMode === 'function') {
                    const m = sanitize(target.__cooptGetForceInfinitePupilMode());
                    if (m) return m;
                }
            } catch (_) {}
            try {
                return sanitize(target.__COOPT_FORCE_INFINITE_PUPIL_MODE ?? target.COOPT_FORCE_INFINITE_PUPIL_MODE);
            } catch (_) {
                return '';
            }
        }

        function setToWindow(target, mode) {
            if (!target) return;
            try {
                if (typeof target.__cooptSetForceInfinitePupilMode === 'function') {
                    target.__cooptSetForceInfinitePupilMode(mode);
                    return;
                }
            } catch (_) {}
            try {
                if (mode) {
                    target.__COOPT_FORCE_INFINITE_PUPIL_MODE = mode;
                    target.COOPT_FORCE_INFINITE_PUPIL_MODE = mode;
                } else {
                    try { delete target.__COOPT_FORCE_INFINITE_PUPIL_MODE; } catch (_) { target.__COOPT_FORCE_INFINITE_PUPIL_MODE = undefined; }
                    try { delete target.COOPT_FORCE_INFINITE_PUPIL_MODE; } catch (_) { target.COOPT_FORCE_INFINITE_PUPIL_MODE = undefined; }
                }
            } catch (_) {}
        }

        function getCurrent() {
            const selfMode = getFromWindow(window);
            if (selfMode) return selfMode;

            const o = getOpener();
            const openerMode = getFromWindow(o);
            if (openerMode) return openerMode;

            try {
                const stored = sanitize(localStorage.getItem(KEY));
                if (stored) return stored;
            } catch (_) {}

            try {
                const fromUrl = sanitize(new URL(window.location.href).searchParams.get('coopt_force_mode'));
                if (fromUrl) return fromUrl;
            } catch (_) {}

            return '';
        }

        function applyMode(mode) {
            const m = sanitize(mode);
            setToWindow(window, m);

            const o = getOpener();
            setToWindow(o, m);

            try {
                if (m) localStorage.setItem(KEY, m);
                else localStorage.removeItem(KEY);
            } catch (_) {}

            try {
                if (typeof window.__cooptBroadcastForceInfinitePupilMode === 'function') {
                    window.__cooptBroadcastForceInfinitePupilMode(m);
                }
            } catch (_) {}

            writeDesktopModeDirect(m);
        }

        async function hydrateFromDesktopStore() {
            const direct = await readDesktopModeDirect();
            if (direct) {
                setToWindow(window, direct);
                try { localStorage.setItem(KEY, direct); } catch (_) {}
                syncUI();
                return;
            }
        }

        function syncUI() {
            const cur = getCurrent();
            const radios = document.querySelectorAll('input[name="force-mode"]');
            radios.forEach(r => {
                r.checked = (sanitize(r.value) === cur);
                if (cur === '' && sanitize(r.value) === '') r.checked = true;
            });

            // Glass Map manufacturers
            let stored = [];
            try {
                stored = sanitizeMfrList(JSON.parse(localStorage.getItem(GLASS_MAP_MFR_KEY) || '[]'));
            } catch (_) {
                stored = [];
            }
            const storedSet = new Set(stored.map(s => String(s).toUpperCase()));
            document.querySelectorAll('.glassmap-mfr-cb').forEach(cb => {
                const v = String(cb.value || '');
                cb.checked = storedSet.has(v.toUpperCase());
            });

            // Dark Mode
            const darkModeCb = document.getElementById('dark-mode-cb');
            if (darkModeCb) {
                let isDark = false;
                try {
                    isDark = localStorage.getItem(DARK_MODE_KEY) === 'true';
                } catch (_) {}
                darkModeCb.checked = isDark;
            }
        }

        function saveGlassMapMfrSelection() {
            const selected = [];
            document.querySelectorAll('.glassmap-mfr-cb').forEach(cb => {
                if (cb.checked) selected.push(cb.value);
            });
            const sanitized = sanitizeMfrList(selected);
            try {
                if (sanitized.length) localStorage.setItem(GLASS_MAP_MFR_KEY, JSON.stringify(sanitized));
                else localStorage.removeItem(GLASS_MAP_MFR_KEY);
            } catch (_) {}
        }

        function applyDarkMode(enabled) {
            const o = getOpener();
            try {
                if (o && typeof o.__cooptSetDarkMode === 'function') {
                    o.__cooptSetDarkMode(enabled);
                }
            } catch (_) {}
            
            try {
                localStorage.setItem(DARK_MODE_KEY, enabled ? 'true' : 'false');
            } catch (_) {}
        }

        document.querySelectorAll('input[name="force-mode"]').forEach(r => {
            r.addEventListener('change', () => {
                if (r.checked) applyMode(r.value);
            });
        });

        document.querySelectorAll('.glassmap-mfr-cb').forEach(cb => {
            cb.addEventListener('change', () => {
                saveGlassMapMfrSelection();
            });
        });

        const darkModeCb = document.getElementById('dark-mode-cb');
        if (darkModeCb) {
            darkModeCb.addEventListener('change', () => {
                applyDarkMode(darkModeCb.checked);
            });
        }

        window.addEventListener('focus', syncUI);
        syncUI();
        hydrateFromDesktopStore();
    <\/script>
</body>
</html>
                        `);try{N.document.close()}catch{}};x&&!ze()&&x.addEventListener("click",_),!ze()&&!T.__cooptSettingsButtonDelegatedBound&&(document.addEventListener("click",S=>{try{const N=S.target;if(!N||typeof N.closest!="function"||!N.closest("#open-settings-btn"))return;_()}catch{}}),T.__cooptSettingsButtonDelegatedBound=!0),(()=>{const S="coopt.darkMode";function N(w){w?document.body.classList.add("dark-mode"):document.body.classList.remove("dark-mode")}function f(){try{return localStorage.getItem(S)==="true"}catch{return!1}}T.__cooptSetDarkMode=w=>{N(w)},N(f())})()}function wl(){const e=document.getElementById("transform-surface-select"),t=document.getElementById("show-local-coords-btn"),n=document.getElementById("cancel-transform-btn"),o=document.getElementById("save-local-coords-btn"),r=document.getElementById("transform-error-bar"),s=document.getElementById("transform-error-text"),a=document.getElementById("transform-progress-wrapper"),c=document.getElementById("transform-progress-text"),i=document.getElementById("transform-progressbar"),l=x=>{r&&s&&(s.textContent=x,r.style.display="")},m=()=>{r&&(r.style.display="none")},p=(x,_)=>{a&&(a.style.display="block"),i&&Number.isFinite(x)&&(i.value=Math.max(0,Math.min(100,x))),c&&_&&(c.textContent=_)},d=()=>{a&&(a.style.display="none")},u=x=>{const _=String(x?.surfType??x?.["surf type"]??x?.type??"").toLowerCase().replace(/\s+/g,"");return _==="coordbreak"||_==="coordinatebreak"||_==="cb"||_==="coordtrans"||_==="coordinatetransform"||_==="ct"},h=x=>{const _=x?.["object type"]??x?.object??x?.Object;return String(_??"").toLowerCase()==="object"},g=x=>String(x?.surfType??x?.["surf type"]??x?.type??"").toLowerCase()==="gap",y=(x,_)=>{if(!Array.isArray(x)||!Number.isFinite(_))return null;const S=Math.floor(_);if(S<=0)return null;let N=0;for(let f=0;f<x.length;f++){const w=x[f];if(!u(w)&&!h(w)&&!g(w)&&(N++,N===S))return f}return null};t&&t.addEventListener("click",async function(){m();try{const x=parseInt(e?.value||"",10);if(!Number.isFinite(x)){l("Please select a surface first.");return}const _=T.getOpticalSystemRows;if(typeof _!="function"){l("Optical system data not available.");return}const S=_();if(!S||S.length===0){l("No optical system data. Please load or create an optical system.");return}const N=y(S,x);if(N===null){l("Selected surface could not be resolved.");return}t.disabled=!0,n&&(n.style.display=""),o&&(o.style.display="none"),T._transformCalculationCancelled=!1;const f=T.calculateAllSurfacesLocalCoordinates;if(typeof f!="function"){l("Coordinate transformation function not available."),t.disabled=!1,n&&(n.style.display="none");return}const w=await f(S,N,(b,v)=>p(b,v));T._cachedLocalCoords=w,T._showLocalCoords=!0,T.tableOpticalSystem&&T.tableOpticalSystem.redraw(),o&&(o.style.display=""),d()}catch(x){console.error("Coordinate transformation error:",x),l(x.message||"Failed to calculate local coordinates."),d()}finally{t.disabled=!1,n&&(n.style.display="none")}}),n&&n.addEventListener("click",function(){T._transformCalculationCancelled=!0,n&&(n.style.display="none"),d(),l("Calculation cancelled by user.")}),o&&o.addEventListener("click",function(){try{if(!T._cachedLocalCoords){l("No coordinate data to save. Please calculate first.");return}const x=T._cachedLocalCoords,_=JSON.stringify(x,null,2),S=new Blob([_],{type:"application/json"}),N=URL.createObjectURL(S),f=new Date().toISOString().replace(/[:.]/g,"-").slice(0,19),b=`local-coords-surf${x.metadata?.targetSurfaceIndex??"unknown"}-${f}.json`,v=document.createElement("a");v.href=N,v.download=b,v.click(),URL.revokeObjectURL(N)}catch(x){console.error("Save error:",x),l("Failed to save JSON file: "+x.message)}}),ji();const A=document.getElementById("analysis-select");A&&A.addEventListener("change",()=>{const x=A.value;if(!x)return;A.value="";const S={"spot-diagram":"open-spot-diagram-window-btn","spherical-aberration":"open-spherical-aberration-window-btn",astigmatism:"open-astigmatism-window-btn",distortion:"open-distortion-window-btn","distortion-grid":"open-distortion-grid-window-btn","magnification-chromatic-aberration":"open-magnification-chromatic-aberration-window-btn","integrated-aberration":"open-integrated-aberration-window-btn","transverse-aberration":"open-transverse-aberration-window-btn",opd:"open-opd-window-btn",psf:"open-psf-window-btn",mtf:"open-mtf-window-btn","through-focus-spot":"open-through-focus-spot-window-btn","through-focus-mtf":"open-through-focus-mtf-window-btn","field-mtf":"open-field-mtf-window-btn"}[x];if(S){try{typeof T.setupAnalysisWindows=="function"&&T.setupAnalysisWindows()}catch{}const N=document.getElementById(S);N&&N.click()}})}function ji(){const e=document.getElementById("transform-surface-select");if(e)try{const t=T.getOpticalSystemRows;if(typeof t!="function")return;const n=t();if(!n||n.length===0)return;e.innerHTML='<option value="">Select surface...</option>';let o=0;n.forEach((r,s)=>{if(String(r?.["object type"]??r?.object??"").toLowerCase()==="object")return;const c=String(r?.surfType??r?.type??"").toLowerCase();if(c==="ct"||c==="coordtrans"||c==="coordinatebreak"||c==="coord trans"||c==="coordinate break")return;o+=1;const i=document.createElement("option");i.value=String(o);let l=`Surf ${o}`;r.comment?l+=`: ${r.comment}`:r.material&&r.material!=="AIR"&&(l+=`: ${r.material}`),i.textContent=l,e.appendChild(i)})}catch(t){console.error("Error updating transform surface select:",t)}}const Qt=window;function Li(){const e=document.getElementById("surface-number-select");if(!e)return;const t=e.selectedOptions&&e.selectedOptions.length>0?e.selectedOptions[0]:null,n=t&&t.dataset&&t.dataset.rowId?String(t.dataset.rowId):"",o=e.value!==void 0&&e.value!==null?String(e.value):"";e.innerHTML='<option value="">Select Surf</option>';let r=null;try{const l=Qt.tableOpticalSystem?.getData?.();Array.isArray(l)&&l.length>0&&(r=l)}catch{}if(!r)try{const l=Qt.opticalSystemTabulator?.getData?.();Array.isArray(l)&&l.length>0&&(r=l)}catch{}if(!r)try{const l=Qt.getOpticalSystemRows?.();Array.isArray(l)&&l.length>0&&(r=l)}catch{}if(!r)try{const l=je(Qt.tableOpticalSystem);Array.isArray(l)&&l.length>0&&(r=l)}catch{}if(!r)try{const l=je();Array.isArray(l)&&l.length>0&&(r=l)}catch{}if(!r||r.length===0)return;const s=Xr(r);let a=null,c=null;for(const l of s){const m=document.createElement("option");m.value=String(l.value),m.textContent=l.label,l.rowId!==void 0&&l.rowId!==null&&String(l.rowId)!==""&&(m.dataset.rowId=String(l.rowId)),Number.isInteger(l.rowIndex)&&(m.dataset.rowIndex=String(l.rowIndex)),typeof l.label=="string"&&l.label.includes("(Image)")&&(a=String(l.value)),c=String(l.value),e.appendChild(m)}let i=!1;if(n){for(const l of e.options)if(l&&l.dataset&&String(l.dataset.rowId||"")===n){e.value=String(l.value),i=!0;break}}i||(o!==""&&e.querySelector(`option[value="${CSS.escape(o)}"]`)?e.value=o:a!==null?e.value=a:c!==null&&(e.value=c));try{const l=Qt.__spotDiagramPopup;l&&!l.closed&&(typeof l.__cooptSpotPopupSyncAll=="function"?l.__cooptSpotPopupSyncAll():typeof l.postMessage=="function"&&l.postMessage({action:"coopt-spot-sync"},"*"))}catch{}}function br(){Li()}function Qn(e){return Number.isFinite(e)}function vl(){const e=ga(),t=[];for(const n of e)if(Array.isArray(n))for(const o of n){if(!o||typeof o!="object")continue;const r=o.nd,s=o.vd;!Qn(r)||!Qn(s)||s<=0||r<=0||t.push({name:String(o.name??""),manufacturer:String(o.manufacturer??"Unknown").trim()||"Unknown",nd:r,vd:s,price:Qn(o.price)?Number(o.price):null})}return t}function Sl(){const e=document.querySelector('script[src*="plotly" i]');return e&&e.src?e.src:null}function wr(e,t){if(typeof window>"u"||typeof document>"u")throw new Error("openGlassMapWindow must be called in a browser");if(typeof e!="function")throw new TypeError("openGlassMapWindow(onRegionSelected): onRegionSelected must be a function");if(typeof t<"u"&&typeof t!="function")throw new TypeError("openGlassMapWindow(onRegionSelected, onGlassSelected): onGlassSelected must be a function");const n=vl(),o=Sl(),r=window.open("","coopt_glass_map","popup=yes,width=800,height=600");if(!r)return alert("Popup was blocked. Please allow popups for this site and try again."),null;r.__COOPT_GLASS_POINTS__=n,r.__COOPT_ON_REGION_SELECTED__=e,r.__COOPT_ON_GLASS_SELECTED__=typeof t=="function"?t:null;const s=`<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Glass Map</title>
  <style>
    html, body { height: 100%; margin: 0; font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; }
    .wrap { height: 100%; display: flex; flex-direction: column; }
    .toolbar { display: flex; gap: 8px; align-items: center; padding: 8px 10px; border-bottom: 1px solid rgba(0,0,0,0.1); }
    .toolbar button { padding: 6px 10px; }
    .hint { color: rgba(0,0,0,0.7); font-size: 12px; }
    .header { padding: 10px 10px 0; }
    .title { font-size: 22px; font-weight: 600; color: rgba(0,0,0,0.85); margin: 0 0 6px; }
    .note { font-size: 12px; color: #666; margin: 0 0 10px; padding: 8px 12px; background-color: #f9f9f9; border-left: 3px solid #007acc; border-radius: 3px; }
    #plot { flex: 1; min-height: 0; }
  </style>
  ${o?`<script src="${o}"><\/script>`:""}
</head>
<body>
  <div class="wrap">
    <div class="toolbar">
      <button id="apply" disabled>Apply Region</button>
      <span class="hint">Drag a box to select a region (Box Select is active by default).</span>
    </div>
    <div class="header">
      <div class="title">Glass Map</div>
      <div class="note"><strong>Note:</strong> Set default manufacturers in Settings.<br>Price indices differ by manufacturer (e.g., HOYA uses a vendor-specific index).</div>
    </div>
    <div id="plot"></div>
  </div>

  <script>
    (function() {
      const points = window.__COOPT_GLASS_POINTS__ || [];
      const applyBtn = document.getElementById('apply');

      let lastRegion = null;

      function ensurePlotlyReady() {
        if (window.Plotly) return Promise.resolve(window.Plotly);
        try {
          if (window.opener && window.opener.Plotly) {
            window['Plotly'] = window.opener.Plotly;
            return Promise.resolve(window.Plotly);
          }
        } catch (e) {}
        return new Promise((resolve, reject) => {
          const start = Date.now();
          const timer = setInterval(() => {
            if (window.Plotly) {
              clearInterval(timer);
              resolve(window.Plotly);
              return;
            }
            if (Date.now() - start > 8000) {
              clearInterval(timer);
              reject(new Error('Plotly failed to load in popup window'));
            }
          }, 50);
        });
      }

      function splitByPrice(points) {
        const priced = [];
        const missing = [];
        for (const p of points) {
          // For log-scaled coloring, require strictly positive price values.
          if (Number.isFinite(p.price) && p.price > 0) priced.push(p);
          else missing.push(p);
        }
        return { priced, missing };
      }

      function log10Safe(x) {
        if (!Number.isFinite(x) || x <= 0) return NaN;
        // Math.log10 is widely supported, but keep a fallback.
        return (typeof Math.log10 === 'function') ? Math.log10(x) : (Math.log(x) / Math.LN10);
      }

      function buildLogTickValues(minPrice, maxPrice) {
        // Nice tick candidates for typical relative-price indices.
        const candidates = [
          1, 2, 3, 5,
          8, 10,
          13, 20,
          34, 50,
          80, 100,
          130, 200,
          340, 500,
          800, 1000
        ];

        /** @type {number[]} */
        const ticks = candidates.filter(v => v >= minPrice && v <= maxPrice);
        if (!ticks.includes(minPrice)) ticks.unshift(minPrice);
        if (!ticks.includes(maxPrice)) ticks.push(maxPrice);

        // Deduplicate (e.g., when min/max coincide with candidates).
        const unique = Array.from(new Set(ticks)).sort((a, b) => a - b);

        // Avoid overlapping tick labels near the upper end (e.g., 34 vs 36.7).
        // If two adjacent ticks are too close in log space, drop the lower one.
        while (unique.length >= 2) {
          const last = unique[unique.length - 1];
          const prev = unique[unique.length - 2];
          const d = log10Safe(last) - log10Safe(prev);
          if (!Number.isFinite(d) || d >= 0.06) break; // ~15% ratio
          unique.splice(unique.length - 2, 1);
        }

        return unique;
      }

      function makeHover(p) {
        const priceStr = Number.isFinite(p.price) ? p.price.toFixed(4) : 'null';
        return (
          String(p.manufacturer) + ' ' + String(p.name) +
          '<br>n<sub>d</sub>=' + Number(p.nd).toFixed(5) +
          '<br>V<sub>d</sub>=' + Number(p.vd).toFixed(2) +
          '<br>price=' + priceStr
        );
      }

      function groupByManufacturer(points) {
        /** @type {Map<string, any[]>} */
        const m = new Map();
        for (const p of points) {
          const key = (String(p?.manufacturer ?? 'Unknown').trim() || 'Unknown');
          const arr = m.get(key);
          if (arr) arr.push(p);
          else m.set(key, [p]);
        }
        return m;
      }

      function getDefaultManufacturerSelectionUpper() {
        // Settings key written by the main app Settings popup.
        const KEY = 'coopt.glassMap.defaultManufacturers';
        try {
          const raw = localStorage.getItem(KEY);
          if (!raw) return new Set();
          const parsed = JSON.parse(raw);
          if (!Array.isArray(parsed)) return new Set();
          const s = new Set();
          for (const v of parsed) {
            const name = String(v ?? '').trim();
            if (!name) continue;
            s.add(name.toUpperCase());
          }
          return s;
        } catch (_) {
          return new Set();
        }
      }

      function renderPlot(Plotly) {
        const { priced, missing } = splitByPrice(points);

        const pricedX = priced.map(p => p.vd);
        const pricedY = priced.map(p => p.nd);
        const pricedC = priced.map(p => p.price);
        const pricedText = priced.map(makeHover);

        const missingX = missing.map(p => p.vd);
        const missingY = missing.map(p => p.nd);
        const missingText = missing.map(makeHover);

        const pricedMin = pricedC.length ? Math.min.apply(null, pricedC) : 1;
        const pricedMax = pricedC.length ? Math.max.apply(null, pricedC) : 1;

        // Log-scaled coloring emphasizes relative (ratio) differences.
        const pricedMinLog = log10Safe(pricedMin);
        const pricedMaxLog = log10Safe(pricedMax);

        // Match original co-opt price colorscale (low->high: blue->green->red).
        const PRICE_COLORSCALE = [
          [0.0, 'rgb(0, 0, 255)'],
          [0.5, 'rgb(0, 255, 0)'],
          [1.0, 'rgb(255, 0, 0)']
        ];

        const tickPrices = buildLogTickValues(pricedMin, pricedMax);
        const tickVals = tickPrices.map(v => log10Safe(v)).filter(v => Number.isFinite(v));
        const tickText = tickPrices.map(v => String(v));

        const traces = [];

        // Always show the price colorbar, independent of manufacturer toggles.
        // Keep the marker fully transparent and outside fixed axis ranges.
        traces.push({
          type: 'scatter',
          mode: 'markers',
          name: 'price',
          showlegend: false,
          x: [0, 0],
          y: [0, 0],
          hoverinfo: 'skip',
          marker: {
            size: 1,
            opacity: 0,
            color: [pricedMinLog, pricedMaxLog],
            cmin: pricedMinLog,
            cmax: pricedMaxLog,
            colorscale: PRICE_COLORSCALE,
            showscale: true,
            colorbar: {
              title: 'price (log)',
              tickmode: 'array',
              tickvals: tickVals,
              ticktext: tickText,
            },
            line: { width: 0 }
          }
        });

        // Per-manufacturer traces so legend can toggle vendors (useful when nd/vd overlap).
        const byMfr = groupByManufacturer(points);
        const manufacturers = Array.from(byMfr.keys()).sort((a, b) => a.localeCompare(b));

        const defaultSelectedUpper = getDefaultManufacturerSelectionUpper();
        const hasDefaultSelection = defaultSelectedUpper && defaultSelectedUpper.size > 0;

        for (const mfr of manufacturers) {
          const arr = byMfr.get(mfr) || [];
          const pricedLocal = arr.filter(p => Number.isFinite(p.price) && p.price > 0);
          const missingLocal = arr.filter(p => !(Number.isFinite(p.price) && p.price > 0));

          const mfrUpper = String(mfr).toUpperCase();
          const isSelected = !hasDefaultSelection || defaultSelectedUpper.has(mfrUpper);
          const initialVisible = isSelected ? true : 'legendonly';

          // Legend entry: always black marker; when toggled off, Plotly greys it.
          traces.push({
            type: 'scatter',
            mode: 'markers',
            name: mfr,
            legendgroup: mfr,
            showlegend: true,
            visible: initialVisible,
            // Plotly may hide legend items for traces with zero points.
            // Keep a single dummy point outside the fixed axis ranges.
            x: [0],
            y: [0],
            hoverinfo: 'skip',
            marker: { size: 8, color: 'rgb(0,0,0)' }
          });

          if (pricedLocal.length > 0) {
            traces.push({
              type: 'scattergl',
              mode: 'markers',
              name: mfr,
              legendgroup: mfr,
              showlegend: false,
              visible: initialVisible,
              x: pricedLocal.map(p => p.vd),
              y: pricedLocal.map(p => p.nd),
              customdata: pricedLocal.map(p => [
                p.name,
                p.manufacturer,
                Number.isFinite(p.price) ? p.price : null,
                p.nd,
                p.vd,
              ]),
              text: pricedLocal.map(makeHover),
              hoverinfo: 'text',
              marker: {
                size: 10,
                opacity: 0.9,
                color: pricedLocal.map(p => log10Safe(p.price)),
                cmin: pricedMinLog,
                cmax: pricedMaxLog,
                colorscale: PRICE_COLORSCALE,
                showscale: false,
                line: { width: 0 }
              }
            });
          }

          if (missingLocal.length > 0) {
            traces.push({
              type: 'scattergl',
              mode: 'markers',
              name: mfr,
              legendgroup: mfr,
              showlegend: false,
              visible: initialVisible,
              x: missingLocal.map(p => p.vd),
              y: missingLocal.map(p => p.nd),
              customdata: missingLocal.map(p => [
                p.name,
                p.manufacturer,
                null,
                p.nd,
                p.vd,
              ]),
              text: missingLocal.map(makeHover),
              hoverinfo: 'text',
              marker: {
                size: 10,
                opacity: 0.75,
                color: 'rgba(160,160,160,0.9)',
                line: { width: 0 }
              }
            });
          }
        }

        const layout = {
          margin: { l: 60, r: 30, t: 70, b: 55 },
          dragmode: 'select',
          xaxis: {
            title: { text: 'V<sub>d</sub>' },
            range: [105, 15],
            dtick: 10,
            zeroline: false,
            showgrid: true
          },
          yaxis: {
            title: { text: 'n<sub>d</sub>' },
            range: [1.3, 2.2],
            zeroline: false,
            showgrid: true
          },
          legend: { orientation: 'h', x: 0, y: 1.02, yanchor: 'bottom', groupclick: 'togglegroup' }
        };

        const config = {
          displayModeBar: true,
          responsive: true
        };

        const plotEl = document.getElementById('plot');

        Plotly.newPlot(plotEl, traces, layout, config);

        // Attach event handlers only when Plotly has augmented the div.
        if (plotEl && typeof plotEl.on === 'function') {
          // Double-click on a point: propose replacing current material in the opener.
          // Plotly's built-in plotly_doubleclick event doesn't provide point info reliably,
          // so we detect a double-click via two plotly_click events close in time.
          let lastClickAt = 0;
          let lastClickKey = '';
          plotEl.on('plotly_click', function(ev) {
            try {
              const pt = ev && ev.points && ev.points[0] ? ev.points[0] : null;
              const cd = pt && pt.customdata ? pt.customdata : null;
              if (!cd || cd.length < 4) return;

              const name = String(cd[0] ?? '').trim();
              const manufacturer = String(cd[1] ?? 'Unknown').trim() || 'Unknown';
              const price = Number.isFinite(cd[2]) ? Number(cd[2]) : null;
              const nd = Number.isFinite(cd[3]) ? Number(cd[3]) : null;
              const vd = Number.isFinite(cd[4]) ? Number(cd[4]) : null;
              if (!name) return;

              const now = Date.now();
              const key = manufacturer + '\\n' + name;
              const isDouble = (now - lastClickAt) < 350 && key === lastClickKey;
              lastClickAt = now;
              lastClickKey = key;
              if (!isDouble) return;

              if (typeof window.__COOPT_ON_GLASS_SELECTED__ === 'function') {
                const shouldClose = window.__COOPT_ON_GLASS_SELECTED__({ name, manufacturer, price, nd, vd });
                if (shouldClose !== false) window.close();
              }
            } catch (_) {
              // ignore
            }
          });

          plotEl.on('plotly_selected', function(ev) {
          lastRegion = null;
          applyBtn.disabled = true;
          if (!ev || !ev.range || !ev.range.x || !ev.range.y) return;

          const x0 = ev.range.x[0];
          const x1 = ev.range.x[1];
          const y0 = ev.range.y[0];
          const y1 = ev.range.y[1];

          const vdMin = Math.min(x0, x1);
          const vdMax = Math.max(x0, x1);
          const ndMin = Math.min(y0, y1);
          const ndMax = Math.max(y0, y1);

          if (![vdMin, vdMax, ndMin, ndMax].every(Number.isFinite)) return;

          lastRegion = { ndMin, ndMax, vdMin, vdMax };
          applyBtn.disabled = false;

          // Clear selection visuals (box/selected points) while keeping lastRegion.
          try {
            if (window.Plotly && plotEl) {
              window.Plotly.restyle(plotEl, { selectedpoints: [null] });
            }
          } catch (_) {}
          });
        }

        applyBtn.addEventListener('click', function() {
          if (!lastRegion) return;
          try {
            if (typeof window.__COOPT_ON_REGION_SELECTED__ === 'function') {
              window.__COOPT_ON_REGION_SELECTED__(lastRegion);
            }
          } finally {
            window.close();
          }
        });
      }

      ensurePlotlyReady()
        .then(renderPlot)
        .catch((err) => {
          console.error(err);
          document.getElementById('plot').innerHTML = '<div style="padding:12px">Failed to load Plotly.</div>';
        });
    })();
  <\/script>
</body>
</html>`;try{r.document.open(),r.document.write(s),r.document.close()}catch(a){console.error("Failed to initialize popup window",a);try{r.close()}catch{}return alert("Failed to initialize popup window."),null}return r}const O=window,Bi=10;function vr(e,t){if(!Number.isFinite(e)||!Number.isFinite(t))return 6;const n=Math.abs(e);return n===0?4:n<1e-6?15:n<.001?10:n<1||t<1?6:t<10?4:t<100?3:2}function Sr(e,t,n,o){if(!o)return t+e*(n-t);if(t<0&&n>0)if(e<.5){const l=Math.abs(t),m=e*2;return l<1e-10?-1e-10*(1-m):-Math.exp(Math.log(l)*(1-m)+Math.log(1e-10)*m)}else{const l=(e-.5)*2;return n<1e-10?1e-10*l:Math.exp(Math.log(1e-10)*(1-l)+Math.log(n)*l)}const r=Math.abs(t),s=Math.abs(n),a=Math.log(Math.max(r,1e-10)),c=Math.log(Math.max(s,1e-10)),i=Math.exp(a+e*(c-a));return t<0&&n<0?-i:i}function eo(e,t,n,o){if(!o)return n===t?.5:(e-t)/(n-t);if(t<0&&n>0)if(e<0){const m=Math.abs(t),p=Math.abs(e);return m<1e-10||p<1e-10?.25:(1-(Math.log(p)-Math.log(1e-10))/(Math.log(m)-Math.log(1e-10)))*.5}else return n<1e-10||e<1e-10?.75:.5+(Math.log(e)-Math.log(1e-10))/(Math.log(n)-Math.log(1e-10))*.5;const r=Math.abs(t),s=Math.abs(n),a=Math.abs(e);if(r<1e-10||s<1e-10||a<1e-10)return .5;const c=Math.log(r),i=Math.log(s),l=Math.log(a);return i===c?.5:(l-c)/(i-c)}function xr(e,t,n){const o=parseFloat(String(n)),r=!Number.isFinite(o)||o===0;if(e==="nd"||e==="material"&&!isNaN(o)&&o>0&&o<4)return{min:1,max:2.5,step:1e-4,useLog:!1};if(e==="vd"||e==="abbe")return{min:20,max:95,step:.1,useLog:!1};if(e.includes("Radius")||e==="radius"){if(r)return{min:-100,max:100,step:.1,useLog:!1};const c=Math.abs(o),i=c*.5;return{min:o-i,max:o+i,step:c*.001,useLog:!1}}if(e.includes("Thickness")||e==="thickness"||e.includes("hickness"))return r?{min:0,max:20,step:.1,useLog:!1}:{min:0,max:o*2,step:o*.001,useLog:!1};if(e.includes("semidia")||e.includes("Semidia")||e.includes("aperture"))return r?{min:.1,max:20,step:.1,useLog:!1}:{min:Math.max(.1,o*.5),max:o*1.5,step:o*.001,useLog:!1};if(e==="conic"){if(r)return{min:-10,max:10,step:.01,useLog:!1};const c=Math.abs(o),i=Math.max(c*.5,1);return{min:o-i,max:o+i,step:c>1?c*.001:.001,useLog:!1}}if(e.startsWith("coef")||e.includes("Coef")){if(r)return{min:-10,max:10,step:.001,useLog:!1};const c=Math.abs(o),i=Math.max(c*.5,c*10);return{min:o-i,max:o+i,step:c*.01,useLog:!1}}if(r)return{min:-10,max:10,step:.01,useLog:!1};const s=Math.abs(o),a=Math.max(s*.5,s);return{min:o-a,max:o+a,step:s*.01,useLog:!1}}function zi(e,...t){try{e.includes("🔴")||e.includes("❌")?console.log(`%c${e}`,"color: red; font-weight: bold; font-size: 13px;",...t):e.includes("🔵")?console.log(`%c${e}`,"color: blue; font-weight: bold; font-size: 12px;",...t):e.includes("⚠️")?console.log(`%c${e}`,"color: orange; font-weight: bold;",...t):e.includes("✅")?console.log(`%c${e}`,"color: green; font-weight: bold;",...t):console.log(e,...t)}catch{}try{const n=window?.opener;n&&n.console&&typeof n.console.log=="function"&&n.console.log(e,...t)}catch{}try{const n=window;Array.isArray(n.__coordTransDebugLogs)||(n.__coordTransDebugLogs=[]),n.__coordTransDebugLogs.push({time:new Date().toISOString(),message:e,args:t})}catch{}}try{window.__coordTransConsoleTest=()=>{const e=new Date().toISOString();return console.log(`[CoordTrans][TEST] console output OK at ${e}`),zi(`✅ [CoordTrans][TEST] coordTransDebugLog OK at ${e}`),e}}catch{}try{window.__coordTransConsoleTestFire=()=>{const e=new Date().toISOString();return console.error(`[CoordTrans][TEST] console.error OK at ${e}`),console.warn(`[CoordTrans][TEST] console.warn OK at ${e}`),console.info(`[CoordTrans][TEST] console.info OK at ${e}`),e}}catch{}async function Ar(e,t){const n=t;if(!(n&&n.__coordTransCalculating)){n&&(n.__coordTransCalculating=!0);try{console.log("%c🔴 [CoordTrans] performCoordTransCalculation CALLED for blockId="+e,"color: red; font-weight: bold; font-size: 14px;"),zi(`🔴 [CoordTrans] performCoordTransCalculation called for blockId=${e}`);const o=F=>{if(!t)return null;let W=t.querySelector(`input[data-param-key="${F}"]`);return W||(W=t.querySelector(`.param-input-with-slider[data-param-key="${F}"] input[type="text"]`)||null),W||(W=t.querySelector(`input[name="${F}"]`)),W||(W=t.querySelector(`select[data-param-key="${F}"]`)),W||(W=t.querySelector(`select[name="${F}"]`)),W?String(W.value??""):null};let r=null;try{if(typeof Ie=="function"){const F=Ie(),W=F?.activeConfigId;r=(Array.isArray(F?.configurations)?F.configurations.find(P=>P&&String(P.id)===String(W)):null)?.blocks?.find(P=>P&&String(P.blockId??"")===String(e))?.parameters||null}}catch{}const s=r&&r.toSurf!==void 0&&r.toSurf!==null?String(r.toSurf):o("toSurf"),a=r&&r.coordReturn?String(r.coordReturn):o("coordReturn")||"none";if(r){const F=W=>String(W??"").trim().toUpperCase();["A","AUTO"].includes(F(r.chiefRayShiftX))&&(r={...r,decenterX:0}),["A","AUTO"].includes(F(r.chiefRayShiftY))&&(r={...r,decenterY:0}),["A","AUTO"].includes(F(r.chiefRayShiftZ))&&(r={...r,decenterZ:0})}if(a!=="none"&&o("order")!=="1")try{if(typeof O.__blocks_setBlockParamValue=="function"){const W=O.__blocks_setBlockParamValue(e,"order","1");(!W||W.ok!==!0)&&(!n||!n.__coordTransOrderWarned)&&(console.warn("[CoordTrans] Failed to set order to 1:",W?.reason),n&&(n.__coordTransOrderWarned=!0))}}catch{}if(!s||String(s).trim()==="")return;const c=Number(s);if(!Number.isFinite(c)){console.error("[CoordTrans] Invalid target index:",s);return}const i=O.getOpticalSystemRows;if(typeof i!="function"){console.error("[CoordTrans] getOpticalSystemRows not available");return}const l=i();if(!l||l.length===0){console.error("[CoordTrans] No optical system data");return}const m=l.map(F=>({...F}));let p=null;try{p=typeof Ie=="function"?Ie():null}catch{}const d=F=>{const W=String(F?.surfType??F?.["surf type"]??"").toLowerCase().replace(/\s+/g,"");return W==="coordbreak"||W==="coordinatebreak"||W==="cb"||W==="coordtrans"||W==="coordinatetransform"||W==="ct"},u=F=>{const W=F?.["object type"]??F?.object??F?.Object;return String(W??"").toLowerCase()==="object"},h=F=>String(F?.surfType??F?.["surf type"]??"").toLowerCase()==="gap",y=((F,W)=>{if(!Array.isArray(F)||!Number.isFinite(W))return null;const I=Math.floor(W);if(I<=0)return null;let E=0;for(let P=0;P<F.length;P++){const Z=F[P];if(!d(Z)&&!u(Z)&&!h(Z)&&(E++,E===I))return P}return null})(l,c),A=y!==null?y:Math.max(0,Math.min(Math.floor(c),Math.max(0,l.length-1))),x=l.map(F=>{const W=String(F._blockId??F.blockId??"");if(!W)return F;let I=null;if(W===String(e)&&r)I=r;else try{if(p&&Array.isArray(p.configurations)){const $=p.activeConfigId,te=p.configurations.find(q=>q&&q.id===$);if(te&&Array.isArray(te.blocks)){const q=te.blocks.find(z=>z&&String(z.blockId??"")===W);q&&(I=q.parameters)}}}catch($){console.warn(`[CoordTrans] Could not get block data for ${W}:`,$)}if(!I)return F;const E=W===String(e),P=$=>String($??"").trim().toUpperCase(),Z=E&&["A","AUTO"].includes(P(I.chiefRayShiftX)),R=E&&["A","AUTO"].includes(P(I.chiefRayShiftY)),k=E&&["A","AUTO"].includes(P(I.chiefRayShiftZ));return{...F,decenterX:Z?0:I.decenterX!==void 0?I.decenterX:F.decenterX,decenterY:R?0:I.decenterY!==void 0?I.decenterY:F.decenterY,decenterZ:k?0:I.decenterZ!==void 0?I.decenterZ:F.decenterZ,tiltX:I.tiltX!==void 0?I.tiltX:F.tiltX,tiltY:I.tiltY!==void 0?I.tiltY:F.tiltY,tiltZ:I.tiltZ!==void 0?I.tiltZ:F.tiltZ,order:I.order!==void 0?I.order:F.order,chiefRayShiftX:I.chiefRayShiftX,chiefRayShiftY:I.chiefRayShiftY,chiefRayShiftZ:I.chiefRayShiftZ,parameters:{...F.parameters||{},decenterX:Z?0:I.decenterX!==void 0?I.decenterX:F.parameters?.decenterX,decenterY:R?0:I.decenterY!==void 0?I.decenterY:F.parameters?.decenterY,decenterZ:k?0:I.decenterZ!==void 0?I.decenterZ:F.parameters?.decenterZ,tiltX:I.tiltX!==void 0?I.tiltX:F.parameters?.tiltX,tiltY:I.tiltY!==void 0?I.tiltY:F.parameters?.tiltY,tiltZ:I.tiltZ!==void 0?I.tiltZ:F.parameters?.tiltZ,order:I.order!==void 0?I.order:F.parameters?.order,chiefRayShiftX:I.chiefRayShiftX,chiefRayShiftY:I.chiefRayShiftY,chiefRayShiftZ:I.chiefRayShiftZ}}}),_=O.calculateAllSurfacesLocalCoordinates;if(typeof _!="function"){console.error("[CoordTrans] calculateAllSurfacesLocalCoordinates not available");return}const S=await _(x,A,null,e,m);let N=-1;for(let F=0;F<l.length;F++)if(String(l[F]._blockId??l[F].blockId??"")===String(e)){N=F;break}if(N<0){console.error("[CoordTrans] Could not find surface for block:",e);return}const f=String(l[N].id);let w=S.surfaces?.[f]||S.surfaces?.[String(f)]||S.surfaces?.[Number(f)]||S.surfaces?.[N]||S.surfaces?.[String(N)];if(!w)for(let F=N+1;F<l.length;F++){const W=String(l[F].id);if(w=S.surfaces?.[W],w)break}if(!w){console.error("[CoordTrans] No surface data found");return}try{a==="xyz"&&(console.log("[CoordTrans] Mode:",a,"blockId:",e,"targetIndex:",A),console.log("[CoordTrans] blockParams:",r),console.log("[CoordTrans] surfData tilt:",{tiltX:w.localTiltX,tiltY:w.localTiltY,tiltZ:w.localTiltZ}),console.log("[CoordTrans] surfData decenter (local):",{decenterX:w.localDecenterX,decenterY:w.localDecenterY,decenterZ:w.localDecenterZ}),console.log("[CoordTrans] surfData decenter (flat):",{decenterX:w.flatDecenterX,decenterY:w.flatDecenterY,decenterZ:w.flatDecenterZ}))}catch{}const b={},v=(F,W)=>typeof W=="number"&&Number.isFinite(W)?(b[F]=W,!0):!1,M=r&&r.chiefRayShiftX!==void 0?r.chiefRayShiftX:o("chiefRayShiftX"),C=r&&r.chiefRayShiftY!==void 0?r.chiefRayShiftY:o("chiefRayShiftY"),D=r&&r.chiefRayShiftZ!==void 0?r.chiefRayShiftZ:o("chiefRayShiftZ"),B=F=>String(F??"").trim().toUpperCase();let j=["A","AUTO"].includes(B(M)),H=["A","AUTO"].includes(B(C)),ce=["A","AUTO"].includes(B(D));a==="xyz"&&(ce=!0,console.log("[CoordTrans] XYZ mode detected: forcing shouldAutoZ=true"));let U={};switch(a){case"none":break;case"xyz":{const F=w.flatDecenterX!==void 0&&Number.isFinite(w.flatDecenterX)?w.flatDecenterX:w.localDecenterX,W=w.flatDecenterY!==void 0&&Number.isFinite(w.flatDecenterY)?w.flatDecenterY:w.localDecenterY,I=w.flatDecenterZ!==void 0&&Number.isFinite(w.flatDecenterZ)?w.flatDecenterZ:w.localDecenterZ;U={decenterX:j?v("decenterX",F):!1,decenterY:H?v("decenterY",W):!1,decenterZ:ce?v("decenterZ",I):!1,tiltX:v("tiltX",w.localTiltX),tiltY:v("tiltY",w.localTiltY),tiltZ:v("tiltZ",w.localTiltZ)}}break}if(a!=="none"?typeof window<"u"&&(window.__coordTransComputedValues||(window.__coordTransComputedValues={}),window.__coordTransComputedValues[e]=b):typeof window<"u"&&window.__coordTransComputedValues&&delete window.__coordTransComputedValues[e],a!=="none")try{window.__coordTransApplyingResults=!0;const F={};if(a==="xyz"){let W=w.localDecenterX;w.flatDecenterX!==void 0&&Number.isFinite(w.flatDecenterX)&&(W=w.flatDecenterX);let I=w.localDecenterY;w.flatDecenterY!==void 0&&Number.isFinite(w.flatDecenterY)&&(I=w.flatDecenterY);let E=w.localDecenterZ;w.flatDecenterZ!==void 0&&Number.isFinite(w.flatDecenterZ)&&(E=w.flatDecenterZ),console.log(`[CoordTrans XYZ] shouldAutoX=${j}, shouldAutoY=${H}, shouldAutoZ=${ce}`),console.log(`[CoordTrans XYZ] srcX=${W.toFixed(4)}, srcY=${I.toFixed(4)}, srcZ=${E.toFixed(4)}`),j&&(F.decenterX=W),H&&(F.decenterY=I),ce&&(F.decenterZ=E),console.log(`[CoordTrans XYZ] After assignment: updates.decenterX=${F.decenterX}, updates.decenterY=${F.decenterY}, updates.decenterZ=${F.decenterZ}`),F.tiltX=w.localTiltX,F.tiltY=w.localTiltY,F.tiltZ=w.localTiltZ}if(typeof Ie=="function"&&typeof Te=="function"){const W=Ie(),I=W?.activeConfigId,E=Array.isArray(W?.configurations)?W.configurations.find(Z=>Z&&String(Z.id)===String(I)):null,P=E?.blocks?.find(Z=>Z&&String(Z.blockId??"")===String(e));if(P){(!P.parameters||typeof P.parameters!="object")&&(P.parameters={}),console.log(`[CoordTrans] Before block update - blockId=${e}:`,{decenterX:P.parameters.decenterX,decenterY:P.parameters.decenterY,decenterZ:P.parameters.decenterZ});for(const[Z,R]of Object.entries(F))typeof R=="number"&&Number.isFinite(R)&&(P.parameters[Z]=R,console.log(`[CoordTrans] Set block.parameters.${Z} = ${R.toFixed(6)}`));console.log(`[CoordTrans] After block update - blockId=${e}:`,{decenterX:P.parameters.decenterX,decenterY:P.parameters.decenterY,decenterZ:P.parameters.decenterZ}),E?.metadata&&typeof E.metadata=="object"&&(E.metadata.modified=new Date().toISOString()),Te(W)}}try{if(typeof Ie=="function"&&typeof $e=="function"){const W=Ie(),I=W?.activeConfigId,E=Array.isArray(W?.configurations)?W.configurations.find(P=>P&&String(P.id)===String(I)):null;if(E&&Array.isArray(E.blocks)){const P=$e(E.blocks);P&&Array.isArray(P.rows)&&(E.opticalSystem=P.rows,typeof Te=="function"&&Te(W))}}}catch{}window.ConfigurationManager&&typeof window.ConfigurationManager.loadActiveConfigurationToTables=="function"?await window.ConfigurationManager.loadActiveConfigurationToTables({applyToUI:!0}):typeof window.loadActiveConfigurationToTables=="function"&&await window.loadActiveConfigurationToTables({applyToUI:!0});try{window.ConfigurationManager&&typeof window.ConfigurationManager.renderBlocksUI=="function"&&window.ConfigurationManager.renderBlocksUI()}catch{}try{typeof window.__blocks_requestRedraw=="function"&&window.__blocks_requestRedraw()}catch{}try{typeof window.refreshAllUI=="function"&&window.refreshAllUI()}catch{}}finally{window.__coordTransApplyingResults=!1}const G=Object.values(U).filter(F=>F).length;console.log("[CoordTrans] Updated",G,"fields:",a);try{Je()}catch{}}catch(o){console.error("[CoordTrans] Calculation error:",o)}finally{n&&(n.__coordTransCalculating=!1)}}}function To(e){const t=e?.semidia??e?.["semidia(mm)"]??e?.semidiameter,n=Number(t);return Number.isFinite(n)?n:NaN}function mo(e){const t=To(e);return Number.isFinite(t)&&t>0?t:null}function Nr(e){const t=String(e??"").trim();return t==="Lens"||t==="PositiveLens"?["front","back"]:t==="Doublet"?["s1","s2","s3"]:t==="Triplet"?["s1","s2","s3","s4"]:[]}function fo(e){if(!e||typeof e!="object")return!1;const t=String(e?.["object type"]??e?.object??"").trim().toLowerCase();if(t==="object"||t==="stop"||t==="image")return!1;const n=String(e?.surfType??e?.["surf type"]??"").trim().toLowerCase().replace(/\s+/g,"");return!(n==="coordtrans"||n==="coordbreak"||n==="coordinatebreak")}function Wi(e){const t=e?.semidia??e?.["semidia(mm)"]??e?.semidiameter;if(t==null||String(t).trim()==="")return!0;const n=Number(t);return!Number.isFinite(n)||n<=0}function $i(e){let t=0;for(const n of e){const o=To(n);Number.isFinite(o)&&o>t&&(t=o)}return t>0?t:null}function xl(e){for(const t of e)if(String(t?.["object type"]??t?.object??"").toLowerCase()==="stop"){const o=To(t);return Number.isFinite(o)&&o>0?o:null}return null}function Ui(e){if(!Array.isArray(e))return-1;for(let t=0;t<e.length;t++){const n=e[t];if(String(n?.["object type"]??n?.object??"").trim().toLowerCase()==="stop")return t}return-1}function Dt(e,t,n,o){if(!Array.isArray(e)||t<0||t>=e.length||!(Number.isFinite(o)&&o>0))return null;try{const r=e.map(c=>({...c||{}}));r[t].semidia=o;const s=On(r,n),a=Number(s?.entrancePupilDiameter);return Number.isFinite(a)&&a>0?a:null}catch{return null}}function Al(e,t,n){const o=Number(n);if(!(Number.isFinite(o)&&o>0))return null;const r=Ui(e);if(r<0)return null;const s=1e-4;let a=Math.max(s,o*.02),c=Math.max(1,o*1.5),i=Dt(e,r,t,a),l=Dt(e,r,t,c);for(let h=0;h<24&&(!Number.isFinite(l)||l<o)&&(c*=1.8,l=Dt(e,r,t,c),!(c>1e6));h++);for(let h=0;h<24&&Number.isFinite(i)&&i>o&&a>s&&(a=Math.max(s,a/1.8),i=Dt(e,r,t,a),!(a<=s));h++);let m=null,p=1/0;const d=(h,g)=>{if(!(Number.isFinite(h)&&h>0)||!(Number.isFinite(g)&&g>0))return;const y=Math.abs(g-o);y<p&&(p=y,m=h)};if(d(a,i),d(c,l),Number.isFinite(i)&&Number.isFinite(l)&&(i-o)*(l-o)<=0){let h=a,g=c,y=i;for(let A=0;A<36;A++){const x=(h+g)/2,_=Dt(e,r,t,x);if(!Number.isFinite(_)||_<=0)break;const S=_;if(d(x,S),Math.abs(S-o)<=1e-4||((y-o)*(S-o)<=0?g=x:(h=x,y=S),Math.abs(g-h)<=1e-8*Math.max(1,x)))break}}else{const h=Math.max(s,o*.005),g=Math.max(c,o*20),y=Math.log(g/h),A=40;for(let x=0;x<=A;x++){const _=x/A,S=h*Math.exp(y*_),N=Dt(e,r,t,S);d(S,N)}}return Number.isFinite(m)&&m>0?m:null}function Nl(e,t){const n=xl(e);if(Number.isFinite(n)&&n>0)return n;const o=Number(t);if(Number.isFinite(o)&&o>0)return o/2;const r=$i(e);return Number.isFinite(r)&&r>0?r:10}function _l(e){const t=e?.thickness;if(t===1/0)return!0;const n=String(t??"").trim();return/^inf(inity)?$/i.test(n)}function Rl(e){const t=Array.isArray(e)?e.map(r=>({...r||{}})):[],n=$i(t),o=Math.max(1e3,Number.isFinite(n)?Number(n)*20:1e3);for(const r of t){if(!r||typeof r!="object")continue;const s=String(r?.["object type"]??r?.object??"").trim().toLowerCase(),a=String(r?.surfType??r?.["surf type"]??r?.type??"").trim().toLowerCase();s!=="stop"&&(s==="object"||s==="image"||a==="coord trans"||a==="coordinate transform"||a==="ct"||a==="coordtrans"||a==="coordinatetransform"||(r.semidia=o))}return t}function Cl(e){let t=0,n=0;if(!Array.isArray(e))return{x:0,y:0};for(const o of e){if(!o||typeof o!="object")continue;const r=Number(o?.xHeightAngle??o?.x??o?.fieldX??0),s=Number(o?.yHeightAngle??o?.y??o?.fieldY??0);Number.isFinite(r)&&(t=Math.max(t,Math.abs(r))),Number.isFinite(s)&&(n=Math.max(n,Math.abs(s)))}return{x:t,y:n}}function _r(e,t,n,o,r,s){try{let a=0,c=Math.max(2,Number(s)*2);const i=12,l=1e-4;for(let m=0;m<i;m++){const p=(a+c)/2,d=r?typeof O.generateInfiniteSystemCrossBeam=="function"?O.generateInfiniteSystemCrossBeam(e,[{x:o==="x"?p:0,y:o==="y"?p:0}],{rayCount:1,wavelength:n,debugMode:!1}):null:typeof O.generateCrossBeam=="function"?O.generateCrossBeam(e,[{x:o==="x"?p:0,y:o==="y"?p:0,z:0}],{rayCount:1,wavelength:n,debugMode:!1}):null;if(!d)return null;const u=Array.isArray(d?.allTracedRays)&&d.allTracedRays.length>0?d.allTracedRays[0]:Array.isArray(d?.objectResults)&&d.objectResults.length>0&&Array.isArray(d.objectResults[0]?.tracedRays)&&d.objectResults[0].tracedRays.length>0?d.objectResults[0].tracedRays[0]:Array.isArray(d?.rays)&&d.rays.length>0?d.rays[0]:null;if(!u)return null;const h=Array.isArray(u?.rayPath)?u.rayPath:Array.isArray(u?.rayPathToTarget)?u.rayPathToTarget:null;if(!Array.isArray(h))return null;const g=h[t];if(!g)return null;const y=o==="x"?g.x:g.y;if(!Number.isFinite(y))return null;if(Math.abs(y)<l)return p;y>0?c=p:a=p}return(a+c)/2}catch{return null}}function Il(e,t,n=[]){const o=e.findIndex(y=>String(y?.["object type"]??y?.object??"").toLowerCase()==="stop");if(o<0)return;const r=e[0],s=_l(r),a=Rl(e),c=Number(e?.__zmxEntrancePupilDiameterMm),i=Nl(e,Number.isFinite(c)?c:void 0);let l=i,m=i;const p=s?Cl(n):{x:0,y:0};if(s&&(p.x>0||p.y>0))p.x>0&&(l=p.x),p.y>0&&(m=p.y);else{const y=_r(a,o,t,"x",s,i),A=_r(a,o,t,"y",s,i);l=Number.isFinite(y)?y:i,m=Number.isFinite(A)?A:i}if(!Number.isFinite(l)||!Number.isFinite(m)||l<=0||m<=0)return;const u=s?typeof O.generateInfiniteSystemCrossBeam=="function"?O.generateInfiniteSystemCrossBeam(a,[{x:l,y:0},{x:0,y:m}],{rayCount:13,wavelength:t,debugMode:!1}):null:typeof O.generateCrossBeam=="function"?O.generateCrossBeam(a,[{x:l,y:0,z:0},{x:0,y:m,z:0}],{rayCount:13,wavelength:t,debugMode:!1}):null;if(!u)return;let h=[];if(Array.isArray(u.allTracedRays))h=u.allTracedRays;else if(Array.isArray(u.rays))h=u.rays;else if(Array.isArray(u.objectResults))for(const y of u.objectResults)Array.isArray(y?.tracedRays)?h.push(...y.tracedRays):Array.isArray(y?.rays)?h.push(...y.rays):Array.isArray(y?.crossBeamRays)&&h.push(...y.crossBeamRays);if(h.length===0)return;const g=new Array(e.length).fill(0);for(const y of h){const A=Array.isArray(y?.rayPath)?y.rayPath:Array.isArray(y?.rayPathToTarget)?y.rayPathToTarget:Array.isArray(y?.path)?y.path:Array.isArray(y?.ray?.path)?y.ray.path:null;if(!Array.isArray(A))continue;const x=Math.min(A.length,e.length);for(let _=0;_<x;_++){const S=A[_];if(!S||!Number.isFinite(S.x)||!Number.isFinite(S.y))continue;const N=Math.sqrt(S.x*S.x+S.y*S.y);N>g[_]&&(g[_]=N)}}for(let y=0;y<e.length;y++){const A=e[y];if(!A||typeof A!="object"||!fo(A))continue;const _=Wi(A),S=mo(A),N=g[y];N>0&&(_||S===null||N>S+1e-6)&&(A.semidia=N)}}function Hi(e,t,n={}){console.log("[autoCalculateMissingSemidia] START");const o=O.tableOpticalSystem||O.tableOpticalSystem,r=o&&typeof o.getData=="function"?o.getData():null;if(!Array.isArray(r)||r.length<2){console.warn("[autoCalculateMissingSemidia] Invalid rows:",r);return}console.log("[autoCalculateMissingSemidia] Table loaded with",r.length,"rows"),console.log("[autoCalculateMissingSemidia] Initial rows (first 5):",r.slice(0,5).map(s=>({surf:s?.surf,type:s?.type,object:s?.object,"object type":s?.["object type"],semidia:s?.semidia,radius:s?.radius,thickness:s?.thickness})));try{const s=(()=>{if(typeof O.getPrimaryWavelength=="function"){const m=Number(O.getPrimaryWavelength());if(Number.isFinite(m)&&m>0)return m}return console.warn("Primary wavelength is unavailable. Semidia auto-calculation is skipped."),NaN})();if(!Number.isFinite(s)||s<=0)return;const a=Number(n?.entrancePupilDiameterMm),c=Ui(r),i=!!n?.stopSemidiaWasMissing;if(Number.isFinite(a)&&a>0&&c>=0&&(i||Wi(r[c]))){const m=Al(r,s,a);Number.isFinite(m)&&m>0?(r[c].semidia=m,console.warn(`[autoCalculateMissingSemidia] Stop semidia backsolved from ENPD=${a}: ${m}`)):console.warn(`[autoCalculateMissingSemidia] Stop semidia backsolve failed (ENPD=${a}, stopIndex=${c})`)}Number.isFinite(a)&&a>0&&(r.__zmxEntrancePupilDiameterMm=a),Il(r,s,t),console.log("[autoCalculateMissingSemidia] Ray tracing completed. Sample rows with semidia:",r.slice(0,5).map(m=>({surf:m?.surf,type:m?.type,semidia:m?.semidia,_blockId:m?._blockId,_surfaceRole:m?._surfaceRole})));try{delete r.__zmxEntrancePupilDiameterMm}catch{}o&&typeof o.setData=="function"&&o.setData(r);try{qt(r),console.log("[autoCalculateMissingSemidia] ✅ Saved to tableOpticalSystem storage")}catch(m){console.error("[autoCalculateMissingSemidia] ❌ Failed to save tableOpticalSystem:",m)}try{const m=typeof Ie=="function"?Ie():null;if(m&&Array.isArray(m.configurations)){const p=m.activeConfigId,d=m.configurations.find(u=>u&&String(u.id)===String(p))||m.configurations[0];d&&typeof d=="object"&&(d.opticalSystem=r.map(u=>({...u||{}})),(!d.metadata||typeof d.metadata!="object")&&(d.metadata={}),d.metadata.modified=new Date().toISOString(),typeof Te=="function"&&(Te(m),console.log("[autoCalculateMissingSemidia] ✅ Saved to active configuration")))}}catch(m){console.error("[autoCalculateMissingSemidia] ❌ Failed to save configuration:",m)}}catch{}}function Fl(){console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] START");try{const e=O.tableOpticalSystem||O.opticalSystemTabulator,t=e&&typeof e.getData=="function"?e.getData():null;if(!Array.isArray(t)||t.length===0){console.warn("[__zmxSyncDesignIntentApertureFromOpticalRows] No table rows found");return}const n=t.filter(u=>fo(u));console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] Table rows count:",t.length,"(physical:",n.length+")");const o=typeof Ie=="function"?Ie():null;if(!o||!Array.isArray(o.configurations)||o.configurations.length===0)return;const r=o.activeConfigId,s=o.configurations.find(u=>u&&String(u.id)===String(r))||o.configurations[0];if(!s||!Array.isArray(s.blocks)||s.blocks.length===0)return;console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] Active config has",s.blocks.length,"blocks");const a=new Map;for(const u of s.blocks){const h=String(u?.blockId??"").trim();h&&a.set(h,u)}const c=new Set;let i=0;for(const u of t){const h=String(u?._blockId??"").trim(),g=String(u?._surfaceRole??"").trim();if(!h||!g)continue;const y=a.get(h);if(!y||!Nr(y.blockType).includes(g))continue;const x=mo(u);x!==null&&((!y.aperture||typeof y.aperture!="object")&&(y.aperture={}),y.aperture[g]=x,c.add(h),i++,console.log(`[Provenance Sync] Block ${h} (${y.blockType}) ${g} = ${x}mm`))}console.log(`[__zmxSyncDesignIntentApertureFromOpticalRows] Provenance-based updates: ${i}`);const l=t.filter(u=>fo(u));console.log(`[__zmxSyncDesignIntentApertureFromOpticalRows] Fallback physical rows: ${l.length}`);let m=0,p=0;for(const u of s.blocks){const h=Nr(u?.blockType);if(h.length===0)continue;const g=String(u?.blockId??"").trim();if(!(g&&c.has(g))){(!u.aperture||typeof u.aperture!="object")&&(u.aperture={});for(const y of h){const A=l[m++];if(!A)break;const x=mo(A);x!==null&&(u.aperture[y]=x,p++,console.log(`[Fallback Sync] Block ${g||"unknown"} (${u.blockType}) ${y} = ${x}mm (row ${m-1}: surf=${A.surf})`))}}}console.log(`[__zmxSyncDesignIntentApertureFromOpticalRows] Fallback updates: ${p}`);let d=!1;try{if(typeof Xo=="function"){const u=Xo(s);d=u&&Array.isArray(u.expandedOpticalSystem),console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] expandBlocksIntoConfiguration result:",d?"SUCCESS":"FAILED",u?`(rows: ${u.expandedOpticalSystem?.length||0}, issues: ${u.issues?.length||0})`:"")}else if(typeof O.expandBlocksIntoConfiguration=="function"){const u=O.expandBlocksIntoConfiguration(s);d=u&&Array.isArray(u.expandedOpticalSystem),console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] expandBlocksIntoConfiguration result:",d?"SUCCESS":"FAILED",u?`(rows: ${u.expandedOpticalSystem?.length||0}, issues: ${u.issues?.length||0})`:"")}}catch(u){console.error("[__zmxSyncDesignIntentApertureFromOpticalRows] expandBlocksIntoConfiguration ERROR:",u)}if(typeof Te=="function"&&(Te(o),console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] ✅ Saved system configurations",d?"(with expanded blocks)":"(WARNING: expand may have failed)")),console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] Final block apertures:",s.blocks.map(u=>({blockId:u.blockId,blockType:u.blockType,aperture:u.aperture}))),d&&Array.isArray(s.opticalSystem)){console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] Expanded opticalSystem sample (first 3):",s.opticalSystem.slice(0,3).map(u=>({surf:u?.surf,type:u?.type,semidia:u?.semidia,_blockId:u?._blockId,_surfaceRole:u?._surfaceRole}))),e&&typeof e.setData=="function"&&(e.setData(s.opticalSystem),console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] ✅ Updated table with expanded rows"));try{qt(s.opticalSystem),console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] ✅ Saved expanded rows to localStorage")}catch(u){console.error("[__zmxSyncDesignIntentApertureFromOpticalRows] ❌ Failed to save to localStorage:",u)}}else console.warn("[__zmxSyncDesignIntentApertureFromOpticalRows] ⚠️ Skipping table/storage update due to expand failure");try{Je()}catch{}try{Ut()}catch{}try{Ht()}catch{}try{typeof O.refreshAllUI=="function"&&O.refreshAllUI()}catch{}console.log("[__zmxSyncDesignIntentApertureFromOpticalRows] COMPLETE")}catch(e){console.error("[__zmxSyncDesignIntentApertureFromOpticalRows] ERROR:",e)}}async function fn(e,t={}){const n=t?.filename||"shared-link.json";try{if(typeof O.normalizeDesign=="function"){const p=O.normalizeDesign(e);p?.normalized&&(e=p.normalized)}}catch{}let o=null;if(e?.systemConfigurations&&e.systemConfigurations.configurations?o=e.systemConfigurations:e&&e.configurations&&e.configurations.configurations?o=e.configurations:Array.isArray(e?.configurations)?o={configurations:e.configurations,activeConfigId:e.activeConfigId,meritFunction:e.meritFunction||[],systemRequirements:e.systemRequirements||[],optimizationRules:e.optimizationRules||{}}:Array.isArray(e)?o={configurations:e}:o=e,!o||!Array.isArray(o.configurations))return console.error("❌ [Load] Invalid configurations format:",o),!1;try{let p=0;for(let d=0;d<o.configurations.length;d++){const u=o.configurations[d];if(!u)continue;(u.id===void 0||u.id===null||String(u.id).trim()==="")&&(u.id=d+1);const h=Number(u.id);Number.isFinite(h)&&h>p&&(p=h)}o.activeConfigId||(o.activeConfigId=o.configurations[0]?.id??p??1)}catch{}try{if(Array.isArray(o.configurations)&&o.configurations.length===0){const p={id:1,name:"Config 1",schemaVersion:o.schemaVersion||pn,blocks:Array.isArray(e?.blocks)?e.blocks:[],source:Array.isArray(e?.source)?e.source:[],object:Array.isArray(e?.object)?e.object:[],opticalSystem:Array.isArray(e?.opticalSystem)?e.opticalSystem:[],meritFunction:Array.isArray(e?.meritFunction)?e.meritFunction:[],systemData:e?.systemData||{referenceFocalLength:""},metadata:{created:new Date().toISOString(),modified:new Date().toISOString(),locked:!1}};o.configurations.push(p),o.activeConfigId=1}}catch{}try{const p=o.activeConfigId,d=o.configurations||[],u=d.find(h=>String(h?.id??"")===String(p))||d[0];u&&((!u.source||u.source.length===0)&&Array.isArray(e?.source)&&(u.source=e.source),(!u.object||u.object.length===0)&&Array.isArray(e?.object)&&(u.object=e.object),(!u.opticalSystem||u.opticalSystem.length===0)&&Array.isArray(e?.opticalSystem)&&(u.opticalSystem=e.opticalSystem),(!u.systemData||typeof u.systemData!="object")&&e?.systemData&&(u.systemData=e.systemData)),!o.meritFunction&&Array.isArray(e?.meritFunction)&&(o.meritFunction=e.meritFunction),!o.systemRequirements&&Array.isArray(e?.systemRequirements)&&(o.systemRequirements=e.systemRequirements)}catch{}const r=Array.isArray(o?.configurations)?o.configurations:[],s=p=>{try{return p&&Array.isArray(p.blocks)&&p.blocks.length>0}catch{return!1}};for(const p of r)try{const d=Array.isArray(p?.opticalSystem)?p.opticalSystem:null;if(!d||d.length===0)continue;const u=s(p);if(typeof O.deriveBlocksFromLegacyOpticalSystemRows=="function"){const h=O.deriveBlocksFromLegacyOpticalSystemRows(d);!(Array.isArray(h?.issues)&&h.issues.some(y=>y&&y.severity==="fatal"))&&(!u||Array.isArray(h?.blocks)&&h.blocks.length>0)&&(p.blocks=Array.isArray(h?.blocks)?h.blocks:[],(!p.metadata||typeof p.metadata!="object")&&(p.metadata={}),p.metadata.importAnalyzeMode=!1)}}catch{}for(const p of r)if(s(p))try{if(typeof O.validateBlocksConfiguration=="function"){const d=O.validateBlocksConfiguration(p),u=Array.isArray(d)?d.filter(h=>h&&h.severity==="fatal"):[];u.length>0&&console.warn("⚠️ Block validation errors:",u)}}catch{}try{const p=o?.activeConfigId||1,d=r.find(u=>u.id===p)||r[0];if(d&&s(d)){const u=Array.isArray(d.opticalSystem)?d.opticalSystem:null;if(typeof O.expandBlocksToOpticalSystemRows=="function"){const h=O.expandBlocksToOpticalSystemRows(d.blocks);if(Array.isArray(u)&&u.length>0){try{typeof O.__blocks_overlayExpandedProvenanceIntoLegacyRows=="function"&&O.__blocks_overlayExpandedProvenanceIntoLegacyRows(u,h.rows)}catch{}try{for(let g=0;g<u.length;g++)u[g]&&typeof u[g]=="object"&&(u[g].id=g)}catch{}d.opticalSystem=u}else Array.isArray(h?.rows)&&(d.opticalSystem=h.rows)}}}catch{}try{Te(o)}catch(p){return console.error("❌ Failed to save configurations:",p),!1}let a=e.source,c=e.object,i=e.opticalSystem,l=e.meritFunction,m=e.systemRequirements;try{const p=o?.activeConfigId||1,d=r.find(u=>u.id===p)||r[0];d&&(s(d)&&Array.isArray(d.opticalSystem)&&(i=d.opticalSystem),d.source&&Array.isArray(d.source)&&d.source.length>0&&(a=d.source),d.object&&Array.isArray(d.object)&&d.object.length>0&&(c=d.object),!i&&d.opticalSystem&&(i=d.opticalSystem)),!l&&o?.meritFunction&&(l=o.meritFunction),!m&&o?.systemRequirements&&(m=o.systemRequirements)}catch{}try{a&&Ln(a)}catch{}try{c&&bo(c)}catch{}try{i&&qt(i)}catch{}try{m&&Qr(m)}catch{}try{l&&Kr(l)}catch{}try{let p=!1;try{p=(Array.isArray(o?.configurations)?o.configurations:[]).some(g=>g&&Array.isArray(g.blocks)&&g.blocks.length>0)}catch{}const d=!p;try{const{setLoadedFileState:h}=await Re(async()=>{const{setLoadedFileState:g}=await import("./loaded-file-storage-DhVIUlJT.js");return{setLoadedFileState:g}},__vite__mapDeps([5,1]));h(n,d)}catch{}const u=document.getElementById("loaded-file-name");u&&(u.textContent=n,u.style.color=d?"#b45309":"#1a4d8f",d&&!u.textContent.includes("(surfaces only)")&&(u.textContent=`${u.textContent} (surfaces only)`));try{window.dispatchEvent(new CustomEvent("coopt:loaded-file-updated"))}catch{}}catch{}try{setTimeout(()=>{try{typeof En=="function"&&En()}catch{}try{const d=Ft(),u=O.tableSource;u&&typeof u.replaceData=="function"?u.replaceData(d):u&&typeof u.setData=="function"&&u.setData(d)}catch{}try{const d=ei(),u=O.tableObject;u&&typeof u.replaceData=="function"?u.replaceData(d):u&&typeof u.setData=="function"&&u.setData(d)}catch{}try{const d=Ms(),u=O.tableOpticalSystem||O.opticalSystemTabulator;u&&typeof u.replaceData=="function"?u.replaceData(d):u&&typeof u.setData=="function"&&u.setData(d)}catch{}try{const d=ks(),u=O.meritFunctionEditor;u&&typeof u.setData=="function"&&u.setData(d)}catch{}try{(async()=>{const u=O.systemRequirementsEditor;if(!u)return;const h=ti();typeof u.setData=="function"&&u.setData(h);const g=async y=>{try{if(typeof u.evaluateAndUpdateNow=="function"){const A=u.evaluateAndUpdateNow({reason:y,forceSilent:!0,silent:!0});A&&typeof A.then=="function"&&await A}}catch{}};await g("load-file-seq-initial"),typeof u.scheduleEvaluateAndUpdate=="function"&&u.scheduleEvaluateAndUpdate();for(let y=0;y<4;y++)await new Promise(A=>setTimeout(A,120)),await g(`load-file-seq-retry-${y+1}`);try{window.dispatchEvent(new CustomEvent("coopt:requirements-updated"))}catch{}})()}catch{}try{Je()}catch{}try{typeof O.updateTransformSurfaceSelect=="function"&&O.updateTransformSurfaceSelect()}catch{}const p=()=>{document.getElementById("config-select")&&typeof O.initializeConfigurationUI=="function"?O.initializeConfigurationUI():setTimeout(p,100)};p()},0)}catch{}return!0}if(typeof window<"u")try{O.__loadAllDataObjectIntoApp=fn,O.autoCalculateMissingSemidia=Hi}catch{}function Ct(e){return e?e.getAttribute("data-react-handled")==="1":!1}function Ml(){const e=document.getElementById("load-all-btn");if(!e||Ct(e))return;const t=()=>{const o=document.createElement("input");o.type="file",o.accept=".json",o.style.display="none",o.addEventListener("change",async r=>{const a=r.target?.files?.[0];try{o.parentNode&&o.parentNode.removeChild(o)}catch{}if(!a){console.warn("⚠️ [Load] No file selected");return}try{const c=await a.text(),i=JSON.parse(c);await fn(i,{filename:a.name})}catch(c){console.error("❌ Load failed:",c),alert(`Load failed: ${c?.message||String(c)}`)}}),document.body.appendChild(o),o.click()},n=e.cloneNode(!0);e.parentNode?.replaceChild(n,e),n.addEventListener("click",t)}function vt(e){if(e===1/0)return!0;const t=String(e??"").trim().toUpperCase();return t==="INF"||t==="INFINITY"||t==="∞"}function Rr(e){const t=Array.isArray(e)?e:[],n=[],o=()=>{for(let d=t.length-1;d>=0;d--){const u=t[d]||{},h=u?.semidia??u?.semiDiameter??u?.semiDia??u?.["semi diameter"]??u?.["Semi Diameter"],g=Number(h);if(Number.isFinite(g)&&g>0)return g}return null},r=t[0]||{},s=vt(r?.thickness)?"INF":"Finite",a=Number(r?.thickness);n.push({blockId:"ObjectSurface-1",blockType:"ObjectSurface",role:null,constraints:{},parameters:s==="INF"?{objectDistanceMode:"INF"}:{objectDistanceMode:"Finite",objectDistance:Number.isFinite(a)?a:10},variables:{},metadata:{source:"zemax-fallback"}});let c=0,i=0,l=0;const m=Math.max(1,t.length-1);for(let d=1;d<m;d++){const u=t[d]||{},h=String(u?.["object type"]??u?.object??"").trim().toLowerCase();if(h==="stop"||h==="sto"){c++;const v=Number(u?.semidia);n.push({blockId:`Stop-${c}`,blockType:"Stop",role:null,constraints:{},parameters:Number.isFinite(v)&&v>0?{semiDiameter:v}:{},variables:{},metadata:{source:"zemax-fallback"}});const M=u?.thickness,C=Number(M);(vt(M)||Number.isFinite(C)&&Math.abs(C)>1e-12)&&(l++,n.push({blockId:`Gap-${l}`,blockType:"Gap",role:null,constraints:{},parameters:{thickness:vt(M)?"INF":C,material:"AIR"},variables:{},metadata:{source:"zemax-fallback",from:"stop-thickness"}}));continue}i++;const A=String(u?.surfType??"").trim()||"Spherical",x=vt(u?.radius)||String(u?.radius??"").trim()===""?"INF":u.radius,_=u?.thickness,S=Number(_),N=vt(_)?"INF":Number.isFinite(S)?S:0,f=String(u?.material??"").trim(),w=Number(u?.conic),b={radius:x,thickness:N,material:f,surfType:A,conic:Number.isFinite(w)?w:0,semidia:u?.semidia??""};if(A==="Toric"){b.radiusX=vt(u?.radiusX)||String(u?.radiusX??"").trim()===""?"INF":u.radiusX,b.radiusY=vt(u?.radiusY)||String(u?.radiusY??"").trim()===""?"INF":u.radiusY;const v=Number(u?.axis);b.axis=Number.isFinite(v)?v:0}for(let v=1;v<=10;v++){const M=Number(u?.[`coef${v}`]);b[`coef${v}`]=Number.isFinite(M)?M:0}n.push({blockId:`SingleSurface-${i}`,blockType:"SingleSurface",role:null,constraints:{},parameters:b,variables:{},metadata:{source:"zemax-fallback",rowIndex:d}})}const p=o();return n.push({blockId:"ImageSurface-1",blockType:"ImageSurface",role:null,constraints:{},parameters:Number.isFinite(p)&&p>0?{semidia:p,semidiaMode:"Auto",optimizeSemiDia:"A"}:{semidiaMode:"Auto",optimizeSemiDia:"A"},variables:{},metadata:{source:"zemax-fallback"}}),n}function to(e){if(!Array.isArray(e))return[];let t=!1;for(const n of e){if(!n||n.blockType!=="ObjectSurface")continue;t=!0;const o=n.parameters&&typeof n.parameters=="object"?n.parameters:n.parameters={},r=String(o.objectDistanceMode??"").trim();if(vt(r)){o.objectDistanceMode="INF";const c=Number(o.objectDistance);o.objectDistance=Number.isFinite(c)?c:10;continue}o.objectDistanceMode="Finite";const a=Number(o.objectDistance);o.objectDistance=Number.isFinite(a)?a:10}return t||e.unshift({blockId:"ObjectSurface-1",blockType:"ObjectSurface",role:null,constraints:{},parameters:{objectDistanceMode:"Finite",objectDistance:10},variables:{},metadata:{source:"zemax-fallback",inserted:!0}}),e}function kl(e){if(e&&Array.isArray(e.configurations))return e;const t=Array.isArray(e?.rows)?e.rows:[],n=Array.isArray(e?.sourceRows)?e.sourceRows:[],o=Array.isArray(e?.objectRows)?e.objectRows:[];let r=[];try{const a=pi(t),c=Array.isArray(a?.issues)?a.issues.filter(i=>i?.severity==="fatal"):[];Array.isArray(a?.blocks)&&a.blocks.length>0&&c.length===0?r=to(a.blocks):(r=to(Rr(t)),c.length>0&&console.warn("⚠️ [Zemax Import] deriveBlocks had fatals; fallback blocks generated:",c))}catch(a){console.warn("⚠️ [Zemax Import] deriveBlocks failed; fallback blocks generated:",a),r=to(Rr(t))}const s=new Date().toISOString();return{configurations:[{id:1,name:"Config 1",schemaVersion:pn,blocks:r,source:n,object:o,opticalSystem:t,meritFunction:[],systemData:{referenceFocalLength:""},metadata:{created:s,modified:s,locked:!1,importedFrom:"zemax"}}],activeConfigId:1,meritFunction:[],systemRequirements:[],optimizationRules:{}}}function Pl(e){const t=String(e??"").trim().replace(/\s*\(surfaces only\)\s*$/i,"");return t?/\.json$/i.test(t)?t.replace(/\.json$/i,".zmx"):/\.zmx$/i.test(t)?t:`${t}.zmx`:"co-opt-export.zmx"}function El(){const e=document.getElementById("import-zemax-btn");if(!e||Ct(e))return;const t=()=>{const o=document.createElement("input");o.type="file",o.accept=".zmx",o.addEventListener("change",async r=>{const a=r.target?.files?.[0];try{o.parentNode&&o.parentNode.removeChild(o)}catch{}if(a)try{console.log("📥 [Zemax Import] Selected file:",a.name,`(${a.size} bytes)`);const c=await a.arrayBuffer(),i=Jr(c);if(!i||typeof i!="object")throw new Error("Invalid Zemax parse result.");console.log("📥 [Zemax Import] Parsed:",{rows:Array.isArray(i?.rows)?i.rows.length:0,sourceRows:Array.isArray(i?.sourceRows)?i.sourceRows.length:0,objectRows:Array.isArray(i?.objectRows)?i.objectRows.length:0,issues:Array.isArray(i?.issues)?i.issues.length:0});const l=kl(i);if(!await fn(l,{filename:a.name}))throw new Error("Zemax import parsed, but app load step returned false.");try{const{loadActiveConfigurationToTables:p}=await Re(async()=>{const{loadActiveConfigurationToTables:d}=await import("./table-configuration-t-aKc2tY.js").then(u=>u.F);return{loadActiveConfigurationToTables:d}},[]);typeof p=="function"&&(await p({applyToUI:!0}),console.log("[Zemax Import] ✅ Loaded active configuration to tables (UI updated)"))}catch(p){console.error("[Zemax Import] ❌ Failed to load configuration to tables:",p)}try{const p=Array.isArray(i?.rows)?i.rows:[],d=p.findIndex(h=>String(h?.["object type"]??h?.object??"").trim().toLowerCase()==="stop"),u=(()=>{if(d<0)return!1;const h=p[d]||{},g=h?.semidia??h?.semiDiameter??h?.semiDia??h?.["semi diameter"]??h?.["Semi Diameter"];if(g==null)return!0;const y=String(g).trim();if(y==="")return!0;const A=Number(y);return!(Number.isFinite(A)&&A>0)})();Hi(Array.isArray(i?.sourceRows)?i.sourceRows:[],Array.isArray(i?.objectRows)?i.objectRows:[],{entrancePupilDiameterMm:Number(i?.entrancePupilDiameterMm),stopSemidiaWasMissing:u})}catch{}try{if(typeof O.calculateImageSemiDiaFromChiefRays=="function"){const p=d=>{setTimeout(()=>{try{Promise.resolve(O.calculateImageSemiDiaFromChiefRays()).then(u=>{if(u===!0){try{Je()}catch{}try{typeof O.refreshAllUI=="function"&&O.refreshAllUI()}catch{}return}d>0&&p(d-1)}).catch(()=>{d>0&&p(d-1)})}catch{d>0&&p(d-1)}},200)};p(4)}}catch{}try{Fl()}catch{}console.log("✅ [Zemax Import] Completed:",a.name)}catch(c){console.error("❌ Zemax import failed:",c),alert(`Import failed: ${c?.message||String(c)}`)}}),document.body.appendChild(o),o.click()},n=e.cloneNode(!0);e.parentNode?.replaceChild(n,e),n.addEventListener("click",t)}function Tl(){const e=document.getElementById("export-zemax-btn");if(!e||Ct(e))return;const t=async()=>{try{const o=O.getOpticalSystemRows?O.getOpticalSystemRows(O.tableOpticalSystem):[],r=O.tableSource&&typeof O.tableSource.getData=="function"?O.tableSource.getData():[],s=O.tableObject&&typeof O.tableObject.getData=="function"?O.tableObject.getData():[];if(!o||o.length===0){alert("No optical system data to export");return}let a=null;try{a=(await Re(()=>import("./loaded-file-storage-DhVIUlJT.js"),__vite__mapDeps([5,1]))).getLoadedFileName()}catch{try{a=O.__cooptLoadedFileStorage?.getLoadedFileName?.()??null}catch{}}const c=Pl(a);let i=prompt("Zemaxエクスポートのファイル名を入力してください（.zmx は自動補完）",c);if(!i||(i=i.trim(),!i))return;if(/\.zmx$/i.test(i)||(i+=".zmx"),typeof O.generateZMXText=="function"){const l=O.generateZMXText(o,{sourceRows:r,objectRows:s});typeof O.downloadZMX=="function"?(O.downloadZMX(l,i),console.log("✅ Zemax file exported successfully")):(console.error("❌ downloadZMX function not available"),alert("Export function not available"))}else console.error("❌ generateZMXText function not available"),alert("Export function not available")}catch(o){console.error("❌ Zemax export failed:",o),alert(`Export failed: ${o?.message||String(o)}`)}},n=e.cloneNode(!0);e.parentNode?.replaceChild(n,e),n.addEventListener("click",t)}function Ol(){const e=document.getElementById("optimize-design-intent-btn");e&&(Ct(e)||e.addEventListener("click",async()=>{const t=typeof globalThis<"u"?globalThis:{},n=!!t.__cooptOptimizerIsRunning,o=t.__cooptOptimizerSchedulerWindow,r=n&&(!o||o.closed);if(n&&!r){alert("Optimization is already running. Please wait for it to complete or stop it first.");return}r&&(t.__cooptOptimizerIsRunning=!1),e.disabled,e.disabled=!0;try{if(ze()){const I=O.getOpticalSystemRows&&O.tableOpticalSystem?O.getOpticalSystemRows(O.tableOpticalSystem):O.tableOpticalSystem&&typeof O.tableOpticalSystem.getData=="function"?O.tableOpticalSystem.getData():[];if(!Array.isArray(I)||I.length===0){alert("最適化対象の光学系データがありません。");return}const E=(()=>{try{const se=window.systemRequirementsEditor;if(se&&typeof se.getData=="function"){const pe=se.getData();if(Array.isArray(pe))return pe}}catch{}return[]})(),P=O.tableSource&&typeof O.tableSource.getData=="function"?O.tableSource.getData():[],Z=O.tableObject&&typeof O.tableObject.getData=="function"?O.tableObject.getData():[],R=(()=>{try{const se=typeof O.loadSystemConfigurationsFromTableConfig=="function"?O.loadSystemConfigurationsFromTableConfig():typeof O.loadSystemConfigurations=="function"?O.loadSystemConfigurations():null;if(se&&se.activeConfigId!==void 0&&se.activeConfigId!==null)return String(se.activeConfigId).trim()}catch{}return""})(),k=O.OptimizationMVP;if(!k||typeof k.run!="function"){alert("OptimizationMVP が利用できません。");return}const $=[],te=await k.run({opticalSystemRows:I,sourceRows:P,objectRows:Z,activeConfigId:R,systemRequirementsRows:E,maxIterations:24,method:"kkt",forceTs:!0,onProgress:se=>{!se||typeof se!="object"||$.push(se)}});Array.isArray($)&&$.length>0&&console.log("📈 [Optimize][TS][Progress]",$.slice(-8));try{typeof window.drawOpticalSystem=="function"&&window.drawOpticalSystem()}catch(se){console.warn("⚠️ [Optimize][TS] result apply failed:",se)}const q=String(te?.method||"kkt"),z=Number(te?.iterations??0),V=Number(te?.variables??0),K=Number(te?.before??Number.NaN),oe=Number(te?.best??Number.NaN),re=Number(te?.violationScore??Number.NaN),fe=!te?.aborted;alert(["TS optimizer step completed",`mode: ${q}`,`iterations: ${z}`,`variables: ${V}`,`merit: ${Number.isFinite(K)?K.toFixed(6):"NaN"} -> ${Number.isFinite(oe)?oe.toFixed(6):"NaN"}`,`requirements: ${Number.isFinite(re)?re.toFixed(6):"NaN"}`,fe?"status: converged":"status: in-progress"].join(`
`));return}const s=O.OptimizationMVP;if(!s||typeof s.run!="function"){alert("OptimizationMVP が利用できません。"),e.disabled=!1;return}let a=!1,c=null,i=0,l=0,m=0;try{const I=typeof O.loadSystemConfigurationsFromTableConfig=="function"?O.loadSystemConfigurationsFromTableConfig():Ie(),E=I?.activeConfigId;c=I?.configurations?.find(R=>R&&R.id===E)||I?.configurations?.[0]||null,c&&Array.isArray(c.scenarios)&&c.scenarios.length>=2&&(a=!0);const P=Wo(c||{}),Z=Array.isArray(P)?P.filter(R=>typeof R?.value=="number"&&Number.isFinite(R.value)):[];i=Array.isArray(P)?P.length:0,l=Z.length,m=Math.max(0,i-l)}catch{}let p=null;const d={stop:!1};let u=null,h=!1;try{if(p=window.open("","coopt-optimizer-progress","width=500,height=550,resizable=yes,scrollbars=no"),p&&p.document){const I=window&&window.location&&window.location.origin?window.location.origin:"",E=`${I}/favicon.svg`;p.document.open(),p.document.write(`<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Optimize Progress</title>
  <base href="${I}/" />
  <link rel="icon" type="image/svg+xml" href="${E}" />
</head>
<body style="font-family: system-ui, -apple-system, Segoe UI, sans-serif; margin: 12px;">
<div style="display:flex; align-items:center; gap:10px; flex-wrap:nowrap; white-space:nowrap; margin-bottom:8px;">
    <div style="font-size:14px; font-weight:600; flex:0 0 auto;">Optimize Progress</div>
    <div id="opt-startup-progress-wrap" style="display:flex; align-items:center; gap:8px; flex:1 1 auto; min-width:0; margin:0;">
        <div style="height:6px; background:#eceef2; border-radius:999px; overflow:hidden; flex:1 1 auto; min-width:120px;">
            <div id="opt-startup-progress-bar" style="width:0%; height:100%; background:#4f8cff; transition:width 120ms linear;"></div>
        </div>
        <div id="opt-startup-progress-label" style="font-size:11px; color:#666; flex:0 0 auto;">Idle</div>
    </div>
</div>
<div style="font-size:12px; color:#555; margin-bottom:10px; white-space:nowrap;">Updates per candidate evaluation (±step)</div>
<div style="margin-bottom:10px; display:flex; align-items:center; gap:6px;">
    <button id="opt-run" style="padding:6px 10px;">Run</button>
    <button id="opt-stop" style="padding:6px 10px;" disabled>Stop</button>
    <span id="opt-stop-state" style="margin-left:8px; font-size:12px; color:#555;"></span>
</div>
<div style="margin-bottom:10px; display:flex; align-items:center; gap:10px; flex-wrap:wrap;">
    <label style="font-size:12px; color:#555; display:flex; align-items:center; gap:6px;">
        Method
        <select id="opt-method" style="padding:4px 6px;">
            <option value="kkt" selected>Augmented Lagrangian (AL)</option>
            <option value="lm">Levenberg-Marquardt (LM)</option>
            <option value="cd">Coordinate Descent (CD)</option>
        </select>
    </label>
    <label style="font-size:12px; color:#555; display:flex; align-items:center; gap:6px;">
        Max Iterations
        <input id="opt-max-iter" type="number" min="1" step="1" value="5000" style="width:100px; padding:4px 6px;" />
    </label>
    <label style="font-size:12px; color:#555; display:flex; align-items:center; gap:6px;">
        Convergence
        <select id="opt-convergence-profile" style="padding:4px 6px;">
            <option value="fast">Fast</option>
            <option value="balanced" selected>Balanced</option>
            <option value="deep">Deep</option>
        </select>
    </label>
    <label style="font-size:12px; color:#555; display:flex; align-items:center; gap:6px;">
        <input id="opt-auto-render" type="checkbox" checked style="width:16px; height:16px;" />
        Auto-render on Accept
    </label>
</div>
<div style="display:flex; gap:10px; flex-direction:column;">
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Phase</span><span id="opt-phase" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Decision</span><span id="opt-decision" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Accept/Reject</span><span id="opt-decision-count" style="margin-left:8px;">0 / 0</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Iter</span><span id="opt-iter" style="margin-left:8px;">0</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Vars</span><span id="opt-vars" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Req</span><span id="opt-req" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Res</span><span id="opt-res" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">ReqScore(active)</span><span id="opt-cur" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Violation</span><span id="opt-vio" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Soft</span><span id="opt-soft" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Best</span><span id="opt-best" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Penalty ρ</span><span id="opt-rho" style="margin-left:8px;">-</span></div>
    <div style="display:flex; align-items:baseline;"><span style="display:inline-block; width:110px; color:#555;">Issue</span><span id="opt-issue" style="margin-left:8px;">-</span></div>
</div>
<details style="margin-top:10px; margin-bottom:10px; font-size:12px; color:#555;">
    <summary style="font-weight:600; margin-bottom:6px; cursor:pointer;">Stability Tuning</summary>
    <div style="display:grid; grid-template-columns: 180px 140px 1fr; gap:6px 10px; align-items:center; margin-top:6px;">
        <div>stepFraction</div>
        <input id="opt-step-fraction" type="number" step="0.001" value="0.02" style="width:120px; padding:4px 6px;" />
        <div>CDの初期ステップ比率（小さくすると安定）</div>

        <div>minStep</div>
        <input id="opt-min-step" type="number" step="1e-7" value="1e-6" style="width:120px; padding:4px 6px;" />
        <div>CDの最小ステップ</div>

        <div>stepDecay</div>
        <input id="opt-step-decay" type="number" step="0.05" value="0.5" style="width:120px; padding:4px 6px;" />
        <div>CDの失敗時縮小率</div>

        <div>lmLambda0</div>
        <input id="opt-lm-lambda0" type="number" step="1e-4" value="1e-3" style="width:120px; padding:4px 6px;" />
        <div>LM初期ダンピング</div>

        <div>lmLambdaUp</div>
        <input id="opt-lm-lambdaup" type="number" step="1" value="10" style="width:120px; padding:4px 6px;" />
        <div>LM拒否時の増加係数</div>

        <div>lmLambdaDown</div>
        <input id="opt-lm-lambdadown" type="number" step="0.05" value="0.3" style="width:120px; padding:4px 6px;" />
        <div>LM受理時の減少係数</div>

        <div>trustRegion</div>
        <input id="opt-trust-region" type="checkbox" checked style="width:16px; height:16px;" />
        <div>信頼領域を有効化</div>

        <div>trustRegionDelta</div>
        <input id="opt-trust-region-delta" type="number" step="0.01" value="0.05" style="width:120px; padding:4px 6px;" />
        <div>信頼領域の基本半径</div>

        <div>trustRegionDeltaMax</div>
        <input id="opt-trust-region-delta-max" type="number" step="0.1" value="1.0" style="width:120px; padding:4px 6px;" />
        <div>信頼領域の最大半径</div>

        <div>backtracking</div>
        <input id="opt-backtracking" type="checkbox" checked style="width:16px; height:16px;" />
        <div>LMのバックトラック探索</div>

        <div>backtrackingMaxTries</div>
        <input id="opt-backtracking-max-tries" type="number" step="1" value="8" style="width:120px; padding:4px 6px;" />
        <div>バックトラック試行回数</div>

        <div>fdStepFraction</div>
        <input id="opt-fd-step-fraction" type="number" step="1e-5" value="1e-4" style="width:120px; padding:4px 6px;" />
        <div>数値微分の相対ステップ</div>

        <div>fdMinStep</div>
        <input id="opt-fd-min-step" type="number" step="1e-19" value="1e-18" style="width:120px; padding:4px 6px;" />
        <div>数値微分の最小ステップ</div>

        <div>fdScaledStep</div>
        <input id="opt-fd-scaled-step" type="number" step="1e-4" value="1e-3" style="width:120px; padding:4px 6px;" />
        <div>スケール付き微分ステップ</div>

        <div>staged</div>
        <input id="opt-staged" type="checkbox" checked style="width:16px; height:16px;" />
        <div>係数の段階的解放</div>

        <div>stageStallLimit</div>
        <input id="opt-stage-stall-limit" type="number" step="1" value="5" style="width:120px; padding:4px 6px;" />
        <div>段階の停滞許容回数</div>

        <div>restartOnRejectStreak</div>
        <input id="opt-restart-on-reject-streak" type="number" step="1" value="8" style="width:120px; padding:4px 6px;" />
        <div>連続拒否でリスタート</div>

        <div>restartMaxCount</div>
        <input id="opt-restart-max-count" type="number" step="1" value="2" style="width:120px; padding:4px 6px;" />
        <div>リスタートの最大回数</div>

        <div>restartJitterScaled</div>
        <input id="opt-restart-jitter-scaled" type="number" step="0.005" value="0.035" style="width:120px; padding:4px 6px;" />
        <div>リスタート時のジッタ量</div>

        <div>lmExploreWhenFlat</div>
        <input id="opt-lm-explore-when-flat" type="checkbox" style="width:16px; height:16px;" />
        <div>LMが平坦時に探索を許可</div>

        <div>lmExploreTries</div>
        <input id="opt-lm-explore-tries" type="number" step="1" value="3" style="width:120px; padding:4px 6px;" />
        <div>探索ステップ試行回数</div>
    </div>
</details>
     </body>
    </html>`),p.document.close();try{const P=p.document.getElementById("opt-vars");if(P){const Z=[];Number.isFinite(i)&&Z.push(String(i)),(Number.isFinite(l)||Number.isFinite(m))&&Z.push(`(num ${l}, cat ${m})`),P.textContent=Z.length?Z.join(" "):"-"}}catch{}try{const P=p.document.getElementById("opt-stop"),Z=p.document.getElementById("opt-run"),R=p.document.getElementById("opt-stop-state");P&&P.addEventListener("click",()=>{d.stop=!0;try{globalThis.__cooptLastUiStopReason="popup-stop-button"}catch{}try{const k=O.OptimizationMVP;k&&typeof k.stop=="function"&&k.stop()}catch{}try{P&&(P.disabled=!0)}catch{}try{Z&&(Z.disabled=!0)}catch{}R&&(R.textContent="Stopping...")}),Z&&Z.addEventListener("click",()=>{try{R&&(R.textContent="Running...");const k=p?.document?.getElementById("opt-phase");k&&String(k.textContent||"").trim()==="-"&&(k.textContent="starting");const $=p?.document?.getElementById("opt-startup-progress-wrap"),te=p?.document?.getElementById("opt-startup-progress-bar"),q=p?.document?.getElementById("opt-startup-progress-label");$&&($.style.display="block"),te&&(te.style.width="5%"),q&&(q.textContent="Run clicked. Preparing..."),Z&&(Z.disabled=!0),P&&(P.disabled=!1)}catch{}try{const k=O.__cooptStartOptimizationFromPopup;typeof k=="function"&&window.requestAnimationFrame(()=>{window.setTimeout(()=>{try{k()}catch{}},0)})}catch{}})}catch{}}}catch{p=null}if(p){try{t.__cooptOptimizerSchedulerWindow=p,t.__cooptOptimizerIsRunning=!1}catch{}try{p.onbeforeunload=function(I){if(t.__cooptOptimizerIsRunning){const E="Optimization is still running. Closing this window may cause instability. Are you sure?";return I.returnValue=E,E}}}catch{}try{u=window.setInterval(()=>{if(!p||p.closed){if(t.__cooptOptimizerIsRunning){alert(`⚠️ Warning: Optimize Progress window was closed while optimization was running.
This may cause instability. Use the Stop button before closing the window.`),d.stop=!0;try{globalThis.__cooptLastUiStopReason="popup-closed-watchdog"}catch{}try{const I=O.OptimizationMVP;I&&typeof I.stop=="function"&&I.stop()}catch{}t.__cooptOptimizerIsRunning=!1}if(u){try{window.clearInterval(u)}catch{}u=null}try{t.__cooptOptimizerSchedulerWindow===p&&(t.__cooptOptimizerSchedulerWindow=null)}catch{}}},250)}catch{}}const g=document.getElementById("total-merit-value");let y="-",A="-",x="-",_="-",S="-",N="-",f="-",w=0,b=0,v="balanced",M=0;const C=500;let D=null,B=0,j=!1,H=!1;const ce=(I=!1)=>{try{typeof O.__cooptInitMeritFunctionEditor=="function"&&O.__cooptInitMeritFunctionEditor()}catch{}D||(D=Re(()=>import("./optimizer-wasm-bridge-muCRhr2Y.js"),__vite__mapDeps([6,0,1])).then(E=>E&&typeof E.preloadOptimizerWasmBridge=="function"?E.preloadOptimizerWasmBridge():null).then(()=>{}).catch(()=>{}))};try{window.setTimeout(()=>ce(!1),0)}catch{}const U=async()=>{let I=NaN,E=NaN;try{const P=O.systemRequirementsEditor;if(P&&typeof P.evaluateAndUpdateNow=="function"){const q=P.evaluateAndUpdateNow({reason:"optimize-progress-prerun"});q&&typeof q.then=="function"&&await q}const Z=(()=>{try{if(P&&typeof P.getData=="function"){const q=P.getData();if(Array.isArray(q))return q}}catch{}try{if(P&&Array.isArray(P.requirements))return P.requirements}catch{}return[]})(),R=(()=>{try{const q=typeof O.loadSystemConfigurationsFromTableConfig=="function"?O.loadSystemConfigurationsFromTableConfig():typeof O.loadSystemConfigurations=="function"?O.loadSystemConfigurations():null;if(q&&q.activeConfigId!==void 0&&q.activeConfigId!==null)return String(q.activeConfigId).trim()}catch{}return""})(),k=q=>{try{if(P&&typeof P._normalizeConfigId=="function")return String(P._normalizeConfigId(q?.configId,cfg,R)||"").trim()}catch{}return String(q?.configId??"").trim()||R},$=q=>{if(!q||typeof q!="object")return!1;const z=q.enabled===void 0||q.enabled===null?!0:!!q.enabled,V=String(q.operand??"").trim(),K=Number(q.weight??1);if(!z||!V||!(Number.isFinite(K)&&K>0))return!1;const oe=k(q);return R?oe===R:!0},te=Array.isArray(Z)?Z.filter(q=>$(q)):[];if(E=te.length,te.length>0){let q=0,z=0;for(const V of te){const K=Number(V?._contribution);Number.isFinite(K)&&(K>0&&(q+=K),z+=1)}z>0&&Number.isFinite(q)&&(I=q)}}catch{}if(Number.isFinite(I)||(I=NaN),!(!p||p.closed))try{const P=p.document,Z=P.getElementById("opt-cur"),R=P.getElementById("opt-best"),k=P.getElementById("opt-req"),$=P.getElementById("opt-vio"),te=P.getElementById("opt-soft"),q=P.getElementById("opt-phase");Z&&(Z.textContent=Number.isFinite(I)?I.toFixed(6):"-"),R&&(R.textContent=Number.isFinite(I)?I.toFixed(6):"-"),k&&Number.isFinite(E)&&(k.textContent=String(Math.max(0,Math.floor(E)))),$&&($.textContent=Number.isFinite(I)?I.toFixed(6):"-"),te&&(te.textContent=Number.isFinite(I)?"0.000000":"-"),q&&String(q.textContent||"").trim()==="-"&&(q.textContent="ready")}catch{}},G=()=>{try{const I=O.popup3DWindow;if(!!!(I&&!I.closed)){const Z=O.__cooptOpenRenderWindow||window.__cooptOpenRenderWindow;if(ze()&&typeof Z=="function"&&Promise.resolve(Z()),typeof O.handleRender3D=="function")O.handleRender3D();else try{const R=document.getElementById("open-3d-window-btn");R&&typeof R.click=="function"?R.click():typeof O.__open3DWindowLegacy=="function"&&O.__open3DWindowLegacy()}catch{}}try{const Z=typeof globalThis<"u"?globalThis:null,k=(Z&&Array.isArray(Z.__cooptOpticalSystemRowsOverride)&&Z.__cooptOpticalSystemRowsOverride.length>0?Z.__cooptOpticalSystemRowsOverride:null)??(typeof O.getOpticalSystemRows=="function"?O.getOpticalSystemRows(O.tableOpticalSystem):[]),$={ts:Date.now(),rows:Array.isArray(k)?k:[]};localStorage.setItem("coopt.renderSyncRequest",JSON.stringify($)),ze()&&(async()=>{try{const te=await Re(()=>import("./event-B_hCB_W5.js"),__vite__mapDeps([2,0,1]));te&&typeof te.emit=="function"&&await te.emit("coopt-render-sync-request",$)}catch{}})()}catch{}const P=()=>{try{const Z=O.popup3DWindow;if(Z&&!Z.closed){const R=Z.document?.getElementById("draw-btn");R&&R.click()}}catch{}};P(),window.setTimeout(P,120)}catch{}},F=I=>{const E=String(I?.phase??""),P=(k,$,te)=>{try{if(!p||p.closed)return;const q=p.document.getElementById("opt-startup-progress-wrap"),z=p.document.getElementById("opt-startup-progress-bar"),V=p.document.getElementById("opt-startup-progress-label");q&&(q.style.display="block"),z&&(z.style.width=`${Math.max(0,Math.min(100,Number(k)||0))}%`),V&&$!==void 0&&(V.textContent=$),te&&window.setTimeout(()=>{try{if(!p||p.closed)return;const K=p.document.getElementById("opt-startup-progress-wrap");K&&(K.style.display="none")}catch{}},350)}catch{}};if(E==="start"?P(92,"Optimization started. Waiting first iteration..."):E==="iter"||E==="candidate"||E==="accept"||E==="reject"?P(100,"First iteration reached",!0):(E==="done"||E==="stopped"||E==="error")&&P(100,"Finished",!0),!j&&B>0&&(E==="start"||E==="iter"||E==="candidate"||E==="accept"||E==="reject")){j=!0;const k=Math.max(0,Date.now()-B),$=`startup=${k}ms`;try{const te=String(y||"").trim();!te||te==="-"?y=$:te.includes("startup=")||(y=`${te} | ${$}`)}catch{}try{console.log(`⏱️ [Optimize] Run→first progress: ${k} ms`)}catch{}}if(E==="stopped"||E==="done"||E==="error"){try{e.disabled=!1}catch{}h=!1}if(E==="done"&&!H)try{if(p&&!p.closed){const k=p.document.getElementById("opt-auto-render");k&&k.checked&&(H=!0,window.setTimeout(()=>G(),80),window.setTimeout(()=>G(),260))}}catch{}if(E==="accept"){w++;const k=I&&"alpha"in I?Number(I.alpha):NaN,$=I&&"rho"in I?Number(I.rho):NaN,te=Number.isFinite(k)?k.toFixed(6):"-",q=Number.isFinite($)?$.toFixed(6):"-";f=`ACCEPT (α=${te}, ρ=${q})`,_=q;try{if(p&&!p.closed){const z=p.document.getElementById("opt-auto-render");z&&z.checked&&G()}}catch{}}else E==="reject"&&(b++,f="REJECT");const Z=Number(I?.current),R=Number(I?.best);g&&Number.isFinite(Z)&&(g.textContent=Z.toFixed(6));try{const k=Date.now();if(k-M>=C&&(E==="start"||E==="iter"||E==="candidate"||E==="accept"||E==="reject")){const $=O.systemRequirementsEditor;$&&typeof $.scheduleEvaluateAndUpdate=="function"&&(M=k,$.scheduleEvaluateAndUpdate())}}catch{}I&&"materialIssue"in I&&(y=I.materialIssue===void 0||I.materialIssue===null||I.materialIssue===""?"-":String(I.materialIssue));try{const k=O.__cooptLastOptimizerResidualDebug&&typeof O.__cooptLastOptimizerResidualDebug=="object"?O.__cooptLastOptimizerResidualDebug:null,$=k&&k.worst&&typeof k.worst=="object"?k.worst:null,te=k?Number(k.at):NaN,q=Number.isFinite(te)?Date.now()-te<3e3:!1,z=V=>{const K=Number(V);return Number.isFinite(K)?K.toFixed(6):String(V??"-")};if(q&&$&&$.operand){const V=String($.operand),K=String($.configId??""),oe=$.scenarioId!==void 0&&$.scenarioId!==null&&String($.scenarioId).trim()?String($.scenarioId):"",re=z($.amount),fe=z($.current),se=String($.reason??"").trim(),pe=(()=>{try{if(!V.startsWith("SPOT_SIZE"))return"";const X=k&&k.spotDebug&&typeof k.spotDebug=="object"?k.spotDebug:null;if(!X)return"";const Y=String(X.impl??"").trim(),J=String(X.reason??"").trim(),le=X.earlyAbortHitRate,Q=le==null||le===""?NaN:Number(le),ee=String(X.failPenaltyKind??"").trim(),ie=X.lastRayTraceFailure&&typeof X.lastRayTraceFailure=="object"?X.lastRayTraceFailure:null,be=ie&&ie.details&&typeof ie.details=="object"?ie.details:null,ae=Number(X.blockSurfaceNumber??be?.surfaceNumber),he=Number(X.blockHitRadiusMm??be?.hitRadiusMm),de=Number(X.blockApertureLimitMm??be?.apertureLimitMm),Ae=Number(X.targetSurfaceIndex),xe=Number(X.wavelength),Ne=Number(X.rayCountRequested),ye=Number(X.hits),Se=[];return Y&&Se.push(Y),J&&Se.push(J),Number.isFinite(Q)&&Se.push(`hitRate=${Q.toFixed(3)}`),ee&&Se.push(ee),Number.isFinite(Ae)&&Ae>=0&&Se.push(`Sidx=${Math.floor(Ae)}`),Number.isFinite(xe)&&xe>0&&Se.push(`wl=${xe.toFixed(4)}um`),Number.isFinite(Ne)&&Ne>0&&Se.push(`rays=${Math.floor(Ne)}`),Number.isFinite(ye)&&ye>=0&&Se.push(`hits=${Math.floor(ye)}`),ee==="PHYSICAL_APERTURE_BLOCK"&&(Number.isFinite(ae)&&ae>0&&Se.push(`S${Math.floor(ae)}`),Number.isFinite(he)&&Number.isFinite(de)&&de>0&&Se.push(`r=${he.toFixed(3)}/${de.toFixed(3)}mm`)),Se.length>0?` [spot:${Se.join(" ")}]`:""}catch{return""}})(),me=oe?` cfg=${K} scn=${oe}`:K?` cfg=${K}`:"",L=se?` (${se})`:"";y=`Worst: ${V}${me} cur=${fe} amt=${re}${L}${pe}`}}catch{}if(I?.requirementCount!==void 0&&(A=String(I.requirementCount)),I?.residualCount!==void 0&&(x=String(I.residualCount)),I&&"rho"in I){const k=Number(I.rho);_=Number.isFinite(k)?k.toFixed(6):"-"}if(I&&"violationScore"in I){const k=Number(I.violationScore);S=Number.isFinite(k)?k.toFixed(6):"-"}if(I&&"softPenalty"in I){const k=Number(I.softPenalty);N=Number.isFinite(k)?k.toFixed(6):"-"}if(p&&!p.closed)try{const k=p.document,$=(te,q)=>{const z=k.getElementById(te);z&&(z.textContent=q)};if($("opt-phase",String(I?.phase??"-")),$("opt-decision",f),$("opt-decision-count",`${w} / ${b}`),$("opt-iter",String(I?.iter??"-")),$("opt-req",A),$("opt-res",x),$("opt-cur",Number.isFinite(Z)?Z.toFixed(6):String(I?.current??"-")),$("opt-vio",S),$("opt-soft",N),$("opt-best",Number.isFinite(R)?R.toFixed(6):String(I?.best??"-")),$("opt-rho",_),$("opt-issue",y),String(I?.phase)==="stopped"){$("opt-stop-state","Stopped");try{const te=k.getElementById("opt-stop");te&&(te.disabled=!0);const q=k.getElementById("opt-run");q&&(q.disabled=!1)}catch{}}else if(String(I?.phase)==="done"){$("opt-stop-state","Done");try{const te=k.getElementById("opt-stop");te&&(te.disabled=!0);const q=k.getElementById("opt-run");q&&(q.disabled=!1)}catch{}}else if(String(I?.phase)==="error"){$("opt-stop-state","Error");try{const te=k.getElementById("opt-stop");te&&(te.disabled=!0);const q=k.getElementById("opt-run");q&&(q.disabled=!1)}catch{}}else d.stop&&$("opt-stop-state","Stopping...")}catch{}},W=async()=>{h=!0,t.__cooptOptimizerIsRunning=!0,B=Date.now(),j=!1,H=!1;const I=(E,P,Z)=>{try{if(!p||p.closed)return;const R=p.document,k=R.getElementById("opt-phase"),$=R.getElementById("opt-issue"),te=R.getElementById("opt-iter"),q=R.getElementById("opt-startup-progress-wrap"),z=R.getElementById("opt-startup-progress-bar"),V=R.getElementById("opt-startup-progress-label");k&&(k.textContent=String(E||"-")),$&&P!==void 0&&($.textContent=String(P||"-")),te&&Z!==void 0&&(te.textContent=String(Z||"-")),q&&(q.style.display="block");const K=String(E||"").toLowerCase();let oe=10;K==="prepare"?oe=35:K==="warmup"?oe=70:K==="start"&&(oe=88),z&&(z.style.width=`${oe}%`),V&&P!==void 0&&(V.textContent=String(P||""))}catch{}};try{let E=null;try{const V=Ie();E=V?JSON.parse(JSON.stringify(V)):null}catch{}d.stop=!1;try{globalThis.__cooptLastUiStopReason=null}catch{}w=0,b=0,y="-",A="-",x="-",_="-",S="-",N="-",f="-",I("prepare","Loading active configuration...");try{const V=typeof O.loadSystemConfigurationsFromTableConfig=="function"?O.loadSystemConfigurationsFromTableConfig():Ie(),K=V?.activeConfigId;c=V?.configurations?.find(fe=>fe&&fe.id===K)||V?.configurations?.[0]||null,c&&Array.isArray(c.scenarios)&&c.scenarios.length>=2?a=!0:a=!1;const oe=Wo(c||{}),re=Array.isArray(oe)?oe.filter(fe=>typeof fe?.value=="number"&&Number.isFinite(fe.value)):[];if(i=Array.isArray(oe)?oe.length:0,l=re.length,m=Math.max(0,i-l),p&&!p.closed)try{const fe=p.document.getElementById("opt-vars");if(fe){const se=[];Number.isFinite(i)&&se.push(String(i)),(Number.isFinite(l)||Number.isFinite(m))&&se.push(`(num ${l}, cat ${m})`),fe.textContent=se.length?se.join(" "):"-"}}catch{}}catch{}I("prepare",`Configuration loaded (vars=${i}, num=${l}, cat=${m})`);try{if(p&&!p.closed){const V=p.document,K=V.getElementById("opt-stop"),oe=V.getElementById("opt-run"),re=V.getElementById("opt-stop-state"),fe=V.getElementById("opt-phase");K&&(K.disabled=!1),oe&&(oe.disabled=!0),re&&(re.textContent="Running..."),fe&&String(fe.textContent||"").trim()==="-"&&(fe.textContent="starting")}}catch{}try{e.disabled=!0}catch{}const P=()=>!!d.stop,Z=()=>{let V=5e3;try{if(p&&!p.closed){const K=p.document.getElementById("opt-max-iter"),oe=K?Number(K.value):NaN;Number.isFinite(oe)&&(V=Math.trunc(oe))}}catch{}return(!Number.isFinite(V)||V<1)&&(V=5e3),V},R=()=>{const V=(me,L)=>{let X=L;try{if(p&&!p.closed){const Y=p.document.getElementById(me),J=Y?Number(Y.value):NaN;Number.isFinite(J)&&(X=J)}}catch{}return X},K=(me,L)=>{let X=L;try{if(p&&!p.closed){const Y=p.document.getElementById(me);Y&&typeof Y.checked=="boolean"&&(X=!!Y.checked)}}catch{}return X},oe=(me,L)=>{let X=L;try{if(p&&!p.closed){const Y=p.document.getElementById(me),J=Y?String(Y.value).trim().toLowerCase():"";J&&(X=J)}}catch{}return X},re=V("opt-trust-region-delta",.05),fe=Math.max(re,V("opt-trust-region-delta-max",1)),se=oe("opt-convergence-profile","balanced"),pe=se==="fast"?{kktPlateauStopMinIter:30,kktPlateauStopWindow:30,kktTailStopMinIter:80,kktTailStopWindow:40,kktWindowNoGainMinIter:70,kktWindowNoGainWindow:30,kktGoodEnoughStopMinIter:55,kktGoodEnoughStopWindow:20,kktNoBestImproveMinIter:120,kktNoBestImproveWindow:80,kktPostBestNoImproveWindow:10,kktPostBestPatienceWindow:10,kktHardIterCap:180,kktMaxWallMs:12e4}:se==="deep"?{kktPlateauStopMinIter:70,kktPlateauStopWindow:70,kktTailStopMinIter:180,kktTailStopWindow:90,kktWindowNoGainMinIter:170,kktWindowNoGainWindow:90,kktGoodEnoughStopMinIter:130,kktGoodEnoughStopWindow:70,kktNoBestImproveMinIter:260,kktNoBestImproveWindow:180,kktPostBestNoImproveWindow:24,kktPostBestPatienceWindow:24,kktHardIterCap:520,kktMaxWallMs:36e4}:{kktPlateauStopMinIter:45,kktPlateauStopWindow:45,kktTailStopMinIter:120,kktTailStopWindow:60,kktWindowNoGainMinIter:110,kktWindowNoGainWindow:50,kktGoodEnoughStopMinIter:90,kktGoodEnoughStopWindow:35,kktNoBestImproveMinIter:180,kktNoBestImproveWindow:120,kktPostBestNoImproveWindow:16,kktPostBestPatienceWindow:16,kktHardIterCap:320,kktMaxWallMs:24e4};return{stepFraction:V("opt-step-fraction",.02),minStep:V("opt-min-step",1e-6),stepDecay:V("opt-step-decay",.5),lmLambda0:V("opt-lm-lambda0",.001),lmLambdaUp:V("opt-lm-lambdaup",10),lmLambdaDown:V("opt-lm-lambdadown",.3),trustRegion:K("opt-trust-region",!0),trustRegionDelta:re,trustRegionDeltaMax:fe,backtracking:K("opt-backtracking",!0),backtrackingMaxTries:Math.max(1,Math.floor(V("opt-backtracking-max-tries",8))),fdStepFraction:V("opt-fd-step-fraction",1e-4),fdMinStep:V("opt-fd-min-step",1e-18),fdScaledStep:V("opt-fd-scaled-step",.001),staged:K("opt-staged",!0),stageStallLimit:Math.max(1,Math.floor(V("opt-stage-stall-limit",5))),restartOnRejectStreak:Math.max(1,Math.floor(V("opt-restart-on-reject-streak",8))),restartMaxCount:Math.max(0,Math.floor(V("opt-restart-max-count",2))),restartJitterScaled:Math.max(0,V("opt-restart-jitter-scaled",.035)),lmExploreWhenFlat:K("opt-lm-explore-when-flat",!1),lmExploreTries:Math.max(1,Math.floor(V("opt-lm-explore-tries",3))),convergenceProfile:se,...pe,useWasmLinearSolve:!0,profile:!1}},k=Z(),$=R();v=String($?.convergenceProfile||"balanced").toLowerCase(),I("prepare",`Options resolved (conv=${v}, maxIter=${k})`);try{const V=`conv=${v}`,K=String(y||"").trim();y=!K||K==="-"?V:`${K} | ${V}`}catch{}I("warmup","Initializing WASM bridge..."),ce(!0);try{D&&await D,I("warmup","WASM bridge ready")}catch{I("warmup","WASM warmup skipped (fallback available)")}const te=()=>{let V="kkt";try{if(p&&!p.closed){const K=p.document.getElementById("opt-method"),oe=K?String(K.value).toLowerCase().trim():"";(oe==="cd"||oe==="lm"||oe==="kkt")&&(V=oe)}}catch{}return V};let q=null,z;try{O.undoHistory&&(O.undoHistory.isExecuting=!0);try{z=t.__COOPT_DISABLE_RAYTRACE_DEBUG}catch{z=void 0}try{t.__COOPT_DISABLE_RAYTRACE_DEBUG=!0}catch{}I("start","Starting optimization and computing initial residuals...","0"),q=await s.run({multiScenario:a,runUntilStopped:!1,maxIterations:k,method:te(),stageMaxCoef:[10],...$,onProgress:F,shouldStop:P});try{if(z!==void 0)t.__COOPT_DISABLE_RAYTRACE_DEBUG=z;else try{delete t.__COOPT_DISABLE_RAYTRACE_DEBUG}catch{}t.__cooptOptimizerIsRunning=!1}catch{}O.undoHistory&&(O.undoHistory.isExecuting=!1);try{if(E&&O.undoHistory&&q?.ok){const V=Ie(),K=V?JSON.parse(JSON.stringify(V)):null;if(JSON.stringify(E)!==JSON.stringify(K)){const oe={name:"Optimization",execute:async()=>{Te(K);try{O.ConfigurationManager&&typeof O.ConfigurationManager.loadActiveConfigurationToTables=="function"&&await O.ConfigurationManager.loadActiveConfigurationToTables({applyToUI:!0})}catch{}try{Ut(O)}catch{}},undo:async()=>{Te(E);try{O.ConfigurationManager&&typeof O.ConfigurationManager.loadActiveConfigurationToTables=="function"&&await O.ConfigurationManager.loadActiveConfigurationToTables({applyToUI:!0})}catch{}try{Ut(O)}catch{}},redo:function(){return this.execute()}};O.undoHistory.record(oe)}}}catch{}}catch(V){console.warn("⚠️ [Optimize] Failed:",V),q={ok:!1,reason:V?.message??String(V)};try{if(z!==void 0)t.__COOPT_DISABLE_RAYTRACE_DEBUG=z;else try{delete t.__COOPT_DISABLE_RAYTRACE_DEBUG}catch{}t.__cooptOptimizerIsRunning=!1}catch{}O.undoHistory&&(O.undoHistory.isExecuting=!1)}finally{h=!1,t.__cooptOptimizerIsRunning=!1;try{e.disabled=!1}catch{}try{if(p&&!p.closed){const V=p.document,K=V.getElementById("opt-stop"),oe=V.getElementById("opt-run"),re=V.getElementById("opt-stop-state");K&&(K.disabled=!0),oe&&(oe.disabled=!1),re&&d.stop&&(re.textContent="Stopped")}}catch{}}if(q&&q.ok===!1){const V=String(q.reason||"Optimize did not run.");try{if(p&&!p.closed){const K=p.document.getElementById("opt-phase");K&&(K.textContent="error");const oe=p.document.getElementById("opt-cur");oe&&(oe.textContent=V)}}catch{}alert(V)}}catch(E){console.warn("⚠️ [Optimize] Outer error:",E),t.__cooptOptimizerIsRunning=!1,h=!1}};try{O.__cooptStartOptimizationFromPopup=W}catch{}try{const I=()=>{h||Promise.resolve().then(()=>U()).catch(()=>{})},E=typeof globalThis<"u"?globalThis:null;E&&typeof E.requestIdleCallback=="function"?E.requestIdleCallback(()=>I(),{timeout:1500}):window.setTimeout(()=>I(),600)}catch{}}catch(s){console.warn("⚠️ [Optimize] Failed:",s),alert("Optimize の実行に失敗しました。console を確認してください。")}finally{try{e.disabled=!1}catch{}const s=typeof globalThis<"u"?globalThis:{};s.__cooptOptimizerIsRunning=!1}}))}function Dl(){const e=document.getElementById("suggest-optimize-btn");e&&e.addEventListener("click",async()=>{try{const t=window.open("","OptimizationProgress","width=900,height=700");if(!t){alert("Popup blocked. Please allow popups for this site.");return}t.document.write(`
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Optimization Progress</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            margin: 0;
            padding: 20px;
            background: #f5f5f5;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        h1 {
            margin: 0 0 20px 0;
            font-size: 24px;
            color: #333;
        }
        .progress-bar {
            width: 100%;
            height: 30px;
            background: #e0e0e0;
            border-radius: 4px;
            overflow: hidden;
            margin-bottom: 20px;
        }
        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #4CAF50, #45a049);
            transition: width 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
        }
        .metrics {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 12px;
            margin-bottom: 20px;
        }
        .metric {
            padding: 12px;
            background: #f9f9f9;
            border-radius: 4px;
            border: 1px solid #e0e0e0;
        }
        .metric-label {
            font-size: 12px;
            color: #666;
            margin-bottom: 4px;
        }
        .metric-value {
            font-size: 18px;
            font-weight: bold;
            color: #333;
        }
        .tuning-params {
            margin-top: 20px;
            padding: 15px;
            background: #f0f8ff;
            border-radius: 4px;
        }
        .tuning-params h3 {
            margin: 0 0 12px 0;
            font-size: 16px;
        }
        .param-row {
            display: flex;
            align-items: center;
            margin-bottom: 8px;
        }
        .param-label {
            flex: 0 0 200px;
            font-size: 13px;
        }
        .param-input {
            flex: 1;
            padding: 4px 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        .button-row {
            display: flex;
            gap: 12px;
            margin-top: 20px;
        }
        .btn {
            flex: 1;
            padding: 12px;
            border: none;
            border-radius: 4px;
            font-size: 14px;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.2s;
        }
        .btn-start {
            background: #4CAF50;
            color: white;
        }
        .btn-start:hover {
            background: #45a049;
        }
        .btn-start:disabled {
            background: #ccc;
            cursor: not-allowed;
        }
        .btn-stop {
            background: #f44336;
            color: white;
        }
        .btn-stop:hover {
            background: #da190b;
        }
        .btn-accept {
            background: #2196F3;
            color: white;
        }
        .btn-accept:hover {
            background: #0b7dda;
        }
        .btn-reject {
            background: #ff9800;
            color: white;
        }
        .btn-reject:hover {
            background: #e68900;
        }
        .log {
            margin-top: 20px;
            padding: 12px;
            background: #f5f5f5;
            border-radius: 4px;
            max-height: 200px;
            overflow-y: auto;
            font-family: monospace;
            font-size: 12px;
        }
        .log-entry {
            margin-bottom: 4px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Optimization Progress</h1>
        <div class="progress-bar">
            <div class="progress-fill" id="progress" style="width: 0%">0%</div>
        </div>
        <div class="metrics">
            <div class="metric">
                <div class="metric-label">Iteration</div>
                <div class="metric-value" id="iteration">0</div>
            </div>
            <div class="metric">
                <div class="metric-label">Merit Function</div>
                <div class="metric-value" id="merit">—</div>
            </div>
            <div class="metric">
                <div class="metric-label">Best Merit</div>
                <div class="metric-value" id="best-merit">—</div>
            </div>
            <div class="metric">
                <div class="metric-label">Accepted</div>
                <div class="metric-value" id="accepted">0</div>
            </div>
            <div class="metric">
                <div class="metric-label">Rejected</div>
                <div class="metric-value" id="rejected">0</div>
            </div>
            <div class="metric">
                <div class="metric-label">Variables</div>
                <div class="metric-value" id="variables">0</div>
            </div>
        </div>
        <div class="tuning-params">
            <h3>Stability Tuning Parameters</h3>
            <div class="param-row">
                <label class="param-label">Step Fraction:</label>
                <input type="number" class="param-input" id="step-fraction" value="0.1" step="0.01" min="0.01" max="1">
            </div>
            <div class="param-row">
                <label class="param-label">Min Step:</label>
                <input type="number" class="param-input" id="min-step" value="1e-6" step="1e-7">
            </div>
            <div class="param-row">
                <label class="param-label">LM Lambda:</label>
                <input type="number" class="param-input" id="lm-lambda" value="0.001" step="0.001">
            </div>
            <div class="param-row">
                <label class="param-label">Max Iterations:</label>
                <input type="number" class="param-input" id="max-iter" value="100" step="10" min="10">
            </div>
            <div class="param-row">
                <label class="param-label">
                    <input type="checkbox" id="auto-render" checked> Auto-render on Accept
                </label>
            </div>
        </div>
        <div class="button-row">
            <button class="btn btn-start" id="start-btn">Start Optimization</button>
            <button class="btn btn-stop" id="stop-btn" disabled>Stop</button>
            <button class="btn btn-accept" id="accept-btn" disabled>Accept</button>
            <button class="btn btn-reject" id="reject-btn" disabled>Reject</button>
        </div>
        <div class="log" id="log"></div>
    </div>
    <script>
        let isRunning = false;
        let currentBest = null;
        
        function log(message) {
            const logEl = document.getElementById('log');
            const entry = document.createElement('div');
            entry.className = 'log-entry';
            entry.textContent = new Date().toLocaleTimeString() + ': ' + message;
            logEl.appendChild(entry);
            logEl.scrollTop = logEl.scrollHeight;
        }
        
        document.getElementById('start-btn').addEventListener('click', () => {
            if (isRunning) return;
            isRunning = true;
            document.getElementById('start-btn').disabled = true;
            document.getElementById('stop-btn').disabled = false;
            
            const params = {
                stepFraction: parseFloat(document.getElementById('step-fraction').value),
                minStep: parseFloat(document.getElementById('min-step').value),
                lmLambda: parseFloat(document.getElementById('lm-lambda').value),
                maxIter: parseInt(document.getElementById('max-iter').value)
            };
            
            window.opener.postMessage({ action: 'start-optimization', params }, '*');
            log('Optimization started');
        });
        
        document.getElementById('stop-btn').addEventListener('click', () => {
            if (!isRunning) return;
            isRunning = false;
            document.getElementById('start-btn').disabled = false;
            document.getElementById('stop-btn').disabled = true;
            window.opener.postMessage({ action: 'stop-optimization' }, '*');
            log('Stop requested');
        });
        
        document.getElementById('accept-btn').addEventListener('click', () => {
            window.opener.postMessage({ action: 'accept-result' }, '*');
            document.getElementById('accept-btn').disabled = true;
            document.getElementById('reject-btn').disabled = true;
            log('Result accepted');
            
            if (document.getElementById('auto-render').checked) {
                window.opener.postMessage({ action: 'request-render' }, '*');
            }
        });
        
        document.getElementById('reject-btn').addEventListener('click', () => {
            window.opener.postMessage({ action: 'reject-result' }, '*');
            document.getElementById('accept-btn').disabled = true;
            document.getElementById('reject-btn').disabled = true;
            log('Result rejected');
        });
        
        window.addEventListener('message', (e) => {
            if (e.data.type === 'optimization-progress') {
                const data = e.data;
                document.getElementById('progress').style.width = data.progress + '%';
                document.getElementById('progress').textContent = data.progress + '%';
                document.getElementById('iteration').textContent = data.iteration;
                document.getElementById('merit').textContent = data.merit?.toFixed(6) || '—';
                document.getElementById('best-merit').textContent = data.bestMerit?.toFixed(6) || '—';
                document.getElementById('accepted').textContent = data.accepted || 0;
                document.getElementById('rejected').textContent = data.rejected || 0;
                document.getElementById('variables').textContent = data.variables || 0;
            }
            
            if (e.data.type === 'optimization-complete') {
                isRunning = false;
                document.getElementById('start-btn').disabled = false;
                document.getElementById('stop-btn').disabled = true;
                document.getElementById('accept-btn').disabled = false;
                document.getElementById('reject-btn').disabled = false;
                log('Optimization complete');
            }
        });
    <\/script>
</body>
</html>
            `)}catch(t){console.error("❌ Failed to open optimization popup:",t)}})}function jl(){const e=document.getElementById("new-file-btn");if(!e||Ct(e))return;const t=()=>{if(confirm("Create new file? Current data will be cleared."))try{console.log("🔵 [New File] Clearing localStorage and creating default configuration..."),ya();const r={configurations:[{id:1,name:"Config 1",schemaVersion:pn,blocks:[{blockId:"ObjectSurface-1",blockType:"ObjectSurface",role:null,constraints:{},parameters:{objectDistanceMode:"INF"},variables:{},metadata:{source:"default"}},{blockId:"Stop-1",blockType:"Stop",role:null,constraints:{},parameters:{semiDiameter:10},variables:{},metadata:{source:"default"}},{blockId:"ImageSurface-1",blockType:"ImageSurface",role:null,constraints:{},parameters:{semidiaMode:"Manual"},variables:{},metadata:{source:"default"}}],source:[{id:1,wavelength:.4358343,weight:1,primary:"",angle:0},{id:2,wavelength:.5875618,weight:1,primary:"Primary Wavelength",angle:0},{id:3,wavelength:.6562725,weight:1,primary:"",angle:0}],object:[{id:1,xHeightAngle:0,yHeightAngle:0,position:"Angle",angle:0},{id:2,xHeightAngle:0,yHeightAngle:17.05,position:"Angle",angle:0}],opticalSystem:[],systemData:{referenceFocalLength:""},metadata:{created:new Date().toISOString(),modified:new Date().toISOString(),locked:!1},meritFunction:[]}],activeConfigId:1,meritFunction:[],systemRequirements:[],optimizationRules:{}};Te(r),console.log("✅ [New File] Default configuration created, reloading..."),location.reload()}catch(o){console.error("❌ Failed to create new file:",o),alert("Failed to create new file. See console for details.")}},n=e.cloneNode(!0);e.parentNode?.replaceChild(n,e),n.addEventListener("click",t)}function Ll(){const e=document.getElementById("save-all-btn");if(!e||Ct(e))return;const t=async()=>{try{document.activeElement&&document.activeElement.blur();const o=Gi();let r=null;try{const{getLoadedFileName:m}=await Re(async()=>{const{getLoadedFileName:p}=await import("./loaded-file-storage-DhVIUlJT.js");return{getLoadedFileName:p}},__vite__mapDeps([5,1]));r=m()}catch{}let s="optical_system_data";r&&(s=r.replace(/\.json$/i,""));let a=prompt(`保存するファイル名を入力してください（拡張子 .json は自動で付きます）

※ダウンロードフォルダに既存ファイルがある場合はブラウザが自動的に連番を付けます`,s);if(!a)return;a.endsWith(".json")||(a+=".json");const c=new Blob([JSON.stringify(o,null,2)],{type:"application/json"}),i=URL.createObjectURL(c),l=document.createElement("a");l.href=i,l.download=a,l.click(),URL.revokeObjectURL(i);try{const{setLoadedFileName:m}=await Re(async()=>{const{setLoadedFileName:p}=await import("./loaded-file-storage-DhVIUlJT.js");return{setLoadedFileName:p}},__vite__mapDeps([5,1]));m(a)}catch{}console.log("✅ データが保存されました:",a)}catch(o){console.error("❌ Failed to save:",o),alert(`Save failed: ${o?.message||String(o)}`)}},n=e.cloneNode(!0);e.parentNode?.replaceChild(n,e),n.addEventListener("click",t)}function Bl(){const e=Ie(),t=e?JSON.parse(JSON.stringify(e)):null;if(t){try{delete t.meritFunction}catch{}try{delete t.systemRequirements}catch{}try{if(Array.isArray(t.configurations)){for(const n of t.configurations)if(n&&typeof n=="object")try{delete n.source}catch{}}}catch{}}return t}function Gi(){const e=document.getElementById("reference-focal-length"),t=e?e.value:"";let n=O.tableOpticalSystem?O.tableOpticalSystem.getData():[];try{const o=typeof O.loadSystemConfigurations=="function"?O.loadSystemConfigurations():null,r=o?.activeConfigId,s=Array.isArray(o?.configurations)?o.configurations.find(c=>String(c?.id)===String(r))||o.configurations[0]:null;if(s&&(c=>{try{return c&&Array.isArray(c.blocks)&&c.blocks.length>0}catch{return!1}})(s)&&typeof O.expandBlocksToOpticalSystemRows=="function"){const c=O.expandBlocksToOpticalSystemRows(s.blocks);c&&Array.isArray(c.rows)&&(n=c.rows)}}catch{}return{source:O.tableSource?O.tableSource.getData():[],object:O.tableObject?O.tableObject.getData():[],opticalSystem:n,meritFunction:O.meritFunctionEditor?O.meritFunctionEditor.getData():[],systemRequirements:O.systemRequirementsEditor?O.systemRequirementsEditor.getData():[],systemData:{referenceFocalLength:t},configurations:Bl()}}function zl(){const e=document.getElementById("load-default-btn");if(!e||Ct(e))return;const t=async()=>{if(confirm("Load default optical system? Current data will be replaced."))try{let o=await fetch("/co-opt/defaults/default-load.json");if(o.ok||(o=await fetch("/defaults/default-load.json")),!o.ok)throw new Error(`Failed to load default system: ${o.statusText}`);const r=await o.json();await fn(r,{filename:"default-load.json"})}catch(o){console.error("❌ Failed to load default system:",o),alert("Failed to load default optical system. Check console for details.")}},n=e.cloneNode(!0);e.parentNode?.replaceChild(n,e),n.addEventListener("click",t)}function Wl(){const e=document.getElementById("share-url-btn");if(!e||Ct(e))return;const t=2e3,n=3e4,o=async()=>{try{document.activeElement&&document.activeElement.blur();let s;try{const l=Gi();s=Is(l)}catch(l){console.warn("❌ [Share] Failed to encode:",l),alert(l?.message||"Failed to generate share URL");return}const a=`${location.origin}${location.pathname}`;let c;try{c=Fs(s,a)}catch(l){console.warn("❌ [Share] Failed to build URL:",l),alert(l?.message||"Failed to generate share URL");return}const i=c.length;if(i>n){alert(`Share URL is too long (${i} chars). Please use Save instead.`);return}if(i>=t&&!confirm(`Share URL is long (${i} chars) and may not work in some apps.

Continue?`))return;try{await navigator.clipboard.writeText(c),alert("Share URL copied to clipboard.")}catch{prompt("Copy this URL:",c)}}catch(s){console.error("❌ Failed to share:",s),alert(`Share failed: ${s?.message||String(s)}`)}},r=e.cloneNode(!0);e.parentNode?.replaceChild(r,e),r.addEventListener("click",o)}function $l(){const e=document.getElementById("calculate-paraxial-btn");e&&e.addEventListener("click",async()=>{try{if(typeof O.outputParaxialDataToDebug=="function"){const t=O.tableOpticalSystem;O.outputParaxialDataToDebug(t)}else console.error("❌ outputParaxialDataToDebug関数が見つかりません")}catch(t){console.error("❌ 近軸計算ボタンエラー:",t)}})}function Ul(){const e=document.getElementById("calculate-seidel-btn");e&&e.addEventListener("click",async()=>{try{typeof O.outputSeidelCoefficientsToDebug=="function"?O.outputSeidelCoefficientsToDebug():console.error("❌ outputSeidelCoefficientsToDebug関数が見つかりません")}catch(t){console.error("❌ Seidel係数計算ボタンエラー:",t)}})}function Hl(){const e=document.getElementById("calculate-seidel-afocal-btn");e&&e.addEventListener("click",async()=>{try{const{calculateAfocalSeidelCoefficientsIntegrated:t}=await Re(async()=>{const{calculateAfocalSeidelCoefficientsIntegrated:d}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(u=>u.bO);return{calculateAfocalSeidelCoefficientsIntegrated:d}},__vite__mapDeps([0,1])),{formatSeidelCoefficients:n}=await Re(async()=>{const{formatSeidelCoefficients:d}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(u=>u.bN);return{formatSeidelCoefficients:d}},__vite__mapDeps([0,1])),o=O.getOpticalSystemRows?O.getOpticalSystemRows():[],r=O.getObjectTableRows?O.getObjectTableRows():[],s=O.getSourceTableRows?O.getSourceTableRows():[];if(o.length===0){console.error("❌ Optical system data is empty"),alert("光学系データがありません。");return}const a=(()=>{if(typeof O.getPrimaryWavelength=="function"){const d=Number(O.getPrimaryWavelength());if(Number.isFinite(d)&&d>0)return d}return alert("Primary wavelength is unavailable. Please set Source Primary Wavelength."),NaN})();if(!Number.isFinite(a)||a<=0)return;let c=o.findIndex(d=>d["object type"]==="Stop"||d.object==="Stop");c===-1&&(console.warn("⚠️ Stop surface not found, using surface 1"),c=1);const i=document.getElementById("reference-focal-length");let l;if(i){const d=i.value.trim();if(d!==""&&d.toLowerCase()!=="auto"){const u=parseFloat(d);l=isFinite(u)?u:void 0}}const m=t(o,a,c,r,l);if(!m){console.error("❌ Afocal Seidel coefficients calculation failed"),alert("アフォーカル系収差係数の計算に失敗しました。");return}const p=document.getElementById("system-data");if(p){if(p.value=n(m),typeof O.renderBlockContributionSummaryFromSeidel=="function")try{O.renderBlockContributionSummaryFromSeidel(m,o)}catch(d){console.warn("⚠️ Block contribution summary render failed (afocal):",d)}}else console.error("❌ System Data textarea not found")}catch(t){console.error("❌ アフォーカル系Seidel係数計算ボタンエラー:",t),alert(`エラーが発生しました: ${t.message}`)}})}function Gl(){const e=document.getElementById("coord-transform-btn");e&&e.addEventListener("click",()=>{try{typeof O.displayCoordinateTransformMatrix=="function"?O.displayCoordinateTransformMatrix():console.error("❌ displayCoordinateTransformMatrix関数が見つかりません")}catch(t){console.error("❌ 座標変換ボタンエラー:",t)}})}function Vl(){const e=document.getElementById("show-spot-diagram-btn");e&&e.addEventListener("click",()=>{typeof O.showSpotDiagram=="function"&&O.showSpotDiagram()})}function ql(){const e=document.getElementById("show-longitudinal-aberration-diagram-btn");e&&e.addEventListener("click",()=>{typeof O.showLongitudinalAberration=="function"&&O.showLongitudinalAberration()})}function Zl(){const e=document.getElementById("show-transverse-aberration-diagram-btn");e&&e.addEventListener("click",()=>{typeof O.showTransverseAberration=="function"&&O.showTransverseAberration()})}function Yl(){const e=document.getElementById("show-magnification-chromatic-aberration-btn");e&&e.addEventListener("click",()=>{if(typeof O.showMagnificationChromaticAberrationDiagram!="function")return;const t=document.getElementById("mca-progress-wrapper"),n=document.getElementById("mca-progressbar"),o=n instanceof HTMLProgressElement?n:null,r=document.getElementById("mca-progress-text"),s=(c,i)=>{try{t&&(t.style.display="block"),o&&Number.isFinite(c)&&(o.value=Math.max(0,Math.min(100,c))),r&&typeof i=="string"&&(r.textContent=i)}catch{}};s(0,"Starting...");const a=c=>{try{const i=Number(c?.percent),l=c?.message||c?.phase||"Working...";Number.isFinite(i)?s(i,l):s(void 0,l)}catch{}};O.showMagnificationChromaticAberrationDiagram({onProgress:a})})}function Xl(){const e=document.getElementById("show-distortion-diagram-btn");e&&(e.__cooptDistortionBound||(e.__cooptDistortionBound=!0,e.addEventListener("click",()=>{typeof O.showDistortion=="function"&&O.showDistortion()})))}function Kl(){const e=document.getElementById("show-integrated-aberration-btn");e&&e.addEventListener("click",()=>{typeof O.showIntegratedAberration=="function"&&O.showIntegratedAberration()})}function Jl(){const e=document.getElementById("show-astigmatism-diagram-btn");e&&e.addEventListener("click",()=>{typeof O.showAstigmatism=="function"&&O.showAstigmatism()})}function Ql(){const e=document.getElementById("psf-log-scale"),t=document.getElementById("psf-contours"),n=document.getElementById("psf-characteristics");e?.addEventListener("change",()=>{typeof O.updatePSFDisplay=="function"&&O.updatePSFDisplay()}),t?.addEventListener("change",()=>{typeof O.updatePSFDisplay=="function"&&O.updatePSFDisplay()}),n?.addEventListener("change",()=>{typeof O.updatePSFDisplay=="function"&&O.updatePSFDisplay()})}function eu(){const e=[{id:"psf-2d-btn",mode:"2d"},{id:"psf-3d-btn",mode:"3d"},{id:"psf-profile-btn",mode:"profile"},{id:"psf-energy-btn",mode:"energy"},{id:"psf-wavefront-btn",mode:"wavefront"}];e.forEach(({id:t,mode:n})=>{const o=document.getElementById(t);o&&o.addEventListener("click",()=>{e.forEach(({id:r})=>{document.getElementById(r)?.classList.remove("active")}),o.classList.add("active"),typeof O.switchPSFDisplayMode=="function"&&O.switchPSFDisplayMode(n)})})}function Vi(e,t){return{id:e,name:t,schemaVersion:pn,blocks:[{blockId:"ObjectSurface-1",blockType:"ObjectSurface",role:null,constraints:{},parameters:{objectDistanceMode:"INF"},variables:{},metadata:{source:"default"}},{blockId:"Stop-1",blockType:"Stop",role:null,constraints:{},parameters:{semiDiameter:Bi},variables:{},metadata:{source:"default"}},{blockId:"ImageSurface-1",blockType:"ImageSurface",role:null,constraints:{},parameters:{semidiaMode:"Manual"},variables:{},metadata:{source:"default"}}],source:[{id:1,wavelength:.4358343,weight:1,primary:"",angle:0},{id:2,wavelength:.5875618,weight:1,primary:"Primary Wavelength",angle:0},{id:3,wavelength:.6562725,weight:1,primary:"",angle:0}],object:[{id:1,xHeightAngle:0,yHeightAngle:0,position:"Angle",angle:0},{id:2,xHeightAngle:0,yHeightAngle:17.05,position:"Angle",angle:0}],opticalSystem:[],meritFunction:[],metadata:{created:new Date().toISOString(),modified:new Date().toISOString(),optimizationTarget:null,locked:!1}}}const tu={configurations:[Vi(1,"Config 1")],activeConfigId:1,optimizationRules:{}};function Ie(){try{return ot()}catch(e){return console.warn("⚠️ [Configuration] Load failed; using default system config:",e),tu}}function Te(e){try{No(e)}catch(t){console.warn("⚠️ [Configuration] Save failed:",t)}}function un(){const e=Ie(),t=e.configurations.find(n=>n.id===e.activeConfigId);return t||(console.warn("⚠️ [Configuration] Active config not found, using first"),e.configurations[0])}function nu(){return Ie().activeConfigId}function ou(e){const t=Ie(),n=t.configurations.find(o=>o.id===e);return n?(t.activeConfigId=e,Te(t),console.log(`✅ [Configuration] Active config changed to: ${n.name}`),!0):(console.error("❌ [Configuration] Config not found:",e),!1)}function ru(){console.log("🔵 [Configuration] Saving current table data to active configuration...");const e=Ie(),t=e.configurations.find(n=>n.id===e.activeConfigId);if(!t){console.error("❌ [Configuration] Active config not found");return}try{const n=O.tableSource?O.tableSource.getData():[];Ln(n)}catch{}t.object=O.tableObject?O.tableObject.getData():[],t.opticalSystem=O.tableOpticalSystem?O.tableOpticalSystem.getData():[],t.meritFunction=O.meritFunctionEditor?O.meritFunctionEditor.getData():[],t.metadata.modified=new Date().toISOString(),t.metadata.designer||(t.metadata.designer={type:"human",name:"user",confidence:null}),Te(e),console.log(`✅ [Configuration] Saved to: ${t.name}`)}function En(){const e=un();if(!e){console.error("❌ [Configuration] No active config found");return}try{const t=Cs()!==null,n=Array.isArray(e.source)?e.source:null;!t&&n&&n.length>0&&Ln(n)}catch{}e.object&&bo(e.object),e.opticalSystem&&qt(e.opticalSystem),e.meritFunction&&Kr(e.meritFunction)}function iu(e){const t=Ie(),o=Math.max(...t.configurations.map(a=>a.id),0)+1,r=Vi(o,e),s=un();return s&&(r.object=JSON.parse(JSON.stringify(s.object)),r.opticalSystem=JSON.parse(JSON.stringify(s.opticalSystem)),r.meritFunction=JSON.parse(JSON.stringify(s.meritFunction))),t.configurations.push(r),Te(t),console.log(`✅ [Configuration] Added new configuration: ${e} (ID: ${o})`),o}function su(e){const t=Ie();if(t.configurations.length<=1)return console.warn("⚠️ [Configuration] Cannot delete last configuration"),!1;const n=t.configurations.findIndex(r=>r.id===e);if(n===-1)return console.error("❌ [Configuration] Config not found:",e),!1;const o=t.configurations[n].name;return t.configurations.splice(n,1),t.activeConfigId===e&&(t.activeConfigId=t.configurations[0].id,console.log(`🔄 [Configuration] Active config changed to: ${t.configurations[0].name}`)),Te(t),console.log(`✅ [Configuration] Deleted configuration: ${o}`),!0}function au(e){const t=Ie(),n=t.configurations.find(a=>a.id===e);if(!n)return console.error("❌ [Configuration] Config not found:",e),null;const r=Math.max(...t.configurations.map(a=>a.id),0)+1,s=JSON.parse(JSON.stringify(n));return s.id=r,s.name=`${n.name} (Copy)`,s.metadata.created=new Date().toISOString(),s.metadata.modified=new Date().toISOString(),t.configurations.push(s),Te(t),console.log(`✅ [Configuration] Duplicated configuration: ${s.name} (ID: ${r})`),r}function cu(e,t){const n=Ie(),o=n.configurations.find(s=>s.id===e);if(!o)return console.error("❌ [Configuration] Config not found:",e),!1;const r=o.name;return o.name=t,o.metadata.modified=new Date().toISOString(),Te(n),console.log(`✅ [Configuration] Renamed: ${r} → ${t}`),!0}function lu(){const e=Ie();return e.configurations.map(t=>({id:t.id,name:t.name,active:t.id===e.activeConfigId,created:t.metadata.created,modified:t.metadata.modified,locked:t.metadata.locked}))}if(typeof window<"u"){const e=O.ConfigurationManager,t=e&&typeof e=="object"?e:{};O.ConfigurationManager={...t,loadSystemConfigurations:t.loadSystemConfigurations||Ie,saveSystemConfigurations:t.saveSystemConfigurations||Te,getActiveConfiguration:t.getActiveConfiguration||un,getActiveConfigId:t.getActiveConfigId||nu,setActiveConfiguration:t.setActiveConfiguration||ou,saveCurrentToActiveConfiguration:t.saveCurrentToActiveConfiguration||ru,loadActiveConfigurationToTables:t.loadActiveConfigurationToTables||En,addConfiguration:t.addConfiguration||iu,deleteConfiguration:t.deleteConfiguration||su,duplicateConfiguration:t.duplicateConfiguration||au,renameConfiguration:t.renameConfiguration||cu,getConfigurationList:t.getConfigurationList||lu}}let Rt=null,uu=[];function Cr(e){if(!e||typeof e!="object")return!1;const t=e?.optimize?.mode;return t==="V"||t===!0}function Ir(e){try{const t=String(e?.optimize?.scope??"").trim();if(t==="global"||t==="shared")return"global";if(t==="perConfig"||t==="local"||t==="per-config")return"perConfig"}catch{}return"perConfig"}function Fr(e,t,n){try{const o=Ie();if(!o||!Array.isArray(o.configurations))return;const r=o.activeConfigId,s=o.configurations.findIndex(i=>i&&i.id===r);if(s<0)return;const a=o.configurations[s];if(!a||!Array.isArray(a.blocks))return;const c=a.blocks.find(i=>i&&String(i.blockId??"")===String(e));if(!c)return;(!c.variables||typeof c.variables!="object")&&(c.variables={}),(!c.variables[t]||typeof c.variables[t]!="object")&&(c.variables[t]={value:c.parameters?.[t]??""}),(!c.variables[t].optimize||typeof c.variables[t].optimize!="object")&&(c.variables[t].optimize={}),c.variables[t].optimize.scope=n==="global"?"global":"perConfig";try{Te(o)}catch{}}catch{}}function Sn(e,t,n,o="perConfig"){try{const r=Ie();if(!r||!Array.isArray(r.configurations))return;const s=[],a=r.activeConfigId,c=o==="global"?r.configurations||[]:[r.configurations.find(l=>l&&l.id===a)||r.configurations[0]];let i=null;if(n&&o==="global")try{const l=r.configurations.find(u=>u&&u.id===a),m=l&&Array.isArray(l.blocks)?l.blocks.find(u=>u&&String(u.blockId??"")===String(e)):null,p=m?.parameters?.[t]??m?.variables?.[t]?.value,d=typeof p=="number"?p:Number(String(p??"").trim());Number.isFinite(d)&&(i=d)}catch{}for(const l of c){if(!l||!Array.isArray(l.blocks)){s.push({configId:String(l?.id??"(none)"),configName:l?.name});continue}const m=l.blocks.find(p=>p&&String(p.blockId??"")===String(e));if(!m){s.push({configId:String(l?.id??"(none)"),configName:l?.name});continue}if((!m.variables||typeof m.variables!="object")&&(m.variables={}),(!m.variables[t]||typeof m.variables[t]!="object")&&(m.variables[t]={value:m.parameters?.[t]??""}),(!m.variables[t].optimize||typeof m.variables[t].optimize!="object")&&(m.variables[t].optimize={}),m.variables[t].optimize.mode=n?"V":"F",m.variables[t].optimize.scope=o==="global"?"global":"perConfig",i!==null&&o==="global")try{(!m.parameters||typeof m.parameters!="object")&&(m.parameters={}),m.parameters[t]=i,m.variables[t]&&typeof m.variables[t]=="object"&&Object.prototype.hasOwnProperty.call(m.variables[t],"value")&&(m.variables[t].value=i)}catch{}}uu=s.length>0?[{blockId:String(e),key:String(t),scope:String(o),missing:s}]:[];try{Te(r)}catch{}}catch{}}function Mr(e){try{const t=Ie();if(!t||!Array.isArray(t.configurations))return{ok:!1,changedCount:0,reason:"no system configurations"};const n=t.activeConfigId,o=t.configurations.find(i=>i&&i.id===n)||null;if(!o||!Array.isArray(o.blocks))return{ok:!1,changedCount:0,reason:"active configuration or blocks not found"};const r=JSON.parse(JSON.stringify(o.blocks)),s=e?"V":"F";let a=0;for(const i of o.blocks){if(!i||typeof i!="object")continue;(!i.variables||typeof i.variables!="object")&&(i.variables={});const l=i.parameters&&typeof i.parameters=="object"?i.parameters:null,m=l?Object.keys(l):[];for(const u of m)(!i.variables[u]||typeof i.variables[u]!="object")&&(i.variables[u]={value:l?l[u]:""}),Object.prototype.hasOwnProperty.call(i.variables[u],"value")===!1&&(i.variables[u].value=l?l[u]:""),(!i.variables[u].optimize||typeof i.variables[u].optimize!="object")&&(i.variables[u].optimize={}),String(i.variables[u].optimize.mode??"").trim()!==s&&a++,i.variables[u].optimize.mode=s,i.variables[u].optimize.scope||(i.variables[u].optimize.scope="perConfig");const p=i.aperture&&typeof i.aperture=="object"?i.aperture:null,d=p?Object.keys(p):[];for(const u of d)(!i.variables[u]||typeof i.variables[u]!="object")&&(i.variables[u]={value:p?p[u]:""}),Object.prototype.hasOwnProperty.call(i.variables[u],"value")===!1&&(i.variables[u].value=p?p[u]:""),(!i.variables[u].optimize||typeof i.variables[u].optimize!="object")&&(i.variables[u].optimize={}),String(i.variables[u].optimize.mode??"").trim()!==s&&a++,i.variables[u].optimize.mode=s,i.variables[u].optimize.scope||(i.variables[u].optimize.scope="perConfig")}if(a<=0)return{ok:!0,changedCount:0};const c=JSON.parse(JSON.stringify(o.blocks));try{if(O.undoHistory&&O.SetDesignIntentOptimizeBulkCommand&&!O.undoHistory.isExecuting){const i=new O.SetDesignIntentOptimizeBulkCommand(String(o.id??n??""),r,c,e);O.undoHistory.record(i)}}catch{}Te(t);try{Je()}catch{}try{typeof O.loadActiveConfigurationToTables=="function"&&O.loadActiveConfigurationToTables()}catch{}try{O.popup3DWindow&&!O.popup3DWindow.closed&&O.popup3DWindow.postMessage({action:"request-redraw"},"*")}catch{}return{ok:!0,changedCount:a}}catch(t){return{ok:!1,changedCount:0,reason:String(t?.message||t)}}}function pu(e){const t=e&&typeof e=="object"?e:null;if(!t)return"";const n=s=>{const a=t.parameters&&typeof t.parameters=="object"?t.parameters:null,c=a?a[s]:void 0;if(c!=null&&String(c).trim()!=="")return c;const i=t.variables&&typeof t.variables=="object"?t.variables:null,l=i&&i[s]&&typeof i[s]=="object"?i[s].value:void 0;return l!=null&&String(l).trim()!==""?l:""},o=String(t.blockType??""),r=s=>String(s??"").trim().toLowerCase().replace(/\s+/g,"").includes("aspheric");if(o==="Lens"||o==="PositiveLens"){const s=n("frontRadius"),a=n("backRadius"),c=n("centerThickness"),i=n("material"),l=n("frontSurfType"),m=n("backSurfType"),p=[];return String(s)!==""&&p.push(`R1=${String(s)}`),String(a)!==""&&p.push(`R2=${String(a)}`),String(c)!==""&&p.push(`CT=${String(c)}`),String(i)!==""&&p.push(`G=${String(i)}`),r(l)&&p.push("Front=Asphere"),r(m)&&p.push("Back=Asphere"),p.join(" ")}if(o==="Doublet"){const s=n("radius1"),a=n("radius2"),c=n("radius3"),i=n("thickness1"),l=n("thickness2"),m=n("material1"),p=n("abbe1")||n("vd1"),d=n("material2"),u=n("abbe2")||n("vd2"),h=n("surf1SurfType"),g=n("surf2SurfType"),y=n("surf3SurfType"),A=[];return String(s)!==""&&A.push(`R1=${String(s)}`),String(a)!==""&&A.push(`R2=${String(a)}`),String(c)!==""&&A.push(`R3=${String(c)}`),String(i)!==""&&A.push(`T1=${String(i)}`),String(l)!==""&&A.push(`T2=${String(l)}`),String(m)!==""&&A.push(`G1=${String(m)}`),String(p)!==""&&A.push(`V1=${String(p)}`),String(d)!==""&&A.push(`G2=${String(d)}`),String(u)!==""&&A.push(`V2=${String(u)}`),r(h)&&A.push("S1=Asphere"),r(g)&&A.push("S2=Asphere"),r(y)&&A.push("S3=Asphere"),A.join(" ")}if(o==="SingleSurface"||o==="Mirror"){const s=n("radius"),a=n("thickness"),c=n("material"),i=n("surfType"),l=n("apertureShape"),m=n("apertureWidth"),p=n("apertureHeight"),d=[];return String(s)!==""&&d.push(`R=${String(s)}`),String(a)!==""&&d.push(`T=${String(a)}`),String(c)!==""&&d.push(`M=${String(c)}`),r(i)&&d.push("Asphere"),String(l)!==""&&String(l)!=="Circular"&&d.push(`Aperture=${String(l)}`),String(m)!==""&&d.push(`AW=${String(m)}`),String(p)!==""&&d.push(`AH=${String(p)}`),d.join(" ")}if(o==="ImageSurface"){const s=n("radius"),a=n("thickness"),c=n("surfType"),i=n("apertureShape"),l=n("apertureWidth"),m=n("apertureHeight"),p=[];return String(s)!==""&&p.push(`R=${String(s)}`),String(a)!==""&&p.push(`T=${String(a)}`),r(c)&&p.push("Asphere"),String(i)!==""&&String(i)!=="Circular"&&p.push(`Aperture=${String(i)}`),String(l)!==""&&p.push(`AW=${String(l)}`),String(m)!==""&&p.push(`AH=${String(m)}`),p.join(" ")}if(o==="Gap"||o==="AirGap"){const s=n("thickness"),a=n("material"),c=[];return String(s)!==""&&c.push(`T=${String(s)}`),String(a)!==""&&String(a).trim().toUpperCase()!=="AIR"&&c.push(`M=${String(a)}`),c.join(" ")}if(o==="Stop"){const s=n("semiDiameter");return String(s)!==""?`SD=${String(s)}`:""}return""}function du(e,t,n){if(!e||typeof e!="object")return;const o=String(t||"").split(".").filter(Boolean);if(o.length===0)return;let r=e;for(let a=0;a<o.length-1;a++){const c=o[a];(!r[c]||typeof r[c]!="object")&&(r[c]={}),r=r[c]}const s=o[o.length-1];r[s]=n}function xn(e,t){const n=String(e??"").trim();return n===""?"":/^[-+]?((\d+\.\d*)|(\d*\.\d+)|(\d+))(e[-+]?\d+)?$/i.test(n)?Number(n):typeof t=="boolean"?n.toLowerCase()==="true":n}function Ge(e,t,n,o){const r=Ie(),s=r?.configurations?.find(i=>i.id===r?.activeConfigId)||r?.configurations?.[0];if(!s)return;const a=Array.isArray(s.blocks)?s.blocks:[],c=a.find(i=>i&&String(i.blockId??"")===String(e));if(c){if(n!==o)try{if(O.undoHistory&&O.SetBlockParameterCommand&&!O.undoHistory.isExecuting){const i=new O.SetBlockParameterCommand(s.name,String(e),String(t),n,o);O.undoHistory.record(i)}}catch{}du(c,t,o);try{const i=$e(a);if(i&&Array.isArray(i.rows)){s.opticalSystem=i.rows;try{qt(i.rows)}catch{}try{typeof O.saveLensTableData=="function"&&O.saveLensTableData(i.rows)}catch{}try{const l=O.tableOpticalSystem||O.opticalSystemTabulator;l&&typeof l.replaceData=="function"?l.replaceData(i.rows):l&&typeof l.setData=="function"&&l.setData(i.rows)}catch{}}}catch{}try{s.metadata&&(s.metadata.modified=new Date().toISOString())}catch{}try{Te(r)}catch{}try{Je()}catch{}try{typeof O.loadActiveConfigurationToTables=="function"&&O.loadActiveConfigurationToTables()}catch{}try{const i=O.popup3DWindow;i&&!i.closed&&typeof i.postMessage=="function"&&i.postMessage({action:"request-redraw"},"*")}catch{}}}function kr(e,t,n=null,o=null){const r=document.getElementById("block-inspector");if(!r)return;r.innerHTML="";try{if(Array.isArray(O.__blocks_lastScopeErrors)&&O.__blocks_lastScopeErrors.length>0){const l=O.__blocks_lastScopeErrors[0],m=Array.isArray(l?.missing)?l.missing:[],p=m.slice(0,6).map(u=>u?.configName?`${String(u.configName)}(${String(u.configId)})`:String(u?.configId??"")).filter(Boolean),d=document.createElement("div");d.style.padding="8px 10px",d.style.margin="6px 0 10px 0",d.style.border="1px solid #f2c2c2",d.style.background="#fff5f5",d.style.color="#8a1f1f",d.style.borderRadius="6px",d.style.fontSize="12px",d.textContent=`ERROR: Cannot apply "Shared (all configs)" because this Block is missing in some configurations: ${String(l?.blockId??"")}.${String(l?.key??"")} / missing in ${m.length} config(s): ${p.join(", ")}${m.length>p.length?", ...":""}`,r.appendChild(d)}}catch{}const s=Array.isArray(e)?e:[];if(s.length===0){const l=document.createElement("div");l.style.padding="8px",l.style.fontSize="13px",l.style.color="#666",l.textContent="No blocks (or no provenance).",r.appendChild(l);return}const a=new Map;try{if(Array.isArray(o)&&o.length>0&&typeof O.expandBlocksToOpticalSystemRows=="function"){const l=O.expandBlocksToOpticalSystemRows(o),m=l&&Array.isArray(l.rows)?l.rows:[];let p=0;for(let d=0;d<m.length;d++){const u=m[d],h=String(u?._blockId??"").trim();if(!h)continue;const g=String(u?._blockType??"").trim();if(g==="Gap"||g==="CoordTrans"||g==="ObjectSurface"||g==="ObjectPlane"||g==="Object")continue;p+=1;const y=a.get(h);y?(p<y.min&&(y.min=p),p>y.max&&(y.max=p)):a.set(h,{min:p,max:p})}}}catch{}if(a.size===0)try{let l=0;for(const m of s){const p=String(m?.blockId??"").trim();if(!p)continue;const d=String(m?.blockType??"").trim();if(d==="ObjectSurface"||d==="ObjectPlane"||d==="Object")continue;const u=Number(m?.surfaceCount??0);if(!Number.isFinite(u)||u<=0)continue;const h=l+1,g=l+u;l=g,a.set(p,{min:h,max:g})}}catch{}const c=(l,m)=>{const p=String(l??"").trim(),d=String(m??"").trim();if(p==="ObjectSurface"||p==="ObjectPlane"||p==="ImageSurface")return p==="ObjectPlane"?"ObjectSurface":p;if(/^ObjectPlane-(\d+)$/i.exec(d))return"ObjectSurface";const h=/^(ObjectSurface|ImageSurface)-(\d+)$/i.exec(d);return h?h[1]:d||"(none)"},i=new Map;try{const l=new Map,m=Array.isArray(o)?o:[];for(const p of m){if(!p||typeof p!="object")continue;const d=String(p.blockId??"").trim();if(!d)continue;const u=String(p.blockType??"").trim();if(!u)continue;if(u==="ObjectSurface"||u==="ObjectPlane"||u==="ImageSurface"){const y=u==="ObjectPlane"?"ObjectSurface":u;i.set(d,y);continue}const h=u==="PositiveLens"?"Lens":u,g=(l.get(h)||0)+1;l.set(h,g),i.set(d,`${h}-${g}`)}}catch{}for(const l of s){const m=String(l.blockId??"").trim(),p=document.createElement("div");p.className="block-inspector-row",m&&Rt===m&&p.classList.add("selected");const d=document.createElement("div");d.className="block-inspector-col-id";{const _=String(l.blockId??"(none)"),S=i.get(_)||c(l.blockType,_),N=String(l.blockType??"").trim();if(N==="ObjectSurface"||N==="ObjectPlane")d.textContent=`${S} → Surf 0`;else{const f=a.get(String(l.blockId??"").trim());if(f&&Number.isFinite(f.min)&&Number.isFinite(f.max)){const w=f.min===f.max?`Surf ${f.min}`:`Surf ${f.min}–${f.max}`;d.textContent=`${S} → ${w}`}else d.textContent=S}}const u=document.createElement("div");u.className="block-inspector-col-type";const h=String(l.blockType??"").trim()==="ObjectPlane"?"ObjectSurface":String(l.blockType??"(none)");u.textContent=h;const g=document.createElement("div");g.className="block-inspector-col-params",g.textContent=String(l.preview??"");const y=document.createElement("div");y.className="block-inspector-col-count";const A=Number(l.surfaceCount??0);y.textContent=`→ ${Number.isFinite(A)?A:0} surfaces`,p.appendChild(d),p.appendChild(u),p.appendChild(g),p.appendChild(y),p.onclick=()=>{if(m){Rt=Rt===m?null:m;try{Je()}catch{}}},r.appendChild(p);const x=n&&typeof n.get=="function"?n.get(m):null;if(x&&Rt===m){const _=document.createElement("div");_.style.padding="6px 8px 10px 8px";const S=document.body.classList.contains("dark-mode");_.style.borderTop=S?"1px solid #333":"1px solid #eee",_.style.fontSize="12px",_.style.color=S?"#ffffff":"#333",_.dataset.blockId=String(m),_.setAttribute("data-block-id",String(m));const N=x.parameters&&typeof x.parameters=="object"?x.parameters:{},f=x.variables&&typeof x.variables=="object"?x.variables:{},w=G=>G.sort((F,W)=>{const I=F.toLowerCase(),E=W.toLowerCase();if(b==="CoordTrans"){const te=V=>{const K=V.toLowerCase();return K==="decenterx"?0:K==="decentery"?1:K==="decenterz"?2:K==="tiltx"?3:K==="tilty"?4:K==="tiltz"?5:K==="order"?6:K==="coordreturn"?7:K==="tosurf"?8:100},q=te(F),z=te(W);if(q!==100||z!==100)return q-z}const P=te=>{switch(te){case"material1":return 0;case"abbe1":case"vd1":return 1;case"material2":return 2;case"abbe2":case"vd2":return 3;default:return 100}},Z=P(F),R=P(W);if(Z!==R)return Z-R;if(F==="material1")return-1;if(W==="material1")return 1;if(F==="material2"&&W!=="material1")return-1;if(W==="material2"&&F!=="material1")return 1;if(I.includes("material")&&!E.includes("material"))return-1;if(E.includes("material")&&!I.includes("material"))return 1;if(F==="abbe"||F==="vd")return-1;if(W==="abbe"||W==="vd")return 1;if(I.startsWith("front")&&!E.startsWith("front"))return-1;if(!I.startsWith("front")&&E.startsWith("front"))return 1;if(I.startsWith("front")&&E.startsWith("front")){const te=I.includes("surf"),q=E.includes("surf"),z=I.includes("radius"),V=E.includes("radius"),K=I.includes("conic"),oe=E.includes("conic"),re=I.includes("coef"),fe=E.includes("coef");if(te&&!q)return-1;if(!te&&q)return 1;if(z&&!V)return-1;if(!z&&V)return 1;if(K&&!oe)return-1;if(!K&&oe)return 1;if(re&&fe){const se=F.match(/\d+/),pe=W.match(/\d+/);if(se&&pe)return parseInt(se[0])-parseInt(pe[0])}}if(I.startsWith("back")&&!E.startsWith("back"))return-1;if(!I.startsWith("back")&&E.startsWith("back"))return 1;if(I.startsWith("back")&&E.startsWith("back")){const te=I.includes("surf"),q=E.includes("surf"),z=I.includes("radius"),V=E.includes("radius"),K=I.includes("conic"),oe=E.includes("conic"),re=I.includes("coef"),fe=E.includes("coef");if(te&&!q)return-1;if(!te&&q)return 1;if(z&&!V)return-1;if(!z&&V)return 1;if(K&&!oe)return-1;if(!K&&oe)return 1;if(re&&fe){const se=F.match(/\d+/),pe=W.match(/\d+/);if(se&&pe)return parseInt(se[0])-parseInt(pe[0])}}if(I.includes("radius")&&!I.startsWith("front")&&!I.startsWith("back")&&!E.includes("radius"))return-1;if(E.includes("radius")&&!E.startsWith("front")&&!E.startsWith("back")&&!I.includes("radius"))return 1;if(I.includes("conic")&&!I.startsWith("front")&&!I.startsWith("back")&&!E.includes("conic"))return-1;if(E.includes("conic")&&!E.startsWith("front")&&!E.startsWith("back")&&!I.includes("conic"))return 1;if(I.includes("thickness")&&!E.includes("thickness"))return-1;if(!I.includes("thickness")&&E.includes("thickness"))return 1;if(F==="apertureShape"&&W!=="apertureShape")return-1;if(W==="apertureShape"&&F!=="apertureShape")return 1;if(F==="apertureWidth"&&W!=="apertureWidth"&&W!=="apertureShape")return-1;if(W==="apertureWidth"&&F!=="apertureWidth"&&F!=="apertureShape")return 1;if(F==="apertureHeight"&&W!=="apertureHeight"&&W!=="apertureShape"&&W!=="apertureWidth")return-1;if(W==="apertureHeight"&&F!=="apertureHeight"&&F!=="apertureShape"&&F!=="apertureWidth")return 1;if(I.includes("semidia")&&!E.includes("semidia"))return-1;if(!I.includes("semidia")&&E.includes("semidia"))return 1;const k=I.includes("coef"),$=E.includes("coef");if(k&&$){const te=F.match(/\d+/),q=W.match(/\d+/);if(te&&q)return parseInt(te[0])-parseInt(q[0])}return k&&!$?1:!k&&$?-1:F.localeCompare(W)}),b=String(x.blockType||x.type||"unknown"),v=Object.keys(N||{}).filter(G=>{const F=G.toLowerCase();return!(F==="chiefrayshiftx"||F==="chiefrayshifty"||F==="chiefrayshiftz")});if((b==="Gap"||b==="AirGap")&&!v.includes("material")&&v.push("material"),(b==="Gap"||b==="AirGap")&&!v.includes("thicknessMode")&&v.push("thicknessMode"),b==="ImageSurface"){v.includes("semidiaMode")||v.push("semidiaMode"),v.includes("apertureShape")||v.push("apertureShape"),v.includes("apertureWidth")||v.push("apertureWidth"),v.includes("apertureHeight")||v.push("apertureHeight"),v.includes("radius")||v.push("radius"),v.includes("thickness")||v.push("thickness"),v.includes("surfType")||v.push("surfType"),v.includes("conic")||v.push("conic");for(let G=1;G<=10;G++){const F=`coef${G}`;v.includes(F)||v.push(F)}}if(b==="Lens"||b==="PositiveLens"||b==="SingleSurface"||b==="Mirror"){v.includes("frontSurfType")||v.push("frontSurfType"),v.includes("backSurfType")||v.push("backSurfType");for(let G=1;G<=10;G++){const F=`frontCoef${G}`,W=`backCoef${G}`;v.includes(F)||v.push(F),v.includes(W)||v.push(W)}}const M=w(v),C=Object.keys(f||{}).sort(),D=G=>String(G??"").trim().toLowerCase().replace(/\s+/g,""),B=G=>{const F=String(G).toLowerCase();return F.startsWith("frontcoef")?N.frontSurfType:F.startsWith("backcoef")?N.backSurfType:F.startsWith("surf1coef")?N.surf1SurfType:F.startsWith("surf2coef")?N.surf2SurfType:F.startsWith("surf3coef")?N.surf3SurfType:N.surfType},j=G=>{const F=String(G).match(/coef(\d+)/i);if(!F)return null;const W=parseInt(F[1],10);if(!Number.isFinite(W)||W<=0)return null;const I=B(G),E=D(I),P=E==="asphericeven"||E==="asphericaleven"||E==="aspheric-even"||E==="aspherical-even";if(!P&&!(E==="asphericodd"||E==="asphericalodd"||E==="aspheric-odd"||E==="aspherical-odd"))return null;const R=P?2*W+2:2*W+1,k=String(G).toLowerCase();let $="";return k.startsWith("frontcoef")?$="front ":k.startsWith("backcoef")?$="back ":k.startsWith("surf1coef")?$="surf1 ":k.startsWith("surf2coef")?$="surf2 ":k.startsWith("surf3coef")&&($="surf3 "),`${$}A${R}`.trim()},H=G=>{const F=document.createElement("div");return F.textContent=G,F.style.fontWeight="600",F.style.margin="8px 0 4px 0",F.style.fontSize="12px",F},ce=(G,F,W,I,E)=>{const P=document.createElement("div");P.style.display="flex",P.style.gap="8px",P.style.alignItems="center",P.style.marginBottom="6px";const Z=document.createElement("div"),R=j(G);Z.textContent=R||G,Z.style.fontSize="12px",Z.style.color=S?"#d1d5db":"#374151",Z.style.flex="0 0 140px";const k=G==="surfType"||G==="frontSurfType"||G==="backSurfType"||G==="surf1SurfType"||G==="surf2SurfType"||G==="surf3SurfType",$=G.toLowerCase().includes("material")||E==="material",te=(b==="Gap"||b==="AirGap")&&G==="thicknessMode",q=(b==="ObjectSurface"||b==="ObjectPlane")&&G==="objectDistanceMode",z=b==="ImageSurface"&&G==="semidiaMode",V=(b==="Mirror"||b==="SingleSurface"||b==="ImageSurface")&&G==="apertureShape",K=b==="CoordTrans"&&G==="coordReturn",oe=b==="CoordTrans"&&G==="order",re=b==="CoordTrans"&&G==="toSurf",se=!$&&!k&&!(G==="nd"||G==="vd"||G==="abbe")&&!te&&!q&&!z&&!V&&!K&&!oe&&!re&&!isNaN(parseFloat(String(F)));let pe;if(k){const L=document.createElement("select");L.style.fontSize="12px",L.style.padding="4px 6px",L.style.border=S?"1px solid #444":"1px solid #ddd",L.style.background=S?"#111827":"#fff",L.style.color=S?"#f9fafb":"#111827",L.style.borderRadius="4px",L.style.flex="1",L.style.cursor="pointer",L.style.minWidth="200px",L.style.height="28px",L.style.boxSizing="border-box";const X=["Spherical","Aspherical even","Aspherical odd","Toric"],Y=String(F||"Spherical");X.forEach(J=>{const le=document.createElement("option");le.value=J,le.textContent=J,J===Y&&(le.selected=!0),L.appendChild(le)}),L.addEventListener("change",()=>{const J=L.value;J!==F&&Ge(m,W,F,J)}),pe=L}else if(te){const L=document.createElement("select");L.style.fontSize="12px",L.style.padding="4px 6px",L.style.border=S?"1px solid #444":"1px solid #ddd",L.style.background=S?"#111827":"#fff",L.style.color=S?"#f9fafb":"#111827",L.style.borderRadius="4px",L.style.flex="1",L.style.cursor="pointer",L.style.minWidth="200px",L.style.height="28px",L.style.boxSizing="border-box";const X=String(F??"").trim().replace(/\s+/g,"").toUpperCase(),Y=X==="IMD"||X==="BFL"?X:"";[{value:"",label:"Manual"},{value:"IMD",label:"Image distance (IMD)"},{value:"BFL",label:"Back focal length (BFL)"}].forEach(({value:Q,label:ee})=>{const ie=document.createElement("option");ie.value=Q,ie.textContent=ee,Q===Y&&(ie.selected=!0),L.appendChild(ie)});const le=Q=>{if(!(Q!=="IMD"&&Q!=="BFL"))try{const ee=Array.isArray(o)&&o.length>0?o:(()=>{const xe=Ie(),Ne=xe?.configurations?.find(ye=>ye.id===xe?.activeConfigId)||xe?.configurations?.[0];return Array.isArray(Ne?.blocks)?Ne.blocks:[]})(),ie=$e(ee),be=ie&&Array.isArray(ie.rows)?ie.rows:[];if(be.length===0)return;const ae=(()=>{try{if(typeof O.getPrimaryWavelength=="function"){const xe=Number(O.getPrimaryWavelength());if(Number.isFinite(xe)&&xe>0)return xe}}catch{}return NaN})();if(!(Number.isFinite(ae)&&ae>0)){console.warn("⚠️ [DesignIntent] Primary wavelength is unavailable. thicknessMode auto-apply is skipped.");return}const he=On(be,ae),de=Q==="IMD"?he?.imageDistance:he?.backFocalLength,Ae=Number(de);if(Number.isFinite(Ae)){const xe=N?.thickness;Ge(m,"parameters.thickness",xe,Ae)}}catch(ee){console.warn("⚠️ [DesignIntent] Failed to apply thicknessMode:",ee)}};L.addEventListener("change",()=>{const Q=L.value;Q!==F&&(Ge(m,W,F,Q),le(Q))}),pe=L}else if(q){const L=document.createElement("select");L.style.fontSize="12px",L.style.padding="4px 6px",L.style.border=S?"1px solid #444":"1px solid #ddd",L.style.background=S?"#111827":"#fff",L.style.color=S?"#f9fafb":"#111827",L.style.borderRadius="4px",L.style.flex="1",L.style.cursor="pointer",L.style.minWidth="200px",L.style.height="28px",L.style.boxSizing="border-box";const X=String(F??"").trim(),J=vt(X)||X.toUpperCase()==="INFINITY"?"INF":"Finite";[{value:"INF",label:"Infinity"},{value:"Finite",label:"Finite"}].forEach(({value:Q,label:ee})=>{const ie=document.createElement("option");ie.value=Q,ie.textContent=ee,Q===J&&(ie.selected=!0),L.appendChild(ie)}),L.addEventListener("change",()=>{const Q=L.value;if(Q!==F&&(Ge(m,W,F,Q),Q==="Finite")){const ee=N?.objectDistance,ie=Number(ee);Number.isFinite(ie)||Ge(m,"parameters.objectDistance",ee,10)}}),pe=L}else if(z){const L=document.createElement("select");L.style.fontSize="12px",L.style.padding="4px 6px",L.style.border=S?"1px solid #444":"1px solid #ddd",L.style.background=S?"#111827":"#fff",L.style.color=S?"#f9fafb":"#111827",L.style.borderRadius="4px",L.style.flex="1",L.style.cursor="pointer",L.style.minWidth="200px",L.style.height="28px",L.style.boxSizing="border-box";const Y=String(F??"").trim().toLowerCase()==="auto"?"Auto":"Manual";["Manual","Auto"].forEach(le=>{const Q=document.createElement("option");Q.value=le,Q.textContent=le,le===Y&&(Q.selected=!0),L.appendChild(Q)}),L.addEventListener("change",()=>{const le=L.value;if(le!==F){Ge(m,W,F,le);const Q=N?N.optimizeSemiDia:void 0,ee=le==="Auto"?"A":"";Q!==ee&&Ge(m,"parameters.optimizeSemiDia",Q,ee),le==="Auto"&&setTimeout(()=>{try{typeof O.calculateImageSemiDiaFromChiefRays=="function"&&Promise.resolve(O.calculateImageSemiDiaFromChiefRays()).then(()=>{try{Je()}catch{}}).catch(ie=>{console.warn("⚠️ [DesignIntent] semidiaMode Auto recalculation failed:",ie)})}catch(ie){console.warn("⚠️ [DesignIntent] semidiaMode Auto trigger failed:",ie)}},0)}}),pe=L}else if(K){const L=document.createElement("select");L.style.fontSize="12px",L.style.padding="4px 6px",L.style.border=S?"1px solid #444":"1px solid #ddd",L.style.background=S?"#111827":"#fff",L.style.color=S?"#f9fafb":"#111827",L.style.borderRadius="4px",L.style.flex="1",L.style.cursor="pointer",L.style.minWidth="200px",L.style.height="28px",L.style.boxSizing="border-box";const X=String(F??"").trim().toLowerCase(),Y=X==="xyz"||X==="none"?X:"none";[{value:"xyz",label:"On"},{value:"none",label:"Off"}].forEach(({value:le,label:Q})=>{const ee=document.createElement("option");ee.value=le,ee.textContent=Q,le===Y&&(ee.selected=!0),L.appendChild(ee)}),L.addEventListener("change",async()=>{const le=L.value;if(le!==F&&(Ge(m,W,F,le),le==="xyz")){const Q=Number(N?.order??0);Q!==1&&Ge(m,"parameters.order",Q,1);try{await Ar(m,_)}catch(ee){console.error("[CoordTrans] Auto calculation on ON failed:",ee)}}}),pe=L}else if(oe){const L=document.createElement("select");L.style.fontSize="12px",L.style.padding="4px 6px",L.style.border=S?"1px solid #444":"1px solid #ddd",L.style.background=S?"#111827":"#fff",L.style.color=S?"#f9fafb":"#111827",L.style.borderRadius="4px",L.style.flex="1",L.style.cursor="pointer",L.style.minWidth="200px",L.style.height="28px",L.style.boxSizing="border-box";const X=Number(F??1),Y=X===0||X===1?X:1;[{value:"0",label:"Tilt → Decenter"},{value:"1",label:"Decenter → Tilt"}].forEach(({value:le,label:Q})=>{const ee=document.createElement("option");ee.value=le,ee.textContent=Q,parseInt(le)===Y&&(ee.selected=!0),L.appendChild(ee)}),L.addEventListener("change",()=>{const le=parseInt(L.value),Q=Number(F??1);le!==Q&&Ge(m,W,Q,le)}),pe=L}else if(re){const L=document.createElement("select");L.style.fontSize="12px",L.style.padding="4px 6px",L.style.border=S?"1px solid #444":"1px solid #ddd",L.style.background=S?"#111827":"#fff",L.style.color=S?"#f9fafb":"#111827",L.style.borderRadius="4px",L.style.flex="1",L.style.cursor="pointer",L.style.minWidth="200px",L.style.height="28px",L.style.boxSizing="border-box";const X=[];try{if(Array.isArray(o)&&o.length>0&&typeof $e=="function"){const Q=$e(o),ee=Q&&Array.isArray(Q.rows)?Q.rows:[];let ie=0;const be=new Map;for(let ae=0;ae<ee.length;ae++){const he=ee[ae],de=String(he?._blockType??he?.type??"").trim();if(de==="Gap"||de==="AirGap"||de==="CoordTrans"||de==="ObjectSurface"||de==="ObjectPlane"||de==="Object")continue;ie+=1;const Ae=String(he?._blockId??"").trim(),xe=(be.get(Ae)||0)+1;be.set(Ae,xe);const Ne=i.get(Ae)||Ae||"Surface";X.push({value:String(ie),label:`${ie}: ${Ne} S${xe}`})}}}catch{}const Y=Number(F),J=Number.isFinite(Y)&&X.some(Q=>Number(Q.value)===Y);!J&&Number.isFinite(Y)&&X.unshift({value:String(Y),label:`${Y}: (current)`}),X.length===0&&X.push({value:String(Number.isFinite(Y)?Y:1),label:"1: Surface 1"});const le=String(J||Number.isFinite(Y)?Y:Number(X[0].value));X.forEach(({value:Q,label:ee})=>{const ie=document.createElement("option");ie.value=Q,ie.textContent=ee,Q===le&&(ie.selected=!0),L.appendChild(ie)}),L.addEventListener("change",async()=>{const Q=Number(L.value),ee=Number(F);if(Number.isFinite(Q)&&Q!==ee&&(Ge(m,W,ee,Q),String(N?.coordReturn??"").trim().toLowerCase()==="xyz"))try{await Ar(m,_)}catch(be){console.error("[CoordTrans] Auto calculation on toSurf change failed:",be)}}),pe=L}else if(V){const L=document.createElement("select");L.style.fontSize="12px",L.style.padding="4px 6px",L.style.border=S?"1px solid #444":"1px solid #ddd",L.style.background=S?"#111827":"#fff",L.style.color=S?"#f9fafb":"#111827",L.style.borderRadius="4px",L.style.flex="1",L.style.cursor="pointer",L.style.minWidth="200px",L.style.height="28px",L.style.boxSizing="border-box";const X=String(F??"").trim(),J=(Q=>{const ee=Q.replace(/\s+/g,"").replace(/[_-]+/g,"").toLowerCase();return ee==="circle"||ee==="circular"?"Circular":ee==="square"||ee==="sq"?"Square":ee==="rect"||ee==="rectangle"||ee==="rectangular"?"Rectangular":"Circular"})(X)||"Circular";["Circular","Square"].forEach(Q=>{const ee=document.createElement("option");ee.value=Q,ee.textContent=Q,Q===J&&(ee.selected=!0),L.appendChild(ee)}),L.addEventListener("change",()=>{const Q=L.value;Q!==J&&Ge(m,W,F,Q)}),pe=L}else if(se){const L=document.createElement("div");L.className="param-input-with-slider",L.style.display="grid",L.style.gridTemplateColumns="120px 40px 40px 40px 140px 220px",L.style.columnGap="6px",L.style.alignItems="center",L.style.flex="1";const X=parseFloat(String(F)),Y=Number.isFinite(X);let J=xr(G,b,F),{min:le,max:Q,step:ee}=J,ie=!1,be=1;const ae=document.createElement("input");ae.type="text",ae.value=F==null?"":String(F),ae.style.fontSize="12px",ae.style.padding="4px 6px",ae.style.border=S?"1px solid #444":"1px solid #ddd",ae.style.background=S?"#111827":"#fff",ae.style.color=S?"#f9fafb":"#111827",ae.style.borderRadius="4px",ae.style.boxSizing="border-box",ae.style.height="28px";const he=document.createElement("button");he.type="button",he.className="scale-mode-btn",he.textContent="Lin",he.title="Toggle linear/logarithmic scale",he.style.fontSize="10px",he.style.padding="2px",he.style.boxSizing="border-box",he.style.height="28px";const de=document.createElement("button");de.type="button",de.className="magnitude-btn",de.textContent="×0.1",de.title="Decrease range by 10x",de.style.fontSize="9px",de.style.padding="2px",de.style.boxSizing="border-box",de.style.height="28px";const Ae=document.createElement("button");Ae.type="button",Ae.className="magnitude-btn",Ae.textContent="×10",Ae.title="Increase range by 10x",Ae.style.fontSize="9px",Ae.style.padding="2px",Ae.style.boxSizing="border-box",Ae.style.height="28px";const xe=document.createElement("div");xe.className="slider-range-display",xe.style.fontSize="9px",xe.style.color="#666",xe.style.whiteSpace="nowrap",xe.style.overflow="hidden",xe.style.textOverflow="ellipsis",xe.style.fontFamily="monospace",xe.title="Slider range (min ~ max)";const Ne=document.createElement("input");Ne.type="range",Ne.min="0",Ne.max="1",Ne.step="0.001",Ne.value=Y?String(eo(X,le,Q,ie)):"0.5";const ye=(ge,we)=>{const ue=Number(ge);if(!Number.isFinite(ue))return String(ge??"");const ve=Math.abs(ue);return ve>0&&ve<1e-6?ue.toExponential(2):ue.toFixed(we)},Se=()=>{const ge=Math.max(2,Math.min(6,-Math.floor(Math.log10(Math.abs(Q-le)/100))));xe.textContent=`[${ye(le,ge)} ~ ${ye(Q,ge)}]`};Se();const _e=()=>{const ge=xr(G,b,F),we=(ge.min+ge.max)/2,ve=(ge.max-ge.min)/2*be;le=we-ve,Q=we+ve,ee=ge.step*be,(G.includes("Thickness")||G.includes("hickness")||G.includes("semidia")||G.includes("aperture"))&&le<0&&(le=0,Q=we*2),Ce(),Se()},Ce=()=>{const ge=parseFloat(ae.value);Number.isFinite(ge)&&(Ne.value=String(eo(ge,le,Q,ie)))};he.addEventListener("click",ge=>{ge.stopPropagation(),ie=!ie,he.textContent=ie?"Log":"Lin",he.style.background=ie?"#007acc":"",he.style.color=ie?"white":"",Ce(),Se()}),de.addEventListener("click",ge=>{ge.stopPropagation(),be*=.1,_e()}),Ae.addEventListener("click",ge=>{ge.stopPropagation(),be*=10,_e()}),Ne.addEventListener("input",ge=>{ge.stopPropagation();const we=parseFloat(Ne.value),ue=Sr(we,le,Q,ie),ve=vr(ue,Q-le);ae.value=ue.toFixed(ve)}),Ne.addEventListener("change",ge=>{ge.stopPropagation();const we=parseFloat(Ne.value),ue=Sr(we,le,Q,ie),ve=vr(ue,Q-le),ke=ue.toFixed(ve);ae.value=ke,Ge(m,W,F,ke)});const Me=()=>{const ge=ae.value,we=parseFloat(ge);Number.isFinite(we)&&(Ne.value=String(eo(we,le,Q,ie))),Ge(m,W,F,ge)};ae.addEventListener("keydown",ge=>{ge.key==="Enter"&&(ge.preventDefault(),ge.stopPropagation(),Me())}),ae.addEventListener("blur",()=>{Me()}),L.appendChild(ae),L.appendChild(he),L.appendChild(de),L.appendChild(Ae),L.appendChild(xe),L.appendChild(Ne),pe=L}else if($){const L=document.createElement("div");L.style.display="flex",L.style.alignItems="center",L.style.gap="8px",L.style.flex="1",L.style.flexWrap="wrap";const X=document.createElement("input");X.type="text",X.value=F==null?"":String(F),X.style.fontSize="12px",X.style.padding="4px 6px",X.style.border=S?"1px solid #444":"1px solid #ddd",X.style.background=S?"#111827":"#fff",X.style.color=S?"#f9fafb":"#111827",X.style.flex="1",X.style.minWidth="200px",X.style.height="28px",X.style.boxSizing="border-box";const Y=document.createElement("button");Y.textContent="🔍",Y.title="Find Glass",Y.style.fontSize="14px",Y.style.padding="2px 8px",Y.style.border=S?"1px solid #444":"1px solid #ddd",Y.style.background=S?"#1f2937":"#f9fafb",Y.style.cursor="pointer",Y.style.borderRadius="4px",Y.style.height="28px",Y.style.boxSizing="border-box";const J=document.createElement("button");J.textContent="🗺️",J.title="Open Glass Map",J.style.fontSize="14px",J.style.padding="2px 8px",J.style.border=S?"1px solid #444":"1px solid #ddd",J.style.background=S?"#1f2937":"#f9fafb",J.style.cursor="pointer",J.style.borderRadius="4px",J.style.height="28px",J.style.boxSizing="border-box",J.onclick=ee=>{ee.preventDefault(),ee.stopPropagation(),typeof wr=="function"&&wr(ie=>{console.log("Region selected:",ie)},ie=>{if(ie&&ie.name){X.value=ie.name;const be=xn(ie.name,F);be!==F&&Ge(m,W,F,be);const ae=W.replace(/material/i,"abbe");if(ae!==W&&Number.isFinite(ie.vd))try{Ge(m,ae,void 0,String(ie.vd))}catch{}return!0}return!1})},Y.onclick=ee=>{ee.preventDefault(),ee.stopPropagation();const ie=X.value.trim(),be=ge=>{const ue=String(ge||"").trim().toLowerCase().match(/^material(\d+)$/);return ue&&ue[1]?`abbe${ue[1]}`:"abbe"},ae=ge=>{const ue=String(ge||"").trim().toLowerCase().match(/^material(\d+)$/);return ue&&ue[1]?`vd${ue[1]}`:"vd"},he=()=>{const ge=N&&typeof N=="object"?N:null;if(!ge)return null;const we=String(G||"").trim(),ue=be(we),ve=ae(we),ke=parseFloat(String(ge[ue]));if(Number.isFinite(ke)&&ke>0)return ke;const Fe=parseFloat(String(ge[ve]));return Number.isFinite(Fe)&&Fe>0?Fe:null},de=ge=>{const we=String(ge||"").trim();if(!we||!/^[+-]?(?:\d+\.?\d*|\.\d+)$/.test(we))return null;const ue=Number(we);return!Number.isFinite(ue)||ue<=0||ue>=4?null:ue};let Ae=[];const xe=de(ie);if(xe!==null)try{const ge=he();if(ge===null){alert("Abbe (Vd) value is required for numeric material search.");return}Ae=Yo(xe,ge,20),console.log("✅ Found",Ae.length,"glasses with similar nd/vd to",xe,ge)}catch(ge){console.error("❌ Failed to find glasses by numeric nd/abbe:",ge)}else{let ge=null,we=null;if(ie)try{const ue=ha(ie);if(ue&&ue.nd!==void 0&&ue.vd!==void 0)ge=ue.nd,we=ue.vd,console.log("✅ Found glass properties - nd:",ge,"vd:",we);else{alert("Current material does not have valid nd/vd in the glass database.");return}}catch(ue){console.warn("❌ Failed to get glass data:",ue),alert("Failed to resolve nd/vd from current material.");return}else{alert("Enter a material name or numeric nd value first.");return}if(!Number.isFinite(ge)||!Number.isFinite(we)){alert("Valid nd/vd values are required to search similar glasses.");return}console.log("🔍 Searching for glasses similar to nd:",ge,"vd:",we);try{Ae=Yo(ge,we,20),console.log("✅ Found",Ae.length,"similar glasses")}catch(ue){console.error("❌ Failed to find similar glasses:",ue)}}if(Ae.length===0){alert("No glasses found in database.");return}const Ne=document.documentElement.classList.contains("dark"),ye=document.createElement("div");ye.style.position="fixed",ye.style.top="0",ye.style.left="0",ye.style.right="0",ye.style.bottom="0",ye.style.background="rgba(0,0,0,0.6)",ye.style.display="flex",ye.style.alignItems="center",ye.style.justifyContent="center",ye.style.zIndex="9999";const Se=document.createElement("div");Se.style.background=Ne?"#1f2937":"#fff",Se.style.borderRadius="8px",Se.style.padding="20px",Se.style.maxWidth="600px",Se.style.maxHeight="80vh",Se.style.overflow="auto",Se.style.boxShadow="0 4px 20px rgba(0,0,0,0.3)";const _e=document.createElement("h3");_e.textContent="🔍 Select Glass Material",_e.style.margin="0 0 15px 0",_e.style.color=Ne?"#f9fafb":"#111827";const Ce=document.createElement("div");Ce.style.display="flex",Ce.style.flexDirection="column",Ce.style.gap="4px",Ce.style.marginBottom="15px",Ae.slice(0,15).forEach((ge,we)=>{const ue=document.createElement("div");ue.style.padding="8px 12px",ue.style.cursor="pointer",ue.style.borderRadius="4px",ue.style.background=Ne?"#374151":"#f3f4f6",ue.style.transition="background 0.15s",ue.textContent=`${we+1}. ${ge.name} [${ge.manufacturer}] (nd=${ge.nd.toFixed(4)}, vd=${ge.vd.toFixed(1)})`,ue.style.fontSize="13px",ue.style.color=Ne?"#f9fafb":"#111827",ue.onmouseenter=()=>{ue.style.background=Ne?"#4b5563":"#e5e7eb"},ue.onmouseleave=()=>{ue.style.background=Ne?"#374151":"#f3f4f6"},ue.onclick=()=>{X.value=ge.name;const ve=xn(ge.name,F);ve!==F&&Ge(m,W,F,ve);const ke=W.replace(/material/i,"abbe");if(ke!==W&&ge.vd!==void 0)try{Ge(m,ke,void 0,String(ge.vd))}catch{}document.body.removeChild(ye)},Ce.appendChild(ue)});const Me=document.createElement("button");Me.textContent="Cancel",Me.style.padding="6px 16px",Me.style.border="none",Me.style.borderRadius="4px",Me.style.background=Ne?"#4b5563":"#d1d5db",Me.style.color=Ne?"#f9fafb":"#111827",Me.style.cursor="pointer",Me.onclick=()=>document.body.removeChild(ye),Se.appendChild(_e),Se.appendChild(Ce),Se.appendChild(Me),ye.appendChild(Se),ye.onclick=ge=>{ge.target===ye&&document.body.removeChild(ye)},document.body.appendChild(ye)},X.addEventListener("blur",()=>{const ee=xn(X.value,F);ee!==F&&Ge(m,W,F,ee)});const le=ee=>{const ie=String(ee||"").trim();return ie?/^[+-]?(?:\d+\.?\d*|\d*\.\d+)$/.test(ie):!1},Q=()=>{if(W.replace(/material/i,"abbe")===W)return;const ie=le(X.value),be=Array.from(_.querySelectorAll('div[style*="display: flex"]'));let ae=-1;for(let he=0;he<be.length;he++)if(be[he].contains(X)){ae=he;break}if(ae>=0)for(let he=ae+1;he<be.length&&he<ae+3;he++){const de=be[he];if(Array.from(de.querySelectorAll("span")).some(Ne=>String(Ne.textContent||"").toLowerCase().includes("abbe"))){const Ne=Array.from(de.querySelectorAll('input[type="text"]'));for(const ye of Ne)ye.disabled=!ie,ye.style.opacity=ie?"1":"0.5",ye.style.pointerEvents=ie?"auto":"none";console.log(`📝 [Abbe Control] Material=${ie?"numeric":"glass name"} → Abbe ${ie?"enabled":"disabled"}`);return}}};X.addEventListener("input",Q),X.addEventListener("change",Q),setTimeout(Q,200),L.appendChild(X),L.appendChild(Y),L.appendChild(J),pe=L}else{const L=document.createElement("input");L.type="text",L.value=F==null?"":String(F),L.style.fontSize="12px",L.style.padding="4px 6px",L.style.border=S?"1px solid #444":"1px solid #ddd",L.style.background=S?"#111827":"#fff",L.style.color=S?"#f9fafb":"#111827",L.style.flex="1",L.style.minWidth="200px",L.style.height="28px",L.style.boxSizing="border-box",L.addEventListener("blur",()=>{const X=xn(L.value,F);X!==F&&Ge(m,W,F,X)}),pe=L}const me=document.createElement("div");return me.textContent=I||"",me.style.fontSize="10px",me.style.padding="2px 6px",me.style.borderRadius="999px",me.style.border=I?S?"1px solid #374151":"1px solid #e5e7eb":"none",me.style.color=S?"#9ca3af":"#6b7280",me.style.visibility=I?"visible":"hidden",P.appendChild(Z),P.appendChild(pe),P.appendChild(me),P};if(M.length>0){_.appendChild(H("Parameters"));for(const G of M){if(b==="ImageSurface"&&G==="optimizeSemiDia"||b==="ImageSurface"&&G==="thickness"||/^coef\d+$/.test(G)&&N.surfType==="Spherical"||/^frontCoef\d+$/.test(G)&&N.frontSurfType==="Spherical"||/^backCoef\d+$/.test(G)&&N.backSurfType==="Spherical"||/^surf1Coef\d+$/.test(G)&&N.surf1SurfType==="Spherical"||/^surf2Coef\d+$/.test(G)&&N.surf2SurfType==="Spherical"||/^surf3Coef\d+$/.test(G)&&N.surf3SurfType==="Spherical")continue;let F=N[G];if(b==="ImageSurface"&&G==="semidiaMode"&&(F==null||String(F).trim()==="")){const R=String(N?.optimizeSemiDia??"").trim().toUpperCase();F=R==="A"||R==="AUTO"?"Auto":"Manual"}(b==="Gap"||b==="AirGap")&&G==="material"&&(F==null||F==="")&&(F="AIR");const W=f[G],I=document.createElement("div");I.style.display="flex",I.style.alignItems="center",I.style.gap="6px",I.style.marginBottom="6px";const E=document.createElement("input");E.type="checkbox",E.style.flex="0 0 auto",E.style.width="16px",E.style.height="16px",E.style.margin="0 4px 0 0",E.checked=Cr(W),E.addEventListener("click",R=>R.stopPropagation());const P=document.createElement("select");P.style.flex="0 0 110px",P.style.fontSize="12px",P.style.padding="2px 4px",P.innerHTML='<option value="perConfig">Per-config</option><option value="global">Shared (all configs)</option>',P.value=Ir(W),P.disabled=!E.checked,P.addEventListener("click",R=>R.stopPropagation()),E.addEventListener("change",R=>{R.stopPropagation();try{P.disabled=!E.checked}catch{}Sn(m,G,E.checked,String(P.value));try{Je()}catch{}}),P.addEventListener("change",R=>{R.stopPropagation();const k=String(P.value);Fr(m,G,k),E.checked&&Sn(m,G,!0,k);try{Je()}catch{}});const Z=ce(G,F,`parameters.${G}`);Z.style.flex="1",Z.style.marginBottom="0",I.appendChild(E),I.appendChild(P),I.appendChild(Z),_.appendChild(I)}}const U=x.aperture&&typeof x.aperture=="object"?x.aperture:null;if(U&&Object.keys(U).length>0){_.appendChild(H("Aperture (Semidiameter)"));const G=Object.keys(U).sort((F,W)=>{const I=Z=>{const R=String(Z??"").trim().toLowerCase();if(R==="front")return 1;if(R==="back")return 2;const k=R.match(/^s(\d+)$/);return k?100+parseInt(k[1],10):1e3},E=I(F),P=I(W);return E!==P?E-P:F.localeCompare(W)});for(const F of G){const W=U[F],I=F==="front"?"s1":F==="back"?"s2":F,E=document.createElement("div");E.style.display="flex",E.style.alignItems="center",E.style.gap="6px",E.style.marginBottom="6px";const P=document.createElement("input");P.type="checkbox",P.style.flex="0 0 auto",P.style.width="16px",P.style.height="16px",P.style.margin="0 4px 0 0",P.checked=!1,P.addEventListener("click",k=>k.stopPropagation());const Z=document.createElement("select");Z.style.flex="0 0 110px",Z.style.fontSize="12px",Z.style.padding="2px 4px",Z.innerHTML='<option value="perConfig">Per-config</option><option value="global">Shared (all configs)</option>',Z.value="perConfig",Z.disabled=!0,Z.addEventListener("click",k=>k.stopPropagation()),P.addEventListener("change",k=>{k.stopPropagation();try{Z.disabled=!P.checked}catch{}});const R=ce(I,W,`aperture.${F}`);R.style.flex="1",R.style.marginBottom="0",E.appendChild(P),E.appendChild(Z),E.appendChild(R),_.appendChild(E)}}if(C.length>0)for(const G of C){if(M.includes(G))continue;const F=f[G],W=F&&typeof F=="object"&&"value"in F?F.value:F,I=document.createElement("div");I.style.display="flex",I.style.alignItems="center",I.style.gap="6px",I.style.marginBottom="6px";const E=document.createElement("input");E.type="checkbox",E.style.flex="0 0 auto",E.style.width="16px",E.style.height="16px",E.style.margin="0 4px 0 0",E.checked=Cr(F),E.addEventListener("click",k=>k.stopPropagation());const P=document.createElement("select");P.style.flex="0 0 110px",P.style.fontSize="12px",P.style.padding="2px 4px",P.innerHTML='<option value="perConfig">Per-config</option><option value="global">Shared (all configs)</option>',P.value=Ir(F),P.disabled=!E.checked,P.addEventListener("click",k=>k.stopPropagation()),E.addEventListener("change",k=>{k.stopPropagation();try{P.disabled=!E.checked}catch{}Sn(m,G,E.checked,String(P.value));try{Je()}catch{}}),P.addEventListener("change",k=>{k.stopPropagation();const $=String(P.value);Fr(m,G,$),E.checked&&Sn(m,G,!0,$);try{Je()}catch{}});const Z=F&&typeof F=="object"&&F.optimize&&F.optimize.mode?`V:${F.optimize.mode}`:"V",R=ce(G,W,`variables.${G}.value`,Z);R.style.flex="1",R.style.marginBottom="0",I.appendChild(E),I.appendChild(P),I.appendChild(R),_.appendChild(I)}if(M.length===0&&C.length===0){const G=document.createElement("div");G.textContent="No parameters defined for this block.",G.style.fontSize="12px",G.style.color=S?"#9ca3af":"#6b7280",_.appendChild(G)}r.appendChild(_)}}}function Je(){const e=document.getElementById("import-analyze-mode-banner"),t=n=>{e&&(e.style.display=n?"":"none")};try{const n=typeof un=="function"?un():null,o=n&&Array.isArray(n.blocks)?n.blocks:null;try{const r=!o||o.length===0;t(!!r)}catch{}if(o&&o.length>0){const r=new Map;let s=null;try{if(typeof $e=="function"){const i=$e(o),l=i&&Array.isArray(i.rows)?i.rows:[];s=l;for(const m of l){const p=m?._blockId;if(p==null)continue;const d=String(p).trim();if(!d||d==="(none)")continue;const u=String(m?._blockType??"").trim();u==="Gap"||u==="CoordTrans"||u==="ObjectSurface"||u==="ObjectPlane"||u==="Object"||r.set(d,(r.get(d)||0)+1)}}}catch{}try{if(Array.isArray(s)&&s.length>0){const i=s.map((m,p)=>{const d=m&&typeof m=="object"?{...m}:{};return d.id=p,p===0?d["object type"]="Object":p===s.length-1&&(d["object type"]="Image"),d}),l=O.tableOpticalSystem&&typeof O.tableOpticalSystem.getData=="function"?O.tableOpticalSystem:O.opticalSystemTabulator&&typeof O.opticalSystemTabulator.getData=="function"?O.opticalSystemTabulator:null;l&&(typeof l.replaceData=="function"?l.replaceData(i):typeof l.setData=="function"&&l.setData(i));try{Ht(O)}catch{}}}catch{}const a=o.map(i=>{const l=String(i?.blockId??"(none)");return{blockId:l,blockType:String(i?.blockType??"(none)"),surfaceCount:r.has(l)?r.get(l):0,preview:pu(i)}}),c=new Map;for(const i of o){const l=String(i?.blockId??"").trim();l&&c.set(l,i)}kr(a,{},c,o)}else{if(typeof O.dumpOpticalSystemProvenance!="function")return;const r=O.dumpOpticalSystemProvenance({quiet:!0});kr(r?.summary||[],r?.groups||{},null,null)}}catch(n){console.warn("⚠️ [Blocks] Failed to refresh block inspector:",n)}}function mu(){const e=document.getElementById("apply-to-design-intent-btn");e&&(e.dataset&&e.dataset.applyToDesignIntentBound==="1"||(e.dataset&&(e.dataset.applyToDesignIntentBound="1"),e.addEventListener("click",()=>{try{const t=O.tableOpticalSystem||O.tableOpticalSystem,n=t&&typeof t.getData=="function"?t.getData():null;if(!Array.isArray(n)||n.length===0){alert("Expanded Optical System が見つかりません。");return}const o=[];try{const r=O.__pendingSurfaceEdits;if(r&&typeof r=="object")for(const[s,a]of Object.entries(r)){const[c,i]=String(s).split(":"),l=Number(c),m=String(i??"").trim();if(!Number.isFinite(l)||!m)continue;const p=n.find(d=>d&&typeof d.id=="number"&&d.id===l);p&&o.push({row:p,field:m,oldValue:a?.oldValue,newValue:p[m]})}}catch{}if(o.length===0&&O.__lastSurfaceEdit&&o.push(O.__lastSurfaceEdit),o.length===0)try{const r=t&&typeof t.getSelectedCells=="function"?t.getSelectedCells():[],s=Array.isArray(r)&&r.length>0?r[r.length-1]:null;if(s&&typeof s.getField=="function"&&typeof s.getRow=="function"){const a=s.getField(),c=s.getRow()?.getData?.()??null,i=typeof s.getValue=="function"?s.getValue():c?c[a]:void 0;let l;try{l=typeof s.getOldValue=="function"?s.getOldValue():void 0}catch{}l===void 0&&(l=null),c&&o.push({row:c,field:a,oldValue:l,newValue:i})}}catch{}if(o.length===0)try{const r=O.__lastActiveSurfaceCell||O.__lastSelectedSurfaceCell,s=Number(r?.surfaceId),a=String(r?.field??"").trim();if(Number.isFinite(s)&&a){const c=n.find(i=>i&&typeof i.id=="number"&&i.id===s);c&&o.push({row:c,field:a,oldValue:null,newValue:c[a]})}}catch{}if(o.length===0){alert("Apply対象の変更が見つかりません。");return}console.log(`✅ Apply to Design Intent: ${o.length} edits processed`);try{Je()}catch{}try{O.__pendingSurfaceEdits={}}catch{}try{const r=O.popup3DWindow;r&&!r.closed&&typeof r.postMessage=="function"&&r.postMessage({action:"request-redraw"},"*")}catch{}}catch(t){console.error("❌ Apply to Design Intent failed:",t),alert(`Apply failed: ${t?.message||String(t)}`)}})))}function Oo(e){const t=String(e??"").trim();return t==="ObjectPlane"?"ObjectSurface":t==="ImagePlane"?"ImageSurface":t==="AirGap"?"Gap":t}function qi(e,t){const o=Oo(t)||"Block";let r=0;const s=new RegExp(`^${o}-(\\d+)$`);for(const a of e||[]){const c=s.exec(String(a?.blockId||""));if(c){const i=parseInt(c[1],10);i>r&&(r=i)}}return`${o}-${r+1}`}function Zi(e,t){const n=Oo(e),r={blockId:String(t??"").trim(),blockType:n,role:null,constraints:{},parameters:{},variables:{},metadata:{source:"ui-add"}};return n==="Lens"||n==="PositiveLens"?(r.parameters={frontRadius:"INF",backRadius:"INF",centerThickness:1,material:"N-BK7",abbe:"",frontSurfType:"Spherical",backSurfType:"Spherical",frontConic:0,backConic:0},r.aperture={front:10,back:10},r):n==="Doublet"?(r.parameters={radius1:"INF",radius2:"INF",radius3:"INF",thickness1:1,thickness2:1,material1:"N-BK7",material2:"N-F2",abbe1:"",abbe2:"",surf1SurfType:"Spherical",surf2SurfType:"Spherical",surf3SurfType:"Spherical",surf1Conic:0,surf2Conic:0,surf3Conic:0},r.aperture={s1:10,s2:10,s3:10},r):n==="Triplet"?(r.parameters={radius1:"INF",radius2:"INF",radius3:"INF",radius4:"INF",thickness1:1,thickness2:1,thickness3:1,material1:"N-BK7",material2:"N-F2",material3:"N-BK7",abbe1:"",abbe2:"",abbe3:"",surf1SurfType:"Spherical",surf2SurfType:"Spherical",surf3SurfType:"Spherical",surf4SurfType:"Spherical",surf1Conic:0,surf2Conic:0,surf3Conic:0,surf4Conic:0},r.aperture={s1:10,s2:10,s3:10,s4:10},r):n==="Gap"?(r.blockType="Gap",r.parameters={thickness:1,material:"AIR",abbe:"",thicknessMode:""},r):n==="ObjectSurface"?(r.parameters={objectDistanceMode:"Finite",objectDistance:100},r):n==="Stop"?(r.parameters={semiDiameter:Bi},r):n==="Mirror"?(r.parameters={radius:"INF",thickness:0,material:"MIRROR",surfType:"Spherical",conic:0,coef1:0,coef2:0,coef3:0,coef4:0,coef5:0,coef6:0,coef7:0,coef8:0,coef9:0,coef10:0,apertureShape:"Circular",semidia:10,apertureWidth:20,apertureHeight:20},r):n==="CoordTrans"?(r.parameters={decenterX:0,decenterY:0,decenterZ:0,tiltX:0,tiltY:0,tiltZ:0,order:0,coordReturn:"none",toSurf:0},r):n==="SingleSurface"?(r.parameters={radius:"INF",thickness:10,material:"AIR",surfType:"Spherical",conic:0,coef1:0,coef2:0,coef3:0,coef4:0,coef5:0,coef6:0,coef7:0,coef8:0,coef9:0,coef10:0,apertureShape:"Circular",semidia:10,apertureWidth:20,apertureHeight:20},r):n==="ImageSurface"?(r.parameters={semidia:"",semidiaMode:"Manual",optimizeSemiDia:""},delete r.variables,r):(r.parameters={},r)}function fu(e,t=null){const n=Ie();if(!n||!Array.isArray(n.configurations))return{ok:!1,reason:"systemConfigurations not found."};const o=n.activeConfigId,r=n.configurations.findIndex(u=>u&&u.id===o);if(r<0)return{ok:!1,reason:"active config not found."};const s=n.configurations[r];if(!s||!Array.isArray(s.blocks))return{ok:!1,reason:"active config has no blocks."};const a=s.blocks,c=Oo(e);if(!c)return{ok:!1,reason:"blockType is required."};if(c==="ImageSurface"&&a.some(h=>h&&String(h.blockType??"").trim()==="ImageSurface"))return{ok:!1,reason:"ImageSurface already exists (only one is supported)."};if(c==="ObjectSurface"&&a.some(h=>{const g=String(h?.blockType??"").trim();return g==="ObjectSurface"||g==="ObjectPlane"}))return{ok:!1,reason:"ObjectSurface/ObjectPlane already exists (only one is supported)."};if(c==="Gap"||c==="AirGap"){const u=String(t??"").trim();let h=-1;if(u)h=a.findIndex(A=>A&&String(A.blockId??"").trim()===u);else for(let A=a.length-1;A>=0;A--)if(String(a[A]?.blockType??"").trim()!=="ImageSurface"){h=A;break}if(h<0)return{ok:!1,reason:"Gap requires a preceding block (e.g., Lens or Stop). Add a Lens/Stop first."};const g=a[h],y=String(g?.blockType??"").trim();if(y==="ObjectSurface"||y==="ObjectPlane")return{ok:!1,reason:"Gap cannot be placed directly after ObjectSurface. Add a Lens or Stop first."}}const i=qi(a,c),l=Zi(c,i);let m=a.findIndex(u=>u&&String(u.blockType??"").trim()==="ImageSurface");m<0&&(m=a.length);let p=m;c==="ObjectSurface"&&(p=0);const d=String(t??"").trim();if(d){const u=a.findIndex(h=>h&&String(h.blockId??"").trim()===d);u>=0&&(p=Math.min(u+1,m))}a.splice(p,0,l);try{(!s.metadata||typeof s.metadata!="object")&&(s.metadata={}),s.metadata.modified=new Date().toISOString()}catch{}try{if(di(s).filter(g=>g&&g.severity==="fatal").length>0)return a.splice(p,1),{ok:!1,reason:"block validation failed."}}catch{}try{Te(n)}catch(u){return{ok:!1,reason:`failed to save: ${u?.message||String(u)}`}}return{ok:!0,blockId:i,blockData:JSON.parse(JSON.stringify(l)),insertIndex:p}}function gu(e){const t=Ie();if(!t||!Array.isArray(t.configurations))return{ok:!1,reason:"systemConfigurations not found."};const n=t.activeConfigId,o=t.configurations.findIndex(p=>p&&p.id===n);if(o<0)return{ok:!1,reason:"active config not found."};const r=t.configurations[o];if(!r||!Array.isArray(r.blocks))return{ok:!1,reason:"active config has no blocks."};const s=r.blocks,a=String(e??"").trim();if(!a)return{ok:!1,reason:"blockId is required."};const c=s.findIndex(p=>p&&String(p.blockId??"").trim()===a);if(c<0)return{ok:!1,reason:`block not found: ${a}`};const i=String(s[c]?.blockType??"").trim(),l=JSON.parse(JSON.stringify(s[c])),m=s.splice(c,1);if(i==="ImageSurface"){const p=qi(s,"ImageSurface"),d=Zi("ImageSurface",p);s.push(d)}try{(!r.metadata||typeof r.metadata!="object")&&(r.metadata={}),r.metadata.modified=new Date().toISOString()}catch{}try{if(di(r).filter(u=>u&&u.severity==="fatal").length>0)return s.splice(c,0,...m||[]),{ok:!1,reason:"block validation failed."}}catch{}try{const p=$e(r.blocks);if(p&&Array.isArray(p.rows)){r.opticalSystem=p.rows;try{qt(p.rows)}catch{}try{typeof O.saveLensTableData=="function"&&O.saveLensTableData(p.rows)}catch{}try{O.tableOpticalSystem&&typeof O.tableOpticalSystem.setData=="function"&&O.tableOpticalSystem.setData(p.rows)}catch{}}}catch{}try{Te(t)}catch(p){return{ok:!1,reason:`failed to save: ${p?.message||String(p)}`}}return{ok:!0,blockData:l,blockIndex:c}}function yu(){const e=document.getElementById("design-intent-add-block-btn"),t=document.getElementById("design-intent-delete-block-btn"),n=document.getElementById("design-intent-param-all-on-btn"),o=document.getElementById("design-intent-param-all-off-btn"),r=document.getElementById("design-intent-add-block-type");e&&!e.dataset.designIntentAddBound&&(e.dataset.designIntentAddBound="1",e.addEventListener("click",s=>{try{s?.preventDefault?.()}catch{}try{s?.stopPropagation?.()}catch{}try{const a=String(r?.value??"Lens").trim(),i=fu(a,Rt);if(!i||i.ok!==!0){alert(`Failed to add block: ${i?.reason||"unknown error"}`);return}Rt=String(i.blockId??"")||null;try{if(O.undoHistory&&O.AddBlockCommand&&!O.undoHistory.isExecuting&&i.blockData&&typeof i.insertIndex=="number"){const l=Ie(),m=new O.AddBlockCommand(l.activeConfigId,i.blockData,i.insertIndex);O.undoHistory.record(m)}}catch{}try{Je()}catch{}try{typeof O.loadActiveConfigurationToTables=="function"&&O.loadActiveConfigurationToTables()}catch{}try{O.popup3DWindow&&!O.popup3DWindow.closed&&O.popup3DWindow.postMessage({action:"request-redraw"},"*")}catch{}}catch(a){console.error("❌ Failed to add block:",a),alert(`Failed to add block: ${a?.message||String(a)}`)}})),t&&!t.dataset.designIntentDeleteBound&&(t.dataset.designIntentDeleteBound="1",t.addEventListener("click",s=>{try{s?.preventDefault?.()}catch{}try{s?.stopPropagation?.()}catch{}try{const a=String(Rt??"").trim();if(!a){alert("Select (expand) a block first to delete.");return}const c=gu(a);if(!c||c.ok!==!0){alert(`Failed to delete block: ${c?.reason||"unknown error"}`);return}try{if(O.undoHistory&&O.DeleteBlockCommand&&!O.undoHistory.isExecuting&&c.blockData&&typeof c.blockIndex=="number"){const i=Ie(),l=new O.DeleteBlockCommand(i.activeConfigId,c.blockData,c.blockIndex);O.undoHistory.record(l)}}catch{}Rt=null;try{Je()}catch{}try{typeof O.loadActiveConfigurationToTables=="function"&&O.loadActiveConfigurationToTables()}catch{}try{O.popup3DWindow&&!O.popup3DWindow.closed&&O.popup3DWindow.postMessage({action:"request-redraw"},"*")}catch{}}catch(a){console.error("❌ Failed to delete block:",a),alert(`Failed to delete block: ${a?.message||String(a)}`)}})),n&&!n.dataset.designIntentParamAllOnBound&&(n.dataset.designIntentParamAllOnBound="1",n.addEventListener("click",s=>{try{s?.preventDefault?.()}catch{}try{s?.stopPropagation?.()}catch{}const a=Mr(!0);(!a||a.ok!==!0)&&alert(`Failed to set Parameter All ON: ${a?.reason||"unknown error"}`)})),o&&!o.dataset.designIntentParamAllOffBound&&(o.dataset.designIntentParamAllOffBound="1",o.addEventListener("click",s=>{try{s?.preventDefault?.()}catch{}try{s?.stopPropagation?.()}catch{}const a=Mr(!1);(!a||a.ok!==!0)&&alert(`Failed to set Parameter All OFF: ${a?.reason||"unknown error"}`)}))}function rn(){try{El(),Tl(),Ol(),Dl(),jl(),Ll(),Wl(),zl(),Ml(),yu(),$l(),Ul(),Hl(),Gl(),Vl(),ql(),Zl(),Yl(),Xl(),Kl(),Jl(),Ql(),eu(),mu()}catch(e){console.error("❌ [DOM] Failed to setup event handlers:",e)}}async function hu(){const e=Ps();if(!e)return{ok:!1,reason:"no_hash"};if(!confirm(`リンクから設計を読み込みます。現在の設計は上書きされます。続行しますか？

Load design from URL? Current design will be overwritten.`))return{ok:!1,reason:"cancelled"};let n;try{n=Es(e)}catch(r){return console.warn("❌ [URL Load] Decode failed:",r),alert(r?.message||"Failed to load design from URL"),{ok:!1,reason:"decode_failed"}}const o=await fn(n,{filename:"shared-link.json"});if(o)try{history.replaceState(null,"",`${location.origin}${location.pathname}${location.search}`)}catch{}return{ok:o}}typeof window<"u"&&(window.addEventListener("coopt:react-mounted",()=>{setTimeout(()=>{rn(),Wt(),$t()},200)}),document.readyState==="loading"?document.addEventListener("DOMContentLoaded",()=>{setTimeout(()=>{rn(),Wt(),$t()},1e3)}):setTimeout(()=>{rn(),Wt(),$t()},1e3));const mt=window;function Pr(){try{const e=document.getElementById("wavefront-object-select");if(!e)return;let t=[];if(typeof window<"u"&&mt.tableObject&&mt.tableObject.getData){const o=mt.tableObject.getData();t=o.filter(r=>r&&r!==null&&r!==void 0),o.length>t.length&&console.warn(`無効なObjectデータが${o.length-t.length}個あります。Clear Cacheでリセットを推奨。`)}else{console.warn("⚠️ tableObjectが利用できません");return}const n=e.value;if(e.innerHTML="",t.length===0){const o=document.createElement("option");o.value="0",o.textContent="Object 1 (Empty)",o.disabled=!0,e.appendChild(o)}else t.forEach((o,r)=>{const s=document.createElement("option");s.value=r.toString();let a=`Object ${r+1}`;const c=o.xHeightAngle||0,i=o.yHeightAngle||0;c!==0||i!==0?a+=` (${c.toFixed(2)}, ${i.toFixed(2)})`:a+=" (0.00, 0.00)",s.textContent=a,e.appendChild(s)});n&&e.querySelector(`option[value="${n}"]`)?e.value=n:t.length>0&&(e.value="0")}catch(e){console.error("❌ Object選択ドロップダウン更新エラー:",e)}}function An(){try{const e=document.getElementById("psf-object-select");if(!e)return;let t=[];if(typeof window<"u"&&mt.tableObject&&mt.tableObject.getData){const o=mt.tableObject.getData();t=Array.isArray(o)?o.filter(r=>r&&r!==null&&r!==void 0):[]}const n=e.value;if(e.innerHTML="",t.length===0){const o=document.createElement("option");o.value="0",o.textContent="Object 1 (Empty)",o.disabled=!0,e.appendChild(o);return}t.forEach((o,r)=>{const s=document.createElement("option");s.value=r.toString();const a=o?.xHeightAngle??o?.xFieldAngle??o?.xAngle??o?.x??0,c=o?.yHeightAngle??o?.yFieldAngle??o?.yAngle??o?.y??o?.fieldAngle??0,i=Number.isFinite(Number(a))?Number(a):0,l=Number.isFinite(Number(c))?Number(c):0,m=String(o?.position??o?.object??o?.objectType??"Object");s.textContent=`${r+1}: ${m} (${i}, ${l})`,e.appendChild(s)}),n&&e.querySelector(`option[value="${n}"]`)?e.value=n:e.value="0"}catch(e){console.error("❌ PSF Object選択ドロップダウン更新エラー:",e)}}function bu(){An(),mt.updatePSFObjectOptions=An,mt.updatePSFObjectSelect=An,mt.setupPSFObjectSelect=An}function wu(){const e=document.getElementById("wavefront-object-select");e&&e.addEventListener("change",function(){parseInt(this.value),mi()})}function vu(){wu(),Pr(),mt.updateWavefrontObjectSelect=Pr,mt.debugResetObjectTable=Su}function Su(){try{mi(),location.reload()}catch(e){console.error("❌ Objectテーブルリセットエラー:",e)}}const Ve={};function xu(e){try{if(typeof window>"u")return;e.scene!==void 0&&(window.scene=Ve.scene),e.camera!==void 0&&(window.camera=Ve.camera),e.renderer!==void 0&&(window.renderer=Ve.renderer),e.controls!==void 0&&(window.controls=Ve.controls),window.__cooptRenderingContext={scene:window.scene,camera:window.camera,renderer:window.renderer,controls:window.controls}}catch{}}function Do(e){return e&&typeof e=="object"&&(e.scene!==void 0&&(Ve.scene=e.scene),e.camera!==void 0&&(Ve.camera=e.camera),e.renderer!==void 0&&(Ve.renderer=e.renderer),e.controls!==void 0&&(Ve.controls=e.controls),xu(e)),Au()}try{typeof window<"u"&&typeof window.__cooptSetRenderingContext!="function"&&(window.__cooptSetRenderingContext=e=>{try{Do(e||{})}catch{}})}catch{}function Au(){try{typeof window<"u"&&(Ve.scene===void 0&&window.scene!==void 0&&(Ve.scene=window.scene),Ve.camera===void 0&&window.camera!==void 0&&(Ve.camera=window.camera),Ve.renderer===void 0&&window.renderer!==void 0&&(Ve.renderer=window.renderer),Ve.controls===void 0&&window.controls!==void 0&&(Ve.controls=window.controls))}catch{}return{scene:Ve.scene,camera:Ve.camera,renderer:Ve.renderer,controls:Ve.controls}}function Qe(e){return!!e&&typeof e=="object"&&!Array.isArray(e)}function tt(e){return JSON.parse(JSON.stringify(e))}function Nu(){try{return new Date().toLocaleString()}catch{return String(Date.now())}}function _u(e){Os("suggestDesignIntent","lastSuggest",e&&typeof e=="object"?e:null)}function Ru(){const e=Ts("suggestDesignIntent","lastSuggest",null);if(e&&typeof e=="object")return e;try{if(typeof window>"u")return null;const t=window.__cooptLastSuggest;return t&&typeof t=="object"?t:null}catch{return null}}function Yi(){try{if(typeof localStorage>"u")return{systemConfig:null,cfg:null};const e=ot(),t=e?.activeConfigId,n=e?.configurations?.find(o=>o&&o.id===t)||e?.configurations?.[0];return{systemConfig:e,cfg:n}}catch{return{systemConfig:null,cfg:null}}}function Cu(){return typeof document>"u"?null:document.getElementById("system-data")}function Iu(e){try{const t=localStorage.getItem(e);if(!t)return null;const n=JSON.parse(t);return Array.isArray(n)?n:null}catch{return null}}function Fu(e,t,n){try{if(Array.isArray(e?.blocks)&&e.blocks.some(r=>{const s=String(r?.blockType??"").trim();return s==="ObjectSurface"||s==="ObjectPlane"}))return}catch{}try{const o=e?.opticalSystem?.[0]?.thickness;if(String(o??"").trim()!=="")return o}catch{}try{if(t&&String(t.activeConfigId)===String(n)){const r=Iu("OpticalSystemTableData")?.[0]?.thickness;if(String(r??"").trim()!=="")return r}}catch{}}async function Mu(e){const{systemConfig:t,cfg:n}=Yi();if(!t||!n)return{ok:!1,reason:"systemConfigurations / active configuration が見つかりません。"};if(!Array.isArray(e)||e.length===0)return{ok:!1,reason:"Recommendation blocks が空です。"};const o=n.id,r=Fu(n,t,o);n.blocks=tt(e);try{const s=$e(n.blocks);s&&Array.isArray(s.rows)&&(r!==void 0&&s.rows[0]&&typeof s.rows[0]=="object"&&(s.rows[0].thickness=r),n.opticalSystem=s.rows)}catch{}try{No(t)}catch{return{ok:!1,reason:"localStorage への保存に失敗しました。"}}try{window.ConfigurationManager&&typeof window.ConfigurationManager.loadActiveConfigurationToTables=="function"&&await window.ConfigurationManager.loadActiveConfigurationToTables({applyToUI:!0,suppressOpticalSystemDataChanged:!0})}catch{}try{Ut()}catch{}try{window.meritFunctionEditor&&typeof window.meritFunctionEditor.calculateMerit=="function"&&window.meritFunctionEditor.calculateMerit()}catch{}return{ok:!0}}function dt(e){if(typeof e=="number")return Number.isFinite(e)?e:null;if(typeof e=="string"){const t=e.trim();if(t===""||/^inf(inity)?$/i.test(t))return null;const n=Number(t);return Number.isFinite(n)?n:null}return null}function Er(e,{radiusScale:t=1,thicknessScale:n=1,gapScale:o=1}={}){for(const r of e){if(!Qe(r))continue;const s=String(r.blockType??"").trim(),a=Qe(r.parameters)?r.parameters:null;if(a){if(s==="Lens"||s==="PositiveLens"){const c=dt(a.frontRadius),i=dt(a.backRadius),l=dt(a.centerThickness);c!==null&&(a.frontRadius=c*t),i!==null&&(a.backRadius=i*t),l!==null&&(a.centerThickness=Math.max(1e-6,l*n))}else if(s==="AirGap"){const c=dt(a.thickness);c!==null&&(a.thickness=Math.max(0,c*o))}}}}function Nn(e,{deltaCurvature:t=0}={}){for(const r of e){if(!Qe(r))continue;const s=String(r.blockType??"").trim();if(!(s==="Lens"||s==="PositiveLens"))continue;const a=Qe(r.parameters)?r.parameters:null;if(!a)continue;const c=dt(a.frontRadius),i=dt(a.backRadius);if(c===null||i===null||!Number.isFinite(c)||!Number.isFinite(i)||Math.abs(c)<1e-12||Math.abs(i)<1e-12)continue;const l=1/c,m=1/i,p=l+t,d=m+t;!Number.isFinite(p)||!Number.isFinite(d)||Math.abs(p)<1e-12||Math.abs(d)<1e-12||Math.abs(p)>1e6||Math.abs(d)>1e6||(a.frontRadius=1/p,a.backRadius=1/d)}}function Tr(e,{deltaCurvature:t=0}={}){for(const r of e){if(!Qe(r))continue;const s=String(r.blockType??"").trim();if(!(s==="Lens"||s==="PositiveLens"))continue;const a=Qe(r.parameters)?r.parameters:null;if(!a)continue;const c=dt(a.frontRadius),i=dt(a.backRadius);if(c===null||i===null||!Number.isFinite(c)||!Number.isFinite(i)||Math.abs(c)<1e-12||Math.abs(i)<1e-12)continue;const l=1/c,m=1/i,p=l+t,d=m-t;!Number.isFinite(p)||!Number.isFinite(d)||Math.abs(p)<1e-12||Math.abs(d)<1e-12||Math.abs(p)>1e6||Math.abs(d)>1e6||(a.frontRadius=1/p,a.backRadius=1/d)}}function ku(e,{oddScale:t=1,evenScale:n=1}={}){let o=0;for(const r of e){if(!Qe(r)||String(r.blockType??"").trim()!=="AirGap")continue;const a=Qe(r.parameters)?r.parameters:null;if(!a)continue;const c=dt(a.thickness);if(c===null)continue;const i=o%2===0?t:n;a.thickness=Math.max(0,c*i),o++}}function Or(e,{scale:t=1}={}){for(const n of e){if(!Qe(n)||String(n.blockType??"").trim()!=="Stop")continue;const r=Qe(n.parameters)?n.parameters:null;if(!r)continue;const s=dt(r.semiDiameter);s!==null&&(r.semiDiameter=Math.max(1e-9,s*t))}}function Pu(e,t,n=12){const o=[],r=new Map;for(const s of e){if(!Qe(s))continue;const a=String(s.blockId??"");a&&r.set(a,s)}for(const s of t){if(!Qe(s))continue;const a=String(s.blockId??"");if(!a)continue;const c=r.get(a);if(!c){if(o.push(`+ ${a} (${s.blockType})`),o.length>=n)break;continue}const i=Qe(c.parameters)?c.parameters:null,l=Qe(s.parameters)?s.parameters:null;if(!i||!l)continue;const m=["frontRadius","backRadius","centerThickness","thickness","semiDiameter","material"];for(const p of m){if(!(p in l)&&!(p in i))continue;const d=i[p],u=l[p],h=String(d??""),g=String(u??"");if(h!==g&&(o.push(`~ ${a}.${p}: ${h} → ${g}`),o.length>=n))break}if(o.length>=n)break}return o}function Dr({configId:e,blocks:t}){const n=typeof window<"u"?window.meritFunctionEditor:null;if(!n)return{ok:!1,reason:"meritFunctionEditor is not available."};const o=typeof window<"u"?Object.prototype.hasOwnProperty.call(window,"__cooptBlocksOverride"):!1,r=typeof window<"u"?window.__cooptBlocksOverride:null,s=r&&typeof r=="object"?{...r}:null,a=r&&typeof r=="object"?r:typeof window<"u"?window.__cooptBlocksOverride||={}:{};a[String(e)]=t;try{if(typeof n.calculateMeritBreakdownOnly=="function"){const c=n.calculateMeritBreakdownOnly(),i=Number(c?.total),l=Array.isArray(c?.terms)?c.terms:[];return{ok:!0,total:Number.isFinite(i)?i:1/0,terms:l}}if(typeof n.calculateMeritValueOnly=="function"){const c=Number(n.calculateMeritValueOnly());return{ok:!0,total:Number.isFinite(c)?c:1/0,terms:[]}}return{ok:!1,reason:"No merit evaluator available."}}finally{if(typeof window<"u")if(o&&s&&typeof s=="object"){const c=window.__cooptBlocksOverride&&typeof window.__cooptBlocksOverride=="object"?window.__cooptBlocksOverride:window.__cooptBlocksOverride||={};for(const i of Object.keys(c))try{delete c[i]}catch{}Object.assign(c,s)}else try{delete window.__cooptBlocksOverride}catch{}}}function Eu(e){const t={};for(const n of e){const o=String(n?.operand??"");o&&(o==="EFFL"&&t.EFFL===void 0&&(t.EFFL=n?.value),o==="TOT3_SPH"&&t.TOT3_SPH===void 0&&(t.TOT3_SPH=n?.value),o==="TOT3_COMA"&&t.TOT3_COMA===void 0&&(t.TOT3_COMA=n?.value),o==="TOT3_ASTI"&&t.TOT3_ASTI===void 0&&(t.TOT3_ASTI=n?.value),o==="TOT3_FCUR"&&t.TOT3_FCUR===void 0&&(t.TOT3_FCUR=n?.value),o==="TOT3_DIST"&&t.TOT3_DIST===void 0&&(t.TOT3_DIST=n?.value),o==="TOT_LCA"&&t.TOT_LCA===void 0&&(t.TOT_LCA=n?.value),o==="TOT_TCA"&&t.TOT_TCA===void 0&&(t.TOT_TCA=n?.value))}return t}function De(e){const t=Number(e);return Number.isFinite(t)?t.toFixed(6):String(e??"-")}function jr(e,t,n){const o=Number(e);return Number.isFinite(o)?Math.max(t,Math.min(n,o)):t}function Tu(e){const t=(Array.isArray(e)?e:[]).filter(o=>Number.isFinite(Number(o))).map(Number).sort((o,r)=>o-r);if(t.length===0)return NaN;const n=Math.floor(t.length/2);return t.length%2===1?t[n]:.5*(t[n-1]+t[n])}function Ou(e){const t=[];for(const s of Array.isArray(e)?e:[]){if(!Qe(s))continue;const a=String(s.blockType??"").trim();if(!(a==="Lens"||a==="PositiveLens"))continue;const c=Qe(s.parameters)?s.parameters:null;if(!c)continue;const i=dt(c.frontRadius),l=dt(c.backRadius);i!==null&&Number.isFinite(i)&&i!==0&&t.push(Math.abs(1/i)),l!==null&&Number.isFinite(l)&&l!==0&&t.push(Math.abs(1/l))}const n=Tu(t);if(!Number.isFinite(n)||n<=0)return{small:.002,large:.004};const o=jr(.02*n,1e-5,.005),r=jr(2*o,2e-5,.01);return{small:o,large:r}}function Xi(e){const t=String(e??"").trim().toLowerCase();return t?t==="tele"||t==="telephoto"||t==="tele-photo"?"telephoto":t==="wide"||t==="wideangle"||t==="wide-angle"?"wide":"balanced":"balanced"}function Du(e,t){const n=Xi(t),o={balanced:["global","bending_small","asym","gap","stop","bending_large","material"],telephoto:["gap","asym","bending_small","global","stop","bending_large","material"],wide:["stop","bending_small","asym","global","gap","bending_large","material"]},r=o[n]||o.balanced,s=new Map(r.map((c,i)=>[c,i])),a=(Array.isArray(e)?e:[]).map((c,i)=>({c,idx:i}));return a.sort((c,i)=>{const l=String(c.c?.tag??""),m=String(i.c?.tag??""),p=s.has(l)?s.get(l):999,d=s.has(m)?s.get(m):999;return p!==d?p-d:c.idx-i.idx}),a.map(c=>c.c)}function Vt(e){const t=e&&e.id!==void 0?String(e.id):"?",n=String(e?.operand??"");return`#${t} ${n||"(unknown)"}`}function ju(e,t=5){const n=Array.isArray(e)?e.slice():[];n.sort((r,s)=>Number(s?.term??0)-Number(r?.term??0));const o=[];for(const r of n.slice(0,t)){const s=Number(r?.term),a=Number(r?.impactPct);o.push(`    - ${Vt(r)}: term=${De(s)}, impact=${De(a)}% (value=${De(r?.value)}, target=${De(r?.target)})`)}return o}function Ki(e,t=2){const n=Array.isArray(e)?e.slice():[];n.sort((r,s)=>Number(s?.term??0)-Number(r?.term??0));const o=n.slice(0,Math.max(0,t));return o.length===0?"-":o.map(r=>{const s=Number(r?.impactPct),a=Number.isFinite(s)?`${s.toFixed(1)}%`:"-";return`${Vt(r)} ${a}`}).join(", ")}function Lu(e,t,{improveK:n=2,regressK:o=1}={}){const r=Tn(e),s=Tn(t),a=[];for(const[p,d]of s.entries()){const u=r.get(p);if(!u)continue;const h=Number(u?.term),y=Number(d?.term)-h;Number.isFinite(y)&&a.push({d:y,ct:d})}a.sort((p,d)=>p.d-d.d);const c=a.filter(p=>p.d<0).slice(0,Math.max(0,n)),i=a.filter(p=>p.d>0).slice(-Math.max(0,o)).reverse(),l=c.length?c.map(p=>`${Vt(p.ct)} ${De(p.d)}`).join(", "):"-",m=i.length?i.map(p=>`${Vt(p.ct)} +${De(p.d)}`).join(", "):"-";return{improveStr:l,regressStr:m}}function Tn(e){const t=new Map;for(const n of Array.isArray(e)?e:[]){const o=String(n?.id??"");o&&t.set(o,n)}return t}function Bu(e,t,{kImprove:n=3,kRegress:o=2}={}){const r=Tn(e),s=Tn(t),a=[];for(const[m,p]of s.entries()){const d=r.get(m);if(!d)continue;const u=Number(d?.term),g=Number(p?.term)-u;Number.isFinite(g)&&a.push({d:g,bt:d,ct:p})}a.sort((m,p)=>m.d-p.d);const c=a.filter(m=>m.d<0).slice(0,n),i=a.filter(m=>m.d>0).slice(-o).reverse(),l=[];if(c.length>0){l.push("  Biggest improvements vs current (Δterm < 0):");for(const m of c)l.push(`    - ${Vt(m.ct)}: Δterm=${De(m.d)} (cur=${De(m.bt?.term)} → cand=${De(m.ct?.term)})`)}if(i.length>0){l.push("  Biggest regressions vs current (Δterm > 0):");for(const m of i)l.push(`    - ${Vt(m.ct)}: Δterm=${De(m.d)} (cur=${De(m.bt?.term)} → cand=${De(m.ct?.term)})`)}return l}function zu({idx:e,title:t,intent:n,merit:o,metrics:r,diffs:s,baseTerms:a,candTerms:c},i={}){const l=[];l.push(`Candidate ${e}: ${t}`),l.push(`  Intent: ${n}`),l.push(`  Merit:  ${De(o)}`);const m=Object.keys(r||{});if(m.length>0){l.push("  Key Metrics:");for(const h of m)l.push(`    ${h}: ${De(r[h])}`)}if(s&&s.length>0){l.push("  Diffs (vs current):");for(const h of s)l.push(`    ${h}`)}const p=Number.isFinite(Number(i.topK))?Number(i.topK):5,d=Number.isFinite(Number(i.improveK))?Number(i.improveK):3,u=Number.isFinite(Number(i.regressK))?Number(i.regressK):2;return Array.isArray(c)&&c.length>0&&(l.push("  Top contributors (candidate):"),l.push(...ju(c,p))),Array.isArray(a)&&a.length>0&&Array.isArray(c)&&c.length>0&&l.push(...Bu(a,c,{kImprove:d,kRegress:u})),l.join(`
`)}function Wu({idx:e,title:t,tag:n,merit:o,deltaMerit:r,baseTerms:s,candTerms:a},c={}){const i=Number.isFinite(Number(c.topK))?Number(c.topK):2,l=Number.isFinite(Number(c.improveK))?Number(c.improveK):2,m=Number.isFinite(Number(c.regressK))?Number(c.regressK):1,p=String(n??"").trim(),d=p?` [${p}]`:"",u=Number(r),h=Number.isFinite(u)?u>=0?`+${De(u)}`:`${De(u)}`:"-",g=Ki(a,i),y=Lu(s,a,{improveK:l,regressK:m}),A=[];return A.push(`C${e}${d}: ${t} | Merit ${De(o)} (Δ ${h})`),A.push(`  Top: ${g}`),A.push(`  Δterm: - ${y.improveStr} | + ${y.regressStr}`),A.join(`
`)}function $u(e){const t=[];for(const n of e){if(!Qe(n))continue;const o=String(n.blockType??"").trim();o&&o!=="ImageSurface"&&t.push(`${o}${n.blockId?`(${n.blockId})`:""}`)}return t.push("ImageSurface"),t.join(" → ")}function Uu(e={}){const t={candidateCount:5,topK:2,improveK:2,regressK:1,designType:"balanced",outputMode:"compact"},n=typeof window<"u"&&window.__cooptSuggestOptions&&typeof window.__cooptSuggestOptions=="object"?window.__cooptSuggestOptions:{},o={...t,...n,...e&&typeof e=="object"?e:{}},{systemConfig:r,cfg:s}=Yi();if(!r||!s){alert("systemConfigurations / active configuration が見つかりません。");return}if(!Array.isArray(s.blocks)||s.blocks.length===0){alert("Blocks がありません。Suggest は Blocks ベースで動作します。");return}const a=s.id,c=tt(s.blocks),i=Ou(c),l=Dr({configId:a,blocks:c}),m=l.ok?l.total:1/0,p=l.ok?Array.isArray(l.terms)?l.terms:[]:[],d=[];{const f=tt(c);Er(f,{radiusScale:.95,thicknessScale:1,gapScale:1.02}),d.push({title:"Global bias A (R×0.95, gaps×1.02)",intent:"やや強めパワー方向へ（近傍探索）",blocks:f,tag:"global"})}{const f=tt(c);Er(f,{radiusScale:1.05,thicknessScale:1,gapScale:.98}),d.push({title:"Global bias B (R×1.05, gaps×0.98)",intent:"やや弱めパワー方向へ（別の局所解）",blocks:f,tag:"global"})}{const f=tt(c);Nn(f,{deltaCurvature:+i.small}),d.push({title:`Bending + (Δc=${De(i.small)})`,intent:"形状因子方向（小ステップ）",blocks:f,tag:"bending_small"})}{const f=tt(c);Nn(f,{deltaCurvature:-i.small}),d.push({title:`Bending - (Δc=-${De(i.small)})`,intent:"形状因子方向（小ステップ）",blocks:f,tag:"bending_small"})}{const f=tt(c);Nn(f,{deltaCurvature:+i.large}),d.push({title:`Bending ++ (Δc=${De(i.large)})`,intent:"形状因子方向（大ステップ）",blocks:f,tag:"bending_large"})}{const f=tt(c);Nn(f,{deltaCurvature:-i.large}),d.push({title:`Bending -- (Δc=-${De(i.large)})`,intent:"形状因子方向（大ステップ）",blocks:f,tag:"bending_large"})}{const f=tt(c);Tr(f,{deltaCurvature:+i.small}),d.push({title:`Asymmetric bend + (Δc=${De(i.small)})`,intent:"前後面の配分を変える（小ステップ）",blocks:f,tag:"asym"})}{const f=tt(c);Tr(f,{deltaCurvature:-i.small}),d.push({title:`Asymmetric bend - (Δc=${De(i.small)})`,intent:"前後面の配分を変える（小ステップ）",blocks:f,tag:"asym"})}{const f=tt(c);ku(f,{oddScale:1.05,evenScale:.95}),d.push({title:"Gap redistribute (odd×1.05, even×0.95)",intent:"ギャップの再配分で探索（全体スケール以外）",blocks:f,tag:"gap"})}{const f=tt(c);Or(f,{scale:1.02}),d.push({title:"Stop +2% (semiDiameter×1.02)",intent:"ストップ径の微調整で反応を見る",blocks:f,tag:"stop"})}{const f=tt(c);Or(f,{scale:.98}),d.push({title:"Stop -2% (semiDiameter×0.98)",intent:"逆方向のストップ径微調整で反応を見る",blocks:f,tag:"stop"})}{const f=tt(c);d.push({title:"Material exploration preset",intent:"形状固定でガラス変更の余地を評価（入口）",blocks:f,tag:"material"})}const u=Du(d,o.designType),h=Number(o.candidateCount),g=Number.isFinite(h)?Math.max(1,Math.floor(h)):u.length,y=u.slice(0,g),A=[];for(let f=0;f<y.length;f++){const w=y[f],b=Dr({configId:a,blocks:w.blocks}),v=b.ok?b.total:1/0,M=b.ok?Eu(b.terms):{},C=Pu(c,w.blocks,12),D=b.ok?Array.isArray(b.terms)?b.terms:[]:[];A.push({idx:f+1,...w,ok:b.ok,merit:v,deltaMerit:v-m,metrics:M,diffs:C,baseTerms:p,candTerms:D})}let x=A[0];for(const f of A)f.merit<x.merit&&(x=f);try{_u({version:1,createdAt:Date.now(),configId:String(a),recommendation:{idx:x.idx,title:x.title,blocks:tt(x.blocks)},candidates:A.map(f=>({idx:f.idx,title:f.title,merit:f.merit}))})}catch{}const _=[];_.push("=== Suggest (B案: Candidates + Compare) ==="),_.push(`Time: ${Nu()}`),_.push(`Config: ${s.name||"(unnamed)"} (ID: ${String(s.id)})`),_.push(`Blocks: ${$u(c)}`),_.push(""),_.push(`Current Merit: ${De(m)}`),p.length>0&&_.push(`Current Top: ${Ki(p,Number(o.topK)||2)}`),_.push(`Suggest Options: designType=${Xi(o.designType)}, candidates=${y.length}, outputMode=${String(o.outputMode||"compact")}, topK=${o.topK}, improveK=${o.improveK}, regressK=${o.regressK}`),_.push(`Heuristic: Δc small=${De(i.small)}, large=${De(i.large)} (from current lens curvatures)`),_.push("");for(const f of A)String(o.outputMode||"").toLowerCase()==="detailed"?_.push(zu(f,{topK:o.topK,improveK:o.improveK,regressK:o.regressK})):_.push(Wu(f,{topK:o.topK,improveK:o.improveK,regressK:o.regressK})),_.push("");_.push(`Recommendation: Candidate ${x.idx} (${x.title})`),_.push("Next: このCandidateを初期値として Optimize を実行（数値V→収束後 material V）。");try{const f=$e(x.blocks);if(f?.issues?.length){_.push(""),_.push("Expand Issues (best candidate):");for(const w of f.issues.slice(0,8))_.push(`- [${w.severity}] ${w.phase}: ${w.message}`);f.issues.length>8&&_.push(`- ... (${f.issues.length-8} more)`)}}catch{}const S=_.join(`
`),N=Cu();N?N.value=S:(console.log(S),alert("Suggest 出力先(#system-data)が見つからないため、consoleに出力しました。"))}typeof window<"u"&&(window.SuggestDesignIntent={run:Uu,applyLastRecommendation:async()=>{const t=Ru()?.recommendation?.blocks;return!Array.isArray(t)||t.length===0?{ok:!1,reason:"Suggestion の Recommendation がありません。先に Suggest を実行してください。"}:Mu(t)}});async function Hu(){console.log("🧪 [benchmarkTFMTF] Function called at",new Date().toISOString());const e=performance.now();console.log("🧪 [Benchmark] TFMTF Through-Focus Performance Analysis"),console.log("=".repeat(80)),console.log("⏱️  Benchmark started at",new Date().toISOString());try{console.log("📦 Importing mtf-plot module...");let t;try{t=(await Re(()=>import("./mtf-plot-TU6szzT9.js").then(_=>_.m),__vite__mapDeps([7,0,1]))).showThroughFocusMTFDiagram,console.log("✅ Successfully imported showThroughFocusMTFDiagram")}catch(x){throw console.error("❌ Failed to import mtf-plot.ts:",x),new Error(`Module import failed: ${x}`)}if(!t)throw new Error("showThroughFocusMTFDiagram function not found in module");let n=document.getElementById("tfmtf-benchmark-container");if(!n){n=document.createElement("div"),n.id="tfmtf-benchmark-container",n.style.width="100%",n.style.height="500px",n.style.marginTop="20px",n.style.border="1px solid #ccc",n.style.padding="10px";const x=document.getElementById("mtf-container");x&&x.parentElement?x.parentElement.appendChild(n):document.body.appendChild(n)}console.log(`📊 Running TFMTF benchmark with different worker configurations...
`),console.log("📈 Configuration 1: Baseline (128×128, 5 steps)");const o=performance.now(),r=[];try{await no(t({wavelengthMicrons:.5876,objectIndex:0,targetFrequencyLpmm:30,defocusMinMm:-.05,defocusMaxMm:.05,steps:5,samplingSize:128,zeroPadTo:256,containerElement:n,onProgress:x=>{const _={percent:x.percent,message:x.message??""};r.push(x),Lr("Sequential",_)}}),6e4)}catch(x){console.error("❌ Sequential benchmark failed:",x);const S=performance.now()-o;return console.log(`⏱️  Sequential took ${S.toFixed(2)} ms (before error)`),{success:!1,error:`Sequential benchmark failed: ${x}`,duration:performance.now()-e,partialResults:{sequential:{duration:S,state:"failed"}},timestamp:new Date().toISOString()}}const a=performance.now()-o;console.log(`✅ Sequential completed in ${a.toFixed(2)} ms
`),console.log("📈 Configuration 2: Second run (128×128, 5 steps)");const c=performance.now(),i=[];try{await no(t({wavelengthMicrons:.5876,objectIndex:0,targetFrequencyLpmm:30,defocusMinMm:-.05,defocusMaxMm:.05,steps:5,samplingSize:128,zeroPadTo:256,containerElement:n,onProgress:x=>{const _={percent:x.percent,message:x.message??""};i.push(x),Lr("Parallel",_)}}),6e4)}catch(x){console.error("❌ Parallel benchmark failed:",x);const S=performance.now()-c;return console.log(`⏱️  Parallel took ${S.toFixed(2)} ms (before error)`),{success:!1,error:`Parallel benchmark failed: ${x}`,duration:performance.now()-e,partialResults:{sequential:{duration:a,state:"success"},parallel:{duration:S,state:"failed"}},timestamp:new Date().toISOString()}}const m=performance.now()-c;console.log(`✅ Parallel completed in ${m.toFixed(2)} ms
`),console.log("📈 Configuration 3: Higher-Resolution (256×256, 5 steps)");const p=performance.now();try{await no(t({wavelengthMicrons:.5876,objectIndex:0,targetFrequencyLpmm:30,defocusMinMm:-.05,defocusMaxMm:.05,steps:5,samplingSize:256,zeroPadTo:256,containerElement:n,onProgress:x=>{(x.percent%20===0||x.percent===100)&&console.log(`  ${x.percent}%: ${x.message}`)}}),12e4)}catch(x){console.error("❌ Higher-resolution benchmark failed:",x);const S=performance.now()-p;return console.log(`⏱️  Higher-resolution took ${S.toFixed(2)} ms (before error)`),{success:!1,error:`Higher-resolution benchmark failed: ${x}`,duration:performance.now()-e,partialResults:{sequential:{duration:a,state:"success"},parallel:{duration:m,state:"success"},fullResolution:{duration:S,state:"failed"}},timestamp:new Date().toISOString()}}const u=performance.now()-p;console.log(`✅ Higher-resolution completed in ${u.toFixed(2)} ms
`);const h=performance.now()-e,g=a/Math.max(m,1),y=u/Math.max(a,1);console.log("=".repeat(80)),console.log("📊 BENCHMARK SUMMARY"),console.log("=".repeat(80)),console.log(`Config 1 - 128×128, 11 steps: ${a.toFixed(2)} ms`),console.log(`Config 2 - 128×128, 11 steps: ${m.toFixed(2)} ms`),console.log(`Run-to-run variance:          ${g.toFixed(2)}× (expect ~1.0)`),console.log(""),console.log(`Config 3 - 256×256, 11 steps: ${u.toFixed(2)} ms`),console.log(`256/128 scaling factor:       ${y.toFixed(2)}× (expect ~4× for O(N²) FFT)`),console.log(""),console.log(`Total benchmark time:         ${h.toFixed(2)} ms`),console.log(""),console.log("📝 Phase 1 (Rust FFT) active. Phase 2 worker parallelization pending PSF refactor."),console.log("=".repeat(80));const A={success:!0,config1:{duration:a,steps:5,resolution:128,zeroPadTo:256},config2:{duration:m,steps:5,resolution:128,zeroPadTo:256},config3:{duration:u,steps:5,resolution:256,zeroPadTo:256},runVariance:g,resolutionScaling:y,metadata:{timestamp:new Date().toISOString(),platform:navigator.userAgent,hardwareConcurrency:navigator.hardwareConcurrency||"unknown"}};return console.log("📦 Benchmark Results:",A),typeof window<"u"&&(window.__benchmarkResult=A),A}catch(t){console.error("❌ [Benchmark] Error during TFMTF benchmark:",t);const n=t instanceof Error?t.message:String(t),o=performance.now()-e,r={success:!1,error:n,duration:o,timestamp:new Date().toISOString(),platform:navigator.userAgent,hardwareConcurrency:navigator.hardwareConcurrency||"unknown"};return console.log("📦 Benchmark Error Result:",r),r}}function Lr(e,t){if(t.percent%10===0||t.percent===100){const n=t.message?` - ${t.message}`:"";console.log(`  ${e}: ${t.percent}%${n}`)}}async function Gu(){console.log("🧪 [benchmarkTFMTFQuick] Function called at",new Date().toISOString()),console.log("🧪 [Benchmark] Quick TFMTF Test (No MTF computation)"),console.log("=".repeat(80));const e=performance.now();try{console.log("Test 1: Importing mtf-plot module...");const t=await Re(()=>import("./mtf-plot-TU6szzT9.js").then(a=>a.m),__vite__mapDeps([7,0,1]));console.log("✅ mtf-plot imported"),t.showThroughFocusMTFDiagram?console.log("✅ showThroughFocusMTFDiagram found"):console.warn("⚠️  showThroughFocusMTFDiagram is falsy"),console.log("Test 2: Checking worker pool...");const n=await Re(()=>import("./mtf-plot-TU6szzT9.js").then(a=>a.t),__vite__mapDeps([7,0,1]));if(console.log("✅ tfmtf-worker-pool imported"),n.getGlobalTFMTFWorkerPool){console.log("✅ getGlobalTFMTFWorkerPool found");const a=n.getGlobalTFMTFWorkerPool();console.log("   Worker pool instance:",a?"✅ initialized":"⚠️  not initialized")}console.log("Test 3: Checking PSF serialization...");const o=await Re(()=>import("./psf-serialization-CPSFDk_1.js"),[]);console.log("✅ psf-serialization imported"),o.extractPSFGridFromCalculatorResult&&console.log("✅ PSF serialization utilities found");const s={success:!0,duration:performance.now()-e,timestamp:new Date().toISOString(),tests:{mtfModule:!0,workerPool:!0,psfSerialization:!0}};return console.log("=".repeat(80)),console.log("✅ Quick test passed! Result:",JSON.stringify(s)),typeof window<"u"&&(window.__benchmarkResult=s),s}catch(t){return console.error("❌ Quick test failed:",t),{success:!1,duration:performance.now()-e,error:String(t),timestamp:new Date().toISOString()}}}async function no(e,t){let n;const o=new Promise((r,s)=>{n=setTimeout(()=>{s(new Error(`Operation timed out after ${t}ms`))},t)});try{return await Promise.race([e,o])}finally{n&&clearTimeout(n)}}if(typeof window<"u"){const e=Hu,t=Gu;window.__tfmtfBenchmark=e,window.__tfmtfBenchmarkQuick=t,window.__tfmtfBenchmarkTest=function(){return console.log("✅ __tfmtfBenchmarkTest called"),{test:!0,timestamp:new Date().toISOString()}}}const Pe=window,ne=globalThis.console;let Vu=0,en=null,Nt=null,tn=null;async function Ji(e={}){const t=e.forceRustWasm===!0,n=e.requireRustWasm===!0,o=`${t?"force":"auto"}:${n?"require":"allow"}`,r=Date.now();if(Nt&&Nt.forceKey===o&&r-Nt.at<3e3)return Nt.options;if(tn)return tn;tn=(async()=>{const s=(()=>{try{return typeof window<"u"&&window.__COOPT_ALLOW_TS_ANALYSIS_FALLBACK===!0}catch{return!1}})(),a=(()=>{if(t||!s)return!0;try{return!(typeof window<"u"&&window.__COOPT_DISABLE_RUST_WASM_ANALYSIS===!0)}catch{return!0}})(),c=(()=>{if(!s||n)return!0;try{return typeof window<"u"&&window.__COOPT_REQUIRE_RUST_WASM_ANALYSIS===!0}catch{return!1}})();if(!a)return Nt={options:null,at:Date.now(),forceKey:o},null;try{const i=await Re(()=>import("./toolbar-collapsed-storage-YH65vEW9.js").then(p=>p.bK),__vite__mapDeps([0,1]));if(i.getRustRayTracingWasmSync?.()||await i.preloadRustRayTracingWasm?.()){const p={useRustWasm:!0,requireRustWasm:c,allowNonStrict:!c};try{window.__COOPT_LAST_ANALYSIS_BACKEND={kind:"rust-wasm",forced:t,at:Date.now()},ne.warn("🧭 [Analysis Backend] Rust-WASM")}catch{}return Nt={options:p,at:Date.now(),forceKey:o},p}const m=typeof i.getRustRayTracingWasmInitError=="function"?i.getRustRayTracingWasmInitError():null;if(c)throw new Error(`Rust WASM is unavailable${m?` (${m})`:""}`);try{window.__COOPT_LAST_ANALYSIS_BACKEND={kind:"js",detail:m||"Rust WASM unavailable",forced:t,at:Date.now()},ne.warn(`🧭 [Analysis Backend] JavaScript fallback${m?` (${m})`:""}`)}catch{}return Nt={options:null,at:Date.now(),forceKey:o},null}catch(i){if(c)throw i;try{const l=String(i?.message||i||"Rust WASM init failed");window.__COOPT_LAST_ANALYSIS_BACKEND={kind:"js",detail:l,forced:t,at:Date.now()},ne.warn(`🧭 [Analysis Backend] JavaScript fallback (${l})`)}catch{}return Nt={options:null,at:Date.now(),forceKey:o},null}})();try{return await tn}finally{tn=null}}function Qi(e,t,n=!1){const o=Number(t);if(!Array.isArray(e))return[];const r=e.map(m=>m&&typeof m=="object"?{...m}:m);if(!Number.isFinite(o)||Math.abs(o)<1e-15)return r;const s=r.findIndex(m=>m&&(m["object type"]==="Image"||m.object==="Image")),a=s>0?s-1:Math.max(0,r.length-2);if(a<0||a>=r.length)return r;const c=r[a]&&typeof r[a]=="object"?{...r[a]}:{},i=Number(c.thickness),l=Number.isFinite(i)?i:0;return c.thickness=l+o,r[a]=c,r}function qu(){const e=it();if(!e)return;ne.log("🧹 Clearing all drawing elements...");const t=[];e.children.forEach(n=>{n.type!=="AmbientLight"&&n.type!=="DirectionalLight"&&t.push(n)}),t.forEach(n=>{e.remove(n),n.geometry&&n.geometry.dispose(),n.material&&(Array.isArray(n.material)?n.material.forEach(o=>o.dispose()):n.material.dispose())}),ne.log(`✅ Cleared ${t.length} objects from scene`)}async function go(e={}){const t=e&&typeof e=="object"?e.forceRustWasmTrace===!0:!1,n=e&&typeof e=="object"?e.requireRustWasmTrace===!0:!1;ne.log("🎯 Starting spot diagram generation...");const o=()=>{try{if(typeof performance<"u"&&typeof performance.now=="function")return performance.now()}catch{}return Date.now()},r=o(),s=++Vu,a={requestId:s,startedAt:Date.now(),backend:null,input:null,result:null,timingsMs:{resolveBackend:0,generateData:0,render:0,total:0},status:"running",error:null};try{if(typeof window<"u"&&Pe.__configurationSwitching===!0){en={requestId:s,options:e,requestedAt:Date.now()},ne.warn(`⚠️ Spot diagram requested during configuration switching; queued request ${s}`),setTimeout(()=>{try{typeof window<"u"&&Pe.__configurationSwitching===!0||go(e).catch(()=>{})}catch{}},50);return}}catch{}const c=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null;e&&typeof e=="object"&&typeof e.chiefRayDefinition=="string"&&e.chiefRayDefinition,e&&typeof e=="object"&&e.logChiefRayDefinition,e&&typeof e=="object"&&e.useActiveConfigSnapshot,e&&typeof e=="object"&&e.configId;let i="spot-diagram-container";if(e&&typeof e=="object"&&(e.containerElement?i=e.containerElement:typeof e.containerId=="string"&&e.containerId.trim()!==""&&(i=e.containerId)),Ls()){en={requestId:s,options:e,requestedAt:Date.now()},ne.warn(`⚠️ Spot diagram generation already in progress; queued request ${s}`);return}try{$o(!0);try{c?.({percent:0,message:"Preparing spot diagram..."})}catch{}const l=Number.isInteger(e?.surfaceIndex)?e.surfaceIndex:null,m=Number.isInteger(e?.surfaceRowIndex)?e.surfaceRowIndex:null,p=e&&typeof e=="object"&&e.surfaceRowId!==void 0&&e.surfaceRowId!==null?String(e.surfaceRowId).trim():"",d=e&&typeof e=="object"&&e.surfaceRowSig!==void 0&&e.surfaceRowSig!==null?String(e.surfaceRowSig).trim():"",u=e&&typeof e=="object"?e.surfaceIsImage===!0:!1,h=Number.isInteger(e?.rayCount)?e.rayCount:null,g=Number.isFinite(e?.wavelengthNm)?e.wavelengthNm:null,y=Number.isInteger(e?.ringCount)?e.ringCount:null,A=document.getElementById("surface-number-select"),x=document.getElementById("ray-count-input"),_=document.getElementById("wavelength-input"),S=document.getElementById("ring-count-select"),w=(()=>{try{if(typeof localStorage>"u")return"";const z=ot();return z&&z.activeConfigId!==void 0&&z.activeConfigId!==null?String(z.activeConfigId).trim():""}catch{return""}})();let b=0,v=501,M=550,C=3;if(l!==null&&l>=0)b=l,ne.log(`📊 Using surface id from options: ${b}`);else if(A&&A.value!=="")b=parseInt(A.value,10),ne.log(`📊 Using surface id from select: ${b}`);else{ne.warn("⚠️ Surface select not found, using default (image surface)");const z=at(),V=je(z);V&&V.length>0?(b=V.length-1,ne.log(`📊 Using last surface (fallback) as default: ${b}`)):ne.warn("⚠️ No optical system data available for default surface calculation")}h!==null&&h>0?v=h:x&&x.value!==""?v=parseInt(x.value)||501:ne.warn("⚠️ Ray count input not found, using default (501)");const D=z=>{if(z===!0||z===1)return!0;const V=String(z??"").trim().toLowerCase();return V.includes("primary")||V==="true"||V==="yes"||V==="1"},B=()=>{try{const z=typeof window<"u"&&Pe.tableSource&&typeof Pe.tableSource.getData=="function"?Pe.tableSource.getData():null;if(!Array.isArray(z)||z.length===0)return null;const V=z.map(oe=>({wlUm:Number(oe?.wavelength),isPrimary:D(oe?.primary)})).filter(oe=>Number.isFinite(oe.wlUm)&&oe.wlUm>0),K=V.find(oe=>oe.isPrimary)||V[0]||null;return K?Number(K.wlUm)*1e3:null}catch{return null}},j=(()=>{if(g!==null&&g>0)return g;const z=B();return Number.isFinite(z)&&z>0?z:null})();if(j!==null&&j>0?M=j:ne.log("ℹ️ No explicit wavelength option, will use Source primary wavelength after rows load"),y!==null&&y>0)C=y;else if(S&&S.value!==""){const z=parseInt(S.value,10);C=Number.isInteger(z)&&z>0?z:3}else ne.warn("⚠️ Ring count select not found, using default (3)");(isNaN(b)||b<0)&&(b=0,ne.warn("⚠️ Invalid surface id, using default (0)"));const ce=(()=>{const z=String(e?.pattern||"").trim().toLowerCase();if(z==="grid"||z==="annular")return z;try{const oe=wo();if(oe==="grid"||oe==="annular")return oe}catch{}const V=document.getElementById("annular-pattern-btn"),K=document.getElementById("grid-pattern-btn");if(K&&K.classList.contains("active"))return"grid";if(V&&V.classList.contains("active"))return"annular";try{if(typeof window<"u"&&typeof Pe.getRayEmissionPattern=="function"){const oe=String(Pe.getRayEmissionPattern()||"").trim().toLowerCase();if(oe==="grid"||oe==="annular")return oe}}catch{}try{const oe=Uo(),re=oe&&w?oe[w]:null,fe=re&&typeof re.pattern=="string"?re.pattern.trim().toLowerCase():"";if(fe==="grid"||fe==="annular")return fe}catch{}return"annular"})();try{typeof window<"u"&&typeof Pe.setRayEmissionPattern=="function"&&Pe.setRayEmissionPattern(ce)}catch{}const U=b;ne.log(`🎯 Generating spot diagram for surfaceId ${U}, ${v} rays, ${M}nm, ring count ${C}`);const G=()=>{if(!w){ne.log("🔍 [Load Rows] Using CURRENT tables (no config selected)");const se=at(),pe=gt(),me=yt(),L={opticalSystemRows:je(se),objectRows:Ke(pe),sourceRows:rt(me)};return ne.log(`🔍 [Load Rows] Current: opticalSystem=${L.opticalSystemRows?.length}, objects=${L.objectRows?.length}`),L}ne.log(`🔍 [Load Rows] Loading Config "${w}"`);const V=se=>!!se&&typeof se=="object"&&!Array.isArray(se),K=se=>{try{return JSON.parse(JSON.stringify(se))}catch{return null}};try{if(typeof localStorage>"u")throw new Error("localStorage unavailable");const se=ot(),pe=Array.isArray(se?.configurations)?se.configurations.find(me=>String(me?.id)===String(w)):null;pe?ne.log(`🔍 [Config Content] Config "${w}":`,{hasBlocks:Array.isArray(pe.blocks)&&pe.blocks.length>0,blockCount:pe.blocks?.length||0,hasOpticalSystem:Array.isArray(pe.opticalSystem)&&pe.opticalSystem.length>0,opticalSystemLength:pe.opticalSystem?.length||0,hasObject:Array.isArray(pe.object)&&pe.object.length>0,objectLength:pe.object?.length||0,firstOpticalSurface:pe.opticalSystem?.[0]||null}):ne.error(`❌ [Config Not Found] Config "${w}" not found in localStorage`)}catch(se){ne.error("❌ [Config Read Error]:",se)}const oe=se=>{const pe=String(se??""),me=pe.indexOf(".");if(me<=0)return null;const L=pe.slice(0,me),X=pe.slice(me+1);return!L||!X?null:{blockId:L,key:X}},re=se=>Array.isArray(se)?se.map(pe=>{if(!pe||typeof pe!="object")return pe;const me={...pe};return me.xHeightAngle==null&&me["object x"]!=null&&(me.xHeightAngle=me["object x"]),me.yHeightAngle==null&&me["object y"]!=null&&(me.yHeightAngle=me["object y"]),me.xHeightAngle==null&&me.x!=null&&(me.xHeightAngle=me.x),me.yHeightAngle==null&&me.y!=null&&(me.yHeightAngle=me.y),me.position==null&&me.objectType!=null&&(me.position=me.objectType),me}):[],fe=(se,pe)=>{const me=K(se);if(!Array.isArray(me))return Array.isArray(se)?se:[];if(!V(pe))return me;const L=new Map;for(const X of me){const Y=V(X)?String(X.blockId??""):"";Y&&L.set(Y,X)}for(const[X,Y]of Object.entries(pe)){const J=oe(X);if(!J)continue;const le=L.get(String(J.blockId));if(!le||!V(le.parameters))continue;const Q=Number(Y);le.parameters[J.key]=Number.isFinite(Q)?Q:Y}return me};try{const se=typeof localStorage>"u"?null:ot(),pe=se&&se.activeConfigId!==void 0&&se.activeConfigId!==null?String(se.activeConfigId):"",me=(()=>{try{return typeof window<"u"&&Pe.__configurationSwitching===!0}catch{return!1}})();if(pe&&String(pe)===String(w)&&!me){ne.log("🔍 [Load Rows] Selected config is ACTIVE; using current tables instead of snapshot");const J=at(),le=gt(),Q=yt(),ee={opticalSystemRows:je(J),objectRows:Ke(le),sourceRows:rt(Q)};try{const ie=Array.isArray(se?.configurations)?se.configurations.find(Ce=>String(Ce?.id)===String(w)):null,be=re(Array.isArray(ie?.object)?ie.object:[]),ae=re(Array.isArray(ee.objectRows)?ee.objectRows:[]),he=Ce=>String(Ce?.id??""),de=Ce=>{const Me=new Map;for(const ge of Ce)if(ge&&typeof ge=="object"){const we=he(ge);we&&Me.set(we,ge)}return Me},Ae=de(ae),xe=de(be),Ne="2",ye=Ae.get(Ne),Se=xe.get(Ne),_e=Ce=>String(Ce??"").trim().toLowerCase();if(ye&&Se){const Ce=_e(ye.position),Me=_e(Se.position),ge=Number(ye.yHeightAngle??ye.y??ye["object y"]),we=Number(Se.yHeightAngle??Se.y??Se["object y"]);if(Ce!==Me||Number.isFinite(ge)&&Number.isFinite(we)&&Math.abs(ge-we)>1e-9)return ne.warn("⚠️ [Load Rows] Live Object table differs from config snapshot; using snapshot for Object rows",{live:{position:ye.position,yHeightAngle:ye.yHeightAngle},snapshot:{position:Se.position,yHeightAngle:Se.yHeightAngle}}),{...ee,objectRows:be}}}catch{}return ee}const L=Array.isArray(se?.configurations)?se.configurations.find(J=>String(J?.id)===String(w)):null;try{if(L&&typeof window<"u"&&Pe.__cooptOpticalSystemByConfigId){const J=Pe.__cooptOpticalSystemByConfigId[String(w)];if(Array.isArray(J)&&J.length>0)return ne.log(`🔍 [Cache Hit] Config "${w}": Using cached opticalSystemRows (${J.length} surfaces)`),{opticalSystemRows:JSON.parse(JSON.stringify(J)),objectRows:re(Array.isArray(L?.object)?L.object:[]),sourceRows:(()=>{try{const Q=Ft();return Array.isArray(Q)?Q:[]}catch{return[]}})()};ne.log(`🔍 [Cache Miss] Config "${w}": Cache empty or invalid, will expand blocks`)}else ne.log(`🔍 [No Cache] Config "${w}": No cache object found, will expand blocks`)}catch{}ne.log(`🔍 [Block Expansion] Config "${w}": Expanding blocks to optical system`);const X=(()=>{try{if(!L||!Array.isArray(L.blocks)||L.blocks.length===0)return ne.log(`⚠️ [Block Expansion] Config "${w}": No blocks found (cfg=${!!L}, blocks=${L?.blocks?.length})`),null;const J=Array.isArray(L.scenarios)?L.scenarios:null,le=L.activeScenarioId?String(L.activeScenarioId):"",Q=le&&J?J.find(ae=>ae&&String(ae.id)===String(le)):null,ee=Q&&V(Q.overrides)?Q.overrides:null,ie=ee?fe(L.blocks,ee):L.blocks,be=$e(ie);if(ne.log(`🔍 [Block Expansion] Config "${w}": exp=${!!be}, exp.rows=${be?.rows?.length}`),!be||!Array.isArray(be.rows))return ne.log(`⚠️ [Block Expansion] Config "${w}": Block expansion failed`),null;try{{const he=Array.isArray(L?.opticalSystem)?L.opticalSystem:null,de=be.rows,Ae=ye=>String(ye?.["object type"]??ye?.object??"").trim().toLowerCase(),xe=ye=>{if(!ye)return null;const Se=String(ye);return Array.isArray(ie)?ie.find(_e=>_e&&String(_e.blockId)===Se):null},Ne=ye=>{const _e=xe(ye)?.parameters?.semiDiameter,Ce=Number(_e);return Number.isFinite(Ce)&&Ce>0?Ce:null};if(he&&de.length>0){const ye=he[0];String(ye?.semidia??"").trim()!==""&&(de[0]={...de[0],semidia:ye.semidia});const _e=Math.min(he.length,de.length);for(let Ce=0;Ce<_e;Ce++){const Me=he[Ce],ge=de[Ce];if(!Me||typeof Me!="object"||!ge||typeof ge!="object")continue;const we=Me.semidia;if(String(we??"").trim()==="")continue;const ve=Ae(ge);ve!=="image"&&(ve==="stop"&&Ne(ge._blockId)!==null||(ge.semidia=we))}}}}catch{}try{const ae=be.rows;if(!(Array.isArray(L?.blocks)&&L.blocks.some(de=>String(de?.blockType??"").trim()==="ObjectSurface"))){const de=Array.isArray(L?.opticalSystem)?L.opticalSystem[0]:null;ae.length>0&&de?(ne.log(`🔧 [Object Row Restore] Config "${L?.name||L?.id}": Using saved Object row`),ne.log(`  Old Object: thickness=${ae[0]?.thickness}, fieldX=${ae[0]?.fieldX}, fieldY=${ae[0]?.fieldY}`),ne.log(`  New Object: thickness=${de?.thickness}, fieldX=${de?.fieldX}, fieldY=${de?.fieldY}`),ae[0]={...ae[0],...de}):ne.log(`⚠️ [Object Row Restore] Config "${L?.name||L?.id}": No saved Object row to restore (hasRows=${ae.length>0}, hasLegacyObject=${!!de})`)}}catch(ae){ne.error("❌ [Object Row Restore Error]:",ae)}try{const ae=be.rows;if(ne.log(`🔍 [Image Semidia Override Check] Config "${L?.name||L?.id}", rows.length=${ae.length}`),ae.length>0){const he=at(),de=je(he);ne.log(`🔍 [Image Semidia Override] currentOpticalRows.length=${de?.length}`);const Ae=ye=>String(ye?.["object type"]??ye?.object??"").trim().toLowerCase(),xe=de?.findIndex(ye=>Ae(ye)==="image"),Ne=ae.findIndex(ye=>Ae(ye)==="image");if(ne.log(`🔍 [Image Semidia Override] currentImageIdx=${xe}, configImageIdx=${Ne}`),xe>=0&&Ne>=0){const ye=de[xe]?.semidia,Se=ae[Ne]?.semidia;if(ne.log(`🔍 [Image Semidia Override] current=${ye}, config=${Se}`),ye!=null&&String(ye).trim()!==""){const _e=Number(ye);ne.log(`🔍 [Image Semidia Override] currentVal=${_e}, isFinite=${Number.isFinite(_e)}, gt0=${_e>0}`),Number.isFinite(_e)&&_e>0&&(ne.log(`🔧 [Image Semidia Override] Config "${L?.name||L?.id}", surface ${Ne}: ${Se} → ${ye} (FORCED)`),ae[Ne]={...ae[Ne],semidia:ye})}}}}catch(ae){ne.error("❌ [Image Semidia Override Error]",ae)}try{const ae=be.rows,he=at(),de=je(he);ne.log(`📊 [SEMIDIA COMPARISON] Config "${L?.name||L?.id}"`);const Ae=Math.max(ae?.length||0,de?.length||0);for(let xe=0;xe<Ae;xe++){const Ne=ae?.[xe],ye=de?.[xe],Se=Ne?.semidia,_e=ye?.semidia,Ce=Ne?.surfType||Ne?.["object type"]||Ne?.object,Me=ye?.surfType||ye?.["object type"]||ye?.object;Se!==_e?ne.log(`  ⚠️ Surface ${xe} (${Ce}): config=${Se}, current=${_e}`):ne.log(`  ✅ Surface ${xe} (${Ce}): ${Se}`)}}catch(ae){ne.error("❌ [SEMIDIA COMPARISON Error]",ae)}return be.rows}catch{return null}})(),Y={opticalSystemRows:Array.isArray(X)?X:Array.isArray(L?.opticalSystem)?L.opticalSystem:[],objectRows:re(Array.isArray(L?.object)?L.object:[]),sourceRows:(()=>{try{const J=Ft();return Array.isArray(J)?J:[]}catch{return[]}})()};return ne.log(`🔍 [Load Result] Config "${w}": opticalSystem=${Y.opticalSystemRows?.length} (from ${Array.isArray(X)?"BLOCK EXPANSION":"DIRECT cfg.opticalSystem"}), objects=${Y.objectRows?.length}`),Y}catch(se){ne.warn("⚠️ Failed to load Spot Diagram config snapshot, falling back to active tables:",se);const pe=at(),me=gt(),L=yt();return{opticalSystemRows:je(pe),objectRows:Ke(me),sourceRows:rt(L)}}};let{opticalSystemRows:F,objectRows:W,sourceRows:I}=G();const P=(z=>{if(j!==null&&j>0)return[{id:"spot-explicit-wavelength",wavelength:Number(j)/1e3,weight:1,primary:"Primary Wavelength",name:"Explicit Wavelength"}];if(!Array.isArray(z)||z.length===0)return[{id:"spot-default-wavelength",wavelength:.5876,weight:1,primary:"Primary Wavelength",name:"Default d-line"}];const V=z.map((oe,re)=>({idx:re,row:oe,wl:Number(oe?.wavelength),isPrimary:D(oe?.primary)})).filter(oe=>Number.isFinite(oe.wl)&&oe.wl>0),K=V.find(oe=>oe.isPrimary)||V[0]||null;return K?[{...K.row&&typeof K.row=="object"?K.row:{},wavelength:Number(K.wl),weight:Number.isFinite(Number(K.row?.weight))?Number(K.row.weight):1,primary:"Primary Wavelength"}]:[{id:"spot-default-wavelength",wavelength:.5876,weight:1,primary:"Primary Wavelength",name:"Default d-line"}]})(I),Z=Number(P[0]?.wavelength)||.5876;if(M=Z*1e3,F&&F.length>20){const z=F[20];ne.log("🔍 [IMAGE SURFACE DEBUG] Index=20:",{surfType:z.surfType||z["surf type"],objectType:z["object type"]||z.objectType,semidia:z.semidia,radius:z.radius,thickness:z.thickness})}W&&W.length>0?(ne.log(`🔍 [Object Debug] objectRows.length=${W.length}`),W.forEach((z,V)=>{ne.log(`🔍 [Object Debug] Object ${V+1}:`,{id:z.id,position:z.position,"object x":z["object x"],"object y":z["object y"],angle:z.angle,"decenter y":z["decenter y"],vignetting:z.vignetting})})):ne.warn(`⚠️ [Object Debug] No objectRows found for config "${w}"`);let R=null;const k=z=>{try{const V=se=>String(se??"").trim().toLowerCase(),K=se=>{const pe=Number(se);return Number.isFinite(pe)?String(pe):V(se)},oe=z?.["object type"]??z?.objectType??z?.object??"",re=z?.surfType??z?.["surf type"]??z?.type??"";return[`t:${V(oe||re||"Standard")}`,`r:${K(z?.radius??z?.R??"")}`,`th:${K(z?.thickness??z?.T??"")}`,`sd:${K(z?.semidia??z?.semiDia??"")}`,`m:${V(z?.material??z?.glass??z?.glass??z?.refractiveIndex??"")}`,`c:${V(z?.comment??z?.name??"")}`].join("|")}catch{return""}};if(p){const z=Array.isArray(F)?F.findIndex(V=>V&&V.id!==void 0&&V.id!==null&&String(V.id)===p):-1;Number.isInteger(z)&&z>=0?(R=z,ne.log(`✅ [Surface Resolution] Using explicit surfaceRowId=${p} -> rowIndex=${R}`)):ne.warn(`⚠️ [Surface Resolution] surfaceRowId=${p} not found in current rows`)}if(R===null&&d){const z=Array.isArray(F)?F.findIndex(V=>k(V)===d):-1;Number.isInteger(z)&&z>=0?(R=z,ne.log(`✅ [Surface Resolution] Using explicit surfaceRowSig -> rowIndex=${R}`)):ne.warn("⚠️ [Surface Resolution] surfaceRowSig not found in current rows")}if(Number.isInteger(m)&&m!==null&&(m>=0&&m<F.length?R===null&&(R=m,ne.log(`✅ [Surface Resolution] Using explicit surfaceRowIndex=${R}`)):ne.warn(`⚠️ [Surface Resolution] Ignoring out-of-range surfaceRowIndex=${m} (rows=${F.length})`)),R===null&&u){const z=Array.isArray(F)?F.findIndex(V=>{const K=String(V?.["object type"]??V?.objectType??V?.object??"").toLowerCase(),oe=String(V?.surfType??V?.["surf type"]??V?.type??"").toLowerCase();return K.includes("image")||oe.includes("image")}):-1;Number.isInteger(z)&&z>=0?(R=z,ne.log(`✅ [Surface Resolution] Using explicit Image selection -> rowIndex=${R}`)):ne.warn("⚠️ [Surface Resolution] Explicit Image selection requested but no Image row found")}try{const{generateSurfaceOptions:z}=await Re(async()=>{const{generateSurfaceOptions:re}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(fe=>fe.bP);return{generateSurfaceOptions:re}},__vite__mapDeps([0,1])),V=z(F||[]);if(u&&Array.isArray(V)&&V.length>0){const re=V.find(fe=>{const se=String(fe?.label??"").toLowerCase();return se.includes("(image)")||se.includes(" image")});re&&Number.isInteger(re.rowIndex)?(R=re.rowIndex,ne.log(`✅ [Surface Resolution] Explicit Image selection mapped via options -> rowIndex=${R} (${re.label})`)):Number.isInteger(V[V.length-1]?.rowIndex)&&(R=V[V.length-1].rowIndex,ne.warn(`⚠️ [Surface Resolution] Explicit Image selection fallback -> last option rowIndex=${R}`))}const K=ni(F);ne.log(`🔍 [Surface Resolution] Conjugate: ${K}, Looking for surfaceId=${U} in ${V.length} options`),ne.log("🔍 [Surface Options Sample]:",V.slice(0,3).map(re=>({surfaceId:re.surfaceId,value:re.value,rowIndex:re.rowIndex,label:re.label}))),R===null&&(R=(()=>{const re=V.find(se=>typeof se?.label=="string"&&se.label.includes("(Image)")),fe=V.find(se=>Number(se?.surfaceId)===Number(U));return fe&&Number.isInteger(fe.rowIndex)?(ne.log(`✅ [Surface Resolution] Matched surfaceId=${U} → rowIndex=${fe.rowIndex} (${fe.label})`),fe.rowIndex):(ne.warn(`⚠️ [Surface Resolution] surfaceId=${U} not found in current config; fallback to Image/last`),re&&Number.isInteger(re.rowIndex)?(ne.warn(`⚠️ [Surface Resolution] Using Image surface at rowIndex=${re.rowIndex}`),re.rowIndex):V.length>0&&Number.isInteger(V[V.length-1].rowIndex)?(ne.warn(`⚠️ [Surface Resolution] Using last surface at rowIndex=${V[V.length-1].rowIndex}`),V[V.length-1].rowIndex):null)})()),Number.isInteger(R)&&ne.log(`✅ [Surface Resolution] ${K} resolved: surfaceId=${U} → rowIndex=${R}`)}catch(z){R=Number.isInteger(U)?U:0,ne.error("❌ [Surface Resolution] Error:",z)}Number.isInteger(R)&&R!==null&&(b=R),(R===null||!Number.isInteger(R)||R<0)&&(R=0),b=R;try{const z=ce;let V=Z;j!==null&&j>0||(M=V*1e3),Ds({surfaceId:U,surfaceRowIndex:b,rayCount:v,ringCount:C,pattern:z||null,primaryWavelengthUm:V,configId:w||null,updatedAt:Date.now()});try{let K=w?String(w).trim():"";if(!K){if(typeof localStorage>"u")return;const oe=ot();K=oe&&oe.activeConfigId!==void 0&&oe.activeConfigId!==null?String(oe.activeConfigId).trim():""}if(K){const oe=Uo();oe[K]={surfaceIndex:b,surfaceId:U,surfaceRowIndex:b,rayCount:v,ringCount:C,pattern:z||null,primaryWavelengthUm:V,configId:K,updatedAt:Date.now()},js(oe),typeof window<"u"&&((!Pe.__cooptSpotDiagramSettingsByConfigId||typeof Pe.__cooptSpotDiagramSettingsByConfigId!="object")&&(Pe.__cooptSpotDiagramSettingsByConfigId={}),Pe.__cooptSpotDiagramSettingsByConfigId[K]=oe[K])}}catch{}}catch{}if(ne.log("📊 Retrieved data:",{configId:w||"(Current)"}),ne.log("  - opticalSystemRows:",F?F.length:"null",F),F&&F.length>0?F.forEach((z,V)=>{ne.log(`    [${V}]`,z)}):(ne.warn("⚠️ opticalSystemRows is empty! サンプルデータを自動生成します。"),F=[{surfaceType:"object",radius:"INF",thickness:"INF",refractiveIndex:1,comment:"Object surface"},{surfaceType:"sphere",radius:50,thickness:5,refractiveIndex:1.5,comment:"Lens front"},{surfaceType:"sphere",radius:-50,thickness:10,refractiveIndex:1,comment:"Lens back"},{surfaceType:"image",radius:"INF",thickness:0,refractiveIndex:1,comment:"Image surface"}],ne.log("📊 Generated sample optical system:",F)),ne.log("  - objectRows:",W?W.length:"null",W),ne.log("  - sourceRows:",I?I.length:"null",I),ne.log("  - effectiveSourceRowsForSpot:",P),F&&F.length>0){const z=F.length-1;b>z&&(ne.warn(`⚠️ Surface index ${b} is too large, using last surface (${z})`),b=z)}const $=b+1,te=o(),q=await Ji({forceRustWasm:t,requireRustWasm:n});if(a.timingsMs.resolveBackend=Math.max(0,o()-te),a.backend=(()=>{try{const z=typeof window<"u"?Pe.__COOPT_LAST_ANALYSIS_BACKEND:null;if(z&&typeof z=="object")return z}catch{}return{kind:q&&q.useRustWasm===!0?"rust-wasm":"js",at:Date.now()}})(),a.input={surfaceId:U,surfaceIndex:b,surfaceNumber:$,rayCount:v,ringCount:C,wavelengthNm:M,pattern:ce,objectCount:Array.isArray(W)?W.length:0,opticalSurfaceCount:Array.isArray(F)?F.length:0},ne.log(`🎯 Final surface resolution: surfaceId(input)=${U} → rowIndex=${b} → surfaceNumber=${$}`),ne.log("🎯 Target surface:",F[b]),!F||F.length===0)throw new Error("No optical system data available");if(!W||W.length===0){ne.warn("⚠️ No object data available, creating default object data");const z=[{id:1,height:10,distance:100,angle:0,wavelength:M/1e3,primary:!0}];ne.log("📊 Using default object data:",z);const{generateSpotDiagramAsync:V,drawSpotDiagram:K}=await Re(async()=>{const{generateSpotDiagramAsync:se,drawSpotDiagram:pe}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(me=>me.bP);return{generateSpotDiagramAsync:se,drawSpotDiagram:pe}},__vite__mapDeps([0,1])),oe=o(),re=await V(F,P,z,$,v,C,{onProgress:c,physicalVignetting:!0,displaySurfaceNumber:U,pattern:ce,traceOptions:q});if(a.timingsMs.generateData+=Math.max(0,o()-oe),!re)throw new Error("Failed to generate spot diagram data");a.result={objectCount:Array.isArray(re?.spotData)?re.spotData.length:0,totalRays:Array.isArray(re?.spotData)?re.spotData.reduce((se,pe)=>se+(Number(pe?.totalRays)||0),0):0,successfulRays:Array.isArray(re?.spotData)?re.spotData.reduce((se,pe)=>se+(Number(pe?.successfulRays)||0),0):0,generationProfile:re&&typeof re=="object"&&re.profile?re.profile:null};try{c?.({percent:90,message:"Rendering..."})}catch{}const fe=o();await K(re,$,i,M/1e3),a.timingsMs.render+=Math.max(0,o()-fe);try{c?.({percent:100,message:"Done"})}catch{}}else{const{generateSpotDiagramAsync:z,drawSpotDiagram:V}=await Re(async()=>{const{generateSpotDiagramAsync:fe,drawSpotDiagram:se}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(pe=>pe.bP);return{generateSpotDiagramAsync:fe,drawSpotDiagram:se}},__vite__mapDeps([0,1])),K=o(),oe=await z(F,P,W,$,v,C,{onProgress:c,physicalVignetting:!0,displaySurfaceNumber:U,pattern:ce,traceOptions:q});if(a.timingsMs.generateData+=Math.max(0,o()-K),!oe)throw new Error("Failed to generate spot diagram data");a.result={objectCount:Array.isArray(oe?.spotData)?oe.spotData.length:0,totalRays:Array.isArray(oe?.spotData)?oe.spotData.reduce((fe,se)=>fe+(Number(se?.totalRays)||0),0):0,successfulRays:Array.isArray(oe?.spotData)?oe.spotData.reduce((fe,se)=>fe+(Number(se?.successfulRays)||0),0):0,generationProfile:oe&&typeof oe=="object"&&oe.profile?oe.profile:null},ne.log("📋 [SPOT DIAGRAM] About to call drawSpotDiagram with:",{spotDataType:typeof oe,spotDataKeys:oe?Object.keys(oe):"null",actualSpotDataLength:oe.spotData?oe.spotData.length:"null",surfaceNumber:$,containerId:typeof i=="string"?i:"(element)",wavelength:M/1e3});try{c?.({percent:90,message:"Rendering..."})}catch{}const re=o();await V(oe,$,i,M/1e3),a.timingsMs.render+=Math.max(0,o()-re);try{c?.({percent:100,message:"Done"})}catch{}ne.log("✅ [SPOT DIAGRAM] drawSpotDiagram call completed")}a.timingsMs.total=Math.max(0,o()-r),a.status="ok";try{if(typeof window<"u"){Pe.__cooptLastSpotDiagramProfile=a;const V=(Array.isArray(Pe.__cooptSpotDiagramProfileHistory)?Pe.__cooptSpotDiagramProfileHistory:[]).concat([a]);Pe.__cooptSpotDiagramProfileHistory=V.slice(Math.max(0,V.length-20))}}catch{}ne.warn("⏱️ [SPOT PROFILE]",{backend:a?.backend?.kind||"unknown",totalMs:Number(a?.timingsMs?.total||0).toFixed(1),resolveBackendMs:Number(a?.timingsMs?.resolveBackend||0).toFixed(1),generateDataMs:Number(a?.timingsMs?.generateData||0).toFixed(1),generateCloneRowsMs:Number(a?.result?.generationProfile?.timingsMs?.cloneRows||0).toFixed(1),generateStartsMs:Number(a?.result?.generationProfile?.timingsMs?.generateStarts||0).toFixed(1),generateTraceRayMs:Number(a?.result?.generationProfile?.timingsMs?.traceRay||0).toFixed(1),generateNonTraceMs:Number(a?.result?.generationProfile?.timingsMs?.nonTrace||0).toFixed(1),renderMs:Number(a?.timingsMs?.render||0).toFixed(1),traceRayCalls:Number(a?.result?.generationProfile?.counters?.traceRayCalls||0),pupilAttempts:Number(a?.result?.generationProfile?.counters?.pupilAttempts||0),startGenerationCacheHits:Number(a?.result?.generationProfile?.counters?.startGenerationCacheHits||0),startGenerationCacheMisses:Number(a?.result?.generationProfile?.counters?.startGenerationCacheMisses||0),raysTried:Number(a?.result?.generationProfile?.counters?.raysTried||0),objects:Number(a?.result?.objectCount||0),rays:Number(a?.result?.successfulRays||0)+"/"+Number(a?.result?.totalRays||0)}),ne.log("✅ Spot diagram generated successfully")}catch(l){a.timingsMs.total=Math.max(0,o()-r),a.status="error",a.error=String(l?.message||l||"unknown error");try{if(typeof window<"u"){Pe.__cooptLastSpotDiagramProfile=a;const d=(Array.isArray(Pe.__cooptSpotDiagramProfileHistory)?Pe.__cooptSpotDiagramProfileHistory:[]).concat([a]);Pe.__cooptSpotDiagramProfileHistory=d.slice(Math.max(0,d.length-20))}}catch{}ne.error("❌ Error generating spot diagram:",l),ne.error("Error details:",l.stack);const m=typeof i=="string"?document.getElementById(i):i;m&&(m.innerHTML=`<div style="padding: 20px; color: red; font-family: Arial;">
                <strong>Spot diagram error:</strong><br>
                ${l.message}<br>
                <small style="color: #888;">Check console for details</small>
            </div>`),alert(`Spot diagram error:
${l.message}`)}finally{$o(!1);try{const l=en;l&&l.requestId>s?(en=null,setTimeout(()=>{go(l.options).catch(m=>{ne.error("❌ Error running queued spot diagram request:",m)})},0)):l&&l.requestId===s&&(en=null)}catch{}}}async function es(e={}){try{if(typeof window<"u"&&Pe.__configurationSwitching===!0){ne.warn("⚠️ Through-Focus Spot requested during configuration switching; retrying shortly"),setTimeout(()=>{try{typeof window<"u"&&Pe.__configurationSwitching===!0||es(e).catch(()=>{})}catch{}},60);return}}catch{}const t=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null;let n="through-focus-spot-container";e&&typeof e=="object"&&(e.containerElement?n=e.containerElement:typeof e.containerId=="string"&&e.containerId.trim()!==""&&(n=e.containerId));const o=(i,l)=>{try{t?.({percent:i,message:l})}catch{}},r=(i,l)=>{const m=parseInt(String(i??""),10);return Number.isInteger(m)?m:l},s=(i,l)=>{const m=Number(i);return Number.isFinite(m)?m:l},a=(i,l,m)=>Math.max(l,Math.min(m,i)),c=i=>!Number.isFinite(i)||i<=0?"#1f77b4":i<.45?"#8B00FF":i<.495?"#0000FF":i<.57?"#00CC44":i<.59?"#C6C400":i<.62?"#FF8800":"#FF0000";try{try{const X=Pe.loadActiveConfigurationToTables;typeof X=="function"&&(await X({applyToUI:!0,suppressOpticalSystemDataChanged:!0}),await new Promise(Y=>setTimeout(Y,0)))}catch{}const i=at(),l=gt(),m=yt();let p=je(i),d=Ke(l);const u=rt(m);try{const X=ot(),Y=Array.isArray(X?.configurations)?X.configurations:[],J=Y.find(le=>String(le?.id)===String(X?.activeConfigId))||Y[0]||null;if(J){let le=Array.isArray(J?.opticalSystem)?J.opticalSystem.map(ee=>ee&&typeof ee=="object"?{...ee}:ee):[];if(Array.isArray(J?.blocks)&&J.blocks.length>0){const ee=$e(J.blocks);Array.isArray(ee?.rows)&&ee.rows.length>0&&(le=ee.rows.map(ie=>ie&&typeof ie=="object"?{...ie}:ie))}const Q=Array.isArray(J?.object)?J.object.map(ee=>ee&&typeof ee=="object"?{...ee}:ee):[];le.length>0&&(p=le),Q.length>0&&(d=Q)}}catch{}if(!Array.isArray(p)||p.length===0)throw new Error("No optical system data available");if(!Array.isArray(d)||d.length===0)throw new Error("No object data available");const h=document.getElementById("surface-number-select"),g=document.getElementById("ray-count-input"),y=document.getElementById("ring-count-select"),A=Number.isInteger(e?.surfaceIndex)?Number(e.surfaceIndex):r(h?.value,0),x=a(Number.isInteger(e?.rayCount)?Number(e.rayCount):r(g?.value,101),1,20001),_=a(Number.isInteger(e?.ringCount)?Number(e.ringCount):r(y?.value,10),1,64),S=s(e?.defocusMinMm,-.1),N=s(e?.defocusMaxMm,.1),f=a(r(e?.steps,5),3,61),b=Math.max(1,s(e?.scaleUm,100))*.5,M=String(e?.wavelengthMode||"all").trim().toLowerCase()==="primary"?"primary":"all",{generateSurfaceOptions:C}=await Re(async()=>{const{generateSurfaceOptions:X}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(Y=>Y.bP);return{generateSurfaceOptions:X}},__vite__mapDeps([0,1])),{runNativeSpotRaytrace:D}=await Re(async()=>{const{runNativeSpotRaytrace:X}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(Y=>Y.bU);return{runNativeSpotRaytrace:X}},__vite__mapDeps([0,1])),B=C(p||[]);let j=0;const H=Array.isArray(B)?B.find(X=>Number(X?.surfaceId)===Number(A)):null;if(H&&Number.isInteger(H.rowIndex))j=H.rowIndex;else{const X=Array.isArray(B)?B.find(Y=>typeof Y?.label=="string"&&Y.label.includes("(Image)")):null;X&&Number.isInteger(X.rowIndex)?j=X.rowIndex:j=Math.max(0,p.length-1)}const ce=j+1,U=Array.from({length:f},(X,Y)=>{if(f<=1)return S;const J=Y/(f-1);return S+J*(N-S)}),G=!Array.isArray(u)||u.length===0?[]:u.map((X,Y)=>{const J=Number(X?.wavelength);if(!Number.isFinite(J)||J<=0)return null;const Q=String(X?.primary||"").toLowerCase().includes("primary");return{...X,wavelength:J,__wlIndex:Y,__isPrimary:Q,__label:`${(J*1e3).toFixed(1)} nm${Q?" (primary)":""}`}}).filter(Boolean),F=G.find(X=>X?.__isPrimary)||(G.length>0?G[0]:null),W=M==="primary"?F?[F]:[{wavelength:.5876,weight:1,__isPrimary:!0,__label:"587.6 nm (primary)"}]:G.length>0?G:[{wavelength:.5876,weight:1,__isPrimary:!0,__label:"587.6 nm (primary)"}],I=(X,Y)=>{const J=X&&X.id!==void 0?String(X.id).trim():"";if(J)return J;const le=X&&X.object!==void 0?String(X.object).trim():"";if(le)return le;const Q=X&&X.position!==void 0?String(X.position).trim():"";return Q?`Object ${Y+1} (${Q})`:`Object ${Y+1}`},E=X=>{const Y=String(X||"").trim(),J=Y.match(/(\d+(?:\.\d+)?)\s*nm/i);return J&&J[1]?`Wavelength ${J[1]}nm`:Y.toLowerCase().includes("primary")?"Wavelength Primary":`Wavelength ${Y}`},P=(X,Y)=>{const J=Number(X?.wavelengthUm);return Number.isFinite(J)&&J>0?`Wavelength ${(J*1e3).toFixed(1)}nm`:E(Y)},Z=(X,Y)=>{const J=String(X||"").trim();if(!J)return{objectLabel:Y,wavelengthLabel:"Wavelength Primary"};const le=J.match(/(Primary(?:\s*\([^)]*\))?|\d+(?:\.\d+)?\s*nm)\s*$/i);if(le&&le[1]){const Q=String(le[1]||"Primary");return{objectLabel:J.slice(0,Math.max(0,le.index||0)).replace(/[|@\-\s]+$/,"").trim()||Y,wavelengthLabel:E(Q)}}return{objectLabel:J,wavelengthLabel:"Wavelength Primary"}},R=d.map((X,Y)=>I(X,Y)),k=new Map(R.map((X,Y)=>[X,Y])),$=Array.from({length:d.length},()=>[]),te=[],q=String(e?.pattern||"").trim().toLowerCase(),z=q==="grid"||q==="annular"?q:wo();for(let X=0;X<U.length;X++){const Y=U[X],J=Math.floor(X/Math.max(1,U.length)*90);o(J,`Defocus ${Y.toFixed(4)} mm (${X+1}/${U.length})`);const le=Qi(p,Y),Q=[];for(let de=0;de<d.length;de++){const Ae=d[de]||{},xe=R[de]||I(Ae,de),Ne=String(Ae.position??Ae.object??Ae.Object??"").trim().toLowerCase()==="angle";for(let ye=0;ye<W.length;ye++){const Se=W[ye],_e=Number(Se?.wavelength),Ce=String(Se?.__label||`${(_e*1e3).toFixed(1)} nm`),Me=jn(Ae,le,x,null,{annularRingCount:_,wavelengthUm:_e,pattern:z}),ge=(Array.isArray(Me)?Me:[]).map(we=>({startP:{x:Number(we?.startP?.x)||0,y:Number(we?.startP?.y)||0,z:Number(we?.startP?.z)||0},dir:{x:Number(we?.dir?.x)||0,y:Number(we?.dir?.y)||0,z:Number(we?.dir?.z)||1},wavelengthUm:_e,pupilU:Number.isFinite(Number(we?.planeCoords?.u))?Number(we.planeCoords.u):void 0,pupilV:Number.isFinite(Number(we?.planeCoords?.v))?Number(we.planeCoords.v):void 0,isChief:we?.isChief===!0||we?.isChief==null&&(we?.rayIndex===0||we?.index===0)})).filter(we=>Number.isFinite(we.startP.x)&&Number.isFinite(we.startP.y)&&Number.isFinite(we.startP.z));ge.length!==0&&Q.push({label:`${xe} ${Ce}`,color:c(_e),hasFieldAngle:Ne,rays:ge})}}const ee=await D({opticalSystemRows:le,sourceRows:W,objectRows:d,surfaceIndex:j,rayCount:x,ringCount:_,pattern:z,wavelengthMode:M,raySeries:Q}),ie=Array.isArray(ee?.seriesStats)?ee.seriesStats:[];for(const de of ie)te.push({backend:String(ee?.backend||"unknown"),defocusMm:Number(Y),label:String(de?.label||""),attemptedRays:Number(de?.attemptedRays||0),hitRays:Number(de?.hitRays||0),missRays:Number(de?.missRays||0),apertureBlockRays:Number(de?.apertureBlockRays||0),noIntersectionRays:Number(de?.noIntersectionRays||0),tirRays:Number(de?.tirRays||0),unknownFailRays:Number(de?.unknownFailRays||0),statusCounts:de&&typeof de.statusCounts=="object"&&!Array.isArray(de.statusCounts)?de.statusCounts:{},hitRatePercent:Number(de?.hitRatePercent||0)});const be=new Map,ae=Array.isArray(ee?.series)?ee.series:[],he=Math.max(1,W.length);for(let de=0;de<ae.length;de++){const Ae=ae[de]||{},xe=Math.floor(de/he),Ne=R[xe]||`Object ${xe+1}`,ye=Z(Ae?.label,Ne),Se=k.has(ye.objectLabel)?Number(k.get(ye.objectLabel)):Math.max(0,Math.min(d.length-1,xe)),_e=P(Ae,ye.wavelengthLabel),Ce=(Array.isArray(Ae?.points)?Ae.points:[]).map(Me=>({xUm:Number(Me?.xUm),yUm:Number(Me?.yUm)})).filter(Me=>Number.isFinite(Me.xUm)&&Number.isFinite(Me.yUm));be.has(Se)||be.set(Se,[]),be.get(Se).push({key:_e,label:_e,color:String(Ae?.color||c(Number(Ae?.wavelengthUm))),points:Ce})}for(let de=0;de<d.length;de++){const Ae=Array.isArray(be.get(de))?be.get(de):[],xe=[];for(const Se of Ae){const _e=Array.isArray(Se?.points)?Se.points:[];for(const Ce of _e)xe.push({xUm:Number(Ce?.xUm)||0,yUm:Number(Ce?.yUm)||0})}let Ne=0,ye=0;xe.length>0&&(Ne=xe.reduce((Se,_e)=>Se+_e.xUm,0)/xe.length,ye=xe.reduce((Se,_e)=>Se+_e.yUm,0)/xe.length),$[de].push({shiftMm:Y,pointsByWavelength:Ae.map(Se=>({key:Se.key,label:Se.label,color:Se.color,points:(Array.isArray(Se.points)?Se.points:[]).map(_e=>({xUm:(Number(_e?.xUm)||0)-Ne,yUm:(Number(_e?.yUm)||0)-ye}))}))})}}try{Pe.__cooptTfSpotLastTraceStats=te}catch{}try{globalThis.__cooptTfSpotLastTraceStats=te}catch{}try{ne.table?ne.table(te):ne.log("[TFSD_TRACE_STATS]",te)}catch{}const V=typeof n=="string"?document.getElementById(n):n;if(!V)throw new Error("Through-Focus Spot container element not found");const oe=(V?.ownerDocument?.defaultView||window)?.Plotly||window?.Plotly;if(!oe||typeof oe.newPlot!="function")throw new Error("Plotly is not available");o(92,"Building plot...");const re=d.length,fe=U.length,se=[],pe={title:{text:"Through-Focus Spot Diagram",x:.5,xanchor:"center",y:.98,yanchor:"top"},showlegend:!0,grid:{rows:re,columns:fe,pattern:"independent"},margin:{l:60,r:20,t:95,b:60},paper_bgcolor:"#ffffff",plot_bgcolor:"#ffffff",height:Math.max(420,re*145+90),legend:{orientation:"h",yanchor:"bottom",y:1.06,xanchor:"center",x:.5},legendgroupclick:"togglegroup"},me=new Set,L=new Map;for(let X=0;X<re;X++)for(let Y=0;Y<fe;Y++){const J=X*fe+Y+1,le=J===1?"x":`x${J}`,Q=J===1?"y":`y${J}`,ee=J===1?"xaxis":`xaxis${J}`,ie=J===1?"yaxis":`yaxis${J}`,be=$?.[X]?.[Y]||{pointsByWavelength:[]},ae=Array.isArray(be.pointsByWavelength)?be.pointsByWavelength:[];for(const he of ae){const de=Array.isArray(he?.points)?he.points:[],Ae=String(he?.key||he?.label||"wavelength");L.has(Ae)||L.set(Ae,{label:String(he?.label||Ae),color:String(he?.color||"blue")}),se.push({x:de.map(xe=>xe.xUm),y:de.map(xe=>xe.yUm),mode:"markers",type:"scattergl",name:String(he?.label||Ae),legendgroup:Ae,showlegend:!1,marker:{size:3,color:String(he?.color||"blue"),opacity:.75},xaxis:le,yaxis:Q,hovertemplate:"x=%{x:.2f} µm<br>y=%{y:.2f} µm<extra></extra>"}),me.add(Ae)}pe[ee]={range:[-b,b],showgrid:!0,zeroline:!0,showticklabels:X===re-1,title:X===re-1?`${U[Y].toFixed(3)} mm`:""},pe[ie]={range:[-b,b],showgrid:!0,zeroline:!0,showticklabels:Y===0,title:Y===0?`Field ${X+1}`:"",scaleanchor:le,scaleratio:1}}for(const[X,Y]of L.entries())se.push({x:[null],y:[null],mode:"markers",type:"scatter",name:Y.label,legendgroup:X,showlegend:!0,marker:{size:8,color:Y.color,symbol:"circle"},hoverinfo:"skip"});o(98,"Rendering plot..."),await oe.newPlot(V,se,pe,{responsive:!0,displaylogo:!1}),o(100,"Done")}catch(i){const l=typeof n=="string"?document.getElementById(n):n;throw l&&(l.innerHTML=`<div style="padding:20px;color:red;font-family:Arial;">Failed to generate Through-Focus Spot Diagram.<br>${String(i?.message||i)}</div>`),i}}async function Zu(e={}){const t=(Y,J)=>{const le=parseInt(String(Y??""),10);return Number.isInteger(le)?le:J},n=(Y,J)=>{const le=Number(Y);return Number.isFinite(le)?le:J},o=(Y,J,le)=>Math.max(J,Math.min(le,Y)),r=Y=>{const J=Array.isArray(Y)?Y.map(ee=>({x:Number(ee?.x),y:Number(ee?.y)})).filter(ee=>Number.isFinite(ee.x)&&Number.isFinite(ee.y)):[];if(J.length===0)return[];const le=J.reduce((ee,ie)=>ee+ie.x,0)/J.length,Q=J.reduce((ee,ie)=>ee+ie.y,0)/J.length;return J.map(ee=>({xUm:(ee.x-le)*1e3,yUm:(ee.y-Q)*1e3}))},s=Y=>{if(!Array.isArray(Y)||Y.length===0)return{count:0,rmsUm:NaN,diaUm:NaN};const J=Y.map(ee=>ee.xUm*ee.xUm+ee.yUm*ee.yUm),le=Math.sqrt(Math.max(...J)),Q=J.reduce((ee,ie)=>ee+ie,0)/J.length;return{count:Y.length,rmsUm:Math.sqrt(Q),diaUm:2*le}},a=(Y,J)=>Array.from(new Set([...Array.from(Y.keys()),...Array.from(J.keys())])).sort().map(Q=>{const ee=Y.get(Q)||{},ie=J.get(Q)||{},be=Number(ee.rmsUm),ae=Number(ie.rmsUm),he=Number(ee.diaUm),de=Number(ie.diaUm);return{key:Q,tsCount:Number(ee.count||0),rustCount:Number(ie.count||0),tsRmsUm:be,rustRmsUm:ae,deltaRmsUm:Number.isFinite(be)&&Number.isFinite(ae)?ae-be:NaN,tsDiaUm:he,rustDiaUm:de,deltaDiaUm:Number.isFinite(he)&&Number.isFinite(de)?de-he:NaN}}),c=Y=>{const J=Number(Y);return Number.isFinite(J)?J:null},i=Y=>{const J=(Y||[]).map(ae=>c(ae?.deltaRmsUm)).filter(ae=>ae!==null).map(ae=>Math.abs(ae)),le=(Y||[]).map(ae=>c(ae?.deltaDiaUm)).filter(ae=>ae!==null).map(ae=>Math.abs(ae)),Q=(Y||[]).map(ae=>c(ae?.deltaRmsUm)).filter(ae=>ae!==null),ee=(Y||[]).map(ae=>c(ae?.deltaDiaUm)).filter(ae=>ae!==null),ie=ae=>ae.length>0?ae.reduce((he,de)=>he+de,0)/ae.length:NaN,be=ae=>ae.length>0?Math.max(...ae):NaN;return{rowCount:(Y||[]).length,meanAbsDeltaRmsUm:ie(J),maxAbsDeltaRmsUm:be(J),meanAbsDeltaDiaUm:ie(le),maxAbsDeltaDiaUm:be(le),signedMeanDeltaRmsUm:ie(Q),signedMeanDeltaDiaUm:ie(ee)}},l=(Y,J,le)=>(Y||[]).map(ee=>({...ee,__abs:Math.abs(Number(ee?.[J]))})).filter(ee=>Number.isFinite(ee.__abs)).sort((ee,ie)=>ie.__abs-ee.__abs).slice(0,le).map(({__abs:ee,...ie})=>({...ie,absDelta:ee})),m=Y=>{const[J,le]=String(Y||"").split("|").map(ee=>ee.trim()),Q=Number(J);return{defocusMm:Number.isFinite(Q)?Q:NaN,wavelengthLabel:le||""}},p=Y=>{const J=new Map;for(const Q of Y||[]){const ie=m(String(Q?.key||"")).wavelengthLabel||"unknown";J.has(ie)||J.set(ie,{absRms:[],absDia:[]});const be=J.get(ie),ae=c(Q?.deltaRmsUm),he=c(Q?.deltaDiaUm);ae!==null&&be.absRms.push(Math.abs(ae)),he!==null&&be.absDia.push(Math.abs(he))}const le=Q=>Q.length>0?Q.reduce((ee,ie)=>ee+ie,0)/Q.length:NaN;return Array.from(J.entries()).map(([Q,ee])=>({wavelengthLabel:Q,meanAbsDeltaRmsUm:le(ee.absRms),meanAbsDeltaDiaUm:le(ee.absDia),sampleCount:Math.max(ee.absRms.length,ee.absDia.length)})).sort((Q,ee)=>{const ie=Number.isFinite(Q.meanAbsDeltaRmsUm)?Q.meanAbsDeltaRmsUm:-1;return(Number.isFinite(ee.meanAbsDeltaRmsUm)?ee.meanAbsDeltaRmsUm:-1)-ie})},d=(Y,J,le)=>{const Q=[],ee=[...Y||[],...J||[]].filter(he=>Number(he?.tsCount||0)!==Number(he?.rustCount||0));ee.length>0&&Q.push({area:"sampling",reason:`TS/Rust の点数不一致が ${ee.length} ケース`,action:"Rust 側の rayCount clamp・grid side 算出・annular 点生成順を TS 実装と完全一致させる"});const ie=Number(le?.throughFocus?.signedMeanDeltaRmsUm);Number.isFinite(ie)&&Math.abs(ie)>.5&&Q.push({area:"spot-scale",reason:`through-focus の平均RMS差が ${ie.toFixed(3)} µm`,action:ie>0?"Rust の spot σ 係数（base/defocus gain）を小さくして全体幅を縮小":"Rust の spot σ 係数（base/defocus gain）を大きくして全体幅を拡大"});const be=Number(le?.spotDiagram?.maxAbsDeltaRmsUm),ae=Number(le?.throughFocus?.maxAbsDeltaRmsUm);return(Number.isFinite(be)&&be>3||Number.isFinite(ae)&&ae>3)&&Q.push({area:"phase-to-spot model",reason:`最大RMS差が大きい (spot=${Number.isFinite(be)?be.toFixed(3):"n/a"} µm, tf=${Number.isFinite(ae)?ae.toFixed(3):"n/a"} µm)`,action:"PSF二次モーメント→spot σ 変換の倍率と anisotropy 項を波長・defocusごとに再フィットする"}),Q};try{if(!(await Re(()=>import("./toolbar-collapsed-storage-YH65vEW9.js").then(J=>J.bL),__vite__mapDeps([0,1]))).isTauriRuntime())throw new Error("runSpotParityDiagnostics requires Tauri desktop runtime")}catch(Y){throw new Error(String(Y?.message||Y||"runtime check failed"))}const{runAnalysisCompute:u}=await Re(async()=>{const{runAnalysisCompute:Y}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(J=>J.bU);return{runAnalysisCompute:Y}},__vite__mapDeps([0,1])),{generateSurfaceOptions:h,generateSpotDiagramAsync:g}=await Re(async()=>{const{generateSurfaceOptions:Y,generateSpotDiagramAsync:J}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(le=>le.bP);return{generateSurfaceOptions:Y,generateSpotDiagramAsync:J}},__vite__mapDeps([0,1])),y=at(),A=gt(),x=yt(),_=je(y),S=Ke(A),N=rt(x);if(!Array.isArray(_)||_.length===0)throw new Error("No optical system rows available");if(!Array.isArray(S)||S.length===0)throw new Error("No object rows available");const f=o(t(e?.objectIndex,0),0,S.length-1),w=S[f]||S[0],b=h(_||[]),v=Number.isInteger(e?.surfaceIndex)?Number(e.surfaceIndex):0,M=Array.isArray(b)?b.find(Y=>Number(Y?.surfaceId)===Number(v)):null,C=Number.isInteger(M?.rowIndex)?Number(M.rowIndex):Math.max(0,_.length-1),D=C+1,B=o(t(e?.rayCount,501),9,20001),j=o(t(e?.ringCount,10),1,64),H=String(e?.pattern||wo()||"annular").toLowerCase()==="grid"?"grid":"annular",ce=String(e?.wavelengthMode||"all").toLowerCase()==="primary"?"primary":"all",U=n(e?.defocusMinMm,-.1),G=n(e?.defocusMaxMm,.1),F=o(t(e?.steps,5),3,61),W=Math.max(1,n(e?.scaleUm,100)),I=!Array.isArray(N)||N.length===0?[]:N.map(Y=>{const J=Number(Y?.wavelength);if(!Number.isFinite(J)||J<=0)return null;const Q=String(Y?.primary||"").toLowerCase().includes("primary");return{...Y,wavelength:J,__isPrimary:Q,__label:`${(J*1e3).toFixed(1)} nm${Q?" (primary)":""}`}}).filter(Boolean),E=I.find(Y=>Y?.__isPrimary)||(I.length>0?I[0]:null),P=ce==="primary"?E?[E]:[{wavelength:.5876,__label:"587.6 nm (primary)"}]:I.length>0?I:[{wavelength:.5876,__label:"587.6 nm (primary)"}],Z=await Ji({forceRustWasm:e?.forceRustWasm===!0,requireRustWasm:e?.requireRustWasm!==!1}),R=new Map;for(const Y of P){const J=String(Y?.__label||`${(Number(Y?.wavelength||.5876)*1e3).toFixed(1)} nm`),Q=(await g(_,[Y],[w],D,B,j,{onProgress:null,physicalVignetting:!0,displaySurfaceNumber:v,pattern:H,traceOptions:Z}))?.spotData?.[0]?.spotPoints||[],ee=r(Q);R.set(J,s(ee))}const k=await u({kind:"spot-diagram",opticalSystemRows:_,sourceRows:N,objectRows:[w],surfaceIndex:C,rayCount:B,ringCount:j,pattern:H,wavelengthMode:ce}),$=new Map,te=Array.isArray(k?.spotDiagramSeries)?k.spotDiagramSeries:[];for(const Y of te){const J=String(Y?.label||"series"),le=Array.isArray(Y?.points)?Y.points.map(Q=>({xUm:Number(Q?.xUm),yUm:Number(Q?.yUm)})).filter(Q=>Number.isFinite(Q.xUm)&&Number.isFinite(Q.yUm)):[];$.set(J,s(le))}const q=Array.from({length:F},(Y,J)=>{if(F<=1)return U;const le=J/(F-1);return U+le*(G-U)}),z=new Map;for(const Y of q){const J=Qi(_,Y);for(const le of P){const Q=String(le?.__label||`${(Number(le?.wavelength||.5876)*1e3).toFixed(1)} nm`),ee=`${Y.toFixed(6)} | ${Q}`,be=(await g(J,[le],[w],D,B,j,{onProgress:null,physicalVignetting:!0,displaySurfaceNumber:v,pattern:H,traceOptions:Z}))?.spotData?.[0]?.spotPoints||[],ae=r(be);z.set(ee,s(ae))}}const V=await u({kind:"through-focus-spot",opticalSystemRows:_,sourceRows:N,objectRows:[w],surfaceIndex:C,defocusMinMm:U,defocusMaxMm:G,steps:F,rayCount:B,ringCount:j,scaleUm:W,pattern:H,wavelengthMode:ce}),K=new Map,oe=Array.isArray(V?.spotSeries)?V.spotSeries:[];for(const Y of oe){const J=Number(Y?.defocusMm),le=String(Y?.wavelengthLabel||"wavelength"),Q=`${J.toFixed(6)} | ${le}`,ee=Array.isArray(Y?.points)?Y.points.map(ie=>({xUm:Number(ie?.xUm),yUm:Number(ie?.yUm)})).filter(ie=>Number.isFinite(ie.xUm)&&Number.isFinite(ie.yUm)):[];K.set(Q,s(ee))}const re=a(R,$),fe=a(z,K),se=o(t(e?.topN,12),3,100),pe={spotDiagram:i(re),throughFocus:i(fe)},me={spotDiagramByRms:l(re,"deltaRmsUm",se),spotDiagramByDia:l(re,"deltaDiaUm",se),throughFocusByRms:l(fe,"deltaRmsUm",se),throughFocusByDia:l(fe,"deltaDiaUm",se),throughFocusByWavelength:p(fe)},L=d(re,fe,pe),X={config:{objectIndex:f,surfaceIndex:C,rayCount:B,ringCount:j,pattern:H,wavelengthMode:ce,defocusMinMm:U,defocusMaxMm:G,steps:F,scaleUm:W},summaries:pe,hotspots:me,correctionCandidates:L,spotDiagramRows:re,throughFocusRows:fe,generatedAt:Date.now()};try{window.__cooptLastSpotParity=X,ne.groupCollapsed("📊 [SpotParity] TS vs Rust"),ne.log("config",X.config),ne.log("summaries",pe),ne.log("hotspots",me),ne.log("correctionCandidates",L),ne.log("spotDiagramRows",re),ne.log("throughFocusRows",fe),ne.table&&(ne.table(me.spotDiagramByRms),ne.table(me.throughFocusByRms),ne.table(me.throughFocusByWavelength),ne.table(re),ne.table(fe)),ne.groupEnd()}catch{}return X}async function Br(e={}){ne.log("📊 Starting transverse aberration calculation...");const t=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null;let n="transverse-aberration-container";if(e&&typeof e=="object"&&(e.containerElement?n=e.containerElement:typeof e.containerId=="string"&&e.containerId.trim()!==""&&(n=e.containerId)),vo()){ne.warn("⚠️ Transverse aberration calculation already in progress");return}try{Gt(!0);try{t?.({percent:0,message:"Preparing transverse aberration..."})}catch{}const o=document.getElementById("transverse-ray-count-input");let r=51;const s=Number.isInteger(e?.rayCount)?e.rayCount:null;if(s!==null&&s>0)r=s;else if(o&&o.value!==""){const f=parseInt(o.value);!isNaN(f)&&f>0&&(r=f)}r=Math.max(9,Math.min(10001,Math.round(r)));const a=at(),c=je(a);if(!c||c.length===0)throw new Error("光学系データが見つかりません");const i=f=>{const b=String(f?.surfType??f?.["surf type"]??f?.surface_type??"").toLowerCase().trim();return b==="coord trans"||b==="coordinate break"||b==="coordtrans"||b==="coordinatebreak"||b==="ct"},l=f=>String(f?.["object type"]??f?.object??f?.Object??f?.surface_type??"").toLowerCase()==="object",m=f=>{const w=B=>String(B??"").trim().toLowerCase(),b=B=>w(B).replace(/[\s_-]+/g,""),v=w(f?.surfType??f?.["surf type"]??f?.type??f?.surface_type??""),M=b(f?.surfType??f?.["surf type"]??f?.type??f?.surface_type??""),C=w(f?._blockType??f?.blockType??""),D=b(f?._blockType??f?.blockType??"");return v==="gap"||v==="air gap"||M==="gap"||M==="airgap"||C==="gap"||C==="air gap"||D==="gap"||D==="airgap"},p=f=>String(f?.["object type"]??f?.object??f?.Object??"").toLowerCase()==="image";let d=-1;for(let f=0;f<c.length;f++)p(c[f])&&(d=f);if(d<0)for(let f=c.length-1;f>=0;f--){const w=c[f];if(!(i(w)||l(w)||m(w))){d=f;break}}d<0&&(d=c.length-1),ne.log(`📊 評価面: Surface ${d+1}`),ne.log(`📊 光線本数: ${r}本`);const{getPrimaryWavelengthForAberration:u}=await Re(async()=>{const{getPrimaryWavelengthForAberration:f}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(w=>w.bS);return{getPrimaryWavelengthForAberration:f}},__vite__mapDeps([0,1])),{plotTransverseAberrationDiagram:h}=await Re(async()=>{const{plotTransverseAberrationDiagram:f}=await import("./transverse-aberration-plot-DHnc-iG-.js");return{plotTransverseAberrationDiagram:f}},[]),{runNativeTransverseAberration:g}=await Re(async()=>{const{runNativeTransverseAberration:f}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(w=>w.bU);return{runNativeTransverseAberration:f}},__vite__mapDeps([0,1])),y=u();ne.log(`📊 Wavelength: ${y} μm`);const A=yt(),x=gt(),_=rt(A),S=Ke(x);try{t?.({percent:10,message:"Computing transverse aberration (Rust API)..."})}catch{}const N=await g({opticalSystemRows:c,sourceRows:Array.isArray(_)?_:[],objectRows:Array.isArray(S)?S:[],surfaceIndex:d,rayCount:r,pattern:"cross",wavelengthMode:"primary",wavelength:y});if(!N)throw new Error("Failed to calculate transverse aberration data");try{t?.({percent:95,message:"Rendering..."})}catch{}h(N,n,typeof n=="string"?document:n.ownerDocument);try{t?.({percent:100,message:"Done"})}catch{}return ne.log("✅ Transverse aberration diagram generated successfully"),N}catch(o){ne.error("❌ Transverse aberration diagram error:",o);const r=typeof n=="string"?document.getElementById(n):n;return r&&(r.innerHTML=`<div style="padding: 20px; color: red; font-family: Arial;">
                <strong>Transverse aberration error:</strong><br>
                ${o.message}<br>
                <small style="color: #888;">Check console for details</small>
            </div>`),alert(`Transverse aberration error: ${o.message}`),null}finally{Gt(!1)}}async function Yu(e={}){ne.log("📊 Starting lateral chromatic aberration calculation...");const t=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null,n=e&&typeof e=="object"&&typeof e.chiefRayDefinition=="string"?e.chiefRayDefinition:"stop-center";let o="magnification-chromatic-aberration-container";e&&typeof e=="object"&&(e.containerElement?o=e.containerElement:typeof e.containerId=="string"&&e.containerId.trim()!==""&&(o=e.containerId));try{try{t?.({percent:0,message:"Preparing lateral chromatic aberration..."})}catch{}const r=document.getElementById("mca-xmin-input"),s=document.getElementById("mca-xmax-input"),a=e&&typeof e=="object"?Number(e.xMin):NaN,c=e&&typeof e=="object"?Number(e.xMax):NaN;let i=Number.isFinite(a)?a:Number(r?.value),l=Number.isFinite(c)?c:Number(s?.value);Number.isFinite(i)||(i=-.05),Number.isFinite(l)||(l=.05),i>=l&&(i=-.05,l=.05);const m=at(),p=gt(),d=yt(),u=je(m),h=Ke(p),g=rt(d);if(!u||u.length===0)throw new Error("光学系データが見つかりません");const x=(U=>{const G=Array.isArray(U)?U:[],F=R=>(R?.position??R?.fieldType??R?.field_type??R?.field??R?.type??"").toString().toLowerCase(),W=G.map(F).filter(Boolean),I=W.some(R=>R.includes("rect")||R.includes("rectangle")),E=W.some(R=>R.includes("height"));return I||E?{mode:"height"}:W.some(R=>R.includes("angle"))?{mode:"angle"}:G.map(R=>parseFloat(R?.yHeight??R?.y??NaN)).filter(R=>Number.isFinite(R)).length>0?{mode:"height"}:{mode:"angle"}})(h).mode==="height",_=(h||[]).map(U=>parseFloat(x?U?.yHeight??U?.y??U?.yHeightAngle??NaN:U?.yHeightAngle??U?.yFieldAngle??U?.yAngle??U?.fieldAngle??U?.y??NaN)).filter(U=>Number.isFinite(U)).map(U=>Math.abs(U));if(_.length===0)throw new Error("Objectテーブルに有効な値がありません");const S=Math.max(..._.map(U=>Number(U)));if(!Number.isFinite(S)||S<=0)throw new Error("Objectテーブルに有効な最大値がありません");const N=document.getElementById("mca-point-count-input"),f=e&&typeof e=="object"?Number(e.pointCount):NaN;let w=Number.isFinite(f)?Math.round(f):Number(N?.value);(!Number.isFinite(w)||w<2)&&(w=11);const b=[];if(w<=1)b.push(S);else for(let U=0;U<w;U++){const G=S*U/(w-1);b.push(Number(G.toFixed(6)))}const v=U=>{const G=Number(U);return!Number.isFinite(G)||G<=0?null:G>10?G/1e3:G},M=[.4358,.5876,.6563],C=(()=>{const U=Array.isArray(g)?g:[],G=[];for(const F of U){const W=v(F?.wavelength??F?.Wavelength);if(W!==null&&(G.some(I=>Math.abs(I-W)<1e-12)||G.push(W),G.length>=6))break}return G.length>0?G:M.slice()})(),D=.5876;C.some(U=>Math.abs(U-D)<1e-6)||(C.push(D),C.sort((U,G)=>U-G));const{runNativeMagnificationChromaticAberration:B}=await Re(async()=>{const{runNativeMagnificationChromaticAberration:U}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(G=>G.bU);return{runNativeMagnificationChromaticAberration:U}},__vite__mapDeps([0,1])),{plotMagnificationChromaticAberration:j}=await Re(async()=>{const{plotMagnificationChromaticAberration:U}=await import("./magnification-chromatic-aberration-plot-PFW3f24C.js");return{plotMagnificationChromaticAberration:U}},[]),H=await B({opticalSystemRows:u,sourceRows:g,fieldSamples:b,wavelengths:C,referenceWavelength:D,heightMode:x,chiefRayDefinition:n});try{ne.log("📊 [LCA] backend:",H?.backend||H?.meta?.backend||"unknown")}catch{}if(!H)throw new Error("倍率色収差の計算に失敗しました");try{t?.({percent:95,message:"Rendering..."})}catch{}if(!j(H,o,{xMin:i,xMax:l}))throw new Error("倍率色収差: 描画可能な有効データがありません");try{t?.({percent:100,message:"Done"})}catch{}ne.log("✅ Lateral chromatic aberration diagram generated successfully")}catch(r){ne.error("❌ Lateral chromatic aberration diagram error:",r);const s=typeof o=="string"?document.getElementById(o):o;s&&(s.innerHTML=`<div style="padding: 20px; color: red; font-family: Arial;">
                <strong>Lateral chromatic aberration error:</strong><br>
                ${r.message}<br>
                <small style="color: #888;">Check console for details</small>
            </div>`),alert(`Lateral chromatic aberration error: ${r.message}`)}}async function zr(e={}){ne.log("📊 Starting astigmatism calculation...");const t=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null,n=e&&typeof e=="object"&&typeof e.chiefRayDefinition=="string"?e.chiefRayDefinition:"stop-center";e&&typeof e=="object"&&e.logChiefRayDefinition;const o=e&&typeof e=="object"?e.useActiveConfigSnapshot===!0:!1,r=e&&typeof e=="object"?e.configId:null;let s="astigmatic-field-curves-container";if(e&&typeof e=="object"&&(e.containerElement?s=e.containerElement:typeof e.containerId=="string"&&e.containerId.trim()!==""&&(s=e.containerId)),vo()){ne.warn("⚠️ Astigmatism calculation already in progress");return}try{Gt(!0);try{t?.({percent:0,message:"Preparing..."})}catch{}const a=R=>Array.isArray(R)?R.map(k=>{if(!k||typeof k!="object")return k;const $={...k};return $.xHeightAngle==null&&$["object x"]!=null&&($.xHeightAngle=$["object x"]),$.yHeightAngle==null&&$["object y"]!=null&&($.yHeightAngle=$["object y"]),$.xHeightAngle==null&&$.x!=null&&($.xHeightAngle=$.x),$.yHeightAngle==null&&$.y!=null&&($.yHeightAngle=$.y),$.position==null&&$.objectType!=null&&($.position=$.objectType),$}):[],c=()=>{try{if(typeof localStorage>"u")return null;const R=ot();if(!R||!Array.isArray(R.configurations))return null;const k=R.activeConfigId!==void 0&&R.activeConfigId!==null?String(R.activeConfigId):"",$=r!=null?String(r):k;if(!$)return null;const te=R.configurations.find(z=>String(z?.id)===$);if(!te)return null;const q=(()=>{try{const z=Ft();return Array.isArray(z)?z:[]}catch{return[]}})();return{opticalSystemRows:Array.isArray(te.opticalSystem)?te.opticalSystem:[],objectRows:a(Array.isArray(te.object)?te.object:[]),sourceRows:q}}catch(R){return ne.warn("⚠️ Failed to load config snapshot for astigmatism:",R),null}},i=at(),l=yt(),m=gt();let p=je(i),d=rt(l),u=Ke(m);if(o||r!=null){const R=c();R?.opticalSystemRows?.length&&(p=R.opticalSystemRows,Array.isArray(R.sourceRows)&&R.sourceRows.length>0&&(d=R.sourceRows),Array.isArray(R.objectRows)&&R.objectRows.length>0&&(u=R.objectRows))}if(!Array.isArray(u)||u.length===0)try{const R=window.tableObject&&Array.isArray(window.tableObject.data)?window.tableObject.data:null;Array.isArray(R)&&R.length>0&&(u=R)}catch{}if((!Array.isArray(u)||u.length===0)&&typeof localStorage<"u")try{const R=localStorage.getItem("objectTableData");if(R){const k=JSON.parse(R);Array.isArray(k)&&k.length>0&&(u=k)}}catch{}if(!Array.isArray(d)||d.length===0)try{const R=Ft();Array.isArray(R)&&R.length>0&&(d=R)}catch{}if(!Array.isArray(u)||u.length===0){const R=ni(p);u=[String(R).toLowerCase()!=="finite"?{name:"AutoField0",position:"Angle",xHeightAngle:0,yHeightAngle:0,x:0,y:0}:{name:"AutoField0",position:"Rectangle",xHeight:0,yHeight:0,x:0,y:0}],ne.warn(`[ASTIG_DEBUG][FALLBACK_OBJECT] Injected default object row for conjugate=${R}`)}ne.log(`[ASTIG_DEBUG][INPUT_ROWS] optical=${Array.isArray(p)?p.length:0} source=${Array.isArray(d)?d.length:0} object=${Array.isArray(u)?u.length:0} snapshot=${o||r!=null}`);try{t?.({percent:12,message:`Input optical=${Array.isArray(p)?p.length:0} source=${Array.isArray(d)?d.length:0} object=${Array.isArray(u)?u.length:0}`})}catch{}if(!p||p.length===0)throw new Error("光学系データが見つかりません");const h=Number(e?.rayCount),g=Number.isFinite(h)?Math.max(9,Math.min(2001,Math.round(h))):30,y=Number(e?.ringCount),A=Number.isFinite(y)?Math.max(1,Math.min(64,Math.round(y))):32,x=String(e?.pattern||"").trim().toLowerCase(),_=x==="grid"||x==="cross"||x==="annular"?x:"annular",S=document.getElementById("astigmatism-ray-filter"),N=S?S.value:"all",f=document.getElementById("astigmatism-field-mode"),w=f?f.value:"interpolate",b=document.getElementById("astigmatism-chief-ray-mode"),M={"stop-center":"stopCenter","beam-midpoint":"beamCenter","beam-centroid":"centroid"}[n]||null,C=M||(b?b.value:"stopCenter"),B=["stopCenter","beamCenter","centroid"].includes(C)?C:"stopCenter";let j=u;const H=(u||[]).some(R=>{const k=(R.position||R.fieldType||R.type||"").toLowerCase();return k.includes("height")||k.includes("rect")});if(w==="interpolate"&&(u||[]).length>0&&!H){const R=Math.max(...u.map(q=>Math.abs(parseFloat(q.yHeightAngle||0)))),k=50,$=16,te=60;j=[];try{t?.({percent:$,message:`Interpolating fields 0/${k} (angle=0.00° / max=${R.toFixed(2)}°)`})}catch{}for(let q=0;q<=k;q++){const z=R*q/k;j.push({name:`Field${q}`,xHeightAngle:0,yHeightAngle:z,position:"angle"});const V=k>0?q/k:1,K=$+(te-$)*V;try{t?.({percent:K,message:`Interpolating fields ${q}/${k} (angle=${z.toFixed(2)}° / max=${R.toFixed(2)}°)`})}catch{}}}const ce=p.length-1,{runNativeAstigmatism:U}=await Re(async()=>{const{runNativeAstigmatism:R}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(k=>k.bU);return{runNativeAstigmatism:R}},__vite__mapDeps([0,1])),{plotAstigmaticFieldCurves:G}=await Re(async()=>{const{plotAstigmaticFieldCurves:R}=await import("./astigmatism-plot-D5wQAwv2.js");return{plotAstigmaticFieldCurves:R}},[]),F=await U({opticalSystemRows:p,sourceRows:d||[],objectRows:j||[],surfaceIndex:ce,rayCount:g,ringCount:A,pattern:_,chiefRayMode:B,wavelengthMode:"all"}),W=String(F?.backend||"tauri-native").trim();ne.log(`[ASTIG_BACKEND] ${W}`);try{t?.({percent:92,message:`Backend: ${W}`})}catch{}const I=Array.isArray(F?.data)?F.data:[],E=I.filter(R=>R?.meridionalDeviation!==null&&R?.meridionalDeviation!==void 0&&Number.isFinite(Number(R?.meridionalDeviation))).length,P=I.filter(R=>R?.sagittalDeviation!==null&&R?.sagittalDeviation!==void 0&&Number.isFinite(Number(R?.sagittalDeviation))).length,Z=new Set(I.map(R=>Number(R?.wavelength)).filter(R=>Number.isFinite(R))).size;try{t?.({percent:94,message:`Summary rows=${I.length} validM=${E} validS=${P} wl=${Z}`})}catch{}if(!F||!F.data||F.data.length===0){ne.warn("⚠️ 非点収差曲線データの生成に失敗しました");try{t?.({percent:100,message:`No plot data (rows=${I.length}, validM=${E}, validS=${P})`})}catch{}const R=typeof s=="string"?document.getElementById(s):s;R&&(R.innerHTML=`<div style="padding:16px;color:#b00020;font-family:Arial;"><div style="font-weight:600;margin-bottom:6px;">No astigmatism plot data</div><div style="font-size:12px;color:#444;">rows=${I.length}, validM=${E}, validS=${P}</div></div>`)}else{try{t?.({percent:95,message:"Rendering..."})}catch{}try{G(s,F);try{t?.({percent:100,message:""})}catch{}}catch(R){throw R}}ne.log("✅ Astigmatism diagram generated successfully")}catch(a){ne.error("❌ Astigmatism diagram error:",a),alert(`Astigmatism diagram error: ${a.message}`)}finally{Gt(!1)}}async function Xu(e={}){ne.log("📊 Starting spherical aberration calculation...");const t=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null;let n="longitudinal-aberration-container";if(e&&typeof e=="object"&&(e.containerElement?n=e.containerElement:typeof e.containerId=="string"&&e.containerId.trim()!==""&&(n=e.containerId)),vo()){ne.warn("⚠️ Spherical aberration calculation already in progress");return}try{Gt(!0);try{t?.({percent:0,message:"Preparing spherical aberration..."})}catch{}const o=e&&typeof e=="object"?e.precomputedAberrationData:null;if(o&&typeof o=="object"){const{plotLongitudinalAberrationDiagram:f}=await Re(async()=>{const{plotLongitudinalAberrationDiagram:w}=await import("./longitudinal-aberration-plot-B8hkQC6I.js");return{plotLongitudinalAberrationDiagram:w}},[]);try{t?.({percent:95,message:"Rendering..."})}catch{}await f(o,n);try{t?.({percent:100,message:"Done"})}catch{}return}const r=document.getElementById("longitudinal-ray-count-input"),s=document.getElementById("longitudinal-reference-focus-mode");let a=0,c=51,i="current-paraxial";const l=yt(),m=rt(l),p=(()=>{const f=v=>{const M=Number(v);return!Number.isFinite(M)||M<=0?null:M>10?M/1e3:M},w=Array.isArray(m)?m:[],b=[];for(const v of w){const M=f(v?.wavelength??v?.Wavelength);if(!(M===null||!Number.isFinite(M)||M<=0)&&(b.some(C=>Math.abs(C-M)<1e-12)||b.push(M),b.length>=6))break}return b.length>0?b:[.5876]})();ne.log(`📊 Wavelengths from Source table: ${p.map(f=>f.toFixed(4)).join(", ")} μm`);let d=at(),u=je(d);u&&u.length>0&&(a=u.length-1,ne.log(`📊 Using default image surface: Surface ${a+1} (0-indexed: ${a})`));const h=Number.isInteger(e?.rayCount)?e.rayCount:null;h!==null&&h>0?c=h:r&&r.value!==""?c=parseInt(r.value)||51:ne.warn("⚠️ Ray count input not found, using default (51)");const g=typeof e?.referenceFocusMode=="string"?e.referenceFocusMode:null;g?i=g:s&&s.value&&(i=s.value),(isNaN(a)||a<0)&&(a=0,ne.warn("⚠️ Invalid surface index, using default (0)")),ne.log(`📊 Calculating spherical aberration for surface ${a}, ${c} rays, wavelengths: ${p.map(f=>f.toFixed(4)).join(", ")} μm`),d=at();const y=gt(),A=je(d),x=Ke(y);if(A&&A.length>0){const f=A.length-1;a>f&&(ne.warn(`⚠️ Surface index ${a} is too large, using last surface (${f})`),a=f)}if(ne.log(`📊 Final surface index: ${a} (0-indexed), using as targetSurfaceIndex: ${a}`),!A||A.length===0)throw new Error("No optical system data available");if(!x||x.length===0)throw new Error("No object data available");const{calculateLongitudinalAberrationAsync:_}=await Re(async()=>{const{calculateLongitudinalAberrationAsync:f}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(w=>w.bR);return{calculateLongitudinalAberrationAsync:f}},__vite__mapDeps([0,1])),{plotLongitudinalAberrationDiagram:S}=await Re(async()=>{const{plotLongitudinalAberrationDiagram:f}=await import("./longitudinal-aberration-plot-B8hkQC6I.js");return{plotLongitudinalAberrationDiagram:f}},[]),N=await _(A,a,p,c,{onProgress:t,debugSA:!!Pe.__COOPT_DEBUG_SA,referenceFocusMode:i});if(!N)throw new Error("Failed to calculate spherical aberration data");try{t?.({percent:95,message:"Rendering..."})}catch{}await S(N,n);try{t?.({percent:100,message:"Done"})}catch{}ne.log("✅ Spherical aberration diagram generated successfully")}catch(o){ne.error("❌ Error generating longitudinal aberration diagram:",o);const r=typeof n=="string"?document.getElementById(n):n;r&&(r.innerHTML=`<div style="padding: 20px; color: red; font-family: Arial;">
                <strong>Spherical aberration error:</strong><br>
                ${o.message}<br>
                <small style="color: #888;">Check console for details</small>
            </div>`),alert(`Error generating longitudinal aberration diagram: ${o.message}`)}finally{Gt(!1)}}function Ku(e){if(ne.log("📈 === Chief Ray Convergence Data ==="),!e||!e.chiefRayData){ne.warn("⚠️ No chief ray data available");return}const t=e.chiefRayData;ne.log(`Field angles: X=${t.fieldAngleX}°, Y=${t.fieldAngleY}°`),ne.log(`Entrance pupil position: ${t.entrancePupilPosition?.toFixed(4)||"N/A"}`),ne.log(`Exit pupil position: ${t.exitPupilPosition?.toFixed(4)||"N/A"}`),t.convergencePoint&&ne.log(`Chief ray convergence point: (${t.convergencePoint.x.toFixed(4)}, ${t.convergencePoint.y.toFixed(4)}, ${t.convergencePoint.z.toFixed(4)})`),t.aberrationCoefficients&&(ne.log("Aberration coefficients:"),Object.entries(t.aberrationCoefficients).forEach(([n,o])=>{ne.log(`  ${n}: ${o.toFixed(6)}`)})),ne.log("================================")}function Ju(){const e=it();if(!e)return null;const t={minX:1/0,maxX:-1/0,minY:1/0,maxY:-1/0,minZ:1/0,maxZ:-1/0};let n=!1;return e.children.forEach(o=>{if(o.visible&&(o.isMesh||o.isLine||o.isGroup)&&o.type!=="AmbientLight"&&o.type!=="DirectionalLight"){const r=new so().setFromObject(o);r.isEmpty()||(t.minX=Math.min(t.minX,r.min.x),t.maxX=Math.max(t.maxX,r.max.x),t.minY=Math.min(t.minY,r.min.y),t.maxY=Math.max(t.maxY,r.max.y),t.minZ=Math.min(t.minZ,r.min.z),t.maxZ=Math.max(t.maxZ,r.max.z),n=!0)}}),n?(t.centerX=(t.minX+t.maxX)/2,t.centerY=(t.minY+t.maxY)/2,t.centerZ=(t.minZ+t.maxZ)/2,t.sizeX=t.maxX-t.minX,t.sizeY=t.maxY-t.minY,t.sizeZ=t.maxZ-t.minZ,t.maxSize=Math.max(t.sizeX,t.sizeY,t.sizeZ),t):(ne.warn("⚠️ No visible objects found for bounds calculation"),null)}function Qu(){const e=Pt(),t=Zt(),n=xt();if(!e||!t||!n){ne.warn("⚠️ Camera, controls, or renderer not available");return}const o=Ju();if(!o){ne.warn("⚠️ No scene bounds available for camera fitting");return}ne.log("🎥 Fitting camera to scene bounds..."),ne.log(`Scene bounds: (${o.minX.toFixed(2)}, ${o.minY.toFixed(2)}, ${o.minZ.toFixed(2)}) to (${o.maxX.toFixed(2)}, ${o.maxY.toFixed(2)}, ${o.maxZ.toFixed(2)})`);const r=o.maxSize*1.5,s={x:o.centerX,y:o.centerY,z:o.centerZ+r};if(e.position.set(s.x,s.y,s.z),t.target.set(o.centerX,o.centerY,o.centerZ),e.isOrthographicCamera){const a=e.right/e.top,c=o.maxSize*.6;e.left=-c*a/2,e.right=c*a/2,e.top=c/2,e.bottom=-c/2,e.updateProjectionMatrix()}t.update(),n.render(it(),e),ne.log(`🎥 Camera fitted to scene, distance: ${r.toFixed(2)}`),ne.log(`🎥 Camera position: (${e.position.x.toFixed(2)}, ${e.position.y.toFixed(2)}, ${e.position.z.toFixed(2)})`)}async function ep(e={}){ne.log("📊 Starting integrated aberration diagram calculation...");const t=e&&typeof e=="object"&&typeof e.onProgress=="function"?e.onProgress:null,n=e&&typeof e=="object"?e.useActiveConfigSnapshot===!0:!1,o=e&&typeof e=="object"?e.configId:null,r=(s,a,c)=>t?i=>{try{const l=Number(i?.percent),m=i?.message||i?.phase||"Working...",p=Number.isFinite(l)?s+a*l/100:s;t({percent:p,message:c?`${c}: ${m}`:m})}catch{}}:null;try{try{t?.({percent:0,message:"Starting..."})}catch{}const s=R=>Array.isArray(R)?R.map(k=>{if(!k||typeof k!="object")return k;const $={...k};return $.xHeightAngle==null&&$["object x"]!=null&&($.xHeightAngle=$["object x"]),$.yHeightAngle==null&&$["object y"]!=null&&($.yHeightAngle=$["object y"]),$.xHeightAngle==null&&$.x!=null&&($.xHeightAngle=$.x),$.yHeightAngle==null&&$.y!=null&&($.yHeightAngle=$.y),$.position==null&&$.objectType!=null&&($.position=$.objectType),$}):[],a=()=>{try{if(typeof localStorage>"u")return null;const R=ot();if(!R||!Array.isArray(R.configurations))return null;const k=R.activeConfigId!==void 0&&R.activeConfigId!==null?String(R.activeConfigId):"",$=o!=null?String(o):k;if(!$)return null;const te=R.configurations.find(z=>String(z?.id)===$);if(!te)return null;const q=(()=>{try{const z=Ft();return Array.isArray(z)?z:[]}catch{return[]}})();return{opticalSystemRows:Array.isArray(te.opticalSystem)?te.opticalSystem:[],objectRows:s(Array.isArray(te.object)?te.object:[]),sourceRows:q}}catch(R){return ne.warn("⚠️ Failed to load config snapshot for integrated aberration:",R),null}},c=at(),i=gt(),l=yt();let m=je(c),p=s(Ke(i)),d=rt(l);if(n||o!=null){const R=a();R?.opticalSystemRows?.length&&(m=R.opticalSystemRows,p=R.objectRows,d=R.sourceRows)}if(!m||m.length===0){alert("光学系データがありません。");return}const u=100,h=30,g=(()=>{const R=[.4308,.5876,.6563],k=q=>{const z=Number(q);return!Number.isFinite(z)||z<=0?null:z>10?z/1e3:z},$=Array.isArray(d)?d:[],te=[];for(const q of $){const z=k(q?.wavelength);if(!(z===null||!Number.isFinite(z)||z<=0)&&(te.some(V=>Math.abs(V-z)<1e-12)||te.push(z),te.length>=6))break}return te.length>0?te:R})(),y=m.length-1;ne.log("📊 Calculating aberrations..."),ne.log("📊 Calculating spherical aberration...");const{calculateLongitudinalAberrationAsync:A}=await Re(async()=>{const{calculateLongitudinalAberrationAsync:R}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(k=>k.bR);return{calculateLongitudinalAberrationAsync:R}},__vite__mapDeps([0,1])),x=await A(m,y,g,u,{onProgress:r(5,30,"Spherical"),referenceFocusMode:"current-paraxial"});if(!x)throw new Error("Failed to calculate longitudinal aberration");ne.log("📊 Calculating astigmatism...");let _=null;const{runNativeAstigmatism:S}=await Re(async()=>{const{runNativeAstigmatism:R}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(k=>k.bU);return{runNativeAstigmatism:R}},__vite__mapDeps([0,1]));if(_=await S({opticalSystemRows:m,sourceRows:d||[],objectRows:p||[],surfaceIndex:y,rayCount:h,ringCount:32,pattern:"annular",chiefRayMode:"stopCenter",wavelengthMode:"all"}),!_)throw new Error("Failed to calculate astigmatism");ne.log("📊 Calculating distortion...");const{runNativeDistortion:w,runNativeMagnificationChromaticAberration:b}=await Re(async()=>{const{runNativeDistortion:R,runNativeMagnificationChromaticAberration:k}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then($=>$.bU);return{runNativeDistortion:R,runNativeMagnificationChromaticAberration:k}},__vite__mapDeps([0,1])),{deriveMaxFieldAngleFromObjects:v}=await Re(async()=>{const{deriveMaxFieldAngleFromObjects:R}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(k=>k.bV);return{deriveMaxFieldAngleFromObjects:R}},__vite__mapDeps([0,1])),D=(R=>{const k=Array.isArray(R)?R:[],$=re=>(re?.position??re?.fieldType??re?.field_type??re?.field??re?.type??"").toString().toLowerCase(),te=k.map($).filter(Boolean),q=te.some(re=>re.includes("rect")||re.includes("rectangle")),z=te.some(re=>re.includes("height"));if(q||z)return{mode:"height"};if(te.some(re=>re.includes("angle")))return{mode:"angle"};const K=(k||[]).map(re=>parseFloat(re?.yHeight??re?.y??re?.yHeightAngle??NaN)).filter(re=>Number.isFinite(re)),oe=(k||[]).map(re=>parseFloat(re?.fieldAngle??re?.yFieldAngle??re?.yAngle??NaN)).filter(re=>Number.isFinite(re));return K.length>0&&oe.length===0?{mode:"height"}:{mode:"angle"}})(p).mode==="height",B=(p||[]).map(R=>parseFloat(R.yHeight??R.y??R.yHeightAngle??NaN)).filter(R=>Number.isFinite(R)),j=10;let H=[];if(D){let R=Math.min(...B),k=Math.max(...B);if(R<=0&&(R=.001,k<R&&(k=R)),R===k)H=[R];else for(let $=0;$<j;$++){const te=R+(k-R)*$/(j-1);H.push(parseFloat(te.toFixed(6)))}ne.log(`📊 Object heights for distortion (${H.length} points): ${H.join(", ")} mm`)}else{const R=v(),k=R*.001;for(let $=0;$<j;$++){const te=k+(R-k)*$/(j-1);H.push(parseFloat(te.toFixed(6)))}ne.log(`📊 Field angles for distortion (${j} points, starting from ${k.toFixed(6)}°): ${H.join(", ")}°`)}const ce=[];for(let R=0;R<g.length;R++){const k=g[R],$=70+25*R/Math.max(1,g.length),te=25/Math.max(1,g.length),q=await w({opticalSystemRows:m,fieldSamples:H,heightMode:D,wavelength:k});q&&ce.push({wavelength:k,data:q})}if(ce.length===0)throw new Error("Failed to calculate distortion for any wavelength");ne.log("📊 Calculating lateral chromatic aberration...");const U=Math.max(...D?B:H.map(R=>Math.abs(R))),G=21,F=[];if(Number.isFinite(U)&&U>0)for(let R=0;R<G;R++){const k=U*R/(G-1);F.push(Number(k.toFixed(6)))}const W=F.length>0?await b({opticalSystemRows:m,sourceRows:d,fieldSamples:F,wavelengths:g,referenceWavelength:.5876,heightMode:D}):null;ne.log("📊 Plotting integrated aberration diagram...");const{plotIntegratedAberrationDiagram:I}=await Re(async()=>{const{plotIntegratedAberrationDiagram:R}=await import("./integrated-aberration-plot-BuuUq4Y6.js");return{plotIntegratedAberrationDiagram:R}},[]);try{t?.({percent:96,message:"Rendering..."})}catch{}const E=typeof localStorage>"u"?null:ot(),P=E?.configurations?.find(R=>R&&String(R.id)===String(E.activeConfigId)),Z=P?P.name:"Default";I(x,_,ce,W,{width:1440,height:600,mainTitle:`Integrated Aberration Diagram - ${Z}`,configName:Z,...e?.containerElement?{containerElement:e.containerElement}:{},...e?.infoElement?{infoElement:e.infoElement}:{}});try{t?.({percent:100,message:"Done"})}catch{}ne.log("✅ Integrated aberration diagram generated successfully")}catch(s){ne.error("❌ Error generating integrated aberration diagram:",s),alert(`Error generating integrated aberration diagram: ${s.message}`)}}(()=>{const e=globalThis;if(!e.__cooptConsoleRestoreApplied){e.__cooptConsoleRestoreApplied=!0;try{typeof e.isDebugEnabled=="function"&&(e.isDebugEnabled=()=>!0);const t=Object.getPrototypeOf(console);if(!t)return;const n=["log","info","warn","debug"];for(const o of n){const r=console[o],s=t[o];if(typeof s!="function")continue;Function.prototype.toString.call(r).includes("isDebugEnabled")&&(console[o]=s.bind(console))}}catch{}}})();(()=>{try{const e=globalThis;typeof e.__COOPT_FORCE_RUST_WASM_OPD>"u"&&(e.__COOPT_FORCE_RUST_WASM_OPD=!0),e&&e.__TAURI_INTERNALS__||(typeof e.__COOPT_ENABLE_RUST_RAYTRACE_WEB>"u"&&(e.__COOPT_ENABLE_RUST_RAYTRACE_WEB=!0),typeof e.__COOPT_REQUIRE_RUST_WASM_ANALYSIS>"u"&&(e.__COOPT_REQUIRE_RUST_WASM_ANALYSIS=!0))}catch{}})();window.THREE=Hr;window.OrbitControls=gi;let on=null;const yo=e=>new Promise(t=>setTimeout(t,e));function tp(){try{return typeof window>"u"||!window.location?null:new URL(window.location.href).searchParams}catch{return null}}function jt(e,t,n=!1){try{if(!e||!e.has(t))return n;const o=String(e.get(t)??"").trim().toLowerCase();return!o||["1","true","yes","on"].includes(o)?!0:["0","false","no","off"].includes(o)?!1:n}catch{return n}}function Lt(e,t,n,o={}){try{if(!e||!e.has(t))return n;const r=Number(e.get(t));if(!Number.isFinite(r))return n;let s=o.integer?Math.floor(r):r;return Number.isFinite(o.min)&&(s=Math.max(o.min,s)),Number.isFinite(o.max)&&(s=Math.min(o.max,s)),s}catch{return n}}function nn(e){try{typeof window<"u"&&(window.__cooptPhaseCAutorunStatus=e,window.dispatchEvent(new CustomEvent("coopt:phasec-autorun-status",{detail:e})))}catch{}try{typeof localStorage<"u"&&localStorage.setItem("coopt.phaseCAutorunStatus",JSON.stringify(e))}catch{}try{typeof document<"u"&&document.documentElement&&(document.documentElement.dataset.phasecAutorunState=String(e?.state??"unknown"))}catch{}}async function ts(e,t,n=1e4,o=50){const r=Date.now();for(;Date.now()-r<=n;){const s=t();if(s)return s;await yo(o)}throw new Error(`${e} was not ready within ${n}ms`)}async function Wr(e=2e3){try{await new Promise(t=>{let n=!1;const o=()=>{if(!n){n=!0;try{window.removeEventListener("coopt:loaded-file-updated",r)}catch{}try{clearTimeout(s)}catch{}t()}},r=()=>o(),s=setTimeout(()=>o(),e);window.addEventListener("coopt:loaded-file-updated",r,{once:!0})})}catch{await yo(250)}await yo(250)}async function np(){const e=await ts("__loadAllDataObjectIntoApp",()=>typeof window<"u"?window.__loadAllDataObjectIntoApp:null,1e4,50);if(ze()){const{project:o}=await ma();await e(o,{filename:"default-load.json"}),await Wr();return}let t=await fetch("/co-opt/defaults/default-load.json");if(t.ok||(t=await fetch("/defaults/default-load.json")),!t.ok)throw new Error(`Failed to load default system: ${t.statusText}`);const n=await t.json();await e(n,{filename:"default-load.json"}),await Wr()}async function op(){try{if(typeof window>"u"||window.__cooptPhaseCAutorunStarted)return;const e=tp();if(!(jt(e,"phasec",!1)||jt(e,"phasecAutorun",!1)))return;window.__cooptPhaseCAutorunStarted=!0;const n=new Date().toISOString();nn({state:"starting",startedAt:n}),jt(e,"phasecLoadDefault",!0)&&(nn({state:"loading-default",startedAt:n}),await np());const o=await ts("OptimizationMVP.exportMatrixFreeJson",()=>window?.OptimizationMVP?.exportMatrixFreeJson,1e4,50),r={repeat:Lt(e,"phasecRepeat",6,{integer:!0,min:1}),warmupDiscard:Lt(e,"phasecWarmupDiscard",1,{integer:!0,min:0}),filterOutliers:jt(e,"phasecFilterOutliers",!0),matchBaselineBestStop:jt(e,"phasecMatchBaselineBestStop",!0),matchBaselineBestMinIter:Lt(e,"phasecMatchBaselineBestMinIter",8,{integer:!0,min:0}),download:jt(e,"phasecDownload",!0)},s=e?.get("phasecFileName");s&&(r.fileName=s);const a=e?.get("phasecMethod");a&&(r.method=a),e?.has("phasecMaxIter")&&(r.maxIter=Lt(e,"phasecMaxIter",0,{integer:!0,min:1})),e?.has("phasecMatchBaselineBestRelTol")&&(r.matchBaselineBestRelTol=Lt(e,"phasecMatchBaselineBestRelTol",0,{min:0})),e?.has("phasecMatchBaselineBestAbsTol")&&(r.matchBaselineBestAbsTol=Lt(e,"phasecMatchBaselineBestAbsTol",0,{min:0})),nn({state:"running",startedAt:n,options:r});const c=await o(r);try{window.__cooptPhaseCAutorunResult=c}catch{}try{typeof localStorage<"u"&&c?.summary&&localStorage.setItem("coopt.phaseCLastSummary",JSON.stringify(c.summary))}catch{}nn({state:"done",startedAt:n,completedAt:new Date().toISOString(),selectedMode:c?.summary?.phaseC?.selectedMode??null,elapsedSpeedup:c?.summary?.phaseC?.elapsedSpeedup??null,fileName:s||null})}catch(e){const t=e instanceof Error?e.message:String(e);console.error("❌ [PhaseC Autorun] Failed:",e),nn({state:"error",completedAt:new Date().toISOString(),error:t})}}async function ns(){try{try{const i=globalThis,l=typeof i.__COOPT_RAYTRACE_WASM_STRICT=="boolean"?i.__COOPT_RAYTRACE_WASM_STRICT:!0,m=typeof i.__COOPT_PSF_WASM_STRICT=="boolean"?i.__COOPT_PSF_WASM_STRICT:!0;Bs(l),zs(m),i.__COOPT_RAYTRACE_WASM_STRICT=l,i.__COOPT_PSF_WASM_STRICT=m,console.log("🔒 [Init] WASM strict mode",{rayStrict:l,psfStrict:m})}catch{}const e=(async()=>{try{const i=await Ws();if(!i){const l=$s?.();l&&console.warn("⚠️ [Init] Rust-WASM preload failed:",l);return}on={backend:"rust-wasm",isWASMReady:!0,api:i},console.log("🔧 [Init] Rust-WASM backend is ready"),typeof window<"u"&&typeof window._setWASMSystem=="function"&&(window._setWASMSystem(on),console.log("🔧 [Init] window._setWASMSystemでインスタンスを更新しました")),console.log("✅ [Init] Rust-WASM初期化が完了しました")}catch(i){console.warn("⚠️ WASM initialization failed:",i),on&&(on.isWASMReady=!1)}})(),{scene:t,camera:n,renderer:o,controls:r}=Oa();try{Ho()}catch{}try{Go()}catch{}try{Vo()}catch{}const s=Da(t),{ambientLight:a,directionalLight:c}=s||{ambientLight:null,directionalLight:null};Us(t,n,o,r,qo,oi,Rn),ja();try{Wt(t)}catch{}try{yl()}catch{}try{hl()}catch{}try{wl()}catch{}try{$t(),setTimeout(()=>{try{typeof window.updateSpotDiagramConfigSelect=="function"&&window.updateSpotDiagramConfigSelect(),Ht()}catch(i){console.warn("Failed to initialize spot diagram selects:",i)}},100)}catch(i){console.error("Failed to setup analysis windows:",i)}try{bl()}catch{}try{}catch{}try{rn()}catch{}try{qn()}catch{}try{vu()}catch{}try{bu()}catch{}try{br()}catch{}try{window.initializeAllTables=()=>{try{Ho()}catch{}try{Go()}catch{}try{Vo()}catch{}try{br()}catch{}try{Ut()}catch{}try{Ht()}catch{}}}catch{}try{window.setupAnalysisWindows=$t,window.setupOpticalSystemChangeListeners=Wt}catch{}try{window.rebindEventHandlers=()=>{console.log("🔄 [Rebind] rebindEventHandlers called");const i=["new-file-btn","save-all-btn","load-all-btn","load-default-btn","import-zemax-btn","export-zemax-btn","optimize-design-intent-btn"];console.log("🔄 [Rebind] Checking button existence:"),i.forEach(l=>{const m=document.getElementById(l);console.log(`  ${l}: ${m?"✅ found":"❌ NOT FOUND"}`)});try{}catch{}try{rn()}catch{}try{qn()}catch{}try{const l=it?.();l&&Wt(l)}catch{}try{$t()}catch{}console.log("✅ [Rebind] rebindEventHandlers completed")}}catch{}return setTimeout(async()=>{Zo();try{window.updateWavefrontObjectSelect&&window.updateWavefrontObjectSelect(),window.updatePSFObjectOptions&&window.updatePSFObjectOptions()}catch{}},1e3),window.refreshBlockInspector=Je,window.loadSystemConfigurations=Ie,window.saveSystemConfigurations=Te,window.fitCameraToScene=Qu,window.clearAllDrawing=qu,window.showSpotDiagram=go,window.showTransverseAberrationDiagram=Br,window.showTransverseAberration=async()=>{const i=document.getElementById("transverse-ray-count-input");let l=51;if(i&&i.value!==""){const m=parseInt(i.value,10);!isNaN(m)&&m>0&&(l=m)}l=Math.max(9,Math.min(10001,Math.round(l))),await Br({rayCount:l})},window.showLongitudinalAberrationDiagram=Xu,window.showMagnificationChromaticAberrationDiagram=Yu,window.showAstigmatismDiagram=zr,window.showAstigmatism=async()=>{const i=document.getElementById("astigmatism-progress-wrapper"),l=document.getElementById("astigmatism-progressbar"),m=l instanceof HTMLProgressElement?l:null,p=document.getElementById("astigmatism-progress-text"),d=(u,h)=>{try{i&&(i.style.display="block"),m&&Number.isFinite(u)&&(m.value=Math.max(0,Math.min(100,u))),p&&typeof h=="string"&&(p.textContent=h)}catch{}};d(0,"Starting...");try{await zr({onProgress:h=>{try{const g=Number(h?.percent),y=h?.message||h?.phase||"Working...";Number.isFinite(g)?d(g,y):d(void 0,y)}catch{}}}),d(100,"Done")}catch(u){console.error("❌ Astigmatism diagram error:",u),d(100,"Failed")}},window.showIntegratedAberrationDiagram=ep,window.showWavefrontDiagram=$a,window.compareOpdNativeVsWasm=async(i={})=>{const l=typeof performance<"u"&&typeof performance.now=="function"?()=>performance.now():()=>Date.now(),m=re=>{const fe=re.filter(L=>Number.isFinite(L));if(!fe.length)return{count:0,rms:NaN,peakToPeak:NaN,min:NaN,max:NaN};let se=0,pe=1/0,me=-1/0;for(const L of fe)se+=L*L,L<pe&&(pe=L),L>me&&(me=L);return{count:fe.length,rms:Math.sqrt(se/fe.length),peakToPeak:me-pe,min:pe,max:me}},p=re=>{const fe=[];if(!Array.isArray(re))return fe;for(const se of re)if(Array.isArray(se))for(const pe of se){if(pe==null)continue;const me=Number(pe);Number.isFinite(me)&&fe.push(me)}return fe},d=(re,fe,se)=>{if(!Array.isArray(re)||!Array.isArray(fe)||!Number.isFinite(se)||se<=0)return{validCount:0,meanUm:NaN,rmsUm:NaN,rmsWaves:NaN,marechalStrehl:NaN};let pe=0,me=0;for(let Q=0;Q<re.length;Q++){const ee=re[Q],ie=fe[Q];if(!ee||!ie)continue;const be=Math.min(ee.length||0,ie.length||0);for(let ae=0;ae<be;ae++){if(!ie[ae])continue;const he=Number(ee[ae]);Number.isFinite(he)&&(me+=he,pe+=1)}}if(pe<=0)return{validCount:0,meanUm:NaN,rmsUm:NaN,rmsWaves:NaN,marechalStrehl:NaN};const L=me/pe;let X=0;for(let Q=0;Q<re.length;Q++){const ee=re[Q],ie=fe[Q];if(!ee||!ie)continue;const be=Math.min(ee.length||0,ie.length||0);for(let ae=0;ae<be;ae++){if(!ie[ae])continue;const he=Number(ee[ae]);if(!Number.isFinite(he))continue;const de=he-L;X+=de*de}}const Y=Math.sqrt(X/pe),J=Y/se,le=Math.exp(-Math.pow(2*Math.PI*J,2));return{validCount:pe,meanUm:L,rmsUm:Y,rmsWaves:J,marechalStrehl:Number.isFinite(le)?Math.max(0,Math.min(1,le)):NaN}},u=je(),h=Ke(),g=rt(Rn),y=Number.isFinite(Number(i?.objectIndex))?Number(i.objectIndex):0,A=Number.isFinite(Number(i?.gridSize))?Math.max(17,Math.min(513,Math.floor(Number(i.gridSize)))):129,x=String(i?.opdDisplayMode||"pistonTiltRemoved"),_=String(i?.wasmOpdMode||"simple"),S=(()=>{try{if(typeof window.getPrimaryWavelength=="function"){const fe=Number(window.getPrimaryWavelength());if(Number.isFinite(fe)&&fe>0)return fe}}catch{}const re=Number(Ko?.());return Number.isFinite(re)&&re>0?re:.5876})(),N=Array.isArray(h)&&h[y]?h[y]:h?.[0]||{},f=String(N?.position??N?.Position??"").toLowerCase(),w=Number(N?.xHeightAngle??N?.x??0)||0,b=Number(N?.yHeightAngle??N?.y??0)||0,v=f==="angle"||f==="field angle"||f==="angles"||f==="point",M={id:N?.id||y+1,displayName:`Object ${y+1}`,type:v?"Angle":"Rectangle",fieldAngle:v?{x:w,y:b}:{x:0,y:0},xHeight:v?0:w,yHeight:v?0:b,objectIndex:y,wavelength:S},C=l(),D=sn(u,S),j=await an(D).generateWavefrontMap(M,A,"circular",{recordRays:!1,progressEvery:0,opdMode:_,skipZernikeFit:!0,renderFromZernike:!1,opdDisplayMode:x,fullBatchTraceExperimental:!0}),H=l()-C;if(j?.error)throw new Error(`WASM/legacy OPD failed: ${j.error?.message||j.error}`);const ce=(Array.isArray(j?.display?.opdsInWavelengths)?j.display.opdsInWavelengths:Array.isArray(j?.opdsInWavelengths)?j.opdsInWavelengths:Array.isArray(j?.opds)?j.opds:[]).map(re=>Number(re)).filter(re=>Number.isFinite(re)),U=(Array.isArray(j?.raw?.opdsInWavelengths)?j.raw.opdsInWavelengths:Array.isArray(j?.raw?.opds)?j.raw.opds:[]).map(re=>Number(re)).filter(re=>Number.isFinite(re)),G=l(),{runNativeOpdMap:F}=await Re(async()=>{const{runNativeOpdMap:re}=await import("./toolbar-collapsed-storage-YH65vEW9.js").then(fe=>fe.bU);return{runNativeOpdMap:re}},__vite__mapDeps([0,1])),W=await F({opticalSystemRows:Array.isArray(u)?u:[],sourceRows:Array.isArray(g)?g:[],objectRows:Array.isArray(h)?h:[],objectIndex:y,gridSize:A,wavelengthUm:S,opdDisplayMode:x==="raw"||x==="pistonTiltRemoved"||x==="pistonTiltDefocusRemoved"?x:"pistonTiltRemoved"}),I=l()-G,E=Array.isArray(W?.displayOpdGrid)&&W.displayOpdGrid.length?W.displayOpdGrid:W?.rawOpdGrid,P=p(E),Z=p(W?.rawOpdGrid),R=m(ce),k=m(P),$=Number.isFinite(Number(W?.hitCount))?Number(W.hitCount):k.count||0,te={rmsAbs:Number.isFinite(R.rms)&&Number.isFinite(k.rms)?Math.abs(k.rms-R.rms):NaN,peakToPeakAbs:Number.isFinite(R.peakToPeak)&&Number.isFinite(k.peakToPeak)?Math.abs(k.peakToPeak-R.peakToPeak):NaN,countDiff:Math.abs($-(R.count||0))},q=m(U),z=m(Z),V=Number.isFinite(Number(W?.hitCount))?Number(W.hitCount):z.count||0,K={rmsAbs:Number.isFinite(q.rms)&&Number.isFinite(z.rms)?Math.abs(z.rms-q.rms):NaN,peakToPeakAbs:Number.isFinite(q.peakToPeak)&&Number.isFinite(z.peakToPeak)?Math.abs(z.peakToPeak-q.peakToPeak):NaN,countDiff:Math.abs(V-(q.count||0))},oe={backend:{wasm:"wavefront.generateWavefrontMap",native:W?.backend||"run_native_opd_map"},options:{objectIndex:y,gridSize:A,wavelength:S,opdDisplayMode:x,wasmOpdMode:_},wasm:{elapsedMs:H,stats:R},native:{elapsedMs:I,stats:k,sampleCount:Number(W?.sampleCount??0),hitCount:Number(W?.hitCount??0)},diff:te,raw:{wasm:q,native:z,diff:K}};console.log("📊 [OPD Parity] native vs wasm",oe);try{console.log(`[OPD Parity Summary] display rms wasm=${Number(R.rms).toFixed(6)} native=${Number(k.rms).toFixed(6)} diff=${Number(te.rmsAbs).toFixed(6)} pp wasm=${Number(R.peakToPeak).toFixed(6)} native=${Number(k.peakToPeak).toFixed(6)} diff=${Number(te.peakToPeakAbs).toFixed(6)} count wasm=${R.count} native=${$} (gridNonNull=${k.count})`),console.log(`[OPD Parity Summary][raw] rms wasm=${Number(q.rms).toFixed(6)} native=${Number(z.rms).toFixed(6)} diff=${Number(K.rmsAbs).toFixed(6)} pp wasm=${Number(q.peakToPeak).toFixed(6)} native=${Number(z.peakToPeak).toFixed(6)} diff=${Number(K.peakToPeakAbs).toFixed(6)} count wasm=${q.count} native=${V} (gridNonNull=${z.count})`)}catch{}return oe},window.comparePsfNativeVsJs=async(i={})=>{const l=typeof performance<"u"&&typeof performance.now=="function"?()=>performance.now():()=>Date.now(),m=ue=>{if(!Array.isArray(ue)||!ue.length||!Array.isArray(ue[0]))return{count:0,sum:NaN,sumSq:NaN,min:NaN,max:NaN,hash:"na"};let ve=0,ke=0,Fe=0,Le=1/0,Ue=-1/0,Be=2166136261;for(let bt=0;bt<ue.length;bt++){const At=ue[bt];if(Array.isArray(At))for(let Tt=0;Tt<At.length;Tt++){const qe=Number(At[Tt]);if(!Number.isFinite(qe))continue;ve++,ke+=qe,Fe+=qe*qe,qe<Le&&(Le=qe),qe>Ue&&(Ue=qe);const ut=Math.round(qe*1e9);Be^=ut&255,Be=Math.imul(Be,16777619)>>>0,Be^=ut>>8&255,Be=Math.imul(Be,16777619)>>>0,Be^=ut>>16&255,Be=Math.imul(Be,16777619)>>>0,Be^=ut>>24&255,Be=Math.imul(Be,16777619)>>>0}}return{count:ve,sum:ke,sumSq:Fe,min:Number.isFinite(Le)?Le:NaN,max:Number.isFinite(Ue)?Ue:NaN,hash:Be.toString(16)}},p=ue=>{const ve=[];if(!Array.isArray(ue))return ve;for(const ke of ue)if(Array.isArray(ke))for(const Fe of ke){const Le=Number(Fe);Number.isFinite(Le)&&ve.push(Le)}return ve},d=(ue,ve,ke)=>{if(!Array.isArray(ue)||!Array.isArray(ve)||!Number.isFinite(ke)||ke<=0)return{validCount:0,meanUm:NaN,rmsUm:NaN,rmsWaves:NaN,marechalStrehl:NaN};let Fe=0,Le=0;for(let qe=0;qe<ue.length;qe++){const ut=ue[qe],It=ve[qe];if(!ut||!It)continue;const Vn=Math.min(ut.length||0,It.length||0);for(let wt=0;wt<Vn;wt++){if(!It[wt])continue;const Kt=Number(ut[wt]);Number.isFinite(Kt)&&(Le+=Kt,Fe+=1)}}if(Fe<=0)return{validCount:0,meanUm:NaN,rmsUm:NaN,rmsWaves:NaN,marechalStrehl:NaN};const Ue=Le/Fe;let Be=0;for(let qe=0;qe<ue.length;qe++){const ut=ue[qe],It=ve[qe];if(!ut||!It)continue;const Vn=Math.min(ut.length||0,It.length||0);for(let wt=0;wt<Vn;wt++){if(!It[wt])continue;const Kt=Number(ut[wt]);if(!Number.isFinite(Kt))continue;const jo=Kt-Ue;Be+=jo*jo}}const bt=Math.sqrt(Be/Fe),At=bt/ke,Tt=Math.exp(-Math.pow(2*Math.PI*At,2));return{validCount:Fe,meanUm:Ue,rmsUm:bt,rmsWaves:At,marechalStrehl:Number.isFinite(Tt)?Math.max(0,Math.min(1,Tt)):NaN}},u=je(),h=Ke(),g=rt(Rn),y=Number.isFinite(Number(i?.objectIndex))?Number(i.objectIndex):0,A=Number.isFinite(Number(i?.samplingSize))?Math.max(32,Math.min(1024,Math.floor(Number(i.samplingSize)))):128,x=i?.zeroPadTo,_=Number.isFinite(Number(x))?Math.max(0,Math.min(4096,Math.floor(Number(x)))):0,S=String(i?.opdDisplayMode||"pistonTiltRemoved"),N=i?.removeTilt===void 0?!1:!!i.removeTilt,f=i?.recenterIfWrapped===void 0?void 0:!!i.recenterIfWrapped,w=(()=>{try{if(typeof window.getPrimaryWavelength=="function"){const ve=Number(window.getPrimaryWavelength());if(Number.isFinite(ve)&&ve>0)return ve}}catch{}const ue=Number(Ko?.());return Number.isFinite(ue)&&ue>0?ue:.5876})();if(!ze())throw new Error("comparePsfNativeVsJs requires Tauri runtime");const b=Array.isArray(h)&&h[y]?h[y]:h?.[0]||{},v=String(b?.position??b?.Position??"").toLowerCase(),M=Number(b?.xHeightAngle??b?.x??0)||0,C=Number(b?.yHeightAngle??b?.y??0)||0,D=v==="angle"||v==="field angle"||v==="angles",B={id:b?.id||y+1,displayName:`Object ${y+1}`,type:D?"Angle":"Rectangle",fieldAngle:D?{x:M,y:C}:{x:0,y:0},xHeight:D?0:M,yHeight:D?0:C,objectIndex:y,wavelength:w},j=l(),H=sn(u,w),U=await an(H).generateWavefrontMap(B,A,"circular",{recordRays:!1,progressEvery:0,opdMode:"simple",skipZernikeFit:!0,renderFromZernike:!1,opdDisplayMode:S,fullBatchTraceExperimental:!0}),G=l()-j;if(U?.error)throw new Error(`Wavefront generation failed: ${U.error?.message||U.error}`);const F=Math.max(16,Math.floor(Number(A))),W=Array.from({length:F},()=>new Float32Array(F)),I=Array.from({length:F},()=>new Float32Array(F)),E=Array.from({length:F},()=>Array(F).fill(!1)),P=new Float32Array(F),Z=new Float32Array(F),R=Number.isFinite(Number(U?.pupilRange))&&Number(U.pupilRange)>0?Number(U.pupilRange):1;for(let ue=0;ue<F;ue++){const ve=ue/(F-1||1)*2-1;P[ue]=ve*R,Z[ue]=ve*R}const k=Array.isArray(U?.pupilCoordinates)?U.pupilCoordinates:[],te=S!=="raw"&&Array.isArray(U?.display?.opds)?U.display.opds:Array.isArray(U?.opds)?U.opds:[],q=Math.min(k.length,te.length),z=[];for(let ue=0;ue<q;ue++){const ve=k[ue],ke=Number.isInteger(ve?.ix)?ve.ix:null,Fe=Number.isInteger(ve?.iy)?ve.iy:null,Le=Number(te[ue]);ke!==null&&Fe!==null&&ke>=0&&ke<F&&Fe>=0&&Fe<F&&Number.isFinite(Le)&&(E[Fe][ke]=!0,W[Fe][ke]=Le,I[Fe][ke]=1);const Ue=Number(ve?.x),Be=Number(ve?.y);Number.isFinite(Ue)&&Number.isFinite(Be)&&Number.isFinite(Le)&&z.push({pupilX:Ue,pupilY:Be,opd:Le,isVignetted:!1})}const V={gridSize:F,wavelength:w,rayData:z,gridData:{opd:W,amplitude:I,pupilMask:E,xCoords:P,yCoords:Z}};let K=24;try{const ue=Number(window.findStopSurfaceIndex?.(u)),ve=Number.isFinite(ue)&&ue>=0&&u?.[ue]?u[ue]:null,ke=ve&&ve.semidia!==void 0&&ve.semidia!==null?ve.semidia:ve&&ve.Semidia!==void 0&&ve.Semidia!==null?ve.Semidia:ve&&ve["Semi Diameter"]!==void 0&&ve["Semi Diameter"]!==null?ve["Semi Diameter"]:ve&&ve.aperture!==void 0&&ve.aperture!==null?ve.aperture:ve&&ve.Aperture!==void 0&&ve.Aperture!==null?ve.Aperture:NaN,Fe=Math.abs(parseFloat(ke));if(Number.isFinite(Fe)&&Fe>0){const Ue=ve&&(ve.aperture!==void 0||ve.Aperture!==void 0)?Fe*.5:Fe;Number.isFinite(Ue)&&Ue>0&&(K=Ue*2)}}catch{}let oe=100;try{if(typeof window.calculateFocalLength=="function"){const ue=Number(window.calculateFocalLength(u,w));Number.isFinite(ue)&&Math.abs(ue)>1e-9&&ue!==1/0&&(oe=Math.abs(ue))}}catch{}const{PSFCalculator:re}=await Re(async()=>{const{PSFCalculator:ue}=await import("./psf-calculator-CvRmJnrY.js");return{PSFCalculator:ue}},__vite__mapDeps([8,0,1])),fe=new re,se=l(),pe=await fe.calculatePSF(V,{samplingSize:F,wavelength:w,zeroPadTo:_,pupilDiameter:K,focalLength:oe,forceImplementation:"native",removeTilt:N,recenterIfWrapped:f}),me=l()-se,L=l(),X=await fe.calculatePSF(V,{samplingSize:F,wavelength:w,zeroPadTo:_,pupilDiameter:K,focalLength:oe,forceImplementation:"javascript",removeTilt:N,recenterIfWrapped:f}),Y=l()-L,J=ue=>Array.isArray(ue?.psfData)?ue.psfData:Array.isArray(ue?.psf)?ue.psf:[],le=ue=>ue?.metrics&&typeof ue.metrics=="object"?ue.metrics:{strehlRatio:Number(ue?.strehlRatio),fwhm:ue?.fwhm,centerPosition:ue?.centerPosition},Q=ue=>String(ue?.implementationUsed||ue?.metadata?.method||"unknown"),ee=J(pe),ie=J(X),be=le(pe),ae=le(X),he=Math.min(ee.length,ie.length),de=he>0?Math.min((ee[0]||[]).length,(ie[0]||[]).length):0,Ae=m(ee),xe=m(ie);let Ne=0,ye=0,Se=0,_e=0;for(let ue=0;ue<he;ue++)for(let ve=0;ve<de;ve++){const ke=Number(ee[ue]?.[ve]),Fe=Number(ie[ue]?.[ve]);if(!Number.isFinite(ke)||!Number.isFinite(Fe))continue;const Le=ke-Fe,Ue=Math.abs(Le);ye+=Le*Le,Se+=Ue,Ue>_e&&(_e=Ue),Ne++}const Ce=d(W,E,w),Me=Number(be?.strehlRatio),ge=Number(ae?.strehlRatio),we={options:{objectIndex:y,samplingSize:F,zeroPadTo:_,wavelength:w,opdDisplayMode:S,removeTilt:N,recenterIfWrapped:f===void 0?null:f,pupilDiameterMm:K,focalLengthMm:oe},timings:{wavefrontMs:G,nativeMs:me,javascriptMs:Y},backend:{native:Q(pe),javascript:Q(X),nativeMethod:String(pe?.metadata?.method||""),javascriptMethod:String(X?.metadata?.method||"")},diff:{comparedGrid:`${he}x${de}`,count:Ne,rmsAbs:Ne>0?Math.sqrt(ye/Ne):NaN,meanAbs:Ne>0?Se/Ne:NaN,maxAbs:_e},metrics:{native:be||null,javascript:ae||null,expected:Ce,delta:{strehl:Me-ge,fwhmAvgUm:Number(be?.fwhm?.average)-Number(ae?.fwhm?.average),centerX:Number(be?.centerPosition?.x)-Number(ae?.centerPosition?.x),centerY:Number(be?.centerPosition?.y)-Number(ae?.centerPosition?.y)}},grids:{native:p(ee).length,javascript:p(ie).length,nativeHash:Ae.hash,javascriptHash:xe.hash,nativeSum:Ae.sum,javascriptSum:xe.sum}};console.log("📊 [PSF Parity] native vs javascript",we);try{console.log(`[PSF Parity Summary] grid=${we.diff.comparedGrid} n=${we.diff.count} rmsAbs=${Number(we.diff.rmsAbs).toExponential(4)} meanAbs=${Number(we.diff.meanAbs).toExponential(4)} maxAbs=${Number(we.diff.maxAbs).toExponential(4)} strehl(native=${Number(Me).toFixed(6)},js=${Number(ge).toFixed(6)},Δ=${Number(we.metrics.delta.strehl).toExponential(4)}) marechal≈${Number(we.metrics.expected?.marechalStrehl).toFixed(6)} opdRms=${Number(we.metrics.expected?.rmsWaves).toExponential(4)}waves fwhmAvgΔ=${Number(we.metrics.delta.fwhmAvgUm).toExponential(4)}µm centerΔ=(${we.metrics.delta.centerX},${we.metrics.delta.centerY}) backend(native=${we.backend.native}, js=${we.backend.javascript})`)}catch{}return we},window.comparePsfNativeVsJsForObjects=async(i={})=>{const l=Ke(),m=Array.isArray(l)?l.length:0,d=(Array.isArray(i?.objectIndices)?i.objectIndices:m>=2?[0,1]:[0]).map(A=>Number(A)).filter(A=>Number.isFinite(A)&&A>=0&&A<Math.max(1,m));if(!d.length)throw new Error("No valid objectIndices. Example: [0, 1]");const u=[];for(const A of d){const x=await window.comparePsfNativeVsJs({...i,objectIndex:A});u.push(x)}const h=u.map(A=>({objectIndex:Number(A?.options?.objectIndex),nativeHash:A?.grids?.nativeHash,jsHash:A?.grids?.javascriptHash,nativeStrehl:Number(A?.metrics?.native?.strehlRatio),jsStrehl:Number(A?.metrics?.javascript?.strehlRatio),marechalStrehl:Number(A?.metrics?.expected?.marechalStrehl),opdRmsWaves:Number(A?.metrics?.expected?.rmsWaves),nativeFwhmAvg:Number(A?.metrics?.native?.fwhm?.average),jsFwhmAvg:Number(A?.metrics?.javascript?.fwhm?.average),centerNative:A?.metrics?.native?.centerPosition,centerJs:A?.metrics?.javascript?.centerPosition})),g=[];for(let A=1;A<h.length;A++){const x=h[A-1],_=h[A];g.push({fromObject:x.objectIndex,toObject:_.objectIndex,nativeHashChanged:x.nativeHash!==_.nativeHash,jsHashChanged:x.jsHash!==_.jsHash,nativeStrehlDelta:_.nativeStrehl-x.nativeStrehl,jsStrehlDelta:_.jsStrehl-x.jsStrehl,nativeFwhmDelta:_.nativeFwhmAvg-x.nativeFwhmAvg,jsFwhmDelta:_.jsFwhmAvg-x.jsFwhmAvg})}const y={objectIndices:d,byObject:h,variation:g};return console.log("📊 [PSF Object Compare] native vs javascript",y),y},window.runOPDProfiling=Hs,window.showMTFDiagram=Zn,window.showMTFComparisonDiagram=wa,window.showThroughFocusSpotDiagram=es,window.runSpotParityDiagnostics=Zu,window.showThroughFocusMTFDiagram=Jo,window.benchmarkMTFOnce=async(i={})=>{const l=typeof globalThis<"u"?globalThis:null,m=l?l.__WAVEFRONT_PROFILE:void 0,p=l?l.__OPD_DEBUG:void 0,d=l?l.__PSF_DEBUG:void 0,u=typeof performance<"u"&&typeof performance.now=="function"?()=>performance.now():()=>Date.now(),h=document.createElement("div");h.id=`mtf-benchmark-${Date.now()}`,h.style.cssText="position:fixed;left:-99999px;top:-99999px;width:800px;height:600px;overflow:hidden;",document.body.appendChild(h);try{l&&(l.__WAVEFRONT_PROFILE=!0,l.__OPD_DEBUG=!1,l.__PSF_DEBUG=!1);const g=u();await Zn({wavelengthMicrons:i.wavelengthMicrons??"all",objectIndex:Number.isFinite(Number(i.objectIndex))?Number(i.objectIndex):0,maxFrequencyLpmm:Number.isFinite(Number(i.maxFrequencyLpmm))?Number(i.maxFrequencyLpmm):100,samplingSize:Number.isFinite(Number(i.samplingSize))?Number(i.samplingSize):256,samplingPoints:Number.isFinite(Number(i.samplingPoints))?Number(i.samplingPoints):64,opdDisplayMode:i.opdDisplayMode??"pistonTiltRemoved",containerElement:h,onProgress:null});const y=u()-g,A=l&&typeof l.getRayTracingProfile=="function"?l.getRayTracingProfile({reset:!0}):null,x=Number(A?.traceCalls)||0,_=Number(A?.traceBatchLockstepCalls)||0,S=Number(A?.traceBatchLockstepRays)||0,N=x>0?_/x:0,f=_>0?S/_:0,w=y>0?x/y:0,b={elapsedMs:y,samplingSize:Number.isFinite(Number(i.samplingSize))?Number(i.samplingSize):256,samplingPoints:Number.isFinite(Number(i.samplingPoints))?Number(i.samplingPoints):64,wavelengthMicrons:i.wavelengthMicrons??"all",maxFrequencyLpmm:Number.isFinite(Number(i.maxFrequencyLpmm))?Number(i.maxFrequencyLpmm):100,objectIndex:Number.isFinite(Number(i.objectIndex))?Number(i.objectIndex):0,rayTracing:{traceCalls:x,traceCallsPerMs:w,lockstepCalls:_,lockstepRays:S,lockstepCallRatio:N,lockstepRaysPerCall:f},note:"See [OPD Profile] logs for wavefront/raytrace breakdown."};return console.log("📊 benchmarkMTFOnce result:",b),b}finally{try{h.remove()}catch{}l&&(l.__WAVEFRONT_PROFILE=m,l.__OPD_DEBUG=p,l.__PSF_DEBUG=d)}},window.benchmarkMTF=async(i={})=>{const l=Math.max(1,Math.floor(Number(i.runs)||3)),m=Math.max(0,Math.floor(Number(i.warmup)||1)),p=[],d=[],u=[];for(let S=0;S<m;S++)await window.benchmarkMTFOnce(i);for(let S=0;S<l;S++){const N=await window.benchmarkMTFOnce(i),f=Number(N?.elapsedMs);Number.isFinite(f)&&p.push(f);const w=Number(N?.rayTracing?.lockstepCallRatio);Number.isFinite(w)&&d.push(w);const b=Number(N?.rayTracing?.lockstepRaysPerCall);Number.isFinite(b)&&u.push(b)}const h=p.length>0?p.reduce((S,N)=>S+N,0)/p.length:NaN,g=p.length>0?Math.min(...p):NaN,y=p.length>0?Math.max(...p):NaN,A=d.length>0?d.reduce((S,N)=>S+N,0)/d.length:NaN,x=u.length>0?u.reduce((S,N)=>S+N,0)/u.length:NaN,_={runs:l,warmup:m,samples:p,avgMs:h,minMs:g,maxMs:y,lockstep:{avgCallRatio:A,avgRaysPerCall:x}};return console.log("📈 benchmarkMTF summary:",_),_},window.benchmarkMTFCompare=async(i={})=>{const l=String(i.beforeLabel??"before"),m=String(i.afterLabel??"after"),p={...i.beforeOptions??{}},d={...i.afterOptions??{}};i.runs!==void 0&&(p.runs=i.runs,d.runs=i.runs),i.warmup!==void 0&&(p.warmup=i.warmup,d.warmup=i.warmup);const u=await window.benchmarkMTF(p),h=await window.benchmarkMTF(d),g=Number(u?.avgMs),y=Number(h?.avgMs),A=Number(u?.minMs),x=Number(h?.minMs),_=Number(u?.maxMs),S=Number(h?.maxMs),N=Number.isFinite(g)&&Number.isFinite(y)?y-g:NaN,f=Number.isFinite(A)&&Number.isFinite(x)?x-A:NaN,w=Number.isFinite(_)&&Number.isFinite(S)?S-_:NaN,b=Number.isFinite(g)&&g>0&&Number.isFinite(y)?(g-y)/g*100:NaN,v=Number(u?.lockstep?.avgCallRatio),M=Number(h?.lockstep?.avgCallRatio),C=Number.isFinite(v)&&Number.isFinite(M)?M-v:NaN,D={labels:{before:l,after:m},before:u,after:h,delta:{avgMs:N,minMs:f,maxMs:w,avgImprovementPct:b,lockstepCallRatioDelta:C}};return console.log("🧪 benchmarkMTFCompare result:",D),D},window.compareMTFvsTFMTFAtFrequency=async(i={})=>{const l=Number.isFinite(Number(i?.frequencyLpmm))?Number(i.frequencyLpmm):10,m=i?.wavelengthMicrons==="all"?"all":Number.isFinite(Number(i?.wavelengthMicrons))?Number(i.wavelengthMicrons):.5876,p=Number.isFinite(Number(i?.objectIndex))?Number(i.objectIndex):0,d=Number.isFinite(Number(i?.samplingSize))?Number(i.samplingSize):256,u=Number.isFinite(Number(i?.zeroPadTo))?Number(i.zeroPadTo):0,h=String(i?.opdDisplayMode||"pistonTiltRemoved"),g=(_,S)=>{const N=Array.isArray(_?.x)?_.x:[],f=Array.isArray(_?.y)?_.y:[];if(!N.length||N.length!==f.length)return NaN;const w=N.map((b,v)=>({x:Number(b),y:Number(f[v])})).filter(b=>Number.isFinite(b.x)&&Number.isFinite(b.y)).sort((b,v)=>b.x-v.x);if(!w.length)return NaN;if(w.length===1||S<=w[0].x)return w[0].y;if(S>=w[w.length-1].x)return w[w.length-1].y;for(let b=1;b<w.length;b++){const v=w[b-1],M=w[b];if(S<=M.x&&M.x>v.x){const C=(S-v.x)/(M.x-v.x);return v.y+C*(M.y-v.y)}}return w[w.length-1].y},y=_=>{const S=(Array.isArray(_)?_:[]).filter(w=>w?.meta?.overlayType!=="diffractionLimit"),N=S.find(w=>/meridional|tangential/i.test(String(w?.name||""))),f=S.find(w=>/sagittal/i.test(String(w?.name||"")));return{meridional:N,sagittal:f,regularCount:S.length}},A=_=>{const S=Array.isArray(_?.x)?_.x:[],N=Array.isArray(_?.y)?_.y:[];if(!S.length||S.length!==N.length)return NaN;let f=0,w=1/0;for(let v=0;v<S.length;v++){const M=Number(S[v]);if(!Number.isFinite(M))continue;const C=Math.abs(M);C<w&&(w=C,f=v)}const b=Number(N[f]);return Number.isFinite(b)?b:NaN},x=document.createElement("div");x.id=`mtf-parity-${Date.now()}`,x.style.cssText="position:fixed;left:-99999px;top:-99999px;width:800px;height:600px;overflow:hidden;",document.body.appendChild(x);try{const _=await Zn({wavelengthMicrons:m,objectIndex:p,maxFrequencyLpmm:Math.max(20,l),samplingSize:d,zeroPadTo:u,opdDisplayMode:h,skipPlot:!0,containerElement:x}),S=Array.isArray(_?.traces)?_.traces:[],N=y(S),f=g(N.meridional,l),w=g(N.sagittal,l),b=await Jo({wavelengthMicrons:m,objectIndex:p,targetFrequencyLpmm:l,defocusMinMm:0,defocusMaxMm:0,steps:3,samplingSize:d,zeroPadTo:u,opdDisplayMode:h,containerElement:x}),v=Array.isArray(b?.traces)?b.traces:[],M=y(v),C=A(M.meridional),D=A(M.sagittal),B={conditions:{frequencyLpmm:l,wavelengthMicrons:m,objectIndex:p,samplingSize:d,zeroPadTo:u,opdDisplayMode:h},mtf:{meridional:f,sagittal:w,traceCount:N.regularCount},tfmtfAtDefocus0:{meridional:C,sagittal:D,traceCount:M.regularCount},delta:{meridional:Number.isFinite(f)&&Number.isFinite(C)?C-f:NaN,sagittal:Number.isFinite(w)&&Number.isFinite(D)?D-w:NaN}};return console.log("📊 [MTF vs TFMTF parity]",B),B}finally{try{x.remove()}catch{}}},window.compareNativeVsTsObjectMtf=async(i={})=>{const l=Number.isFinite(Number(i?.firstFrequencyLpmm))?Number(i.firstFrequencyLpmm):10,m=Number.isFinite(Number(i?.secondFrequencyLpmm))?Number(i.secondFrequencyLpmm):30,p=i?.wavelengthMicrons==="all"?"all":Number.isFinite(Number(i?.wavelengthMicrons))?Number(i.wavelengthMicrons):.5876,d=Number.isFinite(Number(i?.samplingSize))?Number(i.samplingSize):256,u=Number.isFinite(Number(i?.zeroPadTo))?Number(i.zeroPadTo):0,h=Number.isFinite(Number(i?.fieldMin))?Number(i.fieldMin):0,g=Number.isFinite(Number(i?.fieldMax))?Number(i.fieldMax):10,y=Number.isFinite(Number(i?.steps))?Math.max(3,Math.floor(Number(i.steps))):21,A=i?.fieldAxisMode==="height"||i?.fieldAxisMode==="angle"?i.fieldAxisMode:"angle",x=String(i?.opdDisplayMode||"pistonTiltRemoved"),_=document.createElement("div");_.id=`field-mtf-compare-${Date.now()}`,_.style.cssText="position:fixed;left:-99999px;top:-99999px;width:900px;height:650px;overflow:hidden;",document.body.appendChild(_);const S=w=>{const b=String(w?.name||""),v=Array.isArray(w?.x)?w.x.map(C=>Number(C)):[],M=Array.isArray(w?.y)?w.y.map(C=>Number(C)):[];return!v.length||v.length!==M.length?null:{name:b,x:v,y:M}},N=w=>{const b={};for(const v of Array.isArray(w)?w:[]){const M=S(v);M&&(b[M.name]={x:M.x,y:M.y})}return b},f=(w,b)=>{if(!w||!Array.isArray(w.x)||!Array.isArray(w.y)||w.x.length!==w.y.length||w.x.length===0)return NaN;let v=0,M=1/0;for(let D=0;D<w.x.length;D++){const B=Number(w.x[D]);if(!Number.isFinite(B))continue;const j=Math.abs(B-b);j<M&&(M=j,v=D)}const C=Number(w.y[v]);return Number.isFinite(C)?C:NaN};try{const w=window.runDesktopNativeFieldMtfForPopup;if(typeof w!="function")throw new Error("runDesktopNativeFieldMtfForPopup is not available");const b=await w({objectIndex:0,wavelengths:p==="all"?[]:[Number(p)],firstFrequencyLpmm:l,secondFrequencyLpmm:m,fieldMin:h,fieldMax:g,steps:y,samplingSize:d,zeroPadTo:u,opdDisplayMode:x,fieldAxisMode:A}),v=await va({wavelengthMicrons:p,firstFrequencyLpmm:l,secondFrequencyLpmm:m,fieldMin:h,fieldMax:g,steps:y,samplingSize:d,zeroPadTo:u,opdDisplayMode:x,fieldAxisMode:A,containerElement:_}),M=Array.isArray(b?.series)?b.series[0]:null,C=Array.isArray(b?.xAxis)?b.xAxis.map(U=>Number(U)):[],D=[{name:`Meridional ${l.toFixed(1)} lp/mm`,x:C,y:Array.isArray(M?.meridionalFirst)?M.meridionalFirst:[]},{name:`Sagittal ${l.toFixed(1)} lp/mm`,x:C,y:Array.isArray(M?.sagittalFirst)?M.sagittalFirst:[]},{name:`Meridional ${m.toFixed(1)} lp/mm`,x:C,y:Array.isArray(M?.meridionalSecond)?M.meridionalSecond:[]},{name:`Sagittal ${m.toFixed(1)} lp/mm`,x:C,y:Array.isArray(M?.sagittalSecond)?M.sagittalSecond:[]}],B=N(Array.isArray(v?.traces)?v.traces:[]),H=(C.length?C:Array.isArray(Object.values(B)?.[0]?.x)?Object.values(B)[0].x:[]).map(U=>{const G=f({x:C,y:D[0].y},U),F=f({x:C,y:D[1].y},U),W=f({x:C,y:D[2].y},U),I=f({x:C,y:D[3].y},U),E=f(B[Object.keys(B).find(k=>k.startsWith(`Meridional ${l.toFixed(1)} lp/mm`))||""],U),P=f(B[Object.keys(B).find(k=>k.startsWith(`Sagittal ${l.toFixed(1)} lp/mm`))||""],U),Z=f(B[Object.keys(B).find(k=>k.startsWith(`Meridional ${m.toFixed(1)} lp/mm`))||""],U),R=f(B[Object.keys(B).find(k=>k.startsWith(`Sagittal ${m.toFixed(1)} lp/mm`))||""],U);return{field:U,native:{m1:G,s1:F,m2:W,s2:I},ts:{m1:E,s1:P,m2:Z,s2:R},delta:{m1:Number.isFinite(G)&&Number.isFinite(E)?G-E:NaN,s1:Number.isFinite(F)&&Number.isFinite(P)?F-P:NaN,m2:Number.isFinite(W)&&Number.isFinite(Z)?W-Z:NaN,s2:Number.isFinite(I)&&Number.isFinite(R)?I-R:NaN}}}),ce={conditions:{wl:p,freq1:l,freq2:m,fieldMin:h,fieldMax:g,steps:y,samplingSize:d,zeroPadTo:u,axisMode:A,opdDisplayMode:x},native:{xAxis:C,series:M},tsTraceNames:Object.keys(B),perField:H};return console.log("📊 [Object MTF Native vs TS]",ce),ce}finally{try{_.remove()}catch{}}},window.outputParaxialDataToDebug=Gs,window.outputSeidelCoefficientsToDebug=Vs,window.outputDebugSystemData=qs,window.displayCoordinateTransformMatrix=Zs,window.renderBlockContributionSummaryFromSeidel=Ys,window.renderSystemConstraintsFromSurfaceRows=Xs,window.debugTableStatus=Zo,window.initializeTablesWithDummyData=Ks,window.generateZMXText=qc,window.downloadZMX=Zc,window.parseZMXTextToOpticalSystemRows=Js,window.parseZMXArrayBufferToOpticalSystemRows=Jr,window.calculateFocalLength=Yr,window.calculateParaxialData=On,window.calculateEntrancePupilDiameter=Qs,window.calculateImageSpaceDiffractionParams=Dn,window.derivePupilAndFocalLengthMmFromParaxial=qr,window.findStopSurfaceIndex=Zr,window.calculateAllSurfacesLocalCoordinates=ea,window.resetToSurfaceCoordinates=ta,window.shiftToChiefRayOrigin=na,window.restoreFromLocalCoordinates=oa,window.transformToChiefRayLocalCoordinates=ra,window.calculateChiefRaySurfaceIntersections=ia,window.updateTransformSurfaceSelect=ji,window.loadSystemConfigurations=Ie,window.saveSystemConfigurations=Te,window.loadActiveConfigurationToTables=En,ri(),ii("refreshBlockInspector",Je,{overwrite:!0}),window.expandBlocksToOpticalSystemRows=$e,window.getActiveConfiguration=ba,window.loadSourceTableData=Ft,window.saveSourceTableData=Ln,window.loadObjectTableData=ei,window.saveObjectTableData=bo,window.loadSystemRequirementsTableData=ti,window.saveSystemRequirementsTableData=Qr,window.initializeConfigurationUI=qn,setTimeout(()=>{try{const i=je(qo);window.renderSystemConstraintsFromSurfaceRows?.(i)}catch{}},0),window.outputChiefRayConvergenceData=Ku,Do({scene:t,camera:n,renderer:o,controls:r}),{scene:t,camera:n,renderer:o,controls:r,ambientLight:a,directionalLight:c}}catch(e){throw e}}function rp(e={}){const n={...{crossSectionOnly:!1,showSurfaceOrigins:!1,showSemidiaRing:!0,showMirrorBackText:!1,crossSectionDirection:"YZ",crossSectionCenterOffset:0,opticalSystemData:null},...e};try{if(n.opticalSystemData||(n.opticalSystemData=je()),!n.opticalSystemData||n.opticalSystemData.length===0)return;const r=n.opticalSystemData[0]?.thickness,a=r==="INF"||r==="Infinity"||r===1/0?"infinite":"finite",c=window.lastSystemType||null,i=c&&c!==a,l=it?.(),m=xt?.();i&&(m&&m.clear(),l&&[...l.children].forEach(d=>{l.remove(d),d.geometry&&d.geometry.dispose(),d.material&&(Array.isArray(d.material)?d.material.forEach(u=>u.dispose()):d.material.dispose())})),os(a),Bn({opticalSystemData:n.opticalSystemData,scene:l||document.scene,crossSectionOnly:n.crossSectionOnly,showSemidiaRing:n.showSemidiaRing,showSurfaceOrigins:n.showSurfaceOrigins,showMirrorBackText:n.showMirrorBackText,crossSectionDirection:n.crossSectionDirection,crossSectionCenterOffset:n.crossSectionCenterOffset})}catch(o){console.error("[RenderWindow] drawOpticalSystemSurfaceWrapper failed:",o)}}function ip(){try{const e=it?.(),t=Pt?.(),n=Zt?.(),o=xt?.();e&&Fn(e);const r=je();if(!r||r.length===0)return;Bn({opticalSystemData:r,scene:e,crossSectionOnly:!1,showSurfaceOrigins:!1,showSemidiaRing:!0,showMirrorBackText:!1,crossSectionDirection:"YZ",crossSectionCenterOffset:0}),typeof window.adjustCameraView=="function"&&window.adjustCameraView(e,t,n,o)}catch(e){console.error("[RenderWindow] improvedDrawOpticalSystemSurfaceWrapper failed:",e)}}function os(e){window.lastSystemType=e}function $r(e){window.currentDrawCrossRays=e}function sp(e){try{const t=Ke(),n=it?.();if(!n||!t||t.length===0||!e||e.length===0)return;t.forEach((o,r)=>{const s=document.getElementById("render-ray-count-input")||document.getElementById("draw-ray-count-input"),a=s&&parseInt(s.value||"5",10)||5,c=o?.position==="Angle"||o?.position==="angle",i=jn(o,e,a,null,{aimThroughStop:!!c,useChiefRayAnalysis:!0,allowStopBasedOriginSolve:!0,disableCrossExtent:!0});if(!Array.isArray(i)||i.length===0)return;let l=0;for(const m of i)if(!(!m||!m.startP||!m.dir)){if(l>=a)break;try{const p={pos:m.startP,dir:m.dir};console.log(`🔍 正確光線${l} for object ${r}: start=(${p.pos.x}, ${p.pos.y}, ${p.pos.z}), dir=(${p.dir.x}, ${p.dir.y}, ${p.dir.z})`);const d=window.traceRay?window.traceRay(e,p,1,null,null,{allowNonStrict:!0,requireWasmRayTracing:!1,useRustWasm:!0,disableWasmRayTracing:!1}):null;if(d&&d.length>1){console.log(`   開始位置確認: (${d[0].x.toFixed(3)}, ${d[0].y.toFixed(3)}, ${d[0].z.toFixed(3)})`);const u=d.map(A=>new window.THREE.Vector3(A.x,A.y,A.z)),h=new window.THREE.BufferGeometry().setFromPoints(u),g=new window.THREE.LineBasicMaterial({color:65280+r*13056}),y=new window.THREE.Line(h,g);y.userData={type:"optical-ray",objectId:r,rayNumber:l,rayType:"accurate",isRayLine:!0,accurateRayTracing:!0},n.add(y)}}catch{}l++}})}catch{}}function ap(){try{const e=Pt?.(),t=Zt?.(),n=it?.(),o=xt?.();if(!e||!t||!n)return;const{minZ:r,maxZ:s,centerZ:a,totalLength:c,maxY:i}=gn(),l=a,p=Math.max(c,i*2),d=Math.max(p*1.5,600);e.position.set(d*.7,d*.5,l),e.lookAt(0,0,l),e.up.set(0,1,0),t.target.set(0,0,l),t.update(),e.updateProjectionMatrix(),o&&o.render(n,e)}catch{}}function gn(){try{const e=je();if(!e||e.length===0)return{minZ:0,maxZ:414,centerZ:207,totalLength:414,maxY:50};const t=xo(e);if(!t||t.length===0)return{minZ:0,maxZ:414,centerZ:207,totalLength:414,maxY:50};const n=[];let o=0;if(t.forEach((i,l)=>{if(i&&i.origin){const m=i.origin.z;isFinite(m)&&n.push(m)}}),e.forEach((i,l)=>{const m=parseFloat(i.semidia);isFinite(m)&&m>0&&(o=Math.max(o,m))}),n.length===0)return{minZ:0,maxZ:414,centerZ:207,totalLength:414,maxY:o||50};const r=Math.min(...n),s=Math.max(...n),a=(r+s)/2,c=s-r;return{minZ:r,maxZ:s,centerZ:a,totalLength:c,maxY:o}}catch{return{minZ:0,maxZ:414,centerZ:207,totalLength:414,maxY:50}}}function cp(e,t){try{if(!e||!Array.isArray(e)||e.length===0||!t||t.length===0)return;const n=u=>{const g=String(u?.surfType??u?.["surf type"]??u?.surface_type??"").toLowerCase().trim();return g==="coord trans"||g==="coordinate break"||g==="coordtrans"||g==="coordinatebreak"||g==="ct"},o=u=>String(u?.["object type"]??u?.object??u?.Object??"").toLowerCase()==="object",r=(u,h)=>{if(!Array.isArray(u)||h===null||h===void 0)return null;const g=Math.max(0,Math.min(h,u.length-1));let y=0;for(let A=0;A<=g;A++){const x=u[A];n(x)||o(x)||y++}return y>0?y:null},s=(u,h,g)=>{if(!Array.isArray(u))return null;const y=r(h,g);return y===null?null:y>=0&&y<u.length?u[y]:null},a=t.length-1,c=t[a],i=xo(t),l=Array.isArray(i)?i[a]:null;if(!(c.optimizeSemiDia==="U"||c.semidia==="Auto"))return;const p=e.filter(u=>(u.beamType||u.type||"").toLowerCase().includes("chief"));if(p.length===0)return;let d=0;p.forEach((u,h)=>{if(!u.rayPath||!Array.isArray(u.rayPath))return;const g=s(u.rayPath,t,a);if(g&&Number.isFinite(g.x)&&Number.isFinite(g.y)){const y=l?da(g,l):g,A=u.objectPosition||u.originalRay?.objectPosition||null;let x=0;if(A&&(A.x||A.y)){const _=Math.abs(Number(A.x)||0),S=Math.abs(Number(A.y)||0);x=_>S?Math.abs(Number(y.x)||0):Math.abs(Number(y.y)||0)}else x=Math.max(Math.abs(Number(y.x)||0),Math.abs(Number(y.y)||0));console.log(`   主光線${h}: Image面ローカル高さ = ${x.toFixed(6)}`),d=Math.max(d,x)}}),d>0&&(c.semidia=d,window.tableOpticalSystem&&window.tableOpticalSystem.updateData([c]))}catch{}}function lp(){const e=Pt?.();if(e&&e.isOrthographicCamera)try{const t=it?.(),n=up(t),o=gn();if(!o)return;let{minZ:r,maxZ:s,centerZ:a,totalLength:c,maxY:i}=o;if(n){r=Math.min(r,n.min.z),s=Math.max(s,n.max.z),a=(r+s)/2,c=s-r;const f=n.max.y-n.min.y;Number.isFinite(f)&&f>0&&(i=Math.max(i||0,f/2))}const m=Math.min(r,-25),d=s-m;let u=1.5;const h=xt?.();if(h){const f=h.getSize(new nt);u=f.x/f.y}const g=1.1,A=(Number.isFinite(i)&&i>0?i:50)*2*g,x=d*g;let _,S;x/Math.max(1e-9,A)>u?(S=x/2,_=S/u):(_=A/2,S=_*u),e.left=-S,e.right=S,e.top=_,e.bottom=-_,e.updateProjectionMatrix()}catch{}}window.updateCameraViewBounds=lp;function up(e){try{if(!e)return null;const t=new so;let n=!1;return e.traverse(o=>{if(!o||o.visible===!1||!(o.isMesh||o.isLine||o.isGroup)||o.type==="GridHelper"||o.type==="AxesHelper"||o.type==="AmbientLight"||o.type==="DirectionalLight")return;const r=String(o.name||""),s=o.userData||{};if(!!!(s.isOpticalElement||s.isLensSurface||s.isRayLine||s.type==="ray"||s.type==="surfaceProfile"||s.type==="semidiaRing"||s.type==="ring"||s.type==="crossSection"||s.surfaceIndex!==void 0||/surface|lens|cross-section|semidia|mirror|profile|ring|connection/i.test(r)))return;const c=new so().setFromObject(o);c.isEmpty()||(t.union(c),n=!0)}),n?t:null}catch{return null}}function rs(e={}){try{const t=e.camera||Pt?.(),n=e.controls||Zt?.(),o=e.scene||it?.(),r=e.renderer||xt?.();if(!t||!n||!o)return;const{minZ:s,maxZ:a,centerZ:c,totalLength:i,maxY:l}=gn(),p=e.includeRayStartMargin!==!1?25:0,d=Math.min(s,-p),u=a,h=u-d,g=(d+u)/2,y=t?.userData?.__drawCrossOrthoBounds,A=e.preserveDrawCrossBounds===!0&&y,x=Number.isFinite(e.centerZOverride)?e.centerZOverride:A&&Number.isFinite(y.centerZ)?y.centerZ:g,_=e.targetOverride&&Number.isFinite(e.targetOverride.x)&&Number.isFinite(e.targetOverride.y)&&Number.isFinite(e.targetOverride.z)?e.targetOverride:null;let S=1.5;if(r){const D=r.getSize(new nt);S=D.x/D.y}const N=1.1,f=l*2*N,w=h*N;if(t.isOrthographicCamera)if(A)t.left=y.left,t.right=y.right,t.top=y.top,t.bottom=y.bottom;else{let D,B;w/f>S?(B=w/2,D=B/S):(D=f/2,B=D*S),t.left=-B,t.right=B,t.top=D,t.bottom=-D}const b=300,v=_?_.x:0,M=_?_.y:0,C=_?_.z:x;t.position.set(v-b,M,C),t.lookAt(v,M,C),t.up.set(0,1,0),n.target.set(v,M,C),n.update(),t.updateProjectionMatrix(),e.storeDrawCrossBounds===!0&&t.isOrthographicCamera&&(t.userData.__drawCrossOrthoBounds={left:t.left,right:t.right,top:t.top,bottom:t.bottom,centerZ:C}),r&&o&&r.render(o,t)}catch{}}function pp(e={}){try{const t=e.camera||Pt?.(),n=e.controls||Zt?.(),o=e.scene||it?.(),r=e.renderer||xt?.();if(!t||!n||!o)return;const s=gn();if(!s)return;const{minZ:a,maxZ:c,maxY:i}=s,m=e.includeRayStartMargin!==!1?25:0,p=Math.min(a,-m),d=c,u=d-p,h=(p+d)/2,g=t?.userData?.__drawCrossOrthoBounds,y=e.preserveDrawCrossBounds===!0&&g,A=Number.isFinite(e.centerZOverride)?e.centerZOverride:y&&Number.isFinite(g.centerZ)?g.centerZ:h,x=e.targetOverride&&Number.isFinite(e.targetOverride.x)&&Number.isFinite(e.targetOverride.y)&&Number.isFinite(e.targetOverride.z)?e.targetOverride:null;let _=1.5;if(r){const C=r.getSize(new nt);_=C.x/C.y}const S=1.1,N=i*2*S,f=u*S;if(t.isOrthographicCamera)if(y)t.left=g.left,t.right=g.right,t.top=g.top,t.bottom=g.bottom;else{let C,D;f/N>_?(D=f/2,C=D/_):(C=N/2,D=C*_),t.left=-D,t.right=D,t.top=C,t.bottom=-C}const w=e.cameraDistance||300,b=x?x.x:0,v=x?x.y:0,M=x?x.z:A;t.position.set(b,v+w,M),t.lookAt(b,v,M),t.up.set(1,0,0),t.updateProjectionMatrix(),n.target.set(b,v,M),n.update(),r&&o&&r.render(o,t)}catch{}}function dp(){console.log("🖼️ Debugging 3D canvas status...");const e=document.getElementById("threejs-canvas-container"),t=xt?.(),n=it?.(),o=Pt?.(),r=Zt?.(),s=t?.domElement;if(console.log("Canvas container:",!!e),e&&(console.log("Container dimensions:",e.offsetWidth,"x",e.offsetHeight),console.log("Container style:",e.style.cssText)),console.log("Canvas element:",!!s),s&&(console.log("Canvas dimensions:",s.width,"x",s.height),console.log("Canvas style:",s.style.cssText),console.log("Canvas parent:",s.parentElement?.id)),console.log("Renderer:",!!t),t){const a=t.getSize(new nt);console.log("Renderer size:",a.x,"x",a.y)}return console.log("Scene children count:",n?.children?.length||0),console.log("Camera position:",o?.position),console.log("Controls target:",r?.target),{canvasContainer:!!e,canvas:!!s,renderer:!!t,scene:!!n,camera:!!o,controls:!!r}}window.drawOpticalSystemSurfaceWrapper=rp;window.improvedDrawOpticalSystemSurfaceWrapper=ip;window.drawOptimizedRaysFromObjects=sp;window.generateRayStartPointsForObject=jn;window.drawRayWithSegmentColors=si;window.setRayEmissionPattern=io;window.getRayEmissionPattern=sa;window.setRayColorMode=cn;window.getRayColorMode=ao;window.fitCameraToOpticalSystem=ap;window.setCameraForYZCrossSection=rs;window.setCameraForXZCrossSection=pp;window.calculateOpticalSystemZRange=gn;window.debug3DCanvas=dp;window.traceRay=ai;window.getOpticalSystemRows=je;window.getObjectRows=Ke;window.getSourceRows=rt;window.generateSurfaceOptions=Xr;window.initializeApplication=ns;ri();ii("updateSurfaceNumberSelect",Li,{overwrite:!0});const _n=(()=>{let e=!1,t=!1;const n=()=>{if(!t){t=!0;try{window.__cooptMainReady=!0,window.dispatchEvent(new CustomEvent("coopt:main-ready"))}catch(o){console.warn("⚠️ [Init] Failed to dispatch coopt:main-ready",o)}}};return async()=>{if(!e){e=!0;try{Ka();const o=await ns();if(!o)throw new Error("Failed to initialize application components");n(),setTimeout(()=>{Promise.resolve(op()).catch(d=>{console.error("❌ [PhaseC Autorun] Unexpected startup failure:",d)})},0),o&&(Do({scene:o.scene,camera:o.camera,renderer:o.renderer,controls:o.controls}),window.ambientLight=o.ambientLight,window.directionalLight=o.directionalLight),window.tableObject=oi,window.tableSource=Rn,setTimeout(()=>{try{Promise.resolve(hu()).catch(d=>{})}catch{}},0);const r=document.getElementById("debug-object-data");r&&r.addEventListener("click",()=>{const d=window.getObjectRows?window.getObjectRows():[],u=document.getElementById("wavefront-object-select"),h=u?parseInt(u.value):0;if(console.log(`  Object総数: ${d.length}`),console.log(`  選択インデックス: ${h}`),console.log(`  ドロップダウン存在: ${!!u}`),d.length===0){alert("Objectデータが読み込まれていません。JSONファイルをロードしてください。");return}d.forEach((A,x)=>{console.log(`  Object ${x+1}:`,A),console.log(`    Type: ${A.Type||A.type||"未設定"}`),console.log(`    X: ${A.X||A.x||"未設定"}`),console.log(`    Y: ${A.Y||A.y||"未設定"}`);const _=A.Type==="Angle"||A.type==="Angle";if(console.log(`    角度タイプ: ${_}`),_){const S=parseFloat(A.X||A.x||0),N=parseFloat(A.Y||A.y||0);console.log(`    画角: X=${S}°, Y=${N}°`)}});const g=d[h]||d[0];console.log("  データ:",g);const y=ss(g,h);console.log("  変換後フィールド設定:",y),console.log('💡 [ObjectDebug] ヒント: コンソールをクリアするには、ブラウザのF12で開発者ツールを開き、コンソールタブで右クリック→"Clear console"を選択してください。')});const s=document.getElementById("debug-ray-angles");s&&s.addEventListener("click",()=>{window.debugOPDRayAngles?window.debugOPDRayAngles():console.log("💡 [RayAngleDebug] debug-opd-ray-angles.jsが正しく読み込まれているか確認してください")});const a=document.getElementById("draw-cross-btn");a&&a.addEventListener("click",async()=>{try{a.disabled=!0,a.textContent="Generating...";const d=je();if(!d||d.length===0){alert("光学系データが設定されていません。");return}const h=d[0]?.thickness,g=h==="INF"||h==="Infinity"||h===1/0,y=g?"infinite":"finite",A=window.lastSystemType||null;if(A&&A!==y){const H=xt?.(),ce=it?.();H&&H.clear(),ce&&[...ce.children].forEach(G=>{ce.remove(G),G.geometry&&G.geometry.dispose(),G.material&&(Array.isArray(G.material)?G.material.forEach(F=>F.dispose()):G.material.dispose())})}os(y);const _=Ke();if(!_||_.length===0){alert("Objectが設定されていません。");return}const S=[];_.forEach((H,ce)=>{let U;if(Array.isArray(H)){const G=parseFloat(H[1]),F=parseFloat(H[2]);U={x:G||0,y:F||0,z:0}}else{const G=parseFloat(H.xHeightAngle)||0,F=parseFloat(H.yHeightAngle)||0;U={x:G,y:F,z:0}}S.push(U)});const N=document.getElementById("draw-ray-count-input"),f=N&&parseInt(N.value,10)||7,w=document.getElementById("transverse-surface-select");let b=null;if(w&&w.value!=="")b=parseInt(w.value)-1;else{const H=d.findIndex(ce=>ce&&(ce["object type"]==="Image"||ce.object==="Image"));b=H>=0?H:Math.max(0,d.length-1)}let v;const M=typeof window.getPrimaryWavelength=="function"&&Number(window.getPrimaryWavelength())||.5876;if(g){const H=S.map(ce=>({x:ce.x||0,y:ce.y||0}));v=await ci(d,H,{rayCount:f,debugMode:!1,wavelength:M,crossType:"both",targetSurfaceIndex:b,angleUnit:"deg",chiefZ:-20})}else v=await So(d,S,{rayCount:f,debugMode:!1,wavelength:M,crossType:"both"});if(!v.success){alert(`クロスビーム生成失敗: ${v.error}`);return}let C=[],D=0,B=0;v.results&&Array.isArray(v.results)?(v.results.forEach((H,ce)=>{console.log(`   Result${ce+1}:`,H),H.rays&&Array.isArray(H.rays)&&(C=C.concat(H.rays),console.log(`   Result${ce+1} 光線数: ${H.rays.length}`))}),D=v.results.length,B=v.results.length):v.allCrossBeamRays&&Array.isArray(v.allCrossBeamRays)&&v.allTracedRays&&Array.isArray(v.allTracedRays)?(C=v.allTracedRays.map((H,ce)=>{const U=v.allCrossBeamRays[ce];return U&&(H.type=U.type,H.beamType=U.beamType),H}),D=v.processedObjectCount||0,B=v.objectCount||0):v.allCrossBeamRays&&Array.isArray(v.allCrossBeamRays)?(C=v.allCrossBeamRays,D=v.processedObjectCount||0,B=v.objectCount||0):v.allTracedRays&&Array.isArray(v.allTracedRays)?(C=v.allTracedRays,D=v.processedObjectCount||0,B=v.objectCount||0):v.tracedRays&&Array.isArray(v.tracedRays)?(C=v.tracedRays,D=1,B=1):Array.isArray(v)&&(C=v,D=1,B=1);const j=it?.();if(j&&Fn(j),Bn({opticalSystemData:d,scene:j||document.scene,showSemidiaRing:!0,showSurfaceOrigins:!1,crossSectionOnly:!1}),rs(),C.length>0){const H={};C.forEach(U=>{const G=U.objectIndex||0;H[G]=(H[G]||0)+1}),console.log("   Object分布:",H);const ce=C.filter(U=>U&&U.success&&Array.isArray(U.rayPath)&&U.rayPath.length>0);$r(ce.map(U=>({orientation:(()=>{const F=[U.beamType,U.type,U.originalRay?.type,U.originalRay?.beamType].filter(Boolean).map(W=>String(W).toLowerCase()).join(" ");return F.includes("horizontal")||F.includes("x")?"horizontal":F.includes("vertical")||F.includes("y")?"vertical":"unknown"})(),rayPath:U.rayPath,objectIndex:U.objectIndex??U.originalRay?.objectIndex??0,crossParameter:U.originalRay?.crossParameter??U.crossParameter??null,description:U.description||U.originalRay?.description||"",source:U}))),console.log("Stored draw-cross rays for overlay:",window.currentDrawCrossRays.length),is(C,j)}else $r([]);window.crossBeamResult=v,window.lastGeneratedRays=C,cp(C,d)}catch(d){alert(`クロスビーム描画エラー: ${d.message}`)}finally{a.disabled=!1,a.textContent="Draw Cross"}});const c=document.getElementById("undo-btn"),i=document.getElementById("redo-btn");c&&c.addEventListener("click",()=>{window.undoHistory&&window.undoHistory.undo()}),i&&i.addEventListener("click",()=>{window.undoHistory&&window.undoHistory.redo()});const l=document.getElementById("toggle-toolbar-btn"),m=document.getElementById("top-buttons-row"),p=l?.getAttribute("data-toggle-handled")==="react";l&&m&&!p&&(l.addEventListener("click",()=>{const d=m.classList.toggle("collapsed");l.classList.toggle("collapsed",d),ua(d)}),pa()&&(m.classList.add("collapsed"),l.classList.add("collapsed")))}catch(o){alert(`Failed to initialize application: ${o.message}`)}}}})(),Ur=()=>{if(!!document.getElementById("react-root")){if(window.__cooptReactMounted){_n();return}window.addEventListener("coopt:react-mounted",()=>{_n()},{once:!0}),setTimeout(()=>{window.__cooptReactMounted||(console.warn("⚠️ [Init] React mount event timeout, starting anyway"),_n())},2e3)}else _n()};typeof document<"u"&&document?.addEventListener&&(document.readyState==="loading"?document.addEventListener("DOMContentLoaded",Ur):Ur());function is(e,t){const n=t||it?.();if(!e||e.length===0)return;const o=new Set(["chief","left_marginal","right_marginal","upper_marginal","lower_marginal","horizontal_cross","vertical_cross"]);e.forEach(a=>{if(a?.originalRay?.type==="boundary"){const c=a.originalRay.side||a.side;c==="left"?a.originalRay.type="left_marginal":c==="right"?a.originalRay.type="right_marginal":c==="upper"||c==="top"?a.originalRay.type="upper_marginal":(c==="lower"||c==="bottom")&&(a.originalRay.type="lower_marginal")}});const r=e.filter(a=>{const c=a?.originalRay?.type;return!(!(a&&a.success&&c&&o.has(c))||a.fallback||(Array.isArray(a.rayPath)?a.rayPath:Array.isArray(a.rayPathToTarget)?a.rayPathToTarget:[]).filter(m=>m&&typeof m.x=="number"&&typeof m.y=="number"&&typeof m.z=="number").length===0)});if(r.length,e.length,r.filter(a=>a.fallback).length,e=r,!n)return;let s=null;try{if(s=ao(),s!=="object")try{cn("object")}catch{}const a={};e.forEach(l=>{const m=l.objectIndex||0;a[m]=(a[m]||0)+1});const c=e.map(l=>Number.parseInt(String(l?.objectIndex??l?.originalRay?.objectIndex??l?.originalRay?.objIndex??NaN),10)).filter(l=>Number.isFinite(l)),i=c.length>0&&!c.includes(0)&&c.every(l=>l>=1);e.forEach((l,m)=>{if(!l.success)return;const p=l.rayPath;if(!p||p.length===0)return;const d=l.objectIndex??l.originalRay?.objectIndex??l.originalRay?.objIndex??0,u=Number.parseInt(String(d),10),h=Number.isFinite(u)?Math.max(0,u):0,g=i?Math.max(0,h-1):h,y=l.objectPosition,A=l.originalRay||{},x=(A.type||"").toString(),_=(A.side||"").toString();let S=l.beamType;if(!S){const M=x.toLowerCase(),C=_.toLowerCase();M.includes("horizontal")?S="horizontal":M.includes("vertical")?S="vertical":C==="left"||C==="right"?S="horizontal":C==="upper"||C==="lower"||C==="top"||C==="bottom"?S="vertical":M==="chief"?S="chief":S="vertical"}const N=_.toLowerCase()==="top"?"upper":_.toLowerCase()==="bottom"?"lower":_||"center";y&&console.log(`   Object${g+1}位置: (${y.x}, ${y.y}, ${y.z})`);const f=ao();let w;const b=la;if(f==="object")w=b.getColor(b.MODE.OBJECT,g,null);else if(f==="segment"){const M=l.segmentType||"chief";w=b.getColor(b.MODE.SEGMENT,0,M)}else w=16777215;l.optimized;let v;S==="chief"?v=`cross-horizontal-obj${g}`:S==="horizontal"?v=`cross-horizontal-obj${g}`:S==="vertical"?v=`cross-vertical-obj${g}`:v=`cross-vertical-obj${g}`,si(p,v,m,n)})}catch{}finally{if(s&&s!=="object")try{cn(s)}catch{}}}window.drawCrossBeamRays=is;window.generateInfiniteSystemCrossBeam=ci;window.generateCrossBeam=So;window.drawOpticalSystemSurfaces=Bn;window.calculateChiefRayNewton=li;window.findStopSurface=ui;window.isFiniteSystem=function(e){if(e&&e.length>0){const n=e[0].thickness;if(n==="INF"||n===1/0)return!1;const o=parseFloat(n);return Number.isFinite(o)&&o>0}return!1};window.mainDebugFunctions={generateCrossBeam:So,calculateChiefRayNewton:li,traceRay:ai,findStopSurface:ui,calculateSurfaceOrigins:xo,isFiniteSystem:window.isFiniteSystem};window.mainDebugFunctions.generateDistortionPlots=aa;window.mainDebugFunctions.calculateDistortionData=ca;function ss(e,t){if(!e)return{fieldAngle:{x:0,y:0},xHeight:0,yHeight:0,displayName:"On-Axis (No Data)"};if(e.position==="Angle"||e.Type==="Angle"||e.type==="Angle"){const o=parseFloat(e.xHeightAngle||e.X||e.x||0),r=parseFloat(e.yHeightAngle||e.Y||e.y||0);return{fieldAngle:{x:o,y:r},fieldType:"Angle",displayName:`Object ${t+1} - ${o}°, ${r}°`}}else{const o=parseFloat(e.xHeight||e.X||e.x||0),r=parseFloat(e.yHeight||e.Y||e.y||0);return{xHeight:o,yHeight:r,fieldType:"Rectangle",displayName:`Object ${t+1} - ${o}mm, ${r}mm`}}}window.convertObjectToFieldSetting=ss;function wp(){return on}const oo=document.getElementById("analysis-select");oo&&oo.getAttribute("data-react-handled")!=="1"&&oo.addEventListener("change",e=>{const t=e.target,n=t.value;if(!n)return;const r={"spot-diagram":"open-spot-diagram-window-btn","spherical-aberration":"open-spherical-aberration-window-btn",astigmatism:"open-astigmatism-window-btn",distortion:"open-distortion-window-btn","magnification-chromatic-aberration":"open-magnification-chromatic-aberration-window-btn","integrated-aberration":"open-integrated-aberration-window-btn","transverse-aberration":"open-transverse-aberration-window-btn",opd:"open-opd-window-btn",psf:"open-psf-window-btn",mtf:"open-mtf-window-btn","through-focus-spot":"open-through-focus-spot-window-btn","through-focus-mtf":"open-through-focus-mtf-window-btn","field-mtf":"open-field-mtf-window-btn"}[n];if(r){try{typeof window.setupAnalysisWindows=="function"&&window.setupAnalysisWindows()}catch{}const s=document.getElementById(r);s&&s.click()}t.value=""});document.addEventListener("keydown",e=>{const t=document.activeElement,n=t&&(t.tagName==="INPUT"||t.tagName==="TEXTAREA"||t.isContentEditable);(e.ctrlKey||e.metaKey)&&e.key==="z"&&!e.shiftKey&&!n&&(e.preventDefault(),window.undoHistory&&window.undoHistory.undo()),!n&&((e.ctrlKey||e.metaKey)&&e.key==="y"||(e.ctrlKey||e.metaKey)&&e.shiftKey&&e.key==="z")&&(e.preventDefault(),window.undoHistory&&window.undoHistory.redo())});function mp(e){window.undoHistory&&window.undoHistory.clear()}window.clearUndoHistoryOnMajorChange=mp;export{rp as drawOpticalSystemSurfaceWrapper,sp as drawOptimizedRaysFromObjects,wp as getWASMSystem,ip as improvedDrawOpticalSystemSurfaceWrapper,ns as initializeApplication};
